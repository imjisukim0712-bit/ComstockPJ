# 컴스톡(Comstock) 프로젝트 안내

이 문서는 사용자와 코딩 에이전트가 프로젝트를 빠르게 파악할 수 있도록 작성되었습니다.
프로젝트의 구조나 방향성이 바뀔 때마다 최신 상태로 업데이트합니다.

> 작업 이력은 [작업.md](작업.md)에 있습니다(문서 끝부분이 최신).

## 현재 진행 상황 요약

> 이 섹션은 append-only가 아니므로 항상 최신 상태로 덮어써서 갱신합니다.
> 여기만 읽으면 작업.md 전체를 읽지 않아도 현재 상황을 알 수 있게 유지합니다.

- **최근 완료**: Unity-MCP 플러그인 설치 및 연결, 프로젝트 문서 체계 수립
  (프로젝트 안내.md / 작업.md / CLAUDE.md / AGENTS.md)
- **진행 중**: 없음
- **다음 후보 작업**: 아래 [알려진 이슈](#알려진-이슈) 처리, 미구현 목표 기능
  (웨이브 시스템, 로봇 파츠 교체) 착수
- **미커밋 변경사항 있음** — Unity-MCP 설치 파일들과 문서 4종이 아직 커밋되지 않음

## 게임 개요

- **게임 제목**: 컴스톡
- **장르**: 웨이브 서바이벌, 뱀서라이크(뱀파이어 서바이벌류), 로그라이크, 모딩
- **핵심 컨셉**: 플레이어는 로봇을 조작하며, 로봇의 파츠(무기 등)를 교체하고 아이템을
  갈아끼우면서 좀비들을 처치해 나가는 게임

## 협업 / 코딩 에이전트 기본 규칙

1. 사용자의 요구사항이 명확하지 않다고 판단되는 경우, 임의로 진행하지 말고 요구사항을
   구체화하기 위한 질문을 한다.
2. 사용자가 수정을 요청하지 않고 단순히 질문하거나 의견을 묻는 경우에는 임의로 코드나
   파일을 수정하지 않는다.
3. 모든 수정사항과 작업 계획은 즉시 [작업.md](작업.md) 파일에 기재한다.
4. [작업.md](작업.md)에 이전에 기재된 내용은 지우거나 수정하지 않는다. 새 내용은 항상
   문서 맨 아래에 이어서 추가한다(append-only).
5. 모든 문서/커밋 메시지/커뮤니케이션은 한글로 작성한다.
6. 되돌리기 어려운 작업(파일/에셋 영구 삭제, git 강제 push, 히스토리 재작성, 프로젝트
   설정 변경 등)은 실행 전 반드시 사용자에게 확인한다.
7. 코드 변경 후에는 가능한 범위에서 실제 동작(플레이 테스트, 빌드, MCP 도구를 통한
   씬/게임오브젝트 확인 등)을 검증하고, 검증 결과를 작업.md에 함께 기록한다.
8. 이 문서(프로젝트 안내.md)는 프로젝트 구조·시스템·방향성이 바뀔 때 주기적으로 정리하여
   최신 내용으로 유지한다(불필요하게 길어지지 않도록 오래된 세부사항은 요약/정리).

## 구현 현황

**목표 장르와 실제 구현 상태가 다릅니다. 아래 구분을 반드시 확인하고, 미구현 기능을
이미 존재하는 것으로 전제하지 마세요.**

### 구현 완료

- 플레이어 로봇 이동 (Rigidbody 기반, WASD)
- 무기/사격 시스템 (2슬롯 듀얼웨폰, 마우스 조준, 탄약·재장전, 크리티컬, 투사체)
- 적 유닛 및 자동 스폰 (추적·근접공격, boid식 분리, 일정 간격 링 스폰)
- 아이템 드랍 및 획득 (가중치 드랍 테이블, 필드 픽업, 개수 기록)
- 게임 데이터 로드 (Google Sheets CSV → 몬스터/로봇/무기/방어구/드랍 테이블)
- HUD (HP 표시, 게임오버 패널), 카메라 추적

### 미구현 (목표)

- **웨이브 시스템** — 현재 `EnemySpawner`는 `spawnInterval`(기본 3초) 간격으로 무한
  스폰하고 `maxAliveEnemies`(기본 15)로 동시 생존 수만 제한합니다. 웨이브 구분, 난이도
  상승, 웨이브 클리어 개념이 없습니다.
- **로봇 파츠 교체 / 장비 장착** — `PlayerInventory`는 item_id별 보유 개수만 기록하며
  장착 로직과 인벤토리 UI가 없습니다. 게임의 핵심 컨셉이지만 아직 미착수 상태입니다.
- **로그라이크 요소** — 런(run) 기반 진행, 런 중 강화 선택, 영구 성장 요소 없음
- **모딩 지원** — 관련 구조 없음
- **로봇 선택 화면** — `RobotSelectManager` 스크립트는 있으나 어떤 씬에도 배치되어 있지
  않습니다([알려진 이슈](#알려진-이슈) 참고)

## 실행 및 테스트 방법

- **진입 씬**: `Assets/Scenes/Ground01.unity` — `GameDataManager`가 이 씬에만 배치되어
  있는 실제 플레이 씬입니다. `SampleScene.unity`는 Unity 기본 템플릿 씬입니다
  (빌드 세팅에는 SampleScene, Ground01 두 개가 등록되어 있습니다).
- **네트워크 연결 필수** — 아래 주의사항 참고.
- **MCP 도구 사용 조건**: Unity Editor가 이 프로젝트로 실행 중이어야 합니다.
  도구가 응답하지 않으면 아래 명령으로 연결 상태를 확인하세요.

```bash
unity-mcp-cli status "Y:\Unity\ComstockPJ"
```

## 주의사항 (디버깅 시 가장 먼저 확인할 것)

**게임 데이터 전체가 Google Sheets에 실시간으로 의존합니다.**

- `GameDataManager`가 시작 시 5개의 공개 Google Sheets CSV(몬스터/로봇/무기/방어구/드랍)를
  `UnityWebRequest`로 다운로드합니다.
- **네트워크가 없거나 시트 URL이 바뀌면 모든 데이터 테이블이 비어 게임이 정상 동작하지
  않습니다.** 스폰이 안 되거나 스탯이 0으로 나오는 증상이 있으면 이것부터 확인하세요.
- CSV URL은 `GameDataManager.cs:13-17`에 `[SerializeField]`로 선언되어 있습니다.
  **즉 실제 사용되는 값은 씬에 직렬화된 값이며, 코드의 기본값과 다를 수 있습니다.**
  현재는 씬 값과 코드 기본값이 일치하는 것을 확인했습니다.
- 데이터는 비동기 로드되므로, 데이터에 접근하는 코드는 `GameDataManager.Instance.IsLoaded`를
  확인하거나 `OnLoaded` 이벤트를 구독하는 기존 패턴을 따라야 합니다
  (`EnemySpawner.cs:52-57` 참고).

## 알려진 이슈

- **`RobotSelectManager`가 어떤 씬에도 배치되어 있지 않음** — 로봇 선택 화면이 스크립트만
  존재하고 씬에 연결되지 않은 상태입니다.
- **`RobotSelectManager.cs:26`의 `nextSceneName` 기본값이 `"SamplePlay00"`인데 해당 씬이
  존재하지 않음** — 프로젝트에 있는 씬은 `Ground01`, `SampleScene` 둘뿐입니다. 이 상태로
  스크립트를 씬에 배치해 실행하면 씬 로드에 실패합니다.
- **Input System 에셋 미사용** — `Assets/InputSystem_Actions.inputactions`가 존재하지만
  어떤 스크립트도 참조하지 않습니다. 이동/사격 모두 `Keyboard.current` / `Mouse.current`를
  직접 폴링하는 저수준 방식입니다. 키 리바인딩이나 게임패드 지원이 필요해지면 정리가
  필요합니다.

## 기술 스택

- Unity **6000.3.19f1**
- URP(Universal Render Pipeline) 17.3.0
- Input System 1.19.0 (설치되어 있으나 실질 미사용 — 위 알려진 이슈 참고)
- **Unity-MCP** (IvanMurzak/Unity-MCP) 플러그인 설치됨 — Claude Code 등 AI 에이전트가
  Unity Editor(씬, GameObject, 에셋, 스크립트 등)를 MCP 도구로 직접 조작 가능
  (`.mcp.json`, `.claude/skills/` 참고)

## 프로젝트 구조 (Assets/ 하위)

- `Scripts/` — 게임 코드 전체 (현재 17개 파일, 하위 폴더/네임스페이스 없이 flat 구조)
- `Prefebs/` — 프리팹 (`Bullets`, `Energy`, `Zombie`). **폴더명이 "Prefabs"가 아닌
  "Prefebs"인 것은 오타이지만, 코드 내 `Resources.Load` 경로 등에서 이미 이 이름으로
  고정되어 쓰이고 있으므로 임의로 이름을 바꾸지 말 것.**
- `Scenes/` — `Ground01.unity`(플레이 씬), `SampleScene.unity`(기본 템플릿)
- `Resources/` — 런타임 로드용 스프라이트(무기 손모양, 적, 아이템 아이콘 등)
- `Animations/` — 애니메이션 클립, `PlayerAnimator.controller`
- `Settings/` — URP 파이프라인 에셋(Mobile/PC 변형 포함)

## 핵심 시스템

| 영역 | 주요 스크립트 | 설명 |
|---|---|---|
| 데이터 레이어 | `GameDataManager`, `GameDataModels`, `CsvParser` | ScriptableObject 대신 Google Sheets CSV를 런타임에 다운로드하여 각종 테이블 구성 (위 주의사항 참고) |
| 캐릭터 이동 | `PlayerRobotController` | Rigidbody 기반, WASD 이동, HP/스탯은 `RobotData`로 초기화 |
| 전투/무기 | `PlayerShootManager`, `Projectile` | 2슬롯 듀얼웨폰, 마우스 조준, 탄약/재장전, 크리티컬 계산, 그레네이드형 무기(딜레이드 블라스트) 지원 |
| 적 AI | `EnemyUnit`, `EnemySpawner` | 추적+근접공격, boid식 분리 로직, 링 스폰 방식 (웨이브 개념 없음) |
| 인벤토리/드랍 | `PlayerInventory`, `DropTableManager`, `DropItemManager`, `ItemPickup` | 정적(static) 클래스 기반 패턴. 장착 로직은 아직 없음 |
| 전역 상태 | `PlayerSession`, `GameOverManager` | MonoBehaviour 싱글톤 대신 static class 패턴을 일관되게 사용 |
| UI | `GameHUD`, `RobotSelectManager` | HP 폴링 표시, 로봇 선택 화면(씬 미배치) |

## 코드 스타일 관찰

- 네임스페이스 없이 전역 클래스 사용 (규모가 커지면 정리 필요)
- 주석/로그 문자열은 한글, 식별자는 영문 camelCase/snake_case 혼용
- 정적 클래스를 싱글톤처럼 사용하는 패턴이 반복적으로 나타남 (`PlayerInventory`,
  `GameOverManager`, `DropTableManager`, `PlayerSession`)
