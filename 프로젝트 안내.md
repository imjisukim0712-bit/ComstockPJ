# 컴스톡(Comstock) 프로젝트 안내

이 문서는 사용자와 코딩 에이전트가 프로젝트를 빠르게 파악할 수 있도록 작성되었습니다.
프로젝트의 구조나 방향성이 바뀔 때마다 최신 상태로 업데이트합니다.

> 작업 이력은 [작업.md](작업.md)에 있습니다(문서 끝부분이 최신).

## 현재 진행 상황 요약

> 이 섹션은 append-only가 아니므로 항상 최신 상태로 덮어써서 갱신합니다.
> 여기만 읽으면 작업.md 전체를 읽지 않아도 현재 상황을 알 수 있게 유지합니다.

- **다국어(한국어/영어) + 언어 선택 기능 완료** (2026-08-25) — **게임 전체가 번역된다**(UI 문구 +
  무기·파츠·디스크·로봇 등 아이템 데이터 이름/설명까지). 구조는 Unity Localization 패키지가 아니라
  **`Assets/Resources/Localization/strings_<코드>.json` + static `Loc`** 이다(이 프로젝트 UI는 대부분
  코드 생성이라 패키지의 이점을 못 쓰고 Addressables 의존성만 붙는다). **언어 추가 비용은 "JSON 파일
  1개 + `Loc.Supported`에 한 줄"이 전부이며, 언어별 `if` 분기를 코드 어디에도 만들지 않았다.**
  언어 선택은 **타이틀 우하단 "언어" 버튼**(사용자 확정)이고 선택값은 PlayerPrefs에 저장된다.
  **폰트는 Orbitron(사용자 제공, 라틴 전용 207자)이 기본이고 한글은 NotoSansKR 전역 폴백이 받는다** —
  영문·숫자는 Orbitron, 한글은 Noto로 글자 단위로 갈린다. 씬 텍스트 61개는 `LiberationSans SDF`를
  명시적으로 물고 있어서 TMP 기본 폰트만 바꿔서는 안 바뀌므로 전부 교체했다. 아래
  "다국어(로컬라이제이션)" 섹션과 [작업.md](작업.md) 마지막 두 항목 참고
- **"UI 제작 규칙" 신설 + `ItemCellUI` 캡션이 테두리와 겹치던 문제 수정** (2026-08-25) —
  아래 "UI 제작 규칙" 섹션 참고. 정비·상점 화면의 모든 파츠/무기/디스크 칸 캡션(슬롯 이름표)
  앵커가 좌우 6%·위 3% 여백만 둬서 `UI/Black_ui03`의 실제 테두리(약 22.6%)와 겹치고 있었다 -
  `ItemCellUI.GetSafeContentInset()`을 신설해 실제 아트의 `Sprite.border`에서 안전 여백을
  역산하도록 고쳤다. **씬의 `WaveText`/`WaveTimeText` 위치를 서로 바꾼 실험도 진행 중** -
  플레이모드 확인만 했고 씬 저장은 아직 안 함(사용자 확정 대기). 상세는
  [작업.md](작업.md) 마지막 항목 참고
- **상단 웨이브 HUD 문구를 "WAVE [N]" → "웨이브 N"으로 변경** (2026-08-25) — `GameHUD.cs`의
  전투 중 상시 HUD 표시 문구. 상점 화면 안의 `WAVE 01 / 20`(`ShopPanelUI` 소관, 별개)은
  대상이 아니라 그대로 둔다. 상세는 [작업.md](작업.md) 마지막 항목 참고
- **UI 폴리싱 - 등급 칸 테두리 근본 수정 + 드랍 아이템 1.5배** (2026-08-25) — 사용자가 화면
  캡처+빨간표시 PDF로 지적한 것 중 "등급 표시 사각형 테두리가 잘못됨"의 실제 원인을 찾아
  고쳤다. **원인**: `ItemCellUI`의 등급색 링이 실제 프레임 아트(`UI/Black_ui03`, 둥근 모서리·
  9-slice border 30px)와 무관하게 **64px 캔버스에 6px 사각 링을 코드로 임의 생성**한
  `UiIconLibrary.Frame()`을 써서, 등급색이 항상 얇고 각진 선으로만 보이고 실제 카드 베젤과
  안 맞았다. **수정**: 기본 프레임 스프라이트의 **실제 알파 실루엣 + 9-slice border 두께를
  그대로 읽어** 베젤 모양(둥근 모서리 포함)을 따라가는 링을 만드는
  `UiIconLibrary.DeriveEdgeRing()`을 신설해 `ItemCellUI.CreateShell`에 연결했다(원본과
  완전히 같은 크기·border 메타데이터로 만들어 9-slice가 1:1로 겹치는 원리). **임시방편이다** -
  사용자가 나중에 등급별로 실제 색이 다른 UI 리소스를 올리면 이 틴트 방식 자체가 필요
  없어질 수 있다. **PDF의 나머지 빨간 표시(상점/WAVE 등 좌측 글자가 잘린 것처럼 보였던
  부분)는 실제 버그가 아니었다** - 사용자 확인: "빨간 표시는 위치만 가리키는 것"(포인터일
  뿐 실제 클리핑 아님). 그 외 **드랍 아이템(경험치/골드/부품상자) 크기를 1.5배**
  (`RewardPickupManager.ReferenceIconWorldSize` 0.3 → 0.45)로 키웠다. 상세는
  [작업.md](작업.md) "UI 폴리싱 PDF 지적" 항목 참고
- **TC/폴리싱 20건 일괄 반영 완료** (2026-08-25) — 사용자가 전달한 TC·폴리싱 목록을 전부 처리했다.
  **신규 규칙 2개가 생겼으니 앞으로 이 관례를 따를 것**: (1) **화면에 보이는 숫자는
  `StatFormat`을 통해 찍는다** — 체력·공격력·방어력·행운·데미지는 정수, 이동속도·질량·무게는
  소수, 치명타 확률·회피율은 `N%`, 치명타 피해는 비율(0.5)이라 ×100 해서 `50%`. **표시 전용이며
  실제 계산과 아이템 설명 문구의 수치는 반올림하지 않는다**(사용자 지정). (2) **머리별 최고
  기록은 `HeadRecords`(PlayerPrefs)** 에 런 종료 지점(`TriggerGameOver`/`TriggerWin`)에서 남기고
  머리 선택 화면이 읽는다. 그 외 주요 변경: 상점에 **무기 위치 교체 버튼**(소켓 파츠는 그대로
  두고 무기만 맞바꿈) / **디스크 중복 구매 허용**(카드가 잠기지 않는다) / "포근한 치유" 디스크가
  **경험치 획득 시 회복**(`OnKillHeal` → `OnExpGainHeal`, enum 순서는 유지) / 드랍 아이템 크기
  1/2 / **부품 상자 최소 보장**(2웨이브 미드랍이면 확률 12배 + 웨이브 종료 시 확정 1개) /
  **상점 고등급 확률 1/3**(일반·레어 가중치를 3.632배로 올려 총합 300 — 고등급 값을 깎으면
  총합도 줄어 1/3이 안 된다) / **신규 몬스터 해금 웨이브 등장 상한**(4마리부터 +2/웨이브) /
  AI 코어 카드 화면 반응형 보정(이 패널만 고정 픽셀이라 창모드에서 잘렸다) / 옛 우하단 대시
  아이콘(`DashSkill`) 비활성 + `combatHudObjects`에서 제거 / 상점의 음악 볼륨 슬라이더 삭제 /
  데미지 숫자를 캔버스 첫 자식으로 내리고 일시정지에도 정리 / **근접무기에 사거리 보너스 적용**
  (찌르는 거리가 곧 사거리인데 배율이 빠져 있었다) / 보스 사망 연출 중 체력바·별 정리 /
  **자폭 좀비는 체력이 임계치 밑으로 내려가면 무조건 제자리에서 자폭**(맞기 전에 예측해 판정 -
  예전엔 한 방 30% 조건만 있어 약한 무기로는 그냥 죽었다). 상세는 [작업.md](작업.md) 마지막 항목
- **검증용 임시 오브젝트는 `Camera.allCameras` + 씬 루트 순회로 정리할 것** (2026-08-25에 겪은
  함정, 실제로 "게임이 회색 화면" 리포트로 이어졌다) — 아트 비교 캡처용으로 만든 카메라
  (`depth 100`, `clearFlags=SolidColor`)가 씬에 남아 **Main Camera보다 나중에 그려지며 화면을 단색
  으로 덮어**, 월드는 안 보이고 오버레이 UI만 보이는 회색 화면이 됐다. **`HideFlags.DontSaveInEditor`가
  붙은 오브젝트는 `Object.FindObjectsByType`이 돌려주지 않아** 정리 스크립트가 "0개 남음"이라고
  거짓 보고했고, 씬 파일에 저장되지 않는 플래그라 `git diff`로도 안 잡혔다(에디터 인메모리 씬에만
  존재). 또 **`screenshot-camera`는 지정 카메라만 RenderTexture로 렌더해 덮개 카메라와 오버레이
  UI를 둘 다 건너뛰므로** 그 캡처로는 정상으로 보인다 — 화면 확인은 실제 합성 결과인
  `screenshot-game-view`를 기준으로 볼 것. 임시 카메라는 depth를 Main Camera보다 낮게 두거나
  캡처 직후 즉시 제거한다
- **AI 코어 카드 화면은 1080p 설계를 균등 축소한다**(2026-08-25 사용자 확정: "원래 1080p에서는
  동일하게 보이는걸 기준으로 만들어야지") — 씬의 설계 좌표(카드 460x320, 중앙 기준 ±750px)를 그대로
  두고 `DesignRoot_1080p`(1920x1080 래퍼)에 담아 `localScale = min(캔버스폭/1920, 캔버스높이/1080)`
  만 준다. 1080p에서는 배율이 1이라 기존과 픽셀 단위로 동일하고, 작은 창에서는 통째로 비례 축소된다.
  **정규화 앵커로 재배치하면 창 크기마다 비율·간격이 달라져 1080p 모습과 어긋난다**(한 번 그렇게
  했다가 되돌렸다). 리롤 버튼·골드 표시는 원래부터 정규화 앵커라 래퍼 밖에 둔다
- **UI 겹침 검증은 정비 진입 절차를 타야 한다** (2026-08-25에 겪은 함정) — 점수 HUD가 정비 화면에서
  겹친다고 보고했다가 정정한 건이다. `ScoreHudUI`·`DashGaugeUI`·`PlayerHitFeedback`·`DamageNumberUI`는
  **`GameFlowManager.IsIntermission`을 스스로 보고 숨는** 코드 생성 HUD인데, `IsIntermission`을 켜는
  곳은 `EnterPostWaveIntermission` 하나뿐이다. 검증할 때 `ShowAiCoreUpgradeStep()`/`ShowShop()`을
  리플렉션으로 직접 부르면 이 플래그가 꺼진 채라 HUD가 전투 중처럼 계속 떠 있어 **실제로는 없는
  겹침이 보인다**. 반드시 `HandleWaveEnded`부터 타서 확인할 것
- **"적이 공격할 때 갑자기 작아지는" 문제 수정 완료** (2026-08-24) — **캔버스 픽셀 수가 같아도
  캔버스 안에 그려진 몸 크기는 다를 수 있다**는 함정이었다. 특히 보스 돌진 세트(`BossCharge`)는
  돌진 궤적까지 담은 광각 구도라 보스가 기준 크기의 **42%** 로 그려져 있어, 돌진할 때마다 보스가
  절반 이하로 쪼그라들었다. 알파 실루엣 겹침(IoU)으로 배율을 실측해 **PPU로 맞췄다** — 보스
  돌진 42 / 포효 112 / 그로기 105, 차저 돌진준비 73(각 세트에서 가장 오래 보이는 세트 =
  `BossMove`/`ChargerMove`가 기준). PPU를 낮추면 캔버스가 커져 그림의 치우침까지 확대되므로
  **pivot도 함께 옮겼다**(가로=보스 보라색 자루 무게중심, 세로=발끝 중앙값). `BossCharge`만
  그림이 캔버스 안에서 4유닛 넘게 움직여서 **프레임마다 pivot을 따로** 줘 제자리에 고정했다
  (실제 돌진 이동은 Rigidbody 담당). 차저 프리팹의 정지 스프라이트도 걷기 세트보다 38% 작은 옛
  `Charger.png`였어서(공격 직후 한 프레임 반짝 작아지고 체력바도 좁았다) 다른 몬스터와 같은
  관례로 `ChargerMove/f01`로 교체했다. **크기 기준은 `MonsterSizeClass`의 규격 픽셀 수가 아니라
  "그 몬스터가 평소 보여주는 세트의 실제 그림 크기"로 확정했다**(2026-08-24 사용자 확정 -
  "평소 보이는 크기에 맞추면 돼", 너무 작아지면 안 되므로). 그래서 차저 걷기 세트가 대형
  규격(450px)을 벗어난 640px인 것은 **그대로 둔다** — 규격 표는 신규 몬스터의 최초 크기를
  정할 때만 쓰고, 이미 아트가 있는 몬스터는 평소 세트를 기준으로 IoU 실측 정합한다.
  **`SpriteRenderer.bounds`는 캔버스 전체 크기이므로
  몸집 기준값을 재생 중인 프레임에서 재면 안 된다** — `EnemyUnit.BodyVisualWidth`/
  `BodyVisualHalfHeight`(프리팹 스프라이트에서 1회 측정)를 신설해 좀비 피격 이펙트 반경과 보스
  그로기 별 높이가 이 값을 쓰게 했다. **남은 것: 차저 걷기 세트 자체가 대형 규격(450px)을 벗어난
  640px이라 차저 전체가 약 1.4배 크게 보인다** — 이번엔 "공격 중 크기 변화"만 고치는 요청이라
  평소 크기를 기준으로 맞췄고, 규격에 맞춰 줄일지는 사용자 판단이 필요하다.
  상세는 [작업.md](작업.md) 마지막 항목 참고
- **보스 "좀비 군집체" 전면 재구현 완료** (2026-08-23) — 보스몬스터기획서 Ver01 +
  보스연출가이드라인 Ver01을 실제 코드로 반영했다. 그 전까지는 "쿨다운마다 원형 광역 공격 1종 →
  시간 기반 자동 그로기"인 단일 패턴이었고, 지금은 **페이즈 2단계 + 공격 패턴 3종(돌진 박치기 /
  잔해 낙하 / 페이즈2 전용 좀비 소환) + 피해 누적형 그로기 게이지 + 등장·사망 연출**을 갖췄다.
  구조는 **행동 하나 = 코루틴 하나**(`BossAction` enum + `action_routine` 한 자리)라
  그로기/페이즈 전환/사망이 `CancelAction()` 한 번으로 진행 중인 무엇이든 확실히 끊는다.
  신규 아트 8세트(돌진 120 / 소환진 99 / 사망 폭발 60 / 포효 36 / 사망 16 / 잔해 낙하 18 /
  잔해 경고 16 / 돌진 경고 14)를 임포트했고, **몸통 세트는 원본이 오른쪽을 보고 있어 PNG를
  좌우 반전해서 넣었다**(프로젝트 관례: 아트는 왼쪽을 본다 - `EnemyUnit.LateUpdate`의 flipX).
  같은 이유로 기존 `BossGroggy`도 함께 반전해 "그로기 진입 시 보스가 반대편을 보던" 불일치를
  고쳤다. **바닥 이펙트(돌진 레인 / 잔해 경고 서클)는 `sortingOrder 0` + `z=-1.5`** 로 깐다 -
  배경(`map`)도 sortingOrder 0이고 z=-1이라, 정렬 순서는 같게 두고 z만 카메라 쪽으로 당겨야
  "배경 위 · 모든 캐릭터(1 이상) 아래"가 확정된다. `OnDefeated`는 이제 **사망 연출이 전부 끝난
  뒤**에 발행된다(즉시 발행하면 사망 애니메이션 중에 승리 화면이 뜬다). 보스 스탯은 아직
  단일 패턴 시절 실측값(체력 8000/공격력 25/방어력 5) 그대로라 **플레이모드 실측 재조정이
  남아 있다**. 상세는 [작업.md](작업.md) "보스 '좀비 군집체' 전면 재구현" 항목 참고
- **사용자 추가 이미지 다수 적용 + 보스 적용 계획서 작성** (2026-08-23) — 프로젝트 루트 한글
  폴더에 추가된 이미지를 대량 적용했다. **차저(200002)가 마지막까지 남아있던 "정지 스프라이트
  고정" 몬스터였는데 이번에 걷기/돌진/돌진준비 3종 애니메이션을 갖췄다**(`MonsterAnimationLibrary`
  200002 항목 + `ChargerUnit.ResolveMoveClip()`/`TryAttack()` 오버라이드). 그 외 신규 이펙트
  5종(폭발 `ExplosionEffect`/좀비 피격 `ZombieHitEffect`/구르기 땅먼지 `RollDustEffect`/레벨업
  `LevelUpEffect`/차저 돌진 경고 `ChargeWarningEffect`) 신설, 스피터 투사체·정밀화기(대물저격총/
  지정사수소총) 탄환을 전용 아트로 교체(정밀화기는 `PrecisionBullet.prefab` 신설 +
  `WeaponTableGenerator` tanhwan 변경 + 씬의 `PlayerShootManager.projectile_prefabs` 등록까지
  필요했다), 보스 그로기 프레임을 640px→**800px** 재적용(PPU 80→100 동반 변경으로 보이는 크기
  유지 - "800px 캔버스 = PPU 100 = 8유닛"이 이 프로젝트의 초대형 규격 원칙). **새 PNG를
  Resources에 추가할 때 기본 임포트는 `Texture2D`일 뿐 `Sprite`가 아니라서 `Resources.LoadAll
  <Sprite>`가 전부 빈 배열을 반환한다** - `textureType=Sprite`/`spriteImportMode=Single`로
  일괄 재설정해야 한다(자동화하는 AssetPostprocessor 없음, 매번 확인 필요). 보스(좀비 군집체)는
  기획서 2종(보스몬스터기획서/보스연출가이드라인 Ver01)까지 받았지만 사용자 지시로 **계획서만
  HTML 아티팩트로 작성**하고 코드는 미반영 - 현재 보스는 "광역공격 1종 반복 + 시간 기반 자동
  그로기"인데 기획서는 "페이즈 2단계 + 패턴 3종(돌진 박치기/잔해 낙하/좀비 소환) + 피해 누적형
  그로기 게이지"라 사실상 다른 보스다(체력 1800 vs 현재 12000 등 스탯도 전면 재조정 필요).
  상세는 [작업.md](작업.md) "사용자가 추가한 이미지 다수 적용 + 보스 적용 계획서 작성" 항목 참고
- **다리 4종(기본/거미/캐터필러/로켓) 마무리 완료** (2026-08-23) — 다른 PC에서 중단된 세션
  (`다리기획서 Ver02` 반영, 커밋 `a.260821.02`에 구현 포함·기록만 누락)을 이어받아 마무리했다.
  사용자가 네 차례에 걸쳐 재지적한 **"캐터필러/로켓의 머리가 다리에 안 붙는다"** 의 최종 확정
  구성은 레퍼런스 이미지 기준으로: **머리가 다리 유닛보다 앞 레이어**(화염 10 < 하우징/허브 11 <
  앞트랙/노즐 12 < 머리 13 < 무기 14)이고, 깊이는 **시트 정렬** — 머리 밑단 타원 곡선이 다리
  **윗면 개구부 타원의 앞림**과 포개지는 높이다(개구부 뒷림은 머리 양옆으로 드러난다).
  구현은 두 단계다: (1) 원본 아트가 머리와 같은 250px 캔버스에 그려져 있어 2족용 고관절 앵커
  오프셋을 되돌려 캔버스를 포개고(`AltCanvasAlignY()`), (2) 완전 정합은 80px로 너무 깊어("아래쪽
  동그라미에 합쳐진" 모습) 개구부 앞림 실측값까지 올린다(캐터필러 +45px, 로켓 +32.5px -
  `TreadHeadSeatRaise`/`RocketHeadSeatRaise` const). 머리가 내려간 만큼 **무기 소켓도 동반
  하강**한다 — 소켓은 리그 밖 Player 직속이라 `HeadCanvasWorldOffsetY`(월드 단위)를
  `PlayerShootManager.ApplyHeadCanvasOffsetIfChanged()`가 읽어 구르기 원위치 캐시 기준으로
  옮긴다(근접 찌르기 연출이 pivot을 직접 움직이므로 **값이 바뀐 프레임에만** 적용).
  **좌우 정렬**은 두 가지를 고쳤다 — (1) 머리는 `facingSign>0`에서 뒤집히는데 다리 루트는
  `facingSign`을 그대로 스케일로 써서 **반전 부호가 정반대**였고 캐터필러/로켓은 생성 시점
  반전 적용도 없었다 → `ApplyAltVisualFacing()`이 머리 flipX와 같은 조건으로 함께 뒤집는다.
  (2) 캔버스 가로 실측(머리 몸통 중심 129.5 / 컵 131 / 허브 125)으로 컵·허브 중심을 머리
  몸통 중심에 맞추는 x 시트 오프셋(`TreadSeatXOffset` -1.5px / `RocketSeatXOffset` +4.5px,
  반전 시 부호 반전)을 넣었다 — "몸통과 다리의 몸통 보조부분이 일자로 딱".
  전 조합 왕복·이동·좌우 반전을 플레이모드 실측으로 검증했다. 누적 함정 8개(플레이어 사망 시
  다리 교체 사슬 끊김 / isPlaying 토글 상쇄 / 코드 기본값만으론 씬 인메모리 값 부활 /
  Renderer.bounds 금지 / ExitPlay 직후 assets-refresh 시 MCP 사망 / 스포너 컴포넌트 disable로는
  스폰 코루틴이 안 멈춤 / WaveManager를 오브젝트째 끄면 timeScale이 0에 머묾 / 활공 테스트 후
  컨트롤러를 안 되켜면 다리 장착이 화면에 반영 안 됨)는
  [작업.md](작업.md) 2026-08-22~23 항목 참고
- **좀비군집체 그로기(기절) 상태 신설 + 총기 이펙트 4종 + 드롭아이템 신규 아이콘 적용 완료**
  (2026-08-21) — 사용자가 추가한 리소스 3세트를 반영했다. (1) 보스(좀비군집체) 전용 **그로기
  (기절) 상태**를 신설 - 광역 공격 직후 3초간 이동/공격이 멈추고 피해를 1.5배 더 받으며,
  그로기 프레임(12장) + 머리 위 별 이펙트(6장)를 재생한다(트리거 조건은 명세가 없어 "큰 기술
  직후 무방비" 패턴으로 직접 설계 - 밸런스 미확정). (2) **총구 화염 이펙트** 신설 - 근접무기를
  제외한 모든 발사에 재생. (3) **투사체 이펙트 4종** - 기본탄환(6개 무기 공용)/동그란탄환
  (산탄 2종 전용, 신규 `Pellet` tanhwan)/레이저피스톨(2프레임 애니메이션)/플라즈마캐논
  (빔 2프레임 애니메이션)로 무기별 전용 시각을 갖췄다. (4) **드롭아이템**(경험치/골드/
  부품상자) 아이콘 교체 - 기존에 있던 "고정 월드 크기로 강제 재조정"하는 로직을 제거하고
  임포트 크기 그대로(스케일 1) 렌더링하도록 바꿔, 사용자가 의도한 상자:코인 비율(1.667배)이
  그대로 유지되게 했다. 상세는 [작업.md](작업.md) 마지막 항목 참고
- **최근 완료**: Phase 0~5(기반 정리 → 전투 루프 → 스탯/AI 코어 → 상점 → 로봇 모딩 →
  보스/데모 폴리시)에 이어 **Phase 6(템포·가시성 개선 + 정비 완전 정지 + 구르기) 완료**
  (2026-08-06). 컨셉기획서 Ver01(오승명)을 반영해 **20웨이브·웨이브당 60초**로 전환하고,
  적이 화면 밖에서 처치되던 문제(무기 사거리 15~20 vs 카메라 세로 가시 반경 8.66)를
  사거리 축소·화면 테두리 스폰으로 해결, 스폰 압력과 로봇/좀비 이동속도를 상향,
  정비 화면 진입 시 `Time.timeScale=0` + 필드 초기화 + 전투 HUD 숨김을 적용,
  **Space 구르기(무적 대시)** 를 신규 구현했다 (상세: [작업.md](작업.md) "Phase 6" 항목)
- **Phase 7 완료** (2026-08-06) — 사용자 피드백 8건 반영: 웨이브 종료 규칙 변경(제한시간
  종료 후 잔적을 전부 처치하고 2초 뒤 종료), **카메라를 직교로 전환**(원근 카메라 때문에
  배경 z=-1과 오브젝트 z=0 사이에 시차가 생겨 "아이템이 밀려 보이던" 문제 해결),
  웨이브 남은 시간 HUD 표시, 체력 바 위치 교정, 보상 드랍 확률 하향(경험치 1/2·골드 1/3·
  부품 상자 1.5%), 레벨업 필요 경험치 `50+20*레벨`, **구글 시트 연동 완전 제거**
- **Phase 8 완료** (2026-08-06) — 무기 방향 전환에 **회전 속도**(540도/초) 적용해 즉시 스냅하지
  않게 하고, 조준이 25도 안으로 들어와야 발사하도록 변경. 배경을 사용자 제공 이미지
  (`Assets/Resources/ground_ruined_city.png`, 폐허 도시 평지)로 교체하고 타일링 적용
- **Phase 9 완료** (2026-08-07) — **로봇 정비(모딩) 화면 전면 개편**. 부품 상자를 하나씩 여는
  방식을 폐기하고 정비 화면 진입 시 **전부 자동 개봉 → 임시 인벤토리 적재**로 바꿨다.
  교체는 인벤토리 파츠를 누르면 장착 가능한 슬롯이 **노란색으로 강조**되고 그 슬롯을 누르면
  **맞교환**되는 방식이며, 스탯 변화가 즉시 표시된다. 임시 인벤토리는 정비 완료 시 전부 사라진다.
  모딩 슬롯을 6개 → **8개**(헬멧·디스크 슬롯 신규)로 늘리고 머리를 조회 전용 칸으로 노출해
  화면에는 9칸이 보인다. **적재량**(머리 능력치)이 상자 보유 상한이자 인벤토리 크기이며
  인게임 HUD에 `5/20` 형태로 표시된다. 배경도 사용자 제공 새 이미지
  (`ground_ruined_city_v2.png`)로 교체
- **타이틀 화면 + 게임오버 요약 화면 완료** (2026-08-07) — 신규 `Title.unity` 씬(제목 +
  게임 시작/종료 버튼) 추가. **이제 진입 씬은 `Title.unity`다**(기존 `Ground01.unity`
  직접 실행 방식에서 전환). 게임오버 화면은 도달 웨이브·골드·장착 로봇 파츠 8부위·
  무기·디스크·최종 스탯을 전부 보여주고 "타이틀로" 버튼으로 복귀한다. 상세는
  [작업.md](작업.md) "타이틀 화면 + 게임오버 요약 화면" 항목
- **무기 시스템 전면 교체 완료** (2026-08-07) — 기존 무기 8종을 전부 삭제하고 **신규 13종 ×
  5등급 = 65행**으로 교체했다(중무장 5 / 경무장 5 / 근접 3). 관통 횟수·확률 관통·스플래시
  반경·방어력 %무시·지속 빔·넉백을 신규 구현했고, **대기시간이 발사 동작이 끝난 뒤부터
  흐르도록** 바꿔 3초짜리 빔이 겹치지 않게 했다. 디스크를 제외한 모든 파츠에 무게가 생겼다.
  상세는 [작업.md](작업.md) "무기 시스템 전면 교체" 항목
- **절차적(코드 기반) 관절 애니메이션 예시 완료** (2026-08-08) — 사용자가 제공한 파츠 4장
  (몸통/허벅지/정강이/신발)을 코드로 조립하고 관절 각도를 매 프레임 계산하는
  `ProceduralCharacterRig`를 만들었다. 애니메이션 클립을 쓰지 않고 **이동 속도에서 보행
  주파수를 뽑고 2관절 IK로 무릎 각도를 역산**하므로 속도가 변해도 발이 미끄러지지 않는다.
  사용자가 제공한 워크사이클 GIF를 픽셀 실측해 **몸통 대비 다리 비율(0.41)과 고관절 좌우
  간격 비율(0.53)** 을 맞췄다(`legScale`·`hipSeparation`). 길이 계열 파라미터는 전부
  **다리 길이에 대한 배수**라 파츠 크기를 바꿔도 보행이 저절로 따라온다.
  중간에 "무릎을 다리별로 미러링"하는 시도를 했다가(레퍼런스를 "고정된 좌우 대칭 자세"로
  오독), 사용자가 "다리 두 짝이 서로 반대 방향으로 꺾여있다·인간의 움직임과 유사해야
  한다"고 재지적해 **되돌렸다**. 실제로는 레퍼런스가 좌우 대칭 자세가 아니라 정상적인
  보행 위상(스윙/스탠스) 차이였다. **지금은 두 다리·두 발이 완전히 같은 규칙(같은 굽힘
  부호, 같은 그림 방향)을 쓰고, 실제로 보이는 차이는 오직 보행 위상에서 나온다.** 상세는
  작업.md "무릎 미러링을 되돌림(과잉수정)" 항목.
  확인용 데모 씬은 메뉴 `Comstock/절차적 관절 리그 데모 씬 생성`으로 만든다
  (`Assets/Scenes/JointRigDemo.unity`, 빌드 세팅에는 넣지 않음). 상세는
  [작업.md](작업.md) "절차적(코드 기반) 관절 애니메이션 예시" 및 뒤이은 여러 수정 항목
- **절차적 관절 리그를 실제 인게임 Player에 적용 완료** (2026-08-08) — `Ground01.unity`의
  `Player` 밑에 `ProceduralVisual` 자식을 만들어 `ProceduralCharacterRig`를 붙였다.
  기존 단일 스프라이트(`SpriteRenderer` "Stand_00") + `Animator`("PlayerAnimator")는
  **삭제하지 않고 `enabled=false`로만 비활성화**했다(되돌리려면 두 컴포넌트를 다시 켜고
  `ProceduralVisual`을 지우면 됨). `PlayerRobotController.cs`는 수정하지 않았다 — 새 리그가
  `Player`의 기존 `Rigidbody`를 자동으로 찾아 속도를 읽으므로 이동/구르기 코드가 그대로
  동작한다. **`ProceduralVisual.localScale = 4.15`**(기존 스프라이트의 world 크기 기준으로
  역산), **`bodySortingOrder = 13`**(배경 `map`=0·좀비=1보다 다리가 뒤에 그려져 안 보이던
  버그를 고치려고 데모 기본값 0에서 올림), 무기 스프라이트 정렬순서도 4→14로 함께 올렸다.
  상세는 [작업.md](작업.md) "실제 인게임 Player에 적용" 항목
- **무기 소켓(`RigingPoint`) 위치·크기 재조정 완료** (2026-08-08) — 새 리그 적용 후 무기가
  귀 옆이 아니라 발밑 근처(예전 거대 스프라이트 기준 고정좌표)에, 그것도 아주 작게
  나오던 문제를 고쳤다. **무기 이미지(`_img`)의 `localScale`은 `PlayerShootManager`가
  무기 장착 시마다 자동으로 덮어써서 직접 조정해도 소용없다** — 대신 부모인
  `RigingPoint`의 스케일(1.8)·위치(귀 옆, 몸통 반폭 근처)를 조정했다. 상세는
  [작업.md](작업.md) "무기 소켓 위치·크기 수정" 항목(원인 진단 삽질 과정 포함)
- **초반 웨이브 난이도 완화 완료** (2026-08-08) — 사용자 피드백("처음부터 쎈 좀비가 너무
  많다")에 따라, 1웨이브부터 좀비·차저(HP가 좀비의 2.5배)가 50:50으로 섞여 나오던 것을
  **차저는 4웨이브부터 등장**하도록 바꿨다(`EnemySpawner`에 `advancedMonsterIds`/
  `advancedUnlockWave` 신설, `WaveManager`가 매 웨이브 시작 시 `SetCurrentWave()`로 알려줌).
  초반 스폰압력도 완화(배치당 3→2마리, 간격 0.8→1.0초, 최대 동시생존 30→18마리). 웨이브가
  오를수록 강해지는 커브 자체는 유지. 상세는 [작업.md](작업.md) "초반 웨이브 난이도 완화" 항목
- **배경 타일 흰 줄/격자 수정 + 부품 상자 드랍률 하향 + 화면 밖 위치 화살표 UI 추가 완료**
  (2026-08-08) — (1) 배경 타일 경계의 흰 격자는 1차로 밉맵(mipmap)이 원인이라 껐지만,
  사용자가 잔여 흰 줄을 재지적해 조사한 결과 **원본 이미지 자체가 상하좌우 끝단 색이
  안 맞는(seamless가 아닌) 이미지**라는 더 근본적인 원인을 발견했다. 경계를 코드로
  블렌드하는 시도(폭 다르게 3회)는 흐린 선 잔존 또는 유령 이미지 부작용이 있어,
  사용자에게 해결 방식을 직접 물어 **"타일 반복 중단"** 을 선택받았다. **배경
  (`ground_ruined_city_v2.png`, `map` 오브젝트)은 이제 반복(Tiled)하지 않고 이미지
  1장을 맵 전체(240×140유닛)에 균등 스케일(6.4815배)로 펴 바르는 방식(`Simple`
  drawMode)** 이다 — 이음매가 원천적으로 사라지는 대신 확대율이 높아져 화질은 다소
  부드러워졌다(화질 보완을 위해 텍스처 `maxTextureSize`를 4096으로 상향). (2) 부품
  상자 드랍 확률을 1.5%→0.3%(1/5)로 낮춤(`PartsCatalog.asset`). (3) 화면 밖에 있는
  부품 상자 방향을 가리키는 화살표 UI 신설(`PartBoxIndicatorUI`) — 외부 에셋 없이
  코드로 삼각형 스프라이트를 생성하고, `RewardPickup.AlivePartBoxes` 정적 목록을
  순회해 화면 가장자리에 클램프해서 표시한다. 상세는 [작업.md](작업.md)의 해당 항목 참고
- **구르기(Space) 실제 회전 모션 + 구르는 동안 무기 동반·발사 잠금 완료** (2026-08-09) —
  기존에는 Space가 무적 대시일 뿐 시각적으로는 평소 걷기 애니메이션이 그대로 재생됐다
  (미구현 목표로 문서화돼 있던 항목). 이제 구르는 동안 **다리를 숨기고 몸통만 0→360도
  회전 + 살짝 뜨는 호핑**으로 실제 굴러가는 모션을 낸다(`ProceduralCharacterRig.SetRoll()`).
  양쪽 무기도 머리 위로 이동해 몸과 같은 각도로 함께 돌고, **구르는 동안에는 조준·발사가
  완전히 멈춘다**(`PlayerShootManager`가 `Update()`를 조기 반환 — 빠른 이동 대신 잠깐
  공격 불가라는 패널티). 구르기가 끝나면 다리·무기 위치·발사가 즉시 정상 복귀한다.
  상세는 [작업.md](작업.md) "구르기 실제 회전 모션" 항목
- **구르기 거리·속도 조정 + 좌우 회전 방향 버그 수정 완료** (2026-08-09) — 구르기 거리
  3.5→5.0유닛(+43%), 속도는 19.4→17.9유닛/초로 약간만 하향(지속시간 0.18→0.28초로
  늘어나 회전 모션도 더 잘 보인다). 방향 무관하게 항상 시계방향으로만 돌던 버그를 고쳐
  **오른쪽으로 구르면 시계방향, 왼쪽으로 구르면 반시계방향**으로 정확히 돌게 했다
  (`dashDirection.x` 부호로 회전 방향 결정). 상세는 [작업.md](작업.md) 해당 항목 참고
- **유닛 충돌 범위 재설계 + 좀비/차저 체력 상향 완료** (2026-08-09) — (1) 좀비·차저·보스
  콜라이더가 예전엔 머리 위부터 발밑 아래까지 감싸는 세로로 긴 직사각형(로컬
  `(8,10,0.2)`, 원점 중심)이었는데, 다리~아랫배 정도만 감싸도록 재설계했다(로컬
  `center=(0,-1.7,0)` `size=(8,6.6,0.2)`). **세 프리팹이 같은 로컬 콜라이더 값을 공유하고
  `localScale`만 다르므로, 이 값 하나만 맞추면 크기별로 자동 비례한다** — 앞으로 새
  몬스터를 추가할 때도 이 로컬 콜라이더 값을 그대로 쓰고 프리팹 스케일만 조정하는 것이
  정석 패턴. 플레이어 콜라이더(발밑 기준 `(3,3,0.01)`→`(3,1.7,0.01)`)도 실측(리그
  골반높이 1.41, 몸통 높이 2.94)해 같은 원칙으로 맞췄다. 이 콜라이더는 근접 공격 판정
  (`EnemyUnit.OnCollisionEnter`)에도 쓰이므로 겉보기뿐 아니라 실제 판정 정확도도
  개선됐다. (2) 일반 등급 무기 중 즉발 데미지가 가장 큰 대물저격총(120+로봇공격력3=123)
  기준으로 좀비 체력을 30→370(차저는 비율 유지 75→925)으로 올려, 1웨이브부터 어떤 일반
  등급 무기로도 3방 미만에 죽지 않게 했다(무기 화력은 무수정). 상세는 [작업.md](작업.md)
  해당 항목 참고
- **DPS 기준 좀비 리밸런싱 + 적 체력바 UI 추가 완료** (2026-08-09) — 직전 항목의 "burst
  3방" 기준을 "DPS 기준 무기 하나로 3초"로 교체: 일반 등급 무기 중 DPS가 가장 높은
  중기관총(초당 120뎀)을 기준으로 좀비 체력 30→**360**(정확히 12배, 차저는 비율 유지
  75→900). **체력만 올리면 `MaxHp` 비례였던 경험치/골드 보상도 그대로 12배 폭증하는
  부작용**이 있어, `EnemyUnit`의 보상 나눗셈 상수도 같은 배율(10→120, 20→240)로 맞춰
  기존과 동일한 보상량을 유지했다. **적 체력바**는 Canvas 없이 SpriteRenderer 2장으로
  `EnemyUnit.Awake()`가 직접 생성하고(`BossUnit`도 자동 상속), 본인 스프라이트의 월드
  bounds에서 크기를 계산해 좀비/차저/보스 스케일 차이에 자동으로 비례하며, 체력
  100%에서는 숨고 100% 미만부터 색(빨강↔초록)과 함께 표시된다. 상세는
  [작업.md](작업.md) 해당 항목 참고
- **좀비 체력 재조정 완료** (2026-08-09) — 직전 DPS 기준값(360)을 사용자 요청으로 절반인
  **180**으로(차저는 비율 유지 450), 보상 나눗셈 상수도 같이 6배(60/120)로 맞춰 보상량은
  그대로 유지했다. 중기관총 DPS 기준 정확히 1.5초에 사망(직전 3초의 절반). 상세는
  [작업.md](작업.md) 해당 항목 참고
- **템포·가시성·난이도 일괄 개선 완료** (2026-08-10) — 사용자 지적 7건 반영.
  (1) **화면 밖 부품 상자 화살표가 안 보이던 문제 수정** — 로직은 정상이었고, Canvas가
  `ConstantPixelSize`인데 크기가 절대 픽셀(36px)로 박혀 있어 4K 화면에서 화면 폭의
  0.9%짜리 점이 된 것이 원인이었다. **해상도 비례**로 바꾸고(기준 1080) 크기를 키웠으며
  (72px), **부품 상자 아이콘**을 함께 표시하고, 씬 오브젝트의 레이어를 Canvas와 맞췄다.
  (2) **웨이브는 제한시간이 끝나면 즉시 종료**(잔적 대기·2초 여유 폐지, 보스 웨이브만 예외).
  남은 적은 드랍 없이 소멸하고, 필드의 골드·경험치는 자동 수령되지만 **부품 상자는
  자석으로 얻을 수 없다**(직접 주워야 함 → 화살표 UI가 의미를 갖는다).
  (3) 좀비 체력 180 → **90**(차저 225), 보상 나눗셈 상수도 절반(30/60)으로 맞춰 보상량 유지.
  (4) **배경을 원본 스케일(1배)로 축소** — 6.4815배로 늘려 놓아 텍스처 1픽셀이 화면 15픽셀을
  덮던 상태였다. 맵이 248.9x140 → **38.4x21.6유닛**이 되면서 **맵 경계**(`MapBounds`)를
  신설해 플레이어·카메라를 맵 안으로 가둔다.
  (5) **피격 시 0.25초 흰색** 연출 신설(전용 셰이더 + `HitFlash` 컴포넌트, 좀비/차저/보스/
  플레이어 자동 적용). (6) **좀비 겹침 판정을 몸 크기 기준으로 재설계** — 고정 1.3유닛에서
  각자 콜라이더로 실측한 반지름 합(좀비 0.886유닛)으로 바꿔 몸이 실제로 닿을 때만 밀어낸다.
  (7) 스프라이트 확대 배율 전수 조사 결과 **로봇 몸통만 1.141배 확대**이고 나머지는 전부 축소
  (미수정, 아래 "다음 후보 작업" 참고). 상세는 [작업.md](작업.md) 2026-08-10 항목
- **로봇 파츠가 엉뚱한 이미지로 그려지던 버그 수정 완료** (2026-08-10) — "머리에 신발,
  로봇이 좀비 이미지로 바뀌고, 첫 피격 이후에만 정상으로 돌아오던" 장기 미해결 버그의
  **근본 원인을 규명해 고쳤다.** 이전 여러 차례의 수정 시도는 전부 `SpriteRenderer.sprite`
  **필드**가 덮어써진다고 전제하고 매 프레임 복구 코드를 쌓았지만, **`sprite` 필드는 한 번도
  잘못된 적이 없었고 깨진 것은 텍스처 바인딩(그리는 과정)이었다.** 실제 원인은 `HitFlash`가
  피격 연출용으로 갈아끼우는 `Comstock/SpriteFlash` 셰이더가 **빌트인 렌더 파이프라인용
  스프라이트 셰이더**였다는 것 — 이 프로젝트는 **URP 2D Renderer**를 쓰는데, URP 2D는
  빌트인 RP 스프라이트 셰이더에 SpriteRenderer의 렌더러별 데이터(`_MainTex`)를 넘기지 않아
  직전에 바인딩된 남의 텍스처가 그려졌다. 첫 피격 때 `SetPropertyBlock()`이 호출되면 그
  데이터가 강제 업로드되어 정상화되는 것이 "피격 전까지만 깨진다"는 조건의 정체였다.
  **셰이더를 URP 2D판(`Sprite-Unlit-Default` 복제 + `_FlashAmount`)으로 재작성**해 해결했고,
  같은 `HitFlash`를 쓰는 **좀비 6종도 같은 버그였으며 함께 고쳐졌다**. Unity 기본
  `Sprites/Default`를 붙여도 동일하게 깨지는 것을 대조군으로 확인했다. **새 스프라이트용
  셰이더는 반드시 URP(HLSL)로 작성할 것.** 상세는 [작업.md](작업.md) 마지막 항목 참고
- **참고(디버깅 기법)**: `EditorApplication.isPaused = true`를 먼저 켠 뒤 `EnterPlaymode()`를
  호출하면 **1프레임에서 정지한 채로 플레이모드에 진입**한다. 위 버그처럼 "시작 직후 몇 초
  동안만 나타나는" 증상을 그대로 얼려놓고 검사·캡처할 수 있어, MCP 왕복 지연 때문에 놓치던
  초기 상태를 확실히 잡을 수 있다
- **로봇 파츠 크기 비율을 원본 이미지 기준으로 바로잡음 + 정강이를 신발 안으로 완료**
  (2026-08-10) — 사용자 지적("내가 만든 이미지 크기 비율대로 적용이 안 되어 있고 임의로
  비율이 수정되어 있다. **몸통의 외곽선과 다리의 외곽선 두께가 일치하는 비율**이어야 한다").
  원본 PNG의 외곽선 두께를 픽셀로 실측한 결과 **몸통 4.27px / 허벅지 9.06 / 정강이 7.42 /
  발 9.84**로 파츠마다 해상도가 달라 두께가 제각각이었다. 따라서 "원본 크기 비율대로"는
  전부 같은 배율이 아니라 **외곽선이 같아지는 파츠별 배율**을 뜻한다.
  `legScale` 하나(0.30)로 뭉뚱그리던 것을 **`thighScale`(0.471)/`shinScale`(0.576)/
  `footScale`(0.434)** 로 분리하고 `legScale`은 전체 배율(기본 1.0)로 의미를 바꿨다.
  결과적으로 화면상 외곽선이 몸통 4.87 / 허벅지 4.87 / 정강이 4.88 / 발 4.87로 **정확히
  일치**한다. `footAnkleAnchor`를 0.88 → **0.62**로 내려 정강이 끝이 신발 통에 파묻히는
  "신발을 신은" 모습을 만들었고, 다리가 커진 만큼 자세도 보정했다(`hipHeightRatio` 0.96 /
  `idleStanceRatio` 0.05 / `hipSeparation` 0.55 / `frontLegPullBack` 0.06).
  보행 실측 결과 보폭 감소는 1.7%뿐이고 발 들뜸도 없다. **이 값들은 코드 기본값과
  `Ground01.unity` 씬 직렬화 값 양쪽에 넣어야 한다**(씬 값이 우선). 상세는
  [작업.md](작업.md) 마지막 항목 참고
- **참고(스프라이트 크기 원칙)**: 적 6종·배경·플레이어 로봇 몸통이 전부 **배율 x0.5000**
  으로 통일돼 있다. 2026-08-10에 먼저 전부 x1.0000(PPU=100·스케일=1, 원본 크기)으로
  정규화한 뒤, 사용자 지시로 **일괄 1/2 축소**한 결과다. **카메라는 1유닛=100px
  (`orthographicSize=5.4`)** 이므로 화면에 찍히는 픽셀 수 = 원본 이미지 픽셀의 절반이다.
  다리는 "외곽선 두께를 몸통과 일치"시키는 파츠별 배율이 여기에 한 번 더 곱해지므로
  x0.5가 아닌 것이 **정상**이다. 무기는 `PlayerShootManager`가
  `TargetHandImageSize / max_dim`으로 정규화해 무기마다 배율이 다른 것이 **정상**이다
  (원본 픽셀 크기가 제각각인 무기를 화면에서 일정 크기로 보이게 하려는 의도)
- **좀비 교전 거리·밀집도 수정 + 로봇 밀어내기 신설 완료** (2026-08-10) — (1) **공격 사거리
  이중 계산 제거**: `AttackRange + BodyContactRadius()`로 더하던 것을 `Max(둘)`로 바꿨다
  (`EffectiveAttackRange()` 신설). `monster_range`는 이미 중심 기준 사거리인데 몸 반지름을
  더해서 좀비가 **3.350유닛**(실제 접촉거리의 2.7배)에서 때리고 있었다 → **1.600유닛**.
  `BodyContactRadius()`가 콜라이더 긴 축(Max)을 쓰던 것도 짧은 축(Min)으로 바로잡아
  `ComputeBodyRadius()`와 기준을 통일했다. (2) **좀비끼리 겹침 방지 간격을 1/3로**
  (`SeparationRadiusScale`, 1.600 → 0.533유닛). (3) **사거리 안에 들어오면 전진 정지**
  (`StopDistanceRatio=0.9`) — 예전엔 공격 쿨다운 동안 플레이어 몸속까지 파고들었다.
  (4) **로봇이 이동 중 겹친 적을 밀어낸다**(`PlayerRobotController.PushOverlappingEnemies()`
  → `EnemyUnit.ApplyPlayerPush()`). **물리 충돌만으로는 절대 안 밀린다** — 적이 매 프레임
  Rigidbody 속도를 통째로 덮어쓰기 때문에, 이동 속도에 더해질 별도 성분으로 주입해야 한다.
  상세는 [작업.md](작업.md) 마지막 항목 참고
- **카메라를 FHD 기준으로 정규화 + 로봇 배율 원본화 완료** (2026-08-10) — 맵이 작게 느껴지던
  원인(배경 PPU=100 vs 카메라 ÷50 환산 = 1유닛 62.4px)을 실제로 고쳤다. **카메라
  `orthographicSize` 8.660254 → 5.4**(= 1080÷2÷100)로 바꿔 화면이
  **19.188x10.800유닛 · 1유닛=100px**이 되었고, **맵 ÷ 화면 = 정확히 2.0배**(FHD 2개분)가
  됐다. 배경 도트도 1:1로 선명해졌다. 동시에 **로봇만 x1.14125로 확대**돼 있던 것을
  `Player.localScale` 0.5 → **0.43812**로 낮춰 **몸통 배율 x1.0000**(적·배경과 동일 기준)으로
  맞췄다 — 시각만이 아니라 콜라이더·무기 소켓까지 같은 비율로 따라오게 하려고
  `ProceduralVisual`이 아닌 **`Player`** 를 줄인 것이 요점이다(다리 파츠 배율은 손대지 않아도
  외곽선 두께 일치가 유지된다). **무기 감지 상한 `max_detect_range` 8.5 → 5.3**(새 화면 세로
  반경 5.4 안쪽)으로 낮췄고 **코드 기본값과 씬 직렬화 값 양쪽에** 넣었다. 화면 테두리 스폰·
  카메라 맵 클램프는 `orthographicSize`를 읽으므로 자동 반영됨을 실측 확인했다.
  상세는 [작업.md](작업.md) 마지막 항목 참고
- **무기 소켓 개별화 + 소켓 불일치 무게/이동속도 패널티 완료** (2026-08-12) — 이전 데스크톱 앱
  세션에서 plan mode로 확정해두고 미뤄뒀던 작업을 CLI 세션에서 이어받아 구현했다. 소켓마다
  독립된 무기 소켓 파츠(종류+등급)를 낄 수 있도록 `RunState.EquippedWeaponSocketPartIds`
  (소켓 인덱스 → partId)를 신설했고, 소켓 개수는 `ModdingManager.ActiveSocketCount`
  (= Min(씬 리깅 개수, 머리 파츠의 weaponSocketCount))로 동적화했다(지금은 둘 다 2라서 기존과
  동일하게 동작). **타입 불일치 무기도 항상 장착 가능**하게 바꾸고 대신 무게에 배율(기본 x2,
  `PartsCatalog.mismatchWeightMultiplier`)을 곱하며, **지탱력 초과도 더 이상 장착을 막지 않고**
  초과분만큼 `RobotStats.Compute()`에서 이동속도를 깎는다(`overweightSpeedPenaltyPerUnit`,
  기본 0.05/유닛). 상점·정비 화면은 소켓별로 여러 줄(무기 소켓 1~N)로 표시를 확장했다. 플레이모드
  실측으로 이동속도 감소량(3→2.45, 초과 11유닛 x 0.05)까지 정확히 일치 확인. 상세는
  [작업.md](작업.md) "2026-08-12 무기 소켓 개별화 + 소켓 불일치 무게/이동속도 패널티" 참고
- **스피터 공격력 절반 + 웨이브 종료 시 체력 전부 회복 완료** (2026-08-12) — 원거리 좀비(스피터,
  몬스터ID 200003)의 `monster_atk` 20→10(다른 스탯은 그대로). `GameFlowManager.HandleWaveEnded()`
  맨 앞에서 `PlayerRobotController.Heal(MaxHp)`를 호출해 웨이브를 넘길 때마다 체력이 전부
  회복되도록 변경(보스 웨이브는 이 이벤트 자체가 발생하지 않아 자동 제외). 상세는
  [작업.md](작업.md) "2026-08-12 스피터 공격력 절반 + 웨이브 종료 시 체력 전부 회복" 참고
- **PC 피격 판정이 여전히 넓던 문제의 진짜 원인 발견·수정 완료** (2026-08-12) — 앞서 플레이어
  `BoxCollider`를 줄였는데도 "투사체가 스치기만 해도 맞는다"는 리포트가 재발해 조사한 결과,
  실제 원인은 **스피터가 쏘는 `EnemyProjectile`의 콜라이더**였다. 코드로 생성하는 원형 스프라이트
  (반지름 0.16유닛)와 무관하게 `SphereCollider.radius`가 0.5로 하드코딩돼 있어 판정 반경이
  시각 크기보다 항상 **3.1배**(면적 약 9.8배) 컸다(스크립트 작성 시점부터 있던 버그).
  스프라이트 생성 상수에서 반지름을 직접 계산하도록 고쳐 시각 크기와 판정이 정확히 일치하게
  됐다(실측 비율 1.0). 상세는 [작업.md](작업.md) "2026-08-12 PC 피격 판정이 여전히 넓던 문제 -
  진짜 원인 발견(투사체 콜라이더)" 참고
- **돌진 직선화 + 스피터 공격속도 + 리더 도주 삭제 + 폭발 범위 표시 + 상점 장비 상세 UI 완료**
  (2026-08-12) — 사용자 요청 5건. (1) **스프린터/차저 돌진이 유도탄처럼 휘던 문제** 수정 —
  두 유닛 모두 돌진 중 매 물리 프레임 방향을 플레이어 쪽으로 다시 잡고 있었다(과거에 "모든
  좀비는 항상 플레이어를 향한다"는 공통 규칙을 돌진에도 적용한 결과). 이제 `ExecuteAttackEffect()`
  에서 정한 방향으로만 직진하며, 방향 고정의 부작용(피하면 화면 밖까지 밀고 나감 - 차저는 3초 x
  7.5 = 22.5유닛)을 막으려고 **돌진 최대 거리 = 시작 시점 플레이어까지 거리 + 오버슈트 2유닛**을
  함께 넣었다. (2) 스피터(200003) `monster_atsp` 0.8 → **0.4**(발사 간격 1.25초 → 2.5초).
  (3) **리더의 도주 기믹 삭제** — `is_fleeing`·`Update`·`ComputeSeekDirection` 오버라이드를 전부
  없애 무리 전멸과 무관하게 끝까지 싸운다(`SetPack`/무리 스폰 자체는 유지). (4) **폭발 무기의
  보이는 범위가 실제 판정보다 3.2배 넓던 버그** 수정 — `Projectile.Explode()`가
  `localScale = SplashRadius`로 크기를 잡았는데 투사체 스프라이트가 스케일 1에서 반지름 3.2유닛
  (640px/PPU100)이라 그만큼 부풀어 있었다. 이제 스프라이트 로컬 bounds에서 반지름을 읽어
  `SplashRadius / 반지름`으로 역산한다(실측 비율 1.0000). **"시각 크기와 판정 크기가 서로 다른
  상수를 쓰는" 계열의 세 번째 버그**다(앞선 두 건: `EnemyProjectile` 콜라이더, PC 콜라이더).
  (5) **상점에서 보유 장비를 클릭해 상세 능력치를 보는 UI** 신설 — 씬 수정 없이 **TMP 링크 태그 +
  클릭 릴레이**(`TextLinkClickRelay`)와 **코드로 생성하는 팝업**(`EquipmentDetailPopup`)으로
  구현했다(항목 수가 런타임에 변해서 씬에 버튼을 미리 깔 수 없다). 무기·무기소켓 파츠·일반 파츠·
  디스크·로봇 본체 5종을 지원하며 데이터 원본값이 아니라 **실제 적용값**(소켓 배율이 곱해진 실효
  사거리, 로봇 공격력이 분배된 1발 데미지, 무게 초과 시 이동속도 감소량 등)을 보여준다. 상세는
  [작업.md](작업.md) 마지막 항목 참고
- **UI 아트 전면 적용 + BGM 시스템 완료** (2026-08-13) — 사용자가 제공한 UI 아트 50장
  (`Assets/Resources/UI`)과 BGM 3곡(`Assets/Resources/Musics`)을 붙였다.
  (1) **UI 아트**: 50장 전부 `spriteBorder`를 넣어 **9-슬라이스**로 쓰도록 임포트 설정을
  일괄 수정했다(붙일 자리가 앵커 비율이라 해상도마다 늘어나므로 Simple로 쓰면 모서리가 뭉개진다).
  타이틀 / 전투 HUD / AI 코어 3택 / 로봇 정비 / 상점 / 게임오버 요약 / 장비 상세 팝업 전 화면에
  패널·버튼·바를 입혔고, HP는 `HP : ` 글자를 하트 아이콘으로 바꾸고 골드·레벨에 아이콘을 넣었다.
  **정비 칸은 "테두리 아트(흰색 고정) + 안쪽 상태색" 두 겹 구조로 바꿨다** — 한 겹이면 거의 검은
  테두리 스프라이트에 색이 곱해져 노란 슬롯 강조·파란 선택 강조가 전부 죽는다.
  (2) **BGM**: `MusicManager`(신설)가 **AudioSource 하나로만** 재생하고 씬에 배치하지 않은 채
  `RuntimeInitializeOnLoadMethod` + `DontDestroyOnLoad`로 스스로 살아남는다(중복 인스턴스는 즉시
  자살 → 곡 겹침 원천 차단). 타이틀 씬은 타이틀 곡 반복, 그 외 씬은 `Game_BGM*` 랜덤이며
  **다음 곡은 현재 곡을 후보에서 빼고 뽑는다**. 씬 전환은 0.5초 페이드(정비/상점의
  `timeScale=0`에서도 돌도록 `unscaledDeltaTime` 사용). 볼륨은 PlayerPrefs에 저장되고
  **타이틀 화면과 상점 화면 양쪽에 같은 슬라이더**(`MusicVolumeSliderUI`, 코드 생성)가 붙는다.
  (3) 작업 중 **정비 슬롯 칸이 화면을 열 때마다 납작해지던 기존 버그**를 발견해 고쳤다 —
  스크롤이 없는 슬롯 격자에도 `ContentSizeFitter`가 붙어 "컨테이너 높이 → 칸 크기 → 컨테이너
  높이" 되먹임이 생겨 rect 높이가 음수(-106)까지 내려갔다.
  검수는 전부 플레이모드 실측(타이틀↔인게임 2회 왕복 내내 AudioSource 1개, 현재 곡 제외 랜덤
  600회 추첨 검증, 곡 종료 시 자동 전환, 볼륨 저장, 6개 화면 FHD 캡처). 상세는
  [작업.md](작업.md) 마지막 항목 참고
- **남은 UI 아트 적용 + 구르기 쿨다운 아이콘 + 타이틀 무음 버그 수정 완료** (2026-08-13) —
  위 작업에서 미사용으로 남겼던 아트를 마저 붙였다. **`Dash_skill`로 구르기 쿨다운 아이콘**을
  신설하고(`PlayerRobotController.DashCooldownRatio` 신규 노출 → `GameHUD`가 덮개 `fillAmount`
  갱신, `combatHudObjects`에 등록해 정비/상점에서 함께 숨김), **체력 바가 40% 초과면 초록
  (`Green_bar00`) 이하면 빨강(`Red_bar00`)** 으로 바뀌게 했다. 그 외 `Heart_panel`/`Star_panel`
  (아이콘 뒤 실루엣), `Exp_icon`(AI 코어 화면 제목), `Purple_ui01`(정비 섹션 제목 배너),
  `Blue_barpanel01`(경험치 바 배경), `30_panel_bar_purple_border`(남은 시간), `Panel00`/
  `Black_ui04`/`Panel03`(상점 카드 헤더·아이콘 판, 게임오버 헤더)을 적용했다.
  **9-슬라이스 주의점**: 붙는 자리가 원본보다 훨씬 납작하면(체력 바 18px에 테두리 60px) 테두리가
  높이를 다 먹어 가운데가 사라진다 — 에셋 `spriteBorder`를 낮추는 것과 별개로
  **`Image.pixelsPerUnitMultiplier`(2~2.5)로 테두리가 그려지는 크기를 줄이는 것이 핵심**이다.
  또한 **`Title.unity`에 `AudioListener`가 아예 없어 타이틀 BGM이 재생만 되고 소리는 안 나던
  버그**를 발견해 카메라에 추가하고, `MusicManager.EnsureSingleAudioListener()` 안전망(0개면
  추가, 씬에 있으면 자기 것 제거)을 넣었다. 상세는 [작업.md](작업.md) 마지막 항목 참고
- **밸런스·시스템 조정 12건 완료** (2026-08-13) — 사용자 요청 9건 + 추가 3건. (1) **AI 코어
  업그레이드(레벨업 3택)에 등급 도입** — `Option.amount`가 이제 **일반 등급 기준값**이고 등급
  배율은 1 / 5⁄3 / 7⁄3 / 10⁄3 / 5다(기준값 0.3이면 0.3/0.5/0.7/1.0/1.5 - 사용자 예시와 일치).
  카드 문구는 에셋 고정 문자열을 버리고 **매번 생성**한다(등급마다 수치가 달라 고정 문구는 반드시
  어긋난다). 수치는 전부 기존의 1/3~1/4로 너프하고 **방어력만 1/6.7**로 더 깎았다.
  (2) **레벨업 빈도 약 3.5배** — 필요 경험치 `50+20×레벨` → **`4+2×레벨`**, 최대 레벨 20 → **70**
  (총 성장량은 비슷하고 성장 단위만 잘게 쪼갠 것). (3) 스피터 투사체 속도 6.75 → 5.0625.
  (4) **차저 650→180 / 리더 600→170 / 스프린터 120→100 / 디스럭터 150→110**(보스 제외)이며,
  "3라운드 진행 불가"의 실제 주범이던 **방어력**(차저·리더 10 → 3 등)도 함께 낮췄다 — 피해가
  `max(1, 데미지-방어력)`이라 방어력 10이 연사 무기의 DPS를 60~90 → 6~10으로 무력화했다.
  대신 **웨이브마다 체력·공격력 5% 상승**(`EnemySpawner.statIncreasePerWave`, 보스 제외).
  (5) **초반 5웨이브 완화 + 스폰량 점증** — 완충 구간(간격 1.6배에서 5웨이브에 걸쳐 복귀,
  최대 생존 증가폭 절반)과 배치 수 점증(5웨이브마다 +1)을 넣고, 상위 몬스터 해금을 6·7·8·10·12
  웨이브로 미뤄 **1~5웨이브는 기본 좀비만** 나온다. 이 과정에서 **난이도 곡선이 이중으로 누적되던
  버그**를 발견해 고쳤다(`Base*` 프로퍼티가 조정 대상 필드를 그대로 돌려줘서 웨이브 10의 최대
  생존이 54가 아니라 **198마리**, 웨이브 20은 778마리였다 → `Awake`에서 원본 값을 한 번만 찍는다).
  (6) 골드 평균 +25%(골드량이 아니라 **드랍 확률**에 1.25배 - 정수 반올림 때문에 금액에 곱하면
  변화가 없다) + 골드량 계산을 내림 → 반올림. (7) **이전 웨이브 투사체가 남는 버그** —
  `Projectile`만 지우고 `EnemyProjectile`/`BeamProjectile`은 안 지우고 있었다 → 세 종류를
  웨이브 종료 즉시 정리. (8) 아이템 가격 곡선 1/1.8/3/5/8 → **1.25^등급**(전설 8배 → 2.44배).
  (9) **근접무기 판정을 찌르기 동작과 일치시킴** — 판정 반경이 `weapon_range`(4.95~5.85유닛 =
  화면 세로 반경 5.4급)여서 화면 전체가 맞던 것을, **찌르는 칼 스프라이트의 실제 월드 bounds**로
  프레임마다 판정하도록 바꿨다. **무기는 어깨 높이(y +0.79)에 그려지는데 적 히트박스는 발밑에
  있어(원점 −0.62~−0.02) 판정 높이를 소켓 높이만큼 내리는 보정이 필수**다(안 하면 아무도 안 맞는다).
  근접 3종 range/detect는 감지 거리 의미만 남아 1.4/1.6/1.5, 찌르는 거리(atsize)는 0.9/1.05/0.95.
  (10) 방어력이 적 공격력보다 높아도 **최소 1 피해**(예전 하한 0 → 방어력만 올리면 완전 무적).
  상세·실측 검증은 [작업.md](작업.md) 마지막 항목 참고
- **인게임 상단 HUD 재배치·병합 완료** (2026-08-18) — 사용자가 인게임 HUD 기획서를 첨부하며
  "쓸데없이 2개로 나뉘어 있는 UI가 많다"고 지적한 것을 정리했다. 체력(숫자 패널 + 게이지)과
  레벨/경험치(텍스트 패널 + 게이지)가 각각 **별개의 루트 오브젝트**로 나뉘어 있던 것을 게이지
  하나에 아이콘·숫자를 겹쳐 넣어 한 줄로 합쳤고, 골드·부품상자는 아이콘이 이미 있으므로 항목명
  글자를 없애고 짧은 행으로 줄였다. 배치는 **좌상단 세로 스택**(체력/경험치/골드/부품상자) +
  **상단 중앙**(`웨이브 5`·`00:28`) + **우하단**(구르기 아이콘)이다. `Canvas/HP`에 `GameHUD`가
  붙어 있어 이 오브젝트를 지우거나 끄면 HUD 전체가 죽으므로 **좌상단 스택의 컨테이너로 승격**시켜
  재활용했고, 안 쓰게 된 배경 패널 4장은 삭제하지 않고 비활성만 했다. 캔버스가
  `ConstantPixelSize`라 좌상단 스택·웨이브·DASH를 전부 **정규화 앵커 + offset 0**으로 통일했다
  (기존에는 앵커 + 픽셀 오프셋을 섞어 써서 FHD 밖에서 어긋날 상태였다). 검증은 FHD/720p/1440p
  3개 해상도 캡처 + 체력 40% 이하 빨강 전환 + 구르기 덮개 `fillAmount` + 정비 화면 전환 시
  HUD 4개 전부 숨김을 플레이모드 실측. 상세는 [작업.md](작업.md) 마지막 항목 참고
- **AI 코어 업그레이드 화면에 골드 리롤 버튼 추가 완료** (2026-08-18) — 3택 카드 아래에
  **리롤** 버튼을 넣었다. 상점 새로고침과 같은 누적 공식(기본 10골드 + 회당 +5)이며
  **카드 화면을 열 때마다 기본값으로 초기화**된다. 버튼은 씬에 깔지 않고 신규
  `AiCoreExtraButtonsUI.Attach()`가 코드로 생성한다(**씬 수정 0**). 처음에 `Awake()`에서만 붙였다가
  **에디터 도메인 리로드로 직렬화 안 되는 private 필드가 null로 돌아가 버튼이 사라지는** 함정을
  밟아, 카드 화면을 열 때도 없으면 다시 붙이는 `EnsureAiCoreExtraButtons()`를 넣었다.
  (같은 화면에 정비 중 안 보이던 **골드 보유량 표시**도 우상단에 추가했다.) 함께 만들었던
  "레벨업 취소" 버튼은 경험치 반환 없이는 의미가 없다는 판단으로 **곧이어 완전히 삭제**했다
  (레벨을 되돌리며 소모 경험치 50%를 환급하는 방식이었으나, 반환 자체를 없애기로 하며
  버튼째 뺐다). 상세는 [작업.md](작업.md) 마지막 항목 참고
- **좀비 드랍 아이템(경험치·골드·부품상자) 크기 확대 완료** (2026-08-18) — 2026-08-10 "인게임
  모든 이미지 1/2" 지시로 0.35까지 줄었던 `RewardPickupManager.VisualWorldSize`를 **0.6**로
  올렸다(선 길이 약 1.7배·면적 약 2.9배). 세 종류 전부 이 상수 하나로 최종 렌더 크기가
  정해지므로 스크립트 상수 하나만 바꿨다(씬 수정 없음). 플레이모드 실측으로 셋 다 정확히
  0.6×0.6유닛으로 렌더링됨을 확인
- **로봇 무기 소켓 기본 최대치 2 → 4 확장 완료** (2026-08-18) — 사용자 지시: "모든 로봇의 무기
  기본 최대치 4개(더 적을 수도 있지만 특별한 경우)". `ActiveSocketCount = Min(씬 리깅 개수,
  머리 파츠 weaponSocketCount)` 구조는 이미 N개를 지원하도록 설계돼 있었던 것을 실제로
  실행했다. `PartsCatalog.asset`의 두 로봇(100001/100002) `weaponSocketCount`를 4로,
  코드 폴백 기본값도 2→4로 맞췄다. `Player` 밑에 어깨 소켓(y=3.60) 2개를 복제해 **허리
  높이(y=1.5)에 소켓3/4를 신설**하고 `PlayerShootManager.weapon_slots`를 4칸으로 확장했다.
  **상점의 무기 소켓 선택 팝업(`ShopPanelUI.socketButtons`)이 `new Button[2]`로 고정
  크기였던 하드코딩**을 발견해 씬의 `SocketPicker` UI를 2x2 그리드로 확장하고 배열도 4로
  늘렸다(이걸 놓쳤으면 소켓 3/4를 상점에서 살 수 없었다). 플레이모드 실측으로 무기 4정이
  겹침 없이 표시되고 각자 자동조준하는 것, 정비 화면 4소켓 표시, 상점 4버튼 팝업 전부 확인.
  4개 전부 장착하면 무게가 지탱력을 넘어 이동속도가 깎이는 것은 설계된 동작(장착 자체는
  안 막힘). 상세는 [작업.md](작업.md) 마지막 항목 참고
- **`UI 기획서.pdf` 반영 Phase A(헤더·문구 통일) 완료** (2026-08-18) — 기획서 4장(로봇 정비 /
  상점 / 일시정지·설정 / 환경설정)을 현재 구현과 전수 대조하고 **Phase A~F 계획을 확정**했다
  (착수 순서 A→D→E→B→C, F는 보류). 확정된 결정: **무기 소켓 시스템 상한 6**(기본 로봇은 4,
  6소켓 로봇은 별도), **`메모리` 슬롯 추가 / `발` 슬롯 삭제**, **랭킹·도감은 버튼만 비활성**.
  Phase A에서는 정비 화면과 형식이 달랐던 **상점 헤더를 통일**했다 — `상점` 제목 신설,
  `웨이브 7` → `WAVE 07 / 20`, 골드에 코인 아이콘(정비 화면에도 신설). 버튼 문구도
  **`다음 웨이브 시작` → `정비 종료`**, **`새로고침 (10골드)` → `상점 초기화 - 10`**.
  **코인 아이콘과 숫자가 멀어 보이던 문제**(숫자가 오른쪽 정렬이라 패널이 넓을수록 간격이
  벌어짐)는 **왼쪽 정렬 + `TMP_Text.margin` 왼쪽 여백**으로 고쳤다 — 자릿수가 늘어도 아이콘
  바로 오른쪽에서 자란다. **스탯 아이콘 9종은 에셋이 없어 보류**(`Assets/Resources/UI`에
  체력·골드·별·경험치·대시 아이콘만 있다). 상세는 [작업.md](작업.md) 마지막 항목 참고
- **아이템 칸을 아이콘 방식으로 전면 교체 + 기본 무기 1정 + 그에 맞춘 밸런싱 완료**
  (2026-08-18) — 사용자 요청 묶음. (1) **기본 장착 무기 4정 → 1정**(소켓 0 기관단총만, 나머지
  3소켓은 비움). 빈 소켓을 제대로 지원하려고 `PlayerShootManager`에서 **빈 소켓의 손 이미지를
  끄고**(안 끄면 씬의 기본 스프라이트가 그대로 보인다) `weapon_id <= 0`일 때 경고를 내지 않게
  고쳤다. 부수 효과로 무게 초과가 풀려 이동속도가 2.75 → 3으로 회복됐다.
  (2) **정비 인벤토리·슬롯, 상점의 장착 파츠/무기/디스크를 전부 아이콘 칸으로 교체**했다.
  아이콘 뒤에 별도 사각형을 깔지 않고 **칸 자체가 배경**이며, **일반 등급이 아니면 칸이
  등급색**으로 칠해진다. 칸 생김새는 `ItemCellUI`(신규) 한곳에서만 만든다.
  **로봇 파츠 아이콘은 프로젝트에 없어서**(`Assets/Resources/Parts`는 절차적 리그용 몸통·다리
  4장이다) `PartIconLibrary`(신규)가 코드로 그린 **임시 아이콘**을 쓴다 —
  **`Assets/Resources/PartIcons/<슬롯이름>.png`를 넣으면 코드 수정 없이 자동 교체된다**.
  (3) 정비 화면에서 **글씨가 칸 밖으로 삐져나오던 문제**를 고쳤다(원인: 자동 크기 조절만 켜져
  있고 `overflowMode`가 없어 최소 크기에서도 안 들어가는 글이 그대로 흘러나왔다).
  (4) 인벤토리 아이템을 누르면 **팝업 대신 인벤토리 옆 설명 칸**에 선택 파츠와 **교체 대상**
  설명이 함께 뜬다(`InventoryPanel`을 0~0.42 → 0~0.24로 좁혀 0.26~0.42를 설명 칸으로 썼다).
  선택 강조는 **슬롯 강조와 같은 노란색**이다(파랑 계열이면 희귀 등급과 구분되지 않았다).
  (5) 무기가 1정이 되며 화력이 약 1/4로 줄어 **초반 난이도를 재조정**했다 — 좀비 체력 70 → 40
  등 전 몬스터 0.571배, **방어력도 하향**(리더 10 → 4. 투사체당 11뎀인 기관단총 상대로 방어력
  10은 `max(1, 11-10)`으로 사실상 무적이었다). 보상이 `MaxHp / 상수`라 **보상 상수도 같은 배율**
  (30/60 → 17/34)로 맞춰 획득량을 유지했다. 상세는 [작업.md](작업.md) 마지막 4개 항목 참고
- **ESC 일시정지 메뉴 + 환경설정 화면 완료** (2026-08-18) — `UI 기획서.pdf` Phase D·E.
  ESC → 반투명 오버레이 + 옵션 5버튼(돌아가기/랭킹·도감(준비 중)/설정/나가기) → 설정 →
  환경설정(배경음·효과음 슬라이더, 화면 모드 3택, 화면 비율 4택(전체화면 전용), 피드백(URL
  미정으로 비활성), 저장 후 닫기). 씬 수정 없이 전부 코드로 만들어 Canvas에 붙인다.
  **`GameFlowManager.IsPaused`를 `IsIntermission`과 별개 플래그로 신설**하고 timeScale은
  열기 직전 값을 저장했다 복원하는 방식으로 처리했다. `SFXManager`에도 배경음과 같은 방식의
  전역 볼륨을 추가했다(효과음 에셋은 없지만 피격 비프음 1개로 슬라이더 자체는 테스트 가능).
  **플레이모드 실측으로 레이아웃 버그 2건을 잡았다** — 화면 모드/비율 버튼 7개가 앵커 설정
  누락으로 전부 한 점에 뭉쳐 안 보였던 것, 화면비율 아래로 패널 1/3이 비고 저장 버튼이 패널
  밖으로 나가 있던 것. 상세는 [작업.md](작업.md) 마지막 항목 참고
- **정비 화면 레이아웃 재구성 완료(B-6 캐릭터 미리보기 제외)** (2026-08-18) — `UI 기획서.pdf`
  Phase B. **슬롯 구성을 기획서대로 "메모리 추가 / 발 삭제"**로 바꿨다 - `PartSlot.Memory`
  신설(발이 주던 이동속도는 다리 파츠로 통합: 경량 부츠/제트 부스터 계열이 이제 다리 슬롯).
  **메모리 파츠가 AI 코어 최대 레벨을 결정**한다(기본 50, 최상급 대용량 메모리 카드는 120) -
  `AiCoreManager`의 고정값(70)을 대체했다. 정비 화면 레이아웃도 **머리+무기 소켓을 한 줄로
  묶고**(소켓 수만큼 열이 자동으로 늘어남, 6소켓까지도 같은 코드 경로라 문제없음) 나머지
  슬롯은 3열 그리드로 자동 배치(파츠 2행 + 디스크 슬롯 1행). 능력치 패널에 `레벨: N (MAX:M)`도
  새로 표시한다. **`PartsCatalog.asset` 데이터를 코드로 일괄 마이그레이션**(발 삭제·이전,
  메모리 11건 추가)했고 플레이모드 실측(메모리 교체 → 최대 레벨 실시간 반영)으로 검증했다.
  캐릭터 미리보기(RenderTexture)는 이번 범위에서 제외했다. 상세는 [작업.md](작업.md) 마지막
  항목 참고
- **상점 카드·보유 목록 재구성 완료** (2026-08-18) — `UI 기획서.pdf` Phase C. **씬 수정 0건**으로
  카드 안에 필요한 요소를 런타임에 코드로 덧붙여 처리했다(Phase D/E와 같은 관례).
  `bodyText` 한 덩어리에 뭉쳐 있던 `이름\n능력치\n가격`을 **이름(등급색) / 능력치 / 가격 박스**로
  나누고, 가격은 카드 하단 독립 박스(코인 아이콘 + 숫자)가 됐다. **구매하면 카드가 어두워지며
  14도 기울어진 빨간 "구매 완료" 스탬프**가 찍히고 가격 자리는 "구매함"이 된다. 잠금 버튼에는
  **자물쇠 아이콘**이 붙고 잠긴 카드는 **노란 테두리**로 강조된다(정비 화면 슬롯 강조와 같은 색).
  `[장착 무기] 1/4` 개수 표기를 추가했고, **소켓 선택 창은 4칸 고정에서 소켓 수에 맞춰 만들고
  배치하는 방식으로 바꿔 6소켓까지 견딘다**(모자라면 1번 버튼을 복제 - 이때 원본의 `onClick`이
  같이 복사돼 엉뚱한 소켓에 장착되므로 열 때마다 리스너를 지우고 자기 인덱스로 다시 단다).
  아트가 없는 자물쇠·테두리는 신규 `UiIconLibrary`가 코드로 그리며
  **`Assets/Resources/UI/Lock_icon.png` / `Stamp_frame.png`를 넣으면 자동 교체**된다.
  상세는 [작업.md](작업.md) 마지막 항목 참고
- **소켓 선택 창이 보유 목록 격자에 가려지던 버그 수정** (2026-08-18) — 위 작업 중 실측 캡처로
  발견. `PartsGrid`/`WeaponsGrid`/`DiscsGrid`와 음악 볼륨 슬라이더는 전부 런타임에 만들어져
  패널의 **마지막 자식**으로 붙는데, 씬에 있는 `SocketPicker`는 그보다 앞 형제라 UI 그리기
  순서상 뒤로 밀렸다(2026-08-18 아이콘화 때 격자가 생기면서 들어온 문제). 열기 직전
  `SetAsLastSibling()`으로 해결. **런타임에 UI를 덧붙이는 이 프로젝트 관례에서는 씬에 있던
  오브젝트가 조용히 뒤로 밀릴 수 있다** - 숫자 검증만으로는 안 잡히고 캡처를 봐야 드러난다
- **파츠 25종 전면 교체 + 스탯 소수화 + 골드 아이콘 통일 완료** (2026-08-20) — 사용자가 제공한
  장갑 명세 2종과 아이콘 아트 25장을 반영해 **헬멧 6 / 다리장갑 4 / 무기소켓 7 / 메모리 3 /
  디스크슬롯 5 = 25종 x 5등급 = 125행**을 신설하고 그 5개 슬롯의 기존 임시 루트 파츠 55행을
  삭제했다(`Comstock/파츠 테이블 25종 재생성` 메뉴 신설, `PartsTableGenerator`).
  **헬멧은 "머리부 장갑"의 새 명칭**이며 기존 `PartSlot.Helmet`을 그대로 쓴다.
  (1) `PartData`에 **가산 스탯 2쌍 + `PartEffect` 특수효과**를 넣어 효과가 2개 이상인 파츠를
  표현할 수 있게 했고, 파생/조건부/％ 계산은 신규 `PartEffects`(머리의 `HeadEffects`와 같은 구조)가
  맡는다 — **`RobotStats.Compute`에서 `HeadEffects` 다음에 호출해야 한다**(연극 마스크는 최종 행운을,
  철인 헬멧은 최종 공격력을 읽는다). (2) **무기 소켓의 제한축이 무기 타입(경/중/근접) → 무기
  카테고리(연사/산탄/정밀/폭발/에너지/근접 + 범용)로 바뀌고**, 등급 효과도 사거리·감지·회전 배율에서
  **공격속도·공격력·치명타·스플래시·방어력무시**로 교체됐다(카테고리 불일치면 보정 0 + 무게 x2).
  (3) **스탯을 데미지 파이프라인까지 전부 float로** 바꿨다 — 예전에는 `RobotStats.ApplyBonus`가
  보너스를 개별 반올림해서 `공격력 +0.3`이 통째로 사라졌다. 이제 플레이어·적 체력/공격력/방어력,
  투사체·빔·근접 데미지가 모두 소수이고 표시도 `0.##`이다. (4) **메모리의 AI 코어 최대 레벨이
  "대체"에서 "가산"으로** 바뀌었고(머리 기본 50 + 파츠), 시작 레벨(뉴럴 캐시)은 장착 시점에
  부족한 만큼만 지급한다(`RunState.CoreStartLevelGranted`). (5) 파츠 아이콘이 **슬롯 공용에서
  파츠별**(`PartData.iconName` → `Resources/PartIcons/`)로 바뀌었다. 상세는
  [작업.md](작업.md) 마지막 항목 참고
- **진행 중**: 기획서 4종(해금·악세사리·엔드리스·디스크) 반영 **Phase A~E 전부 완료**(2026-08-19).
  `UI 기획서.pdf` 반영도 **A/B/C/D/E 완료**이고, 남은 것은 보류 중인 **B-6(캐릭터 미리보기)** 와
  화면 기획서를 기다리는 **Phase F(랭킹·도감 화면)**, 그리고 **해금 알림 UI / 인게임 도감 /
  랭킹 Firebase 연결**(전부 사용자 판단 대기)
- **기획서 4종 반영 계획 수립 + Phase A(밸런스·버그 5건) 완료** (2026-08-19) — 해금(도감)·
  악세사리·엔드리스·디스크 기획서를 반영하는 **Phase A~E 계획**을 세우고
  (`밸런스 → 점수 → 엔드리스 → 악세사리 → 해금` 순서가 의존관계상 강제된다 - 점수 시스템이
  악세사리·엔드리스의 선행 조건이고, 해금 조건의 상당수가 "엔드리스에서 OO"다), 그중 **Phase A**를
  실행했다. (1) **상점 디스크만 등급 추첨을 안 하고 균등 추첨**하고 있어 전설이 의도 3% →
  실제 **9.5%** 로 나오던 버그 수정(등급을 먼저 뽑고 그 등급에서 고르며, 비면 아래 등급으로만
  내려간다). (2) 경험치 커브에 **15/30/45레벨 구간 배율 x2/x4/x8** 도입(45레벨 누적 2,156 →
  **6,386**). (3) **영구 누적형 디스크 4종**은 초기화 버그가 아니라 **상한이 웨이브 1 안에 다
  차는 것**이 원인이었다 - 상한을 "약 200마리 처치"로 재조정하고, 설명을
  `(현재 +3.2 / 최대 +10)`처럼 **실시간 진행도**로 바꿨으며, 2장 장착 시 상한도 2배가 되게 했다.
  (4) **근접무기 좌/우 방향**은 대기 자세(적 없음)가 이미 정확했고(실측 165도/15도) 사용자가 보던
  시점이 **조준 경로**였던 것이 정체다 - 사용자 확정에 따라 근접무기는 **조준 회전을 아예 하지 않고**
  찌르는 순간에만 적을 향한다. (5) **행운은 등급 확률에 전혀 관여하지 않고 있었다**(전수 조사) -
  사용자 확정으로 신규 `LuckBonus`에 **행운 10당 등급 단계마다 +1%(상한 50)** 의 아주 약한 보정을
  신설했다(전설 3.02% → 행운 50에서 3.47%). 검증은 전부 플레이모드 실측이다. 상세는
  [작업.md](작업.md) "2026-08-19 Phase A" 항목
- **Phase B(점수) · C(엔드리스) · D(악세사리) 완료** (2026-08-19) — 위 계획의 나머지 중 3개.
  (1) **점수**: 신규 `RunScore`(static) — `웨이브 x1,000 + 누적 처치 x10 + 코어 레벨 x200 +
  보유 골드 x1 + 악세사리 점수`. 누적 처치 수는 이 프로젝트에 처음 생기는 카운터로
  `EnemyUnit.OnKilledByPlayer`를 직접 구독해 센다. 인게임 HUD에 실시간 표시(`ScoreHudUI`).
  (2) **엔드리스**: 보스를 **처음** 잡았을 때만 승리 처리로 빠져 점수 정산 팝업
  (`ScoreSummaryPopup`)이 뜨고, `계속 진행`을 고르면 `RunState.IsEndless`가 켜져 이후의 보스는
  일반 웨이브처럼 흘러간다(보스 재등장 주기 = 20 → 39 → 58 ...). 재등장 보스만 일반 몬스터와
  같은 스탯 배율을 받는다. 랭킹은 `LeaderboardService`(인터페이스 + PlayerPrefs 로컬 구현)까지만
  만들어 뒀고 **Firebase 연결만 비어 있다**(사용자 지시).
  (3) **악세사리 6종**: 엔드리스 상점에서만 **칸마다 5%** 확률로 등장하는 점수 전용 아이템
  (효과 없음, 중복 구매 가능). 구매하면 캐릭터에 실제로 그려진다 — 신규 `AccessoryVisual`이
  머리 위(스택) · 얼굴 · 목에 붙이며, **스프라이트의 타이트 메시 정점으로 알파 여백을 뺀 영역**을
  기준으로 크기·위치를 잡아 **머리 12종 어느 것으로 바꿔도 위치가 따라온다**(팬봇만 프로펠러
  때문에 얼굴 높이를 예외 지정). 상세는 [작업.md](작업.md)의 "2026-08-19 Phase B/C", "Phase D" 항목
- **머리(로봇) 12종 전면 구현 완료** (2026-08-19) — `머리 기획서 Ver04`(백승오) + 사용자 제공
  아트 `final_250/` 반영. **머리 = 게임 시작 시 1회 고르는 캐릭터 정체성**(상점·부품 상자에
  등장하지 않고 런 중 교체 불가)이라는 전제로 만들었다.
  (1) **"머리"는 곧 절차적 리그의 몸통 스프라이트다** — 기존 `Parts/Body.png`(250x250)가 컴스톡
  MK-01의 얼굴 달린 원통이고, 사용자 아트도 전부 250x250이라(폴더명 `final_250`이 이 규격을 뜻함)
  **몸통만 갈아끼우면 다리 배율·콜라이더·무기 소켓을 하나도 건드릴 필요가 없다.** 리그에 별도
  head 오브젝트는 존재하지 않는다. 아트는 `Assets/Resources/Heads/` 19장(머리 11 + 네온아이 8프레임).
  (2) **머리 12종**: 컴스톡 MK-01(100001) / 가드맨(100003) / 메테우스(100004) / 버서커(100005) /
  해피 픽셀(100006) / 네온아이(100007) / 핫팟(100008) / 픽시(100009) / 팬봇(100010) /
  소다캔(100011) / 프라이빗 컴스톡(100012) / 미니 픽시(100013). 머리마다 **체력·질량·무기 소켓 수
  (1~6)·기본 장착 무기·고유 효과**가 다르다. 기존 디버그 로봇 100002(미니 컴스톡)는 남겨두되
  선택 목록에서만 제외(`selectableInHeadSelect: 0`).
  (3) **효과는 전부 `HeadEffects` 한 곳에서 계산**한다 — 훅 지점이 6개 파일에 흩어져 있어
  호출부마다 `switch`를 두면 머리를 추가할 때 6곳을 고쳐야 하기 때문이다. 호출부는 배율만 묻는다.
  조건 축으로는 **이미 존재하지만 아무도 읽지 않던 `WeaponType`**(연사/산탄/정밀/폭발/에너지/근접)과
  **`행운` 스탯**을 처음으로 소비한다.
  (4) **`Ground01`에 무기 소켓 5/6을 신설**(x±4.2, y2.55)하고 `weapon_slots`를 6칸으로 늘렸다 —
  `ActiveSocketCount = Min(씬 리깅 수, 머리 값)`이라 이게 없으면 6소켓 머리가 조용히 4가 된다.
  (5) **시작 무기가 씬 고정값에서 머리 데이터로 이동**했다(`PlayerShootManager.ApplyHeadDefaultWeapons`).
  (6) **머리 선택 화면**은 `Title.unity` 위에 뜨는 코드 생성 패널이다(새 씬·빌드 세팅 변경 없음).
  타이틀 씬에 `DataManager`를 추가해 체력·질량·무기 이름을 보여준다 —
  `GameDataManager`가 DontDestroyOnLoad + 중복 가드라 인스턴스는 계속 1개다.
  **보류**: 소다캔 효과(로켓 엔진 다리 파츠가 프로젝트에 없어 조건 판정 불가 - 다리 기획서 대기),
  가드맨·메테우스 전용 아트(기획서 PDF에서 추출한 임시 아트).
  상세는 [작업.md](작업.md) 2026-08-19 항목
- **DASH UI를 캐릭터 밑 게이지 바로 교체 완료** (2026-08-19) — 사용자 요청. 우하단 버튼형 아이콘은
  시선이 캐릭터에 있을 때 눈에 들어오지 않아 쿨다운을 사실상 못 봤다. 신규 `DashGaugeUI`가
  **캐릭터 발밑을 따라다니는 초록 게이지 + `DASH` 글자**를 코드로 만든다(게이지가 가득 차면 구를
  수 있다). 캔버스 UI로 만들되 **크기를 카메라에서 역산**(`Screen.height / (2×orthographicSize)`)해
  해상도가 바뀌어도 캐릭터 대비 비율이 유지된다. 구버전 아이콘(`Canvas/DashSkill`)은 삭제하지 않고
  런타임에 비활성한다
- **드랍 아이템(경험치·골드·부품상자) 전용 아이콘 적용 완료** (2026-08-19) — 사용자가 제공한
  신규 아트 3장(`드롭아이템/` 폴더)을 적용했다. 골드·부품상자는 기존 `Assets/Resources/Gold.png`·
  `ItemBox.png`를 그대로 덮어썼고(같은 파일명이라 참조 그대로 유지, 부품상자 화살표 UI도 자동
  갱신), 경험치는 **에너지 빔 무기 스프라이트와 공유 중이던 임시 `Energy` 리소스**에서 벗어나
  **신규 `Exp.png`**로 분리하고 `RewardPickupManager.ExpResourceName`을 갱신했다(그대로 덮어썼다면
  플라즈마 캐논 등 빔 무기 시각이 깨졌을 것). 플레이모드 스폰+캡처로 세 아이콘 모두 정상 표시 확인.
- **일반 좀비 이동 애니메이션 회귀 수정 완료** (2026-08-19) — 이전에 이미 적용돼 있던(미커밋)
  몬스터별 전용 아트 교체로 좀비 스프라이트 이름이 `Enemy_zombie_S` → `Zombie`로 바뀌면서
  `EnemyUnit.CanPlayZombieAttackFrames()`의 이름 검사가 깨져 걷기·공격 프레임이 멈춰 있었다.
  상수를 새 이름으로 갱신해 해결(플레이모드 실측 확인). **"피격 시 빨간색" 리포트는 원인
  미확정** — 몸체는 항상 흰색으로만 물드는 것을 실측 확인했고 체력바는 사용자가 아니라고
  확인해, 공격 텔레그래프(주황)·보스 AoE 예고(반투명 빨강) 등 다음 세션에 재확인 필요.
- **가드맨 "연속 2회 발사" 두 번째 탄 방향 버그 수정 완료** (2026-08-19) — 산탄총 1회차가
  적을 즉사시키면 2회차 발사 방향이 실제 1회차 방향이 아니라 고정값(오른쪽, 0도)으로 튀던
  버그. `FireExtraBursts()`의 방향 초기값을 1회차 `aim_direction`으로 바꿔 해결. 상세는
  [작업.md](작업.md) 마지막 항목 참고.
- **구르기 게이지 "완충 시 숨김" + 웨이브 1 이후 게이지 소실 버그 수정 완료** (2026-08-19) —
  두 요청의 원인이 같았다: `DashGaugeUI`가 숨을 때 **자기 자신이 붙은 오브젝트를 직접
  꺼버려서**(Unity는 비활성 오브젝트의 `LateUpdate`를 호출하지 않음) 한 번 숨으면 스스로 다시
  켤 기회가 없었다. 정비 화면 진입 때 숨은 뒤 `combatHudObjects`(런타임 생성이라 미등록)가
  되살려주지 않아 웨이브 2부터 계속 사라져 있었다. 배경/채움/글자를 자식 `visual`로 묶고
  숨길 때 `root`가 아니라 `visual`만 끄도록 수정 - `root`(스크립트가 붙은 오브젝트)는 항상
  켜둬 `LateUpdate`가 계속 돈다. 같은 조건에 `쿨다운 남음` 체크를 추가해 완충되면 숨고
  구르기를 쓰는 순간부터 보이게 했다. 플레이모드 실측(리플렉션으로 웨이브 전환·쿨다운 값
  강제 재현)으로 검증. 상세는 [작업.md](작업.md) 마지막 항목 참고.
- **부머(디스럭터) 밸런스 조정 + 프라이빗 컴스톡 정밀 효과 버그 수정 완료** (2026-08-19) —
  디스럭터(자폭 좀비) 피해량 -50%(monster_atk 75→38), 폭발 범위 -25%(3.0→2.25). 프라이빗
  컴스톡 [정밀] 공격속도 배율 0.5→0.75, 관통 +2→+1로 완화. **버그 수정**: "정밀화기 장착 시
  공격력 x2"가 무기 자체 위력뿐 아니라 슬롯에 균등 분배되는 로봇 전체 공격력(robot_atk)까지
  함께 두 배로 늘리던 문제 - `HeadEffects.WeaponAttackMultiplier()`(무기 자체 위력 전용)를
  신설해 `DamageMultiplier`(전체 데미지용, 가드맨·버서커가 계속 씀)와 분리했다. 플레이모드
  리플렉션 실측(120×2+3=243, 수정 전 공식이면 246)으로 검증. 상세는 [작업.md](작업.md)
  마지막 항목 참고.
- **ESC 일시정지 버그 수정 + 우상단 설정 아이콘 + 근접무기 대기 자세 + 머리 정렬 완료**
  (2026-08-19) — (1) **ESC가 아예 동작하지 않던 원인 확정**: `PauseMenuUI.Build()`가 마지막에
  **자기 컴포넌트가 붙은 GameObject를 직접 껐고**, Unity는 비활성 오브젝트의 `Update()`를
  호출하지 않으므로 ESC 폴링이 단 한 번도 실행되지 않았다(구조적으로 열 수 없었다). 딤 배경·
  옵션 패널을 자식 `Content`로 묶어 그것만 토글하도록 고쳤다. ESC 폴링과 아이콘이 조건을
  공유하도록 `TryOpenPause()`를 추출. (2) **우상단 설정 아이콘** 신규 `SettingsIconUI`
  (씬 수정 0건, 런타임 생성). 톱니바퀴는 `UiIconLibrary.Settings()`가 코드로 그리며
  **`Assets/Resources/UI/Settings_icon.png`를 넣으면 자동 교체**된다. (3) **근접무기 대기 자세**
  — 총기용 `rest_rotation_degrees`(좌 8.112 / 우 -3.233, 둘 다 오른쪽 근처)를 공유해 왼팔 칼도
  오른쪽을 향하던 것을, 소켓 좌/우에서 각도를 뽑도록 분리(왼쪽 165도 / 오른쪽 15도 = **위로 15도**
  미러). 좌우 판정은 `rig_point.localPosition.x` 부호를 `Awake`에서 캐시하며,
  **`use_left_hand_image`는 물리적 좌우와 반대라 쓰면 안 된다.** 총기 경로는 무수정.
  (4) **머리 12종 전수 검증** — 알파 bbox 실측 결과 **가드맨·메테우스만** 내용물이 30px 위로
  치우쳐 있어(임시 아트) `.meta`의 피벗을 0.5→0.62 + `alignment` Custom으로 교정,
  **나머지 10종은 정상이라 손대지 않았다.** ESC는 실제 키 주입으로, 근접무기는 각도 실측
  (165.0/15.0)으로, 머리는 12종 월드 좌표 실측으로 검증. 상세는 [작업.md](작업.md) 마지막 항목 참고
- **⚠ 재발 방지 규칙(2026-08-19, 하루에 두 번 터짐)**: **`MonoBehaviour`가 자기 자신이 붙은
  GameObject를 `SetActive(false)`하면 `Update()`/`LateUpdate()`가 영영 호출되지 않아 스스로 다시
  켤 수 없다.** `DashGaugeUI`(구르기 게이지 소실)와 `PauseMenuUI`(ESC 먹통)가 같은 원인이었다.
  **런타임 생성 UI를 숨길 때는 반드시 자식 컨테이너를 토글할 것**(루트는 항상 활성 유지)
- **다리 음영 방향 반전 완료** (2026-08-12) — 사용자가 "왼쪽을 볼 때 오른쪽 다리가, 오른쪽을
  볼 때 왼쪽 다리가 밝아야 한다"고 확정해 `ProceduralCharacterRig`의 두 다리 밝기·그리기
  순서를 서로 맞바꿨다(고관절 위치·보행 위상은 그대로 유지). 실측(월드 좌표+색상 직접 조회)으로
  양방향 모두 일치 확인. 상세는 [작업.md](작업.md) "2026-08-12 다리 음영 방향 반전" 참고
- **죽은 드랍 아이템 경로 제거 + 상점 카드 아이콘 표시 완료** (2026-08-12) — 앞서 발견한
  `DropTableManager`/`DropItemManager`/`ItemPickup`/`PlayerInventory`(주운 개수를 세기만 할 뿐
  아무 효과가 없던 무기·방어구·골드 드랍 경로)를 사용자 확정에 따라 스크립트 4개 + 데이터
  모델(`GameDataAsset.drops`/`DropEntry`/`GameDataManager.DropsByMonster`)까지 완전히
  삭제했다. 상점 카드(4칸)에 무기/디스크 아이콘도 신설했다 - 디스크는 기존 `DiscData.LoadIcon()`,
  무기는 손에 드는 이미지(`weapon_rgwpimg`)를 재사용한다. Unity MCP만으로 UI 오브젝트(RectTransform+
  Image)를 새로 만들어 4개 카드 좌상단에 배치했고, 카메라 컬링마스크를 `UI` 레이어로 좁힌
  `RenderTexture` 캡처로 4칸(디스크 2·무기 2) 전부 정상 표시를 실측 확인했다. 상세는
  [작업.md](작업.md) "2026-08-12 죽은 드랍 아이템 경로 제거 + 상점 카드 아이콘 표시" 참고
- **근접무기 3건 + 사용자 버그 리포트 다수 완료** (2026-08-12) —
  근접무기 방향 보정(`weapon_imgangle` 실측 반영)·사거리 상향·찌르기 애니메이션 신설, 다리 음영
  조사(버그 미확정, 사용자 확인 대기 중), 신발 렌더링 순서 수정(다리 뒤로), 좀비 걷기 애니메이션
  속도를 이동속도에 연동, 좀비 투사체 크기/속도 조정, 상점 아이템 잠금이 웨이브 전환 시 풀리던
  문제 수정, 웨이브 종료 자석 연출 신설. 상세는 [작업.md](작업.md)
  "2026-08-12 근접무기 3건 + 사용자 버그 리포트 다수 처리" 참고
- **밸런스 조정 3건 + 빌드 전용 히트플래시 버그 수정 완료** (2026-08-12) — PC 이동속도
  (`robot_speed` 2.5→3.0, +20%), 좀비(몬스터ID 200001) 이동속도(2.5→2.0, -20%)·체력
  (100→70, -30%)를 `GameDataAsset.asset`에서 직접 조정했다(차저 등 다른 종은 이름이
  다르므로 건드리지 않음). 또한 "빌드에서만 피격 시 흰색 깜빡임이 안 보인다"는 리포트를
  조사해 **빌드가 코드에서만 동적 생성되는 `Comstock/SpriteFlash` 셰이더를 스트리핑**하는
  것이 원인임을 특정하고(에디터의 `Shader.Find`는 항상 성공하지만 빌드는 씬/`Resources`가
  참조하지 않는 셰이더를 뺀다), `Assets/Resources/Materials/SpriteFlashBase.mat` 더미
  머티리얼을 신설해 `HitFlash.cs`가 `Resources.Load`로 셰이더를 가져오도록 바꿔 해결했다
  (프로젝트 설정 변경 없이 해결 - 협업 규칙상 프로젝트 설정 변경은 사용자 확인이 필요해서
  더 간단한 경로를 택함). 상세는 [작업.md](작업.md) 마지막 항목 참고
- **무기 65행의 거리 수치를 ÷100 환산으로 재생성 완료** (2026-08-10) — 위 카메라 변경으로
  화면 환산이 ÷50 → ÷100이 되면서 무기 거리 값이 화면 대비 2배가 됐던 것을 바로잡았다.
  **원거리 10종만** `range`/`detect`/**`speed`** 를 절반으로 낮추고
  (탄속도 유닛/초라 함께 나눠야 탄 비행 시간이 보존된다) 메뉴 `Comstock/무기 테이블 65행 재생성`을
  실행했다. **감지 상한 5.3에 잘리던 무기가 25행 → 0행**이 되어 무기별 감지거리 차별화가
  살아났다. **근접 3종과 `splash`/`atsize`/`knockback`은 환산에서 제외**했다 — 이 값들은
  화면이 아니라 **적 몸 크기**에 종속되고 적 크기는 변하지 않았기 때문이다(근접을 절반으로
  줄이면 좀비 공격 사거리 1.6보다 짧아져 일방적으로 맞게 된다). 검증: 무기 테이블 검증 8개
  전부 통과, 감지거리 밖(3.60) 좀비는 무피해·안(2.40) 좀비는 사살되는 통제 실험 확인.
  상세는 [작업.md](작업.md) 마지막 항목 참고
- **인게임 모든 이미지 1/2 축소 + 배경 미러 타일 4장 완료** (2026-08-10) — **카메라는
  그대로 두고**(`orthographicSize=5.4`) 그림만 절반으로 줄여, 캐릭터 기준 시야와 맵이 2배
  넓어졌다. `Player.localScale` 0.43812 → **0.21906**, 적 6종+보스 프리팹 루트 스케일 ×0.5,
  픽업 그림 크기 상수(1.2→0.6 / 0.7→0.35), 스피터 투사체 0.35→0.175, 무기 `atsize`(원거리
  10종) 절반. **적 콜라이더는 로컬 값이라 루트 스케일에 자동 비례**한다. 배경은 절반 크기
  4장을 **미러 타일**(`flipX`/`flipY` 조합)로 이어붙여 맵 **38.32x21.52유닛**(화면의 정확히
  2.00배)을 유지한다. 타일 경계의 흰 줄은 **원본 이미지 가장자리 3번째 픽셀이 밝기 때문**
  (0.520 vs 내부 0.474)이라 실측으로 규명하고, 미러링 + **가장자리 4px를 잘라낸 타일 전용
  이미지**(`ground_ruined_city_v2_tile.png` 신규, 원본은 보존)로 완전히 없앴다.
  `MapBounds`는 자식 타일들의 bounds를 합산하도록 수정했다. 상세는 [작업.md](작업.md)
  마지막 항목 참고
- **미해결 이슈 (사용자 판단 대기)** — 거리·속도 값이 **몸 크기 대비 커진 상태**다.
  (1) **좀비가 몸 크기의 2.73배 거리에서 때린다**(실효 공격 사거리 1.600 vs 두 몸이 닿는
  거리 0.586). 이미지만 줄이고 `monster_range`(÷50 환산 잔재)는 그대로 뒀기 때문이며,
  1.6 → 0.8로 낮추면 몸 접촉 하한이 작동해 해소된다. 좀비 기획서 Ver04 반영값이라 보류 중.
  (2) 두 산탄총(전투산탄총 감지 1.5 / 소드오프샷건 1.3)이 좀비 공격 사거리보다 짧아
  먼저 맞는다 — 같은 원인이라 (1)을 고치면 함께 해소된다.
  (3) 무기 사거리·감지거리와 이동속도도 몸 크기 대비 2배가 됐다. 실플레이 체감을 보고
  판단할 사안이다
- **무기를 새로 추가/수정할 때는 `Assets/Scripts/Editor/WeaponTableGenerator.cs`의 `Defs`
  배열만 고치고 메뉴 `Comstock/무기 테이블 65행 재생성`을 실행한다.** 이 생성기가
  `GameDataAsset`(무기) + `PartsCatalog.weaponMeta`(타입·무게) + `ShopCatalog.weaponEntries`
  (상점 판매) **세 곳을 한꺼번에** 채운다. 65행 × 24필드를 손으로 넣는 것은 비현실적이고,
  한 곳만 빠뜨리면 무게 0이 되거나 상점에 안 나오는 식으로 **조용히** 어긋난다.
  수정 후에는 메뉴 `Comstock/무기 테이블 검증`으로 정합성 8종을 확인할 것
- **무기 소켓 사양** (2026-08-07 확정 → **2026-08-20 전면 교체**) — 소켓 파츠의 **종류는 장착 가능한
  무기 카테고리**(= `WeaponType`: 연사/산탄/정밀/폭발/에너지/근접, 그리고 제한 없는 범용)를 정하고,
  **등급은 공격속도 · 공격력 · 치명타 확률 · 스플래시 범위 · 방어력 무시**를 보정한다
  (`ModdingManager.GetWeaponSocketModifiers(socketIndex, weaponId)`).
  **카테고리가 맞지 않으면 보정이 0이고 무게에 x2가 붙는다**(장착 자체는 막지 않는다).
  예전 사양(무기 타입 경/중/근접으로 제한 + 등급 = 사거리/감지/회전 배율)은 폐기됐다 —
  `WeaponClass`는 이제 소켓 제한에 쓰이지 않고 상점 표시에만 남아 있으며, 사거리·감지거리·회전속도에
  곱해지던 소켓 배율도 사라졌다(무기 데이터 값 그대로 쓴다).
  **감지거리 = 적을 감지해 발사를 시작하는 거리 / 사거리 = 탄이 날아가는 최대 거리**로 분리되어
  있으며, 이제 **`weapon_detect`라는 별도 데이터 필드**를 갖는다(예전엔 `weapon_range` 하나에서
  둘 다 파생시켰다). 감지거리는 사거리를 넘지 않도록 잘리고, 그 위에 **화면 밖 교전을 막는
  상한 5.3유닛**(`PlayerShootManager.max_detect_range`)이 한 번 더 걸린다.
  거리 단위는 **기획 문서 수치 ÷ 100 = 월드 유닛**이다(2026-08-10 카메라를 FHD 기준으로
  맞추면서 ÷50에서 변경 — **1유닛 = 100px**이므로 기획 수치 1 = 화면 1픽셀).
  직교 카메라 가시 반경은 세로 **5.4** / 가로 **9.6**이다.
  **근접 3종은 이 환산과 무관하다**(2026-08-13 변경) — 근접의 `weapon_range`/`weapon_detect`는
  "이 거리에 적이 들어오면 찌르기를 시작한다"는 **감지 거리**이고 값은 실측 도달 거리
  (1.4 / 1.6 / 1.5유닛)이며, **타격 판정 크기는 데이터가 아니라 찌르는 칼 스프라이트의 실제
  월드 bounds가 정한다**(`PlayerShootManager.ApplyMeleeHitAtBlade`). `weapon_atsize`는 근접에서
  **찌르는 거리**(0.9 / 1.05 / 0.95)로 재활용된다
- **파츠 데이터 구조** (2026-08-07 확정 → 2026-08-20 갱신) — **파츠는 종류마다 5개 등급이 전부
  존재하는 형식**이다(예: "연극 마스크" = 일반/희귀/서사/유일/전설 5행). 따라서 부품 상자는
  **등급을 먼저 추첨한 뒤** 그 등급의 파츠 중에서 뽑는다. **새 파츠 종류를 추가할 때는 5개 등급을
  모두 채울 것.**
  - 2026-08-20부터 `PartsCatalog.asset`에는 **파츠 173개**가 있다 — 기본 장착(`isDefaultStarter`)
    8개 + 명세 기반 신규 25종 x 5등급 125개 + 명세에 없는 슬롯(팔 장갑 / 자기장 코어 / 다리)의
    기존 임시 루트 파츠 40개.
  - **명세가 있는 5개 슬롯(헬멧 / 다리장갑 / 무기소켓 / 메모리 / 디스크슬롯)은 손으로 고치지 말고
    `Assets/Scripts/Editor/PartsTableGenerator.cs`의 `Defs`를 고친 뒤
    메뉴 `Comstock/파츠 테이블 25종 재생성`을 실행할 것**(무기·디스크 테이블과 같은 방식).
    이 생성기는 그 5개 슬롯의 루트 파츠만 갈아끼우고 기본 파츠와 다른 슬롯은 건드리지 않는다.
    검증은 `Comstock/파츠 테이블 검증`.
  - **등급별 수치는 배율이 아니라 명세에 적힌 5개 값을 그대로 넣는다**(예: 행운 +2/2.5/3/4/5).
    값이 하나만 적힌 항목은 등급 무관 고정 수치라 5등급 모두 같다. 2026-08-07의
    "일반 기준값 × 배율(1.0/1.3/1.7/2.2/3.0)"은 명세가 없던 시절의 임시 방식이며, 지금은
    명세가 없는 3개 슬롯의 기존 데이터에만 남아 있다.
  - **효과가 2개 이상인 파츠**는 `bonusStat`/`bonusStat2`(가산 2쌍)와 `effect`(`PartEffect`)로
    표현한다. 파생(행운·공격력에서 방어력)·조건부(체력 50% 이하)·％증가·반사·디스크 비례처럼
    합산 딕셔너리로 표현할 수 없는 것만 `PartEffect`에 넣고, 계산은 전부 `PartEffects`에 있다.
  - **파츠 아이콘은 파츠별**이다 — `PartData.iconName` → `Assets/Resources/PartIcons/{iconName}.png`
    (Sprite 2D and UI). 없으면 슬롯 공용 아이콘(`PartIcons/{PartSlot}`) → 코드 생성 실루엣 순으로
    폴백한다(`PartIconLibrary`). 파츠를 아는 화면은 `PartIconLibrary.Get(part)`를 쓸 것.
- **스탯은 전부 소수(float)다** (2026-08-20 사용자 지시 "전부 소수화 해라 안짤리게") — 플레이어와 적의
  **체력·공격력·방어력**, 그리고 투사체/빔/근접이 나르는 **데미지**가 모두 float이다.
  예전에는 `RobotStats.ApplyBonus`가 `Mathf.RoundToInt(amount)`로 **보너스를 개별 반올림**해서
  `공격력 +0.3` 같은 소수가 통째로 0이 되어 사라졌다. **새 스탯 보너스를 만들 때 반올림을 넣지 말 것.**
  UI 표시는 `0.##`으로 통일한다(정수일 때는 소수점이 붙지 않는다). 정수로 남는 것은 골드·경험치
  수령량, 웨이브/레벨/슬롯 개수, 해금 조건 기록값(내림)뿐이다
- **무기는 등급마다 별도 행이다** (2026-08-07) — 예전처럼 상점에서 등급 배율을 곱하지 않는다.
  ID 체계는 `300000 + 종류(1~13)×100 + 등급(1~5)`이며(중기관총 일반 = 300101 … 전설 = 300105),
  등급별 공격력이 데이터에 이미 반영되어 있다(일반 × 1.15^등급 = 예측화력 15%씩 상승).
  `ShopCatalog.GradeSetting.statMultiplier`는 **무기에 더 이상 적용되지 않는다**(디스크·등급
  게이팅용으로만 남아 있음). 상점은 등급을 먼저 뽑고 그 등급의 무기 중에서 고른다
- **좀비 기획서 Ver04(김지수) 전면 반영 완료** (2026-08-10) — 좀비를 2종(좀비/차저)에서
  **6종**(좀비/차저/스피터/스프린터/디스럭터/리더)으로 확장하고, **규격 시스템**을
  신설해 "모든 유닛을 축소 확대 없이 원본 비율대로"라는 요청을 충족했다.
  기획서 규격표(이미지=인게임의 정확히 2배)와 기존 ÷50 환산 규칙을 대조한 결과 **PPU=100·
  Transform 스케일=1**이면 네 규격(소형250/중형350/대형450/초대형800px) 전부 축소·확대
  없이 정확히 맞아떨어진다는 것을 확인했고, 원본 좀비 이미지를 백업 후 실제로 규격 크기로
  축소 저장했다. 몬스터 데이터에는 질량·규격을 유지하고, 공격 방식 4종(근접/투사체/돌진/
  자폭)을 구현했으며, 사용자 추가 지정에 따라 **모든 근접 공격이 "공격 모션(예비 동작) → 판정"
  구조**를 거치도록 바꿔 단순 접촉만으로 데미지가 들어가지 않게 했다. 리더의 무리(팔로워)
  관리(전멸 시 도주)도 구현했으나, **도주는 2026-08-12 사용자 요청으로 삭제**(무리 스폰만 남고
  리더는 끝까지 싸운다). 또한 Player Settings 해상도가 4:3(1024x768)으로 방치돼 있던
  것을 카메라가 가정하는 16:9(FHD 1920x1080)로 맞췄고, 맵 크기(38.4x21.6유닛)가 같은
  ÷50 환산으로 정확히 FHD(1920x1080)와 일치함을 재확인했다. 이후 사용자 요청으로
  **AI 성격값과 좀비 감지범위는 완전히 제거했으며 모든 좀비가 즉시 플레이어를 추적한다.**
  넉백의 질량 반비례 적용은 사용자 지정으로 보류 중이다. 상세는 [작업.md](작업.md)
  2026-08-10 관련 항목 참고
- **좀비 공격 모션 12프레임 적용 완료** (2026-08-10) — 위 항목에서 채팅 첨부라 파일 경로를
  특정할 수 없어 미뤄뒀던 사용자 제작 공격 모션 스프라이트를, 사용자가 제공한 Claude Code
  **세션 export zip**에서 원본 이미지를 복구해 적용했다. `Assets/Resources/ZombieAttack/`에
  12프레임(대기→팔 들어올리기→내려치기→대기 복귀)을 추가하고, **기본 좀비(MonsterId
  200001)** 의 공격 예비 동작·타격·회복 구간에서 실제로 이 프레임들을 재생하도록
  `EnemyUnit.PerformAttackMotion()`을 수정했다. 다른 몬스터(차저 등)는 규격별 스프라이트
  크기가 달라 이 프레임을 그대로 쓰면 몸 크기가 잠깐 바뀌어 보이므로, 기존 색상 텔레그래프
  방식을 그대로 유지한다(전용 프레임이 생기면 확장 가능). 상세는 [작업.md](작업.md)
  2026-08-10 "좀비 공격 모션 12프레임 적용 완료" 항목 참고
- **로봇 외형·애니메이션·무기 이미지 복구 및 좀비 프레임 침범 방지** (2026-08-10) —
  `Ground01`을 디스크 상태로 다시 열어 `Player/ProceduralVisual`의 절차적 보행·구르기 리그와
  좌우 무기 소켓을 복구했다. `ProceduralCharacterRig`는 이제 빌드할 때 항상
  `Resources/Parts/{Body,LegUpper,LegLower,Foot}`를 기준으로 삼고, 런타임에 다른 스프라이트가
  들어와도 다음 프레임에 원래 파츠로 복원한다. 이후 머리에 신발이 보인다는 추가 제보에 따라
  파츠 필드·렌더러 연결까지 경로별로 검증하고 실행 순서가 늦은 `LateUpdate`에서 렌더 직전
  매핑을 다시 확정한다. 좌우 무기도 슬롯별 마지막 정상 스프라이트를 별도로 보관하고 같은
  시점에 복구하므로 파츠·좀비 공격 이미지가 손 렌더러에 들어와도 장착 무기로 돌아온다.
  좀비 공격 프레임은 MonsterId 200001·순수
  `EnemyUnit`·비 Player 태그·자기 루트 렌더러·원본 `Enemy_zombie_S` 조건을 모두 만족할 때만
  재생하며, 공격 코루틴의 매 프레임과 마지막 대입 직전에도 이 조건을 다시 확인한다. 무기 이미지
  로드 실패 시에는 기존 정상 이미지를 지우지 않는다. 상세는
  [작업.md](작업.md) 마지막 항목 참고
- **다음 후보 작업**: 사용자와 우선순위 논의 필요 — **스플래시(로켓런처·유탄발사기) 실측 검증**
  (구현했으나 검증 중 플레이어가 먼저 죽어 확인하지 못함), **`weapon_imgangle` 실측**(13종
  전부 0이라 무기별로 총구 방향이 어긋날 수 있음 — 화면을 보며 보정각을 넣어야 함),
  **무기 밸런스 실플레이 검증**(전부 1차값이며 후반 웨이브를 실제로 돌려보지 않았다),
  보스 체력(8000) 실측 재조정, 전용 아트 도입(보스/차저는 좀비 스프라이트 재활용),
  무기 소켓 개수 확장(씬 에셋 작업 필요). (2026-08-12: 효과 없이 흔하기만 하던 `ItemPickup`
  드랍 경로 자체를 완전히 제거했으므로 "드랍률 조정" 항목은 더 이상 해당 없음)
- **게임 데이터는 `Assets/Data/GameDataAsset.asset`이 유일한 출처다.** 구글 시트 연동
  (`GameDataCsvImporter`, `CsvParser`)은 사용자 지시로 완전히 제거했으므로 다시 추가하지 말 것.
  값 수정은 Unity 인스펙터에서 이 에셋을 직접 편집한다
- **참고**: MCP로 playmode 검증 시 `Application.runInBackground`를 켜두지 않으면 Editor 창이
  백그라운드일 때 게임 시간이 거의 안 흐르는 문제가 있었음 → `GameDataManager.Awake()`에서
  항상 켜도록 코드에 반영함(작업.md Phase 1 참고)
- **참고**: Unity-MCP의 `editor-application-*`, `screenshot-*` 계열 도구가 두 세션 연속으로
  호출 가능한 도구 목록에 올라오지 않았다(Unity 쪽에는 등록돼 있으나 Claude Code에서 로드 불가).
  플레이모드 진입은 `script-execute`로 `EditorApplication.EnterPlaymode()`를 직접 호출하고,
  화면 캡처는 Canvas를 임시로 `ScreenSpaceCamera`로 바꿔 RenderTexture에 렌더하는 우회법을 쓴다.
  이때 **RenderTexture 크기를 Canvas rect와 반드시 같게** 할 것(다르면 UI가 확대돼 잘린 것처럼
  보인다). UI만 정확히 보려면 카메라 cullingMask를 UI 레이어로 제한해 렌더링한다
- **참고**: 플레이모드 중에는 스크립트가 재컴파일되지 않고, 플레이모드 중의 씬 편집은 종료 시
  사라진다. 스크립트·씬 수정은 반드시 에디트모드에서 한 뒤 플레이모드로 검증할 것
- **참고**: Canvas가 `ConstantPixelSize`라 Game View 해상도에 따라 고정 픽셀 폰트가 칸을 넘칠
  수 있다. 새 UI 텍스트는 TMP 자동 크기 조절(`enableAutoSizing`)을 켜는 것을 권장
- **커밋 상태**: 2026-08-07 무기 시스템 전면 교체 작업분은 커밋됨(`ac07a87 [ADD] 아이템`).
  **2026-08-08 절차적 관절 리그 작업분(데모 제작~인게임 Player 적용까지)은 미커밋**
  (스크립트 신규/수정, 파츠 이미지 4장, 데모 씬 1개, `Ground01.unity` 수정, 문서 2개).
  사용자가 커밋을 지시하지 않아 커밋하지 않은 상태다
- **사용자 버그 리포트 6건 수정 완료** (2026-08-12) — (1) 아이템/보상 획득 반경이 플레이어 몸
  반폭의 6배(반지름 2유닛)라 과했던 것을 1유닛으로 축소. (2) PC 이동속도(`robot_speed`=5)와
  (3) 구르기 거리(`dashDistance`=5)는 2026-08-10 이미지 1/2 축소 때 "그림이 아닌 값이라
  손대지 않았던" 항목이 몸 크기 대비 과속 상태로 남아 있던 것 — 각각 2.5로 축소.
  (4) "초록색 선이 충돌체 아니냐"는 문의는 `ProceduralCharacterRig`의 Scene 뷰 전용 디버그
  Gizmo(`OnDrawGizmosSelected`)로, 실제 충돌 판정과 무관함을 확인(코드 수정 없음).
  (5) 신발이 정강이를 완전히 가리던 문제 — `footAnkleAnchor.y` 0.62 → 0.85로 수정, 스크린샷
  실측으로 정강이가 다시 보이는 것을 확인. (6) **좀비가 이동 방향과 반대로 팔을 뻗던 실제
  로직 버그**를 발견해 수정 — `EnemyUnit.LateUpdate()`의 `flipX` 부호가 반대로 걸려 있었다
  (원본 스프라이트가 왼쪽을 향한 자세인데 "오른쪽을 향한 상태"로 잘못 가정). 상세(원인 진단,
  스크린샷 검증 과정)는 [작업.md](작업.md) "2026-08-12 사용자 버그 리포트 6건 수정" 항목 참고.
  **아직 미커밋**(2026-08-08 작업분 위에 이번 수정분도 누적됨)
- **좀비 이동 애니메이션 8프레임 + 타이틀 배경 이미지 적용 완료** (2026-08-12) — 사용자가
  `Assets/Resources/ZombieMove/walk_left_f0~f7.png`(기본 좀비와 동일 규격 250x250)를 제공해
  기본 좀비(MonsterId 200001)가 이동 중일 때 8프레임을 순환 재생하도록
  `EnemyUnit.UpdateWalkAnimation()`을 신설했다(정지·공격 중에는 기존 정지 스프라이트 유지,
  차저 등 다른 규격 몬스터는 미적용). 실제 플레이모드에서 좀비들이 `walk_left_f1`/`f3` 등으로
  프레임이 바뀌는 것을 실시간 확인했다. 또한 `Assets/Resources/UI/titleimage.png`(1672x941,
  16:9)를 `Title.unity`의 `Canvas/Background` `Image`에 연결해 타이틀 화면 배경으로 적용했고,
  임시 RenderTexture 캡처로 제목·부제·버튼이 배경 위에서 잘 보이는 것을 확인했다. 상세는
  [작업.md](작업.md) "2026-08-12 좀비 이동(걷기) 애니메이션 적용 + 타이틀 배경 이미지 적용"
  항목 참고. **아직 미커밋**
- **무기 사거리 전면 상향 완료** (2026-08-12) — 기본 장착 무기(기관단총/레이저피스톨) 사거리가
  화면 세로 반경의 56~59%로 짧다는 지적에 따라, 원거리 10종의 range·detect를 전부 x1.3(30%)
  상향했다(감지거리 최댓값이 상한 5.3 바로 아래 오도록 역산한 배율). 근접 3종은 이번에도
  제외. `WeaponTableGenerator.Generate()` + `WeaponTableValidator.Validate()`로 재생성·검증
  완료(8개 검사 통과). 상세는 [작업.md](작업.md) "2026-08-12 무기 사거리 전면 상향" 참고
- **디스크 시스템 전면 구현 완료** (2026-08-12) — `20260810_디스크기획서_Ver01_김재원.pdf`의
  21종을 반영. 기존에는 "상승 스탯 하나 + 하락 스탯 하나"만 표현하는 placeholder 8종뿐이었으나,
  실제 기획서는 대부분 처치 시 발동·시간제·조건부·확률형이라 `DiscEffectType` enum(15종) +
  범용 파라미터로 데이터 구조를 재설계했다(`DiscData.cs`, `DiscTableGenerator.cs` 신설,
  메뉴 `Comstock/디스크 테이블 21종 재생성`). 처치 이벤트(`EnemyUnit.OnKilledByPlayer` 신설)·
  시간제 버프·주기 효과·아우라·1회성(마지막 발악)·조건부(위장) 전부를 새 `DiscEffectRuntime`
  컴포넌트(Player에 자동 부착)가 실행한다. `Assets/Resources/Discs/디스크01~21.png`를 실제로
  열어 기획서 아이콘 설명과 대조해 매칭하고 Sprite로 임포트했다. 실제 플레이모드에서 7종을
  강제 장착해 처치 연쇄·스택·주기 회복·아우라·1회성 발동까지 실시간 확인(콘솔 Error 0건).
  **상점 UI의 아이콘 시각 표시는 이후(2026-08-12) 완료됨** - 아래 "죽은 드랍 아이템 경로 제거 +
  상점 카드 아이콘 표시" 항목 참고. 등급/가격/% 환산은 전부 밸런스 미확정 임시값. 상세는
  [작업.md](작업.md) "2026-08-12 디스크 시스템 전면 구현" 참고. **아직 미커밋**
- **미해결(보류 중)**: 위 (2)(3)과 별개로, 좀비 공격 사거리(`monster_range`)가 몸 크기의
  2.73배인 문제는 여전히 미해결이다(2026-08-10 "미해결 이슈" 항목 참고 - 좀비 기획서 Ver04
  반영값이라 사용자 확인 없이 진행하지 않음)
- **좀비 투사체 크기/속도 조정 + PC 피격 판정 범위 축소 완료** (2026-08-12) — 스피터(원거리
  좀비) 투사체 크기(`SpitterUnit.projectileVisualSize`) 0.35 → **1.05**(3배), 투사체 속도
  (`projectileSpeed`) 9 → **6.75**(25% 추가 하향, 12→9였던 이전 조정에 이어 재차 하향). PC
  피격 판정(`Ground01.unity` Player `BoxCollider`)은 다리 그림 실측 바운즈(월드 0.603×0.654)와
  대조해 기존 콜라이더(월드 0.657×0.372)가 가로로 9% 더 넓었던 것을 확인하고, 발밑 앵커를
  유지한 채 로컬 `(3, 1.7, 0.01)`→`(2.1, 1.19, 0.01)`로 30% 축소했다(이 콜라이더가
  `EnemyUnit.BodyContactRadius()`의 하한에도 쓰여 좀비 근접 공격 판정 거리도 함께 소폭
  좁아진다). 상세는 [작업.md](작업.md) "2026-08-12 좀비 투사체 크기/속도 조정 + PC 피격
  판정 범위 축소" 참고. **실제 플레이모드 육안 검증은 다음 세션 과제로 남음**
- **몬스터 공격력 조정 + 근접무기 사거리 10% 상향 + AI코어 "사거리 증폭" 업그레이드 + 피격
  피드백 강화 완료** (2026-08-14) — 스피터/차저/디스럭터 공격력을 각각 18/35/75로 재조정.
  근접무기 3종의 감지거리·실제 타격거리(atsize)를 모두 10% 상향. AI 코어 레벨업 선택지에
  무기 사거리+감지거리를 등급별 0.5~2.5%(기존 공용 등급배율 기준) 함께 올리는 신규 옵션
  "사거리 증폭"을 추가. **피격 피드백**은 기존 `HitFlash`(흰색 점멸)뿐이던 것에 **카메라
  흔들림**(`CameraFollow.Shake()`)·**피격 넉백**(공격자 반대 방향, 감쇠형)·**화면 빨간
  비네트**(코드 생성 풀스크린 UI)·**효과음 인프라**(`SFXManager`, 아직 오디오 파일이 없어
  절차적 비프음으로 임시 대체 - 나중에 `Assets/Resources/SFX/Player_Hit.*` 파일만 넣으면
  자동 교체됨)를 추가했다. 넉백 필드/소비는 실행순서 문제 때문에 `PlayerRobotController` 안에
  그대로 두고, 나머지(흔들림/비네트/SFX 트리거)는 `HitFlash`와 같은 자동 부착 패턴의 신규
  컴포넌트 `PlayerHitFeedback`이 전담한다. 플레이모드 리플렉션 실측으로 넉백 방향/세기,
  흔들림 트리거, 비네트 알파, SFX 재생을 전부 확인. 상세는 [작업.md](작업.md) "2026-08-14"
  항목 참고
- **근접무기 칼끝 방향 버그 + 이끼 낀 디스크(주기 회복) 버그 수정 + 디스크 이름 변경 + 머리
  아트 2종 교체 완료** (2026-08-19) — 사용자 리포트 5건. (1) **근접무기 대기 자세가 항상
  칼끝이 반대 방향(그림 원본 기울기, ~140도)을 향하던 버그** 수정 - 조준 중에는
  `weapon_imgangle` 보정을 더했지만 대기(타겟 없음) 분기에서는 빠뜨렸었다
  (`PlayerShootManager.UpdateSlot`). 플레이모드 캡처로 좌/우 소켓 모두 칼끝이 바깥쪽(오른쪽
  소켓=오른쪽)을 향하는 것을 확인. (2) **이끼 낀 디스크(5초마다 HP 2 회복)가 전혀 발동하지
  않던 버그** 수정 - `DiscEffectRuntime`의 주기 회복 타이머가 "다음 발동 시각"을 지역 변수로만
  계산하고 캐시 딕셔너리에 저장하지 않아, 매 프레임 "지금+5초"로 다시 계산되는 바람에
  `Time.time >= next` 조건이 영원히 참이 될 수 없었다(자기 자신보다 미래 시각과 비교). 최초
  계산 시 바로 저장하도록 고쳤다. 같은 파일의 나머지 20종 디스크 효과를 전수 점검했으나 이
  하나만 버그였다(`OscillatingAtkDef`는 초기값이 `Time.time` 그 자체라 우연히 첫 프레임에
  발동해 정상 동작). 플레이모드 리플렉션으로 타이머가 정상적으로 재무장되는 것을 확인.
  (3) "나무 뭐시기 디스크" → **"숲의 소리 디스크"**로 개명(`DiscTableGenerator` 수정 후
  `Comstock/디스크 테이블 21종 재생성` 재실행). (4) 사용자가 제공한 `새머리/` 폴더의 신규
  아트로 **가드맨·메테우스 머리 아트를 임시 아트에서 정식 아트로 교체**(둘 다 250x250,
  같은 파일명 그대로 덮어써 코드 수정 불필요 - "미구현" 목록에서 항목 제거).
  (5) **골드가 0에 고정되는 버그는 재현하지 못했다** - 아래 참고.

- **골드 버그 원인 규명·수정 + 밸런스 3건 완료** (2026-08-19) — 아래 "재현 실패"로 남겨뒀던
  골드 버그의 **진짜 원인이 정수 반올림**임을 실측으로 확정하고 고쳤다(씬 재시작과 무관한
  순수 산술 버그였다). 골드 획득량은 대부분 **1~3**(좀비 1 / 차저 3 / 리더 10)인데, 여기에
  배율을 곱한 뒤 곧바로 `Mathf.RoundToInt`로 잘라서 배율이 통째로 사라지고 있었다.
  `Mathf.RoundToInt`가 **은행가 반올림**이라 0.5가 1이 아니라 **0**으로 내려가는 것이 결정타 —
  **미니 픽시 머리(-50%)로 플레이하면 골드 1짜리가 0이 되어 1~5웨이브(기본 좀비만 등장)
  내내 골드가 전혀 오르지 않았다.** 금화의 잔향 디스크(+10%)도 골드 1~3에서 증가분이 완전히
  소멸했다. 경험치 경로에는 `Max(1,...)` 가드가 있었는데 **골드에만 없던 비대칭**이 직접
  원인이다. `Max(1,...)`로 막으면 0은 피해도 +10%는 여전히 소멸하므로, **소수점 나머지를 다음
  획득으로 이월하는 누적기**(`RunState.AddGoldWithFraction`)를 신설해 기대값을 정확히 보존했다
  (미니픽시 -50%면 픽업 2번마다 정확히 1골드). 함께 처리한 밸런스 3건: **(a) 웨이브당 체력·
  공격력 증가율 5% → 2.5%**(웨이브 20 배율 x2.53 → x1.60) + **스폰 가중 추첨 신설** — 예전엔
  균등 추첨이라 상위 몬스터가 전부 해금되면 일반 좀비가 1/6(16.7%)까지 밀렸는데, 기본 좀비
  비중 2 / 상위 몬스터 0.5로 바꿔 **좀비 16.7% → 44.5%**(6만 회 실측), 상위 5종 각 11%가 됐다.
  **(b) 정비 화면에서 장착된 파츠 설명 조회 가능** — 슬롯 클릭이 "교체"만 담당해 인벤토리
  선택이 없으면 안내 문구만 나오고 끝나서 **이미 낀 파츠의 능력치를 볼 방법이 아예 없었다.**
  인벤토리 선택이 없을 때만 슬롯 클릭이 조회로 동작하는 모드를 신설했다(선택이 있으면 기존
  교체가 그대로 우선). **(c) 총기 사거리 15% 상향**(원거리 10종만, 근접 3종 제외) + AI 코어
  `WeaponRangeBonus` 0.5 → 1.5. 이때 **감지거리 상한(`max_detect_range`) 5.3이 상향분을 조용히
  잘라낼 뻔한 것을 발견해 6.1로 함께 올렸다**(대물저격총 감지 5.98). 상세는
  [작업.md](작업.md) 마지막 항목 참고

- **동시 생존 상한에 막혀도 3초에 1마리는 계속 스폰 완료** (2026-08-19) — 사용자 요청.
  `SpawnLoop`이 `maxAliveEnemies`(18)에 걸리면 **스폰이 완전히 멈춰** 플레이어가 적을 줄이기
  전까지 전투 압박이 뚝 끊기는 구간이 있었다. `trickleSpawnInterval`(기본 3초)과 **별도 코루틴**
  `TrickleSpawnLoop()`을 신설해 포화 상태에서도 3초마다 1마리를 계속 내보낸다.
  **별도 코루틴인 이유**는 `SpawnLoop` 안에 두면 주기가 `spawnInterval`(웨이브마다 `WaveManager`가
  바꾼다)에 끌려가 "3초에 1마리"가 흔들리기 때문이다. **가중 추첨을 쓰지 않고 기본 좀비만 뽑는
  것**이 핵심 - 이 경로는 상한을 무시하는 유일한 정규 경로라 상위 몬스터가 뽑히면 차저·부머가
  상한 위로 쌓이고, 특히 **리더가 뽑히면 팔로워 3마리까지 함께 우회해 한 번에 4마리**가 추가된다.
  정비/상점·게임오버 중에는 멈춘다(`Time.timeScale=0`이라 이중으로 안전). 실측 검증: 포화
  상태에서 25.97초 동안 +9마리(기대 8.66)로 **정확히 3초당 1마리**, 전부 일반 좀비였다.
  상세는 [작업.md](작업.md) 마지막 항목 참고
- **Phase E(해금·도감) 완료** (2026-08-19) — `해금 기획서 Ver01` 반영으로 **기획서 4종 계획의
  Phase A~E가 전부 끝났다.** 머리 12종 + 디스크 21종 + 악세사리 6종(맵은 아트 부재로 제외)에
  해금 조건을 걸고, 신규 4개 계층으로 나눠 만들었다 — `UnlockData`(조건표 39줄) /
  `UnlockState`(PlayerPrefs 저장) / `UnlockTracker`(게임 이벤트 → 진행도 단일 지점) /
  `CollectionPanelUI`(타이틀의 도감 화면). **초기 해금은 8개**(컴스톡 + 디스크 7종)다.
  진행도는 **누적 / 최고 기록 / 서로 다른 것의 개수** 세 종류이며, 진행도와 해금 여부를 **둘 다**
  저장한다(진행도만 저장하면 목표치를 올릴 때 해금이 도로 잠기고, 해금 여부만 저장하면 `0 / 500`
  진행 표시를 만들 수 없다). **"무슨 무기로 죽였나"를 알아야 하는 조건 2건**(픽시=근접 300 /
  프라이빗 컴스톡=정밀 200) 때문에 `EnemyUnit.TakeDamage`에 `source_weapon_id`(기본값 0)를 늘려
  `Projectile`·`BeamProjectile`·`MeleeSwing` 전 경로에 무기 ID를 실었다. **웨이브 종료 진행도는
  체력 회복 전에 기록해야 한다**(조건이 "체력 30 이하로 클리어"인데 웨이브를 넘길 때 전체 회복이
  일어난다 → `WaveManager.EndWave` 안, `OnWaveEnded` 이전이 유일하게 맞는 자리).
  **잠긴 디스크·악세사리는 상점 추첨에서 제외**되며(이게 없으면 해금이 도감 표시일 뿐 게임에
  영향이 없다), 악세사리는 하나도 해금되지 않으면 그 칸이 무기/디스크가 된다(교착 없음 —
  선글라스가 "엔드리스 상점에서 1회 구매"라 아무 물건이나 사면 열린다). 도감·머리 선택 화면의
  잠긴 항목은 **실루엣 + 자물쇠 + `???`** 이고, 도감에서는 클릭해 해금 조건을 읽을 수 있다.
  **`MonoBehaviour`가 아닌 static 매니저의 저장 누락 2건**(게임 오버·게임 클리어 시 `Flush` 없음)도
  함께 고쳤다. 검증은 전부 실측(20,000회 추첨 4세트로 상점 차단, 훅 9종 실제 오브젝트 실행,
  캔버스 픽셀 좌표로 레이아웃 겹침 확인, 화면 4종 캡처). 상세는 [작업.md](작업.md) 마지막 항목 참고

### (해결됨) 조사했으나 재현 실패했던 "타이틀로 돌아간 뒤 골드가 안 오르는 버그" (2026-08-19)

> **2026-08-19 해결**: 아래는 씬 재시작 경로를 의심하고 조사했던 기록이다. 실제 원인은
> 재시작과 무관한 **정수 반올림**(미니 픽시 머리 -50% + 골드 1짜리 픽업 → `RoundToInt(0.5)`=0)
> 이었다. 위 항목과 작업.md 마지막 항목 참고. 아래 조사 내용은 "이 경로는 정상이다"라는
> 근거로 남겨둔다.

플레이모드에서 실제 코드 경로 그대로 재현을 시도했다: `RunState.Reset()`/`GameOverManager.Reset()`
등은 `PlayerRobotController.Awake()`에서만 호출되므로, ①골드 획득 → ②`GameOverManager.
TriggerGameOver()` → ③`SceneManager.LoadScene("Title")` → ④`SceneManager.LoadScene("Ground01")`
(재시작) → ⑤골드 재획득(직접 호출 + 실제 트리거 콜라이더 접촉 두 경로 모두) 순서로 **총 3회
반복**했다. 매번 `RunState.Gold`가 정확히 0으로 리셋된 뒤 정상적으로 누적됐다(예:
0→41→79→117 등, 트리거 접촉 기반 자동 습득도 포함). `IsGameOver`/`Time.timeScale`도 매번
정상 복구됨을 확인했다. 즉 **현재 코드에서는 이 버그가 재현되지 않는다** - 과거
"static 이벤트를 가진 매니저의 Reset()이 이벤트를 비워서 구독이 끊기는" 패턴(이 문서
"주의사항" 섹션 참고)이 원인이었을 가능성이 높고, 그 패턴은 이미 수정되어 있다(`RunState.
Reset()`/`GameOverManager.Reset()`/`GameWinManager.Reset()` 전부 이벤트를 비우지 않음).
**사용자 재확인 필요**: 계속 재현되면 (a) 정확히 어느 화면에서 0으로 보이는지(HUD 좌상단 /
상점 / 정비 중 어디), (b) 게임오버로 끝났는지 20웨이브 승리로 끝났는지, (c) 타이틀에서
바로 재시작했는지 여러 번 오간 뒤인지를 알려주면 그 조건으로 다시 재현을 시도한다.

- **Firebase Realtime Database 랭킹 연동 + 랭킹 화면 신설 완료** (2026-08-20) — 예고했던 대로
  `LeaderboardService.Current`를 로컬 저장에서 `FirebaseLeaderboardService`(REST 방식,
  SDK 미사용)로 교체했다. 그동안 없었던 **랭킹 조회 화면**(`RankingPanelUI`)을 새로 만들어
  일시정지 메뉴·게임오버 요약 화면·타이틀 우하단 버튼 세 곳에서 열 수 있게 했고, 타이틀 하단의
  음악 볼륨 슬라이더는 "설정" 버튼(기존 `SettingsPanelUI` 재사용)으로 교체했다. Realtime
  Database 규칙에 `.indexOn`이 빠지면 정렬 조회가 400 에러로 조용히 실패해 "기록 없음"처럼
  보이는 함정을 겪었다 - 상세는 [작업.md](작업.md) "Firebase Realtime Database 랭킹 연동" 항목
- **랭킹 맵별 분리 완료** (2026-08-20) — 사용자가 "앞으로 맵 여러개 추가할거임"이라고 밝혀서,
  랭킹 저장/조회에 `mapId`(=씬 이름)를 추가해 맵마다 독립된 랭킹을 갖게 했다. 새 맵 카탈로그를
  만들지 않고 **씬 이름을 그대로 mapId로 쓰므로** 새 맵 씬을 추가해도 랭킹 코드는 그대로다.
  타이틀 화면은 "다음에 플레이할 맵"(`TitleSceneManager.nextSceneName`)의 랭킹을 보여준다 -
  맵이 여러 개가 되면 그때 타이틀에 맵 선택 UI를 추가해야 한다(아직 없음). 상세는
  [작업.md](작업.md) "랭킹 맵별 분리" 항목
- **기본점수 1200점 버그 수정 + 디스럭터 자폭 지연 + 랭킹 닉네임 입력 + 타이틀/빅토리 화면 수정**
  (2026-08-20) — (1) `RunScore`가 웨이브/AI코어 레벨의 "현재 값"을 "진행도"처럼 곱해 시작하자마자
  1200점이 붙던 버그 수정(둘 다 1을 뺀 값을 곱함). (2) 디스럭터가 자폭 계기(큰 피해/사거리 도달)
  즉시 터지지 않고 **2초간 깜빡이며 무적 상태**로 대기하다 터지도록 변경(`DisruptorUnit.cs`).
  (3) 랭킹 제출 직전 닉네임 입력 팝업 신설(`NicknameInputPopup.cs`, 로봇 이름으로 미리 채워짐,
  마지막 닉네임은 PlayerPrefs에 저장). (4) 타이틀 화면에 사용자 제공 로고 이미지 적용 - **처음에
  전체화면 배경으로 잘못 늘려 넣었다가 사용자 지적으로 수정**, 기존 텍스트 제목 자리에 비율
  유지 로고로 교체(`TitleLogo`). (5) 빅토리 화면에서 화면 전체를 가리던 75% 암막 제거
  (`ScoreSummaryPopup.cs`). 상세는 [작업.md](작업.md) 마지막 항목 참고
- **파츠 25종 전면 교체 + 스탯 소수화 + 골드 아이콘 통일 + 누적형 디스크 표시 완료**
  (2026-08-20) — 헬멧 6·다리장갑 4·무기소켓 7·메모리 3·디스크슬롯 5(총 25종)로 파츠를
  전면 교체하고, 스탯 표시에 소수점을 노출하며, UI 골드 아이콘을 인게임 금화 아이콘으로
  통일, 누적형 디스크의 초록 글씨를 "현재 누적 총량"으로 바꿨다. 상세는
  [작업.md](작업.md) "파츠 25종 전면 교체" 항목
- **한글 폰트 아틀라스 글리프 누락 수정 완료** (2026-08-20) — `NotoSansKR-Regular SDF`가
  Dynamic 폰트로 그동안 구워온 글리프가 1024x1024 한 장을 넘겨 2번째 텍스처로 넘어가면서
  일부 글자(예: "상점"→"점")가 그림 없이 렌더되던 버그를 고쳤다. 아틀라스 크기를 2048로
  올리고 동적 데이터를 비우는 에디터 메뉴(`Comstock/한글 폰트 아틀라스 초기화 (2048)`)를
  신설했다 - 재발 시 이 메뉴로 대응한다. 상세는 [작업.md](작업.md) 해당 항목 참고
- **부품 상자 드랍 크기 2배 + 맵 밖으로 나가는 버그 수정 완료** (2026-08-20) — 부품 상자만
  골드·경험치와 분리해 시각 크기를 2배로 키우고, 맵 경계 근처에서 죽은 몬스터의 보상이
  스폰 시 산개(scatter)로 맵 밖까지 밀려나던 버그를 `MapBounds.ClampPosition()` 재사용으로
  고쳤다. 상세는 [작업.md](작업.md) 해당 항목 참고
- **데미지 숫자 팝업(치명타 아이콘 포함) + VICTORY 화면 완전 제거 완료** (2026-08-20) —
  신규 `DamageNumberUI.cs`로 피격 시 데미지 숫자가 뜨는 팝업을 추가하고(치명타는 별도
  아이콘), 무한모드에서 클릭을 영구히 가로채던 구 VICTORY 오브젝트 관련 코드
  (`GameHUD.victoryObject`/`HandleGameWon` 등)를 `GameHUD.cs`에서 전부 제거했다(승리 시에는
  이제 `ScoreSummaryPopup`만 뜬다). 이어서 데미지 숫자에 화면 픽셀 단위(1~10px) 랜덤
  오프셋과, 위로 떠오르던 것 대신 좌우 한쪽으로 흐르며 중력 낙하하는 애니메이션으로
  교체했다. 상세는 [작업.md](작업.md) 해당 두 항목 참고
- **타이틀 설정 버튼을 씬 오브젝트로 전환 완료** (2026-08-20) — 코드로 매 실행 생성돼
  게임시작/종료 버튼과 크기가 안 맞고 에디트 모드 하이라키에 안 보이던 설정 버튼을,
  `QuitButton`을 복제한 고정 씬 오브젝트(`SettingsButton`)로 바꿔 세 버튼이 동일한
  크기·앵커 방식으로 정렬되게 했다. 상세는 [작업.md](작업.md) 해당 항목 참고
- **도감에 무기 13종 추가 완료** (2026-08-20) — 해금 조건 없이(전부 초기 해금) 무기 13종을
  도감에 추가했다. 무기 탭 상세 패널은 다른 탭과 달리 해금 조건 문구 대신 무기 데이터
  원본값(공격력·공격속도·사거리 등)을 보여준다(`CollectionPanelUI.BuildWeaponSpecText`).
  상세는 [작업.md](작업.md) 해당 항목 참고
- **몬스터 5종(스피터/스프린터/디스럭터/리더/보스) 이동 애니메이션 적용 완료** (2026-08-20) —
  기본 좀비 전용이던 8프레임 걷기 애니메이션 로직을 신규 `MonsterAnimationLibrary.cs`로
  일반화해, 사용자가 제공한 나머지 5종의 8프레임 세트를 몬스터ID 단위로 재생한다(프레임 FPS·
  정지 프레임·"제자리에서도 재생할지"를 세트별로 지정). **보스는 이번에 처음 전용 아트
  ("좀비 군집체")를 받아 좀비 스프라이트 재활용 임시 비주얼에서 벗어났다**(제자리 꿈틀거림
  모션 포함, 콜라이더·스케일도 재조정). **차저만 애니메이션 미제공으로 여전히 정지
  스프라이트**다. 상세는 [작업.md](작업.md) "몬스터 5종 이동 애니메이션 적용" 항목
  (⚠ 이 항목은 커밋 당시 기록이 누락되어 2026-08-21에 diff 기반으로 사후 작성됨 - 실제
  플레이 검증이 아직 없으니 다음 세션에서 5종 모션을 캡처로 확인할 것)
- **웨이브 종료 시 데미지 숫자/보스 텔레그래프 잔존 버그 수정 완료** (2026-08-21) —
  `DamageNumberUI`/`HitFlash`가 `Time.deltaTime` 기반이라 정비 화면 진입 시(`timeScale=0`)
  얼어붙은 채 화면에 남던 문제를, `PlayerHitFeedback`이 쓰던 "런타임 생성 오브젝트는
  `GameFlowManager.IsIntermission`을 스스로 체크" 관례로 통일해 고쳤다. 별도로,
  보스의 광역 공격 경고 원(텔레그래프)이 **보스 자식이 아닌 독립 오브젝트라 웨이브 종료
  시 보스가 `Die()` 없이 그냥 `Destroy()`되면 코루틴이 끊겨 영구히 남는** 진짜 누수도
  발견해 `BossUnit.OnDestroy()`에서 안전망으로 정리하도록 고쳤다(`EnemyUnit.OnDestroy()`를
  `protected virtual`로 개방). 상세는 [작업.md](작업.md) 해당 항목 참고
- **행운(Luck) 등급 확률 보정 1/10 완화 완료** (2026-08-21) — 사용자 지적("행운 조금만
  올려도 고등급이 미친듯이 많이 나온다")으로 `LuckBonus.PerTenLuckPerGrade`를
  0.01(행운 10당 +1%)에서 0.001(행운 10당 +0.1%)로 낮췄다. 상점 무기/디스크 등급
  (`ShopCatalog.RollGrade`)과 부품 상자 등급(`PartsCatalog.RollBoxGrade`) 둘 다에
  적용된다. 상세는 [작업.md](작업.md) 해당 항목 참고
- **리더 좀비 개체수 제한 + 무리 동반 기믹 삭제 + 회복 지원기 대체 + 체력 상향 완료**
  (2026-08-21) — **20웨이브 미만에서는 리더가 웨이브당 최대 3마리**만 등장하도록
  `EnemySpawner`에 상한을 뒀고, 상한에 걸리면 그 자리를 리더를 뺀 나머지 후보로 즉시
  재추첨해 자동으로 다른 좀비가 채운다(별도 로직 없이 "리더 대신 다른 좀비를 더 많이"가
  달성됨). **팔로워 동반 스폰 기믹은 완전히 삭제**했다(`EnemySpawner.SpawnLeaderPack`
  제거 - 이제 어떤 몬스터도 다른 몬스터를 대동하지 않는다). 대신 리더는 **3초마다 반경
  6유닛 안의 좀비 최대 3마리를 최대체력의 20%씩 회복**시키는 지원형 능력을 갖는다
  (`LeaderUnit.cs` 재작성, `EnemyUnit.Heal()` 신설). 등장 빈도 감소를 보완하려 체력도
  343 → 700으로 올렸다. 상세는 [작업.md](작업.md) 해당 항목 참고
- **웨이브 전환 / 보스 등장 알림 배너 신설 완료** (2026-08-21) — `UI 기획서.pdf` 시안 반영.
  웨이브가 시작될 때마다(1웨이브 포함) 짧게 정지하며 "WAVE 08" 배너가, 보스 웨이브(엔드리스
  포함 20·39·58...웨이브)면 "BOSS INCOMING" 경고 배너가 뜬다. 신규
  `WaveTransitionBannerUI.cs`(순수 표시)를 `GameFlowManager`가 `WaveManager.OnWaveStarted`
  구독으로 직접 구동한다(`Time.timeScale` 전환은 이 프로젝트 관례대로 GameFlowManager에서만
  일어남). 상세는 [작업.md](작업.md) 해당 항목 참고
- **디스럭터 자폭 폭발 애니메이션 적용 완료** (2026-08-21) — 사용자 제공 8프레임
  이미지를 `Assets/Resources/DisruptorExplosion`으로 임포트하고, 신규
  `DisruptorExplosionEffect.cs`로 디스럭터 자폭 시에만 재생한다(로켓런처 등 다른 폭발형
  무기의 스플래시 연출은 그대로 유지 - 별개 코드). 그동안 디스럭터 자폭에는 시각 효과가
  전혀 없었다. 상세는 [작업.md](작업.md) 해당 항목 참고
- **인벤토리/슬롯 칸 - 안쪽 사각형 레이어 완전 제거 완료** (2026-08-21) — 상점·정비 화면의
  모든 칸이 공유하는 `ItemCellUI.CreateShell()`이 테두리 아트 안쪽에 **등급/강조색으로
  채운 별도 사각형 레이어**를 끼우고 그 위에 캡션·아이콘을 그리는 구조였다 - 색을
  옅게(25% 블렌드) 낮춰봐도 "사각형이 하나 더 있다"는 문제 자체는 그대로였다(사용자가
  테두리 아트 원본 스크린샷과 인게임 "머리" 칸 스크린샷을 나란히 제시하며 재지적:
  "1번 파일 UI 위에 바로 글씨 쓰고 이미지 넣어야 하는데 회색 사각형을 덮어버린다").
  안쪽 사각형을 **완전히 삭제**하고, 캡션·아이콘이 테두리 아트(`UI/Black_ui03`) 위에
  직접 그려지도록 바꿨다. 등급/강조색은 안쪽을 전혀 덮지 않는 속 빈 테두리 링(AccentRing,
  `UiIconLibrary.Frame()` 재사용)으로만 표현한다. 상점(`ShopPanelUI`)·정비
  (`ModdingPanelUI`)가 전부 이 컴포넌트를 공유해 한 번의 수정으로 양쪽 다 해결됐다.
  상세는 [작업.md](작업.md) 해당 항목 참고
- **UI 텍스트/테두리 겹침 전수조사 완료** (2026-08-21) — 위 항목 수정 후 사용자 요청으로
  "글씨가 두꺼운 테두리를 벗어나지 않게" 전체 UI를 검수했다. 코드 전체에서 텍스트 anchor가
  0에 가깝거나(<0.04) 1에 가까운(>0.96) 지점을 추려 테두리 아트가 있는 것만 Play 모드
  캡처로 확인 - **실제로 겹치던 것 2건을 추가로 발견해 수정**했다: 정비 화면
  "인벤토리"/"장착 파츠" 배너(뾰족한 육각형 테두리 왼쪽 꼭짓점에 첫 글자가 닿음 -
  `Ground01.unity` 씬의 텍스트 anchor 조정), "파츠 설명" 제목(위쪽 여백 0.5%뿐이라 글자가
  천장에 닿음 - `ModdingPanelUI.cs`의 anchor·글자 크기 조정). 그 외 일시정지 메뉴·환경설정·
  랭킹·AI 코어 업그레이드·웨이브 전환 배너·도감·머리 선택·상점 전체는 전부 확인 결과
  정상이었다. 상세는 [작업.md](작업.md) 해당 항목들 참고

## 게임 개요

- **게임 제목**: 컴스톡
- **장르**: 웨이브 서바이벌, 뱀서라이크(뱀파이어 서바이벌류), 로그라이크, 모딩
- **핵심 컨셉**: 플레이어는 로봇을 조작하며, 로봇의 파츠(무기 등)를 교체하고 아이템을
  갈아끼우면서 좀비들을 처치해 나가는 게임
- **레퍼런스**: 브로테이토(Brotato) — 기본 시스템은 브로테이토와 거의 동일하다고 보면 됨

### 확정된 방향성 (기획서 `20260805_시스템업데이트_Ver02_김지수.pdf`, 2026-08-06 확정)

- **익스트랙션 장르 포기 → 자동공격 뱀서라이크로 전환**
- 인게임 흐름: `웨이브 전투 → AI 코어 업그레이드 → 로봇 정비(모딩) → 상점 → 다음 웨이브`
- 덱빌딩 4요소(MECE): **AI 코어**(경험치·레벨업 3택) / **전투 무기**(상점 구매, 레벨·모딩
  없음, 종류=수평·등급=수직 강화) / **디스크**(스탯 상승 + 다른 스탯 하락 리스크) /
  **로봇**(= 머리 파츠 종류, 소켓 수·타입·메모리·디스크 슬롯·필살기 슬롯을 결정하며 런 중 고정)
- **탄약·재장전 개념 없음** — 공격속도 쿨다운만으로 발사 제어
- **게임 데이터는 로컬 에셋** — 외부(구글 시트 등) 연동 없음(2026-08-06 Phase 7에서 완전 제거)
- 필살기는 데모 범위 외(슬롯만 노출)

### 추가 확정 (컨셉기획서 `20260806_컨셉기획서_Ver01_오승명.pdf`, 2026-08-06)

위 기획서와 충돌하는 항목은 **이쪽이 최신 확정본**이다.

- **총 20웨이브 · 웨이브당 약 1분 · 20웨이브에 보스 등장**(이전 "10웨이브 + 보스"를 대체).
  보스와 잡몹이 동시에 등장하고, 20웨이브 종료 시에는 정비 시간을 주지 않는다
- **Space = 구르기**(회피 대시) 조작 추가
- 승리 = 20웨이브 보스 처치 / 패배 = 플레이어 체력 0
- 등급 5단계(일반→레어→에픽→유니크→레전더리)를 무기·디스크·레벨업 증강·로봇 파츠에 부여
- 톤&무드: 셀 셰이딩 / 인디 카툰 / SD, 키워드는 단순화·미니멀·캐주얼
- 아웃게임(캐릭터 선택, 로봇/무기/디스크 해금 정보 관리)은 별도 화면 — **머리 선택 화면과
  도감(해금)은 2026-08-19에 구현**됐고 랭킹 화면만 남았다(아래 "미구현" 항목 참고)

## 협업 / 코딩 에이전트 기본 규칙

1. 사용자의 요구사항이 명확하지 않다고 판단되는 경우, 임의로 진행하지 말고 요구사항을
   구체화하기 위한 질문을 한다.
2. 사용자가 수정을 요청하지 않고 단순히 질문하거나 의견을 묻는 경우에는 임의로 코드나
   파일을 수정하지 않는다.
3. 모든 수정사항과 작업 계획은 즉시 [작업.md](작업.md) 파일에 기재한다.
4. [작업.md](작업.md)에 이전에 기재된 내용은 지우거나 수정하지 않는다. 새 내용은 항상
   문서 맨 아래에 이어서 추가한다(append-only).
5. 모든 문서/커밋 메시지/커뮤니케이션은 한글로 작성한다.
6. 되돌리기 어려운 작업(파일/에셋 영구 삭제, git 강제 push, 히스토리 재작성, 프로젝트
   설정 변경 등)은 실행 전 반드시 사용자에게 확인한다.
7. 코드 변경 후에는 가능한 범위에서 실제 동작(플레이 테스트, 빌드, MCP 도구를 통한
   씬/게임오브젝트 확인 등)을 검증하고, 검증 결과를 작업.md에 함께 기록한다.
8. 이 문서(프로젝트 안내.md)는 프로젝트 구조·시스템·방향성이 바뀔 때 주기적으로 정리하여
   최신 내용으로 유지한다(불필요하게 길어지지 않도록 오래된 세부사항은 요약/정리).

## UI 제작 규칙

1. **배경/프레임 아트의 두꺼운 띠(테두리로 간주되는 부분)와 글자가 겹치면 안 된다.** 캡션·
   라벨을 배치할 때 그 여백을 임의의 숫자(예: 6%)로 정하지 말고, 실제 아트의 진짜 테두리
   두께를 기준으로 삼는다 - 배경이 9-slice 스프라이트라면 `Sprite.border`(및 텍스처 크기)에서
   테두리 비율을 직접 구해서 그만큼 안쪽에만 글자를 놓는다(2026-08-25, `ItemCellUI`의 캡션이
   `UI/Black_ui03`의 실제 테두리 22.6%보다 훨씬 좁은 6% 여백만 두어 테두리와 겹쳤던 문제를
   겪었다 - `ItemCellUI.GetSafeContentInset()` 참고). 등급색 테두리 링도 같은 원칙-
   `UiIconLibrary.DeriveEdgeRing()`처럼 실제 아트의 테두리 두께·모양을 그대로 따라 만들어야
   하고, 캔버스/리소스 크기 기준으로 임의의 사각형을 그리면 안 된다.

2. **글자에 `overflowMode = Overflow`를 쓰지 마라.** 이 모드에서는 자동 크기 조절이 <b>세로
   맞춤을 포기</b>해서, 줄 수가 늘어나면 글자가 작아지는 대신 그냥 패널 밖으로 흘러나간다.
   `Ellipsis`(또는 `Truncate`)로 두면 자동 조절이 칸 안에 들어올 때까지 줄여준다
   (2026-08-25 - 한글일 땐 짧아서 안 드러났다가 영어로 바꾸자 정비 화면 5곳이 한꺼번에 터졌다).
3. **제목·수치처럼 한 줄이어야 하는 글자는 `NoWrap`으로 둔다.** 줄바꿈을 켜둔 채 폭만 좁히면
   두 줄로 접혀 오히려 위아래로 삐져나온다. 한 줄로 두면 자동 조절이 폭에 맞춰 크기만 줄인다.
4. **늘여 쓰는 패널의 안전 여백은 `UiSafeArea`로 구한다.** 9-slice 코너는 rect 크기와 무관하게
   항상 `border / pixelsPerUnitMultiplier` 픽셀로 그려지므로, 안전 비율은 "텍스처 안에서의
   border 비율"이 아니라 **실제 렌더 픽셀 ÷ 실제 rect 크기**다. 예: `Purple_ui02`(폭 982px,
   border 75px)를 1804px로 늘리면 뾰족한 끝은 4.2%이지 텍스처 비율 7.6%가 아니다.
   (`ItemCellUI.GetSafeContentInset()`은 아이템 칸처럼 아트와 렌더 크기가 비슷할 때만 맞다.)
5. **칸 경계에 기대어 간격을 만들지 마라.** 짧은 한글에서는 칸이 남아 저절로 간격이 생기지만
   긴 번역에서는 옆 칸 글자와 맞붙어 한 문장처럼 읽힌다. 여백은 `margin`으로 명시한다.
   런타임에 여백을 더하는 코드는 **여러 번 불려도 누적되지 않게** 해야 한다(정비 화면은
   웨이브마다 열린다 - `ModdingPanelUI.text_gutters_applied` 참고).

## 다국어 (로컬라이제이션)

2026-08-25 도입. 한국어(기준) + 영어. 관련 파일은 `Loc.cs` / `LocalizedText.cs` /
`DataNames.cs` / `LanguageSelectPanelUI.cs` / `Assets/Resources/Localization/strings_*.json`.

1. **화면에 보이는 문구는 `Loc.T("key")`를 거친다.** 서식이 필요하면 `Loc.T("key", 값1, 값2)`이고
   번역문의 `{0}`,`{1}` 자리에 들어간다. **주석과 `Debug.Log` 문자열은 번역하지 않는다**(한글 유지).
2. **폴백은 3단이다: 현재 언어 → 한국어 → 키 문자열 그대로.** 마지막 단계에서 키가 화면에 그대로
   보이는 것은 의도한 것이다 - 번역 누락을 빈칸으로 삼키지 않고 즉시 눈에 띄게 한다.
   **화면에 `modding.title` 같은 글자가 보이면 그 키가 JSON에 없다는 뜻이다.**
3. **데이터 에셋의 이름·설명은 `DataNames`의 확장 메서드를 거친다**(`weapon.Weapon()`,
   `part.Part()`, `disc.Disc()`, `disc.DiscEffect()` 등). ID로 만든 키를 번역 파일에서 찾고,
   없으면 **에셋의 한글 원문**을 쓴다. 그래서 **한국어 번역 파일에는 데이터 키를 넣지 않는다** -
   넣으면 에셋과 JSON을 이중 관리하게 되어 한쪽만 고치는 사고가 난다.
4. **언어를 추가하는 방법**: (1) `strings_<코드>.json` 추가, (2) `Loc.Supported`에 한 줄 추가.
   끝이다. 언어 선택 화면의 버튼도 `Loc.Supported`를 순회해 만들어지므로 UI 코드는 손대지 않는다.
   **라틴 문자권이 아닌 언어를 넣으면 TMP 전역 폴백에 그 언어 폰트를 추가해야 한다**(Orbitron과
   NotoSansKR 둘 다 가나·간체를 갖고 있지 않다).
5. **`LocalizedText` 컴포넌트는 씬에 미리 글자가 박힌 정적 라벨에만 붙인다.** 코드가 런타임에
   덮어쓰는 placeholder(`품목`/`잠금`/`소켓 N`/`인벤토리 0/20` 등)에 붙이면 서로 덮어쓰며 깜빡인다.
6. **JSON을 고쳤으면 `assets-refresh`를 해야 한다.** Resources의 TextAsset은 임포트된 사본을
   읽으므로 파일만 고치고 플레이하면 옛 문자열이 나온다.
7. **언어 전환은 타이틀에서만 일어난다는 전제**다(사용자 확정). 인게임 패널은 만들어질 때 문구를
   채우고 `Loc.OnLanguageChanged`를 구독하지 않는다. 설정 패널에도 언어 선택을 넣게 되면 그
   패널들에 구독을 추가해야 한다.
8. `Loc.OnLanguageChanged`는 static 이벤트지만 **어디서도 null로 밀지 않는다**(아래 주의사항의
   "static 이벤트를 Reset()에서 비우지 마라" 참고). 구독자가 각자 해제한다.

## 구현 현황

**목표 장르와 실제 구현 상태가 다릅니다. 아래 구분을 반드시 확인하고, 미구현 기능을
이미 존재하는 것으로 전제하지 마세요.**

### 구현 완료

- 플레이어 로봇 이동 (Rigidbody 기반, WASD) + **구르기(Space)** — 짧은 고속 이동 + 무적,
  쿨다운 있음. **다리를 숨기고 몸통이 0→360도 회전 + 호핑하는 실제 구르기 모션**이
  있으며(`ProceduralCharacterRig.SetRoll()`), 구르는 동안 무기도 머리 위로 이동해 같은
  각도로 함께 돌고 조준·발사가 완전히 멈춘다(패널티)
- 무기/사격 시스템 (소켓별 독립 쿨다운 **자동공격** — 사거리 내 최근접 적 자동 조준·발사,
  탄약/재장전 없음, 크리티컬, 투사체). 무기 거리 데이터는 **÷100 환산**(= 기획 수치 1 =
  화면 1픽셀)이며 최대 사거리 7.5 / 최대 감지거리 4.0이라 **교전이 항상 화면 안에서
  일어난다**(감지 상한 5.3은 이제 아무 무기도 자르지 않는 안전망으로만 남아 있다)
- **적 유닛 6종 + 공통 즉시 추적 + 자동 스폰** (좀비/차저/스피터/스프린터/디스럭터/리더 -
  2026-08-10 좀비 기획서 Ver04 반영. 규격 4단계(소형/중형/대형/초대형)로 스프라이트·콜라이더
  크기가 축소·확대 없이 원본 비율 그대로 나오도록 통일. AI 성격값·각성 상태·좀비 감지범위는
  없으며, 스폰 직후 거리 제한 없이 모두 현재 플레이어 위치를 향한다. 밀집 분리는 플레이어
  반대 성분을 제거해 측면 회피만 허용한다. 공격은 전부 "공격 모션(예비 동작) → 판정" 구조를
  거쳐 단순 접촉만으로는 데미지가 들어가지 않으며, 근접(좀비/리더)·투사체(스피터)·돌진
  박치기(스프린터/차저)·자폭(디스럭터) 4가지 방식으로 나뉜다. **돌진은 시작 시점의 방향으로만
  직진하며**(2026-08-12 - 예전에는 매 물리 프레임 플레이어 쪽으로 방향을 다시 잡아 유도탄처럼
  휘었다) 시작 시점 플레이어 거리 + 2유닛을 지나가면 지속시간이 남아도 멈춘다. 리더는
  **도주하지 않고 끝까지 싸운다**(2026-08-12 도주 기믹 삭제). **무리(팔로워) 동반 스폰
  기믹은 2026-08-21 완전히 삭제됐다**(이제 어떤 몬스터도 다른 몬스터를 대동해 등장하지
  않는다) — 대신 리더는 **3초마다 반경 안 좀비 최대 3마리를 최대체력의 20%씩 회복**시키는
  지원형 능력을 가지며, **20웨이브 미만에서는 웨이브당 최대 3마리로 등장이 제한**된다
  (상한에 걸리면 다른 좀비가 그 자리를 대신 채운다). 모든 적 스프라이트는 최종 Rigidbody
  이동 X 방향에 따라 좌우 반전된다.
  boid식 분리는 각자의 실측 몸 반지름 기준으로 겹칠 때만 작동, 카메라
  화면 바깥 테두리 스폰, 웨이브별 스폰 압력 조절 + 몬스터별 개별 해금 웨이브). **이동 애니메이션**
  은 좀비(2026-08-12)에 이어 스피터/스프린터/디스럭터/리더/보스까지 8프레임 세트로 커버됐고,
  **차저도 2026-08-23부터 걷기/돌진/돌진준비 3종 애니메이션**을 갖춰 더는 정지 스프라이트가
  아니다(`MonsterAnimationLibrary.cs`, `ChargerUnit.ResolveMoveClip()`/`TryAttack()`)
- **보스 "좀비 군집체"** — 2026-08-23 기획서 2종(보스몬스터기획서 Ver01 / 보스연출가이드라인
  Ver01) 전면 반영. **페이즈 2단계**(체력 50%에서 1회 전환, 전환 중 완전 무적 + 행동정지 5초),
  **공격 패턴 3종**(① 돌진 박치기 - 바닥 경고 레인 후 직선 돌진, 쿨타임 10.5초 / ② 잔해 낙하 -
  플레이어 주변 7곳에 경고 서클 후 낙하, 쿨타임 7초 / ③ 좀비 소환 - **페이즈 2 전용**, 포효 후
  8마리, 쿨타임 15초), **피해 누적형 그로기 게이지**(만충 시 진행 패턴 즉시 캔슬 → 5초간
  피해 x3), **등장 연출**(소환진 99프레임 → 본체 등장 → 포효)과 **사망 연출**(사망 16프레임 →
  폭발 60프레임 → 그제서야 승리 판정)을 갖췄다. 기존 근접 접촉 공격도 그대로 유지된다.
  **스탯은 아직 단일 패턴 시절 값**(체력 8000/공격력 25/방어력 5)이라 재조정 필요
- **웨이브 시스템 + 보스 + 승리/패배** (시간 종료형 웨이브, 전투↔AI코어↔로봇정비↔상점
  상태 머신 `WaveManager`/`GameFlowManager`, 처치 시 경험치·골드·부품 상자를 필드 픽업으로
  지급). **20웨이브(설정 가능, 웨이브당 60초)에 보스 등장**(`BossUnit` — 근접 + 주기적 광역
  공격 패턴), 보스 처치 + 웨이브 시간 종료가 둘 다 되어야 승리(`GameWinManager`). 패배는
  `GameOverManager`가 기존대로 처리
- **정비 화면 = 전체 화면 UI + 인게임 완전 정지** — 웨이브 종료 시 `Time.timeScale=0`,
  플레이어 조작·자동공격 차단, 전투 HUD 숨김, 필드 초기화(남은 픽업은 **자동 수령 후** 제거,
  투사체 정리, 플레이어 시작 위치 복귀)
- **스탯 시스템 + AI 코어 업그레이드** — `RobotStats`(로봇 기본값 + AI 코어 + 디스크 +
  파츠 보너스 집계), 경험치·레벨업, 업그레이드 3택 카드 UI(웨이브 종료 후 레벨업했을 때만 노출).
  카드 아래에 **골드 리롤**(상점 새로고침과 같은 누적 비용, 카드 화면마다 초기화) 버튼이
  있다(2026-08-18. "레벨업 취소" 버튼도 같은 날 추가했으나, 경험치 반환 없이는 의미가
  없다는 판단으로 곧이어 삭제했다)
- **상점** — 기획서 p.13의 8요소 화면. 품목 4칸 등급 가중치 추첨, 새로고침(비용 증가),
  개별 잠금, 골드 소비. 무기 등급 5단계(일반/희귀/서사/유일/전설)는 기본 무기에 배율을
  곱하는 수직 강화 방식이며, 무기 구매는 소켓 즉시 교체. **타입 불일치·무게 초과는 더 이상
  구매를 막지 않는다**(2026-08-12 "무기 소켓 개별화" 완료) - 항상 장착되고 대신 무게 패널티로만
  반영되며, 소켓 선택 버튼에 비차단 경고 문구가 함께 표시된다. 디스크는 상승 스탯 + 하락 스탯
  페어로 즉시 장착
- **로봇 정비(모딩)** — 8부위(무기소켓 N개/팔장갑/자기장코어/다리/다리장갑/발/**헬멧**/
  **디스크슬롯**) 장착 + 머리(로봇) 조회 전용 칸 = 화면상 8+N칸(N=현재 2, `ModdingManager.
  ActiveSocketCount`). 부품 상자가 몬스터 처치 시 확률적으로 드랍되고, **정비 화면에 들어가면
  보유한 상자가 전부 자동 개봉되어 임시 인벤토리에 쌓인다**. 인벤토리 파츠를 클릭하면 장착
  가능한 슬롯이 노란색으로 강조되고(무기 소켓 종류 파츠는 N칸이 전부 강조된다), 그 슬롯을
  클릭하면 **맞교환**된다(빠진 파츠는 인벤토리로). 스탯 변화는 능력치 패널에 즉시 반영된다.
  **임시 인벤토리는 정비 화면을 닫는 순간 전부 사라진다.** 상자 보유 상한이자 인벤토리 크기는
  머리(로봇)의 **적재량** 능력치이며, 상한에 도달하면 몬스터가 상자를 드랍하지 않는다.
  무기 소켓 파츠는 **소켓마다 독립적으로** **종류**(장착 가능한 무기 타입 - 경무장/중무장/
  근접무기)와 **등급**(사거리·감지거리·회전속도 배율)을 가진다(2026-08-12 이전에는 파츠 하나가
  모든 소켓에 공통 적용됐다). **타입이 안 맞아도 장착은 항상 되고**, 그 무기의 무게가 배율만큼
  늘어나 지탱력을 넘기면 이동속도가 깎인다(무게 캡 자체는 더 이상 장착을 막지 않음).
  디스크 슬롯 수는 디스크슬롯 파츠가 결정한다
- 아이템 드랍 및 획득 (가중치 드랍 테이블, 필드 픽업, 개수 기록)
- 게임 데이터 로드 (로컬 `GameDataAsset` → 몬스터/로봇/무기/방어구/드랍 테이블. 외부 연동 없음)
- **HUD** — 2026-08-18 재배치. **좌상단 세로 스택**(체력 바 = 하트 아이콘 + `100 / 100` 한 줄,
  경험치 바 = 별 아이콘 + 레벨 숫자 + `18 / 30` 한 줄, 골드 아이콘 + 숫자, 부품상자 아이콘 +
  `07 / 20`), **상단 중앙**(`웨이브 5` + 남은 시간 `00:28`), **캐릭터 발밑**(구르기 게이지
  `DASH` - 2026-08-19에 우하단 아이콘에서 옮겨왔다. `DashGaugeUI`가 코드로 만들며 씬에 없다).
  항목명 글자는 전부 아이콘이 대신한다. 게임오버·승리 패널, 카메라 추적(직교 투영).
  배치는 씬(`Ground01`)이 갖고 `GameHUD`는 문구만 만든다. 좌상단 스택의 컨테이너는
  `Canvas/HP`이며 여기에 `GameHUD` 컴포넌트가 붙어 있으므로 **지우거나 비활성화하면 안 된다**.
  캔버스가 `ConstantPixelSize`라 HUD는 전부 **정규화 앵커 + offset 0**으로 배치해야 한다
  (절대 픽셀을 쓰면 FHD 밖 해상도에서 어긋난다)
- **점수 · 엔드리스 · 악세사리 · 해금(도감)** — 2026-08-19에 기획서 4종을 반영해 추가했다.
  점수 = `웨이브 x1,000 + 처치 x10 + 코어 레벨 x200 + 골드 x1 + 악세사리 점수`이고 HUD에
  실시간 표시된다(`RunScore`). 20웨이브 보스를 **처음** 잡으면 점수 정산 팝업이 뜨고 `계속
  진행`을 고르면 엔드리스가 켜져 보스가 19웨이브마다 다시 나온다(`RunState.IsEndless`).
  악세사리 6종은 엔드리스 상점에만 칸마다 5% 확률로 뜨는 점수 전용 아이템이며 캐릭터에
  실제로 그려진다(`AccessoryVisual`). 해금은 머리 12 / 디스크 21 / 악세사리 6종에 걸려 있고
  (초기 해금 8개) **잠긴 항목은 상점 추첨과 머리 선택에서 제외**된다 — 진행도·해금 여부는
  `PlayerPrefs`에 영구 저장되고(`UnlockState`) 타이틀의 `도감`에서 확인한다.
  진행도를 모으는 훅은 전부 `UnlockTracker` 한 곳을 거친다

### 미구현 (목표)

- **로그라이크 요소** — AI 코어(Phase 2)·무기·디스크(Phase 3)·파츠(Phase 4)는 있으나,
  이 외의 런 중 강화 요소는 없음
- **필살기** — 슬롯 표시만 있고 동작은 데모 범위 밖
- **효과음(SFX) 전량** — `Assets/Resources/SFX` 폴더 자체가 없어 `SFXManager`가 **절차 생성
  사인파 비프음**으로 대신 채우고 있다. 같은 이름의 실제 오디오 파일을 그 폴더에 넣기만 하면
  코드 수정 없이 자동으로 우선 적용된다(배경음 3곡은 `Resources/Musics`에 정상 적용됨)
- **전용 아트** — **보스는 2026-08-20 전용 아트("좀비 군집체") + 제자리 꿈틀거림 애니메이션으로
  교체 완료**(그 전까지는 좀비 스프라이트를 확대·붉게 물들인 임시 비주얼이었다). **차저도
  2026-08-23부터 걷기/돌진/돌진준비 3종 애니메이션을 갖춰 정지 스프라이트에서 벗어났다**(그
  전에도 좀비 스프라이트 재활용이 아니라 전용 정지 이미지 `Charger.png`는 있었고, 이동 프레임만
  없었다)
- **판 안에서 바로 재시작하는 버튼** — 게임오버 요약 화면과 승리 점수 정산 팝업에는
  `타이틀로` 버튼이 있어 타이틀을 거쳐 다시 시작할 수 있다(`GameOverSummaryUI`/
  `ScoreSummaryPopup`, 2026-08-07·08-19). **타이틀을 거치지 않는 "바로 다시 시작"만 없다**
  (이 항목은 두 화면이 생기기 전의 기록이 남아 있던 것이라 2026-08-19에 정정했다)
- **소다캔 머리 효과** — "로켓 엔진 장착 + 최대 추가 이동속도 유지 시 공격력 2배"인데, 프로젝트에
  **로켓 엔진 다리 파츠가 없고**(다리 파츠 5종 전부 고정 보너스) "패시브 최대 추가 이동속도"라는
  램프업 개념 자체가 없어 조건을 판정할 수 없다. 머리·스탯·소켓은 전부 들어가 있고 효과만 비어
  있으며, 다리 기획서가 나오면 `HeadEffects`의 해당 분기만 채우면 된다(2026-08-19 사용자 확정)
- **아웃게임** — 컨셉기획서의 캐릭터 선택 화면과 해금 정보 관리 화면은 **구현됐다**
  (2026-08-19: 타이틀의 머리 선택 화면 + `도감`(2026-08-20부터 무기 13종도 포함). 머리 12 /
  디스크 21 / 악세사리 6 / 무기 13종의 해금 진행도를 보여준다). **랭킹 화면도 2026-08-20
  Firebase Realtime Database 연동 + `RankingPanelUI`로 구현 완료**(맵별 분리, 일시정지·
  게임오버·타이틀 세 곳에서 진입). **남은 것은 해금 알림 연출**과, 일시정지 메뉴에서 도감을
  여는 경로다(현재 도감은 타이틀에서만 열린다)

## 실행 및 테스트 방법

- **진입 씬**: `Assets/Scenes/Title.unity` — 제목 + "게임 시작" 버튼. 누르면 **머리 선택 화면**이
  먼저 열리고, 머리를 고른 뒤 "출발"을 눌러야 `Ground01`로 넘어갑니다(2026-08-19 신설).
  실제 플레이는 `Assets/Scenes/Ground01.unity`에서 이뤄집니다.
  **`GameDataManager`(DataManager)는 이제 Title과 Ground01 양쪽에 있습니다** — 머리 선택 화면이
  체력·질량·무기 이름을 보여줘야 하기 때문입니다. `DontDestroyOnLoad` + 중복 가드 덕분에
  타이틀에서 만들어진 인스턴스가 계속 살아남고 Ground01의 것은 스스로 파괴되어, 실행 중
  인스턴스는 항상 1개입니다(`MusicManager`와 같은 패턴).
  `SampleScene.unity`는 Unity 기본 템플릿 씬입니다(빌드 세팅 순서: Title → Ground01 → SampleScene).
- **네트워크 연결 불필요** — 게임 데이터는 로컬 에셋(`Assets/Data/GameDataAsset.asset`)에서
  로드된다(2026-08-06 Phase 0에서 전환, 아래 주의사항 참고).
- **MCP 도구 사용 조건**: Unity Editor가 이 프로젝트로 실행 중이어야 합니다.
  도구가 응답하지 않으면 아래 명령으로 연결 상태를 확인하세요.

```bash
unity-mcp-cli status "C:\Project\ComstockPJ"
```

## 주의사항 (디버깅 시 가장 먼저 확인할 것)

**게임 데이터는 로컬 에셋이 유일한 출처다 (2026-08-06 Phase 7 이후).**

- `GameDataManager`는 `[SerializeField] GameDataAsset localData`를 Awake에서 그대로
  Dictionary에 채워 넣는다. 런타임 네트워크 요청도, 에디터 임포트 경로도 없다.
- **값을 바꾸려면 `Assets/Data/GameDataAsset.asset`을 Unity 인스펙터에서 직접 편집한다.**
  구글 시트 연동(`GameDataCsvImporter`, `CsvParser`)은 사용자 지시로 완전히 삭제했으므로
  다시 추가하지 말 것.
- 스폰이 안 되거나 스탯이 0으로 나오는 증상이 있으면, `Ground01` 씬의 `DataManager`
  GameObject → `GameDataManager` 컴포넌트의 `localData` 필드가 비어있지 않은지,
  `GameDataAsset.asset`의 리스트들이 실제로 채워져 있는지부터 확인한다.
- `IsLoaded`/`OnLoaded` API는 과거 비동기 로드 시절 그대로 유지되고 있다(호출부 호환을 위해).
  실제로는 Awake에서 즉시 동기적으로 끝나므로, `GameDataManager.Instance.IsLoaded`는 다른
  스크립트의 `Start()` 시점에는 항상 true다.

**static 이벤트를 가진 매니저의 `Reset()`은 이벤트를 null로 비우지 마라 (3번 반복된 패턴).**

- `RunState.OnChanged`(Phase 2), `GameOverManager.OnGameOver`(Phase 5), `GameWinManager.OnGameWon`
  (Phase 5)에서 전부 같은 버그가 실제로 재현됐다: `Reset()`이 `이벤트 = null`로 강제 초기화하는데,
  이 `Reset()`을 호출하는 `PlayerRobotController.Awake()`와 구독자(`AiCoreManager`/`GameHUD` 등)의
  `Awake()` 중 어느 쪽이 먼저 실행되는지는 Unity가 오브젝트별로 보장하지 않는다. 구독자의
  `Awake()`가 먼저 실행되면 그 직후 `Reset()`이 구독을 지워버려 이벤트가 조용히 안 울린다.
- **패턴**: 이런 static 매니저에 `Reset()`을 추가할 때는 상태 필드만 초기화하고 이벤트는
  절대 비우지 않는다. 각 구독자가 자신의 `OnDestroy()`에서 스스로 구독 해제하므로 강제
  초기화는 애초에 불필요하다.

## 알려진 이슈

- **Input System 에셋 미사용** — `Assets/InputSystem_Actions.inputactions`가 존재하지만
  어떤 스크립트도 참조하지 않습니다. 이동/사격 모두 `Keyboard.current` / `Mouse.current`를
  직접 폴링하는 저수준 방식입니다. 키 리바인딩이나 게임패드 지원이 필요해지면 정리가
  필요합니다.

## 기술 스택

- Unity **6000.3.19f1**
- URP(Universal Render Pipeline) 17.3.0
- Input System 1.19.0 (설치되어 있으나 실질 미사용 — 위 알려진 이슈 참고)
- **Unity-MCP** (IvanMurzak/Unity-MCP) 플러그인 설치됨 — Claude Code 등 AI 에이전트가
  Unity Editor(씬, GameObject, 에셋, 스크립트 등)를 MCP 도구로 직접 조작 가능
  (`.mcp.json`, `.claude/skills/` 참고)
- **폰트**: 기본 `Orbitron-Regular SDF`(Static 아틀라스 1024², 라틴 207자) + 전역 폴백
  `NotoSansKR-Regular/Bold SDF`(Dynamic). TMP 기본 폰트는 `TMP Settings.asset`이 갖고 있다
- **다국어**: 자체 경량 구현(`Loc.cs`). 외부 패키지 없음, `Newtonsoft.Json`으로 JSON만 읽는다

## 프로젝트 구조 (Assets/ 하위)

- `Scripts/` — 게임 코드 전체 (하위 폴더/네임스페이스 없이 flat 구조, `Editor/`만 예외)
- `Scripts/Editor/` — 에디터 전용 도구. 빌드에 포함되지 않음
  (`WeaponTableGenerator`=무기 65행 생성, `WeaponTableValidator`=정합성 검사,
  `ProceduralRigDemoBuilder`=관절 리그 데모 씬 생성)
- `Data/` — 게임 데이터 에셋 전부(`GameDataAsset.asset`=몬스터/로봇/무기/방어구/드랍,
  `AiCoreUpgradePool.asset`, `ShopCatalog.asset`, `PartsCatalog.asset`).
  **모든 게임 데이터의 유일한 출처이며, 어떤 데이터도 프로젝트 밖 리소스를 참조하지
  않는다**(사용자 지시)
- `Prefebs/` — 프리팹 (`Bullets`, `Energy`, `Zombie`). **폴더명이 "Prefabs"가 아닌
  "Prefebs"인 것은 오타이지만, 코드 내 `Resources.Load` 경로 등에서 이미 이 이름으로
  고정되어 쓰이고 있으므로 임의로 이름을 바꾸지 말 것.**
- `Scenes/` — `Title.unity`(진입), `Ground01.unity`(플레이 씬), `SampleScene.unity`(기본 템플릿),
  `JointRigDemo.unity`(절차적 관절 리그 확인용 데모 — 빌드 세팅에는 없음)
- `Resources/` — 런타임 로드용 스프라이트(무기 손모양, 적, 아이템 아이콘 등)와 배경.
  현재 배경은 **`ground_ruined_city_v2.png`**(3840x2160, 2026-08-07 사용자 제공,
  `maxTextureSize`=4096). **2026-08-10부터 배경은 `map` 오브젝트의 자식 `map_tile_LB/RB/LT/RT`
  4장으로 구성된다** — 가장자리 4px를 잘라낸 전용 이미지 **`ground_ruined_city_v2_tile.png`**
  (3832x2152)를 스케일 0.5로 그리고 `flipX`/`flipY` 조합으로 **미러 배치**해 이음매를 없앤
  것이다(원본 이미지 가장자리 3번째 픽셀이 밝아서 그냥 이어붙이면 흰 줄이 생긴다).
  맵 크기 **38.32x21.52 유닛**이고 카메라 화면이 19.2x10.8유닛이라 **맵 = 화면 2개분**이다.
  `map` 자신의 SpriteRenderer는 지우지 않고 `enabled=false`로만 꺼 뒀다(되돌리려면 다시 켜고
  자식 4장을 지운다). **`MapBounds`는 자식들의 bounds를 합산**하므로 타일 구성이 바뀌어도
  맵 경계가 저절로 따라온다.
  이전 배경 `ground_ruined_city.png`와 그 전의 `map.png`도 지우지 않고 남아 있다.
  **`map`의 스케일은 반드시 가로세로를 같게 유지할 것** — 다르면 이미지가 늘어난다.
  `Resources/Parts/`에는 절차적 관절 리그용 캐릭터 파츠 4장(`Body`/`LegUpper`/`LegLower`/`Foot`)이 있다.
  **`Resources/UI/`에는 사용자 제공 UI 아트 50장**(패널 `Panel00~04`·`Black_ui00~09`·`Purple_ui00~04`,
  버튼 `Purple_button00~03`, 바 `Red_bar*`/`Green_bar*`/`Blue_bar*`/`Blue_barpanel*`,
  아이콘 `Heart_icon`·`Star_icon`·`Gold_icon00/01`·`Exp_icon`·`Dash_skill`, 타이틀 배경
  `titleimage`)이 있고 **전부 `spriteBorder`가 설정된 9-슬라이스**다 — UI가 앵커 비율로 배치돼
  해상도마다 늘어나므로 반드시 `Image.type = Sliced`로 쓸 것(Simple로 쓰면 모서리 장식이 뭉개진다).
  **`Resources/Musics/`에는 BGM 3곡**(`Title_BGM`, `Game_BGM01`, `Game_BGM02`)이 있으며
  `loadType=Streaming`으로 임포트돼 있다. **인게임 곡을 추가하려면 `Game_BGM`으로 시작하는 이름으로
  이 폴더에 넣기만 하면 된다**(`MusicManager`가 폴더를 훑어 접두사로 거른다)
- `Animations/` — 애니메이션 클립, `PlayerAnimator.controller`
- `Settings/` — URP 파이프라인 에셋(Mobile/PC 변형 포함)
- `Fonts/NotoSansKR/` — 한글 지원 TMP 폰트(Noto Sans KR, SIL OFL 1.1). TMP Settings의
  전역 폴백 체인에 등록되어 있어 별도 지정 없이도 한글이 자동 렌더링됨(상세는 위 "최근
  완료" 및 [작업.md](작업.md) 참고)

## 핵심 시스템

| 영역 | 주요 스크립트 | 설명 |
|---|---|---|
| 데이터 레이어 | `GameDataManager`, `GameDataAsset`, `GameDataModels` | `GameDataAsset`(로컬 ScriptableObject)를 Awake에서 동기 로드. 외부 연동 없이 이 에셋이 유일한 출처 (위 주의사항 참고) |
| 캐릭터 이동 | `PlayerRobotController` | Rigidbody 기반, WASD 이동 + Space 구르기(무적 대시). 스탯은 `RobotStats.Compute()` 결과(로봇 기본값+AI코어+디스크+파츠 보너스)를 사용, `RunState.OnChanged`로 실시간 재계산. 정비 중에는 `GameFlowManager.IsIntermission`을 보고 입력을 스스로 차단. 시각 표현은 `Player/ProceduralVisual`의 `ProceduralCharacterRig`가 담당(기존 단일 스프라이트+Animator는 비활성화 상태로 보존) — 이 스크립트 자체는 새 리그를 몰라도 되게 그대로 두었다(리그가 `Rigidbody`를 자동으로 읽는다) |
| 절차적 관절 리그 (예시) | `ProceduralCharacterRig`, `ProceduralRigDemo`, `ProceduralRigDemoBuilder` | 파츠 스프라이트 4장을 코드로 조립하고 관절 각도를 매 프레임 계산한다. 애니메이션 클립 없음. 뼈 길이·스프라이트 기울기 보정각은 "관절이 스프라이트 안 어디인가"(정규화 앵커)에서 자동 역산하고, 보행 주파수는 `속도 / 보폭`, 무릎 각도는 2관절 IK로 구한다. 스탠스 이동량을 `보폭 x 접지비율`로 맞춰 발이 미끄러지지 않는다. 몸통과 다리의 비율은 `legScale`(다리 파츠에만 곱함)과 `hipSeparation`(=0.7, 몸통 폭 대비 약 28% — 걷는 레퍼런스라 프레임마다 다리 벌어짐이 달라 픽셀 비율을 그대로 못 쓰고 화면 비교로 눈대중 조정했다. 0.62→1.33→0.9→0.7 순으로 네 차례 조정한 값이다)으로 맞췄고, 길이 계열 파라미터는 전부 다리 길이에 대한 배수다. IK 목표가 사거리를 벗어나면 **높이를 살리고 가로를 줄여** 발이 지면에서 뜨지 않게 한다(보폭이 자동으로 잘린다). **두 다리는 완전히 같은 굽힘 부호·같은 그림 방향을 쓴다** — 다리별로 미러링하면 사람과 다르게 두 다리가 서로 반대로 꺾인 것처럼 보인다(실제로 겪은 과잉수정, 작업.md 참고). 발 각도는 뒤꿈치착지→발끝밀기→스윙 롤 모델(`heelStrikeDegrees`/`pushOffDegrees`/`toeLiftDegrees`)을 쓴다. **좌우 반전은 몸통(flipX, 즉시 전환)과 다리(`LegsGroup.localScale.x` 부호, 즉시 전환)를 분리해서 처리한다** — 예전엔 RigRoot 전체를 스케일 보간으로 뒤집어서 중간에 몸통이 찌그러지는 문제가 있었다. **`LegsGroup` 밑에는 Collider를 두면 안 된다**(음수 스케일 경고, 위와 같은 이유). 아직 데모 씬(`JointRigDemo`)에서만 쓰이며 실제 `Player`에는 적용하지 않았다 |
| 전투/무기 | `PlayerShootManager`, `Projectile`, `BeamProjectile`, `MeleeSwing` | 소켓별 독립 쿨다운 **자동공격**(감지거리 내 최근접 적 자동 조준·발사, 탄약/재장전 없음). **대기시간은 발사 동작이 끝난 뒤부터 흐른다** — 빔처럼 지속시간이 있는 무기는 (지속 + 대기)가 한 주기라 여러 개가 겹치지 않는다. `weapon_firemode`에 따라 **투사체 / 빔(`BeamProjectile`) / 근접 찌르기(`MeleeSwing`)** 세 갈래로 갈린다. 근접무기는 찌르기 동작(`StartMeleeThrustVisual`)이 소켓 위치로 완전히 복귀할 때까지 재조준을 멈추고, 복귀 지점은 부모(캐릭터) 기준 로컬 좌표를 매 프레임 월드로 재변환해 캐릭터가 이동해도 어긋나지 않는다(2026-08-12). **2026-08-13부터 근접 타격 판정은 이 찌르기 동작 자체가 담당한다** — 칼이 나가는 프레임마다 `ApplyMeleeHitAtBlade`가 **무기 스프라이트의 월드 bounds**(위치·반경)로 판정하므로 찌르는 경로 전체가 타격 범위이고 "보이는 것 = 맞는 것"이 보장된다(`weapon_range`는 판정에 쓰이지 않는다). 같은 스윙의 중복 피격은 슬롯별 `HashSet<EnemyUnit>`으로 막는다. **판정 높이는 소켓 높이(어깨, 발밑 기준 +0.79)만큼 내려야 한다** — 적 히트박스가 발밑에 있어 보정 없이는 수직으로 떠서 아무도 맞지 않는다. 사거리·감지거리는 각각 별도 필드이며 **소켓마다 독립적인** 무기소켓 파츠 등급의 배율이 곱해진다(`ModdingManager.GetWeaponSocketModifiers(socketIndex)`, 감지는 사거리와 화면 상한 8.5로 두 번 잘린다). 회전 속도·투사체 속도도 이제 **무기 데이터**가 정한다(슬롯 값은 폴백). 다중탄은 **부채꼴**로 벌어지고 탄퍼짐·확률 관통은 **탄마다 개별**로 굴린다. 관통은 횟수 기반(0/N/-1=무제한), 스플래시는 `weapon_splash > 0`이면 어떤 무기든 폭발(예전의 weapon_id 하드코딩 제거). 방어력 %무시·넉백 지원. **등급 배율은 곱하지 않는다**(등급마다 별도 데이터 행). 소켓 개수는 `SocketCount`(=`ModdingManager.ActiveSocketCount`, 머리 파츠 개수와 씬 리깅 개수 중 작은 값)이며, 씬 리깅 원본 개수는 `RiggedSocketCount`로 따로 조회한다(2026-08-12 - 지금은 둘 다 2라서 기존과 동일하게 동작. 확장하려면 씬에 소켓 Transform을 새로 배치해야 함) |
| 스탯/AI 코어 | `RobotStats`, `StatType`, `AiCoreManager`, `AiCoreUpgradePool` | 최종 스탯 집계 레이어(로봇 기본 + AI코어 + 디스크 + 파츠 + **무게 초과 이동속도 패널티**). 경험치→레벨업(필요 경험치 `4 + 2 x 레벨`, 최대 레벨 70 - 2026-08-13 "레벨업을 훨씬 자주" 요청으로 계수를 1/12로 낮춰 약 3.5배 자주 오른다)→**등급이 붙은** 업그레이드 3택 카드(`GameFlowManager`가 웨이브 종료 후 노출. 카드마다 등급을 굴리고 증가량 = 일반 등급 기준값 x 등급 배율 1/1.67/2.33/3.33/5, 문구는 `AiCoreUpgradePool.BuildEffectLine`이 실제 적용값으로 생성). `Compute()`가 `ModdingManager.Instance`를 직접 읽어 `Max(0, 총무게-지탱력) x overweightSpeedPenaltyPerUnit`만큼 이동속도를 깎는다(2026-08-12 - 지탱력 초과는 더 이상 장착 자체를 막지 않는다) |
| 상점/디스크 | `ShopManager`, `ShopPanelUI`, `ShopCatalog`, `ItemGrade`, `DiscData` | 품목 4칸 등급 가중치 추첨(등급별 최소 등장 웨이브 적용), 새로고침(비용 증가)·개별 잠금, 무기 구매=소켓 즉시 교체 / 디스크 구매=슬롯 장착. **무기 소켓 타입 불일치·무게 초과는 더 이상 구매를 막지 않는다**(2026-08-12) - `CanPurchaseWeaponIntoSocket`은 골드/구매여부만 검사하고, `BuildSocketWarning`이 비차단 경고 문구(타입 불일치 배율·무게 초과 여부)를 따로 제공해 소켓 선택 UI에 표시한다. 등급·가격·디스크 수치는 전부 `ShopCatalog.asset`의 **임시값** |
| 로봇 정비/파츠 | `ModdingManager`, `ModdingPanelUI`, `PartsCatalog`, `PartData`, `PartSlot`, `WeaponType` | 8부위 장착 + 머리 조회 전용 = 화면 8+N칸(N=무기 소켓 개수, `ActiveSocketCount`). 무기 소켓(`PartSlot.ArmWeaponSocket`)은 **소켓 인덱스별로 독립 저장**된다(`RunState.EquippedWeaponSocketPartIds: Dictionary<int,int>`, 2026-08-12 이전에는 `EquippedPartIds`의 단일 키였다) - 조회/교체는 `TryGetEquippedWeaponSocketPart`/`TrySwapInventoryWithWeaponSocket`(인덱스 버전), 나머지 7슬롯은 기존 `TryGetEquippedPart`/`TrySwapInventoryWithSlot`(`PartSlot` 버전)을 그대로 쓴다. 정비 진입 시 상자 **일괄 자동 개봉**(`OpenAllBoxesIntoInventory`) → 임시 인벤토리(`RunState.ModdingInventory`) → 슬롯과 **맞교환** → 화면을 닫으면 `ClearInventory()`로 소멸. 상자 보유 상한 = 머리의 **적재량**(`PartBoxCapacity`)이며 `EnemyUnit`(드랍)과 `RewardPickup`(습득) 양쪽에서 지켜진다. **무기 타입 제한·무게 제한은 더 이상 장착/구매를 막지 않는다**(`IsWeaponMismatched`+`GetEffectiveWeaponWeight`가 무게 배율로만 반영, 2026-08-12). 디스크 슬롯 수는 `DiscSlotCount`가 DiscSlot 파츠 → 머리 기본값 순으로 결정. **`PartSlot` enum에 새 슬롯은 반드시 맨 끝에 추가할 것**(중간 삽입 시 에셋의 slot 값이 어긋남). 수치는 전부 **임시값** |
| 적 AI / 보스 | `EnemyUnit`, `EnemySpawner`, `BossUnit`, `SpitterUnit`/`SprinterUnit`/`ChargerUnit`/`DisruptorUnit`/`LeaderUnit`, `MonsterSizeClass` | **2026-08-10 좀비 기획서 Ver04 반영 후 추적 규칙 단순화.** `EnemySpawner.SpawnMonster()`가 몬스터ID→타입 매핑(`MonsterComponentTypes`)을 보고 스폰 시점에 행동 컴포넌트를 붙인다. AI 성격값·각성 상태·좀비 감지범위 없이 모든 좀비가 현재 플레이어를 즉시 직선 추적하며, 밀집 분리 벡터도 플레이어 반대 성분은 제거한다. 최종 Rigidbody 이동 X 방향으로 루트 SpriteRenderer를 좌우 반전하므로 일반 추적·돌진·도주·넉백 방향이 모두 시각에 반영된다. 근접 공격은 전부 `TryAttack()`→`PerformAttackMotion()`→`ExecuteAttackEffect()` 구조이며 스피터는 투사체, 스프린터/차저는 **시작 방향을 고정한 직선 돌진**(2026-08-12 - 유도탄처럼 휘던 것을 수정. `dashOvershoot`/`chargeOvershoot`=2유닛만큼 플레이어를 지나치면 조기 종료), 디스럭터는 자폭으로 교체한다. `MonsterSizeClass`/`MonsterSizeSpec`이 규격별 이미지·콜라이더 크기를 PPU=100·스케일=1 기준으로 정의한다. `LeaderUnit`은 무리를 데리고 나올 뿐 **도주하지 않는다**(2026-08-12 도주 기믹 삭제, `SetPack`만 남음). **카메라 화면 바깥 테두리 스폰**, `EnemyUnit.Alive` 자동 타겟팅, 처치 보상 드랍을 지원하며 `BossUnit`은 주기적 광역 공격을 추가한다. |
| 웨이브/흐름/승리 | `WaveManager`, `GameFlowManager`, `GameWinManager` | 웨이브는 제한시간(60초)이 끝나면 **스폰만 멈추고**, 필드에 남은 적을 전부 처치한 뒤 2초(`clearedGraceSeconds`)가 지나야 끝난다(적이 끼어 안 죽으면 `maxClearWaitSeconds` 후 강제 종료). 웨이브별 스폰 압력 증가. 흐름: 전투 → (AI 코어 업그레이드, 레벨업 시에만) → (로봇 정비, 부품 상자 있을 때만) → 상점(항상) → 다음 웨이브. "다음 웨이브 시작" 버튼은 상점 화면 안에 있다. `CloseAllIntermissionPanels()`로 정비 단계 화면이 항상 하나만 켜지도록 보장. 마지막 웨이브(기본 20)에는 `WaveManager`가 보스를 스폰하고, 보스 처치 + 웨이브 시간 종료가 둘 다 되어야 `GameWinManager.TriggerWin()` 호출. `GameFlowManager.IsIntermission`(static)이 정비 상태를 전역에 알리며, 정비 진입 시 `Time.timeScale=0`·필드 초기화·전투 HUD 숨김(`combatHudObjects`)에 더해 **플레이어 체력 전부 회복**(`PlayerRobotController.Heal(MaxHp)`, 2026-08-12)을 이 매니저(`HandleWaveEnded`)가 일괄 처리한다(보스 웨이브는 이 이벤트가 발생하지 않아 자동 제외) |
| 보상 드랍 | `RewardPickup`, `RewardPickupManager` | 정적(static) 클래스 기반 패턴. 몬스터 처치 시 골드/경험치/부품상자를 필드 픽업으로 지급하는 유일한 경로(2026-08-12: 효과 없이 중복 존재하던 `PlayerInventory`/`DropTableManager`/`DropItemManager`/`ItemPickup` 경로는 완전히 삭제). 무기·디스크는 상점(Phase 3), 팔/다리 파츠는 부품 상자+정비 화면(Phase 4)이 장착을 담당 |
| 전역 상태 | `PlayerSession`, `GameOverManager`, `GameWinManager`, `RunState` | MonoBehaviour 싱글톤 대신 static class 패턴을 일관되게 사용. `RunState`는 웨이브·골드·경험치·AI코어/디스크/파츠 보너스·장착 무기(등급 포함)·장착 파츠·상점 새로고침 횟수·미개봉 부품 상자 개수 등 런 진행 상태 컨테이너. `GameOverManager`/`GameWinManager`의 `Reset()`은 이벤트를 null로 비우지 않는다(초기화 순서 버그 방지 - 아래 참고) |
| 배경음악 | `MusicManager`, `MusicVolumeSliderUI` | **AudioSource 하나**로만 재생한다(곡이 겹치는 사고를 구조적으로 차단). 씬에 두지 않고 `[RuntimeInitializeOnLoadMethod]`로 스스로 생성 + `DontDestroyOnLoad`이며, 두 번째 인스턴스는 `Awake`에서 즉시 자살한다. `sceneLoaded`를 보고 **`Title` 씬이면 `Musics/Title_BGM` 반복 / 그 외에는 `Musics/Game_BGM*` 랜덤**(폴더를 `Resources.LoadAll`로 읽어 접두사로 걸러서 곡을 추가해도 코드 수정 불필요). 인게임 곡은 `loop=false`라 끝나면 `Update`가 다음 곡을 고르며 **직전 곡은 후보에서 제외**한다. 곡 교체는 0.5초 페이드 아웃 → 교체 → 페이드 인이고, 정비/상점의 `timeScale=0`에서도 진행되도록 `Time.unscaledDeltaTime`을 쓴다. 볼륨은 `MusicManager.Volume`(PlayerPrefs `comstock_music_volume`, 기본 0.7) 하나로만 바뀌고 `OnVolumeChanged`로 화면들이 동기화된다. `MusicVolumeSliderUI.Attach()`는 "음악 볼륨 + 슬라이더 + %" 컨트롤을 코드로 만들어 **타이틀 화면**(`TitleSceneManager`)과 **상점 화면**(`ShopPanelUI`)에 붙인다 |
| 장비 상세 조회 | `EquipmentDetailPopup`, `TextLinkClickRelay` | 상점의 "장착 무기/모딩 상태/디스크" 목록에서 **항목을 클릭하면 상세 능력치 팝업**이 열린다(2026-08-12 신설). 목록은 여러 줄이 든 TMP 텍스트 1개씩이고 항목 수가 런타임에 변해서(소켓·디스크 슬롯 개수) 씬에 버튼을 미리 깔 수 없으므로, **TMP 링크 태그 + `IPointerClickHandler` 릴레이**로 클릭을 받고 팝업은 `ModdingPanelUI`처럼 **코드로 생성**한다(씬 수정 0). 링크 id: `w:소켓`/`ws:소켓`/`p:슬롯`/`d:디스크id`/`head`. 표시값은 데이터 원본이 아니라 **실제 적용값**(소켓 배율 곱한 실효 사거리, 로봇 공격력이 분배된 1발 데미지, 무게 초과 시 이동속도 감소량, 타입 불일치 무게 배율). 레이아웃은 전부 앵커 비율 + TMP 자동 크기(캔버스가 `ConstantPixelSize`라 절대 픽셀은 해상도마다 깨진다) |
| UI | `GameHUD`, `GameOverSummaryUI`, `TitleSceneManager`, `RobotSelectManager` | 상시 HUD: HP·웨이브·골드·AI 코어 경험치 바 실시간 표시, 게임오버/승리 패널 활성화 트리거(`GameOver`/`Victory` 오브젝트에 부착). `GameOverSummaryUI`는 게임오버 시 도달 웨이브·골드·로봇 모딩 8부위·무기·디스크·최종 스탯을 정리해 보여주고 "타이틀로" 버튼으로 `Title` 씬 복귀(`ShopPanelUI`와 동일한 조회 패턴 재사용). `TitleSceneManager`는 `Title.unity`의 "게임 시작" 버튼을 처리. 로봇 선택 화면(씬 미배치, `RobotSelectManager`)은 여전히 미구현 |

## 코드 스타일 관찰

- 네임스페이스 없이 전역 클래스 사용 (규모가 커지면 정리 필요)
- 주석/로그 문자열은 한글, 식별자는 영문 camelCase/snake_case 혼용
- 정적 클래스를 싱글톤처럼 사용하는 패턴이 반복적으로 나타남 (`PlayerInventory`,
  `GameOverManager`, `DropTableManager`, `PlayerSession`)
