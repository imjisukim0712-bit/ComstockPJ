# 작업 기록

이 문서는 프로젝트에서 진행된/진행 중인 작업 내역, 수정사항, 작업 계획을 시간순으로
기록합니다.

**⚠️ 이전에 작성된 내용은 절대 삭제하거나 수정하지 않습니다. 새 내용은 항상 이 문서의
맨 아래에 이어서 추가합니다.**

## 기재 형식

- 날짜(`## YYYY-MM-DD`) 아래에 작업 단위별 소제목(`### 제목 [상태]`)으로 기재합니다.
- 상태 태그는 `[완료]` / `[진행중]` / `[보류]` 중 하나를 씁니다.
- **상태가 바뀌어도 기존 줄을 고치지 않습니다.** 대신 문서 맨 아래에 새 항목으로
  변경 사실을 추가합니다. (예: "OO 작업 `[진행중]` → `[완료]`")
- 현재 진행 상황을 한눈에 보려면 [프로젝트 안내.md](프로젝트%20안내.md)의
  "현재 진행 상황 요약" 섹션을 확인하세요(그 섹션은 항상 최신으로 갱신됩니다).

---

## 2026-08-05

### Unity-MCP 플러그인 설치

- `unity-mcp-cli`(전역 npm 패키지)를 v0.82.4 → v0.87.0으로 업데이트
- `unity-mcp-cli install-plugin`으로 Unity-MCP(IvanMurzak/Unity-MCP) 플러그인 설치
  - 변경/생성 파일: `Packages/manifest.json`, `Packages/packages-lock.json`,
    `ProjectSettings/PackageManagerSettings.asset`, `Assets/Plugins/`(신규)
- `unity-mcp-cli setup-mcp claude-code`로 Claude Code용 MCP 서버 설정 생성
  (`.mcp.json`, http transport, 서버명 `ai-game-developer`)
- `unity-mcp-cli setup-skills claude-code`로 Claude Code용 스킬 파일 생성
  (`.claude/skills/`, GameObject/Scene/Prefab/Asset/Script 편집 등 60여개 도구)
- **상태**: 완료. Unity Editor와 MCP 서버 연결 확인됨(정상 응답 확인)
- **커밋 여부**: 미커밋 (사용자 확인 대기 중)

### 프로젝트 구조 파악

- Explore 서브에이전트를 통해 `Assets/` 폴더 구조, 스크립트 17개, 핵심 시스템(캐릭터
  이동, 무기/전투, 적 AI, 인벤토리/드랍, UI, 전역 상태) 조사
- 조사 결과를 [프로젝트 안내.md](프로젝트%20안내.md)에 반영

### 문서 체계 수립

- [프로젝트 안내.md](프로젝트%20안내.md), 작업.md 두 개의 마크다운 문서를 프로젝트
  루트에 신규 생성
- 목적: 사용자 및 코딩 에이전트가 프로젝트 현황·규칙·작업 이력을 빠르게 파악할 수
  있도록 함
- 프로젝트 안내.md에 협업 기본 규칙(요구사항 불명확 시 질문, 단순 질문 시 임의 수정
  금지, 작업.md 즉시 기재 및 append-only 원칙 등)과 게임 개요(제목: 컴스톡, 장르:
  웨이브 서바이벌/뱀서라이크/로그라이크/모딩, 로봇 파츠 교체+좀비 처치 컨셉) 반영

### 문서 검수 및 보강 [완료]

생성된 두 md 파일을 실제 프로젝트 상태와 대조해 검수하고, 발견된 문제를 보완함.

**신규 생성**
- `CLAUDE.md` — Claude Code가 세션 시작 시 자동으로 읽는 파일. 프로젝트 안내.md와
  작업.md를 먼저 읽도록 지시하는 부트스트랩 내용만 최소한으로 작성
- `AGENTS.md` — Codex용. 동일한 부트스트랩 내용
- 이유: 기존에는 `프로젝트 안내.md`가 자동 로드되지 않아 사용자가 매 세션 수동으로
  "문서를 읽어라"고 지시해야 했고, 잊으면 협업 규칙이 전부 무시되는 문제가 있었음

**프로젝트 안내.md 보강**
- "현재 진행 상황 요약" 섹션 신설 (append-only가 아닌 갱신형 섹션)
- "구현 현황" 섹션 신설 — `구현 완료` / `미구현(목표)` 분리. 목표 장르에 적힌
  웨이브 시스템·로봇 파츠 교체·로그라이크·모딩이 실제로는 모두 미구현임을 명시
- "실행 및 테스트 방법" 섹션 신설 — 진입 씬은 `Assets/Scenes/Ground01.unity`
- Google Sheets 의존성을 "주의사항" 섹션으로 격상 (디버깅 1순위 확인 항목)
- "알려진 이슈" 섹션 신설

**작업.md 보강**
- 기재 형식 규칙(상태 태그 `[완료]`/`[진행중]`/`[보류]`) 추가

### 검수 중 발견한 코드 이슈 [보류]

문서 작업 범위 밖이므로 **기록만 하고 수정하지 않음.** 수정 여부는 사용자 확인 필요.

1. **`RobotSelectManager`가 어떤 씬에도 배치되어 있지 않음**
   - `grep -l RobotSelectManager Assets/Scenes/*.unity` 결과 없음
   - 로봇 선택 화면이 스크립트만 존재하고 씬에 연결되지 않은 상태
2. **`RobotSelectManager.cs:26`의 `nextSceneName` 기본값이 존재하지 않는 씬을 가리킴**
   - 기본값 `"SamplePlay00"` / 실제 존재하는 씬은 `Ground01`, `SampleScene` 둘뿐
   - `ProjectSettings/EditorBuildSettings.asset`에도 이 둘만 등록되어 있음
   - 이 상태로 스크립트를 씬에 배치해 실행하면 `SceneManager.LoadScene`이 실패함

**확인만 하고 넘어간 사항(이슈 아님)**
- CSV URL은 씬에 직렬화된 값과 `GameDataManager.cs:13-17` 코드 기본값이 일치함
- `EnemySpawner`에 웨이브 로직이 없는 것은 확인됨(미구현 목표로 문서화)
- `PlayerInventory`에 장착 로직이 없는 것은 확인됨(소스 주석에도 명시되어 있음)

---

## 2026-08-06

### MCP 연결 확인 [완료]

- `unity-mcp-cli run-system-tool ping` 정상 응답(에코 확인)
- `unity-mcp-cli status "C:\Project\ComstockPJ"` — Unity Editor 실행 중(PID 8080),
  Config Server(`localhost:21195`) 연결 성공
- 참고: 로컬 MCP 서버 포트(`localhost:22389`)는 connection refused지만 Config Server
  경유로 도구 호출이 가능한 상태(`ready for tool calls`)
- **문서 오류 발견**: `프로젝트 안내.md`의 status 명령 예시 경로가 `Y:\Unity\ComstockPJ`로
  적혀 있으나 실제 프로젝트 경로는 `C:\Project\ComstockPJ`임 (계획 승인 후 수정 예정)

### 기획서 `20260805_시스템업데이트_Ver02_김지수.pdf` 분석 [완료]

13페이지 PDF(작성자 김지수, Ver02)를 텍스트+렌더링 이미지로 전체 확인.

**방향성 변경 (기획서 p.5~6)**
- 익스트랙션 장르 **포기** → 뱀서라이크로 전환, **자동공격 전투**로 전환
- `웨이브 클리어 - 정비 - 웨이브 진입` 구조 채용
- 덱빌딩 요소를 MECE하게 4분할, 모두 정비 시간에만 업그레이드 가능:
  - **AI 코어**: 기본 스탯 강화. 적 처치 경험치 → 레벨업 → 정비 시간에 업그레이드 선택
  - **전투 무기**: 상점 구매로 즉시 교체. 종류=수평 강화 / 등급=수직 강화. **무기 레벨·모딩 없음**
  - **디스크**: CD 형태, 타 로그라이크의 유물/아이템 역할. 특정 스탯 상승 + 다른 스탯 하락 리스크
  - **로봇**: 모딩 가능. 덱빌딩 방향성을 결정하는 핵심 요소

**모딩 전체 지도 (기획서 p.8)**
- 머리: 어울리는 무기 타입 / 최대 무기 소켓 개수, 헬멧=방어력, 메모리=AI 코어 최대 레벨,
  디스크 슬롯=장착 가능 최대 디스크 개수, 필살기 슬롯
  (※ 머리를 캐릭터로 고정해도 되고, 해금해서 인게임 모딩 요소로 해도 된다고 병기됨)
- 팔: 무기 소켓=장착 가능 무기 타입, 팔 장갑=방어력, 자기장 코어=지탱 가능 무기 무게
- 다리: 무게 지탱 / 이동 기술, 다리 장갑=방어력, 발=기동력

**획득 방법 (기획서 p.10)**
- AI 코어: 적 처치 → 경험치 → 레벨업 → 정비 시간에 업그레이드 3택
  (UI 예시: `레벨 5(+2) 80/100`, 카드 3장 — 사격술 최적화 공격력 +2.5 / 타겟 추적 기술
  치명타 확률 +5% / A* 알고리즘 이동속도 +1.2)
- 무기·디스크: 정비 시간 상점에서 구매
- 로봇 파츠: **인게임에서 부품 상자 파밍**으로 획득, 정비 시간에 갈아끼움

**정비 화면 흐름 (기획서 p.12)** — 웨이브 종료 후 진입
1. `[AI 코어 업그레이드]` — AI 코어 레벨이 올랐을 경우에만 노출
2. `[로봇 정비]` — 획득한 부품 상자가 있을 경우에만 노출
3. `[상점]` — 웨이브 종료 후 **반드시 노출**. 무기·디스크 구매/교체

**상점 화면 명세 (기획서 p.13)** — 8개 요소
1. 현재 웨이브 2. 골드(재화) 3. 다음 웨이브 시작 버튼 4. 로봇 모딩 상태(조회 전용:
헤드/헬멧/메모리카드/무기소켓/팔장갑/디스크슬롯/필살기슬롯/자기장코어/다리/다리장갑/발)
5. 장착된 무기(무기소켓 6칸 예시) 6. 장착된 디스크(10/12 슬롯·최대 24 예시)
7. 현재 능력치(체력/공격력/방어력/치명타확률/치명타피해/공격속도/이동속도/회피율/행운/질량)
8. 상점 품목 4칸 + 새로고침(비용 표시) + 개별 잠금
- 품목 카드는 `등급 · 카테고리`(전설·무기 / 희귀·디스크 / 고급·무기) 형태로 표기, 구매 시
  `구매 완료` 상태로 바뀜

### 데모 개발 계획 수립 [진행중]

위 기획서와 현재 코드(`Assets/Scripts/` 17개 파일, 2126줄)를 대조한 갭 분석 및 단계별
계획을 사용자에게 보고함. **사용자 확인(핵심 결정 4건) 대기 중이며, 코드는 아직 수정하지
않음.**

**갭 분석 요약**
- 재사용 가능: 데이터 로더(CSV), 이동, 투사체/무기 발사 계산(데미지·크리·다중탄), 적 AI·
  드랍 테이블, 카메라, HUD 골격
- 전환 필요: `PlayerShootManager`의 마우스 조준·클릭 발사 → 자동 타겟팅·자동 발사,
  슬롯 1개 활성 → 다중 소켓 동시 사격
- 신규 필요: 웨이브 매니저, 게임 상태 머신(전투/정비), 경험치·레벨(AI 코어), 골드,
  스탯 집계 레이어, 디스크 시스템, 로봇 파츠(모딩) 시스템, 정비/상점 UI,
  부품 상자 드랍, 필살기
- 데이터 신규 테이블 필요: 디스크, 로봇 파츠(머리/팔/다리), AI 코어 업그레이드 풀,
  웨이브 정의, 상점 등급/가격. 기존 무기 테이블에도 등급·가격·타입·무게 컬럼 부재

**단계 계획(총 6단계)** — 상세는 보고 내용 참조
- Phase 0 기반 정리: RunState 도입, 데이터 소스 정리, 알려진 이슈 처리
- Phase 1 전투 루프 전환: 자동공격, 웨이브 시스템, 상태 머신, 경험치·골드
- Phase 2 스탯 시스템 + AI 코어 업그레이드 화면
- Phase 3 상점 화면(무기·디스크 구매/교체, 새로고침·잠금)
- Phase 4 로봇 모딩(부품 상자 드랍, 부위별 장착, 소켓·무게 제약)
- Phase 5 데모 폴리시(HUD, 밸런싱, 승리/패배, 피드백)

### 데모 개발 계획 — 사용자 확정 사항 [완료]

계획 보고 후 사용자에게 확인받은 결정 5건. **이후 모든 구현은 이 결정을 전제로 한다.**

1. **탄약·재장전 전부 제거** — 자동공격 전환에 맞춰 공격속도(쿨다운)만으로 발사 제어.
   `weapon_capacity` / `weapon_reload` / `robot_capacity` / `robot_reload` 및
   `PlayerShootManager`의 `WeaponRuntimeState`(ammo/reload) 관련 코드는 제거 대상.
2. **데이터 전부 로컬로 이전** — 신규 테이블뿐 아니라 기존 5개 Google Sheets 테이블
   (몬스터/로봇/무기/방어구/드랍)도 로컬 에셋으로 이전. 런타임 네트워크 의존 제거.
   기획자 워크플로 보존을 위해 시트 → 로컬 에셋 **에디터 임포터**를 함께 만든다.
3. **무기 소켓은 머리 파츠가 결정** — 소켓 개수·타입 모두 머리 파츠에서 나온다.
4. **로봇 = 머리 파츠의 종류** — 머리 파츠 종류를 곧 "로봇"으로 취급한다(기획서 p.8의
   "머리 자체를 캐릭터로 해서 고정해도 됨" 안을 채택). 기본 로봇 **4종**.
   **머리 파츠는 런 중 변경되지 않는다**(모딩 대상 아님). 나머지 파츠(팔/다리/장갑/발/
   자기장 코어)는 기본 파츠로 시작해 부품 상자로 교체 가능.
   → 머리에서 결정되는 값: 무기 소켓 수·타입, 메모리(AI 코어 최대 레벨), 디스크 슬롯 수,
     필살기 슬롯, 헬멧 방어력
5. **데모 목표 분량: 10웨이브 + 보스** — 일정 15~20일(1인 기준)로 상향.
   필살기는 데모 범위 외(슬롯 UI만 노출, 동작 미구현).

**최종 단계 계획 (Phase 0~5)**

- **Phase 0 기반 정리 (1~1.5일)**
  - `RunState` 신설: 웨이브/골드/경험치·레벨/장착 무기·디스크/파츠/부품상자 보유
  - 데이터 로컬 이전: `ScriptableObject` 테이블 + 시트 CSV 임포터(에디터 전용).
    `GameDataManager`는 로컬 에셋을 읽도록 변경(`IsLoaded`/`OnLoaded` 기존 API는 유지해
    `EnemySpawner` 등 구독 코드를 깨지 않는다)
  - 알려진 이슈 처리: `RobotSelectManager.nextSceneName` → `Ground01`,
    `프로젝트 안내.md`의 프로젝트 경로 오타(`Y:\Unity\ComstockPJ`) 수정
- **Phase 1 전투 루프 전환 (3~4일)**
  - 탄약·재장전 제거, 소켓별 독립 쿨다운 자동 발사, 사거리 내 최근접 적 자동 조준
  - 다중 소켓(최대 6) 동시 사격 — 활성 슬롯 1개 구조 폐기, 슬롯 전환 키(1/2) 제거
  - `WaveManager`(웨이브 시간·스폰 압력 곡선·클리어 판정),
    `GameFlowManager`(전투 → AI코어 → 로봇정비 → 상점 → 다음 웨이브 상태 머신)
  - 적 처치 시 경험치·골드 드랍
- **Phase 2 스탯 시스템 + AI 코어 (2.5일)**
  - `RobotStats` 집계 레이어: 머리(로봇) 기본 + 파츠 + 디스크(+/-) + AI코어 누적 →
    기획서 p.13의 10개 스탯 산출. `PlayerRobotController`/`PlayerShootManager`는 결과만 읽음
  - 경험치 곡선, 레벨업, 업그레이드 3택 카드 UI, 메모리(머리)의 최대 레벨 제한
- **Phase 3 상점 (3일)**
  - 골드 소비, 품목 4칸(등급 가중치), 새로고침(비용 증가), 개별 잠금
  - 무기 구매=소켓 즉시 교체 / 디스크 구매=디스크 슬롯 장착, 디스크는 상승·하락 스탯 페어
  - 기획서 p.13 8요소 레이아웃 구현
- **Phase 4 로봇 모딩 (3일)**
  - 부품 상자 인게임 드랍 → 정비에서 개봉, 팔/다리 계열 파츠 교체
  - 제약 검증: 무기 무게 총합 ≤ 자기장 코어 + 다리 지탱력, 무기 타입 ≠ 소켓 타입 시 거부
- **Phase 5 보스 + 데모 폴리시 (3~4일)**
  - 10웨이브 보스 1종(패턴 포함), 웨이브 HUD·경험치 바·골드 표시, 승리/패배, 밸런싱 1패스

**검증 방식**: 각 Phase 종료 시 MCP로 playmode 실행 + Game View 스크린샷 확인, 결과를
이 문서에 이어서 기록한다.

### 디스크 슬롯 상한 관련 확인 [완료]

- 디스크 슬롯 개수에 데모 차원의 고정 상한(예: 12, 24)을 두지 않음.
- 기획서 p.8 그대로: **디스크 슬롯 최대 개수는 머리 파츠의 능력치 값**으로 결정된다.
  즉 로봇(머리) 종류마다 다른 디스크 슬롯 수를 갖도록 데이터화한다(고정 상수 아님).
- Phase 2(RobotStats)·머리 파츠 데이터 설계 시 `디스크 슬롯 수`를 머리 파츠 필드로
  반영. 이전 턴에서 언급한 "데모에서는 12로 낮춰 잡자"는 제안은 폐기.

### Phase 0 구현 [완료]

기반 정리 5개 항목을 모두 구현하고 MCP로 실제 동작 검증까지 완료.

**신규 파일**
- `Assets/Scripts/RunState.cs` — 웨이브/골드/AI코어 경험치·레벨/장착 무기·디스크/파츠/
  부품상자 보유를 담는 static class. `PlayerSession`/`GameOverManager`와 동일한 패턴.
  아직 웨이브·상점·모딩 시스템이 없어 실제 값 채우기는 Phase 1~4에서 연결.
- `Assets/Scripts/GameDataAsset.cs` — 몬스터/로봇/무기/방어구/드랍 5개 테이블을
  `List<T>`로 담는 `ScriptableObject`. `[CreateAssetMenu]`로 인스펙터에서 생성 가능.
- `Assets/Scripts/Editor/GameDataCsvImporter.cs` — 기획자가 구글시트를 로컬 에셋으로
  가져오는 에디터 도구(`Comstock/게임 데이터 가져오기` 메뉴). 5개 URL을 동기 다운로드해
  `CsvParser` + 기존 파싱 로직으로 `GameDataAsset`에 채워 넣고 저장. `ImportInto()`는
  static이라 script-execute 등에서 창 없이도 호출 가능.
- `Assets/Data/GameDataAsset.asset` — 위 임포터로 실제 시트 데이터를 가져와 생성
  (몬스터 2 / 로봇 2 / 무기 6 / 방어구 2 / 드랍 6행).

**수정 파일**
- `Assets/Scripts/GameDataManager.cs` — `UnityWebRequest`/코루틴 기반 CSV 다운로드
  코드를 전부 제거. `[SerializeField] GameDataAsset localData`를 Awake에서 동기적으로
  읽어 Dictionary들을 채우는 방식으로 교체. `IsLoaded`/`OnLoaded` API는 그대로 유지해
  `EnemySpawner`/`PlayerRobotController` 등 기존 구독 코드는 무수정으로 동작.
- `Assets/Scripts/RobotSelectManager.cs` — `nextSceneName` 기본값을 존재하지 않는
  `"SamplePlay00"`에서 `"Ground01"`로 수정(알려진 이슈 해결).
- `Ground01.unity` 씬의 `DataManager` GameObject → `GameDataManager` 컴포넌트의
  `localData` 필드에 `Assets/Data/GameDataAsset.asset` 연결, 씬 저장.
- `프로젝트 안내.md` — `unity-mcp-cli status` 경로 오타 수정, 확정된 방향성/진행상황 갱신
  (별도 커밋 전 항목이었으나 이번 작업 세션에서 실제로 적용함).

**검증 (MCP)**
1. `assets-refresh` 후 `console-get-logs`(Error 필터) — 컴파일 에러 없음
2. `script-execute`로 `GameDataCsvImporter.ImportInto()` 실행 → 로그로 정상 카운트 확인
   ("게임 데이터 가져오기 완료 - 몬스터 2 / 로봇 2 / 무기 6 / 방어구 2 / 드랍 6행")
3. `editor-application-set-state`로 playmode 진입 → `screenshot-game-view`로 확인:
   플레이어 로봇이 정상 스폰, HP 100/100 표시, 무기 스프라이트 정상 부착
   (네트워크 없이 로컬 에셋만으로 로딩 성공)
4. 콘솔에는 "선택된 로봇이 없어 테스트용 기본 로봇ID 100001로 대체" 경고만 있었음
   (씬을 로봇 선택 없이 바로 실행했을 때 발생하는 기존 의도된 폴백 동작, 버그 아님)
5. playmode 정상 종료 확인

**참고**: `console-clear-logs` 호출 1회가 `Temp/mcp-server/ai-editor-logs.txt` 파일 잠금으로
실패하는 MCP 플러그인 자체의 일시적 이슈를 관찰함(프로젝트 코드와 무관, 재현성 낮음 — 이후
재시도 없이 `console-get-logs`만으로 검증 계속함). 별도 조치 불필요로 판단.

**다음 단계**: Phase 1(전투 루프 전환 — 탄약/재장전 제거, 자동공격, 다중 소켓, 웨이브
매니저, 게임 흐름 상태 머신) 착수 대기.

### Phase 1 구현: 전투 루프 전환 [완료]

탄약/재장전 제거 + 자동공격 + 다중 소켓 + 웨이브/게임흐름 상태 머신을 구현하고
MCP playmode로 실제 동작을 확인했다.

**신규 파일**
- `Assets/Scripts/WaveManager.cs` — 웨이브는 시간이 다 되면 끝난다(전멸 조건 아님,
  뱀서라이크 문법). 웨이브가 오를수록 스폰 간격이 `spawnIntervalDecayPerWave`(기본 0.92)
  만큼 줄고 최대 생존수가 늘어난다. `OnWaveStarted`/`OnWaveEnded` 이벤트 발행.
  10웨이브+보스 처리는 Phase 5에서 이 매니저에 이어붙일 예정.
- `Assets/Scripts/GameFlowManager.cs` — 전투↔정비 상태 머신(`Combat`/`Intermission`).
  AI코어 업그레이드/로봇 정비/상점 화면은 아직 없어 임시 패널 1개로 대체
  (`intermissionPanel`/`intermissionText`/`nextWaveButton` 필드, 전부 비워둬도 안전하게
  동작). 실제 화면은 Phase 2~4에서 이 자리를 교체한다.

**수정 파일**
- `Assets/Scripts/PlayerShootManager.cs` — 전면 재작성. 마우스 조준/클릭, 무기 전환 키(1/2),
  탄약/재장전(`WeaponRuntimeState.ammo_remaining`/`is_reloading`) 전부 제거. 이제 모든 무기
  소켓이 각자 `weapon_range` 안의 최근접 적을 찾아 독립적으로 조준·발사(소켓별 개별 쿨다운).
  타겟이 없으면 대기 자세(`rest_rotation_degrees`)로 복귀. 소켓 개수 자체는 인스펙터에
  설정된 대로 그대로 사용(머리 파츠가 소켓 수/타입을 강제하는 규칙은 Phase 4에서 연결 예정).
- `Assets/Scripts/EnemyUnit.cs` — 정적 생존 목록 `Alive` 신설(PlayerShootManager가 최근접
  적을 찾을 때 순회). `Die()`에서 `RunState.CoreExp`/`RunState.Gold` 지급 추가
  — **가정**: 몬스터별 경험치/골드 데이터 컬럼이 시트에 아직 없어 `MaxHp` 비례 임시 공식
  (`exp = MaxHp/10`, `gold = MaxHp/20`) 사용. 실제 값 컬럼이 추가되면 교체 필요(사용자 확인 필요).
  또한 "드랍"이 아니라 처치 즉시 자동 지급으로 단순화했음(물리적 골드/경험치 오브젝트 없음).
- `Assets/Scripts/EnemySpawner.cs` — 웨이브 제어 인터페이스 추가: `SetSpawningEnabled`,
  `ConfigureDifficulty(spawnInterval, maxAlive)`, `DespawnAllAliveEnemies()`,
  `BaseSpawnInterval`/`BaseMaxAliveEnemies` 프로퍼티. 스폰 루프가 `IsSpawningEnabled`를 확인.
- `Assets/Scripts/GameHUD.cs` — 검증용 웨이브/골드/EXP 텍스트 필드 추가(선택적, 미연결 시 무시).
  아직 씬에 실제 UI 오브젝트로 연결하지 않음(Phase 5에서 정식 HUD 폴리시와 함께 연결).
- `Assets/Scripts/PlayerRobotController.cs` — Awake에서 `RunState.Reset()`,
  `EnemyUnit.Alive.Clear()` 추가(재시작 시 이전 판 상태 잔존 방지).
- `Assets/Scripts/GameDataManager.cs` — Awake에서 `Application.runInBackground = true` 설정
  (아래 검증 중 발견한 이슈의 영구 수정, 런타임 설정이라 ProjectSettings에는 영향 없음).
- Ground01 씬에 `GameFlow` GameObject 신설 → `WaveManager`(→`EnemyManager`의 `EnemySpawner`
  참조) + `GameFlowManager`(→`WaveManager` 참조) 컴포넌트 추가, 씬 저장.

**검증 (MCP)**
1. `assets-refresh` + `console-get-logs`(Error) — 컴파일 에러 없음
2. playmode 진입 후 **`Time.time`이 30초 넘게 거의 진행되지 않는 문제 발견** — Unity
   Editor 창이 포커스/화면 밖에 있을 때 플레이 루프가 거의 멈추는 현상(재현: 실시간은
   흐르는데 `Time.time`/`Time.unscaledTime`은 0.02초에 고정). `Application.runInBackground = true`
   런타임 설정으로 해결 확인 후 코드에 영구 반영(위 참고). **이후 MCP로 하는 모든 playmode
   검증은 이 설정이 있어야 실시간처럼 정확히 흐른다는 점을 기억할 것.**
3. `script-execute`로 `EnemySpawner.SpawnMonster()` 직접 호출 → 정상 스폰 확인(프리팹 매핑 정상)
4. 실제 웨이브 진행 중 상태 스냅샷: `gold=9 exp=18` (자동공격이 몬스터를 실제로 처치해
   보상을 지급함), Game View 스크린샷에서 골드/상자 아이콘과 무기 이펙트 확인, 플레이어
   HP 100/100 유지(몬스터가 도달 전에 처치됨)
5. 웨이브 1 제한시간(30초) 경과 → 콘솔에 "웨이브 1 종료" 로그, `GameFlowManager.CurrentState`
   가 `Intermission`으로 전환, `EnemySpawner.IsSpawningEnabled=false`, 잔존 적 0(despawn 확인)
6. `WaveManager.StartNextWave()` 호출 → 웨이브 2로 증가, 스폰 간격이 3→2.76(0.92배)로
   감소(난이도 곡선 공식대로 동작), 스폰 재개 확인
7. 게임플레이 관련 에러 없음(콘솔 에러는 전부 MCP 플러그인 자체의 `console-clear-logs`
   파일 잠금 이슈로, Phase 0에서도 동일하게 관찰된 것과 같은 종류 — 코드와 무관)

**사용자 확인이 필요한 가정 (구현은 완료, 값만 임시)**
- 몬스터 처치 보상(경험치/골드) 공식이 `MaxHp` 비례 임시값입니다. 실제 밸런스 값은
  시트에 컬럼이 추가되는 대로 교체가 필요합니다.
- "드랍" 대신 즉시 자동 지급 방식으로 단순화했습니다. 기획서상 물리적 드랍이 필요하다면
  Phase 5 폴리시에서 골드/경험치 오브젝트 픽업으로 바꿀 수 있습니다.

**다음 단계**: Phase 2(스탯 시스템 + AI 코어 업그레이드 화면) 착수 대기.

### 처치 보상을 픽업 방식으로 변경 [완료]

Phase 1에서 즉시 자동 지급으로 단순화했던 골드/경험치 보상을, 사용자 요청에 따라
필드 위 물리적 픽업 오브젝트 방식으로 변경(기획서의 "드랍" 표현에 맞춤).

**신규 파일**
- `Assets/Scripts/RewardPickup.cs` — `RewardType`(Gold/Exp) enum + 픽업 컴포넌트.
  `ItemPickup`과 동일한 패턴: 플레이어가 SphereCollider(트리거) 범위 안에 들어오면
  자동 흡수되어 `RunState.Gold`/`RunState.CoreExp`에 더해지고 사라진다.
- `Assets/Scripts/RewardPickupManager.cs` — `DropItemManager`와 동일한 패턴의 정적
  유틸리티. 처치 위치에 약간의 무작위 산개(최대 0.6유닛)를 두고 골드/EXP 픽업을 생성.
  골드 아이콘은 기존 `Resources/Gold.png` 재사용. **EXP 전용 아이콘이 아직 없어
  `Resources/Energy.png`(원래 투사체용)를 임시로 재사용** — 나중에 전용 아이콘으로
  교체 권장(사용자 확인 필요).

**수정 파일**
- `Assets/Scripts/EnemyUnit.cs` — `GrantKillRewards()`가 `RunState`에 즉시 더하는 대신
  `RewardPickupManager.SpawnReward()`를 호출하도록 변경.

**검증 (MCP)**
1. 컴파일 에러 없음
2. playmode 진입 → `Application.runInBackground` 자동 적용 확인(수동 개입 없이
   `Time.time`이 정상 진행됨 - Phase 1에서 추가한 수정이 잘 동작함)
3. 자동공격으로 몬스터 처치 → 필드에 `RewardPickup` 오브젝트 14~18개 생성 확인
   (플레이어가 이동하지 않아 미흡수 상태로 남음 - **의도된 정상 동작**: 이전처럼
   즉시 지급이 아니라 실제로 가까이 가야 흡수되는 것을 증명)
4. `RewardPickupManager.SpawnReward(Gold, 777, playerPos)`를 플레이어 정확한 위치에
   직접 생성 → 즉시 흡수되어 `RunState.Gold=777` 반영 확인 (트리거 픽업 로직 자체는
   정상 동작 확인). 플레이어 콜라이더는 `isTrigger=false`, `Rigidbody.isKinematic=false`로
   확인 - `ItemPickup`과 동일한 조건에서 정상 동작.

**남은 확인사항**: EXP 픽업 아이콘이 임시로 무기 투사체 이미지(Energy.png)를 재사용 중.
전용 아이콘이 생기면 `RewardPickupManager.ExpResourceName`만 교체하면 됨.

**다음 단계**: Phase 2(스탯 시스템 `RobotStats` 집계 레이어 + AI 코어 업그레이드 화면) 진행.

### Phase 2 구현: 스탯 시스템 + AI 코어 업그레이드 화면 [완료]

`RobotStats` 집계 레이어와 AI 코어 경험치·레벨업·업그레이드 3택 카드 UI를 구현하고
MCP playmode로 전체 흐름(레벨업→카드 노출→선택→스탯 반영→정비→다음 웨이브)을 검증했다.

**신규 파일**
- `Assets/Scripts/StatType.cs` — AI 코어/디스크(Phase 3)/파츠(Phase 4)가 공통으로 가감할
  로봇 스탯 enum(MaxHp/Atk/Def/MoveSpeed/Avoid/Luck/CritChance/CritDamage/Mass)
- `Assets/Scripts/RobotStats.cs` — `AggregatedRobotStats` 구조체 + `Compute(RobotData)`.
  최종 스탯 = 머리(로봇) 기본값 + `RunState.CoreStatBonuses` 누적. 디스크·파츠 보너스는
  아직 시스템이 없어 미반영(해당 시스템이 생기면 이 함수에 파라미터를 추가해 같은 방식으로 더하면 됨)
- `Assets/Scripts/AiCoreUpgradePool.cs` — 업그레이드 선택지 풀 ScriptableObject
  (표시이름/설명/StatType/수치). 시트 데이터가 아니라 기획자가 직접 채우는 로컬 전용 데이터
- `Assets/Scripts/AiCoreManager.cs` — 경험치→레벨업 판정(임시 선형 공식 `expPerLevel*레벨`),
  메모리(머리 파츠) 최대 레벨의 임시값(`coreMaxLevelPlaceholder=20`), 업그레이드 3택 추첨
  (`DrawChoices`), 선택 적용(`ApplyChoice` → `RunState.CoreStatBonuses` 누적)
- `Assets/Data/AiCoreUpgradePool.asset` — 기획서 p.10 예시 3개(사격술 최적화/타겟 추적
  기술/A* 알고리즘) + 밸런스 미확정 임시 옵션 5개(체력/방어력/회피율/행운/치명타피해),
  총 8개

**수정 파일**
- `Assets/Scripts/RunState.cs` — `CoreStatBonuses`(Dictionary<StatType,float>),
  `PendingCoreUpgradeChoices` 추가. **`Reset()`에서 `OnChanged = null` 제거** — 아래
  버그 참고
- `Assets/Scripts/PlayerRobotController.cs` — `InitFromSession`이 `RobotData`를 그대로
  쓰지 않고 `RobotStats.Compute()` 결과를 사용하도록 변경. `RunState.OnChanged` 구독 추가:
  AI 코어 업그레이드 등으로 보너스가 바뀔 때마다 `ApplyAggregatedStats()`로 즉시 재계산.
  최대 체력이 늘어난 만큼 현재 체력도 함께 채워줌(로그라이크 통상 관례)
- `Assets/Scripts/GameFlowManager.cs` — 웨이브 종료 시 `RunState.PendingCoreUpgradeChoices > 0`
  이면 AI 코어 업그레이드 카드 패널을 먼저 보여주고(3개 버튼, 선택 시 스탯 적용 후 다음
  대기 카드 또는 정비 패널로 진행), 없으면 바로 기존 정비 임시 패널로 이동
- Ground01 씬: `GameFlow` GameObject에 `AiCoreManager` 컴포넌트 추가(`upgradePool` 연결).
  `Canvas` 아래에 `AiCoreUpgradePanel`(제목+옵션 버튼 3개, `GameOver` 텍스트를 복제해
  RectTransform/폰트 설정 재사용)과 `IntermissionPanel`(정보 텍스트+다음 웨이브 버튼)을
  신설, `GameFlowManager`의 모든 필드를 여기 연결. 씬 저장

**발견 및 수정한 버그: RunState.OnChanged 초기화 순서 버그**
- `RunState.Reset()`이 매 런 시작마다(`PlayerRobotController.Awake()`) `OnChanged`
  이벤트를 강제로 `null`로 비웠는데, Unity의 Awake+OnEnable 처리 순서는 오브젝트별로
  묶여서(오브젝트A의 Awake→OnEnable, 오브젝트B의 Awake→OnEnable, ...) 진행되므로
  **`AiCoreManager.OnEnable()`의 구독이 `Player`의 Awake보다 먼저 실행되면 그 구독이
  지워지는** 문제가 있었다. 실제로 재현됨(구독자 수 1개만 남음, `AiCoreManager` 누락)
- 수정: `Reset()`에서 `OnChanged = null` 제거. 각 구독자는 이미 자신의 `OnDisable()`에서
  스스로 구독 해제하므로 강제 초기화가 필요 없었음
- **오브젝트 생성 순서에 따라 조용히 실패할 수 있는 버그였으므로, 앞으로 static 상태에
  이벤트를 추가할 때는 `Reset()`에서 이벤트 자체를 비우지 않는 이 패턴을 따를 것**

**검증 (MCP)**
1. 컴파일 에러 없음(스크립트 3차 반복 후 최종 확인)
2. `RunState.CoreExp += 10; NotifyChanged()` 실행 → 최초엔 레벨업 안 됨(버그 재현,
   `AiCoreManager` 구독 누락 확인) → 버그 수정 후 재검증: `level=2 pending=1` 정상 반영
3. 웨이브 종료 시뮬레이션(`GameFlowManager.HandleWaveEnded` 직접 호출) →
   `AiCoreUpgradePanel` 활성화, `IntermissionPanel` 비활성화, 3개 옵션에 실제 무작위
   업그레이드(예: "행운의 회로/행운 +1", "A* 알고리즘/이동속도 +1.2", "냉각 시스템
   강화/체력 +10") 텍스트 정상 표시 확인(Game View 스크린샷 포함)
4. 옵션 버튼 클릭(`button.onClick.Invoke()`) → 선택한 보너스가 `PlayerRobotController`에
   실시간 반영됨(`Luck 0→1` 확인), `PendingCoreUpgradeChoices 1→0`, 패널이 자동으로
   정비 임시 패널로 전환됨
5. "다음 웨이브 시작" 버�지 클릭 → `RunState.WaveNumber 1→2`, `GameFlowManager.CurrentState`
   가 `Combat`으로 복귀
6. 위 과정에서 발생한 콘솔 에러 4건은 전부 검증용으로 내가 직접 짠 임시 스크립트 자체의
   버그(비활성 오브젝트에 `GameObject.Find` 호출 → NRE)였고, 게임 코드와는 무관함을 로그로 확인

**발견했지만 수정하지 않은 이슈 (사용자 확인 필요)**
- **한글 폰트 미지원**: 새로 만든 UI 텍스트(및 기존 `GameOver` 텍스트도 동일)가
  `LiberationSans SDF`(Unity 기본 TMP 폰트) 사용 중이며, 이 폰트에는 한글 글리프가 없어
  실제 플레이 화면에서 한글이 네모 박스(tofu)로 깨져 보임. 지금까지는 UI에 한글 텍스트를
  실제로 띄운 적이 없어(HP 표시는 숫자/영문뿐) 발견되지 않았던 프로젝트 전반의 이슈.
  한글 지원 TMP 폰트 에셋(예: Noto Sans KR, 나눔고딕 등)을 도입해야 하며, 폰트 선택/라이선스는
  사용자 확인이 필요해 임의로 진행하지 않음
- AI 코어 업그레이드 카드 8종의 수치는 전부 밸런스 미확정 임시값(기획서 예시 3개 제외)

**다음 단계**: 위 한글 폰트 이슈에 대한 사용자 결정 대기, 이후 Phase 3(상점) 진행.

### 한글 폰트 미지원 이슈 해결 [완료] (2026-08-06)

Phase 2에서 발견된 한글 tofu(네모 박스) 이슈를 해결했다. 사용자에게 폰트 후보
(Noto Sans KR / Pretendard / 나눔고딕)를 제시해 **Noto Sans KR**로 확정받았고, 파일
다운로드 전에도 별도로 승인을 받은 뒤 진행했다.

**신규 파일**
- `Assets/Fonts/NotoSansKR/NotoSansKR-Regular.ttf`, `NotoSansKR-Bold.ttf` — Google Fonts
  공식 저장소(`github.com/google/fonts`, SIL OFL 1.1)에서 배포하는 가변 폰트
  `NotoSansKR[wght].ttf`를 받아, `fonttools varLib.instancer`로 wght=400/700 정적
  인스턴스를 로컬에서 추출했다(Google Fonts가 이 폰트를 가변 폰트로만 배포하고 정적
  weight별 TTF를 별도 제공하지 않기 때문). 이름 테이블(family/subfamily)과
  macStyle/fsSelection의 Bold 플래그도 함께 정리했다
- `Assets/Fonts/NotoSansKR/OFL.txt` — 라이선스 원문(재배포 시 포함 필요, SIL OFL 1.1)
- `Assets/Fonts/NotoSansKR/NotoSansKR-Regular SDF.asset`,
  `NotoSansKR-Bold SDF.asset` — 위 TTF로부터 생성한 TextMeshPro Font Asset. 한글
  음절이 11,000자가 넘어 전체를 미리 굽는 정적 아틀라스는 비현실적이므로
  `AtlasPopulationMode.Dynamic`(런타임에 필요한 글리프만 아틀라스에 추가)으로 생성.
  에디터 UI(Font Asset Creator 창) 없이 `TMP_FontAsset.CreateFontAsset(...)` API를
  `script-execute` MCP 도구로 직접 호출해 생성

**수정 파일**
- `Assets/TextMesh Pro/Resources/TMP Settings.asset` — `m_fallbackFontAssets`에
  `NotoSansKR-Regular SDF`, `NotoSansKR-Bold SDF`를 순서대로 추가(TMP의 전역 폴백
  체인). 기존 `LiberationSans SDF`에 글리프가 없는 문자(한글 등)를 만나면 자동으로
  이 폴백 폰트에서 글리프를 찾아 렌더링한다. **개별 텍스트 컴포넌트나 폰트 지정을
  하나도 건드리지 않고** 프로젝트 전역에 적용되는 방식을 택함 — 영문/숫자 UI의 기존
  모양은 그대로 유지되고, 한글만 자동으로 보완되는 최소 침습적 수정

**검증 (MCP, `script-execute`로 직접 확인 — 이번 세션에서는 `editor-application-*`,
`screenshot-*` 계열 MCP 도구가 "No such tool available"로 응답하지 않아 플레이모드
진입/스크린샷 캡처는 하지 못했다. 대신 아래처럼 TMP의 텍스트 생성 결과와 콘솔 로그를
코드로 직접 검사해 확인함)**
1. 수정 전 콘솔 로그 확인: `[LogCollector]`에 "The character with Unicode value ...
   was not found in the [LiberationSans SDF] font asset or any potential fallbacks"
   경고가 `TitleText`/`IntermissionInfoText`/`NextWaveButtonText`/`Option3Text`에서
   총 24건 기록되어 있었음(모두 폴백 적용 이전 타임스탬프) — 기존 버그 재확인
2. 폴백 적용 후 `TitleText`("AI 코어 레벨업 - 하나를 선택하세요")에 대해
   `TextMeshProUGUI.ForceMeshUpdate()` 실행 → `textInfo.characterInfo[]`를 순회한 결과
   모든 한글 문자(코/어/레/벨/업/하/나/를/선/택/세/요)가 `isVisible=true`이며
   `fontAsset=NotoSansKR-Regular SDF`로 정상 렌더링됨을 확인. 영문/숫자/기호(A, I, -)는
   기존대로 `LiberationSans SDF` 유지
3. 같은 방식으로 `Option1~3Text`, `GameOver`, `IntermissionInfoText`,
   `NextWaveButtonText`도 모두 확인 — 한글 포함 텍스트는 전부 `NotoSansKR-*` 폰트로
   정상 해석됨
4. 검증 과정에서 콘솔 로그를 다시 확인해 "not found ... font asset or any potential
   fallbacks" 경고가 **한 건도 새로 발생하지 않았음**을 확인(위 1번의 24건은 전부
   수정 이전 타임스탬프였고, 이후 발생한 경고는 무관한 MCP 도구 사용 실수 1건뿐)
5. **미검증**: 실제 플레이모드 화면에서 눈으로 보는 시각적 확인(스크린샷)은 이번
   세션에서 관련 MCP 도구가 응답하지 않아 수행하지 못함. 다음에 관련 도구가 복구되면
   Game View 스크린샷으로 재확인 권장

**참고**
- `Assets/Fonts/NotoSansKR/`에 있는 원본 가변 폰트(`NotoSansKR[wght].ttf`)는 중간
  산출물이라 저장하지 않고 삭제함 — 필요하면 위 Google Fonts 저장소 경로에서 다시
  받으면 됨
- 폴백 체인은 전역 설정이므로 앞으로 새로 추가되는 UI 텍스트도 폰트를 따로 지정하지
  않는 한 자동으로 한글이 보인다. 다만 진짜 한글 타이포그래피 품질(자간/줄바꿈 등)을
  세밀하게 다듬으려면 개별 텍스트에 `NotoSansKR-* SDF`를 명시적으로 지정하는 편이 낫다
- `TMP_Settings.m_UseModernHangulLineBreakingRules` 옵션이 존재함(현재 `false`) —
  긴 한글 문장이 많아지면 줄바꿈 품질을 위해 `true` 전환 검토 가치 있음

**다음 단계**: Phase 3(상점) 진행.

### Phase 3 구현: 상점 [완료] (2026-08-06)

기획서 p.13의 상점 화면 8개 요소를 모두 구현하고, MCP 플레이모드로 품목 생성 → 잠금 →
새로고침 → 구매(무기/디스크) → 스탯 반영 → 다음 웨이브까지 전체 흐름을 검증했다.

**사용자 확정 사항 (진행 전 확인받음)**
1. **무기 등급·가격 데이터는 별도 로컬 에셋에 둔다** — 구글시트 무기 테이블에 컬럼을
   추가하지 않는다. 이유: (1) 기획자 시트 스키마를 건드리지 않아도 되고, (2)
   `GameDataCsvImporter`가 컬럼을 **순서(인덱스)** 로 읽기 때문에 시트 중간에 컬럼이
   끼어들면 기존 파싱이 조용히 어긋난다. 나중에 시트로 옮기려면 에셋 값을 그대로 컬럼으로
   옮기면 된다.
2. **무기 등급 5단계** — 일반(Normal) / 희귀(Rare) / 서사(Epic) / 유일(Unique) /
   전설(Legendary). 무기 6종은 그대로 두고 등급별 배율을 곱해 상위 등급을 만들어내는
   방식(수직 강화). 기획자가 나중에 등급별 무기 행을 실제로 채우면 `ShopCatalog.WeaponEntry`에
   등급별 weapon_id를 직접 지정하는 방식으로 바꾸면 된다.

**신규 파일**
- `Assets/Scripts/ItemGrade.cs` — 등급 enum + 한글 표시명/표시 색상. 등급 이름·색은 밸런스
  수치가 아니라 표시용 고정값이라 데이터 에셋이 아니라 코드에 둔다
- `Assets/Scripts/DiscData.cs` — 디스크 1종의 정의(상승 스탯/하락 스탯 페어, 등급, 가격).
  `StatTypeNames`(StatType → 한글 표시명)도 여기 함께 둠
- `Assets/Scripts/ShopCatalog.cs` — 상점이 쓰는 모든 데이터를 담는 ScriptableObject.
  등급 설정(스탯 배율/가격 배율/등장 가중치/최소 등장 웨이브), 무기 목록(+기본 가격),
  디스크 목록, 품목 칸 수, 디스크 등장 확률, 새로고침 비용, 디스크 슬롯 수(임시값)
- `Assets/Scripts/ShopManager.cs` — 상점 로직. 품목 생성(등급 가중치 추첨), 새로고침
  (잠긴 칸 유지), 개별 잠금, 구매/골드 차감, 디스크 장착
- `Assets/Scripts/ShopPanelUI.cs` — 상점 화면 표시/입력. 기획서 p.13의 8개 요소를 모두 채움
- `Assets/Data/ShopCatalog.asset` — 무기 6종(가격 25~45골드) + 디스크 8종. **수치는 전부
  밸런스 미확정 임시값**

**수정 파일**
- `Assets/Scripts/RunState.cs` — `EquippedWeaponIds`(List&lt;int&gt;)를 `EquippedWeapons`
  (무기ID + 등급 + 스탯 배율 구조체 목록)로 교체. `DiscStatBonuses`(디스크 증감 합계),
  `ShopRefreshCount` 추가. 기존 `EquippedWeaponIds`는 아직 아무 데서도 쓰이지 않던 자리표라
  깨지는 호출부 없음
- `Assets/Scripts/RobotStats.cs` — `Compute()`가 `DiscStatBonuses`도 합산.
  디스크 하락 스탯 때문에 이동속도/체력이 0 이하로 내려가 이동 불가·즉사가 되지 않도록
  스탯별 최소값을 둠
- `Assets/Scripts/PlayerShootManager.cs` — 런타임 무기 교체 API `EquipWeapon(소켓, 무기ID,
  등급, 배율)` 추가. 등급 배율을 공격력·공격속도에 반영(로봇 스탯 `robot_atk`에는 곱하지
  않고 무기 성능에만 곱한다). 교체 시 이미지 갱신 + 쿨다운 초기화. UI 표시용
  `SocketCount`/`TryGetSocketInfo` 노출. 시작 무기는 상점을 거치지 않았으므로 일반 등급으로 취급
- `Assets/Scripts/GameFlowManager.cs` — 임시 정비 패널을 상점 화면으로 교체.
  흐름: 웨이브 종료 → (대기 중인 AI 코어 업그레이드 전부 처리) → 상점(항상 노출) →
  다음 웨이브. "다음 웨이브 시작" 버튼은 이제 상점 화면 안에 있다(기획서 p.13의 3번 요소)
- Ground01 씬 — `Canvas` 아래 `ShopPanel` 신설(8요소 + 무기 소켓 선택 오버레이),
  `GameFlow`에 `ShopManager` 컴포넌트 추가(카탈로그 연결), `GameFlowManager.shopPanel` 연결.
  **임시 `IntermissionPanel`은 Phase 3가 대체하는 자리였으므로 삭제함**

**설계 메모**
- 디스크 증감치는 장착 시점에 계산해 `RunState.DiscStatBonuses`에 누적한다. 이렇게 하면
  `RobotStats`(static)가 `ShopCatalog`(에셋)를 직접 조회할 필요가 없다. 무기 등급 배율을
  `RunState.EquippedWeapons`에 저장해두는 것도 같은 이유(`PlayerShootManager`가 카탈로그를
  몰라도 되게)
- 무기 구매는 "소켓 즉시 교체"라 어느 소켓을 바꿀지 골라야 한다. 품목 카드를 누르면 소켓
  선택 오버레이가 열리고, 소켓 버튼을 눌러야 실제 구매가 끝난다. 디스크는 슬롯에 자리만
  있으면 바로 구매된다
- 구매한 칸은 새로고침하면 새 품목으로 채워진다(칸이 영구히 죽어버리면 새로고침 가치가 없어서)

**발견 및 수정한 문제 3건**
1. **패널이 열리자마자 다시 닫히는 버그** — `ShopPanelUI.Awake()`에서
   `gameObject.SetActive(false)`를 호출했는데, 이 패널은 씬에서 비활성으로 시작하므로
   Awake가 "처음 열릴 때" 실행된다. 즉 `Open()`으로 켜자마자 Awake가 다시 꺼버렸다.
   → Awake에서 그 호출을 제거하고, 버튼 리스너 등록도 `Start`가 아니라 `Awake`로 옮겨
   열자마자 클릭이 동작하게 함
2. **디스크에 등급 최소 웨이브가 적용되지 않던 문제** — 무기는 `RollGrade`가 minWave를
   보는데 디스크는 자기 등급이 고정이라 그 검사를 우회해서, **1웨이브에 전설 디스크가
   등장**했다. → `ShopCatalog.IsGradeAvailable()`을 추가하고 디스크 후보를 현재 웨이브에
   등장 가능한 등급으로 먼저 거르도록 수정
3. **UI 가독성 문제 3가지** — 전투 HUD(HP)가 상점 화면 뒤로 비쳐 "웨이브 N" 표시와 겹침 /
   "다음 웨이브 시작" 글자가 잘림 / 품목 카드 본문 줄바꿈이 어색함.
   → `hideWhileOpen`으로 상점이 열려 있는 동안 HP HUD를 숨기고, 버튼 폭을 넓히고,
   모든 텍스트에 TMP 자동 크기 조절(`enableAutoSizing`, 최소 6)을 적용.
   **Canvas가 `ConstantPixelSize`라 Game View 해상도(플레이모드 실측 640x480)에 따라 고정
   픽셀 폰트가 칸을 넘치는 구조적 문제가 있어서, 앞으로 추가되는 UI 텍스트도 자동 크기
   조절을 켜는 편이 안전하다**

**검증 (MCP 플레이모드)**
1. 컴파일 에러 없음
2. 시작 시 `RunState.EquippedWeapons`가 인스펙터 소켓 2개(라이플/기관단총, 일반 등급)로
   정상 동기화됨
3. 상점 열림: 품목 4칸 생성, 등급별 배율·가격이 정확히 적용됨(예: 희귀 스나이퍼 =
   공격력 10×1.3=13, 공격속도 1×1.3=1.3, 가격 40×1.8=72골드)
4. **잠금 + 새로고침**: 0번 칸을 잠그고 새로고침 → 잠긴 칸은 그대로 유지, 다른 칸만 교체됨.
   비용 10 → 15골드로 증가, 골드 정상 차감
5. **디스크 구매**: '경량 프레임'(이동속도 +0.6 / 체력 -8) 구매 → `DiscStatBonuses`에
   `MoveSpeed=0.6, MaxHp=-8` 누적, 플레이어 실측 이동속도 3→3.6, 최대체력 100→92로 즉시 반영
6. **무기 구매·소켓 교체**: 소켓 0의 라이플 → 구매한 에너지 무기로 교체됨. 골드 차감,
   `RunState.EquippedWeapons[0]` 갱신, 무기 스프라이트도 갱신 확인
7. **등급 배율이 실제 발사 데미지에 반영되는지** — `ComputeDamage`를 배율별로 2000회씩
   샘플링: 배율 1/1.3/2.2/3에서 최소 데미지가 각각 20/23/32/40으로 기대값
   (무기atk×배율 + 로봇atk)과 정확히 일치, 최대값은 크리티컬(+50%) 적용값과 일치
8. **등급 minWave 게이팅** — 웨이브 1/4/8에서 각 300회씩 품목을 생성해 분포 확인:
   웨이브 1 = 일반·희귀만 / 웨이브 4 = 서사 추가 / 웨이브 8 = 유일·전설까지 등장.
   무기와 디스크 양쪽 모두에 정상 적용됨
9. **다음 웨이브 전환** — 버튼 실제 클릭 시 `Intermission → Combat`, 웨이브 1 → 2,
   상점 패널 닫힘, HP HUD 복원 확인
10. **화면 캡처로 시각 확인** — 8개 요소가 모두 채워지고 한글이 정상 표시되며 등급 색상
    (일반 회색 / 희귀 파랑 / 서사 보라 / 유일 주황 / 전설 빨강)도 정상. 구매한 칸은
    "구매 완료"로 바뀜

**검증 방법에 대한 메모**: 이번 세션에서도 `screenshot-*` / `editor-application-*` MCP 도구는
목록에 노출되지 않아 사용하지 못했다. 대신 `script-execute`로 `EditorApplication.EnterPlaymode()`를
호출해 플레이모드에 들어가고, 화면 캡처는 **캡처하는 동안만 Canvas를 `ScreenSpaceCamera`로
바꿔 `RenderTexture`에 렌더 → PNG 저장 → 원래 `ScreenSpaceOverlay`로 복원**하는 방식으로 했다
(`ScreenCapture.CaptureScreenshot`과 `WaitForEndOfFrame` 코루틴은 Game View가 렌더링되지 않는
상태에서 완료되지 않아 실패했다). 이 캡처 방식에서는 월드 스프라이트(플레이어 로봇)가 UI 위로
보이는데, 이는 캡처용으로 렌더 모드를 바꾼 탓이며 실제 Overlay 모드에서는 UI가 항상 위에 그려진다.

**남은 임시값 / 후속 과제**
- `ShopCatalog`의 등급 배율·가격·가중치·minWave, 디스크 8종의 수치는 **전부 밸런스 미확정 임시값**
- 디스크 슬롯 수(6칸)는 임시값 — 원래 머리 파츠 능력치가 정하는 값이라 Phase 4에서 대체
- 무기 소켓이 아직 2개뿐(인스펙터 설정). 기획서 예시는 6칸이며, 소켓 개수·타입을 머리 파츠가
  정하는 규칙은 Phase 4에서 연결
- 로봇 모딩 상태(p.13의 4번)는 팔/다리 파츠 시스템이 없어 머리에서 나오는 값만 실제로 보여주고
  나머지 부위는 "기본"으로 표기. 필살기도 데모 범위 밖이라 슬롯만 노출
- 기획서 p.13의 능력치 10종 중 **공격속도**는 로봇 스탯이 아니라 무기별 값이라 능력치 패널에는
  넣지 않고 품목 카드에만 표시했다

**다음 단계**: Phase 4(로봇 모딩 - 부품 상자 드랍, 팔/다리 파츠 교체, 소켓·무게 제약) 진행.

### Phase 4 구현: 로봇 모딩 [완료] (2026-08-06)

기획서 p.8의 팔/다리 모딩 6부위(무기소켓/팔장갑/자기장코어/다리/다리장갑/발)를 구현하고,
MCP 플레이모드로 기본 파츠 초기화 → 부품 상자 드랍 → 개봉 → 장착 → 스탯 반영 →
무기 타입/무게 제약이 실제 상점 구매를 막는지까지 전체 흐름을 검증했다.

**사용자 확정 사항 (진행 전 확인받음)**
1. **무기 타입 6종** — 연사화기 / 산탄화기 / 정밀화기 / 폭발화기 / 에너지무기 / 근접무기.
   근접무기는 현재 데이터에 해당 무기가 없어 향후 확장용으로만 정의됨
2. **부품 상자도 상점과 동일한 5단계 등급**(일반/희귀/서사/유일/전설)을 사용
3. **파츠 데이터는 로컬 에셋 전용** — "어떠한 에셋도 프로젝트 파일 외부를 참조해서는 안
   된다"는 사용자 지시에 따라, 무기 타입·무게·머리별 소켓개수/디스크슬롯수까지 전부
   시트(WeaponData/RobotData)를 건드리지 않고 새 로컬 매핑 테이블로 관리
4. **정비 화면 배치는 사용자 선호 없음** → 기획서 순서(전투→AI코어→로봇정비→상점) 그대로
   상점 앞에 별도 화면으로 구현

**설계상 단순화 (사용자에게 묻지 않고 구현 중 판단한 부분 - 데모 스코프 고려)**
- 기획서는 "팔"에 무기소켓/팔장갑/자기장코어 3개, "다리"에 무게지탱·이동기술/다리장갑/발
  3개, 총 6개의 세부 파츠 개념이 있는데, 이를 팔 1개·다리 1개의 복합 파츠가 아니라
  **6개의 독립된 장착 슬롯**(`PartSlot` enum)으로 모델링함 — `RunState.EquippedPartIds`
  (기존에 있던 부위이름→ID 딕셔너리)를 그대로 6칸으로 채우는 구조라 자연스럽게 맞았음
- 무기 소켓 파츠 하나가 **모든 무기 소켓에 공통으로 적용**되는 방식으로 단순화함(소켓별로
  다른 파츠를 낄 수 있는 구조는 데모 범위를 넘어선다고 판단). 기본 파츠는 타입 제한이
  없어서(`restrictsWeaponType=false`) 시작 시 서로 다른 타입의 무기 2개(라이플=정밀화기,
  기관단총=연사화기)를 동시에 낀 상태와 충돌하지 않음 — 제약은 이후 "무기를 소켓에 넣는"
  행위(상점 구매) 시점에만 검사되고, 이미 장착된 무기를 소급 검증하지 않음
- 기획서의 "다리: 무게 지탱 / **이동 기술**"에서 "이동 기술"을 회피(Avoid) 스탯으로,
  "발: **기동력**"을 이동속도(MoveSpeed) 스탯으로 해석해서 두 부위가 서로 다른 스탯을
  담당하도록 함(둘 다 이동속도로 하면 발과 다리가 기능적으로 중복됨)

**신규 파일**
- `Assets/Scripts/WeaponType.cs` — 무기 타입 enum + 한글 표시명
- `Assets/Scripts/PartSlot.cs` — 6개 장착 슬롯 enum + 한글 표시명
- `Assets/Scripts/PartData.cs` — 파츠 1종의 정의(슬롯/등급/무기타입제한/스탯보너스/무게지탱).
  슬롯마다 의미 있는 필드가 달라 DiscData와 같은 방식으로 구조체 하나에 다 담음
- `Assets/Scripts/PartsCatalog.cs` — 파츠 목록, 무기ID→타입/무게 매핑(`weaponMeta`),
  로봇ID→무기소켓개수·디스크슬롯수(`headModdingInfos`), 부품 상자 등급 가중치, 드랍 확률.
  전부 로컬 데이터이며 어떤 필드도 프로젝트 밖 리소스를 참조하지 않음
- `Assets/Scripts/ModdingManager.cs` — 정비 로직. 런 시작 시 6슬롯 기본 파츠 채우기,
  부품 상자 개봉(등급 추첨→파츠 추첨→즉시 장착), 무기 타입 제한 검사
  (`CheckWeaponTypeAllowed`), 무게 제한 검사(`CheckWeightLimit`)
- `Assets/Scripts/ModdingPanelUI.cs` — 정비 화면(상자 열기, 장착된 6파츠 표시, 상점으로 이동)
- `Assets/Data/PartsCatalog.asset` — 기본 파츠 6개(슬롯당 1개, 무보너스) + 루트 전용 파츠
  11개(등급 다양). **수치는 전부 밸런스 미확정 임시값**

**수정 파일**
- `Assets/Scripts/RunState.cs` — `PartStatBonuses`(Dictionary&lt;StatType,float&gt;) 추가.
  디스크(누적만 함)와 달리 파츠는 같은 슬롯에 새 파츠를 끼우면 이전 파츠의 보너스가
  사라져야 해서, 장착할 때마다 `ModdingManager`가 이 값을 처음부터 다시 계산해서 채움
- `Assets/Scripts/RobotStats.cs` — `Compute()`가 `PartStatBonuses`도 합산(머리 기본값 +
  AI코어 + 디스크 + 파츠, 전부 같은 방식)
- `Assets/Scripts/RewardPickup.cs` — `RewardType.PartBox` 추가. 골드/경험치처럼 필드
  픽업으로 생성되며, 습득 시 즉시 개봉되지 않고 `RunState.UnopenedPartBoxCount`만 증가
  (개봉은 정비 화면에서)
- `Assets/Scripts/RewardPickupManager.cs` — 부품 상자 아이콘(`Resources/ItemBox.png`) 매핑 추가
- `Assets/Scripts/EnemyUnit.cs` — 처치 시 `PartsCatalog.PartBoxDropChance`(기본 5%) 확률로
  부품 상자를 추가 드랍. `ModdingManager` 참조를 정적 캐시로 두고(몬스터마다 새로 찾지
  않도록), `PlayerRobotController.Awake()`가 런 시작마다 캐시를 비워줌(재시도 시 이전 판
  참조가 남지 않도록 - `EnemyUnit.Alive.Clear()`와 같은 이유)
- `Assets/Scripts/PlayerRobotController.cs` — `EnemyUnit.ResetStaticCaches()` 호출 추가
- `Assets/Scripts/ShopManager.cs` — 무기 구매 시 `ModdingManager`를 통해 타입 제한·무게
  제한을 검사하는 `CanPurchaseWeaponIntoSocket()` 추가(`TryPurchaseWeaponIntoSocket()`이
  이걸 거치도록 변경). 디스크 슬롯 수(`DiscSlotCount`)를 Phase 3의 고정 임시값 대신
  `PartsCatalog.HeadModdingInfo`에서 로봇ID로 조회하도록 교체(정비 시스템을 못 찾으면
  기존 임시값으로 안전하게 폴백)
- `Assets/Scripts/ShopPanelUI.cs` — 소켓 선택 창에서 타입/무게 제약으로 못 넣는 소켓은
  버튼을 비활성화하고 이유를 빨간 글씨로 표시. "로봇 모딩 상태"(p.13의 4번)를 하드코딩된
  "기본" 대신 실제 장착 파츠로 표시
- `Assets/Scripts/GameFlowManager.cs` — 정비 단계에 로봇 정비 화면을 삽입:
  `웨이브 종료 → (AI 코어 업그레이드, 대기 중일 때) → (로봇 정비, 부품 상자 있을 때) →
  상점(항상) → 다음 웨이브`. 부품 상자가 남아있어도 "상점으로" 버튼으로 언제든 건너뛸 수
  있음(강제 소진 아님, 다음 웨이브 정비 때 이어서 열 수 있음)
- Ground01 씬 — `Canvas` 아래 `ModdingPanel` 신설, `GameFlow`에 `ModdingManager` 컴포넌트
  추가(카탈로그 연결), `GameFlowManager.moddingPanel` 연결

**발견 및 수정한 버그: 정비 단계 화면 동시 노출**
- 테스트 중 `ShowModdingStep()`이 상점 패널을 닫지 않고 열어서, 대기 시간 동안 실제
  웨이브 타이머가 자연 종료돼 상점이 먼저 열린 상태에서 정비 화면이 겹쳐 열리는 상황을
  발견함(웨이브 타이머가 30초라 MCP 툴 호출 사이 지연으로 실제로 시간이 흘러 자동
  종료되면서 드러남 - 실제 게임플레이에서는 정상적인 버튼 흐름만 타므로 잘 안 드러나는
  경로였음)
- 원인: `ShowAiCoreUpgradeStep()`만 다른 패널을 방어적으로 닫고, `ShowModdingStep()`/
  `ShowShop()`은 자기 패널만 열 뿐 형제 패널을 닫지 않았음
- 수정: `CloseAllIntermissionPanels()`를 신설해 `HandleWaveEnded()`와 세 `Show*()` 메서드
  전부의 시작에서 호출하도록 통일 — 정비 단계 화면은 항상 하나만 켜져 있음을 구조적으로 보장

**검증 (MCP 플레이모드)**
1. 컴파일 에러 없음(각 단계마다 확인)
2. 런 시작 시 6개 슬롯 전부 기본 파츠로 자동 초기화됨(무게 지탱 4+4=8, 인스펙터 시작
   무기 2개 무게 합 5로 여유 있게 수용 가능함을 확인)
3. **부품 상자 개봉**: 상자 3개를 실제 "상자 열기" 버튼 클릭으로 개봉 → 서사 무기소켓
   (에너지무기 제한), 전설 다리장갑(+15 방어력), 서사 팔장갑(+8 방어력) 획득, 즉시 장착
   확인. 상자 소진 후 버튼이 `interactable=false`로 바뀌고, 그 상태에서 다시 눌러도 안전하게
   무시됨(예외 없음)
4. **스탯 반영**: 위 파츠 장착 후 플레이어 실측 방어력 5 → 28(5+8+15) 정확히 일치
5. **무기 타입 제약(실제 상점 구매 경로)**: 무기소켓을 "에너지 방출 소켓"(에너지무기 전용)
   으로 바꾼 뒤, 비-에너지 무기(수류탄) 구매 시도는 `구매가능=False`와 이유
   "이 소켓은 에너지무기만 장착할 수 있습니다 (현재: 폭발화기)"로 정확히 차단됨. 같은
   상태에서 에너지 무기 구매는 성공(골드 정상 차감, 소켓 교체됨)
6. **무게 제약(실제 상점 구매 경로)**: 자기장 코어+다리를 표준(4+4=8)으로 고정한 뒤,
   소켓0에 무게5 무기(수류탄)를 직접 장착한 상태에서 무게4 무기(스나이퍼)를 소켓1에
   사려는 시도가 `구매가능=False`, 이유 "무기 무게 초과 (9 / 8)"로 정확히 차단됨
7. **디스크 슬롯 수 데이터 이전**: `ShopManager.DiscSlotCount`가 Phase 3의 고정값이 아니라
   `PartsCatalog.HeadModdingInfo`를 경유해 조회됨을 확인(현재 두 로봇 모두 6칸으로 설정,
   기존 동작과 동일하지만 이제 로봇별로 다르게 설정 가능)
8. **정비→상점 전환**: 로봇 정비 화면의 "상점으로" 버튼 클릭 시 정비 패널이 닫히고
   상점 패널이 정상적으로 열림(위 버그 수정 후 재검증, 동시 노출 없음)
9. **화면 캡처로 시각 확인** — 로봇 정비 화면의 한글 텍스트, 등급 색상, 장착 파츠 목록,
   무게 지탱 표시(7/8) 모두 정상 렌더링 확인
10. 콘솔 로그 확인 — 이번 세션에서 발생한 콘솔 에러는 검증 스크립트 자체의 실수
    (비활성 오브젝트에 `GameObject.Find` 호출 → NRE) 1건뿐이었고, 게임 코드에서 발생한
    에러는 없었음

**남은 임시값 / 후속 과제**
- `PartsCatalog`의 부품 상자 등급 가중치·드랍 확률(5%)·파츠 17종 수치는 **전부 밸런스
  미확정 임시값**
- 무기 소켓 "개수"는 여전히 `PlayerShootManager` 인스펙터 설정(2개)을 그대로 씀 —
  `PartsCatalog.HeadModdingInfo.weaponSocketCount`는 조회 가능하지만, 실제로 소켓 개수를
  늘리려면 새 소켓의 `muzzle_point`/`rig_point`/`hand_sprite_renderer` 등 씬의 Transform을
  새로 배치해야 해서 이번 Phase 범위에서는 연결하지 않음(로봇 파츠 데이터가 아니라
  씬 에셋/프리팹 작업이 필요한 부분)
- 무기 소켓 파츠가 "모든 소켓에 공통 적용"되는 단순화 상태 — 소켓별로 다른 무기 타입을
  받고 싶으면(예: 왼손은 에너지무기만, 오른손은 아무거나) 슬롯 키를
  `PartSlot.ArmWeaponSocket_0`, `_1`처럼 소켓별로 분리하는 리팩터링이 필요함
- 로봇 정비 화면(p.13 명세에는 없던 화면이라 레이아웃 기준이 상점만큼 상세하지 않음)의
  시각적 디테일(아이콘, 애니메이션 등)은 데모 기준으로 텍스트 위주로만 구현함
- 필살기 슬롯은 여전히 데모 범위 밖(표시만, 동작 없음)

**다음 단계**: Phase 5(보스 + 데모 폴리시 - 10웨이브 보스, 웨이브 HUD, 승리/패배, 밸런싱).

### Phase 5 구현: 보스 + 데모 폴리시 [완료] (2026-08-06)

10웨이브 보스(근접+광역 패턴), 승리 상태 관리자, 상시 전투 HUD(웨이브/골드/AI 코어 경험치
바), 승리 화면을 구현하고, MCP 플레이모드로 보스 처치→승리, 사망 시 정비 화면 미노출까지
전체 흐름을 검증했다. 검증 중 실제로 재현된 버그 2건도 함께 고쳤다.

**사용자 확정 사항 (진행 전 확인받음)**
1. **보스 비주얼**: 전용 아트가 없어 좀비 스프라이트를 확대(3배)+색조 변경(빨강)한 임시
   비주얼 사용. 나중에 실제 아트로 교체 가능하도록 `Assets/Prefebs/Boss.prefab`로 분리
2. **보스 패턴**: 근접(기존 EnemyUnit 그대로 상속) + 주기적 광역(장판) 공격. 예비 동작
   (텔레그래프)을 보여준 뒤 데미지를 줘서 플레이어가 범위 밖으로 피할 수 있게 함
3. **승리 조건**: 보스 처치와 웨이브 제한시간 종료 **둘 다** 필요. 시간이 먼저 끝나도
   보스가 살아있으면 처치할 때까지 계속 대기하고, 보스를 먼저 잡아도 남은 시간만큼 더 대기
4. **밸런싱 범위**: 명백한 임시값만 정리(상세 시뮬레이션 없이 상식적 수준으로)

**신규 파일**
- `Assets/Scripts/BossUnit.cs` — `EnemyUnit`을 상속. 근접 공격은 그대로 물려받고
  (`base.Awake()`가 `EnemyUnit.Alive`에도 등록하므로 `PlayerShootManager`의 자동 타겟팅이
  별도 처리 없이 보스도 그대로 조준한다), 주기적 광역 공격 패턴을 추가. 텔레그래프 원형
  스프라이트는 전용 이펙트 아트가 없어 런타임에 생성(`Texture2D`에 부드러운 원을 그려
  `Sprite.Create`)해서 재사용
- `Assets/Scripts/GameWinManager.cs` — 승리 상태 관리자. `GameOverManager`와 완전히
  대칭되는 구조(이름을 합치지 않은 이유: "게임오버"는 패배를 뜻하는 게 자연스러워서)
- `Assets/Prefebs/Boss.prefab` — 좀비 프리팹 복제, 3배 확대 + 빨간 색조
- `Assets/Prefebs/Charger.prefab` — 좀비 프리팹 복제, 1.4배 확대 + 주황 색조(밸런싱 항목,
  아래 참고)

**수정 파일**
- `Assets/Scripts/EnemyUnit.cs` — `Awake`/`Update`/`FixedUpdate`/`Die`를 `protected virtual`로
  변경(`BossUnit`이 override로 확장할 수 있도록). override 없이 `BossUnit`이 같은 이름의
  메서드를 새로 정의하면 Unity가 base 메서드를 호출하지 않아 조용히 깨지는 함정이 있어서,
  반드시 override + `base.XXX()` 호출 패턴을 지키도록 주석에 남김. 모든 `GameOverManager.IsGameOver`
  체크 지점에 `GameWinManager.IsGameWon`도 함께 확인하도록 추가(승리 후에도 전투 로직 정지)
- `Assets/Scripts/EnemySpawner.cs`, `PlayerShootManager.cs`, `WaveManager.cs` — 동일하게
  `GameWinManager.IsGameWon` 체크 추가(스폰/발사/웨이브 타이머가 승리 후 멈추도록)
- `Assets/Scripts/WaveManager.cs` — `finalWaveNumber`(기본 10) 도달 시 `SpawnBoss()`.
  보스 스탯은 `GameDataAsset`(시트 연동) 밖에 인스펙터 필드로 둠 — 시트를 재임포트할 때
  `asset.monsters.Clear()`로 통째로 비워지는 구조라, 시트에 없는 보스 행을 거기 넣으면
  재임포트 시 조용히 사라지기 때문(`ShopCatalog`/`PartsCatalog`가 시트 밖에 데이터를 두는
  것과 같은 이유). `WaveTimer` 코루틴이 마지막 웨이브에서는 시간 종료 후에도 보스 처치를
  기다렸다가 `WinGame()`을 호출하도록 분기 추가
- `Assets/Scripts/GameFlowManager.cs` — `HandleWaveEnded()`에 `GameOverManager.IsGameOver`
  가드 추가(플레이어가 웨이브 도중 사망했으면 정비/상점 화면으로 넘어가지 않음 - 아래
  발견한 문제 참고)
- `Assets/Scripts/AiCoreManager.cs` — `GetRequiredExpForNextLevel()`(HUD 경험치 바용 공개
  메서드), `MaxLevel` 프로퍼티 추가
- `Assets/Scripts/GameHUD.cs` — 상시 전투 HUD로 확장(웨이브/골드 텍스트, AI 코어 경험치
  바, 승리 오브젝트 처리 추가). 기존에는 필드만 있고 씬에 전혀 연결되어 있지 않았음
- `Assets/Scripts/GameOverManager.cs` — `Reset()`에서 `OnGameOver = null` 제거(아래 발견한
  버그 참고)
- `Assets/Scripts/PlayerRobotController.cs` — `GameWinManager.Reset()`, `EnemyUnit.ResetStaticCaches()`
  호출 추가
- Ground01 씬 — `Canvas` 아래 `TopHud`(웨이브/골드/경험치바) + `Victory` 오브젝트 신설,
  기존 `GameOver` 오브젝트를 복제해 재사용. `HP` 오브젝트에 `GameHUD` 컴포넌트를 부착하고
  전부 연결. `GameFlow`의 `WaveManager`에 `Boss.prefab` 연결. `EnemySpawner`에
  `Charger.prefab`(몬스터ID 200002) 매핑 추가

**밸런싱 1패스 (명백한 임시값 정리)**
- `EnemySpawner.spawnMonsterIds`가 좀비(200001) 하나뿐이었음 — 데이터에는 이미 '차저'
  (200002, HP 40)가 있었는데 프리팹 매핑이 없어서 완전히 죽어있던 데이터였다. `Charger.prefab`을
  만들어 매핑을 추가해 실제로 등장하도록 함
- **보스 체력을 1200 → 4500으로 상향**. 실측 기반 조정: 최초 구현치(1200)로 테스트했더니
  업그레이드 하나도 없는 갓 시작한 플레이어의 기본 무기(라이플+기관단총) 자동공격만으로
  보스가 **약 7초 만에 죽는** 것을 실제 플레이모드에서 확인함 - "보스"라고 부르기엔
  지나치게 쉬웠음. 4500으로 올린 뒤 재검증하니 같은 조건에서 약 22초가 걸려 훨씬 보스전다운
  체감이 됨(참고: 이 값도 여전히 밸런스 미확정 임시값이며, 실제 플레이테스트 후 재조정 권장)

**발견 및 수정한 버그 2건**

1. **`GameOverManager`/`GameWinManager`의 이벤트 구독이 초기화 순서에 따라 조용히
   사라지는 버그** — `RunState.OnChanged`에서 이미 한 번 겪었던 것과 정확히 같은 패턴
   (작업.md Phase 2 참고)이 여기서도 실제로 재현됐다. `GameWinManager.Reset()`이
   `OnGameWon = null`로 강제로 비웠는데, `PlayerRobotController.Awake()`가 이 `Reset()`을
   호출하는 시점과 `GameHUD.Awake()`가 `OnGameWon`을 구독하는 시점의 순서가 오브젝트마다
   달라서(Unity가 보장하지 않음), `GameHUD.Awake()`가 먼저 실행되면 그 구독이 곧바로
   지워졌다. 실측: 보스 처치+시간 종료로 `GameWinManager.TriggerWin()`이 정상 호출됐는데도
   `Victory` 패널이 뜨지 않음 → 구독자 수를 리플렉션으로 확인하니 0명. `GameOverManager`도
   똑같은 구조(`OnGameOver = null`)라 잠재적으로 같은 문제가 있었지만, `GameHUD`가 이번
   Phase 전까지 씬에 전혀 연결돼 있지 않아 드러나지 않았을 뿐이었다. 두 매니저의
   `Reset()`에서 이벤트를 비우는 줄을 제거해 수정(각 구독자가 자기 `OnDestroy`에서 스스로
   해제하므로 강제 초기화가 애초에 불필요했음)
2. **`UIManager` 오브젝트에 미연결 상태의 `GameHUD` 컴포넌트가 이미 붙어있었음** — 이번
   Phase에서 `GameHUD`를 `HP` 오브젝트에 새로 붙이면서 씬에 `GameHUD` 인스턴스가 2개가
   되어(모든 필드가 비어있는 죽은 인스턴스 + 새로 연결한 것) `OnGameOver`/`OnGameWon`
   구독이 항상 2건씩 잡히는 것을 발견, 죽은 쪽을 제거함

**검증 (MCP 플레이모드)**
1. 컴파일 에러 없음(각 단계마다 확인)
2. 웨이브 10 진입 시 보스 정상 스폰(위치: 플레이어로부터 16유닛), `EnemyUnit.Alive`에
   등록되어 플레이어 무기가 별도 처리 없이 자동으로 조준·사격함을 확인
3. **승리 조건 "시간 먼저 끝나고 보스 나중에 죽는" 케이스**: 웨이브 지속시간을 3초로
   줄이고 보스를 스폰 → 실제로 타이머가 3초에 끝난 뒤에도 보스가 살아있는 상태가 로그로
   확인됨 → 보스가 죽자(플레이어 자동공격에 의해) 그 즉시 승리 처리됨. 코드의
   대기 루프(`while (보스 생존 && !처치) yield return null`)가 의도대로 동작
4. **승리 조건 "보스 즉시 처치 후 대기" 케이스**: 스폰 대신 적을 끄고 `TakeDamage(99999)`로
   보스를 즉시 처치 → `GameWinManager.IsGameWon=true`, `Victory` 패널 활성화, 화면 캡처로
   "VICTORY" 텍스트·웨이브 10·골드·레벨 표시 전부 정상 렌더링 확인
5. **보스전 도중 사망 시 정비 화면 미노출**: 아무 업그레이드 없이 웨이브 10에 진입시켜
   좀비+보스에게 실제로 사망 → `GameOverManager.IsGameOver=true`, `Victory`/`ShopPanel`/
   `ModdingPanel`/`AiCoreUpgradePanel` 전부 비활성 상태 유지, `GameOver` 패널만 정상 표시
   (수정 전이었다면 이 케이스에서 상점이 잘못 열렸을 수 있음 - 위 발견한 문제 1번과 별개로
   `GameFlowManager.HandleWaveEnded`에도 가드를 추가해 이중으로 방어함)
6. **HUD 실시간 갱신**: `RunState.CoreExp`를 바꾸고 `NotifyChanged()` 호출 → 웨이브/골드/
   경험치 텍스트와 슬라이더가 다음 프레임에 정상 반영됨(단, 같은 프레임 안에서 동기적으로
   읽으면 아직 안 바뀐 상태로 보임 - `GameHUD.Update()`가 매 프레임 갱신하는 구조라 정상)
7. **광역 공격 패턴**: 코루틴을 직접 여러 차례 실행(자연 발생 포함 총 3회 이상) 시켰을 때
   예외 없이 완료됨(런타임 원형 스프라이트 생성/파괴 포함). 텔레그래프가 뜬 시점의
   스크린샷은 확보하지 못함(지속시간 1.2초가 MCP 도구 왕복 지연보다 짧아서) - 코드 검토와
   무결점 실행 이력으로 갈음
8. **상속 리팩터링 검증**: `BossUnit.Update()`가 `base.Update()`를 통해 근접 공격을 정상
   수행하는 것을 콘솔 스택트레이스로 직접 확인(`EnemyUnit:TryAttack ()`이
   `BossUnit:Update ()`에서 호출됨) - virtual/override 체인이 실제로 의도대로 동작함

**남은 임시값 / 후속 과제**
- 보스 HP(4500)·공격력(25)·광역 데미지(20)·쿨다운(5초) 전부 밸런스 미확정 임시값
- 부품 상자 드랍률(5%), 등급 가중치 등 Phase 4 임시값은 이번에 건드리지 않음(범위 밖)
- 보스가 근접+광역 패턴만 가짐 — 페이즈 전환(체력 구간별 다른 패턴)이나 소환 패턴은
  데모 범위 밖으로 남겨둠
- 승리 화면(Victory)에 "다시 시작"/"메인으로" 같은 후속 액션 버튼이 없음(GameOver 패널도
  마찬가지로 이번 세션 기준 이미 없었음 - 씬 재시작은 여전히 Editor에서 수동으로 해야 함)
- 좀비/차저 모두 같은 프리팹을 재활용한 확대·색조 변경 임시 비주얼 — 전용 아트 도입은
  범위 밖

**다음 단계**: 데모 목표(10웨이브+보스) 핵심 루프는 이번 Phase로 일단 완성됨. 이후는
전용 아트 도입, 밸런스 정밀 조정(실제 플레이테스트 기반), Phase 4에서 남겨둔 무기 소켓
개수 확장(씬 에셋 작업) 등 후속 폴리시 작업 중 우선순위를 사용자와 논의해 진행.

---

## 2026-08-06 (세션 2)

### MCP 도구 가용성 재확인 [완료]

세션 시작 시 연결 상태와, Phase 5에서 기록됐던 "`editor-application-*`/`screenshot-*`
도구가 No such tool available로 응답" 이슈가 이번 세션에도 재현되는지 확인함.

**연결 상태**
- `unity-mcp-cli status "Y:\Unity\ComstockPJ"` — Unity Editor 실행 중(PID 36592),
  Local MCP 서버(`localhost:26721`)는 connection refused지만 Config Server
  (`localhost:27242`) 경유로 정상 연결, 도구 호출 가능("ready for tool calls")

**도구 가용성 확인 방법과 결과**
- `mcp__ai-game-developer__unity-tool-list`(Unity 측 등록 도구 목록 조회)로는
  `editor-application-get-state`/`set-state`, `screenshot-camera`/`game-view`/
  `scene-view`, `gameobject-component-list-all`, `profiler-enable-module`이 전부
  정상 등록된 것으로 나옴
- 그러나 Claude Code의 `ToolSearch`(배포된 MCP 도구를 실제로 로드하는 창구)로
  같은 이름들을 검색하면 **"No matching deferred tools found"** — 즉 Unity 쪽엔
  등록돼 있어도 이번 세션에서 실제로 호출 가능한 도구 목록에는 올라오지 않음.
  `screenshot-isolated`(개별 GameObject 렌더링)만 예외적으로 정상 로드됨
- **결론: Phase 5에서 기록된 이슈가 이번 세션에도 동일하게 재현됨.** 플레이모드
  진입/종료(`editor-application-set-state`)와 Game View·Scene View 스크린샷은
  이번 세션에서도 사용 불가. 필요 시 Phase 3~5에서 썼던 우회법(`script-execute`로
  `EditorApplication.EnterPlaymode()` 직접 호출 + Canvas 렌더 모드를 임시로
  `ScreenSpaceCamera`로 바꿔 RenderTexture 캡처)을 다시 써야 함
- 원인 불명(MCP 플러그인 자체 또는 Claude Code 쪽 도구 배포 문제로 추정) —
  근본 원인 조사는 이번 세션 범위 밖으로 판단, 발생 사실만 기록

**다음 단계**: Phase 5 종료 시점에 남겨진 후속 폴리시 후보(전용 아트 도입 / 밸런스
정밀 조정 / 무기 소켓 개수 확장) 중 우선순위를 사용자와 논의 후 진행.

### 컨셉기획서 `20260806_컨셉기획서_Ver01_오승명.pdf` 분석 [완료]

19페이지 PDF(작성자 오승명, Ver01)를 전체 확인. `pdftotext`로는 한글이 추출되지 않아
(CID 폰트) PyMuPDF로 페이지를 이미지 렌더링해 판독함.

**기존 확정 사항과 달라진 부분 (이번 기획서가 최신 확정본)**
- **총 20웨이브 · 웨이브당 약 1분 · 20웨이브에 보스 등장** (기존: 10웨이브, 30초+3초/웨이브)
- **Space = 구르기** 조작 신규 추가 (현재 미구현)
- 20웨이브 종료 시에는 정비 시간 미부여 → 현재 `WaveManager`가 마지막 웨이브에서
  `EndWave()`를 타지 않고 `WinGame()`으로 가므로 **이미 일치**
- 보스와 잡몹이 동시에 등장 → 보스 웨이브에도 일반 스폰이 계속되므로 **이미 일치**

**현재 구현과 일치하는 부분 (변경 불필요)**
- 정비 시간 UI 순서: 레벨 업 → 로봇기체 정비 → 상점
- 등급 5단계(일반→레어→에픽→유니크→레전더리)를 무기/디스크/레벨업 증강/로봇 파츠에 부여
- 파츠 9부위(머리-머리/헬멧/디스크슬롯, 팔-무기소켓/팔장갑/자기장코어, 다리-다리/다리장갑/발)
- 자동 공격(컴스톡 기준 최근접 적 우선), WASD 이동, 승리=20웨이브 보스 처치 / 패배=체력 0

**데모 범위 밖으로 확인된 것**
- 아웃게임(캐릭터 선택 화면, 해금 정보 관리) — 인게임 흐름과 분리된 별도 화면
- 톤&무드(셀 셰이딩 / 인디 카툰 / SD), 전용 캐릭터·몬스터 아트

### 사용자 피드백 2건 원인 분석 [완료]

사용자가 지적한 두 문제의 원인을 코드/데이터에서 특정함.

**① "업그레이드/상점 진입은 전체 화면 UI로, 인게임은 초기화되고 조작·진행이 안 되어야 함"**
- 패널 레이아웃 자체는 문제가 아니었다 — `AiCoreUpgradePanel`/`ShopPanel`/`ModdingPanel`
  전부 이미 anchorMin(0,0)~anchorMax(1,1) 전체 화면 스트레치이고 배경 이미지도 있음
- **진짜 원인**: `GameFlowManager.HandleWaveEnded()`가 `CurrentState`만 바꿀 뿐
  `Time.timeScale`을 건드리지 않고, 플레이어 조작을 막는 코드도 어디에도 없다.
  정비 화면이 떠 있는 동안에도 플레이어는 계속 이동하고 필드의 보상 픽업을 주울 수 있다
- 부수 문제 2건: (a) `TopHud`가 Canvas 자식 순서상 맨 마지막이라 상점/정비 패널 **위에**
  겹쳐 그려짐 (b) `AiCoreUpgradePanel` 배경 알파가 0.7이라 뒤 게임 화면이 비침

**② "게임 템포가 느리고 화면 밖 좀비를 때려서 좀비가 존재하는지도 안 보임"**
- 카메라 실측: **원근(perspective) FOV 60, z=-15** → 가시 범위가 세로 반경 8.66 /
  가로 반경 15.4(16:9) 유닛
- 무기 사거리(`GameDataAsset`): 라이플·스나이퍼 **20**, 기관단총·수류탄 **15**, 샷건·에너지 10
  → **세로 가시 반경의 최대 2.3배**. 화면 밖 적을 조준·사격하고 있었음
- 스폰 반경: **18~26 유닛** → 화면 밖 한참 뒤에서 스폰되어, 화면에 들어오기 전에
  사거리 안에 걸려 죽는다. 그래서 "좀비가 존재하는지도 안 보이는" 상태가 됨
- 스폰 압력: 간격 3초 / 최대 동시 15마리 → 30초 웨이브당 약 10마리만 등장.
  기획서 레퍼런스 이미지(화면을 가득 채운 좀비 떼)와 정반대

### Phase 6 계획: 템포·가시성 개선 + 정비 화면 정지 + 구르기 [진행중]

**사용자 확정 사항 (진행 전 확인받음)**
1. **20웨이브 · 웨이브당 약 1분**으로 기획서에 맞춘다(기존 10웨이브·30초 폐기)
2. 정비 화면 진입 시 **완전 정지 + 필드 초기화** — `Time.timeScale=0`, 조작·자동공격 차단,
   남은 픽업·투사체 정리, 플레이어 시작 위치 복귀
3. **구르기(Space)를 이번 작업에 함께 구현**

**작업 항목**

1. **정비 화면 완전 정지 + 필드 초기화**
   - `GameFlowManager`: 정비 진입 시 `Time.timeScale=0`, 다음 웨이브 시작 시 `1`로 복구
   - 플레이어 이동/자동공격 입력 차단(정비 상태를 static으로 노출해 각 스크립트가 확인)
   - 필드 정리: 남은 `RewardPickup`은 **버리지 않고 자동 수령 처리한 뒤** 제거
     (기획서 p.12 "레벨업, 로봇 파츠 획득 일괄 처리"와 맞음 — 못 주운 보상이 사라지면 손해),
     `Projectile` 전부 제거, 플레이어를 시작 위치로 복귀
   - `TopHud`를 Canvas 자식 순서 맨 뒤에서 패널보다 앞으로 옮기거나 정비 중 숨김,
     `AiCoreUpgradePanel` 배경 알파 0.7 → 0.97

2. **무기 사거리를 화면 안으로 축소** (`GameDataAsset` 값 조정)
   - 세로 가시 반경 8.66 안에서 교전이 끝나도록 전 무기 사거리 축소.
     상대적 우열(스나이퍼>라이플>기관단총·수류탄>샷건·에너지)은 유지
   - 시트가 아니라 로컬 에셋 값이므로 임포터 재실행 시 덮어써질 수 있음 → 조정 후
     시트에도 반영이 필요하다는 점을 사용자에게 알릴 것

3. **스폰을 "화면 바깥 테두리" 기준으로 변경**
   - 현재는 원형(18~26 유닛) 스폰이라 가로/세로 화면비를 무시한다. 카메라 뷰포트
     사각형 바깥 여백에서 스폰하도록 `EnemySpawner.GetRandomSpawnPosition()` 변경 →
     어느 방향에서 오든 "화면 밖에서 걸어 들어오는" 일관된 체감

4. **스폰 압력 대폭 상향 + 기동력 상향 (템포)** — *추가 지시(2026-08-06): "웨이브에서
   나오는 좀비 숫자도 너무 적으니 늘리고 로봇과 좀비의 기본적인 기동력을 늘려줘"*
   - 스폰 간격 3초 → 0.8초대, 최대 동시 생존 15 → 25로 시작하고 20웨이브 기준으로
     난이도 곡선(`spawnIntervalDecayPerWave`, `maxAliveIncreasePerWave`) 재조정
   - **로봇 이동속도 상향**(`robot_speed` 3 → 상향)과 **좀비/차저 이동속도 상향**
     (`monster_speed` 좀비 2 / 차저 1 → 상향). 적이 화면 안으로 들어오는 시간이 짧아져
     체감 템포가 직접 개선되고, 플레이어도 그만큼 빠르게 움직여 밀집한 적을 피할 수 있게 함
   - 주의: 플레이어보다 적이 빠르면 도망이 불가능해 뱀서라이크 문법이 깨지므로,
     **로봇 속도 > 좀비 속도** 관계는 유지한다

5. **20웨이브 · 웨이브 1분 적용**
   - `WaveManager`: `finalWaveNumber` 10 → 20, `firstWaveDuration` 30 → 60,
     `waveDurationIncreasePerWave` 3 → 0(기획서 "약 1분간 진행")
   - 보스 체력 등 보스 스탯은 20웨이브 기준으로 재검토

6. **구르기(Space) 신규 구현**
   - `PlayerRobotController`에 대시 추가: 이동 방향(입력 없으면 바라보는 방향)으로 짧게
     고속 이동, 대시 중 무적, 쿨다운. 수치는 전부 밸런스 미확정 임시값
   - 정비 중에는 다른 조작과 함께 차단

**검증 방식**: 각 항목 적용 후 `script-execute`로 플레이모드에 진입해 실제 수치
(화면 안 적 수, 교전 거리, 정비 중 플레이어 이동 여부, timeScale)를 측정하고 화면 캡처로
확인한다. 이번 세션에도 `editor-application-*`/`screenshot-*` MCP 도구는 사용 불가이므로
Phase 3~5에서 쓰던 우회법을 그대로 사용한다.

### Phase 6 구현 [완료] (2026-08-06)

위 계획 6개 항목을 모두 구현하고 MCP 플레이모드로 검증 완료.

**신규/수정 파일**
- `Assets/Scripts/PlayerRobotController.cs` — **구르기(Space) 신규 구현**: 이동 방향(제자리면
  마지막으로 향하던 방향)으로 정해진 거리를 고정 속도로 이동하며 그동안 무적(`IsDashing`이면
  `TakeDamage`가 통째로 무시됨). 거리 3.5 / 지속 0.18초 / 쿨다운 1.2초는 전부 임시값.
  또한 정비 중에는 `Update()`가 조기 반환해 입력·애니메이션까지 정지하고,
  `ReturnToStartPosition()`으로 필드 초기화 시 시작 위치로 복귀한다(시작 위치는 `Awake`에서 기억)
- `Assets/Scripts/GameFlowManager.cs` — **정비 = 완전 정지**. `IsIntermission`(static) 신설,
  정비 진입 시 `Time.timeScale=0` / 다음 웨이브 시작 시 `1`로 복구. `ResetFieldForIntermission()`이
  남은 보상 픽업·아이템 픽업을 **자동 수령 처리한 뒤** 제거하고(그냥 지우면 못 주운 보상이
  증발한다), 투사체를 정리하고, 플레이어를 시작 위치로 되돌린다. 전투 HUD 숨김 처리도
  `combatHudObjects`로 여기 일원화(아래 참고)
- `Assets/Scripts/RewardPickup.cs`, `ItemPickup.cs` — 트리거 습득 로직을 `CollectImmediately()`로
  분리해, 플레이어가 닿지 않아도 정비 진입 시 일괄 수령할 수 있게 함
- `Assets/Scripts/PlayerShootManager.cs` — 정비 중 조준·발사 정지(`GameFlowManager.IsIntermission` 확인)
- `Assets/Scripts/EnemySpawner.cs` — **스폰 위치를 원형 → 카메라 화면 바깥 테두리 기준으로 변경**
  (`TryGetCameraHalfExtents()`가 원근/직교 카메라 모두에서 z=0 평면의 가시 범위를 계산).
  변의 길이에 비례해 네 변을 고르므로 테두리 전체에 고르게 퍼진다(그냥 4등분하면 짧은
  좌우 변에 몰린다). 한 번에 여러 마리를 스폰하는 `spawnBatchSize` 추가
- `Assets/Scripts/WaveManager.cs` — 20웨이브·웨이브당 60초로 전환, 난이도 곡선 재조정
- `Assets/Data/GameDataAsset.asset` — 무기 사거리·몬스터/로봇 이동속도 조정(아래 표)
- `Ground01.unity` — 스포너/웨이브 인스펙터 값 갱신, Canvas 자식 순서 정리(정비 패널이 HUD보다
  위에 오도록), `AiCoreUpgradePanel` 배경 알파 0.7 → 0.97, AI 코어 카드 3장에 배경(`Option{N}Card`)
  신설 + 텍스트 빨강 → 흰색, `GameFlowManager.combatHudObjects`에 HP/Hp_Slider/TopHud 연결

**조정한 수치 (전부 밸런스 미확정 임시값)**

| 항목 | 이전 | 이후 | 근거 |
|---|---|---|---|
| 무기 사거리 | 기관단총 15 / 라이플 20 / 샷건 10 / 스나이퍼 20 / 수류탄 15 / 에너지 10 | 6 / 7 / 4 / 8 / 6 / 5 | 카메라 세로 가시 반경이 8.66이라 그 안에서 교전이 끝나야 함 |
| 스폰 위치 | 원형 반경 18~26 | 화면 테두리 바깥 1.5~4.0 | 화면 밖에서 걸어 들어오는 체감 통일 |
| 스폰 간격 / 묶음 | 3초 / 1마리 | 0.8초 / 3마리 | 웨이브당 등장 수 대폭 증가 |
| 최대 동시 생존 | 15 (+2/웨이브) | 30 (+4/웨이브) | 20웨이브 기준 최대 106마리 |
| 좀비 / 차저 이동속도 | 2 / 1 | 3.2 / 2.4 | 기동력 상향(사용자 지시). 로봇보다는 느리게 유지 |
| 로봇 이동속도 | 3 | 5 | 위와 같음. 반드시 적보다 빨라야 회피가 성립 |
| 웨이브 | 10웨이브 / 30초+3초 | 20웨이브 / 60초 고정 | 기획서 확정 |
| 보스 체력 | 4500 | 12000 | 보스가 20웨이브로 밀려 플레이어가 훨씬 강해진 상태로 만남 — **실측 아님, 재조정 필요** |

**설계 판단 (묻지 않고 정한 부분)**
- 무기 사거리를 줄이는 대신 카메라를 줌아웃하는 선택지도 있었지만, 줌아웃하면 SD 캐릭터가
  작아져 기획서의 톤&무드(단순화·미니멀·SD)와 멀어진다고 판단해 사거리 쪽을 조정함
- 전투 HUD 숨김이 `ShopPanelUI`/`ModdingPanelUI`에 각각 `hideWhileOpen` 배열로 흩어져 있었는데
  (그나마 씬에서는 둘 다 비어 있어 실제로 동작하지 않았고, AI 코어 화면에는 그 처리 자체가
  없었다), 정비 단계 전체를 아는 `GameFlowManager`로 일원화함. 패널의 `Close()`가 HUD를
  되살릴 수 있어 `SetCombatHudVisible(false)`는 항상 `CloseAllIntermissionPanels()` **뒤에** 호출한다
- 한 GameObject에는 Graphic(Image/TMP)이 하나만 올 수 있어, AI 코어 카드 배경은 텍스트에
  컴포넌트를 추가하는 대신 **텍스트 바로 앞 순서의 형제 오브젝트**로 만들었다
  (`raycastTarget=false`로 두어 클릭은 기존 Button이 받는다)

**검증 (MCP 플레이모드)**
1. 컴파일 에러 없음 (게임 코드 기준. 콘솔 에러 4건은 전부 검증용 임시 스크립트 자체의 실수)
2. **가시성**: 웨이브 1 진행 중 실측 — 적 17마리 중 **화면 안 10마리**, 플레이어와의 최단
   거리 4.5. 화면 캡처로 좀비가 화면을 둘러싸고 있는 것을 눈으로 확인(수정 전에는 사거리
   15~20으로 화면 밖에서 처치되어 좀비가 거의 보이지 않았음)
3. **구르기**: `IsDashing=True` → 실제 이동 거리 **3.50**(설정 3.5와 정확히 일치) → 종료 후
   `IsDashing=False`. 대시 중 `TakeDamage(9999)`에도 HP 100 유지(무적 확인). 쿨다운 중
   재시도는 무시되고, 1.5초 뒤 재시도는 성공
4. **정비 완전 정지**: 웨이브 종료 시 `timeScale 1 → 0`, `IsIntermission=True`,
   플레이어를 (7,-3,0)에 강제로 두고 종료시켰더니 **(0,0,0) 시작 위치로 복귀**,
   골드 21 → 187(픽업 자동 수령분 반영). 다음 프레임 확인 결과 픽업·아이템·투사체·적이
   **전부 0개**로 정리됨
5. **전체 화면 UI**: HP/Hp_Slider/TopHud 3개가 모두 비활성으로 숨겨지고 `AiCoreUpgradePanel`만
   활성. UI 레이어만 렌더링한 캡처로 전체 화면 불투명 배경 + 카드 3장 + 흰색 텍스트를 확인
6. **흐름 재개**: AI 코어 선택 8회 소진 → 로봇 정비 화면 → "상점으로" → 상점 →
   "다음 웨이브 시작" 클릭 시 `timeScale 0 → 1`, `IsIntermission=False`, 상태 `Combat`,
   웨이브 2, HUD 3개 전부 복구됨. 정비 단계 화면은 항상 하나만 켜져 있었음

**검증 중 배운 점 (다음 세션 참고)**
- 화면 캡처 시 **RenderTexture 크기를 Canvas rect(=Game View 해상도)와 다르게 잡으면 UI가
  확대되어 잘린다.** 1280x720으로 캡처했더니 1920x1080 기준으로 배치된 UI가 1.5배로 확대돼
  화면 밖으로 넘쳤고, 하마터면 멀쩡한 레이아웃을 "깨졌다"고 오판할 뻔했다. 반드시
  `canvas.GetComponent<RectTransform>().rect` 크기로 캡처할 것
- 월드 스프라이트가 UI 위에 겹쳐 보이는 것은 캡처용으로 `ScreenSpaceCamera`로 바꾼 탓이다
  (Phase 3에도 같은 메모가 있음). UI만 정확히 보려면 **카메라 cullingMask를 UI 레이어로만
  제한**해서 렌더링하면 된다
- **플레이모드 중에는 스크립트가 재컴파일되지 않는다.** 스크립트에 새 `[SerializeField]`를
  추가한 뒤 플레이모드 안에서 그 필드를 건드리려 하면 리플렉션이 null을 반환한다.
  또한 플레이모드 중의 씬 편집은 종료 시 전부 사라지므로, 씬 수정은 반드시 에디트모드에서 할 것
- `Time.timeScale=0`에서는 `WaitForSeconds`가 영원히 끝나지 않는다. 정비 상태를 검증하는
  코루틴은 `yield return null`(프레임 단위)로 기다려야 한다

**남은 임시값 / 후속 과제**
- **보스 체력 12000은 실측이 아니라 추정값**이다. 20웨이브까지 실제로 플레이해 보스전 시간을
  재고 조정해야 한다(Phase 5의 4500은 10웨이브 기준 실측이었다)
- 무기 사거리·몬스터/로봇 이동속도를 `GameDataAsset.asset`에서 직접 고쳤다. 이 값들은 원래
  구글 시트에서 오는 컬럼이라 **기획자가 `Comstock/게임 데이터 가져오기`로 재임포트하면
  전부 시트 값으로 되돌아간다.** 시트에도 같은 값을 반영해 두어야 한다
- 스폰 압력(0.8초/3마리, 최대 30 +4/웨이브)과 난이도 곡선은 웨이브 1만 실측했다.
  후반 웨이브에서 최대 100마리 넘게 몰릴 때의 성능·체감은 미검증
- 구르기 수치(거리 3.5 / 지속 0.18초 / 쿨다운 1.2초)와 무적 지속 시간은 임시값이며,
  구르기 애니메이션·이펙트는 없다(위치만 이동)
- 아이템 픽업(`ItemPickup`)이 매우 흔하게 떨어진다 — 60초 웨이브 1회에 194개가 쌓였다.
  드랍 테이블 조정이 필요해 보이나 이번 범위 밖으로 남겨둠

### Phase 7 구현: 사용자 피드백 8건 반영 [완료] (2026-08-06)

Phase 6 이후 사용자가 지적한 7건 + 추가 지시 1건(구글 시트 연동 제거)을 처리했다.

**1. 웨이브 종료 규칙 변경 — "1분 종료 후 남은 좀비가 없으면 2초 뒤 끝난다"**
- 이전에는 제한시간이 끝나는 즉시 남은 적을 전부 despawn하고 정비로 넘어갔다.
- 이제 `WaveManager.WaveTimer()`가 제한시간 종료 시 **스폰만 멈추고**, 필드에 남은 적을
  플레이어가 전부 처치할 때까지 기다린 뒤 `clearedGraceSeconds`(기본 2초)를 더 두고 종료한다.
- **안전장치**: 적이 어딘가에 끼어 영영 죽지 않으면 게임이 멈춰버리므로,
  `maxClearWaitSeconds`(기본 45초)를 넘기면 경고 로그를 남기고 강제로 진행한다
  (사용자가 요청하지 않았지만 무한 대기를 막기 위해 넣음)

**2. "플레이어가 움직일 때 아이템 위치가 미묘하게 밀리는 느낌" — 원인은 원근 카메라 시차**
- 원인: 카메라가 **원근(perspective, FOV 60, z=-15)** 인데 배경 `map` 스프라이트는 z=-1,
  픽업·적·플레이어는 z=0에 있었다. 카메라로부터의 거리가 14 vs 15로 달라, 카메라가 따라
  움직일 때 배경과 오브젝트가 **약 7% 다른 속도로 밀린다**(시차). 이것이 "미묘하게 밀리는 느낌"의 정체.
- 참고로 `Rigidbody.interpolation`은 이미 `Interpolate`였으므로 물리 보간 문제는 아니었다.
- 조치: 카메라를 **직교(orthographic)** 로 전환. 직교 투영은 z를 무시하므로 시차가 원천적으로
  사라진다. `orthographicSize = 8.660`으로 잡아 **Phase 6에서 맞춰둔 세로 가시 범위(8.66)를
  그대로 유지** → 무기 사거리·스폰 여백 튜닝이 그대로 유효하다

**3. 부품 상자 드랍 확률 하향** — `PartsCatalog.partBoxDropChance` 5% → **1.5%**

**4. "둥둥 떠다니는 빨간 게이지" 정체 확인** — **플레이어 체력 바(`Hp_Slider`)가 맞았다.**
   `GameHUD.hpSlider`가 실제로 참조하는 살아있는 UI이므로 지우지 않았고, 대신 잘못된 위치를
   고쳤다: 앵커가 화면 중앙(0.5, 0.5) + 오프셋 (-501.94, 260.08)이라 HP 텍스트와 동떨어진
   화면 중앙 왼쪽에 떠 있었다 → HP 텍스트와 같은 앵커(0.08, 0.92)에 붙이고 텍스트 바로 아래로 배치

**5. 웨이브 남은 초 표시** — `WaveManager.RemainingSeconds` / `IsClearingRemainingEnemies`를
   공개하고 `GameHUD`가 `TopHud/WaveTimeText`(WaveText를 복제해 스타일 재사용)에 `M:SS`로 표시.
   제한시간이 끝난 뒤에는 0에서 멈춰 있으면 멈춘 것처럼 보이므로 **"잔적 처치"** 로 바꿔 보여준다

**6. 보상 드랍 확률 하향** — 이전에는 처치할 때마다 경험치·골드가 **항상** 나와 60초 웨이브
   한 번에 픽업이 수백 개씩 쌓였다. `EnemyUnit.GrantKillRewards()`를 확률 드랍으로 바꿈:
   경험치 **1/2**, 골드 **1/3**(`ExpDropChance`/`GoldDropChance` 상수)

**7. 레벨업 필요 경험치 공식** — `expPerLevel * 레벨`(10*N) → **`50 + 20 * 레벨`**
   (`expBase`/`expPerLevel` 필드)

**8. 구글 시트 연동 완전 제거 (추가 지시)**
- 삭제: `Assets/Scripts/Editor/GameDataCsvImporter.cs`(시트 CSV 5개 URL + 임포터 창),
  `Assets/Scripts/CsvParser.cs`(임포터 전용 파서, 다른 사용처 없음 확인)
- 두 파일 모두 커밋 `a06e519`에 포함돼 있어 **git으로 복구 가능**함을 확인한 뒤 삭제했다
- `GameDataAsset`/`GameDataManager`/`PartsCatalog`/`ShopCatalog`의 주석에서 시트·임포터 언급을
  제거하고, **`GameDataAsset.asset`이 게임 데이터의 유일한 출처**임을 명시.
  값 수정은 Unity 인스펙터에서 직접 한다
- 이로써 Phase 6에서 "재임포트하면 되돌아간다"고 경고했던 문제(무기 사거리·이동속도)가
  **구조적으로 사라졌다**

**검증 (MCP 플레이모드)**
1. 컴파일 에러 없음, 플레이 중 콘솔 에러 **0건**
2. **웨이브 종료 규칙**: 제한시간을 3초로 바꿔 실행 → 3초 시점에 `IsSpawningEnabled=false`,
   `IsClearingRemainingEnemies=true`, 남은 적 17마리, 상태는 아직 `Combat` 유지 →
   **잔적 0마리 도달(t+7.82초) → 웨이브 종료(t+9.81초)로 정확히 2.00초 뒤 종료** 확인
3. **카메라**: `orthographic=True, size=8.660` → 세로 반경 8.66 / 가로 반경 15.40으로
   Phase 6과 동일한 가시 범위 유지 확인(배경 map은 여전히 z=-1이지만 직교라 시차 없음)
4. **경험치 공식**: 레벨 1 기준 필요 경험치 **70**(=50+20*1) 확인. HUD에도 "레벨 2 46/90" 표시
5. **보상 확률**: 실측 픽업 골드 12개 : 경험치 18개 = **1 : 1.5** — 기대 비율(1/3 : 1/2)과 일치
6. **HUD 화면 확인**: "웨이브 2" 아래 "0:55" 남은 시간, "골드 29", "레벨 2 46/90" 경험치 바,
   HP 텍스트 바로 아래에 붙은 체력 바를 캡처로 확인. 좀비도 화면에 가득 보임

**검증 중 다시 겪은 함정 (Phase 6과 같은 종류)**
- 스크립트의 `[SerializeField]` 기본값을 바꿔도 **씬에 이미 직렬화된 값이 그대로 남는다.**
  `expPerLevel`을 코드에서 20으로 바꿨는데 씬에 10이 저장돼 있어 레벨업 필요 경험치가
  70이 아니라 60으로 나왔다. 실측으로 잡아내 씬 값을 함께 고쳤다.
  **코드에서 기본값을 바꿨으면 씬의 컴포넌트 값도 반드시 같이 확인할 것**

**남은 임시값 / 후속 과제**
- 경험치/골드 드랍 확률(1/2, 1/3)과 부품 상자 확률(1.5%)은 전부 임시값이며 실제 플레이테스트
  기반 조정이 필요하다. 경험치/골드 확률은 데이터 에셋에 자리가 없어 `EnemyUnit`의 상수로 뒀다
- `maxClearWaitSeconds`(45초)는 안전장치 값일 뿐 실제로 걸리는 상황은 검증하지 못했다
- 아이템 픽업(`ItemPickup`) 드랍 빈도는 이번에도 손대지 않았다(여전히 매우 흔함)
- 보스 체력 12000은 여전히 추정값이다(Phase 6 참고)

### Phase 8: 무기 회전 연출 + 배경 교체 [완료] (2026-08-06)

**1. 무기 방향 전환에 회전 속도 적용**
- 이전에는 `pivot.rotation = Quaternion.Euler(...)`로 타겟 방향에 **즉시 스냅**했다.
- `PlayerShootManager.RotatePivotTowards()` 신설 — `Mathf.MoveTowardsAngle()`로
  `rotation_speed_degrees_per_second`(권장 540) 속도만큼만 돌린다. 조준 중일 때와
  대기 자세로 돌아갈 때 모두 같은 경로를 쓴다. 0 이하로 두면 예전처럼 즉시 회전(하위 호환).
- **발사도 함께 지연시켰다**: 조준각이 타겟 방향과 `fire_angle_tolerance_degrees`(권장 25도)
  안으로 들어와야 발사한다. 이게 없으면 무기가 돌아가는 동안에도 총알은 타겟으로 정확히
  날아가서(발사 방향은 총구→타겟으로 따로 계산됨) 회전 연출이 눈속임에 그친다.
  **DPS에 영향이 있는 변경이므로 허용각을 넉넉히(25도) 잡았다** — 540도/초면 180도 돌아도
  0.33초라 체감 손실은 작다.
- **주의**: `WeaponSlot`은 `struct`라 C# 9에서 필드 초기화식(`= 540f`)을 쓸 수 없어
  컴파일 에러가 났다(CS8773/CS8983). 코드에서는 기본값 없이 선언하고 **씬의 인스펙터 값으로
  주입**했다(소켓 2개에 540 / 25 설정).

**2. 배경을 사용자 제공 이미지로 교체**
- 신규: `Assets/Resources/ground_ruined_city.png` (3840x2160, 폐허 도시 평지 - 균열·맨홀·잡초)
  — 사용자가 첨부한 이미지를 `Downloads`에서 프로젝트로 복사
- 임포트 설정: Sprite / PPU 100 / maxSize 4096 / mipmap on / FullRect / Repeat
- 씬 `map` 오브젝트의 스프라이트를 교체하고 **Tiled 드로우 모드**로 전환.
  scale (2,2,1), size (120,70) → 실제 240x140 유닛을 덮는다(타일 1장 = 76.8x43.2 유닛).
  위치도 (-3,-1) → 원점으로 옮겼다(플레이어가 웨이브마다 원점으로 복귀하므로)
- 기존 `Assets/Resources/map.png`는 지우지 않고 남겨뒀다

**구현 중 발견해 고친 문제: 비균등 스케일로 배경이 가로로 늘어남**
- `drawMode`를 Simple → Tiled로 바꾸면 Unity가 **기존 transform 스케일을 `size`로 옮겨 담으면서
  스케일에 잔여값을 남긴다.** 그 결과 스케일이 `(2.50, 2.11, 1.00)`이 되어 배경 이미지가
  가로로 약 18% 늘어난 상태였다(로그로 발견).
- 수정: `size`를 먼저 정하고 **마지막에 스케일을 균등값으로 덮어쓰는** 순서로 변경.
  타일 가로세로 비율이 원본과 정확히 일치(1.778)함을 로그로 확인

**검증 (MCP 플레이모드)**
1. 컴파일 에러 없음(위 struct 에러는 수정 후 재확인), 플레이 중 에러 0건
2. **회전 속도 실측**: 무기 피벗(`RigingPoint`)의 각도를 240프레임 샘플링 →
   프레임당 최대 2.18도, **초당 환산 최대 540도/초로 설정값에 정확히 걸림**(즉시 스냅이면
   훨씬 큰 값이 찍힌다). 240프레임 중 94프레임에서 회전이 일어나 서서히 도는 것도 확인
3. **배경**: `drawMode=Tiled`, 스케일 (2,2,1) 균등, 덮는 범위 240x140 유닛.
   텍셀 밀도 50px/유닛 vs 화면 62px/유닛 → 1.25배 확대(과하지 않음)
4. **타일 이음매**: 플레이어를 타일 경계 부근(19,10)으로 옮겨 촬영 — 눈에 띄는 이음매 없음
5. 화면 캡처로 최종 확인 — 폐허 지면 위에 좀비/픽업/HUD가 정상 표시

**남은 임시값 / 후속 과제**
- 회전 속도 540도/초, 발사 허용각 25도는 임시값이다. 무기 종류별로 다르게 주고 싶으면
  (예: 스나이퍼는 느리게) 소켓이 아니라 무기 데이터로 옮겨야 한다
- 배경 타일 크기(76.8x43.2 유닛)는 시각적 판단으로 정한 값이다. 지면 디테일을 더 작게
  보이게 하려면 `map`의 스케일을 낮추면 된다(균등하게 유지할 것)

### 프로젝트 안내.md "미커밋 변경사항 있음" 문구 정정 [완료] (2026-08-06)

사용자가 전부 커밋했다고 알려와 `git status`로 확인한 결과 working tree clean(커밋 안
된 변경사항 없음). `프로젝트 안내.md`의 "현재 진행 상황 요약"에 남아있던 "미커밋
변경사항 있음"(Unity-MCP 설치 파일·문서 4종·한글 폰트·Phase 3~6 작업분) 문구는 실제로는
그 이후 커밋되었는데도 갱신되지 않고 남아있던 오래된 내용이었음을 확인하고 최신 상태로
수정함. `git log` 확인 결과 최근 커밋은 `5794ade [ADD] 인게임 플로우` 등 총 8개이며 모두
반영되어 있음.

---

## 2026-08-07

### MCP 연결 확인 [완료]

- `unity-mcp-cli status "C:\Project\ComstockPJ"` — Unity Editor 실행 중(PID 10148),
  Local MCP 서버(`localhost:22389`)는 connection refused지만 Config Server
  (`localhost:21195`) 경유로 정상 연결("ready for tool calls")
- `mcp__ai-game-developer__unity-tool-list` 실제 호출 성공 — 도구 호출 경로 정상
- **기존 알려진 이슈가 이번 세션에도 재현됨**: `editor-application-*` / `screenshot-*`
  계열은 Unity 쪽에는 등록돼 있으나 Claude Code의 `ToolSearch`에서는
  "No matching deferred tools found"로 로드 불가. 플레이모드 진입·화면 캡처는
  기존 우회법(`script-execute`)을 써야 함
- `.mcp.json`이 미커밋 상태로 변경돼 있음(포트 `27242` → `21195`). Unity-MCP가 세션마다
  포트를 재발급하는 정상 동작으로 보이며, 커밋 여부는 사용자 판단 대기

### Phase 9 계획: 로봇 모딩 화면 개편 [진행중]

사용자가 제시한 정비 화면 시안 이미지와 요구사항 5건에 따라 로봇 모딩(정비) 화면을
전면 개편한다.

**사용자 요구사항 (원문)**
1. 상자는 플레이어가 일일이 여는 게 아닌 자동으로 내용물이 인벤토리에 적재됨
2. 이 인벤토리는 임시 인벤토리로, 정비 화면이 끝나면 획득하여 적재된 내용물은 전부 사라짐
3. 교체는 인벤토리 내용물을 누르면 장착할 수 있는 파츠 슬롯이 노란색으로 활성화되고,
   그걸 클릭하면 서로 교체됨
4. 모딩으로 인한 스탯 변화는 바로 표시되어 확인 가능함
5. 이에 따라 UI도 전체적으로 변경

**착수 전 확인받은 사항 4건**
1. **파츠 슬롯 9칸** — 시안 이미지의 8칸(머리/헬멧/무기소켓/디스크슬롯/팔장갑/자기장코어/
   다리/다리장갑)에 기존 '발'을 유지해 9칸으로 간다(컨셉기획서의 9부위와 일치)
2. **머리 슬롯은 조회 전용** — 기존 확정 사항("로봇 = 머리 파츠 종류, 런 중 변경 불가")
   유지. UI에 표시만 하고 인벤토리에서 장착할 수 없다
3. **인벤토리는 스크롤로 전부 표시하되 크기는 머리(로봇)의 "적재량" 능력치가 결정** —
   적재량이 20이면 애초에 부품 상자를 20개 넘게 획득할 수 없다. 이에 따라 **인게임
   HUD에도 `5/20` 형태로 상자 보유량/상한을 표시**한다
4. **정비 화면 노출 조건은 현행 유지** — 부품 상자를 하나라도 획득했을 때만 열린다

**구현 계획 (A~E)**

- **A. 데이터 레이어**
  - `PartSlot` enum에 `Helmet`, `DiscSlot`을 **맨 끝에 추가**(중간 삽입하면 이미
    직렬화된 `PartsCatalog.asset`의 slot 값이 통째로 어긋난다). 머리는 enum에 넣지
    않는다 — 로봇 자체이고 조회 전용이라 `TryRollLootPart`가 뽑아버리면 안 되기 때문
  - `PartData`에 `discSlotCount`(디스크 슬롯 파츠 전용) 추가
  - `PartsCatalog.HeadModdingInfo`에 `partBoxCapacity`(적재량) 추가
  - `PartsCatalog.asset`에 헬멧·디스크슬롯 파츠(기본 + 루트) 추가, 로봇별 적재량 설정
- **B. 상자 적재량 상한 + 인게임 표시**
  - `ModdingManager.PartBoxCapacity` 제공, `EnemyUnit.TryDropPartBox()`가 상한 도달 시
    드랍 중단, `RewardPickup` 습득 시에도 clamp(이미 필드에 떨어진 상자 대비)
  - `GameHUD`에 "부품 상자 5/20" 표시 추가 + 씬에 텍스트 오브젝트 신설
- **C. 임시 인벤토리 + 교체 로직**
  - `RunState.ModdingInventory`(파츠 ID 목록) 신설, `Reset()`에 포함
  - `ModdingManager`: `OpenAllBoxesIntoInventory()`(정비 진입 시 일괄 자동 개봉),
    `SwapInventoryWithSlot()`(서로 교체 - 빠진 파츠는 인벤토리 그 칸으로 들어간다),
    `ClearInventory()`(정비 완료 시 전부 폐기)
  - `ShopManager.GetDiscSlotCount()`가 DiscSlot 파츠 값을 우선 반영
- **D. UI 전면 재구축** — `ModdingPanelUI` 전면 재작성 + 씬 레이아웃 신규 구축
  (상단바 / 인벤토리 그리드(스크롤) / 파츠 슬롯 9칸 / 스탯 9종 / 로봇 프리뷰 / 정비 완료)
- **E. MCP 플레이모드 검증** 후 문서 갱신

### 배경 이미지 교체 [완료] (2026-08-07)

사용자가 제공한 새 지면 이미지로 배경을 교체했다(작업 도중 요청받아 먼저 처리).

- 신규: `Assets/Resources/ground_ruined_city_v2.png` (3840x2160) — 사용자 제공 파일
  `Desktop/팀프로젝트/컴스톡/Assets/Backgrounds/ruined_city_flat_ground_character_match_4k.png`를
  복사. **기존 `ground_ruined_city.png`는 지우지 않고 남겨뒀다**(되돌릴 수 있도록)
- 임포트 설정은 기존 배경에서 그대로 읽어와 적용(Sprite / PPU 100 / maxSize 4096 / mipmap on /
  FullRect / Repeat) — 설정이 어긋나면 타일 크기나 이음매가 달라지기 때문
- 씬 `map` 오브젝트: 스프라이트 교체, `drawMode=Tiled` 유지, `size=(120,70)`,
  스케일 `(2,2,1)` 균등, 위치 원점
- **검증**: 타일 1장 = 76.8 x 43.2 유닛, 비율 1.778로 **원본 비율과 정확히 일치**(늘어남 없음),
  덮는 범위 240x140 유닛. Phase 8에서 겪었던 "drawMode 전환 시 스케일 잔여값으로 가로가
  늘어나는" 문제를 피하려고 이번에도 size를 먼저 정하고 스케일을 마지막에 균등값으로 덮어썼다

### Phase 9 구현: 로봇 모딩 화면 개편 [완료] (2026-08-07)

위 계획 A~E를 모두 구현하고 MCP 플레이모드로 전체 흐름을 검증했다.

**신규 개념**
- **임시 인벤토리**(`RunState.ModdingInventory`) — 정비 화면에서만 존재하며 화면을 닫으면 비워진다
- **적재량**(`PartsCatalog.HeadModdingInfo.partBoxCapacity`) — 머리(로봇) 능력치.
  보유 가능한 최대 부품 상자 수이자 인벤토리 크기이며, 상한에 도달하면 몬스터가 상자를 드랍하지 않는다

**수정 파일**
- `Assets/Scripts/PartSlot.cs` — `Helmet`, `DiscSlot`을 **enum 맨 끝에** 추가(중간에 끼우면
  `PartsCatalog.asset`에 이미 저장된 slot 값이 통째로 어긋난다). 머리는 조회 전용이라 enum에
  넣지 않았다 — 넣으면 `TryRollLootPart`가 머리 파츠를 뽑아 "런 중 변경 불가" 규칙이 깨진다.
  화면 배치 순서를 담은 `DisplayOrder` 배열 추가(enum 선언 순서는 직렬화 때문에 못 바꾸므로 분리)
- `Assets/Scripts/PartData.cs` — `discSlotCount` 추가, `BuildDescription()`이 디스크 슬롯 표기 지원
- `Assets/Scripts/PartsCatalog.cs` — `partBoxCapacity` 추가, `DefaultPartBoxCapacity`(20) 상수.
  적재량이 0인 구 데이터를 만나면 기본값으로 보정해서 돌려준다(상자를 아예 못 얻는 상태 방지)
- `Assets/Scripts/RunState.cs` — `ModdingInventory` 추가, `Reset()`에 포함
- `Assets/Scripts/ModdingManager.cs` — `Instance`(static) 노출, `PartBoxCapacity`/`DiscSlotCount`,
  `CanReceiveMorePartBoxes`/`AddPartBoxes()`(상한 clamp), `OpenAllBoxesIntoInventory()`(일괄 자동 개봉),
  `GetInventoryParts()`, `TryGetEquipableSlot()`, `TrySwapInventoryWithSlot()`(맞교환), `ClearInventory()`.
  기존 `TryOpenBox()`(하나씩 열어 즉시 장착)는 제거
- `Assets/Scripts/ModdingPanelUI.cs` — **전면 재작성**. 칸 개수가 데이터에 따라 달라져 씬에
  미리 배치할 수 없으므로 인벤토리 칸과 슬롯 칸을 코드로 생성한다
- `Assets/Scripts/EnemyUnit.cs` — 적재량 상한 도달 시 부품 상자를 아예 드랍하지 않음
  (주울 수 없는 상자가 필드에 쌓이면 헛걸음이 된다)
- `Assets/Scripts/RewardPickup.cs` — 상자 습득을 `ModdingManager.AddPartBoxes()` 경유로 변경.
  드랍 시점에도 상한을 보지만 여러 몬스터가 거의 동시에 죽으면 둘 다 통과한 뒤 습득 시점에
  상한을 넘을 수 있어 여기서 한 번 더 막는다
- `Assets/Scripts/ShopManager.cs` — 디스크 슬롯 수를 `ModdingManager.DiscSlotCount`에서 가져와
  **DiscSlot 파츠가 실제로 반영**되도록 변경(기존에는 머리 기본값만 봤다)
- `Assets/Scripts/GameHUD.cs` — `partBoxText` 추가, 전투 중 "부품 상자 5/20" 표시
- `Assets/Scripts/WaveManager.cs` — `FinalWaveNumber` 공개(정비 화면 "WAVE 07 / 20" 표기용)
- `Assets/Data/PartsCatalog.asset` — 파츠 17개 → **25개**. 헬멧 4종(표준/경량+3/강화+7/티타늄+14),
  디스크 슬롯 4종(표준 6칸/확장 8칸/대용량 10칸/초대용량 12칸) 추가. 로봇별 적재량
  (100001=20, 100002=24) 설정. **전부 밸런스 미확정 임시값**
- `Ground01.unity` — `ModdingPanel` 하위를 전면 재구축(상단바 / 인벤토리 스크롤뷰 / 파츠 슬롯
  그리드 / 능력치 / 안내 / 정비 완료 버튼), `TopHud`에 `PartBoxText` 신설

**구현 중 발견해 고친 문제 4건**

1. **UI 칸이 2배로 생성됨** (슬롯 9개여야 하는데 18개, 인벤토리 20개여야 하는데 40개) —
   `Destroy()`는 프레임 끝에야 실제로 파괴되는데, `Open()` 안에서 Refresh가 두 번 돌면서
   (명시적 호출 + `OpenAllBoxesIntoInventory()`의 `NotifyChanged`) 아직 살아있는 칸 위에 새 칸이
   겹쳐 쌓였다. → 파괴 전에 `SetParent(null)`로 부모에서 먼저 떼어내 `childCount`가 즉시
   정확해지도록 수정
2. **`DestroyImmediate`가 물리 콜백 중 예외** — 1번을 처음엔 `DestroyImmediate`로 고쳤는데,
   정비 화면이 열린 채 보상 픽업이 흡수되면 `OnTriggerEnter → NotifyChanged → ClearChildren`
   경로로 들어와 "Destroying GameObjects immediately is not permitted during physics trigger"
   예외가 났다. → 위 `SetParent(null)` + `Destroy()` 방식으로 최종 수정
3. **해상도가 바뀌면 그리드가 무너짐** — `GridLayoutGroup.cellSize`는 고정 픽셀인데 Canvas가
   `ConstantPixelSize`라 Game View 해상도에 따라 컨테이너 폭이 크게 달라진다. 실측에서 캔버스가
   절반 크기가 되자 4열이 2열로 무너졌다. → `Update()`에서 매 프레임 컨테이너 폭 기준으로
   cellSize를 다시 계산(칸을 다시 만들지는 않는다)
4. **슬롯 칸이 세로로 넘쳐 다른 패널을 덮음** — 슬롯 영역은 스크롤이 없는데 비율만 유지하다
   보니 3행 높이가 컨테이너의 1.7배가 됐다. → `EnsureGrid`에 `fitRows`를 추가해 슬롯은 높이도
   컨테이너에 맞추도록 수정(인벤토리는 스크롤이 있어 넘쳐도 되므로 비율만 유지)

**검증 (MCP 플레이모드, 실제 버튼 클릭으로 확인)**
1. 컴파일 에러 없음
2. **런 시작 초기화**: 8개 모딩 슬롯 전부 기본 파츠 자동 장착(신규 헬멧·디스크 슬롯 포함),
   적재량 20 / 디스크 슬롯 6 정상 조회
3. **적재량 상한**: 25개 지급 시도 → 실제 20개만 지급, 가득 찬 뒤 5개 추가 시도 → 0개 지급
4. **자동 개봉**: 정비 화면 진입 시 보유 상자 7개 → 0개, 인벤토리 7개로 이동
   (플레이어가 버튼을 누르는 과정이 사라짐을 확인)
5. **칸 생성**: 인벤토리 20칸(적재량만큼 빈 칸 포함), 슬롯 9칸(머리 1 + 모딩 8) — 위 1번 버그 수정 후 정확
6. **노란색 강조**: 인벤토리 0번('제트 부스터', 발) 클릭 → 강조된 슬롯이 정확히 1개(`Slot_Foot`)
7. **잘못된 슬롯 클릭**: 헬멧 슬롯을 눌러도 장착물이 바뀌지 않음(안내만 표시)
8. **머리 슬롯**: `Button` 컴포넌트 자체가 없어 클릭 불가(조회 전용) 확인
9. **맞교환**: 발 슬롯 교체 후 인벤토리 0번이 빠진 '표준 발'로 바뀌고 총 개수는 7개 그대로 유지
10. **스탯 즉시 반영**: 제트 부스터(이동속도 +1.5) 장착 → 플레이어 실측 이동속도 5 → 6.5
11. **정비 완료**: 인벤토리 7개 → 0개(남은 파츠 소멸), 정비 패널 닫힘, 상점 정상 오픈
12. **디스크 슬롯 연동**: `ModdingManager.DiscSlotCount` == `ShopManager.DiscSlotCount` (6=6)
13. **화면 캡처**(3840x2160): 상단바·인벤토리 4열 그리드·슬롯 3x3·능력치 9종·안내·정비 완료
    버튼이 시안대로 배치되고 한글과 등급 색상(일반 회색/희귀 파랑/서사 보라/유일 주황/전설 빨강)
    이 정상 렌더링됨

**남은 임시값 / 후속 과제**
- 헬멧·디스크 슬롯 파츠 수치, 로봇별 적재량(20/24)은 **전부 밸런스 미확정 임시값**
- 시안에 있는 **로봇 프리뷰 이미지**(우측 하단 캐릭터 그림)는 이번에 넣지 않았다 —
  전용 아트가 없어 표시할 소스가 마땅치 않아서, 그 자리는 안내 문구 패널로 대체했다
- 무기 소켓 파츠는 여전히 "모든 소켓에 공통 적용"되는 단순화 상태(Phase 4와 동일)
- 파츠 교체 시 무기 타입·무게 제약을 소급 검증하지 않는 기존 정책을 유지했다(제약은 상점에서
  무기를 살 때만 적용). 다만 정비 화면 능력치 패널에 무기 무게 `5 / 8`을 표시하고, 초과 시
  빨간색으로 경고하도록 해 눈으로 확인할 수는 있게 했다

### 파츠 등급 구조 확정 및 등급 추첨 복구 [완료] (2026-08-07)

Phase 9에서 보고한 "등급 게이팅 무력화" 문제에 대해 사용자가 **파츠 데이터 구조를 확정**해 주어
그에 맞게 추첨 로직과 데이터를 함께 고쳤다.

**사용자 확정 사항**
- **파츠는 하나의 종류마다 5개 등급이 전부 존재하는 형식이다.**
  예: "무쇠다리"라면 일반 무쇠다리 / 희귀 무쇠다리 / 서사 / 유일 / 전설이 모두 있다
- 그러므로 **등급을 먼저 선정해도 된다**(후보가 비지 않으므로)

**수정 파일**
- `Assets/Scripts/PartsCatalog.cs` — `TryRollLootPart()`를 **등급 우선 추첨**으로 재작성.
  추첨된 등급의 루트 파츠 전체를 후보로 삼는다. 예전의 "슬롯을 먼저 고르고 그 등급이 없으면
  등급 무관하게 뽑는" 폴백을 제거했다(이 폴백이 minWave 게이팅을 무력화시킨 원인이었다).
  데이터가 아직 없는 등급을 만나면 **하위 등급으로 내려가며** 찾는다 — 상위로 올라가면
  게이팅이 다시 깨지기 때문
- `Assets/Data/PartsCatalog.asset` — 루트 파츠를 확정된 형식으로 **전면 재구성**.
  기존 루트 파츠 17개를 지우고 **19종 × 5등급 = 95개** 생성(기본 파츠 8개는 그대로 유지, 총 103개).
  - 무기 소켓 5종(정밀/속사/에너지/산탄/폭발 - 무기 타입 6종 중 근접을 뺀 전부를 커버),
    팔장갑 2종, 자기장 코어 2종, 다리 2종, 다리장갑 2종, 발 2종, 헬멧 2종, 디스크 슬롯 2종
  - 등급별 수치 = **일반 등급 기준값 × 배율(1.0 / 1.3 / 1.7 / 2.2 / 3.0)**.
    디스크 슬롯만 배율이 어색해서 등급별 절대값(6/7/8/10/12, 7/8/10/12/14)을 직접 지정
  - 무기 소켓은 허용 타입이 등급과 무관하게 고정이라 등급 차이를 표현할 값이 없었다.
    → **등급에 비례하는 공격력 보너스**를 주어 상위 등급 소켓이 의미를 갖게 했다(임시 설계)
  - 파츠 ID 체계: `510000 + 종류인덱스*10 + 등급인덱스`. 기본 파츠는 기존 500001~500008 유지

**검증**
1. 컴파일 에러 없음
2. **등급별 후보 수가 19개씩 완전히 균등**, 19종 전부 5등급을 갖춤(누락 0건)
3. **웨이브별 등급 분포(각 600회 샘플)** — minWave 게이팅이 정확히 복구됨:
   - 웨이브 1~2: 일반·희귀만 (서사/유일/전설 **0건**)
   - 웨이브 3: 서사 등장 / 웨이브 5: 유일 등장 / 웨이브 7: 전설 등장
   - 수정 전에는 웨이브 1에서도 전설이 그대로 나왔다
4. **플레이모드 실측** — 웨이브 1에서 10개 개봉 시 일반·희귀만, 웨이브 10에서는 서사·유일·전설이
   섞여 나옴. 루트 파츠를 전부 갈아엎었지만 기본 파츠 8개는 정상 장착 유지됨
5. 파츠 종류가 17종 → 19종으로 늘어 **동일 파츠 중복 등장도 함께 완화**됨
   (Phase 9에서 지적한 "티타늄 다리 장갑 6개" 같은 쏠림)

**남은 임시값**
- 등급 배율(1.0/1.3/1.7/2.2/3.0)과 각 파츠 종류의 일반 등급 기준값은 전부 밸런스 미확정 임시값
- 무기 소켓의 "등급 = 공격력 보너스" 해석은 임의 설계다 → **바로 아래 항목에서 사용자 확정
  사양으로 교체됨**
- 근접무기(Melee) 전용 소켓은 해당 무기가 데이터에 없어 만들지 않았다 → 아래 항목에서 추가함

### 무기 소켓 사양 확정: 무기 타입 3종 + 등급 = 사거리/감지거리/회전속도 [완료] (2026-08-07)

무기 소켓의 종류와 등급이 각각 무엇을 결정하는지 사용자가 확정해 주어 그에 맞게 구현했다.

**사용자 확정 사항**
1. **무기 소켓의 종류 = 장착 가능한 무기 타입을 결정**하며, 무기 타입은 **경무장 / 중무장 /
   근접무기 3가지뿐**이다
2. **무기 타입과 투사체 타입은 다른 개념이며 둘 다 유지된다.** 소켓은 **무기 타입에만** 영향을 준다
   (기존 6종 연사/산탄/정밀/폭발/에너지/근접은 투사체 타입으로 그대로 남는다)
3. **무기 소켓의 등급 = 사거리 · 감지거리 · 공격 방향 회전 속도를 제어**한다 (배율 방식)
4. **감지거리 = 적을 감지하여 발사하는 거리 / 사거리 = 탄이 날아가는 최대 거리** (별개 개념)

**신규 파일**
- `Assets/Scripts/WeaponClass.cs` — 무기 타입 enum(경무장/중무장/근접무기) + 한글 표시명.
  기존 `WeaponType`(투사체 타입)과 별개 개념임을 주석에 명시

**수정 파일**
- `Assets/Scripts/PartData.cs` — 무기 소켓 필드를 무기 타입 기준으로 교체
  (`restrictsWeaponType`/`allowedWeaponType` → `restrictsWeaponClass`/`allowedWeaponClass`).
  등급 효과 3종(`socketRangeMultiplier`/`socketDetectRangeMultiplier`/`socketRotationSpeedMultiplier`)
  추가. 배율이 0인 옛 데이터를 만나도 1배로 동작하도록 보정 프로퍼티를 함께 둠
- `Assets/Scripts/PartsCatalog.cs` — `WeaponMetaEntry`에 `weaponClass` 추가
  (기존 `type`(투사체 타입)은 그대로 유지 - 둘 다 살아있다)
- `Assets/Scripts/ModdingManager.cs` — `CheckWeaponTypeAllowed()`가 투사체 타입이 아니라
  **무기 타입**으로 검사하도록 변경. 소켓 등급 배율을 넘겨주는 `GetWeaponSocketModifiers()` 신설
- `Assets/Scripts/PlayerShootManager.cs` — **감지거리와 사거리를 분리**했다.
  예전에는 `weapon_range` 하나가 타겟 탐색 반경과 투사체 비행거리 양쪽에 쓰였다.
  - 사거리(`GetTravelRange`) = `weapon_range` x 소켓 사거리 배율 → 투사체 최대 비행거리
  - 감지거리(`GetDetectRange`) = `weapon_range` x 소켓 감지거리 배율 → 타겟 탐색 반경.
    **사거리를 넘지 않도록 잘라낸다** (닿지도 않을 적을 조준해 헛방을 쏘지 않도록)
  - 회전속도 = 슬롯의 `rotation_speed_degrees_per_second` x 소켓 회전속도 배율
  - 배율은 `Update()`에서 프레임당 한 번만 조회해 슬롯마다 재조회하지 않는다
- `Assets/Data/PartsCatalog.asset`
  - 무기 6종에 **무기 타입 매핑** 추가. 분류 기준은 **무게**(2~3=경무장, 4 이상=중무장):
    경무장 = 기관단총(2)·라이플(3)·샷건(3)·에너지 무기(3) / 중무장 = 스나이퍼(4)·수류탄(5).
    근접무기에 해당하는 무기는 아직 없다
  - 무기 소켓 파츠를 투사체 타입 기준 5종에서 **무기 타입 기준 3종(경무장/중무장/근접 결합) ×
    5등급 = 15개**로 교체(기존 25개 삭제). 등급별 배율은
    사거리 1.0/1.15/1.3/1.5/1.75, 감지 1.0/1.1/1.2/1.35/1.5, 회전 1.0/1.2/1.45/1.75/2.2
  - 기본 파츠 '표준 소켓'은 타입 제한 없음 + 전 배율 1배
  - 최종 파츠 수: 총 93개(기본 8 + 루트 17종 × 5등급 = 85)

**검증 (MCP)**
1. 컴파일 에러 없음
2. **등급 게이팅 유지**: 루트 후보가 등급별 17개씩 균등, 17종 전부 5등급 완비(미달 0종).
   웨이브 1=일반·희귀만 / 3=서사 / 5=유일 / 7=전설 등장으로 이전과 동일하게 정상
3. **사거리/감지거리 실측**(라이플, 기본 `weapon_range`=7):

   | 소켓 | 사거리 | 감지거리 |
   |---|---|---|
   | 표준(기본) | 7 | 7 |
   | 경무장 일반 | 7 | 7 |
   | 경무장 서사 | 9.1 (7×1.3) | 8.4 (7×1.2) |
   | 경무장 전설 | 12.25 (7×1.75) | 10.5 (7×1.5) |
   | 중무장 희귀 | 8.05 (7×1.15) | 7.7 (7×1.1) |

   전 구간에서 **감지거리 ≤ 사거리**가 유지됨
4. **무기 타입 제한**(실제 `CheckWeaponTypeAllowed` 호출):
   - 경무장 소켓 장착 시 → 기관단총·라이플·에너지 무기 통과 / 스나이퍼·수류탄은
     "이 소켓은 경무장만 장착할 수 있습니다 (현재: 중무장)"로 차단
   - 중무장 소켓 장착 시 → 기관단총 차단, 스나이퍼 통과 (반대 케이스도 정상)
5. **미검증**: 회전 속도 배율은 `socket_modifiers.RotationSpeed`가 올바른 값으로 전달되는 것까지만
   확인했고, 실제 초당 회전 각도를 프레임 샘플링으로 실측하지는 않았다(Phase 8에서 검증한
   회전 로직에 배율만 곱하는 구조라 코드 검토로 갈음)

**남은 임시값 / 후속 과제**
- 무기 타입 분류(무게 2~3=경무장 / 4+=중무장)는 임시 기준이다. `PartsCatalog.asset`의
  `weaponMeta`에서 무기별로 바로 바꿀 수 있다
- 소켓 등급 배율 3종의 수치는 전부 밸런스 미확정 임시값
- **근접무기가 데이터에 없어 '근접 결합 소켓'은 지금 끼우면 어떤 무기도 장착할 수 없는 상태**다.
  근접 무기가 추가되면 그때 의미를 갖는다(부품 상자에서는 나올 수 있으므로 주의)
  → **아래 항목에서 근접무기 2종을 추가해 해소함**

### 근접무기 2종 임시 추가 [완료] (2026-08-07)

'근접 결합 소켓'이 무용지물이던 문제를 해소하기 위해, 사용자 요청으로 근접무기를 임시로 2종 만들었다.

**추가한 무기 (수치는 전부 밸런스 미확정 임시값)**

| ID | 이름 | 공격력 | 공격속도 | 사거리 | 범위 | 관통 | 무게 | 가격 |
|---|---|---|---|---|---|---|---|---|
| 300601 | 진동 블레이드 | 8 | 4 | 2 | 0.7 | O | 2 | 30 |
| 300701 | 파일 벙커 | 30 | 1.2 | 3 | 1.2 | O | 4 | 45 |

- 근접무기 설계 방향: **사거리가 매우 짧은 대신 공격력이 높고 관통**(한 번에 여러 적을 벤다).
  경량(진동 블레이드) = 빠르게 여러 번 / 중량(파일 벙커) = 느리지만 한 방이 크다

**수정한 데이터 3곳** (한 곳만 빠져도 동작하지 않아 함께 넣어야 한다)
- `Assets/Data/GameDataAsset.asset` — 무기 테이블에 2종 추가(6종 → 8종)
- `Assets/Data/PartsCatalog.asset`(`weaponMeta`) — 투사체 타입 `Melee` / 무기 타입 `근접무기` / 무게 매핑
- `Assets/Data/ShopCatalog.asset`(`weaponEntries`) — 상점 판매 등록.
  **이게 없으면 근접 소켓을 껴도 무기를 구할 방법이 없다**

**구현 중 확인한 것**
- `weapon_range`는 **정수 필드**다. 파일 벙커를 2.5로 넣으려다 반올림으로 2가 되어 진동 블레이드와
  같아졌고, 중량 근접이 더 길도록 3으로 고쳤다. **무기 사거리에는 소수를 쓸 수 없다**
- 근접 전용 아트·이펙트가 없어 손 이미지는 기관총(`LeftGiganchong`/`RightGiganchong`),
  투사체는 `Energy`를 임시 재활용했다. 실제로는 짧은 사거리의 투사체가 순간적으로 날아가는
  형태라 "휘두르기" 연출은 아니다

**검증 (MCP 플레이모드)**
1. 컴파일 에러 없음, 무기 데이터 정상 로드(진동 블레이드/파일 벙커 둘 다 `GameDataManager.Weapons`에서 조회됨)
2. **무기 타입 제한**: 근접 결합 소켓(서사) 장착 상태에서 근접무기 2종은 통과,
   기관단총(경무장)·스나이퍼(중무장)는 "이 소켓은 근접무기만 장착할 수 있습니다"로 차단
3. **소켓 등급 배율 적용**: 근접 결합 소켓(서사, 사거리 x1.3 / 감지 x1.2) 기준
   진동 블레이드 사거리 2 → **2.6**, 감지거리 **2.4**로 정확히 반영
4. **무게 제한**: 진동 블레이드(무게 2) 장착 시 4/8로 통과
5. **실제 전투 검증**: 두 소켓을 근접무기로만 채우고 좀비를 근접 거리에 배치 → **정상 처치됨**.
   동시에 플레이어 체력이 100 → 40으로 깎였는데, 이는 근접 사거리라 적이 밀착한 결과로
   근접무기의 리스크가 의도대로 드러난 것이다
6. 검증 도중 "발사가 전혀 안 되는" 현상을 한 번 관찰했으나, 원인은 직전 정비 화면 테스트로
   `IsIntermission=true` / `timeScale=0` 상태가 유지되고 있었기 때문이었다(게임 코드 문제 아님).
   `HandleNextWaveRequested()`로 전투를 재개한 뒤 정상 동작 확인

**남은 과제**
- 근접무기 전용 아트/이펙트(휘두르기 모션, 근접 히트 이펙트)는 없다. 현재는 투사체 재활용
- 근접무기는 투사체 시스템 위에 얹혀 있어, 진짜 "붙어서 베는" 판정(지속 히트박스 등)은 아니다.
  데모 범위에서는 짧은 사거리 투사체로 충분하다고 판단했다

### 무기 시스템 전면 교체 (신규 13종) — 계획 수립 [진행중] (2026-08-07)

사용자가 제공한 `컴스톡 무기 프롬프트.md`(무기 13종 스펙)와 `무기_리소스_FIX (2)`
이미지 23장을 근거로, **기존 무기 8종을 전부 삭제하고 신규 13종 × 5등급 = 65행으로
완전 교체**한다. 착수 전 사용자에게 8건을 확인받았다.

**사용자 확정 사항 (8건)**

1. **신규 메커니즘 전부 구현** — 관통 횟수/확률 관통, 스플래시 반경, 방어력 %무시,
   지속시간 빔, 넉백까지 원안 그대로 (근사·생략 없음)
2. **등급별 실제 데이터 행 65개 생성** — 배율 방식이 아니라 13종 × 5등급 실제 행
3. **사거리 환산 = 1유닛 : 50** — FHD 16:9 기준 가로 가시반경 15.4유닛에 최대 사거리
   750을 맞춘 값. 감지 400→8유닛, 근접 60→1.2유닛
4. **기존 무기 8종 전부 삭제** 후 13종으로 완전 교체
5. **쿨다운을 "발사 종료 후 대기" 방식으로 변경** — 모든 무기 공통. 지금까지는 발사
   시점부터 `1/atsp`가 흘렀는데, 앞으로는 **발사 동작이 끝난 뒤** 대기시간이 시작된다.
   일반 총기는 발사 시간이 0이라 결과가 같지만, **플라즈마캐논(3초 지속 빔)은
   3초 + 대기 2초 = 5초 주기**가 되어 빔이 여러 개 겹치는 문제가 구조적으로 사라진다
6. **밸런스 재조정 포함** — 무기 공격력을 낮추고 몬스터 HP를 올려 재조정
7. **근접 사거리는 실제로 닿도록 조정** — 원안 1.2유닛은 적 몸통 접촉 최소거리
   1.4유닛보다 짧아 한 대도 못 때린다
8. **디스크를 제외한 모든 파츠에 무게 부여** + 다리 지탱 무게를 그에 맞춰 재조정

**조사에서 확인한 주요 갭 (현재 코드로는 표현 불가)**

| 필요 기능 | 현재 상태 |
|---|---|
| 관통 N회 / 확률 관통 | `weapon_penetration`이 **bool**(무제한 관통 or 없음)뿐 |
| 스플래시 반경 | `weapon_id 300400~300499` **하드코딩 범위 전용** |
| 방어력 % 무시 | `TakeDamage(int)`가 **고정 뺄셈**만, %무시 개념 없음 |
| 지속시간 빔 | 관련 코드 **전무** |
| 넉백 | 관련 코드 **전무**. `EnemyUnit.cs:183`이 매 프레임 velocity를 통째로 덮어써서 **`AddForce`는 100% 무효** |
| 무기별 회전속도/투사체속도 | **슬롯 인스펙터 고정값**이라 무기를 바꿔도 안 변함 |
| 감지거리 | `weapon_range` 하나에서 파생. **별도 컬럼 없음** |
| 소수 사거리 | `weapon_range`가 **int** |
| 산탄 | 다중탄이 **평행 배치 + 발사당 1회 퍼짐 롤**이라 부채꼴로 흩어지지 않음 |

**추가로 발견한 기존 문제 (이번에 함께 해결)**

- **등급 배율이 공격력·공격속도에 이중으로 곱해져 실질 화력이 배율²로 오르고 있었다**
  (희귀 1.3 → 실제 1.69배). 등급별 실제 행이 생기므로 배율 적용 자체를 제거한다
- **`robot_atk`가 투사체마다 평탄 가산**되어 다발 무기에 몰아준다. `robot_atk=10` 기준
  소드오프샷건은 +167%, 대물저격총은 +5% → **발사 1회당 균등 배분**으로 수정
- **몬스터 HP(좀비 20/차저 40)가 신규 무기 화력에 비해 너무 낮아 전부 1방에 즉사**
- 드랍 테이블이 존재하지 않는 `item_id: 300002`를 참조 중(기존 버그)

**작업 계획 (7단계)**

| # | 단계 | 내용 |
|---|---|---|
| 1 | 스키마·코어 코드 | `WeaponData` 15→24필드 재설계, `Projectile`/`EnemyUnit`/`PlayerShootManager` 수정 |
| 2 | 신규 컴포넌트 | `BeamProjectile`(지속 빔), `MeleeSwing`(근접 부채꼴 판정+넉백) |
| 3 | 이미지 임포트 | 21장 → `Assets/Resources/`, 영문명 + 좌우 교차 규칙, Sprite 임포트 설정 강제 |
| 4 | 65행 생성 | `Editor/WeaponTableGenerator.cs`로 3개 에셋(무기/weaponMeta/상점)을 65행씩 생성 |
| 5 | 밸런스 재조정 | 몬스터 HP 상향, `robot_atk` 하향+균등배분 |
| 6 | 파츠 무게 | `PartData.weight` 신설, 95개 루트 파츠에 무게 부여, 지탱력 재조정 |
| 7 | 씬·검증 | 시작 무기·프리팹·삭제 필드 정리, MCP 플레이모드 실측 |

**설계 결정 근거**

- **ID 체계**: `300000 + 종류(1~13)×100 + 등급(1~5)`. 하위 2자리가 등급 슬롯으로 비워져
  있던 기존 구조를 그대로 활용. 예) 중기관총 일반=300101 … 전설=300105
  - ⚠️ 300101~300701은 **삭제될 구 무기와 ID가 겹친다.** 씬의 시작 장착 무기를 갱신하지
    않으면 경고 없이 조용히 다른 무기가 장착되므로 7단계에서 반드시 교체할 것
- **"예측화력" 정의 = `공격력 × 투사체수 × 공속`**, 등급별 15%는 **공격력에만** 반영한다
  (`atk[g] = atk × 1.15^g`). 공속을 함께 올리면 전기톱검 3/s→5.2/s처럼 발사 빈도가
  붕괴하고, 사거리를 올리면 Phase 6에서 해결한 "화면 밖 교전" 문제가 등급별로 재발한다
- **65행 데이터는 반드시 생성기로 만든다.** 65행 × 24필드 = 1,560개 값을 YAML로 손으로
  넣는 것은 비현실적이고(한글이 `\uXXXX`로 저장됨), 밸런스는 반드시 재조정되므로
  `Assets/Scripts/Editor/WeaponTableGenerator.cs`를 **영구 파일로 남겨** 재실행 가능하게 한다.
  사람이 정의하는 값은 13종 × 24 = 312개이고 나머지는 `1.15^g` 파생이다.
  구글 시트 연동 금지 규칙과 무관하다 — 프로젝트 내부 C# 상수에서 생성하며 외부 리소스를
  일절 참조하지 않는다
- **넉백은 속도 합성 방식**으로 구현한다. `EnemyUnit.FixedUpdate`가 velocity를 덮어쓰므로
  `AddForce`는 무효이고, 이동을 완전히 정지시키면 전기톱검(공속 3/s) 앞에서 적이 영구히
  밀려나 접근 불가가 되므로 이동 속도에 넉백 속도를 **더하는** 방식을 쓴다
- **근접 3종은 투사체를 폐기하고 전용 스윙 판정**으로 바꾼다. 사거리 1.2유닛을 속도 10으로
  날리면 0.12초 만에 소멸해 아무것도 보이지 않는다

**확정 수치 (일반 등급 기준, 전부 밸런스 1차값)**

| 무기 | 타입 | 공격력 | 공속 | 사거리 | 감지 | 투사체 | 관통 | 스플래시 | 기타 | 무게 |
|---|---|---|---|---|---|---|---|---|---|---|
| 중기관총 | 중 | 4 | 10 | 7.0 | 6.0 | 1 | — | — | | 5.0 |
| 전투산탄총 | 중 | 6 | 0.8 | 4.0 | 3.0 | 8 | 60% 1회 | — | | 4.5 |
| 대물저격총 | 중 | 85 | 0.25 | 15.0 | 8.0 | 1 | 3회 | — | | 5.5 |
| 플라즈마캐논 | 중 | 90 | 3초빔+2초 | 8.0 | 7.0 | 1 | 무제한 | — | 방어무시 50% | 6.0 |
| 로켓런처 | 중 | 45 | 0.4 | 7.0 | 4.8 | 1 | — | 2.4 | | 5.5 |
| 소드오프샷건 | 경 | 6 | 1.2 | 3.6 | 2.6 | 6 | 40% 1회 | — | | 2.0 |
| 레이저피스톨 | 경 | 20 | 2 | 6.0 | 5.0 | 1 | 50% 1회 | — | 방어무시 25% | 1.5 |
| 지정사수소총 | 경 | 40 | 0.55 | 10.0 | 7.0 | 1 | 1회 | — | | 3.0 |
| 기관단총 | 경 | 7 | 2 | 6.4 | 5.6 | 3 | — | — | | 2.5 |
| 유탄발사기 | 경 | 30 | 0.5 | 5.0 | 4.0 | 1 | — | 1.5 | | 3.0 |
| 생존단검 | 근접 | 24 | 1.8 | 2.2 | 2.2 | 1 | — | — | 넉백 5 | 1.0 |
| 전술 마체테 | 근접 | 36 | 1.2 | 2.6 | 2.6 | 1 | — | — | 넉백 9 | 1.5 |
| 전기톱검 | 근접 | 14 | 3 | 2.4 | 2.4 | 1 | — | — | 넉백 5 | 2.0 |

- 사거리·감지는 md 수치 ÷ 50. **근접 3종만 +1.0유닛 오프셋**(적 몸통 접촉 거리 1.4유닛 대응)
- 예측화력은 단일타겟 무기 40~43, 관통·광역 무기 15~22로 배치했다. 후자는 여러 마리를
  동시에 때리므로 실효 화력이 보정된다
- 몬스터 HP: 좀비 20→**60**, 차저 40→**150** / `robot_atk` 10→**3**(+균등 배분)
- 파츠 무게(등급 무관): 무기소켓 2.0 / 팔장갑 1.5 / 자기장코어 2.5 / 다리 3.5 /
  다리장갑 2.0 / 발 1.2 / 헬멧 1.5 / 디스크슬롯 0.8 = **기본 합계 15.0**
- 지탱력: 기본 코어+다리 8 → **24**(12+12)로 상향, 루트 최상위 42 유지
  → 기본 상태로 경무장 2정은 가능, **중무장 2정(9.5~12)은 상위 파츠 필요**

**이미지 매핑 규칙 (기존 기관단총 파일 대조로 확인)**

`LeftSMG.png` = `기관단총_오른쪽사선`, `RightSMG.png` = `기관단총_왼쪽사선`으로 **교차**되어
있다(파일 크기 일치 확인). 파일명의 "왼쪽/오른쪽"은 총구가 향하는 방향이고 Unity의
`Left/Right`는 손 슬롯이라 의도적으로 교차시킨 것이다.
→ **왼쪽사선/왼쪽하단 → `Right○○` / 오른쪽사선/오른쪽하단 → `Left○○`**
기관단총 2장은 기존 `LeftSMG`/`RightSMG`와 바이트가 동일하므로 재사용하고 복사하지 않는다
(복사 대상 21장). 근접 3종은 방향 이미지가 1장뿐이라 좌우 공용으로 쓴다.

### 무기 시스템 전면 교체 — 구현 [완료] (2026-08-07)

위 계획대로 1~7단계를 전부 구현하고 MCP 플레이모드로 실측 검증했다.

**신규 파일**
- `Assets/Scripts/WeaponFireMode.cs` — 공격 생성 방식 enum(투사체/빔/근접스윙).
  `WeaponType`(분류)·`WeaponClass`(소켓 제한)와는 또 다른 축이다
- `Assets/Scripts/BeamProjectile.cs` — 플라즈마 캐논용 지속 빔. 발사 순간의 위치·방향을
  고정한 채 0.2초마다 `Physics.OverlapBox`로 직선 범위를 타격한다. **총구를 따라다니지 않는
  이유**: 3초 내내 조준을 따라 회전하면 화면 전체를 쓸어버리는 무기가 된다
- `Assets/Scripts/MeleeSwing.cs` — 근접 3종의 부채꼴(반각 60도) 즉시 판정. static 유틸리티
  (`DropItemManager`와 같은 패턴). **투사체로 흉내내지 않은 이유**: 사거리 2유닛짜리 탄을
  속도 10으로 쏘면 0.2초 만에 사라져 화면에 아무것도 안 보인다
- `Assets/Scripts/Editor/WeaponTableGenerator.cs` — 13종 정의에서 65행 × 3테이블을 생성.
  **영구 파일로 남긴다** (밸런스는 반드시 재조정되므로 계수만 고쳐 재실행하면 된다).
  메뉴 `Comstock/무기 테이블 65행 재생성`
- `Assets/Scripts/Editor/WeaponTableValidator.cs` — 세 테이블의 정합성 8종 검사.
  메뉴 `Comstock/무기 테이블 검증`

**주요 수정**
- `GameDataModels.cs` — `WeaponData` 15필드 → **24필드**. `weapon_range`를 int→float로
  바꾸고(근접 2.2 같은 소수 사거리 필요), `weapon_detect`(감지거리)를 사거리와 분리했으며,
  관통 횟수/확률·스플래시 반경·방어무시·넉백·지속시간·발사방식·투사체속도·회전속도·
  이미지 배율/각도를 신설했다. `weapon_capacity`/`weapon_reload`/`weapon_penetration` 삭제
- `Projectile.cs` — 인자 9개를 `Projectile.Spec` 구조체로 묶었다. 관통을 bool에서
  **카운터**(0=없음/N=N회/-1=무제한)로 바꾸고, 스플래시를 **weapon_id 하드코딩 범위에서
  해방**시켜 `weapon_splash > 0`이면 어떤 무기든 폭발하게 했다
- `EnemyUnit.cs` — `TakeDamage(int, float defIgnore = 0)`로 방어력 % 무시 지원.
  `ApplyKnockback()` 신설. **`AddForce`를 쓸 수 없다** — `FixedUpdate`가 매 프레임
  `linearVelocity`를 통째로 덮어쓰기 때문에 다음 프레임에 사라진다. 그래서 넉백 속도를
  따로 들고 있다가 **이동 속도에 더하는** 방식을 썼다. 이동을 정지시키지 않은 이유는
  전기톱검(공속 3/s) 앞에서 적이 영구히 밀려나 접근조차 못 하는 것을 막기 위함
- `PlayerShootManager.cs` — **대기시간이 발사 동작 종료 후부터 흐르도록 변경**(사용자 확정).
  `firemode` 3분기, 다중탄을 평행 배치에서 **부채꼴**로 교체하고 탄퍼짐·확률관통을
  **탄마다 개별**로 굴리게 했다. 등급 배율 제거, 무기별 회전속도/투사체속도 사용,
  `weapon_imgangle` 보정각 추가, **감지거리 상한 8.5유닛**(화면 밖 교전 방지) 신설
- `ShopManager.cs` — 등급을 먼저 뽑고 **그 등급의 무기 행 중에서** 고르는 방식으로 전환
  (후보가 없으면 한 등급씩 하향 — `PartsCatalog.TryRollLootPart`와 같은 폴백)
- `PartData.cs` / `ModdingManager.cs` — `weight` 필드 신설, 파츠 교체 시에도 무게 검증
  (`CheckPartWeightLimit`). 자기장코어·다리를 교체할 땐 지탱력 자체가 바뀌므로 새 파츠의
  `weightCapacity`를 반영해 비교한다
- `RunState.cs` — `EquippedWeapon.StatMultiplier` 삭제(등급별 행이 생겨 불필요)

**데이터 결과**
- 무기 65행 / `weaponMeta` 65행 / 상점 65행 — 세 테이블 ID 집합 완전 일치
- 파츠 93개에 무게 부여, 지탱력 전체 ×3 (기본 8 → **24**)
- 몬스터 HP 좀비 20→**30**, 차저 40→**75** / `robot_atk` 10→**3** / 보스 12000→**8000**
- 드랍 테이블의 죽은 ID 2건(300001, 존재한 적 없는 300002) 정리
- 이미지 21장 임포트 + Sprite 설정 강제 → **23장 전부 `Resources.Load<Sprite>` 성공**

**밸런스를 두 번 잡은 경위 (중요)**
1차로 좀비 HP를 40/100까지 올렸더니 플레이어가 **7초 만에 사망**했다. 원인은 스폰 압력이
`spawnBatchSize 3 / spawnInterval 0.8` = **초당 3.75마리**인데 무기 화력이 2.4마리/초에
그쳐 적이 상한(30마리)까지 쌓인 것이었다. 기존 무기가 강했던 게 아니라(구 기관단총은
공속 15에 270 DPS였다) **원안 수치를 그대로 낮추면 게임이 성립하지 않는 구조**였다.
→ 예측화력을 중무장 90 / 경무장 60 / 근접 60(1정 기준)으로 다시 올리고 몬스터 HP는
30/75로 낮춰 재조정했다. 관통·스플래시가 있는 무기는 여러 마리를 동시에 때리므로
명목 수치를 그만큼 낮게 잡았다(대물저격총 명목 30 → 3관통이라 실효 90).

**검증 (MCP)**
1. 컴파일 에러 0건, `WeaponData` 필드 28개(프로퍼티 포함) 확인
2. **데이터 검증 8종 전부 통과** — 65행/ID 중복 없음/세 테이블 ID 일치/등급 필드와 ID 일치/
   공격력이 정확히 `1.15^등급`/스프라이트 전부 로드/감지거리 ≤ 사거리/발사방식별 필수값
3. **관통 3회 실측**: 일직선 5마리 중 **4마리 사망**(최초 1 + 관통 3), 남은 1마리는
   거리 8.8로 감지거리(8) 밖 — 관통 횟수와 감지 상한이 둘 다 의도대로 동작
4. **빔 실측**: 일직선 6마리 중 5마리가 한 번의 빔에 사망(무제한 관통 + 3초 지속 피해).
   필드의 `BeamProjectile`이 항상 1개 이하 — **빔 중첩이 구조적으로 차단됨을 확인**
5. **근접 스윙 실측**: 전술 마체테 2정으로 2유닛 거리의 3마리 중 2마리 처치
6. **넉백 실측**: `ApplyKnockback(right, 9)` 호출 후 내부 넉백 속도가 정확히 `(9,0,0)`으로
   설정됨. 방향이 0벡터면 무시되는 방어 로직도 확인
7. **파츠 무게 성장 곡선 확인**: 기본 파츠 무게 15 / 지탱 24 →
   경무장 2정(21) 가능 · 중무장1+경무장1(24) 가능 · **중무장 2정(27)은 상위 코어/다리 필요**

**미검증 / 남은 과제**
- **스플래시(로켓런처·유탄발사기)는 실측하지 못했다.** 실험을 두 번 시도했으나 그때마다
  플레이어가 먼저 사망해(MCP 검증이라 조작 입력이 없어 제자리에서 포위당한다) 발사가
  멈췄다. 코드 경로 자체는 관통·빔과 같은 `Projectile`/`TakeDamage`를 쓰므로 동작할
  가능성이 높지만 **확인된 것은 아니다** — 다음에 우선 검증할 것

### 타이틀 화면 + 게임오버 요약 화면 [완료] (2026-08-07)

사용자 요청으로 (1) 간단한 타이틀 화면과 (2) 로봇 파츠·무기·디스크·골드·스탯·도달
웨이브를 보여주는 상세 게임오버 화면을 추가했다.

**신규 파일**
- `Assets/Scenes/Title.unity` — 신규 씬. Canvas(Screen Space Overlay, `ScaleWithScreenSize`
  1920x1080 기준) + `EventSystem`(`InputSystemUIInputModule` — Ground01과 동일 구성) +
  배경 + "컴스톡" 제목 + 부제 + "게임 시작"/"종료" 버튼. Main Camera(단색 배경)도 함께 둠
- `Assets/Scripts/TitleSceneManager.cs` — "게임 시작" 클릭 시 `Ground01` 씬을 바로 로드.
  로봇 선택 화면(`RobotSelectManager`)은 여전히 어떤 씬에도 배치되지 않은 미구현
  상태라 거치지 않는다 — `PlayerRobotController.InitFromSession()`이
  `PlayerSession.SelectedRobotId == -1`이면 테스트용 기본 로봇으로 자동 대체하므로
  그대로 둬도 정상 시작된다. "종료"는 에디터에서는 플레이모드 종료, 빌드에서는 `Application.Quit()`
- `Assets/Scripts/GameOverSummaryUI.cs` — 게임오버 시 이번 런의 최종 상태를 정리해서
  보여준다. `ShopPanelUI`의 "로봇 모딩 상태/장착 무기/디스크/능력치" 섹션과 **동일한
  조회 방식**(`FindFirstObjectByType` + 문자열 조립, `ItemGrade.ToColorHex()`로 등급 색상)을
  그대로 재사용했다 — 이미 검증된 패턴이라 새로 설계하지 않았다

**수정한 씬 — `Ground01.unity`**
- `Canvas/GameOver` 오브젝트를 단순 "GAME OVER" 텍스트에서 **전체화면 요약 패널**로
  확장했다. 기존에 그 오브젝트 자신에 붙어있던 `TextMeshProUGUI`는 제거하고(같은
  오브젝트에 배경 Image와 텍스트를 함께 두면 나중에 추가한 쪽이 항상 위에 그려져
  배경이 텍스트를 가리는 문제가 있어, 배경은 루트의 `Image` 컴포넌트로 두고 텍스트는
  전부 자식으로 옮겼다) 대신 `Image`(반투명 검정 배경)를 붙였다.
  자식: `TitleText`("게임 오버") / `SummaryHeaderText`(도달 웨이브·골드) /
  `ModdingStatusText`(로봇 모딩 8부위 + 헤드) / `EquippedWeaponsText` / `EquippedDiscsText` /
  `StatsText`(최종 9개 스탯) / `TitleButton`("타이틀로", 클릭 시 Title 씬 로드).
  **GameHUD의 `gameOverObject` 참조는 그대로 유지된다** — 오브젝트 자체를 바꾸지 않고
  자식·컴포넌트만 채워 넣어서, "체력 0 → `GameOverManager.OnGameOver` → 이 오브젝트
  활성화"라는 기존 배선을 다시 연결할 필요가 없었다
- 전체 텍스트는 `enableAutoSizing`을 켜서 해상도에 따라 폰트가 칸을 넘치는 문제를
  피했다(프로젝트 안내.md의 기존 권장사항을 따름)

**빌드 세팅**: `Title.unity`(0번) → `Ground01.unity`(1번) → `SampleScene.unity`(2번) 순으로
갱신. **이제 실질적인 진입 씬은 `Title.unity`다**(이전에는 `Ground01.unity`를 직접 실행)

**부수 수정**: `ShopPanelUI.cs`의 무게 표시가 무기 무게만 합산하고 있었다(파츠에 무게가
생기기 전에 작성된 코드). 이번 무기 시스템 작업에서 파츠에도 무게가 생겼으므로
`ModdingManager.GetTotalWeight()`(무기+파츠 합산)로 교체해 상점 화면의 무게 표시를
게임오버 화면과 일치시켰다

**검증 (MCP 플레이모드, 실제 씬 전환까지 실측)**
1. 컴파일 에러 0건
2. Title 씬 진입 → `Canvas`/`EventSystem`/`TitleSceneManager`/버튼 2개 정상 확인
3. `StartButton.onClick.Invoke()` 실행 → `Ground01`로 정상 전환, `PlayerRobotController`
   정상 스폰 확인(HP 100/100)
4. 조작 입력 없이 대기(MCP 검증 특성상 자동) → 적에게 포위당해 사망 → 게임오버 트리거
5. **게임오버 패널 실제 표시 내용 전량 로그로 확인** — 헤드(컴스톡 MK-1), 8부위 파츠(전부
   기본 파츠), 무게(19/24), 장착 무기 2정(기관단총/레이저피스톨), 디스크(0/6, 없음),
   최종 스탯 9종, 도달 웨이브 1·골드 14까지 **전부 정상 표시**
6. `TitleButton.onClick.Invoke()` 실행 → `Title` 씬으로 정상 복귀, `Time.timeScale`도
   1로 정상 복원됨 확인
7. **미검증**: 이번 세션에서도 `screenshot-*` 계열 MCP 도구가 로드되지 않아 실제 화면
   레이아웃(글자 겹침, 3열 배치 여백 등)은 육안으로 확인하지 못했다. 데이터 정확성은
   로그로 전량 확인했지만 **시각적 확인 필요**
- **`weapon_imgangle`이 13종 모두 0이다.** 무기 그림마다 총구가 그려진 각도가 달라서,
  실제 화면을 보며 무기별로 보정각을 실측해 넣어야 총구가 타겟을 정확히 향한다.
  현재는 슬롯 공통 보정각(`rotation_offset_degrees`)만 적용된 상태다
- **밸런스는 전부 1차값이다.** 특히 중기관총이 단일 타겟인데도 명목 DPS가 가장 높고
  (240, 2정 기준), 웨이브 후반(스폰 간격 0.96배씩 감소 → 20웨이브에 초당 8.3마리)을
  실제로 플레이해 검증하지 않았다. 보스 체력 8000도 추정값이다
- 근접무기 전용 연출(휘두르기 모션·히트 이펙트)은 여전히 없다. 판정만 있다
- 빔 연출은 `Energy` 스프라이트를 길게 늘린 임시 비주얼이다

### 절차적(코드 기반) 관절 애니메이션 예시 [완료] (2026-08-08)

사용자 요청: "메인 캐릭터의 관절 움직임을 프로그래밍으로 움직이는 것으로 바꾸고 싶다.
올린 파일로 예시를 만들어 달라". 사용자가 파츠 이미지 4장(몸통 / 다리 위쪽 관절 /
다리 아래쪽 관절 / 신발)을 제공했다.

**이번 작업은 예시(데모)까지만이다. 기존 `Player`(Ground01 씬)와 `PlayerAnimator`
컨트롤러는 전혀 건드리지 않았다.** 실제 교체 여부는 사용자가 데모를 보고 결정한다.

**신규 리소스**
- `Assets/Resources/Parts/Body.png` (250x250) — 몸통. 얼굴이 몸통에 그려진 정면 뷰
- `Assets/Resources/Parts/LegUpper.png` (98x111) — 허벅지. 아래쪽에 무릎 볼조인트
- `Assets/Resources/Parts/LegLower.png` (74x91) — 정강이
- `Assets/Resources/Parts/Foot.png` (117x134) — 신발. 발끝이 **왼쪽**을 향해 그려져 있다
- 4장 모두 Sprite(Single) / PPU 100 / pivot Center / 압축 없음으로 임포터를 강제 설정했다
  (`Resources.Load<Sprite>("Parts/...")` 4건 전부 성공 확인)

**신규 스크립트**
- `Assets/Scripts/ProceduralCharacterRig.cs` — 본체. 애니메이션 클립을 쓰지 않고
  계층 구조를 코드로 조립한 뒤 매 프레임 관절 각도를 계산한다
- `Assets/Scripts/ProceduralRigDemo.cs` — 데모 조작기(A/D 이동, Tab 자동왕복,
  화면 슬라이더로 보폭/발높이/스탠스비율/바운스/골반높이 실시간 조절)
- `Assets/Scripts/Editor/ProceduralRigDemoBuilder.cs` — 메뉴
  `Comstock/절차적 관절 리그 데모 씬 생성` → `Assets/Scenes/JointRigDemo.unity` 생성.
  기존 씬은 건드리지 않고 데모 전용 씬만 새로 만든다. 빌드 세팅에도 추가하지 않았다

**동작 원리 (핵심 4가지)**
1. **뼈 길이·보정각을 스프라이트에서 자동으로 뽑는다.** 사람이 넣는 값은 "스프라이트
   안에서 관절이 어디인가"(정규화 앵커) 6쌍뿐이고, 뼈 길이와 기울기 보정각은 거기서
   역산한다. 허벅지 그림이 약 19도 기울어 그려져 있는데, 앵커 두 개(고관절·무릎)를
   잇는 방향이 정확히 -Y가 되도록 스프라이트를 회전시켜 자동 보정한다
   (`TiltToDown`). 그림을 다시 그려도 앵커만 고치면 된다
2. **보행 주파수를 실제 이동 속도에서 뽑는다.** `주파수 = 속도 / 보폭`. 그래서 속도가
   변하면 다리 젓는 속도가 자동으로 따라온다(클립 방식의 "속도와 다리가 따로 노는" 문제 없음)
3. **발이 미끄러지지 않는다.** 스탠스(접지) 구간에서 발을 뒤로 미는 거리를
   `보폭 x 접지비율`로 맞춰서, 몸이 전진하는 거리와 발이 로컬에서 뒤로 가는 거리가 정확히 상쇄된다
4. **2관절 IK로 각도를 역산한다.** 고관절을 고정하고 발 목표 위치를 준 뒤 코사인 법칙으로
   허벅지·정강이 각도를 구한다(`SolveLeg`). 무릎이 굽는 방향은 토글 하나(`kneeBendsForward`)로
   앞/뒤가 바뀐다. 발은 정강이 각도를 상쇄해 접지 중에는 항상 지면과 평행을 유지하고,
   스윙 중에만 발끝을 든다

여기에 몸통 반응(착지 시 골반 하강 = 사이클당 2회, 좌우 흔들림, 전진 방향 기울기,
착지 스쿼시)과 정지 시 숨쉬기 모션이 얹히고, 이동↔정지는 `gaitBlend`로 섞인다.

**설계 판단**
- **좌우 반전은 RigRoot의 `localScale.x` 부호**로 처리했다. 프로젝트 안내.md에 "음수 스케일 +
  Collider" 경고가 있으므로 **이 리그 밑에는 Collider를 두지 않는다**(무기 앵커처럼 Collider가
  필요한 오브젝트는 리그의 형제로 둘 것). 반전을 `MoveTowards`로 서서히 하면 도는 연출이
  공짜로 생기지만, 그 사이 몸통이 납작해 보이므로 `turnSpeed`를 12로 올려 구간을 짧게 잡았다
- 골반 높이 비율(`hipHeightRatio`)은 **발목~밑창 높이를 제외한 다리뼈에만** 적용한다.
  처음엔 전체 다리 길이에 곱했는데, 신발이 다리 길이의 44%를 차지해서 0.92만 줘도
  무릎이 심하게 굽어 앉은 자세처럼 보였다
- 대기 자세에서 두 발을 `idleStanceWidth`만큼 앞뒤로 벌린다. 0이면 두 다리가 완전히 겹쳐
  신발이 한 짝만 보인다
- 정렬 순서: 두 다리 모두 몸통 뒤(`-10` / `-5`)에 두어 몸통이 고관절을 가리게 하고,
  뒤쪽 다리는 살짝 어둡게 틴트해 깊이감을 준다. 한 다리 안에서는 정강이 → 허벅지(무릎 볼) →
  신발 순으로 올려 그린다

**검증 (MCP, 실제 플레이모드 + 카메라 RenderTexture 캡처)**
1. 컴파일 에러 0건, 런타임 에러 0건
2. 계층 구조 실측: `RigRoot > BodyPivot/Body`, `Leg_Back|Leg_Front > Thigh > Knee > Shin >
   Ankle > Foot`. 앵커에서 자동 계산된 뼈 길이 = 허벅지 0.610 / 정강이 0.610 / 발목~밑창 0.941
3. 화면 캡처로 육안 확인 — 대기 자세(두 신발이 앞뒤로 벌어져 서 있음), 보행 3프레임
   (무릎 굽힘·발 들림·몸통 기울기가 프레임마다 다름), 좌우 전환 모두 정상
4. `screenshot-*` 계열 MCP 도구는 이번 세션에도 로드되지 않아, 프로젝트 안내.md에 적힌
   우회법(카메라를 RenderTexture에 렌더 → PNG 저장 → 파일 읽기)으로 확인했다.
   URP 17에서는 `camera.Render()` 대신 `RenderPipeline.SubmitRenderRequest`를 쓴다

**남은 과제 / 다음 단계 후보**
- **팔이 없다.** 사용자가 준 파츠에 팔이 없어서 몸통+다리만 조립했다. 현재 무기는
  `PlayerShootManager`의 손 슬라이트(`LeftWp_img`/`RightWp_img`)가 따로 들고 있다.
  팔 파츠가 생기면 어깨-팔꿈치 2관절 IK로 무기 그립을 따라가게 만들 수 있다
- **실제 Player 적용은 아직 안 했다.** 적용하려면 `Player` 오브젝트의 SpriteRenderer/Animator를
  이 컴포넌트로 대체하고, `PlayerRobotController`가 `rb.linearVelocity`를 넘겨주면 된다
  (리그가 부모의 Rigidbody를 자동으로 찾으므로 코드 수정 없이도 동작한다).
  단 무기 앵커가 리그 밑으로 들어가면 안 된다(위 "좌우 반전" 항목 참고)
- **구르기(Space) 전용 포즈**는 아직 없다. 대시 중 다리를 접는 포즈를 얹을 자리는 있다
- 수치는 전부 1차값이다. 데모 씬의 슬라이더로 감을 잡은 뒤 인스펙터 기본값을 확정할 것

### 절차적 관절 리그 — 레퍼런스 비율에 맞춤 [완료] (2026-08-08)

사용자가 완성된 캐릭터 모습(워크사이클 GIF, 577x922 8프레임)을 주며 "이 모습이 되어야 한다"고
지적했다. 첫 구현은 파츠 4장을 **원본 픽셀 크기 그대로** 조립했는데, 그러면 다리가 몸통보다
길어져 레퍼런스와 완전히 다른 체형이 된다.

**레퍼런스 GIF 실측 (프레임 0)**

| 항목 | 레퍼런스 | 수정 전 | 수정 후 |
|---|---|---|---|
| 다리(고관절→밑창) ÷ 몸통 높이 | 252/611 = **0.41** | 216/153 = **1.41** | **0.43** |
| 고관절 좌우 간격 ÷ 몸통 폭 | 251/472 = **0.53** | 0.14/1.16 = **0.12** | **0.55** |

**수정 내용**
- **`legScale`(기본 0.30) 신설** — 다리 파츠(허벅지/정강이/신발)에만 곱하는 배율.
  몸통에는 곱하지 않는다. 뼈 길이·앵커 오프셋·스프라이트 스케일에 전부 반영된다
- **`hipSeparation` 0.14 → 0.62** — 레퍼런스처럼 몸통 바닥 좌우 구석 가까이에서 다리가 나온다
- **길이 계열 파라미터를 전부 '다리 길이에 대한 배수'로 바꿨다** — `strideLength`/`stepHeight`/
  `bobAmount`/`swayAmount`/`breathAmplitude`/`idleStanceWidth` → `strideRatio`/`stepHeightRatio`/
  `bobRatio`/`swayRatio`/`breathRatio`/`idleStanceRatio`. **legScale이나 그림을 바꿔도 보행이
  저절로 따라오게 하려는 것**이다(절대값이면 legScale을 만질 때마다 6개를 다시 잡아야 한다)
- **`splayFeetWhenIdle`(기본 켬) 신설** — 정지 시 뒤쪽 다리의 신발을 좌우 반전해 발끝이
  바깥을 향하게 한다. 몸통이 정면 뷰라 두 발이 같은 쪽을 보면 어색하고, 레퍼런스도 벌어져 있다.
  앵커 x가 정확히 대칭으로 뒤집히므로 `localPosition.x`의 부호만 바꾸면 발목 위치가 유지된다
- `rigScale` 0.32 → 0.55 (다리가 짧아져 전체가 작아진 만큼 보정)
- 데모 슬라이더에 **다리 파츠 크기 / 고관절 좌우 간격**을 추가했다. 이 둘은 뼈 길이가 바뀌므로
  리그를 다시 조립해야 하는데, 슬라이더 드래그 중에 매번 즉시 `Build()`를 부르면
  `Destroy()`가 지연 파괴라 RigRoot가 잠깐 두 개가 된다 → **플래그만 세우고 다음 Update에서
  한 번만 조립**하고, 파괴는 `DestroyImmediate`로 바꿨다

**같이 잡은 버그 3건 (전부 실제로 화면에서 재현됨)**
1. **발이 지면에서 떴다.** 숨쉬기가 골반을 위로 올리는데 다리가 거의 펴진 상태(`hipHeightRatio`
   0.97)라 IK 목표가 사거리를 벗어나 잘렸다. → **숨쉬기를 아래로만 움직이게** 바꾸고
   (`-(0.5+0.5sin)`) 기본 `hipHeightRatio`를 0.90으로 낮췄다
2. **보폭이 크면 발이 지면에서 떴다.** 사거리를 벗어난 목표를 "방향 유지 + 거리 축소"로 자르면
   발이 공중에 뜬다. → **높이(y)를 살리고 가로(x)를 줄이는 방식**으로 바꿔서 접지를 보장했다.
   결과적으로 보폭이 다리 길이에 맞게 자동으로 잘린다(`strideRatio` 기본 0.7)
3. **몸통이 납작하게 찌그러진 채 멈춰 보였다.** 데모 씬에는 `GameDataManager`가 없어서
   `Application.runInBackground`를 켜주는 주체가 없었고, Editor 창이 백그라운드면 시간이 거의
   안 흘러 **좌우 전환(스케일 반전) 중간에서 멈춘** 것이었다. → `ProceduralRigDemo.Awake()`에서
   켜도록 했다. 프로젝트 안내.md에 이미 적혀 있던 함정을 그대로 밟았다

**검증 (MCP 플레이모드 + RenderTexture 캡처)**
1. 컴파일 에러 0건 / 런타임 에러 0건
2. 대기 자세 캡처 — 몸통 대비 다리 비율 0.43, 고관절 간격 비율 0.55로 레퍼런스와 일치.
   두 신발이 바깥을 향해 벌어지고 **양발 모두 접지**(밑단 y가 지면 오차 내)
3. 보행 캡처 2장 — 무릎 굽힘·발 들림·몸통 전방 기울기가 프레임마다 다르고, 걷기 시작하면
   두 발이 진행 방향으로 정렬됨을 확인
4. **검증 절차 주의**: 상태를 바꾸는 호출과 캡처를 **한 번의 script-execute에 몰아넣으면 안 된다.**
   그 사이에 Update가 한 번도 돌지 않아 이전 상태(전환 중인 스케일 등)가 찍힌다. 실제로 이것 때문에
   한 번 오진했다 — 상태 변경과 캡처는 호출을 분리할 것

### 절차적 관절 리그 — 무릎이 몸통과 반대로 꺾이던 버그 수정 [완료] (2026-08-08)

사용자가 "다리 방향이 몸통과 반대야 지금"이라고 지적했다. 프로젝트 파일로 넣어준
`관절움직임 레퍼런스.gif`(577x922, 8프레임)를 확인했는데, 픽셀 비교 결과 앞서 쓴
워크사이클 GIF와 **완전히 동일한 파일**이었다(바이트 단위 일치, 8프레임 전부).

**원인**: 2관절 IK의 무릎 굽힘 부호(`kneeBendsForward`)를 **두 다리에 똑같이** 적용했다.
레퍼런스를 다리 부분만 확대해 보면 두 무릎이 **몸통 중심선을 기준으로 대칭으로 바깥쪽을
향해** 벌어져 있다(활 다리 자세). 그런데 좌표를 손으로 계산해보니:
- 왼쪽(뒤쪽) 다리: 목표 x가 고관절보다 **음수 쪽**(대기 자세의 앞뒤 스태거 때문) →
  `baseAngle`이 약 -101.5°이고 여기에 `+a`(약 26°)를 더하면 무릎이 고관절보다
  **+x 방향**(=몸통 안쪽)으로 밀렸다
- 오른쪽(앞쪽) 다리: 목표 x가 고관절보다 양수 쪽 → 같은 `+a`를 더하면 무릎이
  **+x 방향**(=몸통 바깥쪽)으로 밀렸다

두 계산 모두 "+x 방향"으로 무릎이 밀리는 건 같지만, 왼쪽 다리에게는 그게 **안쪽**이고
오른쪽 다리에게는 **바깥쪽**이라 결과적으로 왼쪽 다리만 몸통 쪽으로 꺾인 것처럼 보였다.
실측 스크린샷([mine_legs_zoom.png] 참고)으로 재현을 확인했다.

**수정**
- `Leg` 클래스에 `mirror` 필드 신설(뒤쪽/왼쪽 다리 = true)
- `SolveLeg()`: `sign = kneeBendsForward ? 1 : -1; if (leg.mirror) sign = -sign;` —
  미러링된 다리는 부호를 뒤집어서 두 무릎이 **몸통 바깥쪽으로 대칭**으로 굽게 했다
- `BuildLeg()`: 허벅지·정강이의 **앵커 좌표와 스프라이트 자체(flipX)도 다리별로 미러링**했다.
  뼈의 굽힘 방향만 고치고 그림은 그대로 두면 그림의 고유한 비대칭(허벅지가 19도 기울어
  그려진 것 등)이 스켈레톤과 어긋나 보이기 때문이다. 뼈 **길이**는 미러링해도 보존되므로
  (거울 대칭은 거리를 바꾸지 않는다) `thighLength`/`shinLength`는 기존처럼 한 번만 계산해
  두 다리에 공통으로 쓴다
- 신규 헬퍼 `MirrorAnchorX(normalized, mirror)` — 기존 `MaybeMirrorX`(발의 기본 방향 보정,
  `footSpriteFlipX` 필드에 종속)와는 별개다. 발은 이번 수정 대상이 아니다(이미
  `splayFeetWhenIdle`로 대기 시 바깥으로 벌어지게 되어 있었고 레퍼런스와 맞았다)

**검증 (MCP 플레이모드 + RenderTexture 캡처)**
1. 컴파일 에러 0건
2. 대기 자세 다리 확대 캡처 — 양 무릎이 대칭으로 바깥쪽을 향함(레퍼런스와 일치,
   `mirrorfix_legs_zoom.png`)
3. 보행 중 캡처 2장 — 무릎 굽힘이 자연스럽고 좌우 다리가 시각적으로 대칭적인 스타일을 유지
4. 레퍼런스 GIF의 여러 프레임(idle 근접/스윙 중)을 확대 대조한 결과 모두 무릎이 바깥쪽으로
   굽는 패턴이 일관됨을 재확인했다

### 절차적 관절 리그 — 무릎 미러링을 되돌림(과잉수정) [완료] (2026-08-08)

사용자가 프로젝트에 `관절움직임 레퍼런스.gif`를 넣어주며 다시 지적했다:
1. "다리와 발 방향이 얼굴이 바라보는 방향과 반대임"
2. "다리 두 짝이 서로 반대 방향으로 꺾여있음. 인간의 움직임과 유사해야 하니 수정"

**받은 GIF는 이전에 쓰던 파일과 바이트 단위로 동일했다**(8프레임 전부 일치, 확인 완료).
즉 레퍼런스가 바뀐 게 아니라, **직전 커밋("무릎이 몸통과 반대로 꺾이던 버그 수정")에서
내가 레퍼런스를 잘못 해석**한 것이었다.

**무엇을 잘못 읽었나**: 직전 수정에서 무릎이 "몸통 바깥쪽으로 대칭으로 벌어진 고정 자세"라고
판단해 왼쪽 다리의 굽힘 부호를 영구적으로 반전시켰다. 그런데 레퍼런스를 보행 위상 여러
프레임(ref_00 정지, ref_04 스침)에 걸쳐 다시 보니, 이건 **고정된 좌우 대칭 자세가 아니라
정상적인 보행 위상 차이**였다 — ref_04에서는 두 다리가 스치듯 거의 겹쳐 보였는데, 이는
한쪽은 스윙(앞으로 내딛는 중), 한쪽은 스탠스(뒤에 남은 중)인 자연스러운 걸음걸이의
한 순간일 뿐, "왼쪽 다리는 항상 이렇게, 오른쪽 다리는 항상 저렇게" 굽는 고정 규칙이 아니었다.

**되돌린 내용** (직전 커밋을 정확히 역순으로 되돌림)
- `Leg.mirror` 필드 삭제
- `SolveLeg()`의 `if (leg.mirror) sign = -sign;` 제거 — **두 다리가 다시 같은 부호**를 쓴다.
  실제로 어느 쪽이 더 굽어 보이는지는 각 다리의 보행 위상(목표 각도)에서 자연스럽게 갈린다
- `BuildLeg()`의 허벅지/정강이 앵커·그림 좌우 미러링 제거 — 두 다리가 다시 완전히 같은
  그림·같은 배치 규칙을 쓴다(`MirrorAnchorX` 헬퍼도 삭제)
- **`splayFeetWhenIdle` 기능 자체를 삭제**했다. 이것도 "대기 시 뒤쪽 발을 반대로 뒤집어
  바깥으로 벌린다"는, 같은 레퍼런스 오독에서 나온 내 임의의 추가였다. 두 발은 이제 항상
  **같은 방향**(`footSpriteFlipX`가 정한 진행 방향)을 향한다 — 사람이 두 발을 서로 다른
  쪽으로 벌리고 서지 않는 것과 같다

**검증 (MCP 플레이모드 + RenderTexture 캡처)**
1. 컴파일 에러 0건
2. 대기 자세 — 양 무릎·양 발이 이제 **같은 방향**을 향함(`revert_idle`)
3. 보행 3프레임 — 무릎 굽힘이 자연스럽게 위상에 따라 갈리고, 두 발 모두 진행 방향을 향하며
   서로 충돌하지 않음(`revert_walk1~3`)

**교훈**: 정적 프레임 한두 장만 보고 "고정된 좌우 대칭 규칙"이라고 일반화하면 안 된다.
걷기 애니메이션 레퍼런스는 **여러 위상의 프레임을 같이 보고, 그 차이가 (a) 다리 정체성
(왼쪽/오른쪽)에서 오는 건지 (b) 보행 위상(스윙/스탠스)에서 오는 건지**를 먼저 구분해야 한다.

### 절차적 관절 리그 — 머리 좌우반전 방식 변경 + 발 각도 보강 [완료] (2026-08-08)

사용자 요청 두 가지: (1) "머리 좌우반전으로 바꿔", (2) "발도 각도가 적절히 조절되었으면
좋겠어".

**(1) 머리 좌우반전 — 스케일 방식에서 flipX 방식으로 전환**

기존에는 `RigRoot` 하나의 `localScale.x` 부호를 보간(`facingLerp`, `turnSpeed`)해서
몸통·다리를 통째로 뒤집었다. 이 방식은 보간이 0을 지나는 순간 몸통이 납작하게 찌그러져
보이는 문제가 있었다(이미 프로젝트 안내.md에 알려진 이슈로 기록돼 있었음).

- **몸통(머리)은 `SpriteRenderer.flipX`로 즉시 전환**한다. 픽셀만 좌우로 뒤집으므로
  중간에 찌그러지는 프레임이 없다. flipX는 배치 앵커(`bodyHipAnchor`)도 좌우로 미러링해야
  위치가 어긋나지 않는다(발의 `MaybeMirrorX`와 같은 원리) — `ApplyBodyFacing()` 신설
- **다리는 새로 만든 `LegsGroup`(rigRoot의 자식, 몸통과는 별개)의 `localScale.x` 부호로
  뒤집는다.** 크기가 항상 1(±1)이라 찌그러지는 중간값이 없다
- **보간을 아예 없앴다.** `facingLerp`/`facingTarget`/`turnSpeed`를 전부 제거하고
  `facingSign`(이산값 +1/-1) 하나로 통일 — 값이 바뀌는 순간에만 `ApplyBodyFacing()` +
  `legsGroup.localScale` 갱신을 1회 호출한다. 매 프레임 계산할 필요가 없어져 더 간단해졌다
- `RigRoot` 자체는 이제 `(rigScale, rigScale, 1)` 고정이며 다시는 건드리지 않는다(전체
  리그의 "찌그러짐 가능 지점"을 LegsGroup 하나로 좁혔다는 뜻이기도 하다)

**(2) 발 각도 — 뒤꿈치착지·발끝밀기·스윙 롤 모델 도입**

기존에는 스탠스(접지) 내내 발을 완전히 평평(0도)하게 고정하고, 스윙(공중) 중에만
사인 곡선으로 들어올렸다. 실제 걸음은 이렇게 딱딱하지 않다.

- **스탠스 구간**: `heelStrikeDegrees`(뒤꿈치착지, 기본 +10도=발끝이 살짝 든 채 착지) →
  `pushOffDegrees`(발끝밀기, 기본 -18도=발끝이 눌린 채 지면을 참) 로 **선형 보간**
- **스윙 구간**: `pushOffDegrees`(직전 발끝밀기와 각도가 정확히 이어짐) → 중간에
  `toeLiftDegrees`(지면 안 걸리게 추가로 드는 각도, sin 곡선, 기본 20도) 만큼 더 들었다가
  → `heelStrikeDegrees`(다음 착지와 각도가 정확히 이어짐)로 복귀
- 스탠스↔스윙 경계에서 각도가 끊기지 않도록(sin(0)=sin(180°)=0이라 양끝이 자동으로
  이어짐) 수식을 짰다 — 발이 순간적으로 툭 꺾이는 프레임이 없다
- 새 인스펙터/데모 슬라이더: `HeelStrikeDegrees`, `PushOffDegrees`, `ToeLiftDegrees`(기존
  필드를 프로퍼티로 노출)

**검증 (MCP 플레이모드 + RenderTexture 캡처)**
1. 컴파일 에러 0건
2. 우측 이동 후 정지 캡처 / 좌측 이동 후 정지 캡처 비교 — 몸통의 비대칭 디테일(귀 폭 차이,
   상단 이음선 방향)이 좌우로 정확히 뒤집힘을 확인. 두 캡처 모두 찌그러짐 없이 온전한 형태
3. 보행 중 3프레임 캡처 — 발 각도가 프레임마다 다르게(뒤꿈치착지 시 발끝 듦 / 밀어내기 시
   눌림 / 스윙 중 듦) 자연스럽게 바뀜을 확인

### 절차적 관절 리그 — 머리 좌우 flipX 방향 반전 수정 [완료] (2026-08-08)

사용자 지적: "왼쪽으로 갈 때 오른쪽을 보고, 오른쪽으로 갈 때 왼쪽을 본다" — 바로 직전에
도입한 `ApplyBodyFacing()`의 flip 조건이 반대로 되어 있었다.

**수정**: `ProceduralCharacterRig.cs`의 `ApplyBodyFacing()`에서
`bool flip = facingSign < 0f;` → `bool flip = facingSign > 0f;`로 부호만 뒤집었다.

**검증 (MCP 플레이모드 + RenderTexture 캡처)**: 오른쪽 이동 중 캡처와 왼쪽 이동 중 캡처를
비교해 몸통의 비대칭 디테일(귀 폭 차이·상단 이음선 방향)이 이동 방향에 맞게 뒤집힘을 확인.

### 절차적 관절 리그 — 몸통 기울임(lean)이 이동 방향과 무관했던 버그 수정 [완료] (2026-08-08)

사용자 지적: "오른쪽으로 갈 때는 머리를 오른쪽으로 기울이는데 왼쪽으로 갈 때도 오른쪽으로
기울이니까 어색하다. 왼쪽으로 갈 때는 왼쪽으로 기울여야 한다."

**원인**: `Apply()`의 lean 계산이 `Mathf.Abs(velocity.x)`(항상 양수)를 쓰고
`-lean`(항상 같은 부호)을 적용하고 있었다. 방향 부호가 애초에 계산에 들어가지 않아
좌/우 어느 쪽으로 걷든 몸통이 항상 같은 쪽으로만 기울었다.

**수정**: `Mathf.Abs(velocity.x)` → `velocity.x`(부호 유지), 클램프 범위도
`(0, maxLeanDegrees)` → `(-maxLeanDegrees, maxLeanDegrees)`로 대칭 변경.

**검증 (MCP 플레이모드)**
1. 컴파일 에러 0건
2. `BodyPivot.localEulerAngles.z` 실측: 오른쪽 이동 -10.1도, 왼쪽 이동 +6.9도로 **부호가
   반대**임을 확인(수정 전에는 방향과 무관하게 항상 음수였을 것)
3. 좌/우 이동 캡처 비교 — 몸통이 이동 방향에 맞게 반대로 기울어짐을 육안으로 확인

### 절차적 관절 리그 — 고관절 간격(hipSeparation) 재조정 [완료] (2026-08-08)

사용자 지적: "다리가 너무 몸 앞쪽에 쏠려있는데 적절히 중간 위치로 조절해줘".

**원인 파악**: `hipSeparation`(0.62)이 몸통 로컬 폭(250px÷100ppu=2.5유닛)의 24.8%에
불과했다. 예전에 "0.53 비율 → 0.62"라고 적어둔 계산 근거를 다시 따져보니, 그 계산에 쓰인
"몸통 폭 1.16유닛"이라는 수치 자체가 실제 몸통 폭(2.5)과 맞지 않는 **잘못된 값**이었다
(어느 시점에 어떻게 잘못됐는지는 추적 불가 — 이번에 실측으로 다시 잡았다). 결과적으로
고관절 간격이 목표의 절반 수준이라 다리가 몸통 중앙에 몰려 보였다.

**1차 재계산의 실패**: "고관절 간격 ÷ 몸통 폭 = 0.53"이라는 예전 비율을 그대로 되살려
`0.53 × 2.5 = 1.33`으로 바꿨더니, 다리가 몸통 실루엣 **밖으로 완전히 벗어나** 붕 뜬 것처럼
보였다(캡처로 확인, 사용자에게 보여주지 않고 자체 검증 단계에서 발견해 되돌림).
**레퍼런스 GIF에서 이 비율을 다시 뽑아보니 프레임마다 값이 크게 달랐다**(정지에 가까운
프레임 0.41, 보행 중 프레임 0.64) — 걷는 동작이라 보폭에 따라 다리 벌어짐이 계속
바뀌기 때문에, "고정된 고관절 간격"을 레퍼런스 픽셀에서 그대로 뽑아낼 수 있는 값이
애초에 아니었다(보폭에 따라 벌어지는 정도는 이미 `strideRatio`/`stanceRatio`가 코드로
시뮬레이션하고 있고, `hipSeparation`은 그와 별개로 다리가 "뿌리내리는" 고정 지점이어야 한다).

**최종 조정**: 픽셀 비율 역산을 포기하고 **화면 캡처로 직접 비교하며 대기·보행 자세를
모두 확인**해 `hipSeparation = 0.9`(몸통 폭 대비 약 36%)로 정착시켰다. 다리가 몸통 밖으로
벗어나지 않으면서도 중앙에 몰리지 않는 지점을 눈대중으로 찾았다.
데모 슬라이더의 상한도 1.2 → 2.0으로 넓혀 필요 시 더 조정해볼 수 있게 했다.

**검증 (MCP 플레이모드 + RenderTexture 캡처)**
1. 컴파일 에러 0건
2. 대기 자세 캡처 — 다리가 몸통 아래 좌우로 적절히 퍼져 있고 몸통 밖으로 벗어나지 않음
3. 보행 중 캡처 — 걷는 자세에서도 자연스러운 다리 위치 유지

**교훈**: 걷기 애니메이션 레퍼런스에서 "고정 간격" 성격의 파라미터를 뽑을 때는, 값이
보행 위상에 따라 변하는 게 정상인 항목(보폭 등)과 원래 고정이어야 하는 항목(고관절
뿌리 위치)을 구분해야 한다. 후자를 전자가 섞인 프레임에서 그대로 역산하면 엉뚱한 값이 나온다.

### 절차적 관절 리그 — 고관절 간격 재조정 2차 (0.9 → 0.7) [완료] (2026-08-08)

사용자 지적: "다리 사이 거리가 너무 넓어". 직전에 0.9(몸통 폭 대비 36%)로 늘렸던 게
이번엔 반대로 과했다.

**조정**: `hipSeparation` 0.9 → **0.7**(몸통 폭 대비 약 28%). 대기·보행 자세 모두
화면 캡처로 확인해 다리가 몸통 밖으로 벗어나지 않으면서도 지나치게 넓어 보이지 않는
지점으로 맞췄다.

**검증 (MCP 플레이모드 + RenderTexture 캡처)**: 대기 자세, 보행 자세 각 1장씩 캡처해
균형 잡힌 다리 위치를 확인했다.

**참고**: 0.62(너무 좁음) → 1.33(계산상 목표치였지만 실제로는 몸통 밖으로 벗어남) →
0.9(사용자가 "너무 넓다"고 판단) → 0.7(현재)로 네 차례 조정했다. 레퍼런스 GIF에서
고정 비율을 픽셀로 뽑을 수 없는 값이라(위 항목 참고) 정확한 공식이 없고, 화면을
보면서 눈대중으로 맞추는 값임을 인지하고 있을 것.

### 절차적 관절 리그 — 앞다리 대기 위치를 살짝 뒤로 당김 [완료] (2026-08-08)

사용자 요청: "앞쪽 다리를 아주 약간만 뒤로 땡겨봐".

**구현**: `frontLegPullBack`(기본 0.06, 다리 길이 배수) 신설. 대기 자세의 앞다리(`Leg_Front`,
i==1)에서만 `idleX`를 이 값만큼 추가로 줄여(중심 쪽으로 당겨) 뒤쪽 다리는 그대로 두고
앞다리만 살짝 뒤로 이동시켰다. 데모 슬라이더("앞다리 뒤로 당기기")로도 노출.

**검증**: MCP 플레이모드에서 대기 자세 캡처 — 이전 스크린샷과 비교해 앞다리(오른쪽, 흰색)
발이 무릎 아래로 더 가깝게 당겨졌음을 확인.

### 절차적 관절 리그 — 앞다리 위치 조정 방식 수정 (idle 타겟 → 고관절 자체) [완료] (2026-08-08)

사용자 피드백: "앞쪽 다리 자체를 뒤쪽 다리에 가깝게 조금만 이동시키라는 의미야" — 직전
구현(`frontLegPullBack`을 대기 자세의 발 목표(idleX)에만 적용)은 의도와 달랐다. 그 방식은
**정지 상태에서만** 발 끝 위치를 살짝 당기는 것이라, 걷기 시작하면 효과가 사라지고
다리 뿌리(고관절) 자체는 그대로였다.

**수정**: `frontLegPullBack`을 **앞다리(Leg_Front)의 고관절 위치 자체**에 적용하도록
옮겼다. `Build()`에서 앞다리를 만들 때 `sideX = hipSeparation*0.5f - frontLegPullBack`으로
계산해 다리 뿌리(hip)를 뒤다리 쪽(중심)으로 당긴다. `leg.sideOffsetX`가 매 프레임
`leg.hip.localPosition`과 발 타겟(`target.x += leg.sideOffsetX`) 양쪽에 그대로 쓰이므로,
**대기·보행 구분 없이 다리 전체(뼈·발 모두)가 통째로 이동**한다.
단위도 "다리 길이 배수"에서 `hipSeparation`과 같은 로컬 단위로 바꿨다(뿌리 위치를
조정하는 값이니 뿌리 간격을 정하는 값과 같은 스케일을 쓰는 게 자연스럽다). 기본값 0.1.
뼈 위치가 바뀌므로 `LegScale`/`HipSeparation`과 같은 방식으로 **다음 프레임에 리그를
재조립**하도록 프로퍼티를 고쳤다(값이 바뀔 때만 `rebuildRequested=true`).

**검증 (MCP 플레이모드 + RenderTexture 캡처)**
1. 컴파일 에러 0건
2. 대기 자세 캡처 — 앞다리(오른쪽, 흰색) 고관절이 뒤다리 쪽으로 눈에 띄게 당겨짐
3. 보행 중 캡처 — 걷는 자세에서도 자연스럽게 유지됨(대기 때만 적용되던 이전 버그 해결)

### 절차적 관절 리그 — 보행이 "제자리 내려찍기"처럼 보이던 문제 수정 [완료] (2026-08-08)

사용자 지적: "지금은 약간 발을 제자리에서 위아래로 내려찍는 느낌인데, 발을 확실히
앞으로 내딛는 느낌이 들어야 할 것 같다".

**원인**: 스윙 중 수직 이동(발 드는 높이)이 수평 이동(보폭)에 비해 너무 컸다.
`stepHeightRatio(0.32) × legLength` ÷ `stanceTravel(strideRatio×legLength×stanceRatio)`
= 약 **76%** — 위로 드는 양이 앞으로 나가는 양의 4분의 3에 달해 "앞으로 내딛는다"보다
"제자리에서 들었다 놓는다"는 인상을 줬다.

**수정**: `strideRatio` 0.7 → **1.0**(보폭 확대), `stepHeightRatio` 0.32 → **0.14**(발
드는 높이 축소). 수직/수평 비율이 76% → **약 23%**로 낮아져 지면에 안 걸릴 정도로만
살짝 들면서 수평 이동이 뚜렷하게 드러난다.

**검증 (MCP 플레이모드 + RenderTexture 캡처)**: 보행 중 3프레임을 클로즈업으로 캡처 —
발이 몸통 아래에서 확실히 앞뒤로 벌어지는 것을 확인(이전에는 두 발이 거의 같은 x위치에서
위아래로만 움직이는 것처럼 보였다).

### 절차적 관절 리그 — 실제 인게임 Player에 적용 [완료] (2026-08-08)

사용자 요청: "지금 괜찮은거 같으니 인게임에 이거 적용시켜줘" — 데모 씬에서 다듬어온
`ProceduralCharacterRig`를 실제 플레이 씬(`Ground01.unity`)의 `Player`에 적용했다.

**적용 방식**
- `Player`(SpriteRenderer+Animator로 단일 스프라이트 "Stand_00"을 그리던 기존 방식) 밑에
  새 자식 `ProceduralVisual`을 만들고 여기에 `ProceduralCharacterRig`를 붙였다.
  **기존 `SpriteRenderer`/`Animator`는 삭제하지 않고 `enabled=false`로 비활성화만** 했다
  (되돌리려면 두 컴포넌트를 다시 켜고 `ProceduralVisual`을 지우면 된다).
- `PlayerRobotController.cs`는 **전혀 수정하지 않았다.** `ProceduralCharacterRig.Awake()`가
  `GetComponentInParent<Rigidbody>()`로 `Player`의 기존 `Rigidbody`를 자동으로 찾아
  `linearVelocity`를 매 프레임 읽으므로, 이동·구르기(대시) 코드가 그대로 새 리그를
  구동한다. 기존 `animator.SetBool(...)`/`spriteRenderer.flipX=...` 호출은 비활성화된
  컴포넌트를 대상으로 계속 실행되지만 아무 시각적 효과가 없어 무해하다(죽은 코드지만
  삭제하지 않음 — 되돌리기를 쉽게 하려는 의도적 선택)

**크기 보정 (여러 차례 재측정 필요했음)**
- 데모 씬의 `rigScale`(0.55)을 그대로 썼더니 인게임에서 좀비 대비 **터무니없이 작게**
  나왔다(리그 자체 world bounds 0.688×0.731, 반면 Player의 BoxCollider world 크기는
  1.5×1.5, 기존 스프라이트 "Stand_00"의 world 크기는 4.608×3.072였다)
- 처음엔 BoxCollider(1.5×1.5) 기준으로 맞췄으나 실제 게임 화면(좀비 무리 캡처)과 비교하니
  여전히 작았다 — **콜라이더는 히트박스일 뿐 시각 크기 기준이 아니었다.** 기존 스프라이트의
  world 높이(3.072)를 기준으로 다시 잡아 `ProceduralVisual.localScale = (4.15,4.15,4.15)`로
  최종 결정(리그 world bounds가 old sprite와 비슷한 높이 ~3.0이 되도록 역산)
- 스케일은 `ProceduralCharacterRig`의 `rigScale` 필드가 아니라 **`ProceduralVisual`
  래퍼 오브젝트의 Transform.localScale**에 적용했다 — 컴포넌트 기본값(데모 기준)은
  그대로 두고 인게임 전용 배율만 바깥에서 곱하는 방식이 더 명확하다고 판단

**정렬순서(sortingOrder) 버그 — 다리가 완전히 안 보이던 문제**
- 리그의 기존 정렬 방식은 `bodySortingOrder`(데모 기본값 0) 기준으로 다리를 **-10 ~ +2**
  범위에 배치한다(무릎 공이 정강이를 덮고, 발이 그 위를 덮는 3단 구조 + 뒷다리/앞다리 분리를
  위해). 데모 씬은 배경이 LineRenderer라 이 넓은 음수 범위가 문제되지 않았지만,
  실제 게임에는 **`map`(배경) sortingOrder=0, 좀비 sortingOrder=1**이 있어서 다리가
  전부 배경보다 뒤에 그려져 **완전히 안 보였다**(몸통만 둥둥 떠 있는 것처럼 보임 —
  캡처로 실측 확인 후 원인 파악)
- `bodySortingOrder`를 **13**으로 올려 다리 최저값(13-10=3)이 배경(0)·좀비(1)보다
  확실히 앞에 오도록 했다. 몸통(13)이 기존 무기 정렬순서(4)보다 높아져 무기가 몸통 뒤로
  숨는 문제가 새로 생겨서, **무기 스프라이트(`LeftWp_img`/`RightWp_img`)의 sortingOrder도
  4 → 14로 함께 올렸다**(예전처럼 몸통보다 위에 그려지도록)

**검증 (MCP 플레이모드, 실제 Ground01 씬)**
1. 컴파일 에러 0건, 클린 부팅 시 런타임 에러 0건
2. 좀비 무리와 크기 비교 캡처 — 기존 스프라이트와 비슷한 체감 크기 확인
3. 다리 가시성 근접 캡처 — sortingOrder 수정 전(완전히 안 보임) / 후(정상 표시) 대조 확인
4. 무기 레이어링 확인 — 총이 몸통 위에 정상적으로 그려짐(정렬순서 수정 후)
5. **보행 애니메이션 실측**: 좀비 무리에 둘러싸여 물리 충돌로 속도가 0에 수렴하는 현상을
   먼저 발견했다(버그 아님 — 밀집한 몸 사이에 끼여 못 움직이는 정상적인 물리 결과).
   빈 공간으로 순간이동시켜 재확인한 결과 `rig.CurrentSpeed`가 설정한 속도(3)를 정확히
   반영했고, 걷는 자세도 정상적으로 나타남
6. 씬 저장 후 재확인 — `ProceduralVisual` 스케일(4.15)·`bodySortingOrder`(13)·
   무기 정렬순서(14)·기존 SpriteRenderer/Animator 비활성화 상태가 전부 그대로 유지됨

**남은 과제**
- 팔이 없어 무기가 몸통 옆에 붕 떠 있는 것처럼 보이는 느낌은 여전하다(이전부터 알려진
  한계 — 절차적 관절 리그 예시 항목 참고)
- 구르기(Space) 전용 포즈 없음 — 대시 중에도 일반 보행 애니메이션이 재생된다
- **좀비 무리에 순식간에 둘러싸여 죽는 문제는 이번 작업과 무관한 기존 밸런스 이슈**이며
  이번 검증 중 다시 확인됐을 뿐이다(작업.md 다른 항목에 이미 기록된 사항)

### 무기 소켓 위치·크기 수정 [완료] (2026-08-08)

사용자 지적 두 가지: (1) "무기 위치가 이상해. 로봇 머리 옆 소켓에 위치한게 아닌 이상한
위치로 가있어", (2) "무기의 배율이 너무 작은데, 로봇과 좀비가 크게 설정된건지, 무기가
작게 설정된건지 모르겠네".

**구조 파악**: 무기는 `PlayerShootManager`(별도 `ShootManager` 오브젝트, `Player`가 아님)의
`weapon_slots`에 `rig_point`(=`RigingPoint`, 조준 시 회전하는 피벗)·`muzzle_point`
(=`RightHandWeapon`/`LeftHandWeapon`, 발사 원점)·`hand_sprite_renderer`
(=`RightWp_img`/`LeftWp_img`, 무기 이미지)로 연결되어 있다. `RigingPoint`는 `Player`의
**직계 자식**(리그와는 무관한 별도 하이러키)이라 새 절차적 리그를 붙여도 저절로 따라오지
않는다 — 예전의 거대한 단일 스프라이트("Stand_00", world 4.6×3.1) 기준으로 잡아둔 고정
좌표를 그대로 물려받고 있었다.

**원인 1 (위치)**: `RigingPoint`의 world 위치가 몸통 폭(2.853)의 절반(1.4265)보다도 **작은
값**(1.235/1.16)이라 귀 옆이 아니라 몸통 안쪽 정도의 애매한 위치에, 그것도 몸통 **바닥 쪽**
(world y=0.54~0.64, 몸통 bottom=0.085 바로 위)에 있었다 — "귀 옆"이 아니라 사실상 발밑에
가까운 위치였다.

**원인 2 (크기) — 삽질의 원인**: `RightWp_img`/`LeftWp_img`의 `localScale`을 직접 늘려도
계속 원래 작은 값으로 되돌아갔다. 원인은 `PlayerShootManager.AssignWeaponSlotWeapon()`이
무기 장착 시마다 `slot.hand_sprite_renderer.transform.localScale = Vector3.one *
(normalized_scale * data.ImageScale)`로 **매번 덮어쓰기** 때문이었다(무기 데이터의
`ImageScale` 기반 자동 계산 — 등급/무기별 밸런스 값이라 건드리면 안 된다). 이 사실을
모르고 자식(`_img`)의 스케일을 직접 조정했다가 몇 번이나 원상복구되는 걸 보고서야
`ScaleChainCheck` 스크립트로 실측해 발견했다. **자식이 아니라 게임 로직이 건드리지 않는
부모(`RigingPoint`) 쪽의 스케일을 곱하는 방식으로 우회**했다.

**추가 삽질**: 위치를 고쳤는데도 스크린샷에서 계속 무기가 멀리 떨어져 보여서, 처음엔
"스프라이트 내부에 투명 여백이 커서 pivot과 실제 그림이 어긋난다"는 가설을 세우고
`RightSMG.png`/`LeftLaserPistol.png`를 픽셀 단위로 실측했다 — **틀린 가설이었다**
(실제 여백은 캔버스의 1~5%뿐, 무시할 수준). 진짜 원인은 스케일 덮어쓰기(위)였고, 스케일이
`0.8`이 아니라 실제로는 `0.227`이었던 것 때문에 렌더링된 무기 자체가 작아서 몸통과
비례가 안 맞아 보인 것이었다. **빨간 2D 스프라이트 마커를 `RigingPoint` 위치에 직접
찍어보는 진단**으로 카메라/좌표 자체는 문제없음을 먼저 확인하고, 그 다음 스케일 체인
(`ScaleChainCheck`)을 실측해서야 진짜 원인을 찾았다.

**최종 값** (`RigingPoint` 기준, `Player` 로컬 공간)
- 위치: `(∓2.75, 3.60, 0)` → world 기준 몸통 중심에서 좌우 1.375, 위로 1.80
  (몸통 반폭 1.4265와 거의 같아 귀 바로 옆, 높이는 몸통의 위쪽 60%쯤 = 귀 높이)
- 스케일: `(1.8, 1.8, 1.8)` — 게임 로직이 매기는 무기 자체 스케일(예: 0.227) 위에 곱해져
  실제 표시 크기를 좀비와 비슷한 비중으로 키운다
- `RightWp_img`/`LeftWp_img`(그림) local pos `(∓0.10, -0.05, 0)`,
  `RightHandWeapon`/`LeftHandWeapon`(발사 원점) local pos `(∓0.35, -0.15, 0)` —
  피벗에서 아주 조금만 벌어지도록 축소(예전 값은 −0.85~−1.53 등 훨씬 컸다)

**검증 (MCP 플레이모드, RenderTexture 캡처 + 실측)**
1. 컴파일 에러 0건
2. `ScaleChainCheck`로 실제 world scale/렌더 크기 실측 확인
3. 2D 스프라이트 마커로 좌표계·카메라 캡처 기법 자체는 문제없음을 별도 검증
4. 근접 좀비 스폰 → 무기가 좀비와 비슷한 비중으로 보이고, 소켓이 귀 옆에 붙어 있음을
   캡처로 확인
5. 플레이모드 재진입 → 에디트모드에서 반영한 최종값이 그대로 유지됨을 재확인 후 씬 저장

**참고**: 플레이모드 중에 만든 변경은 종료 시 전부 사라진다(이미 알려진 사실이지만
이번에도 실수로 플레이모드에서 값을 확정한 뒤 저장하려다 다시 에디트모드에서
재적용해야 했다). 무기별 회전(`weapon_imgangle`)·조준 로직은 이번에 건드리지 않았다.

---

## 2026-08-08

### 초반 웨이브 난이도 완화 [완료]

사용자 피드백: "밸런스 지금 너무 빡세서 좀만 쉽게 해줘 처음부터 쎈 좀비가 너무 많음".
원인 분석 결과, 1웨이브부터 좀비(HP 30)와 차저(HP 75, 좀비의 2.5배)가 스폰 후보에
50:50으로 섞여 있었고, 스폰압력(초당 3.75마리, 최대 동시생존 30마리)도 처음부터
그대로 걸려 있어 초반 체감 난이도가 과했다.

**수정 파일**
- `Assets/Scripts/EnemySpawner.cs` — "상위 몬스터 해금 웨이브" 개념 신설.
  기존 `spawnMonsterIds`(항상 스폰 후보)와 별개로 `advancedMonsterIds`(일정 웨이브
  이상부터만 후보에 합류) + `advancedUnlockWave` 필드를 추가했다. `SetCurrentWave(int)`를
  외부(WaveManager)에서 호출해 현재 웨이브를 알려주면 `SpawnLoop`이 매 스폰마다
  `spawn_pool_buffer`(항상후보 + 조건부 상위후보)를 조합해 그 안에서 무작위로 고른다.
- `Assets/Scripts/WaveManager.cs` — `BeginWave()`에서 `ConfigureDifficulty` 호출 직후
  `enemySpawner.SetCurrentWave(wave)`를 추가로 호출.
- `Ground01.unity`의 `EnemyManager` → `EnemySpawner` 컴포넌트 값 조정(MCP로 직접 수정):
  - `spawnMonsterIds = [200001]`(좀비만 항상 스폰), `advancedMonsterIds = [200002]`(차저),
    `advancedUnlockWave = 4` — **차저는 4웨이브부터 등장**
  - `spawnBatchSize` 3→2, `spawnInterval` 0.8→1.0초, `maxAliveEnemies` 30→18
    (웨이브가 오를수록 강해지는 `WaveManager`의 커브 자체는 그대로 유지 - 초기값만 완화)

**검증 (MCP 플레이모드)**
1. `assets-refresh` 후 로그에서 `error CS` 검색 — 컴파일 에러 없음
2. `script-execute`로 `EditorApplication.EnterPlaymode()` 호출 후 실시간 진행
3. 웨이브 1 진행 중(55.3초 시점, 종료 3초 전) `EnemyUnit.Alive`를 리플렉션 없이
   `MonsterId` 프로퍼티로 직접 집계 → `Counts=[200001x5]`, **차저(200002) 0마리** 확인
   (콘솔 로그 "[검증2] Wave=1 Counts=[200001x5] Time=55.3" 그대로 확인)
4. 이후 "웨이브 1 종료" → 정비 진입까지 정상 흐름 확인, 에러 없음
5. `EditorApplication.ExitPlaymode()`로 종료, 씬 저장 완료

**남은 확인사항**: 4웨이브 이후 실제로 차저가 섞여 나오는지, 그리고 좀비/차저 자체
스탯(HP·공격력)이나 웨이브별 스폰압력 곡선(`spawnIntervalDecayPerWave`=0.96,
`maxAliveIncreasePerWave`=4)이 여전히 사용자 체감에 맞는지는 실플레이 후 추가 조정이
필요할 수 있다.

### 배경 타일 경계 흰 격자 수정 [완료]

사용자 피드백: "화면 특정 영역에서 하얀 줄이 생기는데 이것 해결해줘 / 애초에 인게임
화면에 하얀 격자가 왜 생기지?"

**원인 조사**: 에디트모드에서 `Camera.main`을 임시로 `orthographicSize=90`까지 넓혀
RenderTexture로 캡처해보니, 배경(`ground_ruined_city_v2.png`, `map` 오브젝트의
`SpriteRenderer`, `drawMode=Tiled`/`tileMode=Continuous`, 타일 1장 76.8×43.2유닛)의
**타일 경계마다 정확히 격자 모양의 밝은 선**이 나타났다. `wrapMode`를 `Repeat→Clamp`로
바꿔봤지만 변화 없음(픽셀 단위로 비교해도 경계 밝기 차이가 거의 없어, 노이즈 많은 배경
위에 완벽한 직선이 있다는 사실 자체가 눈에 크게 띄는 것이었다). **밉맵(mipmap)을 끄니
격자가 완전히 사라짐**을 확인 — Tiled 스프라이트는 같은 텍스처를 가리키는 사각형(quad)을
반복 배치하는 방식이라, 각 사각형 경계에서 UV가 1.0→0.0으로 급격히 꺾이고 GPU가 이
지점의 화면공간 미분값(ddx/ddy)을 거대하게 계산해 가장 흐린 밉레벨을 선택 → 그 결과가
주변보다 밝은/흐릿한 선으로 보이는 것이었다. 배경은 항상 비슷한 확대 비율로만 보이는
2D 탑다운 배경이라 밉맵이 실질적으로 불필요하다.

**수정 파일**
- `Assets/Resources/ground_ruined_city_v2.png.meta` — `TextureImporter.mipmapEnabled`
  를 `false`로, `wrapMode`를 `Clamp`로 변경(스크립트로 `TextureImporter.SaveAndReimport()`
  호출). 코드 변경 없음, 텍스처 임포트 설정만 수정.

**검증 (MCP, 에디트모드)**
1. 수정 전/후 각각 `orthographicSize=90`으로 넓게 캡처해 격자가 사라진 것을 스크린샷으로
   직접 비교 확인
2. 일반 플레이 줌 화면도 다시 캡처해 화질 저하나 부작용 없음을 확인
3. 컴파일 에러 없음(코드 변경이 없으므로 해당 없음, 확인 차 재확인)

### 부품 상자 드랍 확률 1/5로 하향 [완료]

사용자 요청: "부품 상자 나올 확률 지금의 1/5로 낮춰줘". `PartsCatalog.asset`의
`partBoxDropChance`가 실제로는 1.5%(0.015, 2026-08-07 하향 조정된 값)였으므로 그 1/5인
0.3%(0.003)로 낮췄다.

**수정 파일**
- `Assets/Data/PartsCatalog.asset` — `partBoxDropChance` 0.015 → 0.003 (MCP
  `assets-modify`로 직접 수정)

### 부품 상자 화면 밖 위치 화살표 UI 추가 [완료]

사용자 요청: "부품 상자 나오면 화면 바깥에 있을때 화면 가장자리에서 어디쪽에 있는지
화살표로 위치 표시해주는 UI 추가해줘".

**신규 파일**
- `Assets/Scripts/PartBoxIndicatorUI.cs` — Canvas 밑 화면 전체를 덮는 RectTransform에
  붙이는 컴포넌트. `RewardPickup.AlivePartBoxes`를 순회해 각 상자의 화면 좌표를 계산하고,
  화면(여백 `edgeMargin`=60px 인셋) 밖에 있으면 그 방향으로 화면 가장자리에 화살표를
  클램프해서 띄운다(화면 안에 있으면 필드에 이미 아이콘이 보이므로 화살표 생략). 화살표
  스프라이트는 외부 에셋 없이 코드로 생성한 삼각형(64x64 텍스처, 위쪽을 가리키는 형태)이며,
  최대 8개(`maxIndicators`)까지 동시 표시 가능한 고정 풀로 관리한다. 좌표 변환은
  `RectTransformUtility.ScreenPointToLocalPointInRectangle`을 사용해 Canvas의 실제
  렌더 모드/스케일과 무관하게 정확히 맞춘다.

**수정 파일**
- `Assets/Scripts/RewardPickup.cs` — `AlivePartBoxes`(정적 리스트, `EnemyUnit.Alive`와
  동일한 패턴) 신설. `Init()`에서 타입이 `PartBox`면 등록, `CollectImmediately()`에서
  해제.
- `Assets/Scripts/PlayerRobotController.cs` — `Awake()`의 기존 정적 상태 초기화 목록에
  `RewardPickup.AlivePartBoxes.Clear()` 추가(재시작 시 이전 판 잔존 참조 방지).
- `Ground01.unity` — `Canvas` 밑에 `PartBoxIndicators`(RectTransform, 화면 전체를 덮도록
  anchorMin(0,0)/anchorMax(1,1)) 신설 + `PartBoxIndicatorUI` 부착. `GameFlowManager`의
  `combatHudObjects` 배열에도 추가해 정비 화면에서 다른 전투 HUD와 함께 숨겨지도록 함.

**검증 (MCP 플레이모드)**
1. `assets-refresh` 후 컴파일 에러 없음 확인
2. 플레이모드 진입 후 `RewardPickupManager.SpawnReward(PartBox, 1, playerPos + (40,5,0))`로
   화면 밖(오른쪽 위)에 상자를 강제 스폰
3. Canvas를 임시로 `ScreenSpaceCamera`로 바꿔 RenderTexture로 UI 포함 전체 화면을 캡처
   (문서화된 우회법) → **화면 오른쪽 가장자리에 오른쪽 위를 향해 살짝 기운 주황색
   화살표가 정확히 표시됨**을 스크린샷으로 확인
4. `EditorApplication.ExitPlaymode()`로 종료

### 배경 타일 이음매 잔존 문제 재수정 — 타일링 자체를 중단 [완료]

사용자가 흰 줄 수정 후 스크린샷으로 "가로 하얀 선이 아직 있다"고 재지적. 밉맵을 끈 것만으로는
근본 원인이 다 해결되지 않았음을 확인하고 추가 조사·수정했다.

**추가 원인**: 밉맵을 꺼도 남아있던 얇은 선은, 배경 원본 이미지(`ground_ruined_city_v2.png`)
자체가 상하좌우 끝단 색이 서로 정확히 안 맞는(진짜 seamless가 아닌) 이미지였기 때문.
`Tiled` 반복 특성상 이 불일치가 매 타일 경계(76.8×43.2유닛 간격)에 그대로 드러난다.

**시도했다가 되돌린 방법**: 텍스처의 상하좌우 가장자리를 코드로 크로스페이드 블렌드
(폭 60px → 폭 320px+smoothstep → 폭 150px+smoothstep 순으로 3회 시도, 매번 git으로
원본 복구 후 재시도). 폭이 좁으면 흐릿한 선이 남고, 넓히면 근처 그림(돌·나뭇가지)이
유령처럼 겹쳐 보이는 부작용이 생겨 어느 쪽도 완전히 만족스럽지 않았다. 사용자에게
`AskUserQuestion`으로 해결 방식(거울 반사 타일 / 타일링 중단 / 약한 블렌드 유지 / 보류)을
직접 물어 **"타일 반복 중단(단순 배경)"** 을 선택받았다.

**최종 수정 파일**
- `Assets/Resources/ground_ruined_city_v2.png.meta` — `maxTextureSize`를 Default/Standalone
  플랫폼 모두 2048→4096으로 상향(반복이 없어져 화질을 최대한 보존하기 위함). 이미지 픽셀
  자체는 원본 그대로(블렌드 실험은 전부 git checkout으로 되돌림)
- `Ground01.unity`의 `map` 오브젝트 — `SpriteRenderer.drawMode`를 `Tiled` → `Simple`로 변경
  (더 이상 반복하지 않고 이미지 1장을 그대로 표시), `Transform.localScale`을
  `(2,2,1)` → `(6.4815, 6.4815, 1)`로 변경해 이미지 1장이 맵 전체(240×140유닛)를
  가로세로 비율 유지한 채 덮도록 함(세로 140을 기준으로 균등 스케일 계산 - 가로가
  240보다 약간(≈9유닛) 남지만 가로세로 비율을 다르게 주면 이미지가 늘어나 보이므로
  이 쪽을 택함, "프로젝트 안내.md"의 "map 스케일은 가로세로 반드시 같게" 규칙을 지켰다)

**검증 (MCP, 에디트모드)**
1. `orthographicSize=90`으로 맵 전체를 캡처 → 이음매 없이 매끄럽고, 반복이 사라져
   더 이상 같은 바위/식물 패턴이 3x3으로 되풀이되지 않음(오히려 더 자연스러워짐)을 확인
2. 이전에 흰 선이 보였던 정확한 좌표(-43.2, 17.4)를 플레이 줌 수준(orthoSize=9)으로
   클로즈업 재캡처 → **선 완전히 사라짐**을 확인
3. 일반 플레이 줌 화면도 재확인 — 화질은 이전 타일 버전보다 다소 부드러워졌으나
   (이미지 1장을 넓게 펴 바르는 방식의 당연한 트레이드오프, 사용자도 선택 시 인지한 내용)
   눈에 띄는 결함은 없음
4. 컴파일 에러 없음(코드 변경 없음)

**참고**: 이제 배경은 반복되지 않는 이미지 1장이므로, 앞으로 맵 크기를 키우거나 플레이
가능 범위를 넓히려면(현재 240×140유닛) 더 큰 원본 이미지가 필요하다(예전 Tiled 방식은
작은 타일을 반복해 어떤 크기든 채울 수 있었지만 지금은 아니다).

### 구르기(Space) 실제 회전 모션 + 구르는 동안 무기 동반·발사 잠금 [완료] (2026-08-09)

사용자 요청 3건: (1) Space가 원래 "빠르게 이동하는 구르기"였음을 재확인, (2) 사람이
앞구르기 하듯 실제로 한 바퀴 도는 모션 추가, (3) 구르는 동안 무기도 머리 위치로 같이
구르고 그동안은 발사/공격이 불가능한 패널티 추가.

**설계**: 절차적 관절 리그(`ProceduralCharacterRig`)의 다리 IK는 구르기 속도(대시:
3.5유닛/0.18초 ≈ 19.4유닛/초)를 감당하도록 설계되지 않았고, 억지로 걷기 로직에
욱여넣으면 다리가 미끄러지듯 이상하게 움직일 위험이 컸다. 그래서 구르는 동안은
**다리를 숨기고 몸통만 통째로 0→360도 회전** 시키는 방식(+ 회전 중간에 살짝 뜨는
아치형 호핑)을 택했다 — 브로테이토 등 톱다운 뱀서라이크에서 구르기/회피를 표현할 때
흔히 쓰는 방식과 같다(레퍼런스: 프로젝트 안내.md "레퍼런스: 브로테이토").

**신규/수정 파일**
- `Assets/Scripts/ProceduralCharacterRig.cs` — `SetRoll(bool active, float spinDegrees)`
  공개 메서드 신설. `active`인 동안 `Update()`가 평소의 속도 기반 보행 로직(다리 IK,
  좌우 반전, gait blend 등)을 전부 건너뛰고 `ApplyRollPose()`만 호출한다: `LegsGroup`을
  비활성화하고, `BodyPivot`의 회전을 `spinDegrees` 그대로(외부에서 매 프레임 갱신),
  위치는 `standHipY + sin(진행률*π) * 다리길이*0.25`(회전 중간에 살짝 떠올랐다 내려오는
  호핑)로 설정한다. `active=false`가 되는 순간 `LegsGroup`을 다시 켜고 평소 로직으로
  복귀한다. 이 컴포넌트는 원래 범용(데모 씬에서도 재사용)이라 `PlayerRobotController`를
  직접 참조하지 않고, 순전히 외부가 넣어주는 `SetRoll()` 값만으로 동작하게 했다.
- `Assets/Scripts/PlayerRobotController.cs` — `DashSpinDegrees`(공개, 0~-360) 프로퍼티
  신설. `UpdateDashTimers()`가 매 프레임 구르기 진행률(0→1)을 `-진행률*360도`로 변환해
  `proceduralRig.SetRoll(true, DashSpinDegrees)`를 호출하고, 구르기가 끝나면
  `SetRoll(false, 0)`으로 되돌린다. `proceduralRig` 필드는 비어있으면
  `GetComponentInChildren`로 자동 탐색(씬에 수동 연결 불필요 — 실제로 씬 수정 없이도
  바로 동작함을 확인). `ReturnToStartPosition()`(정비 진입 시 필드 초기화)에서도 구르기
  상태를 함께 초기화하도록 추가.
- `Assets/Scripts/PlayerShootManager.cs` — `Update()` 맨 앞에서 `player_stats.IsDashing`을
  확인해, 구르는 동안이면 **조준·발사(`UpdateSlot`) 전체를 건너뛰고** 대신
  `ApplyRollPoseToAllSlots()`만 실행한다: 양쪽 무기 소켓(`rig_point` = `RigingPoint`)의
  로컬 위치를 `roll_rig_local_position`(머리 위, 기본값 (0, 4.3, 0))으로 옮기고 회전을
  `player_stats.DashSpinDegrees`와 똑같이 맞춰 몸과 함께 도는 것처럼 보이게 한다. 두
  무기가 완전히 겹쳐 보이지 않도록 원래 좌/우 위치(x 부호)만큼
  `roll_rig_lateral_spread`(기본 0.5)로 살짝 벌린다. 구르기가 끝나는 순간(`was_rolling`
  플래그로 전이 감지) `RestoreRollHomePositions()`로 각 소켓을 `Start()`에서 캐시해둔
  원래 로컬 위치로 되돌린다(회전은 다음 프레임 `UpdateSlot`이 조준/대기 자세로 알아서
  다시 계산하므로 따로 복원하지 않아도 됨). **이 Update() 조기 반환 덕분에 구르는 동안은
  `TryFireSlot`(투사체/빔/근접 스윙 전부 포함)이 물리적으로 호출되지 않는다** —
  요청하신 "빠르게 이동하는 대신 잠깐 공격도 못하는 패널티"가 이렇게 구현됨.

**검증 (MCP 플레이모드)**
1. `assets-refresh` 후 컴파일 에러 없음 확인
2. 구르기 지속시간(0.18초)이 관찰하기엔 너무 짧아, `Time.timeScale`을 일시적으로
   0.002까지 낮추고(MCP 왕복 지연이 수 초~수십 초라 0.05로는 부족했음 - 실측 후 재조정)
   리플렉션으로 `PlayerRobotController.TryStartDash()`를 직접 호출해 구르기를 유발한
   뒤 여러 시점에서 캡처
3. 좀비를 사거리 안에 미리 강제 스폰해둔 채로 구르기 진행 — 진행률 12%(-45도)/53%
   (-192도)/91%(-327도) 세 시점 모두 **다리가 숨겨지고 몸통만 회전, 무기가 머리 위로
   이동해 같은 각도로 회전**하는 것을 스크린샷으로 확인, 세 시점 모두
   `projectiles=0`(발사되지 않음)을 로그로 확인
4. `Time.timeScale`을 1로 되돌려 구르기를 자연 종료 → **다리 복귀, 무기가 원래
   귀 옆 위치로 정확히 복귀, 그 직후 좀비를 향해 실제로 발사(투사체 3개 생성 확인)**
   되는 정상 흐름을 스크린샷·로그로 확인
5. 무기 겹침 완화(좌우 스프레이드) 패치 후 재확인 — 기능적으로 이상 없음(다만 회전
   중 특정 각도에서는 여전히 한쪽 무기가 다른 쪽에 가려 보이는 경우가 있음, 사소한
   연출 디테일이라 추가 조정하지 않음)
6. `EditorApplication.ExitPlaymode()` 후 `Time.timeScale=1` 복구, 씬은 플레이모드 중
   변경분이 아니라 코드만 수정했으므로 별도 씬 저장 불필요(git status로 스크립트
   3개만 변경됨을 확인)

**남은 확인사항**: 구르기 지속시간(0.18초)이 실제 배속(1배속) 플레이에서 육안으로
회전이 잘 읽히는지는 정성적 판단이 필요하다 — 너무 빨라 "그냥 반짝하고 이동하는"
느낌이면 `dashDuration`을 살짝 늘리는 조정이 필요할 수 있다(밸런스 값이라 사용자 확인
후 진행 권장).

### 구르기 거리·속도 조정 + 좌우 회전 방향 버그 수정 [완료] (2026-08-09)

사용자 피드백 2건: (1) 구르는 거리를 늘리고 속도는 약간만 줄여달라, (2) 왼쪽으로 구를 때도
항상 오른쪽으로 도는 것처럼 보이는 버그(방향 무관하게 시계방향 고정이었음) 수정.

**수정 파일**
- `Assets/Scripts/PlayerRobotController.cs`
  - `dashDistance` 3.5 → **5.0**(+43%), `dashDuration` 0.18 → **0.28초**(코드 기본값과
    씬의 실제 값 둘 다 수정). 결과 구르기 속도 = 19.4 → **17.9유닛/초(약 -8%, "약간만"
    감소)**, 지속시간이 늘어난 만큼 회전 모션도 더 잘 보이게 되는 부수 효과가 있다(직전
    항목의 "남은 확인사항"이 자연스럽게 해소됨).
  - **회전 방향 버그 수정**: `UpdateDashTimers()`가 `DashSpinDegrees`를 항상
    `-progress*360`(고정 시계방향)으로 계산하던 것을, `dashDirection.x`의 부호를 보고
    **오른쪽으로 구르면 시계방향(-), 왼쪽으로 구르면 반시계방향(+)** 으로 바뀌도록 고쳤다
    (`spinSign = dashDirection.x < -0.0001f ? 1f : -1f`, 순수 수직 이동은 기존처럼
    시계방향을 기본값으로 유지). `PlayerShootManager`의 무기 회전은 이 값을 그대로
    읽으므로 별도 수정 없이 함께 고쳐졌다.

**검증 (MCP 플레이모드)**
1. `assets-refresh` 후 컴파일 에러 없음 확인
2. `Time.timeScale=0.003`으로 늦추고, 리플렉션으로 `lastMoveDirection`을 `Vector3.left`로
   설정한 뒤 `TryStartDash()` 직접 호출 → `dashDirection=(-1,0,0)`,
   `DashSpinDegrees=+42.2도`(반시계 방향) 확인, 스크린샷에서도 몸이 반시계 방향으로
   기운 것을 확인
3. 같은 방식으로 `lastMoveDirection`을 `Vector3.right`로 바꿔 재시험(`dashCooldownLeft`도
   리플렉션으로 0으로 만들어 쿨다운 우회) → `dashDirection=(1,0,0)`,
   `DashSpinDegrees=-42.0도`(시계 방향) 확인 — 방향에 따라 정확히 반대로 회전함을 확인
4. 진행률 대비 이동 거리도 대략 일치함을 좌표 로그로 확인(11.7% 진행 시점에 약 0.55~0.59
   유닛 이동 ≈ 5.0유닛 × 11.7%)
5. `Time.timeScale=1` 복구 후 `EditorApplication.ExitPlaymode()`로 종료. 씬 저장은
   `scene-save`로 완료(`dashDistance`/`dashDuration` 값 자체는 씬의 컴포넌트 값이라
   저장이 필요했음 — 방향 버그 수정은 코드만 바뀌므로 별도 씬 저장 불필요)

### 유닛 충돌 범위 재설계 — 다리 기준 + 중심점 포함 [완료] (2026-08-09)

사용자가 "각 유닛 충돌 범위를 다리 기준 정사각형으로 만들건데 이미지로 예시 들어줘봐"라고
요청해, 코드 수정 전에 `mcp__visualize__show_widget`로 SVG 목업을 3회 반복(정사각형 →
위로 살짝 키운 직사각형 → 중심점을 반드시 포함하고 그 위로 더 나오도록)해 사용자 확인을
받은 뒤 실제로 적용했다.

**조사 결과**: `BoxCollider.size/center`는 프리팹의 로컬 스케일이 곱해져 월드 크기가
정해진다. 좀비(`localScale=0.1342`)·차저(`0.1879`)·보스(`0.4026`)가 전부 **같은 로컬
콜라이더 값**(`center=(0,0,0)`, `size=(8,10,0.2)`)을 공유하고 스케일만 다르므로, 로컬
콜라이더 모양 하나만 고치면 세 프리팹 모두(크기 비례) 자동으로 적용된다는 것을 먼저
확인했다 — **"앞으로 모든 유닛은 크기에 따라 적절히 해당 충돌 범위를 가져야 함"** 요구를
정확히 이 패턴(로컬 콜라이더는 통일, 프리팹 스케일이 알아서 비례)으로 만족시킨다.

**측정**: `Assets/Resources/Enemy_zombie.png`(1254×1254, pivot 정중앙)를 직접 열어
머리/몸통/다리 경계를 픽셀 단위로 실측한 결과, 원점(로컬 0,0)이 캐릭터의 대략적인
수직 중심(허리~아랫가슴 부근)에 위치함을 확인했다. 발밑은 로컬 Y≈-5.5, 머리 위는
Y≈+5.4. 플레이어는 반대로 원점이 이미 발밑(지면) 기준이라, 플레이모드에서
`ProceduralCharacterRig.LegLength`/`standHipY`와 `ProceduralVisual.localScale(4.15)`×
`RigRoot.localScale(0.55)`를 곱해 world 골반높이(1.412)를 역산하고, 몸통 스프라이트의
실제 world bounds(발밑 기준 top=2.938)까지 직접 측정해 반영했다.

**수정 파일**
- `Assets/Prefebs/Zombie.prefab`, `Charger.prefab`, `Boss.prefab`의 `BoxCollider` —
  `center` `(0,0,0)`→`(0,-1.7,0)`, `size` `(8,10,0.2)`→`(8,6.6,0.2)` (셋 다 동일한 로컬
  값, 자기 프리팹의 `localScale`이 곱해져 크기별로 자동 비례). 결과: 발밑(-5.0)부터
  원점(0)을 넘어 위로(+1.6, 아랫가슴 부근까지) 확장 — 예전처럼 머리 위부터 발밑 아래까지
  감싸던 세로로 긴 직사각형에서, 다리~아랫배 정도만 감싸는 형태로 바뀌었다. 폭(x=8)은
  사용자가 요청하지 않아 손대지 않음.
- `Ground01.unity`의 `Player` → `BoxCollider` — `center` `(-0.1,0.4,0)`→`(-0.1,0.85,0)`,
  `size` `(3,3,0.01)`→`(3,1.7,0.01)`. 발밑(0)부터 골반(1.412)보다 살짝 위(1.7)까지.
  기존 3×3은 캐릭터 전체 높이(2.94)보다도 커서 명백히 과대 상태였다.

**참고 — 이 콜라이더의 실제 용도**: `EnemyUnit.OnCollisionEnter/Stay`가
`TryAttackFromCollision(collision.collider)`를 호출해 **물리적으로 겹쳐야 근접 공격이
발동**한다. 즉 이번 수정은 겉보기(비주얼) 정합성뿐 아니라, 좀비의 콜라이더가 머리 위까지
떠 있어서 시각적으로 안 닿아 보이는데도 공격이 발동하던 문제를 실제로 개선하는
게임플레이 변경이기도 하다.

**검증 (MCP)**
1. `assets-get-data`로 세 프리팹의 `localScale`이 서로 다름을 먼저 확인(비례 스케일링
   전제 검증)
2. `Enemy_zombie.png`를 직접 읽어 머리/몸통/다리 픽셀 경계 실측
3. 플레이모드에서 `ProceduralCharacterRig`의 `LegLength`/`standHipY`와 `Body`
   스프라이트의 실제 `SpriteRenderer.bounds`를 리플렉션으로 조회해 플레이어 실측치 확보
4. 네 콜라이더(`Zombie`/`Charger`/`Boss`/`Player`) 수정 후 `assetPath` 기준으로 다시
   읽어 저장이 실제로 반영됐음을 재확인, 씬은 `scene-save`로 저장

**남은 확인사항**: 폭(x)은 이번에 건드리지 않았다 — 좀비의 8유닛 폭은 원래 팔을 벌린
전신 폭 기준이라, 다리만 감싸는 지금 높이(6.6)에 비해 다소 넓을 수 있다(실측 결과
다리 폭만 보면 약 5유닛 정도로 추정). 사용자가 요청하면 폭도 좁히는 후속 조정 가능.

### 좀비/차저 체력 상향 — 일반 등급 무기도 최소 3방 [완료] (2026-08-09)

사용자 요청: "일반 등급 무기, 1웨이브 인데도 모든 적이 한방에 죽으면 안돼. 최소 3방은
맞춰서 죽여야 하니 무기 화력은 놔두고 적 체력 높여봐"

**분석**: 일반 등급 무기 13종의 `weapon_atk`를 전수 조사한 결과 8~150까지 편차가 매우
컸다(`Assets/Scripts/Editor/WeaponTableGenerator.cs`가 생성한 `GameDataAsset.weapons`).
그 중 **순간적으로 한 번에 꽂히는(즉발) 데미지가 가장 큰 무기**가 대물저격총
(`weapon_id 300301`, `weapon_atk=120`)이었다 — 플라즈마캐논(150)은 즉발이 아니라
`BeamProjectile`이 0.2초 tick으로 지속시간에 걸쳐 나눠 때리는 방식(`weapon_duration`에
따라 tick당 데미지가 훨씬 작아짐)이라 "한 방에 죽는다"는 체감과는 달라 기준에서 제외했다.
로봇 기본 공격력(`robot_atk=3`, 기본 로봇 "컴스톡 MK-1")까지 더한 **저격총의 실제
1회 데미지 = 123**을 기준으로, 좀비 체력이 최소 `123×3=369` 이상이면 이 무기조차
3방 안에 죽지 않는다.

**수정 파일**
- `Assets/Data/GameDataAsset.asset`의 `monsters` — 좀비(`200001`) `monster_hp` 30 →
  **370**(저격총 기준 정확히 4방 필요, 여유 있게 3방 조건 만족). 차저(`200002`)는
  기존 좀비 대비 2.5배 비율을 유지해 75 → **925**. `monster_atk`/`monster_def`/
  기타 스탯은 요청대로 손대지 않음(무기 화력도 마찬가지로 무수정).

**검증 (MCP 플레이모드)**
1. 플레이어가 가만히 있으면 자동 스폰되는 좀비 무리에게 금방 죽어(정상 동작 — 의도된
   생존 게임 난이도) 실시간 발사 테스트가 반복 실패함. 스폰을 꺼도 라운드트립 지연 중에
   이미 게임오버가 발생해, 무기 발사 시스템을 거치지 않고 **`EnemyUnit.TakeDamage()`를
   직접 반복 호출**해 격리 검증하는 방식으로 전환
2. 좀비를 강제 스폰하고 저격총 데미지(123)를 5회까지 순차 적용 → 1방(247) → 2방(124)
   → 3방(1, 생존) → **4방(-122, 사망)** 확인. "최소 3방"(3방으로는 안 죽고, 그 이상은
   필요) 조건을 정확히 만족
3. `EditorApplication.ExitPlaymode()`로 종료. 데이터 에셋 변경이라 별도 씬 저장 불필요

**남은 확인사항**: 차저의 2.5배 비율을 그대로 유지했는데, 차저는 애초에 4웨이브부터만
등장하고(`EnemySpawner.advancedUnlockWave`) 그 시점엔 플레이어 화력도 더 올라가 있을
것이므로 비율 유지가 타당한지는 실제 4웨이브 플레이 후 재확인 권장. 약한 무기(예:
기관단총 등)는 상대적으로 훨씬 많은 타수가 필요해지는데, 이는 "무기 화력은 그대로
두라"는 요청에 따른 당연한 결과라 별도 조정하지 않았다.

**참고**: 이 항목의 좀비 체력 370은 바로 다음 항목("DPS 기준 좀비 리밸런싱")에서
360으로 다시 조정됐다(사용자가 "burst 3방" 기준 대신 "DPS 기준 3초" 기준을 새로
지정함). 아래 항목이 최신 값이다.

### DPS 기준 좀비 리밸런싱 + 보상 공식 동반 수정 + 적 체력바 UI 추가 [완료] (2026-08-09)

사용자 요청 2건: (1) "dps 기준으로 좀비 잡는데 무기 하나로 3초는 걸려야 해. 그 기준으로
적 체력 리밸런싱 해봐" — 앞선 항목의 "burst(즉발 한 방) 3방" 기준을 "지속 DPS 3초"
기준으로 교체. (2) "적 체력이 100% 아래면 머리 위에 체력바 띄우게 만들어줘"

**DPS 계산**: 일반 등급 무기 13종 전부의 `weapon_atk × weapon_projectiles + robot_atk`
(로봇 기본 공격력 3)를 `(weapon_duration + 1/weapon_atsp)`로 나눈 실제 DPS를 전수
계산했다(플라즈마캐논은 즉발이 아니라 `BeamProjectile`이 0.2초 tick으로 지속시간에
나눠 때리는 방식이라 이 공식에 자연히 낮게 반영됨). **가장 DPS가 높은 무기는
중기관총(`weapon_id 300101`)으로 초당 120데미지**(9데미지×10발/초 + 로봇공격력
3×10... 정확히는 (9+3)×10=120)였다. `120×3초=360`을 좀비 체력 기준으로 잡았다(burst
기준 370과 우연히 거의 같은 값이 나왔다).

**수정 파일**
- `Assets/Data/GameDataAsset.asset`의 `monsters` — 좀비(`200001`) `monster_hp` 370(직전
  항목 값) → **360**(정확히 30 × 12배). 차저(`200002`)는 2.5배 비율 유지 925 → **900**
  (75 × 12배 — 좀비와 정확히 같은 12배수라 비율이 그대로 보존된다).
- `Assets/Scripts/EnemyUnit.cs` — **체력을 12배 올리면서 놓칠 뻔한 부작용을 함께
  수정**: `GrantKillRewards()`가 `MaxHp / 10`(경험치), `MaxHp / 20`(골드) 임시 공식을
  쓰고 있었는데, 체력만 12배로 올리면 보상도 그대로 12배(예: 좀비 처치 경험치 3 → 36)
  쏟아져나와 레벨업/골드 속도가 의도치 않게 폭증한다. 나눗셈 상수를 `ExpPerMaxHp=120`,
  `GoldPerMaxHp=240`(기존 10/20의 정확히 12배)으로 같이 올려서, **체력을 올리기 전과
  동일한 보상 수치가 그대로 나오도록** 맞췄다(좀비 경험치 3, 골드 1 그대로 유지 —
  직접 계산해 확인함). 코드 주석에 "체력을 다시 조정하면 이 상수도 같은 배율로 맞출
  것"이라고 남겨뒀다.
- `Assets/Scripts/EnemyUnit.cs` — **체력바 UI 신규**: Canvas 없이 `SpriteRenderer` 두 장
  (배경 어두운 회색 + 채움)만으로 만든 월드 스페이스 체력바를 `BuildHealthBar()`가
  `Awake()`에서 생성한다(플레이어의 `RewardPickupManager` 등과 같은 "코드로 스프라이트
  직접 생성" 패턴). 폭·두께·머리 위 여백을 **본인 `SpriteRenderer.bounds`(월드 스페이스)
  에서 계산**하므로 좀비/차저/보스처럼 스케일이 크게 달라도(0.13배~0.40배) 몸집에 맞는
  바가 자동으로 나온다(체력바 자체가 "크기에 따라 적절히" 원칙을 그대로 따름 — 직전
  콜라이더 작업과 같은 철학). `UpdateHealthBar()`가 `TakeDamage()`와 `Init()`에서
  호출되어 **체력 100%(`CurrentHp == MaxHp`)일 때는 숨기고, 100% 미만이 되는 순간부터만
  보이게** 한다. 채움 바는 `Color.Lerp(빨강, 초록, 비율)`로 체력에 따라 색이 바뀐다.
  `BossUnit`은 `EnemyUnit.Awake()`를 `base.Awake()`로 호출하므로 별도 수정 없이 체력바를
  동일하게 상속받는다.

**버그 발견 및 수정 (좌표계)**: 첫 구현에서 `SpriteRenderer.bounds`(월드 스페이스 값)를
`health_bar_root.localPosition`/`localScale`에 그대로 넣었더니, 부모(적 본체) 트랜스폼의
스케일이 다시 곱해져 **스케일이 이중으로 적용**되는 버그가 있었다 — 좀비(스케일 0.13배)
기준 체력바가 훨씬 작고 얼굴 높이쯤(머리 위가 아니라)에 그려졌다. `transform.lossyScale`로
한 번 나눠서(월드 값을 로컬 값으로 되돌림) 해결했다. 채움 바가 왼쪽은 고정된 채 오른쪽만
줄어들도록, 배경과는 별도로 **pivot이 왼쪽(0, 0.5)인 전용 스프라이트**를 새로 만들어 썼다
(가운데 pivot이면 양쪽이 다 줄어들어 부자연스럽다).

**검증 (MCP 플레이모드)**
1. `EnemyUnit.TakeDamage()`를 0.1초 간격(중기관총 공격속도)으로 12데미지씩 반복 적용 →
   **정확히 30발, 3.00초 만에 사망** 확인(요청한 "무기 하나로 3초" 조건을 정확히 충족)
2. 좀비를 300데미지 때려 16.7%(빨강 근처), 차저를 300데미지 때려 66.7%(초록 근처)로
   만든 뒤 스크린샷 캡처 → 좌표계 버그 수정 전에는 얼굴 높이에 작은 색 조각으로만
   보이던 것이, 수정 후 **머리 위에 정확히 위치하고 각 유닛 크기에 비례한 폭의 체력바**
   (좀비는 작게, 차저는 크게)로 정상 표시됨을 확인. 색상도 각각 빨강/초록 계열로 정상
   반영
3. 두 번 다 `assets-refresh` 후 컴파일 에러 없음 확인, `EditorApplication.ExitPlaymode()`
   로 종료. 데이터 에셋 + 코드 변경이라 씬 저장은 불필요

### 좀비 체력을 다시 절반으로 [완료] (2026-08-09)

사용자 요청: "그 절반으로 줄여". DPS 기준(무기 하나로 3초)과 체력 값(360)은 DPS가
고정인 이상 수학적으로 같은 값이므로("3초 절반=1.5초"든 "체력 360 절반=180"이든
결과가 같다), 별도 확인 없이 바로 절반으로 적용했다.

**수정 파일**
- `Assets/Data/GameDataAsset.asset`의 `monsters` — 좀비 `monster_hp` 360→**180**
  (중기관총 DPS 120 기준 정확히 1.5초), 차저는 2.5배 비율 유지 900→**450**.
- `Assets/Scripts/EnemyUnit.cs` — 보상 나눗셈 상수를 체력 배율(6배, `30×6=180`)에 맞춰
  `ExpPerMaxHp` 120→**60**, `GoldPerMaxHp` 240→**120**로 같이 낮춰서 경험치·골드
  보상량이 리밸런싱 전과 동일하게 유지되도록 함.

**검증 (MCP 플레이모드)**: `EnemyUnit.TakeDamage()`를 중기관총 페이스(0.1초 간격, 12뎀)로
반복 적용 → **정확히 15발, 1.50초 만에 사망**(직전 3.00초의 정확히 절반) 확인. 보상
계산식(`MaxHp/60`, `MaxHp/120`)도 좀비 기준 exp=3·gold=1로 리밸런싱 이전과 동일한 값이
나옴을 확인. `assets-refresh` 컴파일 에러 없음, 데이터+코드 변경이라 씬 저장 불필요.

---

## 2026-08-10

### 화면 밖 부품 상자 추적 화살표가 안 보이던 문제 수정 [완료]

사용자 지적: "카메라 밖의 상자를 트래킹 해주는 시스템이 제대로 작동하지 않음".

**원인 진단 (MCP 플레이모드 실측)** — 두 가지가 겹쳐 있었고, 둘 다 "로직은 맞는데 눈에
안 보이는" 종류였다.

1. **절대 픽셀 크기 + ConstantPixelSize Canvas** (주된 원인). 이 프로젝트의 CanvasScaler는
   `ConstantPixelSize`라 UI가 해상도에 따라 자동으로 커지지 않는데, 화살표 크기(36px)와
   화면 가장자리 여백(60px)이 절대 픽셀로 박혀 있었다. 실측 결과 Game View가
   **3840x2160**이라 화살표는 화면 폭의 **0.9%짜리 점**, 여백은 1.5%라 화면 맨 끝에
   붙었다. 플레이모드에서 상자를 화면 밖에 강제 생성해 확인한 결과 **화살표는 정상적으로
   활성화되고 위치·각도도 정확했다** (상자 2개 → 화살표 2개, 오른쪽 상자는 회전 270도,
   좌상단 상자는 51도로 모두 정확). 즉 로직 버그가 아니라 크기 버그였다.
2. **레이어 불일치**. `Canvas`는 UI 레이어(5)인데 씬의 `PartBoxIndicators` 오브젝트와
   코드로 생성하는 화살표 오브젝트는 전부 기본 레이어(0)였다. 코드로 UI를 만들 때
   `new GameObject(...)`는 레이어 0으로 생성되므로 부모 레이어를 명시적으로 물려줘야 한다.

**진단 과정에서 겪은 삽질(기록용)**: 처음 플레이모드 진단에서 `rect=null` / `pool.Count=0`이
나와 "Awake가 아예 안 돈다"고 잘못 결론냈는데, 그 사이에 **플레이모드가 종료돼 에디트
모드에서 진단이 돌고 있었던 것**이었다(에디트 모드에서는 당연히 Awake가 안 돈다).
`EditorApplication.isPlaying`을 매 스크립트 첫 줄에서 확인하도록 바꾼 뒤 재검증하니
Awake는 정상이었다. **앞으로 MCP로 플레이모드를 검증할 때는 모든 진단 스크립트가
`isPlaying`을 먼저 찍고 시작할 것.**

**수정 파일**
- `Assets/Scripts/PartBoxIndicatorUI.cs` — 전면 개편
  - `referenceScreenHeight`(기본 1080) 신설. 크기·여백을 `Screen.height / referenceScreenHeight`
    배율로 계산해 **해상도 비례**로 만들었다. 4K에서는 자동으로 2배가 된다.
  - 크기 상향: `arrowSize` 36 → **72**, `edgeMargin` 60 → **90** (1080 기준. 4K에서는
    각각 144px·180px). 화면 폭 대비 0.9% → **3.8%**.
  - **부품 상자 아이콘 추가** (`iconSize`=56, `iconResourceName`="ItemBox" — 필드에 떨어지는
    상자와 같은 이미지). 화살표만으로는 무엇을 가리키는지 알기 어려웠다. 표시 하나가
    `root`(위치) → `Arrow`(방향으로 회전) + `Icon`(항상 똑바로) 3단 구조로 바뀌었다.
  - **화면 밖 판정을 화면 전체 기준으로 교정.** 예전에는 여백 안쪽 사각형을 벗어나면
    화살표를 띄워서, 화면에 멀쩡히 보이는 가장자리 근처 상자에도 화살표가 겹쳐 떴다.
    이제 판정은 화면 전체, 클램프만 여백 안쪽으로 한다.
  - 생성하는 모든 UI 오브젝트가 **Canvas의 레이어를 물려받도록** 수정.
  - 해상도가 바뀌면(`Screen` 크기 변화 감지) 크기를 다시 계산한다.
- `Assets/Scripts/RewardPickup.cs` — `ResetStaticCaches()`(정적 목록 비우기)와 `OnDestroy`
  (수령을 거치지 않고 파괴되는 경로에서도 목록에서 제거) 추가. 예전에는 `CollectImmediately()`
  에서만 목록을 지워서, 타이틀→게임 재시작처럼 씬만 바뀌는 경로에서 파괴된 픽업이
  정적 목록에 계속 쌓였다.
- `Assets/Scripts/PlayerRobotController.cs` — `RewardPickup.AlivePartBoxes.Clear()` 직접
  호출을 `RewardPickup.ResetStaticCaches()`로 교체.
- `Ground01.unity` — `PartBoxIndicators`의 레이어를 0 → **5(UI)**, 직렬화 값 갱신
  (`edgeMargin` 90 / `arrowSize` 72 / `iconSize` 56 / `referenceScreenHeight` 1080).
  **씬에 저장된 옛 직렬화 값이 새 C# 기본값을 덮어쓰므로 씬 값도 반드시 같이 고쳐야 한다.**

**검증 (MCP 플레이모드)**
1. 화면 밖 상자 2개 + 화면 정중앙 상자 1개를 생성 → **활성 화살표 정확히 2개**(중앙 상자는
   제외됨), 화살표 크기 112x112px(당시 56 기준), 아이콘 스프라이트 `ItemBox` 96x96px,
   회전각 270도/51도 모두 기대값과 일치
2. 렌더링 대조 실험: (A) 캔버스 직속 런타임 생성 이미지, (B) `PartBoxIndicators` 밑 런타임
   생성 이미지, (C) **실제 화살표 풀 오브젝트**를 각각 크게 키워 RenderTexture로 캡처 →
   셋 다 정상 렌더링되고, (C)는 **삼각형 화살표 + 부품 상자 아이콘**이 함께 그려지는 것을
   눈으로 확인
3. 참고: Screen Space Overlay Canvas를 RenderTexture로 캡처하는 우회법은 **RT 크기를
   `Screen` 크기와 정확히 같게** 해야 한다(다르면 Canvas rect가 RT 크기로 바뀌는데
   화살표 좌표는 `Screen` 기준으로 계산돼 프레임 밖으로 나간다). 이것 때문에 캡처에서
   화살표가 안 보여 한참 헤맸다

### 웨이브 종료 규칙 변경 - 제한시간 끝나면 즉시 종료 [완료]

사용자 지정: "보스 웨이브를 제외한 웨이브는 제한 시간이 끝나면 즉시 종료, 남은 좀비는
아이템 드랍 없이 사라지며, 종료직후 드랍된 아이템은 전부 자석으로 흡수".

기존에는 제한시간이 끝나면 스폰만 멈추고 **남은 적을 전부 처치할 때까지 대기 → 2초 여유**
후에 종료했다(2026-08-06 Phase 7에서 사용자 요청으로 넣었던 규칙). 잔적을 쫓아다니는
시간이 길어져 템포가 늘어져서 이번에 되돌린다.

**수정 파일**
- `Assets/Scripts/WaveManager.cs`
  - `WaveTimer()`에서 잔적 대기 루프와 `clearedGraceSeconds` 대기 루프를 **전부 제거**.
    제한시간이 끝나면 곧바로 `EndWave()`를 호출한다.
  - 더 이상 쓰이지 않는 `clearedGraceSeconds` / `maxClearWaitSeconds` 필드와
    `CountAliveEnemies()` 헬퍼 삭제.
  - **보스 웨이브(마지막 웨이브)는 예외로 그대로 유지** — 시간이 끝나도 보스를 처치할
    때까지 기다렸다가 승리 처리한다(기존 확정 사항: 처치 + 시간 종료 둘 다 필요).
  - `IsClearingRemainingEnemies` → **`IsWaitingForBossDefeat`** 로 이름/의미 변경.
    이제 이 상태가 되는 건 보스 웨이브뿐이다.
- `Assets/Scripts/GameHUD.cs` — 위 이름 변경에 맞춰 HUD 표시를 "잔적 처치" → **"보스 처치"**
  로 교체(일반 웨이브는 이 상태에 들어가지 않으므로 "잔적 처치"라는 문구가 더 이상 맞지 않는다).

**"드랍 없이 사라진다"와 "자석 흡수"는 기존 코드가 이미 정확히 그렇게 동작한다**(이번에
새로 만들 필요가 없었다):
- 남은 적 → `EndWave()`가 부르는 `EnemySpawner.DespawnAllAliveEnemies()`는 `Destroy()`만
  하고 `EnemyUnit.Die()`를 거치지 않는다. `Die()` 안에 드랍 테이블·보상 픽업·부품 상자
  로직이 전부 들어 있으므로, 이 경로로 사라지는 적은 아무것도 남기지 않는다.
- 필드에 이미 떨어져 있던 보상·아이템 → `GameFlowManager.ResetFieldForIntermission()`이
  모든 `RewardPickup`/`ItemPickup`에 `CollectImmediately()`를 호출해 전부 자동 수령한다.

**검증 (MCP 플레이모드)** — 실제 타이머 경로로 검증하기 위해 리플렉션으로 진행 중인
`wave_timer_routine`을 멈추고 1.5초짜리 `WaveTimer()`를 새로 돌렸다.
1. 콘솔에 **"웨이브 1 종료 (제한시간 종료 즉시)"** — 잔적 대기/2초 여유 없이 즉시 종료
2. 종료 직후 상태: **생존 적 0**(전부 소멸), **필드 픽업 0**,
   "정비 진입 - 필드 초기화 (보상 픽업 **6개** 자동 수령...)" 로그 확인
3. 골드가 뿌려둔 픽업만큼 정확히 증가(+200), 부품 상자 +2, `AlivePartBoxes` 정적 목록 0
4. `GameFlowManager.CurrentState=Intermission`, `IsIntermission=True`, `Time.timeScale=0`
5. 검증 중 발견: 플레이어가 이미 죽어 `GameOverManager.IsGameOver=true`면 `WaveTimer`의
   경과 시간이 아예 안 흐른다(`RemainingSeconds`가 그대로 멈춤). **설계상 의도된 동작**이며,
   앞으로 웨이브 타이머를 검증할 때는 게임오버 상태를 먼저 해제해야 한다

### 좀비 체력을 다시 절반으로 (난이도 완화) [완료]

사용자 지정: "난이도 전체적으로 더 낮춰야 하니 좀비 체력 1/2로 줄여".

**수정 파일**
- `Assets/Data/GameDataAsset.asset` — 좀비 `monster_hp` 180 → **90**, 차저는 2.5배 비율
  유지 450 → **225**.
- `Assets/Scripts/EnemyUnit.cs` — 보상 나눗셈 상수를 같은 배율(1/2)로 조정.
  `ExpPerMaxHp` 60 → **30**, `GoldPerMaxHp` 120 → **60**. 이걸 같이 안 고치면 체력만
  절반이 된 것으로 경험치·골드도 절반이 되어 레벨업/골드 획득 속도가 의도치 않게 느려진다.
  (원래 값 10/20 대비 최종 3배)

**검증**: 플레이모드에서 `GameDataManager.Instance.Monsters` 조회 → 좀비 hp=90 / 차저 hp=225
확인. 중기관총 DPS 120 기준 좀비 처치 시간은 1.5초 → **0.75초**.

### 배경을 원본 스케일로 축소 + 맵 경계 신설 [완료]

사용자 지적: "배경이 지금 확대되어 있는거 같은데 원본 비율로 줄이고 맵 크기도 그에 맞춰".

**실측한 원인**: 배경 스프라이트는 3840x2160 / PPU 100이라 **원본 월드 크기가 38.4 x 21.6
유닛**인데, `map` 오브젝트의 스케일이 **6.4815배**로 들어가 있어 실제로는 248.9 x 140
유닛에 펴 발라져 있었다(2026-08-08 타일링을 중단하면서 이미지 1장을 맵 전체에 늘린 결과).
카메라 가시 범위가 30.8 x 17.32 유닛이므로, 텍스처 1픽셀이 화면 약 15픽셀을 덮는
극단적인 확대 상태였다 — 사용자가 본 "확대되어 있다"가 정확한 진단이었다.

**사용자에게 확인받은 사항**: 원본 스케일로 되돌리면 맵이 38.4 x 21.6(화면의 약 1.25배)로
줄어들어 거의 한 화면짜리 아레나가 되고, 게다가 **이 프로젝트에는 맵 경계(벽/이동 제한)가
아예 없어서** 배경 밖 빈 공간으로 걸어나갈 수 있다는 점을 수치와 함께 보고했다. 세 가지
안(원본1배+경계 / 원본1배+경계없음 / 중간3배+경계) 중 **"원본 1배 + 이동 경계 추가"** 를
선택받았다.

**신규 파일**
- `Assets/Scripts/MapBounds.cs` — 맵의 월드 영역을 알려주는 static 유틸리티
  (`PlayerInventory`/`DropTableManager`와 같은 정적 클래스 패턴).
  **맵 크기를 상수로 박지 않고** 배경 오브젝트(`map`)의 `SpriteRenderer.bounds`를 그대로
  읽으므로, 인스펙터에서 배경 스케일이나 이미지를 바꾸면 이동 한계와 카메라 한계가 저절로
  따라온다. `ClampPosition`(여유값 포함)과 `ClampCameraCenter`(맵이 화면보다 작은 축은
  중앙 고정) 두 가지를 제공한다.

**수정 파일**
- `Ground01.unity` — `map` 오브젝트의 `localScale` 6.4814816 → **1**
  (월드 크기 248.9x140 → **38.4x21.6**).
- `Assets/Scripts/PlayerRobotController.cs` — `mapEdgeMargin`(기본 0.8) 신설,
  `FixedUpdate()` 끝에서 `ClampToMap()` 호출. **속도를 미리 자르지 않고 위치를 잘라낸 뒤
  그 축의 속도만 0으로 만든다** — 구르기는 속도를 통째로 덮어쓰기 때문에 속도 쪽에서
  막으면 벽에 붙은 채 계속 밀리고, 위치를 자르면 걷기·구르기 어느 쪽이든 똑같이 막힌다.
  `Awake()`에 `MapBounds.ResetCache()` 추가.
- `Assets/Scripts/CameraFollow.cs` — `clampToMap`(기본 true) 신설. 카메라가 z=0 평면에서
  보여주는 절반 크기를 구해 `MapBounds.ClampCameraCenter()`로 위치를 제한한다.
  **이게 없으면 플레이어가 맵 가장자리로 갔을 때 배경 없는 빈 공간이 화면에 들어온다.**
  플레이어는 맵 끝까지 갈 수 있고 카메라만 멈추므로, 그때 플레이어가 화면 중앙에서
  벗어나 보이는 것은 2D 게임의 정석적인 처리라 의도된 동작이다.
- `Assets/Scripts/EnemySpawner.cs` — `ResolveSpawnCenter()`(Transform 반환)를
  **`ResolveSpawnOrigin()`(Vector3 반환, 카메라 위치 우선)** 으로 교체.
  **카메라 클램프를 넣으면서 생긴 부작용을 막기 위한 필수 수정이다**: 이 계산의 목적은
  "지금 화면에 보이는 사각형의 바깥"인데 기준점이 플레이어였다. 예전에는 카메라가 항상
  플레이어를 정확히 따라다녀서 둘이 같았지만, 이제 플레이어가 맵 가장자리에 가면 화면
  중심과 플레이어가 최대 15유닛까지 어긋난다 — 그대로 두면 플레이어 반대편 스폰 지점이
  화면 안쪽으로 들어와 **적이 화면 한복판에서 튀어나온다**. 인스펙터에서 `spawnCenter`를
  명시적으로 지정한 경우에는 그 의도를 존중해 그대로 쓴다.

**텍스처 해상도에 대한 정정**: `.meta` 파일 상단의 `maxTextureSize: 2048`을 보고 원본이
다운샘플되고 있다고 판단해 사용자에게 4096 상향을 제안했으나, 실제로는 **그 값이 더 이상
쓰이지 않는 레거시 필드**였다. 유효한 설정은 `platformSettings`의 `DefaultTexturePlatform`
= **4096**이며, 플레이모드에서 실측한 결과 텍스처가 이미 **3840x2160 전체 해상도**로
로드되고 있었다. 따라서 텍스처 설정은 **변경하지 않았다**(이미 원하는 상태였고, 레거시
필드만 손대면 무의미한 재임포트만 유발한다).

**검증 (MCP 플레이모드)**
1. `MapBounds.HasBounds=True`, `bounds=(38.40, 21.60)` 확인
2. 플레이어를 (500, 500)으로 강제 이동 후 `FixedUpdate()` 호출 → **(18.40, 10.00)** 으로
   정확히 클램프(맵 반폭 19.2 - 여유 0.8 = 18.4 / 반높이 10.8 - 0.8 = 10.0)
3. 카메라 → **(3.80, 2.14)** 로 클램프(맵 반폭 19.2 - 화면 반폭 15.4 = 3.8 /
   10.8 - 8.66 = 2.14). 계산값과 정확히 일치
4. 게임 화면 캡처(RenderTexture 우회법) → 배경 디테일(균열·잔해·맨홀)이 **원본 스케일의
   선명한 크기**로 표시되고, 플레이어를 맵 오른쪽 끝에 두어도 배경 밖 빈 공간이 화면에
   들어오지 않음을 눈으로 확인

### 부품 상자는 웨이브 종료 시 자석 흡수에서 제외 [완료]

사용자 지정(위 "웨이브 종료 규칙 변경" 직후 추가 요청): "웨이브가 종료 되면 해당 웨이브에서
드랍된 경험치, 골드는 모두 자석으로 빨아들이지만, 상자는 자석으로 못 얻도록 만들어.
상자는 직접 얻는걸로 하는게 좋을듯".

**수정 파일**
- `Assets/Scripts/GameFlowManager.cs` — `ResetFieldForIntermission()`에서 `RewardPickup`을
  종류별로 나눠 처리한다. 골드/경험치는 기존대로 `CollectImmediately()`(자석 흡수),
  **`RewardType.PartBox`는 수령하지 않고 `Destroy()`** 한다. 로그에도 "못 주운 부품 상자
  N개 소멸"을 함께 찍는다.

이 변경으로 **화면 밖 부품 상자 화살표(`PartBoxIndicatorUI`)가 비로소 의미를 갖는다** —
어차피 웨이브 끝에 자동으로 받아진다면 굳이 찾아갈 이유가 없었다.

**검증 (MCP 플레이모드)**: 플레이어에게서 멀리 골드 100 / 경험치 50 / 부품 상자 2개를
뿌려두고 웨이브를 종료시킴 → 로그 "보상 픽업 **2개** 자동 수령 ... 못 주운 부품 상자
**2개 소멸**", 골드 +100 정확히 반영, **부품 상자 보유량 0 유지**, 필드 픽업/`AlivePartBoxes`
모두 0 확인.

### 스프라이트 확대 배율 전수 조사 [완료]

사용자 요청: "로봇, 좀비, 무기 중 원래 이미지 비율에서 원본보다 확대된 부분이 있는지 확인".

각 SpriteRenderer의 **원본 월드 크기**(`sprite.rect / pixelsPerUnit`)와 **실제 표시 크기**
(원본 x `lossyScale`)를 플레이모드에서 전수 실측했다.

| 대상 | 원본 월드 크기 | 배율 | 표시 크기 | 판정 |
|---|---|---|---|---|
| 좀비 | 12.54 x 12.54 | 0.134 | 1.68 x 1.68 | 축소 |
| 차저 | 12.54 x 12.54 | 0.188 | 2.36 x 2.36 | 축소 |
| 보스 | 12.54 x 12.54 | 0.403 | 5.05 x 5.05 | 축소 |
| 무기(SMG) | 10.80 x 9.72 | 0.204 | 2.20 x 1.98 | 축소 |
| 무기(레이저피스톨) | 15.36 x 10.24 | 0.143 | 2.20 x 1.47 | 축소 |
| 로봇 다리(허벅지/정강이/발) | 0.98~1.17 | 0.342 | 0.25~0.46 | 축소 |
| **로봇 몸통(Body)** | **2.50 x 2.50** | **1.141** | **2.85 x 2.85** | **원본보다 확대** |
| 배경(map) | 38.40 x 21.60 | 1.000 | 38.40 x 21.60 | 등배(이번에 수정) |

**결론: 확대된 곳은 로봇 몸통(Body) 한 군데뿐이고 배율은 1.141배(약 14% 확대)다.**
나머지는 전부 축소이므로 화질 손실이 없다.

몸통이 1.141배인 것은 `ProceduralVisual`의 `localScale`(4.15)과 `ProceduralCharacterRig`의
`legScale`(다리 파츠에만 곱해지는 값) 조합에서 나온 결과다. 몸통만 따로 줄이면 레퍼런스
GIF를 픽셀 실측해 맞춰 놓은 **몸통 대비 다리 비율(0.41)이 깨지므로**, 고치려면 리그 전체를
1/1.141 = 0.876배로 줄여야 하고 그러면 캐릭터가 화면에서 14% 작아진다(게임 체감이 바뀐다).
**14% 확대는 화질 열화가 거의 없는 수준이라 이번에는 수정하지 않고 보고만 한다** —
수정 여부는 사용자 판단이 필요하다.

### 피격 시 0.25초 흰색 연출 [완료]

사용자 지정: "각 유닛이 피해를 입으면 0.25초 동안 하얀색이 되게끔 변경".

**`SpriteRenderer.color`로는 흰색을 만들 수 없다.** 색은 텍스처에 곱해지는 값이라 밝아지기만
하고 원래 색상이 그대로 비친다. 흰 실루엣을 만들려면 알파는 유지하고 RGB만 흰색으로
바꿔야 하므로 셰이더가 필요하다.

**신규 파일**
- `Assets/Shaders/SpriteFlash.shader` — Unity 기본 `Sprites/Default`와 렌더링 결과가
  동일하도록 만들고 `_FlashAmount`(0~1) / `_FlashColor`만 추가한 셰이더.
  `c.rgb = lerp(c.rgb, _FlashColor.rgb, _FlashAmount)` 후 기존과 동일하게 premultiply한다.
  **`_FlashAmount`가 0이면 기본 스프라이트와 완전히 동일하게 보이므로**, 유닛에 이 머티리얼을
  항상 물려두고 피격 순간에만 1로 올렸다 되돌리면 된다.
- `Assets/Scripts/HitFlash.cs` — 연출 컴포넌트.
  - 자신과 모든 자식의 SpriteRenderer를 대상으로 삼되, 체력바처럼 코드로 붙인 장식용
    렌더러는 `excludedNames`("HealthBar"/"Background"/"Fill")로 걸러낸다.
  - **머티리얼 인스턴스를 유닛마다 만들지 않는다**(`sr.material`에 접근하면 인스턴스가
    복제되어 드로우콜과 GC가 늘어난다). 공유 머티리얼 1개 + `MaterialPropertyBlock`으로
    렌더러별 값만 덮어쓴다.
  - 플레이어처럼 **런타임에 자식 스프라이트가 생기는**(절차적 리그) 경우를 위해, 대상이
    비어 있으면 첫 피격 때 다시 수집한다.
  - `flashDuration`(0.25초) / `flashColor`(흰색)는 인스펙터에서 조정 가능.

**수정 파일**
- `Assets/Scripts/EnemyUnit.cs` — `Awake()`에서 `HitFlash`를 **자동으로 붙이고**
  (좀비/차저/보스 프리팹 3개를 각각 수정할 필요가 없고, 앞으로 추가될 몬스터도 자동 적용),
  `TakeDamage()`에서 `PlayHitFlash()` 호출. `BuildHealthBar()`보다 뒤에 붙여야 체력바
  렌더러가 대상에서 제외된다.
- `Assets/Scripts/PlayerRobotController.cs` — `Awake()`에서 `HitFlash` 자동 부착,
  `TakeDamage()`에서 호출. `HitFlash.ResetStaticCaches()`를 다른 static 초기화와 함께 호출.

**검증 (MCP 플레이모드)**
1. 셰이더 `Comstock/SpriteFlash`의 `isSupported=True`, `_FlashAmount` 프로퍼티 존재 확인
2. 좀비 `TakeDamage(1)` 직후 `MaterialPropertyBlock`의 `_FlashAmount=1` 확인, 좀비/플레이어
   모두 `HitFlash` 자동 부착 확인
3. **화면 캡처로 눈으로 확인** — 필드의 좀비 절반만 피격시켜 한 화면에 담았더니, 맞은
   좀비는 **완전한 흰색 실루엣**, 안 맞은 좀비는 **평소와 완전히 동일한 색**으로 나왔다.
   플레이어·무기·배경도 모두 정상
4. **중요**: 이 프로젝트의 스프라이트 기본 머티리얼은
   `Universal Render Pipeline/2D/Sprite-Unlit-Default`(URP 2D 렌더러)인데, 이걸 built-in
   계열 셰이더로 교체해도 **렌더링 결과가 달라지지 않음을 위 캡처로 확인**했다(2D 라이트를
   쓰지 않기 때문). 나중에 2D 라이팅을 도입하면 이 셰이더를 URP 2D용으로 다시 짜야 한다

### 좀비 겹침 방지 반경을 몸 크기 기준으로 재설계 [완료]

사용자 지적: "좀비끼리 겹치지 않는 것은 좋은데 지금 충돌반경이 보이는 이미지 이상으로 큼.
겹치지 않게 판정하는 크기는 좀비 몸 크기에서 벗어나지 않아야 함."

**실측한 원인**: `EnemyUnit.SeparationRadius`가 **1.3유닛 고정 상수**였는데, 좀비의 실제
몸(콜라이더)은 월드 기준 **1.07 x 0.89유닛**뿐이었다. 즉 두 좀비의 몸이 전혀 닿지 않은
상태(중심간 1.07~1.3유닛)에서도 서로 밀어내고 있었다. 게다가 상수라서 몸집이 3배인
보스(3.22 x 2.66)에게는 반대로 너무 작았다.
또 밀어내는 힘이 `1 / 거리`라 반경 안에서는 **거리와 무관하게 항상** 밀어냈다.

**수정 파일**
- `Assets/Scripts/EnemyUnit.cs`
  - `SeparationRadius` 상수 제거. 대신 각 유닛이 `Awake()`에서 자기 콜라이더로부터
    `body_radius`를 실측한다(`ComputeBodyRadius()`). 콜라이더가 없으면 스프라이트로 폴백.
  - **extents의 x/y 중 작은 쪽을 쓴다** — 원으로 근사하는 이상 큰 쪽을 쓰면 짧은 축에서
    판정이 몸 밖으로 나가기 때문이다(사용자 요구: "몸 크기에서 벗어나지 않아야 한다").
  - `ComputeSeparation()`을 **침투 깊이 기반**으로 교체. 두 유닛의 반지름 합
    (`body_radius + other.body_radius`)보다 가까울 때만, 파고든 비율(0~1)에 비례해 밀어낸다.
    **몸이 닿기 전에는 서로 전혀 밀지 않는다.**
  - `Physics.OverlapSphere` 검색 반경은 `내 반지름 + 전역 최대 반지름`
    (`max_body_radius`, 스폰 때마다 갱신)으로 잡아 보스처럼 큰 유닛도 놓치지 않는다.
  - `ResetStaticCaches()`가 `max_body_radius`도 초기화한다.

**결과 (플레이모드 실측)**: 좀비 `BodyRadius=0.443` → 두 좀비가 밀어내기 시작하는 중심간
거리 **0.886유닛**(예전 1.300). 좀비 몸(콜라이더) 폭이 1.073이므로 **판정 지름이 몸 크기
안에 완전히 들어온다**. 보스·차저는 같은 코드로 자기 몸 크기에 맞춰 자동 계산되므로,
앞으로 새 몬스터를 추가해도 프리팹 스케일만 맞추면 겹침 판정이 저절로 따라온다.

### 좀비 기획서 Ver04 반영 계획 수립 [진행중] — 착수 전, 사용자 승인 대기

사용자 제공 문서: `20260807_좀비기획서_Ver04_김지수.pdf` (22쪽). 함께 받은 지시:
"좀비, 로봇 등 이미지 크기 자체를 축소시켜서 진행해야 할 수도 있음. 앞으로 모든 이미지는
규격에 벗어나면 축소하는 방식으로 해결해야 할듯."

**이 항목은 계획만 기재한 것이며 코드/에셋은 아직 하나도 건드리지 않았다.**

#### 기획서 핵심 내용

**(1) 좀비 규격 4종 (p.6)** — 이번 "원본 비율" 논의의 정답이 여기 있다.

| 규격 | 충돌 범위(px) | 이미지(px) | 인게임(px) |
|---|---|---|---|
| 소형 | 100 x 80 | 250 x 250 | 125 x 125 |
| 중형 | 140 x 90 | 350 x 350 | 175 x 175 |
| 대형 | 180 x 100 | 450 x 450 | 225 x 225 |
| 초대형 | 300 x 150 | 800 x 800 | 400 x 400 |

**이미지 = 인게임의 정확히 2배**다(4종 모두). 즉 원화를 2배로 그려두고 화면에는 50%로
축소해서 쓰는 구조이며, **어떤 경우에도 확대가 일어나지 않는다** — 사용자가 말한
"규격에 벗어나면 축소"와 정확히 일치한다.

**(2) 좀비 스탯 6종 (p.15~20)**

| 종류 | 규격 | 체력 | 공격력 | 방어력 | 이동속도 | 질량 | 사거리 | 공격속도 | 감지범위 | 공격 방식 |
|---|---|---|---|---|---|---|---|---|---|---|
| 좀비 | 소형 | 100 | 15 | 1 | 2.5 | 50 | 80 | 0.75 | 650 | 팔 휘두르기(근접 타게팅) |
| 스피터 | 소형 | 75 | 20 | 0 | 1.5 | 55 | 300 | 0.8 | 750 | 직선 투사체 발사 |
| 스프린터 | 소형 | 120 | 35 | 5 | 5 | 40 | 80 | 1.5 | 1200 | 몸 낮춰 준비 후 돌진 박치기 |
| 디스럭터 | 중형 | 150 | 100 | 5 | 1.6 | 120 | 120 | 1 | 600 | 큰 피해/근접 시 자폭 광역 |
| 차저 | 대형 | 650 | 85 | 10 | 3 | 250 | 300 | 0.5 | 800 | 3초간 돌진 몸통 박치기 |
| 리더 | 대형 | 600 | 25 | 10 | 2.5 | 200 | 120 | 1.2 | 1200 | 머리 휘둘러 박치기 |

**(3) AI 성격값 4종 (p.10)** — 추격형 / 압박형(예상 이동 방향으로 선회) /
휴식형(정지 → 감지범위 1/10 내 또는 피격 시 추격) / 배회형(자유 배회 → 동일 조건에 추격).
추격 종료 조건은 공통으로 "거리 > 감지범위 x2". 성격값에 따라 **색깔로 구분**(p.7).
추가 컨텐츠로 매복형/차단형(p.11)이 있으나 이번 범위 밖.

**(4) 예외처리 (p.22)** — 피격 시 뒤로 밀려남(**질량이 낮을수록 더 밀려남**), 사망 시
시체조각/체액 흔적, 확률적 아이템 드랍, 리더 사망 시 무리 해제, 리더의 무리가 전멸하면
리더 도주, 주변 좀비가 죽으면 배회/휴식 상태도 추격으로 전환.

**(5) 기타** — 좀비 기본형은 2등신, 실루엣으로 구분, 이동/추격/피격/공격/사망 효과음.

#### 규격 → 월드 유닛 환산 (기존 프로젝트 규칙과 정확히 맞물린다)

이 프로젝트의 기존 규칙은 **"기획 문서 수치 ÷ 50 = 월드 유닛"**이다(무기 사거리에서 이미
쓰고 있다). 이 규칙을 규격 표에 적용하면:

| 규격 | 인게임px ÷ 50 = 월드 크기 | 이미지px / PPU 100 | 일치? |
|---|---|---|---|
| 소형 | 125 / 50 = **2.5** | 250 / 100 = **2.5** | 일치 |
| 중형 | 175 / 50 = **3.5** | 350 / 100 = **3.5** | 일치 |
| 대형 | 225 / 50 = **4.5** | 450 / 100 = **4.5** | 일치 |
| 초대형 | 400 / 50 = **8.0** | 800 / 100 = **8.0** | 일치 |

**네 규격 모두 PPU 100에서 정확히 맞아떨어진다.** 따라서 규칙이 아주 단순해진다:

> **원본 이미지를 규격의 "이미지" 픽셀 크기로 축소 저장하고, PPU=100, Transform 스케일=1.**

이렇게 하면 사용자가 요구한 "축소 확대 없이 원본 비율"이 자동으로 성립한다
(Transform 스케일이 전부 1이 되고, 텍스처는 화면에서 100% 이하로만 그려진다).
충돌 범위도 ÷50 하면 소형 2.0 x 1.6 / 중형 2.8 x 1.8 / 대형 3.6 x 2.0 / 초대형 6.0 x 3.0 유닛이며,
Transform 스케일이 1이므로 **로컬 콜라이더 값 = 월드 값**이 되어 지금처럼 스케일을 역산할
필요가 없어진다.

**해상도 검토**: 4K(3840px)에서 카메라 가로 가시 범위가 30.8유닛이므로 1유닛 = 124.7 화면px.
소형 좀비 2.5유닛 = 312 화면px인데 원본이 250px이라 **4K에서는 약 1.25배 확대**가 된다.
1080p에서는 156 화면px로 원본(250px) 이하라 확대가 없다. 즉 **규격은 1080p 기준으로 설계된
값**이며, 4K를 목표로 하면 규격의 이미지 크기를 2배(500/700/900/1600)로 잡아야 확대가
완전히 사라진다. → **사용자 확인 필요 항목 1**

#### 현재 상태와의 차이 (실측)

| 항목 | 현재 | 기획서 | 변화 |
|---|---|---|---|
| 좀비 원본 이미지 | 1254 x 1254 | 250 x 250 (소형) | **1/5로 축소 필요** |
| 좀비 표시 크기 | 1.68유닛 (scale 0.134) | 2.5유닛 (scale 1) | +49% |
| 좀비 콜라이더 | 1.07 x 0.89 | 2.0 x 1.6 | +87% / +80% |
| 차저 표시 크기 | 2.36유닛 (scale 0.188) | 4.5유닛 (대형) | +91% |
| 좀비 체력 | 90 | 100 | +11% |
| 좀비 공격력 | 15 | 15 | 동일 |
| 좀비 이동속도 | 3.2 | 2.5 | -22% |
| 좀비 종류 | 2종(좀비/차저) | **6종** | 4종 신규 |
| MonsterData 필드 | hp/atk/def/speed/range/type/atsp | + **질량 / 감지범위 / 규격 / AI 성격값** | 4개 신규 |
| 차저 스탯 | hp 225 / atk 10 / spd 2.4 | hp 650 / atk 85 / spd 3 | 전면 교체 |

**주의**: 유닛이 1.5~1.9배 커지는데 맵은 이번에 38.4 x 21.6유닛으로 줄여놓은 상태다.
카메라 가시 범위(30.8 x 17.32)에 소형 좀비가 가로 12마리 정도만 들어간다. 현재 스폰 압력
(최대 동시 생존 18마리, 웨이브당 +4)이 훨씬 빡빡하게 느껴질 것이므로 **밸런스 재조정이
반드시 따라와야 한다.** → **사용자 확인 필요 항목 2**

#### 단계별 계획 (제안)

- **Phase A. 규격 시스템 도입 (기반)**
  - `MonsterSizeClass` enum(소형/중형/대형/초대형) + 규격표를 담는 static 테이블 신설.
    이미지px / 인게임px / 충돌범위px / 월드 환산값을 한 곳에서 관리한다.
  - `MonsterData`에 `monster_mass`(질량), `monster_detect`(감지범위), `monster_size`(규격),
    `monster_ai`(AI 성격값) 4개 필드 추가. `GameDataAsset.asset`도 함께 갱신.
  - 좀비/차저/보스 프리팹의 **Transform 스케일을 1로** 되돌리고 콜라이더를 규격값으로 교체.
  - 원본 이미지를 규격 크기로 축소(아래 "확인 필요" 참고).
  - **에디터 검증 도구** 신설: 모든 유닛 프리팹을 훑어 "스케일이 1이 아니거나, 이미지가
    규격을 벗어나거나, 콜라이더가 규격과 다른" 항목을 콘솔에 나열. 무기 테이블 검증
    도구와 같은 패턴으로, 앞으로 규격 위반을 조용히 놓치지 않게 한다.
- **Phase B. 좀비 6종 데이터/프리팹**
  - 기획서 스탯 6종을 `GameDataAsset`에 입력, 몬스터ID 체계 정리, 프리팹 6개 구성.
  - 전용 아트가 없으므로 색조 구분으로 임시 처리(기획서 p.7 "AI 성격값에 따라 색깔 구분"과
    맞물림).
- **Phase C. 공격 방식 4종 구현**
  - 스피터: 직선 투사체(기존 `Projectile` 재사용 가능).
  - 스프린터/차저: 준비 동작 → 돌진(차저는 3초 지속).
  - 디스럭터: 자폭 광역(기존 `BossUnit`의 장판 패턴 로직 참고).
  - 리더: 근접 박치기 + 무리 시스템.
- **Phase D. AI 성격값 4종**
  - `EnemyUnit`의 "무조건 플레이어 직진 추격"을 성격값별 상태 머신으로 교체
    (추격/압박/휴식/배회 + 공통 추격 종료 조건 "거리 > 감지범위 x2").
  - 압박형의 "예상 이동 방향" 예측, 휴식/배회형의 각성 조건(감지범위 1/10 또는 피격).
- **Phase E. 예외처리**
  - 넉백을 **질량 반비례**로 교체(현재 `ApplyKnockback`은 질량을 보지 않는다).
  - 주변 좀비 사망 시 배회/휴식 → 추격 전환.
  - 리더 무리 해제/도주.
  - 시체 흔적(바닥 데칼) — 아트 필요.
- **Phase F. 밸런스 재조정 + 검증**
  - 커진 유닛 크기에 맞춘 스폰 압력·맵 크기·무기 사거리 재검토.
  - MCP 플레이모드로 규격/충돌/AI 실측 검증.

효과음(이동/추격/피격/공격/사망)은 오디오 에셋이 없으므로 이번 범위에서 제외한다.

#### 사용자 확인이 필요한 항목

1. **규격 이미지 크기를 그대로(250/350/450/800) 쓸지, 4K 대응으로 2배(500/700/900/1600)로
   쓸지.** 그대로 쓰면 4K에서 소형 좀비가 약 1.25배 확대된다(1080p에서는 확대 없음).
2. **유닛이 1.5~1.9배 커지는 데 따른 맵/스폰 밸런스 재조정 범위.** 맵을 다시 키울지,
   스폰 수를 줄일지, 카메라를 줄지.
3. **원본 이미지를 실제로 축소 저장할지**(되돌리기 어려운 작업이라 원본은 별도 폴더에
   보관 제안), 아니면 원본을 그대로 두고 임포트 설정(PPU)만 규격에 맞출지.
4. **Phase 우선순위** — 전부 진행할지, 규격(Phase A)만 먼저 적용할지.

### 좀비 기획서 Ver04 반영 - 규격/스탯/AI/공격방식/무리 시스템 전면 구현 [완료 - 일부 보류 항목 있음]

`20260807_좀비기획서_Ver04_김지수.pdf`(22쪽)를 전체 분석해 세운 Phase A~F 계획(직전 항목 참고)을
전부 구현했다. 사용자 확정 사항: (1) 규격 이미지 크기는 기획서 그대로(250/350/450/800, 4K
대응 2배 미적용) (2) 맵/카메라가 기획 의도(FHD)에서 벗어나는지 검증 (3) 원본 이미지를 백업
후 실제로 축소 저장 (4) 전부 진행.

#### 사전 검증: 맵/카메라가 FHD 의도와 일치하는지

- **Player Settings 기본 해상도가 1024x768(4:3)로 되어 있어 카메라가 가정하는 16:9와
  어긋나 있었다** - `ProjectSettings.asset`의 `defaultScreenWidth/Height`를
  **1920x1080(FHD)** 으로 수정.
- **맵 크기는 이미 정확히 FHD와 일치했다** - 이번 세션 앞서 배경을 원본 스케일(1배)로
  되돌리며 얻은 맵 크기 38.4 x 21.6유닛에 기존 규칙(기획 수치 ÷ 50)을 거꾸로 적용하면
  **38.4×50=1920, 21.6×50=1080 - 정확히 FHD**다. 즉 배경을 원본 스케일로 되돌린 결정이
  기획 의도와 정확히 일치했음을 이번에 재확인했다(우연이 아니라 같은 설계 원칙의 결과).
  카메라 가시 영역(866×1540, 같은 ÷50 환산 기준)은 맵보다 작아 화면을 스크롤하며
  돌아다니는 구조이고, 이는 앞서 만든 `MapBounds`/`CameraFollow.clampToMap`과 그대로 맞물린다.

#### Phase A: 규격 시스템

- `Assets/Scripts/MonsterSizeClass.cs`(신규) - 규격 4단계(소형/중형/대형/초대형) enum +
  `MonsterSizeSpec` 조회 테이블. 기획서 "이미지 = 인게임의 정확히 2배"와 이 프로젝트의 기존
  ÷50 규칙을 대조한 결과, **네 규격 전부 PPU=100에서 정확히 일치**한다는 것을 확인했다
  (소형 250/100=2.5=125/50, 중형 350/100=3.5=175/50, 대형 450/100=4.5=225/50, 초대형
  800/100=8.0=400/50). 그래서 "원화를 규격 픽셀 크기로 물리적으로 축소하고 PPU=100,
  Transform 스케일=1"이라는 아주 단순한 규칙 하나로 충족된다.
- **원본 백업 + 실제 축소**: `Enemy_zombie.png`(1254x1254)를
  `ArtBackup_ZombieOriginals/Enemy_zombie_original_1254x1254.png`(프로젝트 루트, Assets
  밖 - Unity가 임포트하지 않음)로 백업한 뒤, Pillow(LANCZOS)로 4개 규격 파일을 새로 생성:
  `Enemy_zombie_S.png`(250) / `_M.png`(350) / `_L.png`(450) / `_XL.png`(800, 이번엔
  6종 어디에도 쓰지 않지만 규격표 완결성을 위해 함께 만들어둠). 전부 PPU=100, pivot 중앙,
  `Sprite Single` 모드로 임포트 설정.
- `GameDataModels.cs`의 `MonsterData`에 4개 필드 신규: `monster_mass`(질량)/
  `monster_detect`(감지범위, ÷50 환산)/`monster_size`(MonsterSizeClass)/
  `monster_ai`(MonsterAiType). 하위 호환용 `EffectiveMass`/`EffectiveDetectRange`
  프로퍼티도 함께 추가(미설정 시 각각 "기준 질량"/"무제한 감지"로 폴백 - 보스처럼 시트에
  없는 임시 데이터가 깨지지 않는다).

#### Phase B: 좀비 6종 데이터

`Assets/Data/GameDataAsset.asset`의 `monsters` 목록을 기획서 p.15~20 스탯으로 전면 교체
(체력/공격력/방어력/이동속도/공격속도는 기획서 값 그대로, 사거리·감지범위만 ÷50 환산):

| ID | 이름 | HP | ATK | DEF | 속도 | 사거리(월드) | 감지(월드) | 질량 | 규격 | AI |
|---|---|---|---|---|---|---|---|---|---|---|
| 200001 | 좀비 | 100 | 15 | 1 | 2.5 | 1.6 | 13.0 | 50 | 소형 | 추격형 |
| 200002 | 차저 | 650 | 85 | 10 | 3 | 6.0 | 16.0 | 250 | 대형 | 추격형 |
| 200003 | 스피터 | 75 | 20 | 0 | 1.5 | 6.0 | 15.0 | 55 | 소형 | 추격형 |
| 200004 | 스프린터 | 120 | 35 | 5 | 5 | 1.6 | 24.0 | 40 | 소형 | 압박형 |
| 200005 | 디스럭터 | 150 | 100 | 5 | 1.6 | 2.4 | 12.0 | 120 | 중형 | 배회형 |
| 200006 | 리더 | 600 | 25 | 10 | 2.5 | 2.4 | 24.0 | 200 | 대형 | 휴식형 |

**AI 성격값 배정은 기획서에 명시돼 있지 않아 임의로 정한 가정이다**(기획서 p.10은 성격값
4종을 정의만 하고 어떤 몬스터가 어떤 성격인지는 지정하지 않음) - 좀비/차저는 단순 추격형,
스피터는 원거리라도 AI 자체는 추격형(공격 방식과 무관), 스프린터는 사냥꾼답게 예측 추적
(압박형), 디스럭터는 함정형 트랩 몹이라 배회하다 접근 시 자폭(배회형), 리더는 우두머리답게
가만히 있다가 각성(휴식형)으로 배정했다. **사용자 확인 후 조정 가능.**

#### Phase B: 프리팹

`EnemyUnit`/`BossUnit`은 원래부터 프리팹에 컴포넌트를 미리 넣지 않고
`EnemySpawner.SpawnMonster()`/`WaveManager.SpawnBoss()`가 스폰 시점에 `AddComponent`하는
방식이었다(발견 후 그대로 활용) - 그래서 프리팹은 전부 Transform+SpriteRenderer+
BoxCollider+Rigidbody만 있는 셸이고, 행동 컴포넌트는 몬스터ID→타입 매핑으로 결정된다.

- `Zombie.prefab`/`Charger.prefab` 갱신: **Transform 스케일을 1로**, 스프라이트를 규격별
  축소 이미지로, BoxCollider를 규격의 충돌 범위(÷50)로 교체. 콜라이더 Y 오프셋은
  "스프라이트 맨 아래에 콜라이더 아래쪽을 맞추고 그 높이만큼 위로"라는 공식
  (`center_y = -스프라이트절반높이 + 콜라이더절반높이`)으로 통일 - 기존의 "다리~아랫배만
  감싼다" 원칙과 같은 결과를 규격표 기준으로 재현한다.
- `Spitter.prefab`/`Sprinter.prefab`/`Disruptor.prefab`/`Leader.prefab` 신규 생성(Zombie를
  뼈대로 복제 - `PrefabUtility.LoadPrefabContents`+`SaveAsPrefabAsset`). 규격에 맞는
  스프라이트/콜라이더 + 성격 구분용 색조(보라=원거리, 노랑=질주, 빨강=자폭, 진보라=우두머리 -
  기획서 p.7 "AI 성격값에 따라 색깔 구분"에 대응하되 실제로는 몬스터 종류별로 나눴다.
  전용 아트가 없어 기존 관례대로 좀비 스프라이트 재활용+색조 변경).
- `EnemySpawner.cs`에 `MonsterComponentTypes`(몬스터ID→행동 컴포넌트 타입) 딕셔너리 신설,
  `advancedMonsterIds`/`advancedUnlockWave`(단일 임계값)를 **`unlockSchedule`**(몬스터별
  개별 해금 웨이브 목록)로 일반화. 기본값(임의 배정, 확인 필요): 스피터 3웨이브 / 차저
  4웨이브(기존 유지) / 스프린터 5웨이브 / 디스럭터 8웨이브 / 리더 10웨이브.
  씬의 `EnemyManager`→`EnemySpawner`의 `monsterPrefabs`에 4개 신규 프리팹 참조를 추가.

#### Phase C: 공격 방식 4종 + 공통 "공격 모션" 구조

**사용자 추가 지정(작업 중간)**: "각 몬스터는 공격 모션으로 공격하고, 충돌한다고 해서
무작정 플레이어의 체력이 닳아서는 안됨". `EnemyUnit.TryAttack()`을 통째로 재설계:

- 사거리 안에 들어오면 곧바로 데미지를 주지 않고 **공격 모션(예비 동작, 기본 0.3초 -
  임시값) → 그 시점에 플레이어가 여전히 사거리 안에 있어야 판정**하는 구조로 바꿨다
  (`PerformAttackMotion()` 코루틴). 플레이어가 예비 동작 중 벗어나면 공격이 빗나간다.
  모션 중에는 이동도 멈춘다(`ComputeSeekDirection()`이 `IsAttacking`이면 0 반환).
  전용 애니메이션이 없어 몸 색을 짧게 주황으로 물들이는 것으로 대신했다(`HitFlash`의
  흰색 피격 연출과는 완전히 다른 경로 - `SpriteRenderer.color` 직접 변경이라 서로 간섭하지
  않음). **사용자가 직접 그린 13프레임 공격 모션 스프라이트 시퀀스를 전달했으나, 채팅에
  붙여넣어진 이미지라 파일 경로를 특정할 수 없어 아직 적용하지 못했다 - 파일 경로를
  받으면 이 색상 텔레그래프를 프레임 애니메이션으로 교체할 것.**
- 판정 성공 시 실제로 벌어지는 효과는 `ExecuteAttackEffect()`(protected virtual)로
  분리해, 이 부분만 서브클래스가 교체한다(모션·쿨다운 뼈대는 공통 재사용):
  - **스피터**(`SpitterUnit.cs`) - 직선 투사체 발사. 전용 `EnemyProjectile.cs`(신규,
    플레이어의 `Projectile`과는 반대 방향이라 별도 클래스) 발사, 코드로 생성한 붉은
    원형 스프라이트(전용 아트 없음).
  - **스프린터**(`SprinterUnit.cs`) - 판정 성공 시 플레이어 방향으로 짧게(0.35초, 4배속)
    돌진해 몸으로 부딪혀야 데미지가 들어간다(맞으면 그 자리에서 멈춤).
  - **차저**(`ChargerUnit.cs`) - **3초(기획서 지정값) 동안** 돌진하며, 스프린터와 달리
    맞아도 멈추지 않고 공격속도 간격으로 재히트 가능("계속 부딪히며 미는" 돌진).
  - **디스럭터**(`DisruptorUnit.cs`) - 판정 성공 시 자폭(반경 3유닛 범위 피해) + 사망 처리.
    `TakeDamage()`를 override해 "큰 피해"(최대체력의 30% 이상 - 기획서에 구체적 기준이
    없어 임시값)를 받으면 사거리와 무관하게 즉시 자폭하도록도 처리.
- **리더**(`LeaderUnit.cs`) - 공격 방식은 기본 근접(박치기)이라 별도 구현 없이 EnemyUnit의
  공격 모션을 그대로 쓰고, 무리(팔로워) 관리만 추가(Phase E에서 서술).

#### Phase D: AI 성격값 4종

`EnemyUnit`에 감지/각성 상태 머신 신설(`IsAlerted`/`UpdateAlertState()`):
- 추격형/압박형은 감지범위 전체로 정찰, 휴식형/배회형은 감지범위의 **1/10**만(또는 피격)
  으로 깨어난다(기획서 p.10).
- 한 번 깨어나면 거리가 **감지범위 x2** 이상 벌어져야 다시 대기 상태로 돌아간다(공통 종료
  조건 - 기획서에 각 성격값 항목마다 "이후엔 추격형과 동일" 반복 명시된 것을 이렇게 해석함).
- 이동 방향(`ComputeSeekDirection()`, protected virtual): 대기 상태에선 배회형만 자유
  배회(1.5~3.5초마다 방향 전환)하고 나머지는 정지, 각성 상태에선 압박형만 플레이어의
  예상 이동 방향(속도×0.6초 - 임시값)을 선회 추적하고 나머지는 직진 추격.
- 시트에 monster_detect가 없는 기존/임시 데이터(보스 등)는 `EffectiveDetectRange`가
  무제한으로 폴백해 항상 즉시 추격 - 기존 동작을 깨지 않는다.

#### Phase E: 예외처리 (일부만 적용, 넉백은 사용자 지정으로 보류)

- **주변 좀비 사망 시 배회/휴식 좀비도 각성** - `Die()`에서 `BroadcastDeathAlert()` 호출,
  자신의 감지범위 안(무제한이면 거리 무관 전체)의 미각성 배회/휴식 좀비를 즉시 각성시킨다.
- **리더 무리 해제/전멸 시 도주** - `LeaderUnit`이 팔로워 목록을 들고 매 프레임 전멸
  여부를 확인, 전멸하면 `is_fleeing=true`로 전환해 플레이어 반대 방향으로 이동하고 더
  이상 감지/공격 판정을 하지 않는다("무리 해제"는 리더→팔로워 단방향 참조뿐이라 팔로워
  쪽에 되돌릴 상태가 없음 - 리더가 파괴되면 참조 자체가 사라지므로 별도 처리 불필요).
  `EnemySpawner`에 리더 전용 무리 스폰 로직(`SpawnLeaderPack` - 기본 팔로워 3마리,
  반경 2.5유닛) 신설.
- **질량 반비례 넉백은 사용자 지정으로 이번엔 적용하지 않음** - `MonsterData.EffectiveMass`/
  `EnemyUnit.Mass`/`EnemyUnit.ReferenceMass`(=50, 좀비 기준)는 이미 데이터에 들어와 있어
  나중에 `ApplyKnockback()`에서 `ReferenceMass / Mathf.Max(1f, Mass)` 배율만 곱하면 바로
  켤 수 있다.
- **시체 흔적(체액/조각)** - 전용 아트가 없어 미구현(기존 관례와 동일하게 데모 범위 밖으로 둠).

#### 검증 (MCP 플레이모드)

1. 6종을 전부 강제 스폰해 컴포넌트 타입/스탯/스프라이트/색/스케일(전부 1)/콜라이더/
   AI 타입이 데이터와 정확히 일치함을 실측 확인.
2. 스프린터가 실제 대전 중 돌진 박치기로 플레이어를 처치(우연한 실전 검증) -
   공격 모션→판정→돌진 파이프라인이 실제로 동작함을 확인.
3. 디스럭터가 자력으로 접근해 자폭·소멸(내가 강제하기 전에 이미 스스로 트리거) -
   근접 자폭 트리거가 실전에서 동작함을 확인.
4. 리더+팔로워 1마리를 스폰 후 팔로워를 처치 → **리더가 도주 상태(`is_fleeing=true`)로
   전환되고 실제로 플레이어 반대 방향으로 멀리 이동**함을 확인.
5. 스피터에 `TryAttack()`을 강제 호출해 `IsAttacking=true`(공격 모션 개시) 확인.
6. `assets-refresh` 컴파일 에러 없음(전 구간).

#### 확인/조정이 필요한 항목

1. **AI 성격값 배정**(위 표) - 기획서에 없는 임의 가정. 확인 후 `GameDataAsset.asset`의
   `monster_ai` 값만 바꾸면 즉시 반영된다.
2. **웨이브별 해금 순서**(스피터3/차저4/스프린터5/디스럭터8/리더10) - 임의 배정.
3. **넉백(질량 반비례)** - 사용자 지정으로 보류 중, 필요 시 활성화만 하면 됨.
4. **공격 모션 텔레그래프 시각 연출** - 사용자가 그린 13프레임 스프라이트 시퀀스를
   전달받았으나 채팅 첨부라 파일 경로를 특정할 수 없어 아직 적용 못함(현재는 임시로
   몸 색 주황 펄스). 파일 경로를 받으면 프레임 애니메이션으로 교체 예정.
5. **맵/스폰 밀도 재검토는 이번엔 불필요로 판단** - 몬스터가 커졌지만(소형 1.68→2.5,
   대형 2.36→4.5유닛) 최대 동시 생존(18~) 기준 맵 점유율이 여전히 10% 미만이라 별도
   조정 없이도 붐비지 않는다(실측 계산으로 확인, 실플레이 재검증은 남아있음).

### 좀비 공격 모션 12프레임 적용 완료 (2026-08-10, 기본 좀비 한정)

바로 위 "확인/조정이 필요한 항목 4"에 있던 미해결 항목 - 사용자가 그린 13(실제 12)프레임
공격 모션이 채팅 첨부라 파일 경로를 특정할 수 없어 적용하지 못했던 것을 사용자가 제공한
**Claude Code 세션 export zip**(`session-export-1786345263256.zip`, 다운로드 폴더)에서 원본
데이터를 복구해 해결했다. zip 안의 `transcript.jsonl`에 그 세션의 원본 첨부 이미지가
base64로 그대로 남아있어(라인 939, `attachment.prompt`, 사용자 메시지 "이 공격모션
적용해줘"), Node.js 스크립트로 12장을 PNG로 복원했다(회색조 2등신 좀비, 300x250px,
프레임1=대기 자세 → 프레임6=팔을 최대로 들어올림(예비 동작 정점) → 프레임8=내려치기(타격
순간) → 프레임12=다시 대기 자세로 복귀).

- **에셋**: `Assets/Resources/ZombieAttack/ZombieAttack_01.png` ~ `_12.png`(신규) - 기존
  `Enemy_zombie_S.png`와 동일한 임포트 규칙(Sprite Single, PPU=100, pivot 중앙,
  alphaIsTransparency)으로 맞췄다. 캔버스가 300x250(정사각형 아님 - 팔을 들어올릴 때
  옆으로 튀어나오는 여유 공간 때문)이라 월드 크기는 3.0 x 2.5유닛(좀비 정면 높이 2.5는
  기존 소형 규격과 동일, 폭만 살짝 넓다 - 팔 동작이라 자연스럽다).
- **적용 범위는 기본 좀비(순수 `EnemyUnit`, MonsterId 200001)로 한정했다.** 차저/스피터/
  스프린터/디스럭터/리더/보스는 전부 `EnemyUnit`을 상속하는 서브클래스이고 `PerformAttackMotion()`을
  재정의하지 않아 그대로 재사용하지만, 규격(소형~초대형)마다 스프라이트 픽셀 크기가 달라
  (PPU=100 고정) 좀비 전용 프레임을 그대로 쓰면 공격하는 순간 몸 크기가 좀비 크기로 잠깐
  바뀌어 보이는 문제가 생긴다. `EnemyUnit.PerformAttackMotion()`에서
  `GetType() == typeof(EnemyUnit)`로 판별해 기본 좀비만 새 프레임 애니메이션 경로를 타고,
  나머지는 기존 색상 텔레그래프 + 몸 이동(뒤로 젖혔다 내지르기) 방식을 그대로 유지한다
  (다른 몬스터도 전용 프레임이 생기면 같은 방식으로 확장 가능).
- **`EnemyUnit.cs` 변경 내용**:
  - `original_body_sprite` 필드 신설, `Awake()`에서 `original_body_color`와 함께 캡처.
  - `ZombieAttackFrames`(static, `Resources.LoadAll<Sprite>("ZombieAttack")`를 파일명
    오름차순 정렬해 지연 로딩·캐시)와 `PlayAttackFrames(startIndex, endIndexInclusive, duration)`
    헬퍼(구간 프레임을 duration에 걸쳐 균등 재생, 죽음/게임오버 시 즉시 중단) 신설.
  - `PerformAttackMotion()`을 분기: 기본 좀비는 예비 동작(1~6번 프레임, `attackWindupSeconds`)
    → 타격(7~8번 프레임, `attackStrikeSeconds`, 이 끝에서 기존과 동일하게 판정) → 판정/쿨다운
    → 회복 동작(9~12번 프레임, 신규 `attackRecoverSeconds`=0.15초 - 인스펙터 노출)을 재생하고
    원래 스프라이트로 복귀. **쿨다운(`next_attack_time`)은 기존과 동일하게 타격 직후 시점에
    설정되므로 회복 동작 재생 시간만큼만 `IsAttacking`이 살짝 더 길게 유지된다**(다음 추격
    이동 시작이 0.15초 늦어지는 정도의 미세한 밸런스 변화, 다른 값들과 마찬가지로 임시값).
    위치 이동(뒤로 젖힘/내지르기)은 프레임 애니메이션이 대신하므로 기본 좀비는 더 이상
    물리적으로 움직이지 않는다.
- **검증(MCP 플레이모드)**: 정비/상점 화면 중이라 스폰이 멈춰 있어(`Time.timeScale=0`)
  `EnemySpawner.SpawnMonster(200001, ...)`로 좀비를 직접 스폰하고 테스트 중 플레이어 피해를
  막기 위해 리플렉션으로 해당 개체의 `Atk`만 0으로 낮춰 확인했다. (1) 공격이 시작되자
  `SpriteRenderer.sprite`가 실제로 `ZombieAttack_08`(타격 프레임)로 바뀌는 것을 실시간 폴링으로
  확인. (2) 공격이 끝난 뒤 `Enemy_zombie_S`(원래 스프라이트)로 정확히 복귀하고 `IsAttacking`도
  `false`로 돌아옴을 확인. (3) 전용 임시 카메라로 좀비만 클로즈업 촬영해 프레임6(팔을 들어올린
  예비 동작 정점)이 크기·정렬 문제 없이 정상적으로 그려짐을 육안으로 확인(스프라이트 bounds
  3.0 x 2.5유닛, 발이 뜨거나 잘리지 않음). (4) `assets-refresh` 및 플레이모드 진입 모두 컴파일
  에러 없음. 테스트로 스폰한 좀비는 전부 정리하고 `Time.timeScale`도 원래대로(정비 중이므로 0)
  복구했다.
- **참고(본 작업과 무관하게 관찰된 이슈)**: 검증 도중 에디트 모드에서도(플레이 중이 아닐 때도)
  `Ground01` 씬의 `Player/ProceduralVisual`(`ProceduralCharacterRig`)이 매 프레임
  `NullReferenceException`(`Apply()` 507번 줄, `leg.hip.localPosition` 참조)을 던지는 것을
  발견했다 - 플레이모드를 한 번 종료한 뒤 에디터가 캐시하고 있던 다리 Transform 참조가
  끊긴 것으로 보인다(런타임에 절차적으로 만들어지는 다리 하이어라키가 플레이모드 종료 시
  사라지는데, 컴포넌트가 에디트 모드에서도 계속 `Update()`를 도는 것으로 추정됨 -
  `ExecuteAlways`/`ExecuteInEditMode` 여부 확인 필요). 이번 작업 범위 밖이라 고치지 않았고,
  플레이모드로 다시 들어가면 `Awake()`가 다리를 재생성해 사라지는 것으로 보인다 - 계속
  거슬리면 별도로 다뤄야 한다.

### 로봇 이미지·애니메이션·무기 복구 및 좀비 공격 프레임 경계 강화 (2026-08-10)

사용자 제보: 로봇에 `Resources/Parts`가 아닌 이미지가 보이고, 특정 상황에서 좀비 공격 모션
또는 좀비 이미지가 로봇에 적용되는 것으로 보임. 이어서 기존 절차적 애니메이션과 무기 이미지도
복구해 달라고 요청했다.

**확인한 원인과 상태**

- 디스크의 `Assets/Scenes/Ground01.unity`는 이미 `Player/ProceduralVisual` +
  `ProceduralCharacterRig` 구조였지만, Unity 에디터 메모리에는 이전의 잘못된 `RobotVisual`
  상태가 남아 있었다. 플레이모드를 종료하고 씬을 Single 모드로 다시 열어 디스크 기준 상태를
  복구했다.
- 재로딩 후 로봇은 `Body`, `LegUpper`, `LegLower`, `Foot` 파츠 7개 렌더러를 사용했고,
  기존 단일 `Stand_00` 렌더러/Animator는 설계대로 비활성 상태였다. 좌우 무기 소켓과
  `RightSMG`/`LeftLaserPistol` 런타임 기본 장비 이미지도 정상 표시됐다.
- 좀비 프레임 코드는 주석상 MonsterId 200001 전용이었지만 실제 조건은
  `GetType() == typeof(EnemyUnit)` 하나뿐이었다. 현재 Player에는 `EnemyUnit`이 없어 직접 재현되진
  않았으나, 문서·기획보다 경계가 약하고 잘못된 컴포넌트 배치 시 다른 루트 렌더러를 바꿀 여지가
  있어 방어 조건을 강화했다.

**수정 내용**

- `ProceduralCharacterRig.cs`
  - `Build()` 때 인스펙터의 기존 참조를 신뢰하지 않고 `Resources/Parts/Body`, `LegUpper`,
    `LegLower`, `Foot`을 항상 다시 로드한다.
  - 몸통·양쪽 허벅지·정강이·발 렌더러를 보관하고 매 프레임 기대 파츠와 비교한다. 외부 코드가
    sprite만 잘못 바꾸면 다음 프레임에 원래 파츠로 복구한다. 색·회전·스케일은 건드리지 않아
    피격 플래시, 보행, 좌우 전환, 구르기 애니메이션은 그대로 유지한다.
- `EnemyUnit.cs`
  - 좀비 공격 프레임 재생 조건을 MonsterId 200001 + 순수 `EnemyUnit` + Player 태그 아님 +
    자기 GameObject의 루트 SpriteRenderer + 원본 `Enemy_zombie_S`로 제한했다.
  - 공격 코루틴 정상 종료 및 오브젝트 비활성화 시 원래 색·스프라이트를 공통 복원해 공격
    프레임이 남는 경우도 막았다.
- `PlayerShootManager.cs`
  - 무기 데이터의 이미지 이름이 누락되거나 잘못돼 `Resources.Load`가 실패해도 현재 정상
    무기 이미지를 null로 지우지 않고 유지하도록 했다.

**검증 (Unity MCP 플레이모드)**

1. `assets-refresh` 재컴파일 오류 0건, `git diff --check` 통과.
2. 로봇 몸통에 `ZombieAttack_01`을 강제로 주입한 뒤 다음 프레임에
   `Assets/Resources/Parts/Body.png`로 자동 복구됨을 확인.
3. 외부 속도 3.0을 넣어 `ProceduralCharacterRig.CurrentSpeed=3.00` 및 절차적 보행 리그
   활성 상태를 확인. 격리 캡처에서 파츠 조립 로봇과 좌우 무기가 정상 표시됨을 육안 확인.
4. 활성 로봇 렌더러 전수 검사: 몸통/양쪽 다리 7개가 모두 `Assets/Resources/Parts/` 아래,
   무기는 `RightSMG.png`/`LeftLaserPistol.png`, Player 하위 `EnemyUnit`은 0개.
5. 기본 좀비에서는 강화된 프레임 조건이 true, 동일 개체에 Player 태그를 임시 지정하면 false가
   되어 로봇 경계가 동작함을 확인. 테스트 변경은 플레이모드 종료로 모두 폐기했다.

### 좀비 AI 성격값·감지범위 제거 및 이동 방향 반전 적용 (2026-08-10)

사용자 요청에 따라 좀비의 추격/압박/휴식/배회 성격 구분과 감지·각성 상태를 폐기하고,
6종 모두 스폰 직후부터 거리 제한 없이 현재 플레이어를 향하도록 단순화했다.

**수정 내용**

- `MonsterData`에서 `monster_ai`, `monster_detect`, `EffectiveDetectRange`를 제거하고
  `GameDataAsset.asset`의 6종 직렬화 값도 삭제했다. 더 이상 쓰이지 않는 `MonsterAiType.cs`와
  `.meta`도 제거했다. 무기의 `weapon_detect`는 플레이어 자동사격 범위이므로 그대로 유지했다.
- `EnemyUnit`에서 `AiType`, `DetectRange`, `IsAlerted`, `UpdateAlertState()`, 압박형 예상 위치
  추적, 휴식/배회 대기, 사망 알림 각성 로직을 제거했다. `ComputeSeekDirection()`은 공격 모션
  중이 아닐 때 항상 현재 플레이어 위치 벡터를 반환한다.
- 밀집 방지 분리 힘이 플레이어 반대편으로 작용하는 성분을 제거했다. 따라서 몸이 겹치면
  좌우로 비켜 갈 수는 있지만 뒤로 도망가지는 않는다.
- 차저와 스프린터도 돌진 시작 방향을 고정하지 않고, 돌진 중 매 물리 프레임 현재 플레이어
  방향을 다시 계산한다. 리더의 팔로워 전멸 후 도주는 몬스터 고유 예외 행동으로 유지했다.
- `EnemyUnit.LateUpdate()`에서 최종 `Rigidbody.linearVelocity.x`를 읽어 왼쪽 이동 시
  `SpriteRenderer.flipX=true`, 오른쪽 이동 시 false를 적용한다. 일반 추적뿐 아니라 돌진·도주·
  넉백의 실제 이동 방향까지 반영하며 정지 시에는 마지막 방향을 유지한다.

**검증 (Unity MCP 플레이모드)**

1. `assets-refresh` 후 컴파일 및 콘솔 오류 0건, `git diff --check` 통과.
2. 좀비/차저/스피터/스프린터/디스럭터/리더를 플레이어 좌우에 강제 스폰해 최종 속도와
   플레이어 방향의 내적이 6종 모두 양수(`6/6 toward`)임을 확인.
3. 같은 6종의 `flipX`가 최종 이동속도 X 부호와 모두 일치(`6/6 flip`)함을 확인.
4. 리플렉션 검사로 `MonsterData.monster_ai`/`monster_detect`와
   `EnemyUnit.AiType`/`DetectRange`/`IsAlerted`가 전부 존재하지 않음을 확인.
5. 검증용 개체와 런타임 변경은 플레이모드 종료로 모두 폐기했다.

### 로봇 머리·신발 파츠 교차 적용 방어 강화 (2026-08-10)

사용자 추가 제보: 로봇 머리에 신발 이미지가 적용되는 등 파츠 교차 적용이 여전히 보임.

**진단**

- 에디트모드와 플레이모드에서 실제 참조를 전수 확인한 결과 `Body` 렌더러와 직렬화 필드는
  `Assets/Resources/Parts/Body.png`, 양쪽 발은 `Foot.png`를 정확히 가리키고 있었다.
- 당시 캡처에서는 `SpriteFlash`가 로봇 전체를 완전한 흰색으로 바꾸는 현상도 함께 관찰했지만,
  이후 사용자가 무릎 파츠 및 좀비 이미지가 얼굴·무기·다리에 실제로 들어오는 경우를 추가
  제보했다. 따라서 플래시는 별도 연출일 뿐 이번 파츠 교차 적용의 확정 원인으로 보지 않는다.
- 정상 상태만 확인하는 데 그치지 않고, 파츠 필드와 렌더러 참조 자체가 서로 바뀌는 최악의
  경우도 자동 복구하도록 방어를 강화했다.

**수정 및 검증**

- `ProceduralCharacterRig`가 `Resources/Parts`의 네 원본을 정적 기준으로 캐시하고, 매 프레임
  `bodySprite`·`thighSprite`·`shinSprite`·`footSprite` 필드가 그 기준과 같은지 검사한다.
- 몸통은 `BodyPivot/Body`, 각 다리는 해당 뼈의 `Thigh`·`Shin`·`Foot` 자식에서 렌더러를
  다시 찾아, 렌더러 참조 자체가 다른 파츠로 교차 연결된 경우까지 복구한다.
- 기존 `Update` 검사에 더해 `LateUpdate`에서도 렌더 직전에 매핑을 확정한다. 다른 컴포넌트나
  코루틴이 `Update` 이후 sprite를 바꿔도 잘못된 파츠가 화면에 그려지지 않는다.
- 플레이모드에서 의도적으로 머리의 실제 sprite, `bodySprite` 필드, `bodyRenderer` 필드를 모두
  `Foot`으로 강제 오염시켰다. 다음 프레임에 실제 sprite=`Body`, 필드=`Body`, 렌더러 연결=`Body`
  세 항목이 모두 자동 복구(`repaired=True`)됨을 확인했다. 테스트 변경은 플레이모드 종료로 폐기했다.

### 플레이어 전 파츠·무기 좀비 이미지 침범 재검증 및 복구 강화 (2026-08-10)

사용자 재제보: 시작 직후 머리에 무릎 등 다른 파츠가 보이고, 특정 경우 좀비 이미지가 얼굴·무기·
다리 전체에 들어옴. 이전의 피격 플래시 원인 추정은 사용자 관찰과 일치하지 않아 철회하고 실제
`SpriteRenderer.sprite` 및 에셋 GUID 기준으로 다시 검사했다.

**재검증**

- 실행 중 Player 하위 렌더러를 전수 조사했다. 몸통·양쪽 허벅지·정강이·발은 각각
  `Resources/Parts/Body`, `LegUpper`, `LegLower`, `Foot`, 양손은 `RightSMG`/`LeftLaserPistol`을
  가리켰고 Player 하위 `EnemyUnit`은 0개였다.
- 프로젝트에서 좀비 공격 프레임을 `sprite`에 직접 넣는 곳은 `EnemyUnit.PlayAttackFrames()`의
  자기 루트 렌더러 한 곳뿐이었다. 간헐 증상의 단일 발생 경로는 현재 실행에서 재현되지 않았으므로
  확인되지 않은 원인을 단정하지 않고, 잘못된 이미지가 들어온 경우 카메라 렌더 전 복구하는 경계를
  플레이어의 모든 표시 파츠로 확대했다.

**수정**

- `PlayerShootManager`가 시작 시 씬의 정상 무기 이미지를 슬롯별 기준값으로 잡고, 데이터 로드나
  장착 교체가 성공할 때 기준값을 갱신한다. `LateUpdate()`에서 손 렌더러가 기준과 다르면 정상 장착
  무기로 복구한다. 이미지 로드 실패 시 기존 정상 기준을 유지한다.
- `ProceduralCharacterRig`에 `DefaultExecutionOrder(10000)`을 지정했다. 기존 파츠 복구
  `LateUpdate()`가 일반 스크립트·코루틴 뒤에 실행되어 얼굴과 양쪽 다리 7개를 렌더 직전에
  `Resources/Parts` 원본으로 확정한다.
- `EnemyUnit.PlayAttackFrames()`는 공격 시작 시뿐 아니라 프레임을 대입하는 매 반복과 마지막
  프레임 대입 직전에도 기본 좀비 전용 조건을 다시 검사한다. 조건이 하나라도 깨지면 즉시 원본
  외형으로 복구하고 공격 프레임 대입을 중단한다.

**검증 (Unity MCP 플레이모드)**

1. 깨끗하게 플레이모드를 다시 시작한 뒤 얼굴·양손 무기·양쪽 다리의 9개 렌더러 모두에
   `ZombieAttack_01`을 강제로 주입했다.
2. 다음 프레임 전수 검사에서 9/9가 `Body`·`LegUpper`·`LegLower`·`Foot`·`RightSMG`·
   `LeftLaserPistol`로 복구됐고, Player 하위 좀비 이미지 참조는 0개였다.
3. 격리 캡처에서 얼굴·두 무기·양쪽 다리가 정상으로 보이는 것을 확인했다.
4. `assets-refresh` 재컴파일 성공, 검증 직후 Unity 콘솔 Error/Exception 0건.

### 로봇 파츠가 엉뚱한 이미지로 그려지던 버그 근본 원인 규명 및 수정 (2026-08-10)

사용자 제보: **인게임 로봇의 각 파츠(머리·다리·발·무기 등)가 전부 다른 이미지로 그려진다.
로봇 이미지가 좀비로 바뀌기도 하고, 머리가 신발로 바뀌기도 한다. 좀비에게 처음 공격당하기
전까지 유지되고, 피격 이후에는 정상 이미지로 돌아온다.** 이전 세션에서 여러 차례 수정을
시도했으나 전부 원인 규명에 실패했던 항목이다.

#### 이전 시도가 전부 실패한 이유

이전 수정들은 모두 `SpriteRenderer.sprite` **필드 값**이 잘못 덮어써졌다고 전제하고,
`ProceduralCharacterRig`가 매 프레임 `Resources/Parts` 원본으로 되돌리는 방어 코드
(`RestorePartSprites()`, `DefaultExecutionOrder(10000)`, `LateUpdate` 재확정 등)와
`EnemyUnit.PlayAttackFrames()`의 조건 강화를 반복해서 쌓았다. 그런데 **`sprite` 필드는
처음부터 한 번도 잘못된 적이 없었다.** 실제로 깨진 것은 그 스프라이트를 **화면에 그리는
과정(텍스처 바인딩)** 이었기 때문에, 데이터를 아무리 검사·복구해도 증상이 그대로였다.
이번에 플레이모드 1프레임에서 실측한 결과 `Body`/`LegUpper`/`LegLower`/`Foot` 7개 렌더러의
`sprite`는 전부 정상인데 화면에는 엉뚱한 텍스처가 그려지는 것을 확인해 이 전제를 뒤집었다.

#### 진짜 원인

`HitFlash`가 피격 연출을 위해 유닛의 모든 `SpriteRenderer`에 전용 머티리얼
(`Comstock/SpriteFlash`)을 갈아끼우는데, 이 셰이더가 **빌트인 렌더 파이프라인용 스프라이트
셰이더**(`CGPROGRAM` + `UnitySprites.cginc`)로 작성되어 있었다. 이 프로젝트는 **URP 2D
Renderer**(`PC_Renderer`, `Sprite-Unlit-Default`가 기본 스프라이트 머티리얼, SRP Batcher 켜짐)를
쓴다. **URP 2D Renderer는 빌트인 RP용 스프라이트 셰이더에 SpriteRenderer의 렌더러별
데이터(`_MainTex` 등)를 전달하지 않는다.** 그래서 해당 렌더러들은 자기 스프라이트 텍스처를
받지 못하고 직전에 바인딩돼 있던 다른 텍스처(다른 파츠·좀비·배경)로 그려졌다 — 이것이
"머리에 신발", "로봇이 좀비로" 증상의 정체다.

**"첫 피격 이후 정상으로 돌아오는" 이유**도 여기서 설명된다. 첫 피격 때
`HitFlash.SetFlashAmount()`가 `SetPropertyBlock()`을 호출하는데, `MaterialPropertyBlock`을
한 번이라도 설정하면 렌더러별 데이터가 강제로 업로드되면서 정상 텍스처가 바인딩된다.
그 뒤로는 계속 정상이라 "피격 전까지만 깨진다"는 재현 조건이 만들어졌다.

#### 재현 및 원인 검증 (MCP 플레이모드, 전부 실측)

`EditorApplication.isPaused = true`를 먼저 켜고 `EnterPlaymode()`를 호출하면 **1프레임에
정지한 상태로 진입**한다. 피격이 일어나기 전 상태를 그대로 얼려놓고 검사할 수 있어
이번 재현의 핵심이었다(앞으로도 "초기 몇 프레임에서만 나타나는" 버그에 쓸 것).

1. 1프레임 정지 상태에서 `Camera.main`을 RenderTexture로 캡처 → **버그 재현 성공**
   (머리에 얼굴이 없고, 부츠가 흰색 덩어리로 나옴). 이때 7개 렌더러의 `sprite`는 전부
   정상(`Body`/`LegUpper`/`LegLower`/`Foot`)이고 `HasPropertyBlock()`은 전부 false였다.
2. 같은 프레임에서 `SetPropertyBlock()`만 호출 → **즉시 정상 복구**(얼굴·주황 부츠 정상).
   → 첫 피격이 증상을 고치던 경로와 정확히 일치.
3. 같은 프레임에서 URP 기본 스프라이트 머티리얼로 교체 → **정상**.
4. **결정적 대조군**: Unity 기본 `Sprites/Default`(빌트인 RP 셰이더)를 붙여봤더니
   **완전히 동일하게 깨졌다.** → 우리 셰이더 로직 문제가 아니라 "빌트인 RP 스프라이트
   셰이더 + URP 2D Renderer" 조합 자체가 원인임이 확정됐다.
5. SRP Batcher 호환성 코드 실측: `Sprites/Default`=10(비호환),
   기존 `Comstock/SpriteFlash`=10(비호환), URP 2D `Sprite-Unlit-Default`=0(호환).

#### 수정 내용

- **`Assets/Shaders/SpriteFlash.shader`를 URP 2D 셰이더로 전면 재작성.**
  URP의 `Universal Render Pipeline/2D/Sprite-Unlit-Default`(패키지 원본)를 그대로 복제하고
  `_FlashColor`/`_FlashAmount`만 `CBUFFER_START(UnityPerMaterial)` 안에 추가했다.
  `Core2D.hlsl`/`2DCommon.hlsl`을 include하는 HLSLPROGRAM 구조이며,
  `RenderPipeline = UniversalPipeline` 태그와 URP와 동일한 블렌드
  (`Blend SrcAlpha OneMinusSrcAlpha, One OneMinusSrcAlpha`)를 쓴다.
  URP 2D는 **straight alpha 블렌딩**이므로 예전 빌트인 셰이더의 `c.rgb *= c.a`
  (premultiplied alpha) 처리는 **제거했다** — 그대로 두면 반투명 경계가 어두워진다.
  셰이더 이름(`Comstock/SpriteFlash`)은 그대로라 `HitFlash.cs`의 `Shader.Find`는 무수정이다.
- **`Assets/Scripts/HitFlash.cs`** — 클래스 주석에 "이 셰이더는 반드시 URP(HLSL)용이어야 하며
  빌트인 RP 스프라이트 셰이더로 바꾸면 파츠가 엉뚱한 텍스처로 그려진다"는 경고를 추가했다
  (같은 실수의 재발 방지. 코드 동작은 변경 없음).
- 이전 세션들이 쌓아둔 `ProceduralCharacterRig`/`EnemyUnit`의 스프라이트 복구·조건 강화
  코드는 **그대로 두었다.** 이번 원인과는 무관하지만 그 자체로 해가 없고, 제거하면 검증
  범위가 불필요하게 커지기 때문이다(원인이 규명된 지금은 사실상 불필요한 방어 코드라는
  점만 기록해 둔다 — 정리는 별도 판단 사항).

#### 검증 (MCP 플레이모드, 수정 후)

1. 셰이더 재컴파일: `supported=True`, `passCount=1`, **SRP Batcher 호환 코드 0(호환)**,
   `shaderMessageCount=0`(경고·에러 없음).
2. 1프레임 정지 상태(피격 전, `renderersWithBlock=0` 실측 확인)에서 캡처 →
   **머리에 얼굴, 주황 부츠, 다리 파츠 전부 정상.** 수정 전 동일 프레임·동일 카메라 캡처와
   직접 비교했다.
3. 피격 연출 동작 확인: `HitFlash.Play()` 호출 → 로봇 전체가 흰색으로 물듦 →
   `_FlashAmount=0`으로 되돌리면 정상 복귀. **SRP Batcher 호환 셰이더로 바꿔도
   MaterialPropertyBlock 기반 피격 연출은 그대로 동작한다**(블록을 쓰는 렌더러는 SRP Batcher
   경로에서 빠져 블록 값이 적용됨).
4. **좀비도 같은 버그의 대상이었음을 확인하고 함께 수정됨을 검증** — `EnemyUnit.Awake()`가
   같은 `HitFlash`를 붙이므로 좀비도 첫 피격 전에는 동일하게 깨진다. 한 번도 피격되지 않은
   좀비 2마리를 강제 스폰해 캡처한 결과 정상 렌더링을 확인했다.
5. 실제 플레이 24초(좀비 14마리 동시 생존, 게임오버까지) 진행 후 전수 검사 —
   9개 렌더러 전부 정상 스프라이트·정상 셰이더, 화면에도 좀비 텍스처 섞임 없음.
6. 프로젝트 전체에서 같은 패턴(빌트인 RP 셰이더를 스프라이트에 배정)을 검색 →
   `HitFlash` 외 유일한 사례는 `Editor/ProceduralRigDemoBuilder.cs:41`의 **LineRenderer**
   (데모 씬 지면선, 빌드 미포함)라 이 버그와 무관하다. 미수정.
7. 게임 코드 예외 0건(콘솔의 Exception은 진단용 리플렉션 스크립트의 것 하나뿐).

#### 남은 확인 필요 항목 (이번 수정 범위 밖, 사용자 판단 필요)

- **빌드 시 피격 연출이 조용히 사라질 수 있다(기존부터 있던 문제).**
  `HitFlash`는 `Shader.Find("Comstock/SpriteFlash")`로 셰이더를 찾는데, 이 셰이더는 어떤
  머티리얼 에셋·씬·Resources에서도 참조되지 않는다. 따라서 **스탠드얼론 빌드에서는
  셰이더가 스트립되어 `Shader.Find`가 null을 반환**할 수 있다. 그 경우 `CollectTargets()`가
  조기 반환해 **파츠는 정상으로 나오지만 피격 흰색 연출만 사라진다**(깨짐은 없음).
  해결하려면 (a) Graphics Settings의 Always Included Shaders에 추가하거나,
  (b) 이 셰이더를 쓰는 머티리얼 에셋을 `Resources/`에 만들어 참조하면 된다.
  (a)는 프로젝트 설정 변경이라 협업 규칙 6에 따라 사용자 확인 후 진행할 항목이다.

### 스프라이트 원본 크기 대비 실제 렌더 배율 전수 검증 (2026-08-10, 검증만 — 수정 없음)

사용자 요청: "각 스프라이트들이 원본 이미지와 같은 크기로 그려지고 있는지 검증. 특히 다리
파츠가 원래보다 얇고 작게 그려지고 있으니 검증." **협업 규칙 2에 따라 검증만 수행하고
코드·에셋은 수정하지 않았다.**

측정 방법: 플레이모드에 1프레임 정지로 진입(`isPaused=true` → `EnterPlaymode()`)한 뒤
모든 `SpriteRenderer`에 대해 원본 PNG 픽셀 크기(파일 IHDR 직접 판독) / 임포트된 텍스처 크기 /
`sprite.rect` / `pixelsPerUnit` / `transform.lossyScale`을 읽어 **원본 유닛 크기 대비 실제
렌더 배율**을 계산했다.

#### 결과 요약 (배율 x1.0000 = 원본 크기 그대로)

| 대상 | 원본(px) | 원본(유닛) | 실제(유닛) | 배율 |
|---|---|---|---|---|
| 좀비/스프린터/스피터 | 250x250 | 2.50x2.50 | 2.50x2.50 | **x1.0000** |
| 디스럭터 | 350x350 | 3.50x3.50 | 3.50x3.50 | **x1.0000** |
| 차저/리더 | 450x450 | 4.50x4.50 | 4.50x4.50 | **x1.0000** |
| 배경 map | 3840x2160 | 38.40x21.60 | 38.40x21.60 | **x1.0000** |
| 로봇 몸통(Body) | 250x250 | 2.50x2.50 | 2.853x2.853 | **x1.1413** (14% 확대) |
| 로봇 허벅지(LegUpper) | 98x111 | 0.98x1.11 | 0.336x0.380 | **x0.3424** (66% 축소) |
| 로봇 정강이(LegLower) | 74x91 | 0.74x0.91 | 0.253x0.312 | **x0.3424** |
| 로봇 발(Foot) | 117x134 | 1.17x1.34 | 0.401x0.459 | **x0.3424** |
| 우측 무기(RightSMG) | 1080x972 | 10.80x9.72 | 2.203x1.983 | x0.2040 |
| 좌측 무기(LeftLaserPistol) | 1536x1024 | 15.36x10.24 | 2.203x1.469 | x0.1434 |

- **적 6종과 배경은 전부 정확히 x1.0000** — 좀비 기획서 Ver04의 규격 원칙(PPU=100·스케일=1)이
  그대로 지켜지고 있다. 임포트된 텍스처 크기도 원본 PNG와 100% 일치(축소 임포트 없음).
- **플레이어 로봇만 이 원칙에서 벗어나 있다.** 몸통은 확대, 다리는 대폭 축소다.
- 무기 배율이 서로 다른 것은 **의도된 설계**다. `PlayerShootManager.ApplyWeaponSprite()`가
  `TargetHandImageSize / max_dim * weapon_imgscale`로 정규화해 원본 픽셀 크기가 제각각인
  무기들이 화면에서 일정한 크기로 보이게 만든다(두 무기 모두 실제 폭이 2.203유닛으로 동일).
  균등 스케일이라 종횡비 왜곡은 없다.

#### 다리가 얇고 작아 보이는 원인 (사용자 지적은 정확했다)

스케일 체인 실측:

- 몸통 배율 = `Player.localScale`(0.5) x `ProceduralVisual.localScale`(4.15) x `rigScale`(0.55)
  = **1.14125**
- 다리 배율 = 위 값 x `legScale`(0.30) = **0.34238**

즉 다리 파츠에만 `legScale=0.30`이 추가로 곱해져 **몸통 대비 0.30배**로 그려진다.
가로·세로 배율이 동일(0.3424, 0.3424)하므로 "얇아지는" 종횡비 왜곡은 없고,
**전체가 작아져서 상대적으로 가늘어 보이는 것**이다(걷는 중 몸통에만 적용되는 squash는
최대 x1.03/y0.95의 의도된 비균등 연출이며 다리에는 없다).

**더 중요한 발견 — 현재 값은 코드 자신이 명시한 목표와도 어긋나 있다.**
`legScale` 인스펙터 주석은 "레퍼런스 GIF 실측 기준 '다리 길이 ÷ 몸통 높이 = 0.41'을 맞추는
값이 약 0.30"이라고 적고 있으나, 실제로 계산해 보면:

- 현재 `legLength`(고관절~밑창) = 0.6552, 몸통 높이 = 2.5 → **실제 비율 0.2621**
- `legScale=1`(원본 크기)일 때 `legLength` = 2.1841 → 비율 0.8736
- **목표 0.41을 실제로 맞추는 `legScale` = 0.4693**

따라서 현재 0.30은 목표치보다 **36.1% 더 축소**된 값이다. 주석의 "약 0.30" 계산이 처음부터
틀렸던 것으로 보인다.

#### 시각 비교 (플레이모드 임시 변경 후 캡처, 종료 시 전부 폐기)

`legScale`을 0.30 / 0.4693 / 1.00 세 값으로 바꿔 재조립(`Build()`)하고 같은 카메라로 촬영:

- **0.30 (현재)**: 다리가 가늘고 짧아 몸통에 비해 빈약하다.
- **0.4693 (주석 목표 0.41 달성값)**: 허벅지·정강이가 눈에 띄게 두꺼워지고 무릎 관절이
  또렷해진다. 몸통과의 균형이 가장 자연스럽다.
- **1.00 (원본 크기 그대로)**: 다리와 부츠가 몸통에 육박할 만큼 커져 하체 비대형이 된다.
  적·배경처럼 "원본 크기 그대로"를 로봇에도 적용하려면 몸통 스케일까지 함께 재설계해야 한다.

#### 사용자 판단이 필요한 선택지 (수정하지 않고 대기)

1. `legScale`을 **0.4693**으로 — 코드 주석이 원래 의도한 비율(0.41)을 실제로 달성. 변경 최소.
2. `legScale`을 **1.0**으로 + 몸통 스케일 재조정 — 적·배경과 같은 "원본 크기 그대로" 원칙을
   로봇에도 적용. 리그 전체 비율 재설계가 필요하다.
3. 임의의 중간값으로 눈대중 조정.

어느 쪽이든 `legScale`은 뼈 길이 계산의 기준이라 바꾸면 보폭·골반 높이·IK가 전부 따라
움직인다(길이 계열 파라미터가 전부 다리 길이 배수이므로 자동 추종). 몸통 배율(1.14125)을
건드릴 경우 `RigingPoint`(무기 소켓) 위치·크기와 콜라이더도 함께 재검증해야 한다.

### 로봇 파츠 크기 비율을 원본 이미지 기준으로 바로잡음 + 정강이를 신발 안으로 (2026-08-10)

사용자 지시 3건을 반영했다.

1. 참조 이미지(로봇 전신 컨셉)를 제시하며 "이 정도 비율이어야 의도한 바다. 맞춰서 수정해라."
2. "로봇 다리가 신발 안으로 들어가게 못 만드나? 지금은 신발에 발 넣는 부분이 따로 떼져
   있는데, 신발을 신은 것처럼 발이 안으로 들어간 듯한 모습으로 만들어 달라."
3. **"비율 수정은 몸통의 외곽선과 다리의 외곽선 두께가 일치하는 비율로 만들어야 한다.
   애초에 기본 이미지를 그렇게 만들었는데 적용은 다르게 되어 있다. 내가 만든 이미지 크기
   비율대로 적용이 안 되어 있고 임의로 비율이 수정되어 있다."**

3번이 결정적인 기준이었다. 눈대중이 아니라 **원본 PNG의 외곽선 두께를 픽셀로 실측해
역산**할 수 있게 되었다.

#### 원본 외곽선 두께 실측

`Texture2D.LoadImage()`로 원본 PNG를 직접 디코딩하고, **수직에 가까운 외곽선 구간만 골라**
(스캔라인마다 가장자리 x가 1px 이내로만 움직이는 구간) 안티앨리어싱을 서브픽셀로 적분해
측정했다.

| 파츠 | 원본 해상도 | 외곽선 두께 | 몸통과 맞추는 배율 |
|---|---|---|---|
| Body | 250x250 | **4.27 px** | (기준) |
| LegUpper | 98x111 | 9.06 px | 4.27/9.06 = **0.471** |
| LegLower | 74x91 | 7.42 px | 4.27/7.42 = **0.576** |
| Foot | 117x134 | 9.84 px | 4.27/9.84 = **0.434** |

**파츠마다 원본 해상도가 달라 외곽선 두께도 제각각이었다.** 따라서 "원본 이미지 크기
비율대로"란 전부 같은 배율(legScale=1)로 그리는 것이 아니라, **외곽선 두께가 같아지는
파츠별 배율로 그리는 것**이다. 기존 코드는 이 셋을 `legScale` 하나(0.30)로 뭉뚱그렸고,
그 결과 다리 외곽선이 몸통의 **0.52배 두께**로 그려져 다리가 얇고 작아 보였다.

**교차 검증**: 참조 이미지의 몸통:다리 길이 비율에서 역산한 값(약 0.477~0.56)과, 외곽선
기준 평균값(0.487)이 서로 독립적으로 같은 범위로 수렴했다. 두 근거가 일치해 신뢰도가 높다.

#### 수정 내용 — `ProceduralCharacterRig.cs`

- **파츠별 배율 신설**: `thighScale`(0.471) / `shinScale`(0.576) / `footScale`(0.434).
  `legScale`은 "다리 전체에 한 번 더 곱하는 배율"로 의미를 바꾸고 기본값을 **1.0**으로
  되돌렸다(데모 슬라이더 `LegScale` 프로퍼티 호환 유지).
- 실제 적용 배율은 `ThighScale`/`ShinScale`/`FootScale` 프로퍼티(파츠별 x legScale) 하나로
  모았다. **스프라이트 크기와 뼈 길이가 반드시 같은 값을 써야 관절이 어긋나지 않으므로**
  `thighLength`/`shinLength`/`ankleToSole` 계산과 `AttachVisual()`이 모두 이 프로퍼티를 쓴다.
- **`footAnkleAnchor` 0.88 → 0.62** — 정강이가 꽂히는 지점을 신발 입구(0.88)에서 신발 통
  안쪽으로 내렸다. 신발이 정강이보다 위에 그려지므로(`sortingBase+2` vs `+0`) 정강이 끝이
  신발에 파묻혀 **"신발을 신은" 모습**이 된다. `ankleToSole`도 이 앵커에서 자동 계산되므로
  골반 높이가 함께 따라온다.
- **자세 보정** — 다리가 커지자 예전 값으로는 무릎이 과하게 접히고 발이 몸통 폭 밖으로
  벌어졌다. 참조 이미지처럼 두 다리가 몸통 아래 나란히 서도록:
  `hipHeightRatio` 0.90 → **0.96**, `idleStanceRatio` 0.2 → **0.05**,
  `hipSeparation` 0.7 → **0.55**, `frontLegPullBack` 0.1 → **0.06**.
- 위 값은 **코드 기본값과 `Ground01.unity` 씬 직렬화 값 양쪽에** 적용했다(씬 값이 우선하므로
  코드만 고치면 반영되지 않는다 - 주의).

#### 검증 (MCP 플레이모드, 전부 실측)

1. **외곽선 두께 일치 실측** — 화면상 외곽선(원본 px 환산):
   몸통 **4.87** / 허벅지 **4.87** / 정강이 **4.88** / 발 **4.87**. 사용자 요구가 정확히 달성됐다.
   (수정 전에는 몸통 4.87 대비 다리가 2.2~2.9로 절반 수준이었다.)
2. 다리 노출 길이 / 몸통 그림 높이 = **0.377 → 0.537**.
3. **보행 영향 실측** — `hipHeightRatio`를 올리면 보폭이 잘린다는 코드 주석의 경고 때문에
   위상 20개를 강제 재생해 측정했다. 발 x 이동폭 0.587(0.90) → **0.577(0.96)** 로 1.7%만
   줄었고, 스탠스 중 발 뜸은 -0.0146유닛(음수 = 접지 유지)이라 **미끄러짐·들뜸 없음**.
   사전 계산으로는 36% 잘릴 것으로 봤으나 실측은 달랐다(IK 클램프가 거의 걸리지 않음).
4. 대기 자세·보행 자세를 흰 배경에 단독 렌더해 참조 이미지와 대조 확인.
5. 실제 플레이 16초(좀비 13마리 동시 생존)에서 7개 파츠 스프라이트가 전부 정상
   (`Body`/`LegUpper`/`LegLower`/`Foot`), 인게임 화면에서도 무기·다리·부츠 정상.
6. `assets-refresh` 재컴파일 오류 0건.

#### 사용자 확인이 필요한 후속 항목 (이번에는 손대지 않음)

- **플레이어 콜라이더가 상대적으로 낮아졌다.** 콜라이더는 그대로(world 1.50 x 0.85유닛,
  발밑 0~0.85 구간)인데 다리가 길어지면서 골반 높이가 world 0.706 → **1.004**로 올라갔다.
  이제 콜라이더가 다리 구간만 덮으므로 피격 판정 위치를 재조정할지 판단이 필요하다.
- **무기 소켓(`RigingPoint`) 높이** — 몸통이 위로 올라가 무기가 상대적으로 몸통 중앙쯤에
  걸린다. 인게임 캡처상 어색하지 않아 그대로 뒀으나, 귀 옆에 맞추려면 소켓 y를 올려야 한다.
- **데모 씬 `JointRigDemo.unity`** — 같은 컴포넌트가 예전 값으로 직렬화돼 있을 수 있다.
  빌드에 포함되지 않아 이번엔 건드리지 않았다.

### 좀비 교전 거리·밀집도 수정 + 로봇 밀어내기 신설 + 맵 체감 크기 원인 분석 (2026-08-10)

사용자 지적 5건. 1~4는 수정, 5는 원인 분석만 수행했다.

#### 1) 좀비 공격 사거리가 비정상적으로 길었다 — 이중 계산이 원인

`EnemyUnit.Update()`가 `distance <= AttackRange + BodyContactRadius()`로 판정하고 있었다.
`monster_range`는 이미 **중심 기준** 사거리인데 여기에 두 몸의 반지름 합을 **더해서**,
몸집이 클수록 사거리가 통째로 늘어나는 구조였다. 좀비 실측: `1.600 + 1.750 = 3.350유닛`으로
**두 몸이 실제로 맞닿는 거리(1.225)의 2.7배**에서 공격이 들어갔다.

`BodyContactRadius()`는 원래 "몸집 때문에 실제로는 닿았는데 사거리가 짧아 판정이 안 나는"
경우를 구제하려던 보정이므로, **더하기가 아니라 하한**이어야 한다.

- `EffectiveAttackRange()` 신설 = `Mathf.Max(AttackRange, BodyContactRadius())`.
  `Update()`와 `IsPlayerWithinAttackRange()` 양쪽이 이걸 쓴다.
- `BodyContactRadius()`가 콜라이더 extents의 **긴 축(Max)** 을 쓰던 것도 **짧은 축(Min)** 으로
  바꿨다. `ComputeBodyRadius()`는 "원으로 근사하는 이상 긴 쪽을 쓰면 짧은 축에서 몸 밖까지
  판정이 나간다"는 이유로 이미 Min을 쓰고 있었는데 여기만 기준이 어긋나 있었다
  (좀비 기준 1.750 → 1.225).
- **결과: 판정거리 3.350 → 1.600유닛 (52% 감소).** 데이터(`monster_range`)는 건드리지 않았다.

#### 2) 좀비끼리 벌어지는 간격이 지나치게 넓었다

`ComputeSeparation()`이 두 몸의 반지름 합(좀비 기준 1.600유닛)을 겹침 임계로 썼는데,
콜라이더가 그림보다 넉넉해 실제 몸보다 훨씬 멀찍이 벌어져 보였다.
사용자 지정("현재의 1/3으로 줄여도 무방")대로 `SeparationRadiusScale = 1/3` 상수를 신설해
임계와 `OverlapSphere` 검색 반경 양쪽에 곱했다. **1.600 → 0.533유닛.**

#### 3) 공격 후에도 사거리 안으로 계속 파고들었다

`ComputeSeekDirection()`이 공격 모션 중이 아니면 **항상** 플레이어를 향해 전진했다.
그래서 한 번 때리고 쿨다운이 도는 동안 플레이어 몸속까지 밀고 들어왔다.
사거리보다 살짝 안쪽(`StopDistanceRatio = 0.9`)에 도달하면 `Vector3.zero`를 돌려 멈추도록 했다.
경계에서 멈췄다 나갔다 하며 떠는 것을 막으려고 1.0이 아니라 0.9로 뒀다.

#### 4) 로봇이 이동할 때 좀비를 밀어낼 수 있게 신설

**물리 충돌만으로는 절대 밀리지 않는다.** `EnemyUnit.FixedUpdate()`가 매 프레임 Rigidbody
속도를 통째로 덮어쓰기 때문에, 충돌로 밀려나도 바로 다음 프레임에 원래 속도로 돌아간다.
그래서 이동 속도에 **더해질 별도 성분**으로 주입하는 방식을 썼다.

- `EnemyUnit.ApplyPlayerPush(Vector3)` + `player_push_velocity` 필드 신설.
  `FixedUpdate()`가 `move_velocity + knockback_velocity + player_push_velocity`로 합산한 뒤
  **즉시 비운다**(넉백과 달리 감쇠 없음 → 몸이 떨어지는 순간 바로 멈춘다).
- `PlayerRobotController.PushOverlappingEnemies()` 신설. `FixedUpdate()`에서 매 프레임 호출한다.
  멈춰 있으면(속도 0) 밀지 않고, 몸이 겹친 적만 **파고든 깊이에 비례**해 밀어낸다.
  세기는 `enemyPushSpeedRatio`(기본 1.2, 인스펙터 노출) x 내 이동 속도.
  좀비가 다시 다가오는 속도(2.5)보다 커야 실제로 밀리므로 1보다 크게 잡았다.
  구르기 중에도 그대로 적용되어 무리를 뚫고 지나갈 수 있다.
- 밀어내기 판정 반지름은 `EnemyUnit.BodyRadius`와 같은 기준(콜라이더 짧은 축)을 쓴다.

#### 검증 (MCP 플레이모드)

1. 공격 판정거리 `3.350 → 1.600유닛` 실측(리플렉션으로 `EffectiveAttackRange()` 직접 호출).
2. 좀비간 겹침 임계 `1.600 → 0.533유닛`, **실플레이에서 좀비 16마리의 최소 간격 0.540유닛**
   측정(임계값과 일치).
3. 정지 로직: 사거리 안(1.0유닛)에서 `seek=0`, 사거리 밖(3.0유닛)에서 `seek=3.0` 확인.
   **실플레이에서 플레이어에 도달한 좀비 4마리가 전부 1.403~1.655유닛에 정지**(정지거리 1.44).
4. 밀어내기: 플레이어 속도 (5,0,0)일 때 겹친 좀비에 **3.061유닛/초**가 전달되고,
   적 `FixedUpdate` 1회 후 0으로 비워지는 것까지 확인. 멈춰 있으면 밀지 않는다.
5. `assets-refresh` 재컴파일 오류 0건.

#### 5) 맵이 예상보다 훨씬 작게 느껴지는 원인 (분석만, 수정하지 않음)

실측 결과:

| 항목 | 값 |
|---|---|
| 맵 월드 크기 | **38.40 x 21.60 유닛** (배경 3840x2160px, PPU=100) |
| 화면에 보이는 범위 | **30.77 x 17.32 유닛** (`orthographicSize=8.66`, 16:9) |
| **맵 ÷ 화면** | **가로 1.25배 / 세로 1.25배** |

**FHD 2개분이 아니라 1.25개분이다.** 원인은 **배경과 카메라의 픽셀→유닛 환산 기준이 서로
다르다**는 것이다.

- 배경 스프라이트: **PPU=100** → 1유닛 = 100픽셀
- 카메라: 예전 **"기획 문서 수치 ÷ 50"** 환산에서 나온 `orthographicSize=8.66`이 그대로 남아
  있다 → 화면 세로 1080px이 17.32유닛을 덮으므로 **1유닛 = 62.4픽셀**

그래서 **배경 텍스처 1픽셀이 화면 0.624픽셀로 축소되어** 그려진다(원본 도트보다 흐릿하고,
같은 화면에 더 넓은 영역이 들어와 맵이 작게 느껴진다).

`orthographicSize`를 **5.4**(= 1080 ÷ 2 ÷ 100)로 바꾸면 배경과 카메라의 기준이 일치한다:

- 화면 = 19.2 x 10.8유닛 → **맵 ÷ 화면 = 정확히 2.0 x 2.0배** (사용자가 기대한 크기)
- 배경 텍스처 1픽셀 = 화면 1픽셀 → 도트가 원본 그대로 선명해진다

**다만 게임플레이에 큰 영향이 있어 임의로 바꾸지 않았다**(협업 규칙 1·2):

- 화면에 보이는 범위가 가로/세로 모두 **62%로 줄어** 캐릭터·좀비가 1.6배 크게 보인다
- **무기 감지거리 상한 8.5유닛**(`PlayerShootManager.max_detect_range`, "화면 밖 교전 방지"
  목적)이 새 화면 세로 반경 5.4보다 커져서 함께 조정해야 한다
- 화면 테두리 스폰(`EnemySpawner`)·부품 상자 화살표 UI·웨이브 체감 난이도가 모두 달라진다

### 카메라를 FHD 기준으로 정규화 + 로봇 배율 원본화 (2026-08-10)

직전 항목에서 "원인만 분석하고 수정하지 않았다"고 남겨둔 건을, 사용자 지시로 실제 적용했다.

#### 사용자 지적 — 내 설명이 잘못된 프레이밍이었다

> "화면 가시 범위가 왜 줄어들어? 모든 객체 크기를 원본 이미지 크기에 맞게 조절하면 되잖아
> 카메라 크기도 조절해야지 fhd로"

직전 보고에서 나는 `orthographicSize`를 5.4로 바꾸면 "가시 범위가 62%로 줄어든다"고 손해처럼
설명했다. 그러나 객체(좀비·배경)는 **이미 원본 크기(x1.0)** 이고 어긋난 것은 **카메라 하나**였다.
즉 62%로 줄어드는 것이 아니라, **지금까지 모든 것이 62%로 축소돼 보이던 상태가 원본 배율로
정상화되는 것**이다. 부수 조정이 필요한 이유도 "가시 범위가 줄어서"가 아니라, 거리 기반
수치들이 예전 카메라 기준으로 튜닝돼 있어서다.

#### 수정 1 — 카메라 `orthographicSize` 8.660254 → **5.4**

`5.4 = 1080 ÷ 2 ÷ 100`. 배경 스프라이트의 PPU(100)와 카메라의 픽셀→유닛 환산을 일치시킨 값이다.

| 항목 | 이전 | 이후 |
|---|---|---|
| 화면 크기 | 30.773 x 17.321 유닛 | **19.188 x 10.800 유닛** |
| 픽셀 ÷ 유닛 | 62.4 px | **100.0 px** (배경 PPU와 일치) |
| 맵 ÷ 화면 | 1.25 x 1.25 | **2.001 x 2.000** (기대값 = FHD 2개분) |

배경 텍스처 1픽셀이 화면 1픽셀로 그려져 도트가 원본 그대로 선명해졌다.

#### 수정 2 — 플레이어 로봇 배율 x1.14125 → **x1.0** (`Player.localScale` 0.5 → 0.43812)

"스프라이트 확대 배율 전수 조사"(2026-08-10)에서 **로봇 몸통만 x1.1413으로 확대**되어 있고
적 6종·배경은 전부 x1.0이라는 것이 이미 확인돼 있었다. 카메라를 원본 기준으로 맞춘 이상
로봇만 14% 확대된 채로 둘 이유가 없어 같이 정규화했다.

- **`ProceduralVisual`이 아니라 `Player`의 스케일을 조정했다.** 시각만 줄이면 콜라이더·무기
  소켓이 예전 크기로 남아 어긋난다. `Player`를 줄이면 **시각·콜라이더·무기 소켓이 전부 같은
  비율로** 따라온다.
- 배율은 눈대중이 아니라 **몸통 스프라이트의 자연 크기(250px ÷ PPU 100 = 2.5유닛) 대비 실제
  월드 크기를 실측해 역산**했다(`0.5 ÷ 1.14125 = 0.43812`).
- 다리 파츠(`thighScale`/`shinScale`/`footScale`)는 **손대지 않았다**. 전부 같은 비율로 함께
  줄어들므로 직전 작업에서 맞춘 "몸통과 다리의 외곽선 두께 일치"가 그대로 유지된다
  (실측: 허벅지 배율 0.4529 → 0.3969 = 정확히 1/1.14125배).

#### 수정 3 — 무기 감지 상한 `max_detect_range` 8.5 → **5.3**

"화면 밖의 보이지 않는 적과 교전하는 것을 막는다"는 이 값의 목적상, 새 화면 세로 반경(5.4)
바로 안쪽으로 맞췄다. **코드 기본값과 `Ground01.unity` 씬 직렬화 값 양쪽에 적용**했다
(씬 값이 우선하므로 코드만 고치면 반영되지 않는다 — 씬에는 8.5가 그대로 박혀 있었다).

#### 자동으로 따라오는 것들 (수정 불필요 — 실측 확인)

- **화면 테두리 스폰**(`EnemySpawner.TryGetCameraHalfExtents`)과 **카메라 맵 클램프**
  (`CameraFollow`)는 `cam.orthographicSize`를 읽으므로 자동 반영된다.
- **HUD·부품 상자 화살표 UI**는 Canvas가 `ScreenSpaceOverlay` + `ConstantPixelSize`라 무관하다.
- **적 체력바**는 각자 스프라이트의 월드 bounds에서 크기를 계산하므로 무관하다.

#### 검증 (MCP 플레이모드, 전부 실측)

1. 화면 = **19.188 x 10.800 유닛**, 픽셀÷유닛 = **100.00**, 맵÷화면 = **2.001 x 2.000**.
2. 몸통 배율 = **1.0000** (자연 2.500 / 월드 2.500). 무기·다리도 같은 비율로 축소 확인.
3. 플레이어 콜라이더 월드 1.5000x0.8500 → **1.3143x0.7448** (`Physics.SyncTransforms()` 후 측정.
   동기화 전에는 캐시된 옛 값이 나오므로 주의).
4. 실플레이 스폰: 적 14마리 중 화면 밖에서 걸어 들어오는 개체 확인(가장 먼 개체 12.02유닛).
5. 맵 우상단(18, 9)으로 이동시켰을 때 카메라가 **(9.61, 5.40)** 에서 정확히 멈추고
   가시 범위가 x[0.01, 19.20] y[0.00, 10.80] — **맵 경계와 정확히 일치**, 배경 바깥 여백 없음.
6. 콘솔 Error 0건.

#### 남은 밸런스 이슈 (사용자 판단 필요 — 이번에는 손대지 않음)

카메라 기준이 ÷50 → ÷100으로 바뀌었으므로, **예전 기준으로 튜닝된 거리·속도 값들은 화면 대비
1.6배로 커졌다.** 실측치는 다음과 같다.

| 항목 | 값 | 화면 대비 영향 |
|---|---|---|
| `weapon_range` | 2.20 ~ 15.00 유닛 | **35행/65행이 화면 세로 반경(5.4) 초과** — 탄이 위아래 화면 밖으로 날아간다 |
| `weapon_detect` | 2.20 ~ 8.00 유닛 | 상한 5.3에 **25행/65행이 잘린다**(예전 상한 8.5에서는 0행) → 잘린 무기끼리 감지거리 차별화가 사라짐(평균 1.42유닛 손실) |
| 플레이어 이동속도 5.0 | 화면 세로 횡단 **2.16초** | 예전 3.46초 |
| 좀비 이동속도 2.5 | 화면 세로 횡단 **4.32초** | 예전 6.93초 |
| 구르기 거리 5.0 | 화면 세로의 **46%** | 예전 29% |

정석 해법은 **무기 65행의 거리 수치를 ÷50이 아니라 ÷100 환산으로 재생성**하는 것이다
(`Assets/Scripts/Editor/WeaponTableGenerator.cs`의 `Defs` 수정 → 메뉴 `Comstock/무기 테이블 65행 재생성`).
그러면 감지거리 최대가 4.0유닛이 되어 상한 5.3에 아무것도 잘리지 않고 무기별 차별화가 살아난다.
다만 이는 전투 밸런스 전면 재조정이라 사용자 확인 없이 진행하지 않았다.
`GameDataModels.cs`의 `WeaponData` 주석에 이 어긋남을 명시해 두었다.

### 무기 65행의 거리 수치를 ÷100 환산으로 재생성 (2026-08-10)

직전 항목에서 "사용자 판단 대기"로 남겨둔 밸런스 이슈를, 사용자가 **"÷100 환산으로 재생성"**
을 선택해 실행했다.

#### 왜 필요했나

카메라를 FHD 기준(1유닛 = 100px)으로 맞추면서, ÷50 환산으로 만들어진 무기 거리 값이 화면
대비 2배가 됐다. 특히 **감지거리 상한 5.3에 25행/65행이 잘려** 원거리 무기끼리 감지거리
차별화가 사라진 상태였다(예전 상한 8.5에서는 잘리는 행이 0개였다).

#### 수정 — `WeaponTableGenerator.cs`의 `Defs` 13줄

**원거리 10종만** `range` / `detect` / `speed`를 절반으로 낮추고 메뉴
`Comstock/무기 테이블 65행 재생성`을 실행했다.

| 종류 | 사거리 | 감지거리 | 탄속 |
|---|---|---|---|
| 중기관총 | 7.0 → **3.5** | 6.0 → **3.0** | 22 → **11** |
| 전투산탄총 | 4.0 → **2.0** | 3.0 → **1.5** | 18 → **9** |
| 대물저격총 | 15.0 → **7.5** | 8.0 → **4.0** | 40 → **20** |
| 플라즈마캐논 | 8.0 → **4.0** | 7.0 → **3.5** | 0(빔) |
| 로켓런처 | 7.0 → **3.5** | 4.8 → **2.4** | 14 → **7** |
| 소드오프샷건 | 3.6 → **1.8** | 2.6 → **1.3** | 18 → **9** |
| 레이저피스톨 | 6.0 → **3.0** | 5.0 → **2.5** | 30 → **15** |
| 지정사수소총 | 10.0 → **5.0** | 7.0 → **3.5** | 32 → **16** |
| 기관단총 | 6.4 → **3.2** | 5.6 → **2.8** | 22 → **11** |
| 유탄발사기 | 5.0 → **2.5** | 4.0 → **2.0** | 12 → **6** |

**`speed`(투사체 속도)도 함께 나눈 이유**: 유닛/초는 거리 단위를 포함한 값이라, 거리만
줄이면 탄의 비행 시간이 절반이 되어 "쏘자마자 도착"하게 된다. 같은 배율로 나누면 비행
시간이 보존되고 화면상 탄속(px/초)도 기획 수치와 일치한다.

#### 환산에서 **제외한** 것들과 그 이유

- **근접 3종(생존단검 2.2 / 전술 마체테 2.6 / 전기톱검 2.4)** — 이 값들은 화면이 아니라
  **"적 몸통에 닿는 거리"** 에 종속된다(원래도 기획 수치에 +1.0유닛 접촉 오프셋을 얹은
  값이라고 생성기 주석에 명시돼 있었다). 적 몸 크기는 그대로인데 절반으로 줄이면
  **좀비의 공격 사거리(1.600유닛)보다 짧아져 일방적으로 맞기만 하게 된다.**
- **`splash`(폭발 반경) / `atsize`(투사체 크기) / `knockback`** — "적 몇 마리를 휩쓰는가"로
  의미가 정해지는 값이고 적 크기는 변하지 않았으므로 그대로 뒀다.
- 공격력·공속·무게·가격 등 거리와 무관한 값은 손대지 않았다.

#### 검증 (MCP, 실측)

1. 메뉴 재생성 로그: **무기 65행 / 무기메타 65행 / 상점 65행** 모두 갱신.
2. 메뉴 `Comstock/무기 테이블 검증`: **8개 검사 전부 통과.**
3. 런타임 실측: 최대 사거리 15.0 → **7.5**, 최대 감지거리 8.0 → **4.0**,
   **감지 상한 5.3에 잘리는 무기 25행 → 0행** (목표 달성).
4. 사거리가 화면 세로 반경(5.4)을 넘는 무기는 대물저격총 5등급 5행뿐이며, 감지거리가 4.0이라
   **조준은 언제나 화면 안에서 시작**된다(탄만 위아래로 더 날아갈 수 있다).
5. **통제 실험**: 자동 스폰을 끄고 좀비를 감지거리 밖(3.60유닛)과 안(2.40유닛)에 하나씩
   고정 배치 → **안쪽 좀비만 사살되고 바깥 좀비는 HP 100/100 무피해**. 장착 무기
   기관단총(감지 2.8)·레이저피스톨(감지 2.5)의 데이터와 정확히 일치한다.
6. 콘솔 Error 0건.

#### 새로 드러난 문제 (미수정 — 사용자 판단 필요)

**두 산탄총의 감지거리가 좀비의 공격 사거리보다 짧아졌다.**

| | 거리 |
|---|---|
| 좀비 실효 공격 사거리 | **1.600 유닛** |
| 전투산탄총 감지거리 | 1.500 |
| 소드오프샷건 감지거리 | 1.300 |

즉 산탄총을 든 로봇은 **좀비에게 먼저 맞고 나서야 사격을 시작한다.** 원인은 무기만 ÷100으로
바꾸고 **몬스터 데이터(`monster_range`)는 여전히 ÷50 기준**이기 때문이다.
`monster_range`를 1.6 → 0.8로 낮추면 `EffectiveAttackRange = Max(사거리, 몸 접촉 반경)`의
하한이 작동해 실효 **1.172유닛**(몸이 닿는 거리)이 되고, 산탄총(1.3~1.5)이 다시 우위에 선다.
다만 이는 좀비 기획서 Ver04 반영값을 건드리는 일이라 사용자 확인 없이 진행하지 않았다.

### 인게임 모든 이미지 1/2 축소 + 배경 미러 타일 4장 (2026-08-10)

사용자 지시 2건:

> * 인게임 모든 이미지 1/2 크기로 들어가도록 수정.
> * 배경또한 1/2크기로 줄이되, 4개 이어붙여 확장

**카메라(`orthographicSize=5.4`)는 건드리지 않았다.** 화면에 보이는 월드 범위(19.2x10.8)는
그대로 두고 그 안의 그림만 절반으로 줄였으므로, 캐릭터 기준으로 시야와 맵이 2배 넓어진다.
배경을 절반으로 줄이면 맵이 반토막 나므로 4장을 이어붙여 맵 크기를 유지한다.

#### 1) 절반으로 줄인 것

| 대상 | 방법 | 값 |
|---|---|---|
| 플레이어 로봇 | `Ground01`의 `Player.localScale` | 0.43812 → **0.21906** |
| 적 6종 + 보스 | 프리팹 루트 스케일 | 1.0 → **0.5** (보스 0.4026 → 0.2013) |
| 아이템 상자 픽업 | `DropItemManager.BoxWorldSize` | 1.2 → **0.6** |
| 보상 픽업(골드/경험치/부품상자) | `RewardPickupManager.VisualWorldSize` | 0.7 → **0.35** |
| 스피터 투사체 | `SpitterUnit.projectileVisualSize` | 0.35 → **0.175** |
| 무기 투사체·빔 | `WeaponTableGenerator`의 `atsize` (원거리 10종) | 0.2~0.5 → **0.1~0.25** |
| 배경 | 아래 2) 참고 | 스케일 **0.5** |

- **`ProceduralVisual`이 아니라 `Player`를 줄인다**(이전 항목과 같은 이유) — 시각·콜라이더·
  무기 소켓이 한 번에 따라온다. 무기 이미지도 `PlayerShootManager`가 로컬 스케일로 정규화하므로
  부모가 절반이면 자동으로 절반이다.
- **적 콜라이더는 로컬 값이라 루트 스케일에 자동 비례**한다(좀비 월드 콜라이더 2.0x1.6 → 1.0x0.8).
- **근접 무기의 `atsize`(1f)는 `MeleeSwing`이 쓰지 않는 미사용 값**이라 그대로 뒀다.
- 그림이 아닌 값(습득 거리 `PickupRange`, 보스 광역 공격 반경, 무기 사거리·감지거리,
  이동속도)은 **손대지 않았다.**

#### 2) 배경 - 미러 타일 4장

`map` 오브젝트의 SpriteRenderer를 `Tiled` 드로우 모드로 2x2 반복시켜 봤더니 **타일 경계에
흰 십자 줄**이 그대로 나타났다(2026-08-08에 사용자가 "타일 반복 중단"을 택했던 바로 그 문제).

**원인을 픽셀로 실측했다.** 원본 PNG의 **가장자리에서 3번째 픽셀 열/행이 밝다**
(밝기 0.518~0.520 vs 내부 0.474~0.481). 이미지 둘레에 밝은 테두리가 한 줄 있는 것이며,
이것이 이어붙인 경계에서 두 겹으로 겹쳐 흰 줄로 보인다.

해결은 두 단계다.

1. **미러 타일링** — 4장을 `flipX`/`flipY` 조합으로 배치해 경계에서 **같은 픽셀 열이
   마주보게** 했다. 이러면 이음매의 색 불연속(원본이 seamless가 아닌 문제)이 원천적으로 사라진다.
   - `map_tile_LB`(-9.58,-5.38, flip 없음) / `RB`(+9.58,-5.38, flipX) /
     `LT`(-9.58,+5.38, flipY) / `RT`(+9.58,+5.38, flipX+flipY)
2. **밝은 테두리 잘라내기** — 미러링만으로는 밝은 테두리가 두 겹이 되어 여전히 줄이 남는다.
   원본에서 상하좌우 **4px를 잘라낸 타일 전용 이미지**를 새로 만들었다
   (`Assets/Resources/ground_ruined_city_v2_tile.png`, 3832x2152, PPU=100·밉맵 끔).
   **원본 `ground_ruined_city_v2.png`는 지우지 않고 그대로 뒀다.**

결과 맵 크기는 **38.32 x 21.52유닛**(타일 1장 19.16x10.76의 2x2)이며 화면(19.2x10.8) 대비
정확히 2.00배다.

`map` 자신의 SpriteRenderer는 **지우지 않고 `enabled=false`로만 껐다** — 되돌리려면 다시 켜고
자식 타일 4개를 지우면 된다.

#### 3) `MapBounds`가 자식 타일까지 합산하도록 수정

배경이 렌더러 1개 → 자식 4개 구성으로 바뀌면서, 부모의 렌더러만 읽던 `MapBounds`가 맵을
1/4로 인식하는 문제가 생긴다. `ResolveRenderer()`를 **`TryResolveBounds()`** 로 바꿔
`GetComponentsInChildren<SpriteRenderer>()`를 `Encapsulate`로 합치게 했다.
**꺼져 있는 렌더러는 제외**하므로 위에서 비활성화한 원본 렌더러가 끼어들지 않는다.

#### 검증 (MCP 플레이모드, 전부 실측)

1. 배율 실측: 로봇 몸통 **0.5000** / 좀비 **0.5000** / 배경 타일 **0.5000**.
   다리 파츠도 `lossyScale`이 정확히 절반(허벅지 0.471→0.2355, 정강이 0.576→0.2880,
   발 0.434→0.2170)이라 **외곽선 두께 일치가 유지**된다.
2. 콜라이더: 좀비 월드 1.000x0.800, 플레이어 0.657x0.372 — 둘 다 정확히 절반.
3. **이음매 정량 측정**: 가로 이음매(y=540) 주변 밝기 편차 **-0.36%** = 줄 없음.
   세로 이음매에서 처음 +4.83%가 나왔으나, 이는 **화면 정중앙에 서 있던 흰색 로봇** 때문이었고
   로봇을 비켜 측정하면 편차가 사라진다(스크린샷으로도 십자 줄이 완전히 사라진 것을 확인).
4. 맵 경계: `MapBounds.WorldBounds` = **38.32 x 21.52**(자식 4장 합산 정상).
   맵 우상단으로 이동시켰을 때 카메라가 (9.56, 5.36)에서 멈추고 **맵 밖이 보이지 않는다.**
5. 무기 테이블 재생성 후 검증 8개 전부 통과, 콘솔 Error 0건.

#### 새로 생긴 문제 (미수정 — 사용자 판단 필요)

**좀비가 몸 크기의 2.73배 거리에서 때린다.**

| | 값 |
|---|---|
| 좀비 실효 공격 사거리 | **1.600 유닛** (변경 없음) |
| 두 몸이 실제로 닿는 거리 | 1.172 → **0.586 유닛** (몸이 절반이 되어서) |
| 비율 | **2.73배** |

이미지만 절반으로 줄이고 `monster_range`(÷50 환산 잔재)는 그대로 두었기 때문이다.
2026-08-10 "좀비 교전 거리 수정" 항목에서 3.350 → 1.600으로 고쳤던 문제가 형태를 바꿔
되살아난 셈이다. **`monster_range`를 1.6 → 0.8로 낮추면** `EffectiveAttackRange = Max(사거리,
몸 접촉 반경)`의 하한이 작동해 실효 0.8유닛(몸의 1.37배)이 되어 해소된다.
직전 항목에서 제안한 것과 같은 수정이며, 좀비 기획서 Ver04 반영값이라 확인 없이 진행하지 않았다.
같은 이유로 무기 사거리·감지거리와 이동속도도 몸 크기 대비 2배가 된 상태다.

## 2026-08-12 사용자 버그 리포트 6건 수정

사용자가 플레이 중 발견한 버그/밸런스 문제 6건을 접수해 조사·수정했다. Unity MCP로 에디터에
직접 접속해 `script-execute`로 씬을 조작하고 `screenshot-isolated`로 실측 스크린샷을 찍어
확인한 뒤 수정했다(`editor-application-*`·`screenshot-game-view`는 이번 세션에서도 여전히
로드되지 않아 이 방식을 사용했다 - 프로젝트 안내.md 기존 참고 항목과 동일한 제약).

### 1) 아이템 획득 반경이 PC 크기보다 과하게 큼

`RewardPickupManager.PickupRange`(골드/경험치/부품상자)와 `DropItemManager.PickupRange`(아이템
상자) 둘 다 반지름 **2유닛**으로 고정돼 있었다. 현재 플레이어 콜라이더 실측(월드 기준
0.657 x 0.372, 즉 반폭 약 0.33유닛)과 비교하면 획득 반경이 몸 반폭의 **6배**에 달해, 몸에서
한참 떨어진 보상도 저절로 빨려들어왔다. 두 값 모두 **1유닛**으로 축소.
(`RewardPickupManager.cs`, `DropItemManager.cs`)

### 2) PC 이동속도가 너무 빠름

`GameDataAsset.asset`의 로봇 100001(컴스톡 MK-1) `robot_speed` = **5** (유닛/초, 변환 없이
그대로 `rb.linearVelocity`에 곱해짐). 2026-08-10 "인게임 모든 이미지 1/2 축소" 작업 당시
"그림이 아닌 값(이동속도 포함)은 손대지 않았다"고 명시적으로 기록돼 있던 값 중 하나로,
몸이 절반으로 줄어든 뒤에도 그대로 남아 몸 크기 대비 과속 상태였다. **5 → 2.5**로 축소
(에셋 직접 수정, `assets-modify`).

### 3) PC 구르기 거리가 너무 김

`PlayerRobotController.dashDistance` = **5유닛**. 이 값은 2026-08-09에 몸이 지금의 2배였을
때(3.5→5.0, +43%) 튜닝된 값이라 마찬가지로 2026-08-10 이미지 축소에서 보정되지 않고
남아 있었다. **5 → 2.5**로 축소(지속시간 0.28초는 유지 - 구르기 속도 자체는 17.9→8.9유닛/초로
같이 느려진다). 코드 기본값과 `Ground01.unity` 씬 직렬화 값 양쪽 수정.

### 4) 'Player-ProceduralVisual-초록색 선'이 충돌체인지 확인 → 충돌체 아님

`ProceduralCharacterRig.cs`의 `OnDrawGizmosSelected()`(697~716행)에서 `Gizmos.color =
Color.green`으로 그리는 지면 표시선이다. `#if UNITY_EDITOR`로 감싸져 있고 Scene 뷰에서 해당
오브젝트를 **선택했을 때만** 보이는 순수 디버그 시각화(Gizmo)로, Game 뷰·빌드에는 전혀
나타나지 않으며 물리 충돌 판정과도 무관하다(Gizmos는 렌더링 전용, Physics 시스템이 전혀
참조하지 않는다). 실제 충돌체(BoxCollider)는 `Player` 루트 오브젝트에만 있고
`ProceduralVisual`이나 그 자식(LegsGroup 등)에는 없음을 씬에서 직접 확인했다 - 코드 주석의
"이 리그 밑에는 Collider를 두지 말 것" 원칙이 실제로 지켜지고 있다. **코드 수정 없음.**

### 5) 컴스톡 신발이 다리 하단부(정강이)보다 레이어가 위로 배치되는 문제

`script-execute`로 `ProceduralCharacterRig.Build()`를 에디트 모드에서 직접 호출하고
`screenshot-isolated`로 확인한 결과, 정강이(LegLower)가 **완전히 안 보이고** 허벅지에서
곧바로 신발로 이어지는 것을 실측으로 재현했다. 원인은 `footAnkleAnchor.y = 0.62` -
2026-08-10에 "신발을 신은 모습"을 만들려고 0.88(신발 입구)에서 0.62로 내렸던 값인데, 신발
텍스처 안에서 이 지점이 신발 몸통 중간쯤이라 신발 이미지의 약 38%(입구부터 이 지점까지)가
발목 연결점 **위로** 솟아올라 정강이 전체를 가려버렸다 - 의도한 "발목만 살짝 파묻힘" 수준을
크게 넘어선 상태였다. **0.62 → 0.85**로 올려 재검증(스크린샷으로 정강이가 무릎~발목까지
정상적으로 보이고 신발은 발목 부분만 살짝 덮는 것을 확인). 코드 기본값과 씬 직렬화 값 양쪽 수정.

### 6) 좀비 이동 방향이 바라보는 반대로 적용된 문제 → 실제 로직 버그 확인·수정

`EnemyUnit.LateUpdate()`가 `body_sprite_renderer.flipX = horizontal_velocity < 0f`로
되어 있었다. `Enemy_zombie_S.png` 원본 이미지를 직접 확인한 결과 팔을 뻗은 자세가
**왼쪽**을 향한 상태로 그려져 있는데(코드 주석은 "오른쪽을 향한 상태로 취급"이라고 잘못
적혀 있었다), 부호가 반대라 오른쪽으로 이동할 때 왼쪽을 보고 왼쪽으로 이동할 때 오른쪽을
보는 상태였다. `horizontal_velocity > 0f`로 부호를 반전해 수정.

**검증**: 좀비는 `EnemySpawner.SpawnMonster()`가 프리팹에 `AddComponent`로 붙이는 방식이라
프리팹 자체에는 `EnemyUnit`이 없다 - 에디트 모드에서 `Zombie.prefab`을 인스턴스화하고
`AddComponent<EnemyUnit>()` + 리플렉션으로 `Awake()`/`LateUpdate()`를 직접 호출해 실제 스폰
경로와 동일한 상태를 재현했다. `rb.linearVelocity = (3, 0, 0)`(오른쪽 이동)일 때
`flipX = True`가 되어 스크린샷상 팔이 오른쪽으로 뻗은 것을 확인(수정 전에는 왼쪽으로 뻗은
채였을 것). 테스트 오브젝트는 정리하고 `EnemyUnit.ResetStaticCaches()`로 정적 캐시도 되돌렸다.

**공통 검증**: 씬 저장 후 `assets-refresh`로 재컴파일, 콘솔에 Error 0건 확인.
`Ground01.unity`는 저장 후 `IsDirty=false` 확인.

## 2026-08-12 좀비 이동(걷기) 애니메이션 적용 + 타이틀 배경 이미지 적용

### 좀비 이동 애니메이션 8프레임 적용

사용자가 채팅에 붙여넣은 좀비 걷기 사이클 8장을 처음엔 이 환경에서 파일로 직접 접근할 수
없어 못 받았다(2026-08-10 ZombieAttack 때와 같은 제약 - 프로젝트 안내.md 참고). 이번에는
사용자가 `Assets/Resources/ZombieMove/`에 직접 `walk_left_f0~f7.png`로 넣어줘서 해결됐다.

- **규격 확인**: 8장 전부 **250x250** — `Enemy_zombie_S.png`(기본 좀비 정지 이미지)와 정확히
  같은 규격이라 별도 스케일 보정이 필요 없었다(ZombieAttack 프레임은 300x250이라 약간
  달랐던 것과 대조적).
- **import 설정**: 새로 추가된 파일이라 `.meta`가 없어(Unity가 아직 인식 못한 상태)
  `assets-refresh`만으로는 안 잡혔고, `AssetDatabase.ImportAsset(..., ImportRecursive |
  ForceUpdate)`를 직접 호출해야 인식시킬 수 있었다. 이후 `TextureImporter`를
  `Enemy_zombie_S.png`와 동일하게(Sprite/Single, PPU 100, pivot 중앙, 밉맵 끔) 맞췄다.
- **코드**: `EnemyUnit.cs`에 `ZombieMoveFrames`(파일명 오름차순 캐시, `ZombieAttackFrames`와
  동일 패턴)와 `UpdateWalkAnimation()`을 추가했다. 좀비가 실제로 이동 중(평면 속도가
  `FacingVelocityThreshold` 이상)일 때만 `walkFrameFps`(기본 6, 8프레임이라 초당 0.75바퀴)
  속도로 프레임을 순환시키고, 멈추거나 공격 모션 중이면 `original_body_sprite`(정지 이미지)로
  되돌린다. 적용 대상은 공격 프레임과 같은 조건(`CanPlayZombieAttackFrames()`를 그대로
  재사용 + `!IsAttacking && !IsDead`) — 기본 좀비(MonsterId 200001)에만 적용되고 차저 등
  다른 규격 몬스터는 기존 정지 스프라이트 그대로다.
- **LateUpdate 구조 변경**: 기존에는 `Mathf.Abs(horizontal_velocity) < FacingVelocityThreshold`일
  때 메서드 전체가 조기 반환해 `flipX`도 걷기 애니메이션도 갱신되지 않았다(순수 수직 이동 시
  둘 다 멈추는 버그가 될 뻔함). `flipX` 갱신문에만 조건을 걸고 `UpdateWalkAnimation()`은
  무조건 호출하도록 분리했다 - "입력이 없으면 마지막 방향 유지"라는 `flipX`의 기존 의도는
  그대로 유지된다.

**검증(실제 플레이모드)**: MCP로 `EditorApplication.isPlaying=true`를 호출해 실제 웨이브를
돌렸다. 처음 두 차례는 이 환경이 백그라운드에서 극단적으로 빠르게 실행돼(포커스 없는 상태의
Editor가 초당 500프레임 이상으로 도는 것을 확인, `frameCount`로 실측) MCP 호출 왕복 사이에
게임 시간이 수십 초씩 흘러버려 플레이어가 손쓸 새 없이 죽고 관찰 시점엔 이미 게임오버 상태였다
(전부 `EnemyUnit.FixedUpdate()`가 게임오버 시 속도를 0으로 죽이는 정상 동작이었음 - 애니메이션
버그가 아니었다). 세 번째 시도에서 리플렉션으로 `PlayerRobotController.IsDashing` 백킹
필드를 강제로 `true`로 만들어(`TakeDamage()`가 `IsDashing`이면 즉시 반환하는 기존 무적 판정을
그대로 이용) 관찰 도중 게임오버를 막고, 실제 스폰된 좀비들의 스프라이트가 `walk_left_f1`,
`walk_left_f3` 등으로 실시간 순환되는 것을 확인했다(정지해 있던 좀비는 여전히
`Enemy_zombie_S`). 테스트용 무적 설정은 플레이모드 종료로 자동 소멸(세이브되는 값이 아님).

### 타이틀(아웃게임) 배경 이미지 적용

사용자가 `Assets/Resources/UI/titleimage.png`(1672x941, 좀비 무리와 로봇이 대치하는 폐허
도시 일러스트)를 추가해 타이틀 화면 배경으로 요청했다.

- `Title.unity`의 `Canvas/Background`(`UI.Image`, `RectTransform`이 anchorMin(0,0)~
  anchorMax(1,1)로 이미 전체 화면을 덮도록 세팅돼 있었다 - 스프라이트만 비어 있던 상태)에
  스프라이트를 연결하기만 하면 됐다.
- `titleimage.png`는 Unity가 기본값(`textureType: Default`, Sprite 아님)으로 임포트해 놔서
  `Image.sprite`에 바로 못 쓰는 상태였다 - `TextureImporter`를 Sprite/Single로 바꾸고
  재임포트한 뒤 `Image.sprite`에 연결했다(`Image.type = Simple`, `preserveAspect = false`
  — 이미지가 이미 1672x941 ≈ 16:9라 캔버스 전체에 늘려도 왜곡이 없다).
- **검증**: `editor-application-*`·`screenshot-game-view` 계열 도구가 이번 세션에서도 로드되지
  않아(프로젝트 안내.md 기존 참고 항목과 동일), Canvas를 임시로 `ScreenSpaceCamera`로 바꿔
  1920x1080 `RenderTexture`에 `Main Camera`를 렌더링한 뒤 PNG로 저장해 확인하는 기존 우회법을
  다시 사용했다. 캡처 결과 배경이 전체 화면을 정상적으로 채우고 제목("컴스톡")·부제("웨이브
  서바이벌 · 로봇 모딩")·버튼("게임 시작"/"종료") 전부 배경 위에서 잘 읽히는 것을 확인했다.
  Canvas의 렌더 모드는 캡처 직후 원래 값(`ScreenSpaceOverlay` 등)으로 복원했다.

## 2026-08-12 무기 사거리 전면 상향

사용자 지적: "기본 무기 사거리가 너무 짧다. 이에 맞춰 전체적으로 모든 무기 사거리 밸런스
조절." 기본 장착 무기(`Ground01.unity`의 `ShootManager` 소켓 2정에 씬 값으로 박혀 있는
기관단총 300901 / 레이저피스톨 300701)의 사거리가 3.2 / 3.0으로, 화면 세로 반경(5.4) 대비
56~59%밖에 안 됐다.

- **원거리 10종(kind 1~10) range·detect를 전부 x1.3(30% 상향)**했다
  (`WeaponTableGenerator.cs` `Defs` 배열). 배율은 감지거리 최댓값(대물저격총 4.0→5.2)이
  `PlayerShootManager.max_detect_range`(5.3) 바로 밑에 오도록 역산해서 골랐다 - 이보다 더
  올리면 상한에 잘려 증가분이 낭비된다.
- **speed(투사체 속도)는 그대로 뒀다.** `Projectile.cs`를 확인한 결과 사거리는 순수
  `Vector3.Distance(spawn, current) >= MaxRange`로만 소멸을 판정하고 속도와 무관하므로,
  속도를 안 올려도 화면 밖 교전 같은 부작용이 없다(발사 시작 자체는 감지거리 5.3 상한이
  이미 화면 안으로 묶어둔다).
- **근접 3종(kind 11~13)은 이번에도 제외** — 화면이 아니라 적 몸 크기에 종속된 값이라는
  기존 원칙(2026-08-10) 그대로.
- 등급별로 사거리가 갈리지 않는 구조(무기는 등급마다 공격력만 오르고 사거리는 종류당 고정값)
  라 65행 전부가 아니라 13개 `Defs` 값만 고치면 됐다.

| 종류 | 사거리(이전→이후) | 감지거리(이전→이후) |
|---|---|---|
| 중기관총 | 3.5 → 4.55 | 3.0 → 3.9 |
| 전투산탄총 | 2.0 → 2.6 | 1.5 → 1.95 |
| 대물저격총 | 7.5 → 9.75 | 4.0 → 5.2 |
| 플라즈마캐논 | 4.0 → 5.2 | 3.5 → 4.55 |
| 로켓런처 | 3.5 → 4.55 | 2.4 → 3.12 |
| 소드오프샷건 | 1.8 → 2.34 | 1.3 → 1.69 |
| 레이저피스톨(기본) | 3.0 → 3.9 | 2.5 → 3.25 |
| 지정사수소총 | 5.0 → 6.5 | 3.5 → 4.55 |
| 기관단총(기본) | 3.2 → 4.16 | 2.8 → 3.64 |
| 유탄발사기 | 2.5 → 3.25 | 2.0 → 2.6 |

**검증**: MCP로 `WeaponTableGenerator.Generate()` 직접 호출(메뉴 `Comstock/무기 테이블 65행
재생성`과 동일) → 무기 65행/무기메타 65행/상점 65행 정상 재생성 로그 확인. 이어서
`WeaponTableValidator.Validate()`(메뉴 `Comstock/무기 테이블 검증`)로 정합성 8개 검사 전부
통과, 콘솔 Error/Warning 0건 확인. 씬 파일은 건드리지 않았다(무기 ID 자체는 그대로이고
`GameDataAsset.weapons`의 range/detect 값만 바뀌었으므로 `ShootManager` 소켓 설정은
자동으로 새 값을 읽는다).

**공통 검증**: `Title.unity`·`Ground01.unity`(좀비 걷기 애니메이션 관련 코드는 씬 값 변경
없음, 코드만 수정) 저장, 콘솔 Error 0건.

## 2026-08-12 디스크 시스템 전면 구현 (`20260810_디스크기획서_Ver01_김재원.pdf` 반영)

사용자가 기획서 PDF와 함께 "Assets/Resources/Discs 에 있는 디스크 이미지 적용, 기획서 확인하고
디스크 효과와 텍스트 적용"을 요청. 기존 디스크 시스템은 "상승 스탯 하나 + 하락 스탯 하나"만
표현 가능한 placeholder 8종(400001~400008, 이름부터 기획서와 무관)뿐이었는데, 실제 기획서
21종은 대부분 처치 시 발동/시간제/조건부/확률형이라 데이터 구조부터 다시 설계했다.

### 아이콘-효과 매칭

`Assets/Resources/Discs/디스크01~21.png`는 파일명에 효과 정보가 없어(단순 번호), 21장을 전부
직접 열어 그림(색상·문양)과 기획서의 아이콘 설명을 대조해 매칭했다(예: 노랑+번개무늬=교향곡:
번개, 카모 무늬=위장, 성운 무늬=은하수, 브러시드 메탈=금속음). 파랑 계열 3종(물 빠지는 소리/
교향곡:파도/결정의 마찰음 후보였던 서킷보드 무늬)은 확실한 단서가 부족해 색 계열로 근사
배정했다 - `DiscTableGenerator.cs`의 클래스 주석에 매칭 근거와 함께 "기획자 확인 필요 시
iconName만 바꾸면 됨"을 남겨뒀다.

### 데이터 구조 확장 (`DiscData.cs`)

`DiscEffectType` enum(15종)을 신설해 21개 디스크의 효과를 "모양"별로 분류했다(무기의
`WeaponFireMode`와 같은 접근 - 종류별 클래스 대신 데이터+해석자):
`StatModifier`(영구 스탯 가감, 기존 방식) / `OnKillChainLightning` / `OnKillTempMoveAtkSpeed` /
`OnKillTempDefDodge` / `OnKillHeal` / `OnKillStackStat`(처치마다 누적, cap 있음) /
`OnAttackChanceBonusDamage` / `PassiveAuraSlow` / `CritChancePerDisc` / `LastStand` /
`PeriodicHeal` / `MoveSpeedWhenNotAttacking` / `OscillatingAtkDef` / `WaveShieldMaxHp` /
`SkillCooldownReduction`(필살기 미구현이라 현재 소비처 없음, 값만 보관).

statA/B/C + amountA/B/C(스탯 파라미터), flatValue/chance01/multiplier/cap/interval/duration/
radius/maxUses(범용 파라미터)로 21종 전부를 표현한다. `Assets/Scripts/Editor/
DiscTableGenerator.cs`(메뉴 `Comstock/디스크 테이블 21종 재생성`)가 `ShopCatalog.discs`를
채운다(`WeaponTableGenerator`와 동일 패턴). ID는 `400000+파일번호`.

**등급/가격**: 기획서에 디스크별 등급이 명시돼 있지 않아(예시 스크린샷의 "에픽" 태그는 UI
목업일 뿐 전체 배정표가 아님) 효과 강력함을 보고 5등급에 나눠 배정했다(Normal 4 / Rare 5 /
Epic 6 / Unique 4 / Legendary 2). 가격은 기존 placeholder 8종의 등급별 가격
20/35/55/80/120을 그대로 이어받았다. **전부 밸런스 미확정 임시값.**

**% 수치 환산**: 회피율/치명타확률/치명타피해/행운/골드획득량처럼 이미 0~100 스케일인 스탯은
기획서의 "%"를 그대로 %p로 적용했지만, 이동속도(기준 2.5)·공격력(기준 3)처럼 절대값 스탯은
로봇 100001(컴스톡 MK-1) 기준값 대비 %로 환산한 절대치를 대신 넣었다 - 이 프로젝트의 스탯
보너스 체계(CoreStatBonuses/DiscStatBonuses/PartStatBonuses)가 전부 가산식이라 진짜 곱연산
%를 지원하지 않기 때문(다른 무기·디스크 수치와 같은 전제). 로봇 기준 스탯이 크게 바뀌면 같이
재조정해야 한다.

### 새 훅 지점 (기존 코드 최소 침습)

기존 시스템에 없던 개념이 여럿이라 여러 파일에 훅을 추가했다:
- **`EnemyUnit.OnKilledByPlayer`**(신규 static 이벤트) - `Die()`가 `Destroy()` 직전에
  발동. 이 게임은 플레이어만 데미지를 줄 수 있어 "적이 죽음 = 플레이어가 처치함"이 항상
  같으므로 별도의 "누가 죽였는지" 구분이 필요 없었다.
- **`PlayerRobotController`**: `ApplyTempStatBonus(stat,amount,duration)` + `GetTempStatBonus(stat)`
  (시간제 버프, `List<(StatType,float,float expireTime)>`로 보관하고 조회 시점에 만료분을
  정리), `Heal()`, `SetConditionalMoveSpeedBonus()`(위장), `SetShieldMaxHp()`/`RefillShield()`
  (에너지 베리어). `TakeDamage()`에 마지막 발악 훅(체력이 0 이하가 되기 직전에
  `DiscEffectRuntime.TryTriggerLastStand()`를 확인)과 실드 흡수 로직을 추가했다.
- **`PlayerShootManager`**: `ComputeDamage()`에 777(공격 시 확률 데미지 증폭) 훅과
  `player_stats.GetTempStatBonus(Atk)`(공명의 소리) 반영. 공격속도라는 로봇 스탯이 원래
  없어서 `ApplyTempAttackSpeedBuff()`(광분 바이러스 전용 발사 대기시간 배율)를 신설했다.
  `IsTargetingEnemy` 프로퍼티(위장 디스크가 "공격 중" 여부를 판단하는 데 씀)도 추가.
- **`EnemyUnit`**: `SetAuraSlowMultiplier()`(물 빠지는 소리) - FixedUpdate의 이동속도 계산에
  곱해진다.
- **`RewardPickup.CollectImmediately()`**: 골드 수령 시 `RunState.DiscStatBonuses[GoldGain]`
  (금화의 잔향)을 곱한다. `StatType.GoldGain` 신설 - 로봇 스탯이 아니라
  `RobotStats.ApplyBonus`가 조회하지 않으므로 안전하게 무시된다.
- **`ShopManager.EquipDisc()`**: 상시 스탯(`StatModifier`, `PassiveAuraSlow`의 자기 회피율
  성분)만 장착 즉시 `RunState.DiscStatBonuses`에 반영하고, `LastStand`는 사용 횟수를
  시드하고, `CritChancePerDisc`(염동력)는 착용 디스크 총 개수 기준으로 기여분을 재계산한다
  (장착할 때마다 이전 기여분을 빼고 새로 더해 이중 적용을 막는다).

### 신규 `DiscEffectRuntime.cs` (Player에 HitFlash와 같은 방식으로 자동 부착)

나머지 전부(처치 시 발동 디스패치, 주기 효과, 오실레이션, 아우라, 조건부 이동속도, 웨이브
시작 시 실드 리필)를 처리한다. 같은 디스크를 여러 장 장착해도(기획서 "디스크는 중복하여
착용 가능") 디스크별 독립 타이머 대신 장 수(`CountCopies`)를 수치에 곱하는 방식으로
단순화했다 - 예: 이끼 낀 2장 = 5초마다 회복량 2배.

### 검증 (실제 플레이모드, MCP)

`ShopManager.EquipDisc()`를 리플렉션으로 직접 호출해 7종(스탯형/온킬-번개/온킬-스택/공격시
확률/주기/1회성/아우라)을 강제 장착하고 실제 웨이브를 돌렸다. 결과:
- `RunState.NotifyChanged()`를 안 부르면 스탯이 안 갱신되는 것을 먼저 확인(테스트 아티팩트였고
  실제 구매 흐름인 `TryPurchaseDisc`는 이미 호출하고 있어 문제 없음) - 수동 호출 후 `Luck=7`로
  정상 반영 확인.
- 실제 좀비들이 죽는 과정에서 번개 연쇄·처치 스택·주기 회복·아우라 감속이 여러 차례
  발동했지만 콘솔 **Error 0건**.
- "마지막 발악"이 정확히 1회 발동한 뒤(`DiscUsesRemaining` 1→0) 이후 히트에는 정상적으로
  게임오버로 이어지는 것을 확인 - 무적 남용도, 미발동도 아닌 정확히 설계한 대로 동작.

### 텍스트 적용

`DiscData.BuildDescription()`이 이제 기획서 "효과:" 문구를 그대로 반환한다(예전엔
"공격력 +3 / 방어력 -2" 같은 자동 생성 문자열이었다). 상점 카드(`ShopManager.Offer.
BuildDescription()`)는 기존 호출부 그대로라 자동으로 새 문구가 나온다. 정비 화면
"장착된 디스크" 목록(`ShopPanelUI.cs`)에는 이름 밑에 효과 설명을 작게 추가로 표시하도록
수정했다.

### 미적용 - 상점 카드/장착 목록의 아이콘 시각 표시

아이콘 **스프라이트 자체는** `Discs/디스크01~21`로 정확히 매핑돼 `DiscData.LoadIcon()`으로
불러올 수 있게 완료했지만, 상점 UI(`ShopPanelUI`)가 무기·디스크 가리지 않고 처음부터
텍스트 전용(Image 컴포넌트가 카드 어디에도 없음)이라 실제로 화면에 아이콘을 그리려면
카드 레이아웃 자체를 다시 짜야 한다(무기도 아이콘이 없어서 디스크만 넣으면 카드마다
있다/없다가 들쭉날쭉해진다). 이번 작업 범위에서는 보류하고, 다음 후보 작업으로 남긴다
- 필요하면 무기 아이콘 체계와 함께 상점 카드 UI를 별도로 설계하는 편이 낫다.

**공통 검증**: 씬 변경 없음(스크립트 + `ShopCatalog.asset` + 21장 텍스처 임포트 설정만
바뀜), `assets-refresh` 재컴파일 콘솔 Error 0건, `WeaponTableGenerator`/`WeaponTableValidator`
등 기존 시스템에 회귀 없음(디스크만 건드림).

## 2026-08-12 밸런스 조정 3건 + 빌드에서 히트플래시가 사라지는 버그 수정

사용자 요청 4건 처리.

### 1)~3) 밸런스 수치 조정 (`GameDataAsset.asset`, Unity MCP `assets-modify`로 직접 수정)

- **PC 이동속도 20% 상승**: 로봇 100001(컴스톡 MK-1) `robot_speed` 2.5 → **3.0**
- **좀비 이동속도 20% 하향**: 몬스터 200001(좀비) `monster_speed` 2.5 → **2.0**
- **좀비 체력 30% 하향**: 몬스터 200001(좀비) `monster_hp` 100 → **70**

**주의**: "좀비"라는 명칭 그대로 몬스터ID 200001(좀비)에만 적용했다. 이 프로젝트는
2026-08-10 좀비 기획서 Ver04 반영 이후 좀비/차저/스피터/스프린터/디스럭터/리더 6종이
분리된 별개 이름을 가지므로, 차저 등 다른 종은 건드리지 않았다(과거 2몬스터 체계 시절엔
"좀비 체력"을 조정하면 차저도 비율 유지로 같이 조정했었지만, 지금은 각자 이름이 있는
별개 종이라 그 관례를 적용하지 않았다). 만약 6종 전체 또는 차저까지 의도한 것이었다면
알려주면 추가로 조정한다.
로봇/몬스터 스탯은 `RobotStats`/`EnemyUnit.Awake()`가 `GameDataAsset`을 매번 직접 읽는
구조라 씬(`Ground01.unity`)에는 값이 중복 저장되어 있지 않음을 확인했다(그렙으로 재확인,
씬 수정 불필요 - 2026-08-12 앞선 항목의 `dashDistance`처럼 씬 직렬화 값이 별도로 있는
필드와는 다르다).

### 4) 빌드에서 피격 시 흰색 깜빡임(HitFlash)이 사라지는 문제 수정

**증상**: 에디터 플레이모드에서는 정상 동작하는데(2026-08-10에 URP 셰이더로 재작성해
확인 완료), 빌드한 실행 파일에서는 피격 연출이 아예 안 보인다는 리포트.

**원인**: `HitFlash.GetFlashMaterial()`이 `Shader.Find("Comstock/SpriteFlash")`로 셰이더를
찾아 `new Material(shader)`를 코드에서 즉석으로 생성하는 구조였다. 에디터에서는 프로젝트의
모든 셰이더가 항상 컴파일돼 있어 `Shader.Find`가 항상 성공하지만, **실제 빌드는 어떤
씬·프리팹·`Resources` 애셋도 참조하지 않는 셰이더를 스트리핑(제외)한다.** 이 셰이더는
`ProjectSettings/GraphicsSettings.asset`의 "Always Included Shaders" 목록에도 없고, 이
셰이더를 쓰는 머티리얼 애셋도 프로젝트에 없어(코드에서만 동적 생성) 빌드 시 완전히
제외되고 있었다 - `Shader.Find`가 빌드에서 null을 반환해 `warned_missing_shader` 경고
로그만 남기고 매 피격마다 조용히 아무 효과도 내지 않는 상태였다(에디터에서는 재현되지
않는 유형의 버그라 이전 검증에서 못 잡았다).

**수정**: 프로젝트 설정(`ProjectSettings/GraphicsSettings.asset`)을 건드리는 대신 -
**협업 규칙상 프로젝트 설정 변경은 사용자 확인이 필요**하고, `Resources` 폴더 방식이
더 원인에 가깝고 유지보수하기 쉽다고 판단해 - 이 셰이더를 쓰는 더미 머티리얼
`Assets/Resources/Materials/SpriteFlashBase.mat`을 새로 만들었다(Unity MCP
`assets-material-create`). `Resources/` 밑의 애셋은 참조 여부와 무관하게 항상 빌드에
포함되므로, 이 머티리얼이 포함되면 그것이 쓰는 셰이더도 함께 포함된다. `HitFlash.cs`의
`GetFlashMaterial()`을 `Shader.Find` 대신 `Resources.Load<Material>("Materials/
SpriteFlashBase")`로 베이스 머티리얼을 불러온 뒤 `new Material(baseMaterial)`로 인스턴스를
만들도록 변경했다(이후 `MaterialPropertyBlock`으로 렌더러별 `_FlashAmount`를 덮어쓰는
기존 로직은 그대로).

**검증**: `assets-refresh` 재컴파일 콘솔 Error 0건. `script-execute`로
`Resources.Load<Material>("Materials/SpriteFlashBase")`를 직접 호출해 머티리얼이 null이
아니고 `shader.name == "Comstock/SpriteFlash"`, `isSupported == True`임을 실측 확인했다.
**실제 빌드 산출물로 재생을 검증하지는 못했다**(이 세션은 에디터 MCP 도구로만 검증
가능 - 프로젝트 안내.md의 기존 도구 제약 참고). `Resources.Load` 성공 여부는 에디터·빌드
동일하게 동작하는 API라 이 검증으로 근본 원인(빌드 스트리핑)이 해소됐다고 판단하지만,
사용자가 실제로 빌드해 재확인하는 것을 권장한다.

**공통 검증**: 씬 변경 없음(데이터 애셋 3필드 + 스크립트 1개 수정 + 머티리얼 애셋 1개
신규 생성), 콘솔 Error 0건.

## 2026-08-12 근접무기 3건 + 사용자 버그 리포트 다수 처리

사용자가 연속으로 8건을 접수(근접무기 3건 + 다리 음영·상점 잠금·좀비 애니메이션/투사체·
자석 연출 등). Unity MCP로 에디터에 직접 접속해 각 항목을 실측·검증했다.

### 1) 근접무기(생존단검/전술 마체테/전기톱검) 방향/사거리/찌르기 애니메이션

**원인 진단**: `weapon_imgangle`이 13종 전부 0으로 방치돼 있었다(이전부터 "다음 후보 작업"에
적혀 있던 항목). 세 원본 그림을 Python(PIL)으로 주성분축 실측한 결과 칼끝이 우측(0도)이 아니라
좌상단(생존단검 140.2도 / 마체테 145.0도 / 전기톱검 141.3도)을 향하고 있었다. 게다가 근접무기는
좌우 이미지가 완전히 같은 파일인데(`leftImg == rightImg`), 조준 계산에 슬롯별 `rotation_offset_degrees`
(총기 전용으로 튜닝된 값, 소켓0=126.3도/소켓1=37도로 서로 다름)가 그대로 더해지고 있어서
같은 무기라도 어느 손에 장착하느냐에 따라 칼끝 방향이 달라지는 구조였다.

- `PlayerShootManager.UpdateSlot()`: `weapon_firemode == MeleeSwing`이면 `slot.rotation_offset_degrees`를
  더하지 않고 `weapon_imgangle`만 적용하도록 분기(주석으로 이유 명시). `ApplyAngleFlip`(상하 반전)도
  근접무기는 건너뛴다 - 총기 전용 각도 범위라 근접에 적용하면 오히려 부자연스럽다. 총이 장착됐다가
  근접무기로 바뀔 때 flipY/localRotation 잔류값을 매 프레임 강제로 정상화하는 방어 코드도 추가했다.
- `WeaponTableGenerator.cs`: `WeaponDef`에 `imgangle` 필드 신설, 근접 3종에 실측값의 반대 부호
  (-140/-145/-141)를 채우고 `Generate()`가 `weapon_imgangle`에 반영하도록 수정.
- **사거리**: 실측 결과(플레이어 반폭 0.33유닛, 좀비 반폭 0.5유닛, 무기 소켓이 몸통에서 약
  1.0유닛 거리) 기존 값(2.2~2.6)도 이미 접촉거리(0.83유닛)보다 훨씬 컸지만, 사용자가 "좁다"고
  체감했으므로 40~50% 상향했다: 생존단검 2.2→3.3 / 마체테 2.6→3.9 / 전기톱검 2.4→3.6
  (감지거리도 동일하게, 근접무기는 사거리=감지거리 관례를 유지).
- **찌르기 애니메이션**: 기존엔 `MeleeSwing.Execute()`가 부채꼴 판정만 즉시 수행하고 시각적으로는
  무기가 가만히 있었다. `weapon_duration`(근접에서는 기존 미사용값 0)을 "찌르기 총 시간"으로,
  `weapon_atsize`(마찬가지로 미사용이던 1f)를 "찌르는 거리"로 재활용해 생존단검 0.16초/0.5유닛,
  마체테 0.22초/0.65유닛, 전기톱검 0.12초/0.55유닛으로 채웠다. `PlayerShootManager`에
  `StartMeleeThrustVisual`/`UpdateMeleeThrustVisual`을 신설해 무기 소켓(rig_point)의 <b>월드 위치</b>를
  0→1→0 삼각파로 조준 방향으로 밀었다 되돌린다(데미지 판정 자체는 그대로 스윙 시작 시점에 즉발 -
  연출만 분리). 구르기가 중간에 시작되면 찌르기 연출을 취소해, 애니메이션이 구르기 시작 전 위치로
  캐릭터를 다시 끌어당기는 어긋남을 방지했다.

**검증**: `WeaponTableGenerator.Generate()` + `WeaponTableValidator.Validate()` 재생성·검증 8개 통과.
에디트 모드에서 `RigingPoint.rotation`을 계산값대로 직접 세팅하고 `screenshot-isolated`로 촬영해
양쪽 소켓 모두 칼끝이 조준 방향(동쪽)을 정확히 향함을 실측 확인(스크린샷 2장). 콘솔 Error 0건.

### 2) "다리 음영이 반대인 것 같다" - 조사했으나 버그를 확정하지 못함

`ProceduralCharacterRig.BuildLeg()`가 다리 인덱스 0(`Leg_Back`, 로컬 -X 고관절)은
어둡게(`backLegTint` 0.80배) + 낮은 sortingOrder(뒤에 그려짐), 인덱스 1(`Leg_Front`, 로컬 +X)은
흰색 + 높은 sortingOrder(앞에 그려짐)로 정적으로 고정 배정하는 구조를 확인했다. `phase`/`gaitBlend`
필드를 리플렉션으로 직접 세팅해 걷는 자세를 재현하고 좌우 반전(`facingSign`) 양쪽에서
스크린샷을 비교한 결과, **"보행 방향으로 앞서 나가는 다리 = 밝은 색, 뒤처지는 다리 = 어두운 색"**
규칙이 좌우 반전과 무관하게 일관되게 유지됨을 확인했다 - 코드 자체에서 모순되는 지점을
찾지 못했다. 다만 이 배정은 **다리의 좌우 정체성에 고정**돼 있어(보행 위상과 무관), 원본 캐릭터
아트가 어느 쪽을 "카메라에 가까운 다리"로 의도했는지에 따라 반대로 배정됐을 가능성은 배제할 수
없다. **코드 수정 없이 보류** - 어떤 장면에서 어떻게 반대로 보이는지 스크린샷이나 좀 더 구체적인
설명을 받으면 바로 대응한다(2026-08-08 "무릎 미러링을 되돌림" 항목처럼, 다리 관련 시각 로직을
확신 없이 고쳤다가 되돌린 전례가 있어 이번엔 먼저 확인을 구한다).

### 3) 신발이 다리보다 앞에 그려지던 문제 수정

`BuildLeg()`에서 발(신발)의 sortingOrder가 `sortingBase + 2`(정강이보다 높음 = 앞에 그려짐)였던
것을 `sortingBase - 1`(정강이보다 낮음 = 뒤에 그려짐)로 바꿨다. 이전 2026-08-10/12의
`footAnkleAnchor` 조정은 조인트 "위치" 문제였고, 이번 건은 그리기 "순서" 문제로 별개다.
스크린샷으로 정강이가 신발 통 위로 겹쳐 보이는 것을 확인.

### 4) 좀비 걷기 애니메이션 속도가 이동속도와 무관했던 문제 수정

`EnemyUnit.UpdateWalkAnimation()`의 프레임 재생 속도(`walkFrameFps`)가 고정값이라 감속 오라·
넉백 등으로 실제 이동속도가 바뀌어도 다리 애니메이션은 항상 같은 속도로 돌고 있었다.
`walkFrameFps`를 "기준 이동속도(MoveSpeed)에서의 프레임 속도"로 재정의하고, 실제 속도와
MoveSpeed의 비율을 곱해 재생 속도를 결정하도록 수정(`speed_ratio = 실제속도/MoveSpeed`).

### 5) 좀비(스피터) 투사체 크기/속도 조정

`SpitterUnit`의 `projectileVisualSize`(0.175→0.35, 2026-08-10 이미지 일괄 절반 축소 때 가시성
검토 없이 줄었던 것을 원복) / `projectileSpeed`(12→9, 사용자 요청 25% 감소).

### 6) 상점 아이템 잠금이 새 웨이브에서 풀리던 문제 수정

`ShopManager.OpenForNewWave()`가 예전엔 "웨이브가 바뀌면 잠금이 의미 없다"는 전제로
`RerollOffers(respectLocks: false)`를 불러 매 웨이브 잠금을 전부 해제했다. 사용자가 "잠긴
아이템은 다음 웨이브에도 유지돼야 한다"고 요청해 `respectLocks: true`로 바꿨다(첫 진입 시엔
모든 칸이 비어있어 자연히 전부 새로 뽑히고, 이후 웨이브부터는 잠긴 칸만 남는다). 새로고침
(`TryRefresh`)은 원래부터 `respectLocks: true`로 잠금을 지키고 있었다(코드 검토로 확인, 별도
수정 불필요 - 사용자가 겪은 "새로고침에도 안 지켜진다"는 인상은 웨이브 전환 시 잠금이 풀리는
현상과 겹쳐 보였을 가능성이 있다).

### 7) 웨이브 종료 시 골드/경험치 "자석 연출"이 없던 문제 수정

`GameFlowManager.ResetFieldForIntermission()`이 필드에 남은 보상을 `CollectImmediately()`로
즉시(연출 없이) 흡수하고 있었다 - 코드 주석엔 "자석 흡수"라고 적혀 있었지만 실제로는 순간
증발이었다. `Time.timeScale`이 아직 1인 시점(정지 처리 이전)에 코루틴
(`ResetFieldForIntermissionRoutine` → `PlayMagnetFlightRoutine`)으로 바꿔, 픽업이 플레이어
위치로 0.35초에 걸쳐 ease-out 곡선으로 날아간 뒤(Collider는 애니메이션 시작 시 꺼서 도중
재수령 트리거를 막는다) 실제 수령·파괴가 일어나도록 했다. 애니메이션이 끝난 뒤에야
`Time.timeScale=0`과 정비 화면 전환이 이어진다. **검증**: 리플렉션으로 `HandleWaveEnded()`를
직접 호출해(`GameOverManager.Reset()` + `IsDashing` 강제 true로 무적 처리해 관찰 도중 게임오버를
막음) 골드 2→103, 경험치 4→48로 정상 증가하고 "정비 진입 - 필드 초기화..." 로그와
`timeScale=0`/`IsIntermission=True` 전환까지 예외 없이 완료됨을 실측 확인.

### 8) 좀비(200001) 이동속도 20% 추가 하향 요청 - 이미 처리됨 확인

같은 세션 앞부분에서 이미 `monster_speed` 2.5→2.0(20% 하향)을 적용했다. 이후 사용자가 같은
요청을 다시 보내 혼동이 있었는지 확인했으나, 값은 이미 2.0으로 반영돼 있어 추가 변경 없음
(중복 적용해 1.6으로 더 낮추면 사용자 의도와 다를 수 있어 보류 - 필요하면 알려달라고 안내).

### 9) 드랍 아이템 조사 - 사용되지 않는 구식 드랍 경로 발견(수정 보류, 사용자 확인 필요)

사용자 질문("골드/상자로 얻는 아이템 종류가 뭔지, 먹어도 효과 없는 경우가 많다")에 답하려고
`EnemyUnit.Die()`의 보상 경로를 추적한 결과, **완전히 분리된 두 시스템이 동시에 존재**함을
확인했다:
- **실제로 동작하는 경로**(`GrantKillRewards()` → `RewardPickupManager.SpawnReward()` →
  `RewardPickup.CollectImmediately()`): 골드/경험치는 `RunState.Gold`/`RunState.CoreExp`에 직접
  더해진다(확률 드랍 - 경험치 1/2, 골드 1/3). 부품 상자(`RewardType.PartBox`)는
  `PartBoxDropChance`(현재 0.3%로 낮게 설정돼 있음, 사용자 요청과 이미 일치)로 드랍되고,
  정비 화면에서 개봉하면 `ModdingManager.OpenAllBoxesIntoInventory()`가 상자 1개당 파츠 정확히
  1개를 뽑는 구조라 **"상자를 얻으면 반드시 파츠 하나 이상"이라는 요청도 이미 충족**돼 있다.
- **아무 효과도 없는 죽은 경로**(`DropTableManager.RollDrop(MonsterId)` → `DropItemManager.SpawnDrop()`
  → `ItemPickup.CollectImmediately()` → `PlayerInventory.AddItem()`): `GameDataAsset.drops`
  테이블(무기 300000번대/방어구 400000번대/골드 700000번대 ID)을 기반으로 별도의 "상자"가 필드에
  추가로 떨어진다. 그런데 `PlayerInventory`는 `PlayerInventory.cs` 자체 주석에 "실제 장비 장착/UI
  표시는 아직 없다"고 적혀 있는 대로, 주운 개수를 딕셔너리에 세기만 할 뿐 **어디서도 읽지 않는다**
  - 골드 대역(700000번대) 아이템조차 `RunState.Gold`로 이어지지 않는다(카테고리 표시 로직도
  `IsGold()`를 안 거쳐서 로그에 "아이템"으로 잘못 나온다). 실제 플레이 로그에서 "무기 300901
  획득"/"아이템 700001 획득"이 반복적으로 찍히는 것을 확인했다 - 매번 아무 효과 없이 소모되고
  있었다. **사용자가 말한 "먹어도 효과가 없는 경우가 많다"는 정확히 이 죽은 경로를 가리키는
  것으로 보인다.**

**수정 보류 이유**: 이 경로를 없앨지(더 이상 쓰이지 않는 구식 시스템으로 보고 `EnemyUnit.Die()`의
`DropTableManager`/`DropItemManager` 호출과 관련 스크립트를 정리) 살릴지(예: 무기/방어구 드랍을
`RunState`/상점 시스템과 연결해 실제 효과를 주도록 재설계)는 게임 디자인 판단이 필요해서
사용자 확인 없이 진행하지 않았다. 다음 대화에서 방향을 확정하면 바로 처리한다.

### (당시) 미완료: 상점 카드에 무기/디스크 아이콘 이미지 표시

기존 "미적용" 항목(2026-08-12 디스크 시스템 항목 참고)과 동일 - `ShopPanelUI`가 텍스트 전용
카드 구조라 아이콘을 넣으려면 레이아웃을 다시 짜야 한다. 이 세션에서는 착수하지 못했다
→ **바로 다음 항목에서 완료했다.**

**공통 검증**: 위 1)/6)/7) 항목은 `assets-refresh` 재컴파일 + 콘솔 Error 0건, 6)/7)은 리플렉션으로
런타임 동작까지 실측 확인했다. 4)/5)는 컴파일만 확인(런타임 실측은 이번 세션에서 반복적으로
겪은 "백그라운드 에디터 프레임 폭주로 관찰 전에 게임오버" 문제로 생략 - 프로젝트 안내.md 기존
참고 항목과 동일한 제약). 씬 파일(`Ground01.unity`) 자체는 수정하지 않았다(전부 스크립트/데이터
에셋 변경이며, 에디트 모드에서 검증용으로 만든 테스트 오브젝트·변경사항은 전부 씬을 다시 열어
디스크 상태로 되돌렸다).

## 2026-08-12 죽은 드랍 아이템 경로 제거 + 상점 카드 아이콘 표시

바로 위 항목에서 발견한 "죽은 드랍 경로"를 사용자가 **제거**로 확정했고, 상점 아이콘 표시도
**착수**를 요청해 같은 세션에서 이어서 처리했다.

### 1) 죽은 드랍 아이템 경로 완전 삭제

발견 당시 분석대로, `EnemyUnit.Die()`가 부르던 `DropTableManager.RollDrop()` →
`DropItemManager.SpawnDrop()` → `ItemPickup` → `PlayerInventory.AddItem()` 경로는 실제로는
아무 효과가 없었다(주운 개수를 세기만 할 뿐 소비처가 없음). 스크립트 4개를 완전히 삭제했다:
`DropTableManager.cs` / `DropItemManager.cs` / `ItemPickup.cs` / `PlayerInventory.cs`
(+ 각 `.meta`). 연쇄적으로 정리한 항목:
- `EnemyUnit.Die()`에서 드랍 호출 제거.
- `PlayerRobotController.Awake()`의 `PlayerInventory.Reset()` 호출 제거.
- `GameFlowManager`의 웨이브 종료 자석 연출(`ResetFieldForIntermissionRoutine`/
  `PlayMagnetFlightRoutine`)에서 `ItemPickup` 관련 로직(수집·카운트·로그) 제거 - 골드/경험치
  (`RewardPickup`)만 남았다.
- 데이터 모델까지 완전히 정리: `GameDataAsset.drops` 필드, `GameDataModels.DropEntry` 구조체,
  `GameDataManager.DropsByMonster`(및 채우는 코드) 전부 삭제. `GameDataAsset.asset`을 재저장해
  이제는 의미 없는 `drops:` YAML 블록도 파일에서 걷어냈다.
- `MapBounds`/`MeleeSwing`/`RunState`/`RewardPickupManager`/`RewardPickup`의 "이런 static
  유틸리티 패턴을 따른다"는 예시 주석이 삭제된 클래스들을 가리키고 있어서 남아있는 클래스
  이름으로 갱신했다.

**검증**: 그렙으로 프로젝트 전체(`Assets/`)에서 네 클래스명 참조가 완전히 사라졌음을 확인,
`assets-refresh` 재컴파일 콘솔 Error 0건.

### 2) 상점 카드에 무기/디스크 아이콘 이미지 표시

`ShopPanelUI.OfferSlotUI`에 `iconImage`(Image) 필드를 추가하고, `RefreshOffers()`가
`ResolveOfferIcon()`으로 오퍼 타입에 맞는 스프라이트를 채운다:
- **디스크**: 기존에 이미 매핑돼 있던 `DiscData.LoadIcon()`(`Discs/디스크01~21`)을 그대로 재사용.
- **무기**: 전용 아이콘이 없어서 `weapon_rgwpimg`(무기 장착 시 손에 드는 이미지와 동일한
  리소스)를 `Resources.Load`로 그대로 재사용했다 - 새 아트 없이도 무기 종류를 구분할 수 있다.
  `Resources.Load` 반복 호출을 피하려고 이름→스프라이트 캐시(`offer_icon_cache`)를 뒀다
  (PlayerShootManager의 sprite_by_name과 동일 패턴). 아이콘을 못 찾으면(null) 이미지를 꺼서
  빈 사각형이 안 보이게 한다.

**씬 작업**: 상점 카드 4개(`OfferCard1~4`)에 각각 `IconImage` 자식(RectTransform+Image, 카드
좌상단 x:[0.05,0.182] y:[0.74,0.96] 앵커 - 헤더 텍스트 줄 높이에 맞춘 정사각형에 가까운
칸)을 신설하고, 헤더 텍스트(등급·카테고리)는 `anchorMin.x`를 0.05→0.20으로 밀어 아이콘과
겹치지 않게 했다. 본문 텍스트(이름/스탯/가격, 3줄 이상이라 폭을 줄이면 줄바꿈이 깨질
위험이 컸다)는 건드리지 않아 회귀 위험을 최소화했다. `Image.preserveAspect=true`,
`raycastTarget=false`(카드 버튼 클릭을 가리지 않도록)로 설정. Unity MCP
`gameobject-create`로 빈 오브젝트를 만든 뒤 `gameobject-component-add`로 `RectTransform`을
추가하면(일반 `Transform`이 `RectTransform`으로 업그레이드됨) UI 오브젝트를 코드/MCP만으로
정상 구성할 수 있음을 확인했다.

**검증(실제 플레이모드)**: `screenshot-isolated`는 UI(Canvas) 오브젝트에 렌더러 바운즈가 없어
동작하지 않아("No Renderers found"), 기존에 문서화된 우회법(Canvas를 임시
`ScreenSpaceCamera`로 바꿔 `RenderTexture`에 렌더 후 PNG 저장)을 다시 썼다. 처음 시도는
카메라 컬링마스크를 안 바꿔서 3D 월드(좀비 등)가 UI와 뒤섞여 찍혔는데, 카메라
`cullingMask`를 `"UI"` 레이어만으로 좁히고 `planeDistance`를 0.3으로 낮추자 깨끗한 UI만
캡처됐다. 캡처 결과 4칸(디스크 2 · 무기 2)에서 아이콘이 전부 올바른 그림(디스크는 원반
아이콘, 무기는 손 이미지)으로 정상 표시되고 텍스트와도 겹치지 않음을 확인했다.

**공통 검증**: `assets-refresh` 콘솔 Error 0건, 씬은 아이콘 오브젝트 신설분만 저장했고
(`scene-save`) 플레이모드 테스트용 임시 조작(무적화, 게임오버 강제 리셋, 카메라 렌더 모드
임시 변경 등)은 전부 원래 값으로 복원한 뒤 플레이모드를 종료했다(재진입 시 자동 폐기).

## 2026-08-12 다리 음영 방향 반전

앞선 "다리 음영이 반대인 것 같다" 조사(코드는 자체 모순이 없어 보류)에 대해 사용자가 구체적으로
확정했다: "왼쪽을 볼 때 오른쪽 다리가, 오른쪽을 볼 때 왼쪽 다리가 더 밝아야 한다" - 즉 지금과
정반대. 실측(`Leg_Front`/`Leg_Back`의 실제 월드 X좌표 + `SpriteRenderer.color`를 직접 조회)으로
기존 배정이 정확히 그 반대(오른쪽을 볼 때 오른쪽 다리가 밝음)였음을 재확인했다.

**수정**: `ProceduralCharacterRig.Build()`에서 두 다리를 만드는 `BuildLeg()` 호출의 **밝기(색)와
그리기 순서(sortingOrder)만 서로 맞바꿨다** - 고관절 위치(`sideX`)와 보행 위상(`phaseOffset`,
`frontLegPullBack` 당김 포함)은 그대로 뒀다. 즉 "어느 물리적 다리가 앞다리 자세를 갖는가"는
안 바꾸고 "그중 어느 쪽이 밝게/앞에 그려지는가"만 뒤집었다 - 밝기 문제와 자세(당김) 문제는
서로 다른 축이라 자세 쪽은 사용자가 언급하지 않았으므로 건드리지 않았다.

**검증**: 에디트 모드에서 `Build()` 후 `phase`/`gaitBlend`/`facingSign`을 리플렉션으로 강제
설정해 보행 자세를 재현하고, 각 다리의 실제 `transform.position.x`(화면 좌/우 판정용)와
`SpriteRenderer.color`(밝기 판정용)를 직접 로그로 찍어 확인했다(스크린샷 눈대중은 20% 밝기
차이가 잘 안 보여 신뢰하지 않고 수치로 확인).
- 오른쪽을 향할 때(`facingSign=+1`): 흰색(밝은) 다리 worldX=-0.13(화면 왼쪽), 어두운 다리
  worldX=+0.12(화면 오른쪽) → **왼쪽 다리가 밝음** (요청과 일치)
- 왼쪽을 향할 때(`facingSign=-1`): 흰색 다리 worldX=+0.13(화면 오른쪽), 어두운 다리
  worldX=-0.12(화면 왼쪽) → **오른쪽 다리가 밝음** (요청과 일치)

양쪽 방향 모두 정확히 일치함을 확인했다. `assets-refresh` 재컴파일 콘솔 Error 0건. 테스트로
빌드한 리그 오브젝트는 씬을 다시 열어 디스크 상태로 되돌렸다(코드 수정만 남음).

## 2026-08-12 좀비 투사체 크기/속도 조정 + PC 피격 판정 범위 축소

사용자 요청 3건을 순서대로 처리했다: (1) 좀비가 쏘는 투사체 크기 3배, (2) PC 피격 판정 범위
축소, (3) 좀비 투사체 속도 25% 추가 하향.

### 1) 스피터 투사체 크기 3배 [완료]

`SpitterUnit.projectileVisualSize`(`EnemyProjectile.Spawn()`의 `visualSize` 인자로 전달돼
`transform.localScale`에 그대로 곱해짐)를 0.35 → **1.05**로 올렸다. 스피터가 유일하게 투사체를
쏘는 몬스터라 이 값이 "좀비가 쏘는 투사체"에 해당한다. `SpitterUnit`은 프리팹이 아니라
`EnemySpawner.MonsterComponentTypes`에 따라 런타임에 `AddComponent`로 붙는 컴포넌트라(코드
자체 주석 "런타임에 AddComponent로 붙는 경로에서도 확실히 적용" 참고) 씬/프리팹에 직렬화된
값이 따로 없어 코드 기본값 수정만으로 충분했다.

### 2) 스피터 투사체 속도 25% 추가 하향 [완료]

같은 파일의 `projectileSpeed`를 9 → **6.75**로 낮췄다(앞선 2026-08-12 세션에서 이미 12→9로
한 차례 낮췄던 것을 사용자가 재차 25% 추가 하향 요청).

### 3) PC(Player) 피격 판정 콜라이더 축소 [완료]

`Ground01.unity`의 `Player` `BoxCollider`(로컬 `size (3, 1.7, 0.01)` / `center (-0.1, 0.85, 0)`)를
Unity MCP로 직접 조회·수정했다. 실측 비교를 먼저 했다: `Player/ProceduralVisual/RigRoot/LegsGroup`의
월드 바운즈는 `size (0.603, 0.654)`인데 기존 콜라이더의 월드 크기는 `size (0.657, 0.372)`
(`Player.localScale`=0.219058 적용)로 **가로가 실제 다리 그림 폭보다 9% 더 넓었다**. 발밑 기준
앵커(로컬 y=0, 콜라이더 아랫변)는 유지한 채 크기를 로컬 `(2.1, 1.19, 0.01)` / `center (-0.07, 0.595, 0)`
로 30% 줄였다(월드 기준 가로 0.460·세로 0.261 - 이제는 다리 폭보다도 좁다).

이 콜라이더는 근접/투사체 피격 판정뿐 아니라 `EnemyUnit.BodyContactRadius()`(좀비 근접 공격
사거리의 하한)에도 그대로 쓰이므로, 좀비 공격 판정 거리도 함께 소폭 좁아진다(부작용이 아니라
"피격 범위가 넓다"는 요청과 같은 방향).

**검증**: `assets-refresh` 재컴파일 콘솔 Error 0건, `scene-save` 후 `IsDirty=false` 확인. 실제
플레이모드에서 피격 판정 체감이나 투사체 크기/속도를 육안으로 확인하지는 못했다(이번 세션에서는
생략) - 다음 플레이 테스트에서 확인 필요.

## 2026-08-12 근접무기 사거리 1.5배 상향 + 소켓 복귀 버그 수정

사용자가 데스크톱 앱 세션(Claude Code 세션 export zip으로 전달)에서 만들어 둔 플랜
("무기 소켓 개별화 + 소켓 불일치 무게/이동속도 패널티", 아래 항목)을 CLI에서 이어가기 전에,
새로 접수한 근접무기 관련 요청 2건을 먼저 처리했다.

### 1) 근접무기 사거리(=감지거리) x1.5 추가 상향

`WeaponTableGenerator.cs`의 근접 3종 `range`/`detect`를 1.5배 올렸다: 생존단검 3.3→**4.95**,
전술 마체테 3.9→**5.85**, 전기톱검 3.6→**5.4**(2026-08-12 앞선 세션에서 이미 40~50% 올렸던
값 위에 추가로 곱한 것). `Comstock/무기 테이블 65행 재생성` + `Comstock/무기 테이블 검증`
실행 완료 - 8개 검사 전부 통과, 콘솔 Error 0건.

### 2) 근접무기가 공격 후 소켓 위치로 복귀하지 못하던 버그 수정

**원인**: `StartMeleeThrustVisual`이 찌르기 시작 시점의 소켓 <b>월드 좌표</b>를 고정값
(`melee_thrust_home_world`)으로 찍어두고, 찌르기가 끝나면(t≥1) 그 고정값으로 되돌리고
있었다. 캐릭터(부모)가 스윙 도중(0.12~0.22초) 계속 이동하는 뱀서라이크 게임 특성상, 스윙이
끝나는 시점에는 캐릭터가 이미 그 고정 좌표에서 멀어져 있어 무기가 "몸에서 떨어진 채 공중에
남는" 것처럼 보였다. 수학적으로 재현: 부모가 스윙 도중 5유닛 이동한다고 가정하면 옛 방식은
복귀 위치가 기대값(캐릭터 기준 원래 소켓 자리)과 5유닛 어긋났고, 새 방식(아래)은 오차 0이었다
(`script-execute`로 임시 부모/자식 Transform을 만들어 두 계산식을 실측 비교, 콘솔 로그로 확인).

**수정**: 복귀 지점을 월드 좌표 스냅샷이 아니라 <b>부모 기준 로컬 좌표</b>(`melee_thrust_home_local`
= 찌르기 시작 시점의 `pivot.localPosition`)로 저장하고, 매 프레임 `pivot.parent.TransformPoint
(home_local)`로 "현재 부모 기준 정위치"를 다시 계산한 뒤 그 위로 찌르는 방향 오프셋(월드 단위,
기존 설계 그대로 유지)을 더한다. 이러면 캐릭터가 스윙 도중 얼마나 움직였든 소켓이 항상 캐릭터를
따라간 뒤 정확히 제자리로 복귀한다.

**추가 요청**: "완전히 복귀한 후 다시 대상 찾게 변경" - `UpdateSlot()`이 매 프레임 무조건
새 타겟을 찾아 무기를 회전시키던 것을, 근접무기는 `melee_thrust_active`가 true인 동안(찌르기
연출이 재생 중일 때) 조준·재타겟팅을 완전히 건너뛰도록 최상단에 조기 반환을 추가했다(찌르기가
끝나 소켓으로 복귀한 뒤에만 다시 타겟을 찾고 회전한다). 이 대기 중에도 `IsTargetingEnemy=true`로
유지해 "위장" 디스크(공격하지 않을 때만 이동속도 보너스)가 스윙 중에 잘못 발동하지 않게 했다.

**검증**: `assets-refresh` 재컴파일 콘솔 Error 0건. 위 수학적 재현 테스트로 새 계산식의 오차가
0임을 실측 확인(에디트 모드, 임시 GameObject 생성 후 즉시 삭제). **실제 플레이모드에서 캐릭터를
이동시키며 근접무기를 직접 휘둘러보는 육안 검증은 하지 못했다** - 다음 플레이 테스트에서 확인 필요.

## 2026-08-12 무기 소켓 개별화 + 소켓 불일치 무게/이동속도 패널티

사용자가 이전 데스크톱 앱 세션(Claude Code 세션 export zip으로 전달, 전체 대화 내용은
`transcript.jsonl`)에서 **plan mode로 설계를 확정해두고 "구현은 지금 하지마"로 미뤄뒀던** 작업을
이 세션에서 이어받아 실제로 구현했다. 원본 플랜 파일은 `C:\Users\user\.claude\plans\
glistening-floating-bonbon.md`. 사용자 요청 2건이 골자다: (1) 무기 소켓마다 독립적인 종류(허용
무기 타입)와 등급(사거리·감지거리·회전속도 배율)을 가지게 한다. (2) 타입이 안 맞는 무기도 장착은
항상 허용하되, 무게 패널티가 커져서 이동속도가 더 깎이게 한다(사용자가 제시받은 3가지 선택지
중 이 방식을 직접 선택함 - "지탱력 초과를 항상 허용하고 초과분만큼 비례 감속").

### 1) 소켓별 독립 타입/등급 - 데이터 복제 없이 인덱스로 저장

`PartsCatalog.asset`의 기존 "무기 소켓" 파츠 16개(표준 1 + 경무장/중무장/근접결합 각 5등급,
id 500001/520000~520024)는 그대로 두고 복제하지 않았다. 대신 장착 상태 저장 방식을 바꿨다:
`RunState.EquippedPartIds`(슬롯당 1개, 문자열 키)와 별개로 `RunState.EquippedWeaponSocketPartIds`
(`Dictionary<int, int>`, 소켓 인덱스 → partId)를 신설했다. `PartSlot.ArmWeaponSocket`은
`PartSlotExtensions.DisplayOrder`에서 제거하고(UI가 소켓 개수만큼 별도로 그린다),
`ModdingManager.EnsureDefaultPartsEquipped()`도 `Enum.GetValues` 루프에서 `ArmWeaponSocket`을
건너뛴 뒤 `0..ActiveSocketCount-1` 인덱스에 기본 파츠(표준 소켓)를 따로 채우도록 바꿨다.

인덱스 기반 조회/교체 API를 신설했다(`ModdingManager.cs`):
`TryGetEquippedWeaponSocketPart(socketIndex, out PartData)`(없으면 표준 소켓으로 폴백),
`TrySwapInventoryWithWeaponSocket(inventoryIndex, socketIndex, out reason)`(기존
`TrySwapInventoryWithSlot`과 동일한 맞교환 로직이되 대상이 소켓 인덱스 - 빠지는 소켓이 아직
아무것도 없었으면 표준 소켓을 인벤토리로 돌려준다), `GetWeaponSocketModifiers(socketIndex)`(기존
인자 없는 버전을 대체 - 소켓마다 다른 파츠를 낄 수 있으므로).

### 2) 소켓 개수 동적화

`ModdingManager.ActiveSocketCount` 신설 = `Min(PlayerShootManager.RiggedSocketCount, 머리(로봇)
파츠의 weaponSocketCount)`. `PlayerShootManager.SocketCount`의 의미를 "씬에 리깅된 개수"에서
"실제로 쓸 수 있는 소켓 수(=ActiveSocketCount)"로 재정의하고, 씬 리깅 그대로의 원래 개수는
`RiggedSocketCount`(신규)로 따로 노출했다. 이러면 `ShopPanelUI`/`GameOverSummaryUI`의 기존
`for (i < SocketCount)` 루프들이 코드 변경 없이 자동으로 올바른 개수를 순회한다.
`PlayerShootManager.Update()`의 발사 루프와 `SyncRunStateFromInspectorSlots()`의 반복 범위를
`weapon_slots.Count`에서 `SocketCount`로 바꿨고, `RefreshAllWeaponVisuals()`는 `SocketCount`
밖의 리깅된 소켓(로봇이 소켓을 적게 가질 때)의 무기 이미지를 명시적으로 숨긴다. 소켓별 회전
속도/사거리/감지거리 배율은 매 프레임 공유 필드(`socket_modifiers`)로 캐시하던 것을 제거하고
`UpdateSlot(slot_index)` 안에서 그때그때 `GetWeaponSocketModifiers(slot_index)`로 조회하도록
`GetTravelRange`/`GetDetectRange`/`RotatePivotTowards`에 `slot_index` 매개변수를 추가했다.
**지금은 두 로봇 모두 리깅 2개·weaponSocketCount 2로 동일해 실질적으로 기존과 똑같이
동작한다** - 3번째 이상 소켓은 씬에 RigingPoint 등을 배치해야 실제로 나타난다(기존부터 대기 중인
별도 후보 작업, 이번 범위 밖).

### 3) 타입 불일치 상시 허용 + 무게 패널티

`ModdingManager.CheckWeaponTypeAllowed()`(하드 블록, `bool` 리턴)를 삭제하고
`IsWeaponMismatched(socketIndex, weaponId)`(판정 로직은 동일하되 막지 않고 사실만 알려주는
불리언)로 대체해 UI 경고 문구와 무게 계산 양쪽에서 재사용한다. `GetEffectiveWeaponWeight
(socketIndex, weaponId)` 신설 = 기본 무게 x (불일치면 `mismatchWeightMultiplier`, 아니면 1).
배율은 `PartsCatalog`의 새 필드(기본 2.0, "밸런스 미확정 임시값"으로 명시)로 노출했다.
`GetEquippedWeaponWeightSum()`이 이제 `GetEffectiveWeaponWeight`를 쓰므로 총 무게·UI 표시·
이동속도 패널티 계산이 전부 자동으로 불일치 패널티를 반영한다. `ShopManager.CanPurchaseWeaponIntoSocket`
은 골드/구매여부만 검사하고(타입·무게 검사를 제거), 대신 `BuildSocketWarning(index, socketIndex)`
신설(비차단 경고 문자열, 예: "타입 불일치 - 무게 x2 적용 / 무게 초과 (33 / 24) - 이동속도 감소")
을 상점 UI가 별도로 표시한다.

### 4) 무게 → 이동속도 (지탱력 하드 블록 제거)

`ModdingManager.CheckWeightLimit`/`CheckPartWeightLimit`는 그대로 남기되(totalAfter/capacity
계산은 UI 경고 표시용으로 계속 반환) 이 반환값을 게이트로 쓰던 세 호출부(`ShopManager.
CanPurchaseWeaponIntoSocket`, `ModdingManager.TrySwapInventoryWithSlot`,
`TrySwapInventoryWithWeaponSocket`)에서 하드 블록 자체를 제거했다 - 이제 무엇을 장착하든
항상 성공한다. `RobotStats.Compute()`(순수 정적 계산 - 기존처럼 `ModdingManager.Instance`를
직접 읽는 방식)에 새 단계를 추가: 초과 무게 = `Max(0, GetTotalWeight() - GetTotalWeightCapacity())`,
`MoveSpeed -= 초과무게 x overweightSpeedPenaltyPerUnit`(`PartsCatalog`의 새 필드, 기본 0.05).
기존 `MoveSpeed = Mathf.Max(0.1f, ...)` 최소값 클램프는 그대로 둬서 완전히 못 움직이는 상태는
방지한다.

### UI 반영

- `ModdingPanelUI.RebuildSlots()`: 기존 "무기 소켓" 카드 1개 대신 `ActiveSocketCount`개 카드
  ("무기 소켓 1" ~ "무기 소켓 N")를 새 인덱스 API로 그린다(칸은 원래부터 코드로 동적 생성돼
  씬에 미리 배치하지 않는 구조라 N개로 늘리는 데 추가 씬 작업이 필요 없었다). 인벤토리에서 무기
  소켓 종류 파츠를 선택하면 N개 카드가 전부 노란색으로 열리고, 그중 하나를 누르면
  `HandleWeaponSocketCellClicked` → `TrySwapInventoryWithWeaponSocket`으로 교체된다.
  `SlotRowCount`도 "머리 1 + 무기 소켓 N + 나머지 7슬롯" 기준으로 다시 계산한다.
- `ShopPanelUI.OpenSocketPicker()`: 타입 불일치/과적이어도 버튼을 비활성화하지 않고
  (`interactable` 조건에서 제거), 대신 `ShopManager.BuildSocketWarning()`의 비차단 경고 문구를
  버튼 텍스트에 노란색으로 덧붙인다.
- `ShopPanelUI`/`GameOverSummaryUI`의 "무기 소켓: N칸 (…)" 한 줄 요약을
  `BuildWeaponSocketPartsBlock()` 헬퍼로 교체해 소켓별로 "무기 소켓 1: …" 여러 줄로 펼친다
  (이미 있는 "장착 무기" 섹션의 `for i < SocketCount` 순회 스타일을 그대로 따름).

### 검증

`assets-refresh` 재컴파일 콘솔 Error 0건. 플레이모드(`EditorApplication.isPaused=true` 후
`EnterPlaymode()`로 프레임 1에서 정지 - 배경 프레임 폭주로 관찰 전에 게임오버되는 문제 회피)에서
`script-execute`로 실제 API를 호출해 실측 확인했다:
- 소켓0에 경무장 소켓(Rare, 사거리 배율 1.15)·소켓1에 표준 소켓(1.0)을 장착 →
  `GetWeaponSocketModifiers(0)`과 `(1)`이 서로 다른 값을 반환(기존에는 항상 동일했음).
- 경무장 소켓(Light 전용)에 중기관총(Heavy) 장착 시도 → `IsWeaponMismatched`=True,
  `GetEffectiveWeaponWeight`=기본 5 x 2 = 10. 표준 소켓(무제한)에는 False, 그대로 5.
- 무게가 큰 무기(플라즈마캐논, weight 6)를 두 소켓에 억지로 장착(하나는 타입 불일치) →
  `EquipWeapon`이 예전처럼 막히지 않고 둘 다 성공, 총 무게 35 / 지탱력 24 = 초과 11,
  `RunState.NotifyChanged()` 후 `player.MoveSpeed`가 3 → 2.45로 정확히
  `3 - 11 x 0.05` 만큼 감소(기대치와 오차 0).
- `TrySwapInventoryWithWeaponSocket`으로 인벤토리의 중무장 소켓과 소켓1을 교환 → 소켓1이
  중무장 소켓으로 바뀌고, 빠진 표준 소켓(500001)이 정확히 인벤토리의 그 자리로 돌아옴.
- `ModdingPanelUI.Refresh()`/`ShopPanelUI.Refresh()`/`GameOverSummaryUI.RefreshSummary()`를
  임시로 활성화해 호출 → 무기 소켓 카드 N개(지금은 2개) 포함해서 예외 없이 완료.
- `ShopManager.CanPurchaseWeaponIntoSocket`이 타입 불일치·무게 초과에도 더 이상 false를
  반환하지 않음을 확인(이 세션의 빈 런에서는 골드 0이라 "골드가 부족합니다"로만 막혔다 - 타입/
  무게가 원인이 아님을 reason 문자열로 직접 확인). `BuildSocketWarning`은 "타입 불일치 - 무게
  x2 적용 / 무게 초과 (33 / 24) - 이동속도 감소"를 정확히 반환.

전부 기대치와 정확히 일치했다. 테스트에 사용한 런타임 상태 변경(소켓/무기 장착, 인벤토리 조작
등)은 플레이모드 종료 시 전부 폐기되며, 씬 파일(`Ground01.unity`)은 이번 작업에서 전혀 수정하지
않았다(스크립트 8개 + `PartsCatalog.asset` 필드 2개 추가만). **실제 정비 화면/상점 화면을 열어
소켓 카드가 화면에 시각적으로 잘 배치되는지는 스크린샷으로 확인하지 못했다** - UI 캡처는 이전
세션에서 문서화된 우회법(Canvas를 `ScreenSpaceCamera`로 바꿔 RenderTexture 캡처)을 다음
플레이 테스트에서 적용해 볼 것.

## 2026-08-12 스피터 공격력 절반 + 웨이브 종료 시 체력 전부 회복

사용자 요청 2건을 처리했다.

### 1) 원거리 좀비(스피터) 공격력 1/2로 감소

"원거리 좀비"는 `EnemySpawner.MonsterComponentTypes`에서 `SpitterUnit`으로 매핑되는 몬스터ID
200003(이름 "스피터")를 가리킨다(좀비 기획서 Ver04 6종 중 유일하게 투사체로 공격하는 종류).
`GameDataAsset.asset`의 이 몬스터 행에서 `monster_atk` 20 → **10**으로 직접 수정했다(다른 필드는
건드리지 않음 - hp=75/speed=1.5/range=6은 그대로). `script-execute`로 값이 정확히 반영됐는지
재조회해 확인, `EditorUtility.SetDirty`+`AssetDatabase.SaveAssets()`로 저장까지 완료(git diff로
파일이 실제로 바뀌었음을 확인).

### 2) 웨이브 종료 시 체력 전부 회복

`GameFlowManager.HandleWaveEnded()`(웨이브 종료 이벤트 핸들러, 정비 화면 진입의 시작점) 맨 앞에
`FindFirstObjectByType<PlayerRobotController>().Heal(player.MaxHp)`를 추가했다.
`PlayerRobotController.Heal(int)`이 이미 `Mathf.Min(MaxHp, CurrentHp + amount)`로 클램프하므로
`MaxHp`를 그대로 넘기면 항상 완전 회복이 된다(새 메서드를 만들지 않고 기존 API를 그대로 재사용).
**마지막(보스) 웨이브는 이 이벤트 자체가 발생하지 않는다** - `WaveManager`가 보스 웨이브에서는
`EndWave()`(→`OnWaveEnded` 발행) 대신 `WinGame()`으로 바로 끝내기 때문에, 자동으로 회복 대상에서
제외된다(별도 분기 불필요).

**검증**: `assets-refresh` 콘솔 Error 0건. 플레이모드에서 `GameOverManager.Reset()` 후
비치명 데미지로 체력을 51/100까지 깎고 `HandleWaveEnded`(private)를 리플렉션으로 직접 호출 →
체력이 정확히 100/100으로 회복됨을 확인. (첫 시도는 이전 테스트에서 죽었던 플레이어 인스턴스의
`IsDead` 플래그가 남아있어(`GameOverManager.Reset()`은 `GameOverManager.IsGameOver`만 초기화하고
`PlayerRobotController.IsDead`는 건드리지 않음) `Heal()`이 조용히 무시돼 회복이 안 되는 것처럼
보였다 - 플레이모드를 완전히 재시작해 깨끗한 인스턴스로 재검증했다. 실제 게임 플레이에서는 이
문제가 발생하지 않는다 - 사망 시 게임오버 화면으로 가고 애초에 `HandleWaveEnded`가 호출되는
경로를 타지 않는다). 씬 파일 수정 없음(스크립트 1개 + 데이터 에셋 1필드).

## 2026-08-12 PC 피격 판정이 여전히 넓던 문제 - 진짜 원인 발견(투사체 콜라이더)

사용자가 "여전히 PC의 피격 범위 판정이 지나치게 넓다. 투사체가 스치기만 해도 충돌 판정이
발생한다"고 재차 리포트했다. 앞선 세션(2026-08-12 "좀비 투사체 크기/속도 조정 + PC 피격 판정
범위 축소")에서 플레이어 쪽 `BoxCollider`를 다리 그림 실측 바운즈에 맞춰 30% 줄였는데도 여전히
넓다고 느껴진 것은, **원인이 플레이어가 아니라 스피터가 쏘는 `EnemyProjectile`의 콜라이더였기
때문**이다.

**원인**: `EnemyProjectile.Spawn()`이 `SphereCollider.radius = 0.5f`를 스프라이트 크기와
무관하게 하드코딩하고 있었다. 코드로 생성하는 투사체 원형 스프라이트는 텍스처 32px ÷
PPU 100 = **반지름 0.16유닛**(로컬)인데, 콜라이더 반지름은 0.5로 약 **3.1배** 컸다(면적 기준
약 9.8배). 둘 다 같은 `transform.localScale`(= `visualSize`)로 함께 커지므로 이 3.1배 비율은
`visualSize`가 얼마든 항상 유지됐다 - 즉 이 버그는 이번 세션에 새로 생긴 게 아니라 스크립트가
처음 작성된 시점부터 있었고, 최근 investigate 대상이 다른 곳(플레이어 콜라이더, 투사체
시각 크기)에 쏠려 있어 발견되지 않았던 것이다.

**수정**: 원형 스프라이트 생성 상수(`SpriteTextureSize`=32, `SpritePixelsPerUnit`=100)를
필드로 뽑아 `GetOrCreateSprite()`와 콜라이더 반지름 계산이 항상 같은 값을 쓰도록 통일했다.
`col.radius = SpriteRadius`(= `(32/2)/100 = 0.16`)로 교체 - 이제 "보이는 빨간 원"과 "맞는
판정 범위"가 항상 정확히 일치한다(어떤 `visualSize`에서도).

**검증**: `assets-refresh` 재컴파일 콘솔 Error 0건. 플레이모드에서 실제 스피터 발사 파라미터
(`speed=6.75, visualSize=1.05`)로 `EnemyProjectile.Spawn()`을 직접 호출해 콜라이더 월드 반지름과
스프라이트(`SpriteRenderer.bounds`) 월드 반지름을 둘 다 실측 비교 → 수정 전 이론상 비율 3.125,
수정 후 **정확히 1.0**(0.168 / 0.168)임을 확인. 씬 파일 수정 없음(스크립트 1개,
`EnemyProjectile.cs`).

## 2026-08-12 돌진 직선화 + 스피터 공격속도 + 리더 도주 삭제 + 폭발 범위 표시 + 상점 장비 상세 UI

사용자 요청 5건을 한 세션에서 처리했다. 스크립트 6개 수정/신설 + `GameDataAsset.asset` 1필드.
씬 파일(`Ground01.unity`)은 전혀 수정하지 않았다.

### 1) 스프린터/차저의 돌진이 유도탄처럼 휘던 문제

**원인**: `SprinterUnit.FixedUpdate`/`ChargerUnit.FixedUpdate`가 돌진 중 **매 물리 프레임**
`dash_direction`/`charge_direction`을 현재 플레이어 방향으로 다시 계산하고 있었다. 이건 실수가
아니라 예전에 의도적으로 넣은 것으로(ChargerUnit 주석: "모든 좀비가 항상 플레이어를 향해야 한다는
공통 규칙을 돌진 중에도 유지한다"), 그 결과 돌진이 추적으로 변해 플레이어를 따라 곡선으로 휘었다.

**수정**: 방향은 `ExecuteAttackEffect()`에서 한 번만 정하고 돌진이 끝날 때까지 고정한다.

방향을 고정하면 **플레이어가 옆으로 피했을 때 계속 직진한다**는 부작용이 생긴다. 특히 차저는
지속 3초 x (속도 3 x 배율 2.5) = **22.5유닛**이라 화면(19.2x10.8) 밖까지 밀고 나간 뒤 한참을
걸어 돌아와야 한다. 그래서 두 유닛 모두에 **돌진 최대 거리**를 함께 넣었다 -
`dashOvershoot`/`chargeOvershoot`(기본 2유닛, 인스펙터 노출)를 두고
`최대거리 = 돌진 시작 시점의 플레이어까지 거리 + 오버슈트`를 넘어가면 지속시간이 남았어도
그 자리에서 멈춘다. "플레이어를 살짝 지나칠 때까지만 내지른다"는 뜻이다.

**검증**(플레이모드, `EditorApplication.isPaused=true` 후 `EnterPlaymode()`로 1프레임 정지):
`EnemySpawner.SpawnMonster()`로 차저/스프린터를 실제 스폰하고 `ExecuteAttackEffect()`를 호출해
돌진을 시작시킨 뒤, **플레이어를 8유닛 위로 이동시키며** `FixedUpdate()`를 5번 수동 호출해
`Rigidbody.linearVelocity` 방향을 기록했다 → 두 유닛 모두 시작 방향 `(1,0,0)`에서 **방향 변화
최대 0도**(수정 전이라면 플레이어를 따라 크게 꺾였을 상황). 오버슈트도 별도 확인 -
시작 x=-4, 플레이어 x=0이면 최대거리 6이고, 이동거리 5.9까지는 `is_charging=True`,
6.1에서 `False`로 바뀌며 속도가 0이 됐다(기대치와 정확히 일치).

### 2) 스피터(원거리 좀비) 공격속도 절반

`GameDataAsset.asset`의 몬스터ID 200003 `monster_atsp` **0.8 → 0.4**. `EnemyUnit`의 쿨다운이
`1 / AtSp`라 발사 간격이 1.25초 → **2.5초**가 된다(다른 필드는 건드리지 않음). MCP
`script-execute`로 값을 바꾸고 `SetDirty` + `SaveAssets()`까지 수행, 파일에서 재확인했다.
직전 세션에서 이 몬스터의 `monster_atk`를 20 → 10으로 낮춘 것과는 별개 항목이다.

### 3) 리더 좀비 도주 기믹 삭제

`LeaderUnit`에서 도주 상태를 완전히 제거했다 - `is_fleeing` 필드, `Update()` 오버라이드
(도주 중 공격 판정 스킵), `ComputeSeekDirection()` 오버라이드(플레이어 반대 방향 이동)를 전부
삭제해 이제 리더는 무리 생존 여부와 무관하게 `EnemyUnit`의 공통 추적·근접 공격을 그대로 쓴다.

`SetPack()`과 `pack` 목록은 **남겼다** - `EnemySpawner.SpawnLeaderPack()`이 호출하는 등록
지점이고, 무리를 함께 스폰하는 기능 자체는 그대로 유지되기 때문이다(다만 이제 행동을 바꾸지
않는다는 것을 클래스 주석에 명시). 기획서 Ver04 p.22의 도주 항목을 사용자 지시로 뒤집은
것이므로 주석에 날짜와 사유를 남겼다.

**검증**: 리플렉션으로 `is_fleeing` 필드 없음, `ComputeSeekDirection`/`Update`의 `DeclaringType`이
둘 다 `EnemyUnit`(= LeaderUnit이 더 이상 오버라이드하지 않음)임을 확인.

### 4) 로켓런처/유탄발사기의 "보이는 폭발 범위 > 실제 판정 범위"

**원인**: `Projectile.Explode()`가 폭발 연출 크기를
`transform.localScale = Vector3.one * SplashRadius`로 잡고 있었다. 이 식은 "스프라이트가 스케일
1에서 반지름 1유닛"일 때만 맞는데, 실제 투사체 스프라이트(`Bullets.png`)는 **640x640px / PPU
100 = 스케일 1에서 반지름 3.2유닛**이다. 그래서 화면에 그려지는 폭발 반지름이 실제 판정 반지름의
**정확히 3.2배**(면적 약 10배)였다 - 로켓런처(splash 2.4)는 반지름 7.68유닛짜리 폭발이 그려지는데
실제로는 2.4유닛 안의 적만 맞았다. `EnemyProjectile`의 콜라이더 하드코딩 버그(직전 세션)와
같은 계열의 "시각과 판정이 서로 다른 상수를 쓰던" 문제다.

**수정**: `ComputeBlastVisualScale()`을 신설해
`localScale = SplashRadius / (스프라이트 스케일1 기준 반지름)`으로 계산한다. 반지름은 하드코딩하지
않고 `SpriteRenderer.sprite.bounds.extents`에서 매번 읽으므로 무기마다 투사체 프리팹이 달라도
(`weapon_tanhwan`: Bullets/Energy…) 저절로 맞는다. **월드 bounds가 아니라 스프라이트 로컬
bounds를 쓴다** - 투사체는 `Quaternion.Euler(0,0,각도)`로 회전한 채 날아가서 월드 AABB가 최대
√2배까지 부풀기 때문이다. 스프라이트가 자식에 있고 자체 스케일을 갖는 경우도 lossyScale 비율로 보정한다.

**검증**: 플레이모드에서 `Bullets` 프리팹을 실제로 Instantiate(37도 회전 상태)하고
`Launch()` → `Explode()`(리플렉션)를 호출해 스프라이트 화면 반지름을 실측했다.
splash 2.4 → localScale 0.75 → 화면 반지름 **2.400** / 판정 2.4 → **비율 1.0000**,
splash 1.5 → localScale 0.4688 → 화면 반지름 **1.500** / 판정 1.5 → **비율 1.0000**.
(수정 전 이론값은 두 경우 모두 비율 3.2)

### 5) 상점에서 보유 무기/파츠/디스크의 상세 능력치 보기

**설계 판단**: 상점의 "장착 무기 / 로봇 모딩 상태 / 디스크" 목록은 원래부터 **여러 줄이 한
덩어리로 들어간 TMP 텍스트 3개**다. 줄마다 클릭을 받으려면 보통은 씬에 항목 수만큼 버튼을
깔아야 하는데, 항목 수가 런타임에 변한다(무기 소켓 개수 = `ActiveSocketCount`, 디스크 슬롯 수).
그래서 씬을 건드리지 않고 **TMP 링크 태그(`<link="id">`) + 클릭 릴레이** 방식을 택했다.

신설한 스크립트 2개:
- **`TextLinkClickRelay.cs`** - `IPointerClickHandler`로 클릭 좌표를 받아
  `TMP_TextUtilities.FindIntersectingLink()`로 링크 id를 뽑아 콜백한다. `Attach()` 정적 메서드가
  대상 텍스트에 컴포넌트를 붙이고 `raycastTarget`도 켠다. ScreenSpaceOverlay 캔버스에서는
  이벤트 카메라를 null로 넘겨야 좌표가 맞는다.
- **`EquipmentDetailPopup.cs`** - 아이콘 + 제목 + 본문 + 닫기 버튼으로 된 팝업을 **코드로
  생성**한다(`ModdingPanelUI`가 정비 칸을 코드로 만드는 것과 같은 패턴). 배경(반투명 검정)
  자체가 버튼이라 바깥을 누르면 닫히고, 창에도 빈 버튼을 깔아 클릭이 배경까지 새지 않게 했다.
  레이아웃은 전부 **앵커 비율**이고 글자는 **TMP 자동 크기 조절**을 켰다 - 이 프로젝트 캔버스가
  `ConstantPixelSize`라 절대 픽셀로 만들면 해상도마다 크기가 제각각이 되기 때문이다.

`ShopPanelUI` 변경:
- `Awake()`에서 팝업을 캔버스 밑에 만들고 텍스트 3개에 릴레이를 붙인다(`SetupDetailInspector`).
  팝업은 캔버스 직속이라 상점 패널과 같이 꺼지지 않으므로 `Open()`/`Close()`에서 명시적으로 닫는다.
- 각 항목을 `Clickable(linkId, label)`(= `<link><u>…</u></link>`)로 감싸고 목록 제목에
  "(클릭 = 상세)" 안내를 붙였다. 링크 id 체계: `w:소켓번호`(장착 무기) / `ws:소켓번호`(무기 소켓
  파츠) / `p:슬롯이름`(그 외 파츠) / `d:디스크id` / `head`(로봇 본체).
- 상세 내용은 데이터 원본 값만 나열하지 않고 **지금 이 로봇에서 실제로 적용되는 값**을 같이 보여준다:
  - 무기 - 분류(경/중 + 투사체 타입), `공격력 10 (+로봇 3 분배 → 1발 11)`처럼 실제 1발 데미지
    (`weapon_atk + robot_atk / 발수`, `PlayerShootManager.ComputeDamage`와 같은 식), 초당 피해,
    `사거리 4.16 → 실제 4.16`처럼 **소켓 파츠 배율이 곱해진 실효 사거리/감지거리**, 발사 방식,
    다중탄·탄퍼짐·탄속·관통·폭발·방어무시·넉백·회전속도, 그리고 **소켓 타입 불일치 시 무게 배율**.
  - 무기 소켓 파츠 - 허용 무기 타입, 사거리/감지/회전 배율, 현재 낀 무기와 타입이 맞는지.
  - 그 외 파츠 - 보너스 스탯/무게/지탱력 + **로봇 전체 무게 합계와 초과 시 이동속도 감소량**.
  - 디스크 - 효과 설명 + 값이 들어있는 파라미터만(확률/지속/주기/범위/상한/횟수) + 상승·하락 스탯.
  - 로봇(머리) - 기본 스탯, 무기 소켓/디스크 슬롯 수, 부품 상자 적재량.
- 실효 사거리를 UI가 알 수 있도록 `PlayerShootManager`에 `GetEffectiveTravelRange(socketIndex)` /
  `GetEffectiveDetectRange(socketIndex)`(기존 private 계산을 그대로 호출하는 공개 래퍼)를 추가했다.
- **덤으로 고친 것**: 모딩 상태의 `헬멧: 기본`이 하드코딩이라 실제로 장착한 헬멧이 반영되지 않고
  있었다 → `PartLine(modding, PartSlot.Helmet)`로 교체. `디스크 슬롯: N칸`도 칸 수만 보여주고
  어떤 파츠인지 안 보여줘서 `디스크 슬롯(N칸): 등급 파츠명`으로 바꿨다.

**검증**(플레이모드): 상점을 열고 `w:0`/`w:1`/`ws:0`/`p:Foot`/`p:MagneticCore`/`head` 6종의
팝업을 전부 열어 본문을 덤프해 확인했다(예: 기관단총 = "경무장 / 연사화기, 공격력 10 (+로봇 3
분배 → 1발 11), 사거리 4.16 → 실제 4.16, 동시 발사 3발(탄퍼짐 5도), 무게 2.5"). 클릭 경로도
**끝까지** 실측했다 - `ForceMeshUpdate()` 후 `linkCount=2`(`w:0`,`w:1`)를 확인하고, 첫 링크
글자의 월드 좌표를 화면 좌표로 변환해 `FindIntersectingLink()`에 넣으니 `linkIndex=0 / id=w:0`이
나왔고, 그 좌표로 `TextLinkClickRelay.OnPointerClick()`을 호출하니 팝업이 열리며 제목이
"소켓 1 · 일반 기관단총"이 됐다. 마지막으로 캔버스를 임시로 `ScreenSpaceCamera`로 바꿔
RenderTexture(716x403, 캔버스 rect와 동일)로 캡처해 **아이콘·제목·본문·닫기 버튼이 화면에
제대로 배치되는 것을 눈으로 확인**했다(캡처 후 캔버스 설정은 원복).

`assets-refresh` 재컴파일 콘솔 Error 0건(경고는 기존 `ModdingPanelUI`와 동일한
`enableWordWrapping` deprecated 1건뿐).

### (후속) 사거리·감지거리 표시를 값 하나로 단순화

사용자 요청으로 무기 상세 팝업의 `사거리 4.16 → 실제 4.16` / `감지거리 3.64 → 실제 3.64`
표기에서 "→ 실제 …" 부분을 없애고 **`사거리 4.16` / `감지거리 3.64`처럼 값 하나만** 남겼다.
남긴 값은 무기 데이터 원본이 아니라 **소켓 파츠 배율까지 적용된 최종값**
(`PlayerShootManager.GetEffectiveTravelRange/GetEffectiveDetectRange`)이라, 화면에 적힌 숫자가
곧 게임에서 실제로 나가는 거리다(지금은 두 소켓 모두 표준 소켓 = 배율 1.0이라 원본과 같은 값).
플레이모드에서 팝업을 다시 열어 두 소켓 모두 한 줄로만 나오는 것을 확인했다.

## 2026-08-13 UI 아트 전면 적용 + BGM 시스템(타이틀/인게임 랜덤 재생 + 볼륨 설정)

사용자가 `Assets/Resources/UI`에 UI 아트 50장, `Assets/Resources/Musics`에 BGM 3곡
(`Title_BGM`, `Game_BGM01`, `Game_BGM02`)을 넣어주고 (1) UI 전반에 적용 (2) 타이틀/인게임 BGM
재생 + 볼륨 설정 + 검수를 요청했다.

### 1) 리소스 임포트 설정 - 9-슬라이스가 이 작업의 핵심

UI 아트는 크기가 제각각인데(버튼 274x62, 큰 패널 294x262, 배너 984x169) 붙일 자리는 앵커 비율로
잡혀 있어 해상도마다 늘어난다. 그냥 `Simple`로 붙이면 모서리 장식이 뭉개지므로 **50장 전부
`spriteBorder`를 넣고 `Image.type = Sliced`로 쓴다.** 경계값은 이미지별 테두리 폭을 보고 정했다
(예: 판때기류 30, 큰 패널 34, 각진 배너는 좌우 75/상하 25, 버튼 26/20, 바 40). 스크립트로
`TextureImporter`를 일괄 수정했고(`textureType=Sprite`, `alphaIsTransparency=true`, mipmap 끔),
**BGM 3곡은 `loadType=Streaming`+`preloadAudioData=false`** 로 바꿨다(곡당 1.5~2.6MB).

### 2) UI 아트 적용 범위

씬에 있는 UI는 씬을 직접 수정하고(`Ground01.unity`/`Title.unity` 저장), 코드로 생성되는 UI는
스크립트에서 `Resources.Load<Sprite>("UI/...")`로 입혔다. 적용 목록:

- **타이틀**: 시작/종료 버튼 → `Purple_button00/01`, 제목 뒤 `Purple_ui02` 배너 신설
- **전투 HUD**: HP 슬라이더 배경 `Blue_barpanel00` + 채움 `Red_bar00`, 경험치 슬라이더
  배경 `Blue_barpanel00` + 채움 `Blue_bar00`, 웨이브/골드/레벨/남은시간/부품상자 텍스트 뒤에
  `Panel01`·`Panel04` 판때기 신설, **골드에 `Gold_icon00` · 레벨에 `Star_icon` 아이콘 추가**
  (아이콘을 텍스트 rect 왼쪽 안에 넣고 텍스트를 우측 정렬로 밀어 겹치지 않게 했다),
  **`HP : ` 글자를 `Heart_icon`으로 교체**
- **AI 코어 3택**: 카드 3장 `Black_ui01`, 제목 뒤 `Purple_ui02` 배너
- **로봇 정비**: 상단 바 `Purple_ui02`, 인벤토리/슬롯/능력치/안내 패널 `Black_ui01`,
  정비 완료 버튼 `Purple_button00`, **칸(코드 생성) `Black_ui03`**
- **상점**: 품목 카드 4장 `Black_ui01`, 정보 4블록 뒤 `Black_ui01`, 버튼 전부
  `Purple_button00/01`, 소켓 선택 창 `Purple_ui04`, 골드에 아이콘
- **게임오버 요약**: 제목 배너 `Purple_ui02`, 정보 블록 `Black_ui01`, 버튼 `Purple_button00`
- **장비 상세 팝업**(코드 생성): 창 `Purple_ui04`, 닫기 버튼 `Purple_button00`

**정비 칸은 구조를 두 겹으로 바꿨다.** 기존에는 칸 이미지 하나에 상태색(일반 회색/선택 파랑/
슬롯 강조 노랑)을 칠했는데, 테두리 스프라이트가 거의 검정이라 색을 곱하면 강조색이 전부 검게
죽는다. 그래서 **바깥 = 테두리 아트(흰색 고정) / 안쪽 "State" 자식 = 상태색**으로 나누고
`CreateCell`이 **안쪽 이미지를 반환**하게 해서 기존 색 변경 코드를 그대로 쓴다.

### 3) 발견해서 함께 고친 버그 - 정비 슬롯 칸이 열 때마다 납작해짐

검수 중 정비 화면 슬롯 칸 높이가 75 → 34.65로 줄고 컨테이너 rect 높이가 **-106.48(음수)** 까지
내려가는 것을 발견했다. 원인은 `EnsureGrid()`가 스크롤이 없는 슬롯 격자에도 `ContentSizeFitter`
(세로 PreferredSize)를 붙이고 있었던 것 - **"컨테이너 높이 → 칸 크기 → (fitter가) 컨테이너 높이"
되먹임**이 생겨 화면을 열 때마다 값이 줄어든다. `ScrollRect` 안에 있을 때만 fitter를 세로로
동작시키고, 밖이면 세로 fit을 끄고 `sizeDelta.y = 0`으로 앵커 높이를 되돌린 뒤 칸 크기를
계산하도록 고쳤다. 이 버그는 아트 적용 이전부터 있던 것이다.

### 4) BGM 시스템 (`MusicManager.cs` 신설)

- **AudioSource가 하나뿐이다.** "여러 곡이 동시에 나오는" 사고를 원천 차단하는 가장 확실한
  방법이라, 페이드 아웃/인도 이 한 소스의 볼륨을 오르내려 처리한다.
- **씬에 배치하지 않는다.** `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]`로 스스로 생성되고
  `DontDestroyOnLoad`로 살아남으며, 두 번째 인스턴스는 `Awake`에서 즉시 자살한다. 씬마다
  오브젝트를 두면 전환 순간 두 개가 공존해 곡이 겹치거나 매번 처음부터 다시 시작된다.
  에디터에서 `Ground01`을 직접 실행해도 똑같이 동작한다.
- 씬 이름이 `Title`이면 타이틀 곡을 **반복 재생**, 그 외에는 `Game_BGM*`을 **랜덤 재생**한다.
  곡 목록은 `Resources.LoadAll`로 폴더를 통째로 읽어 접두사로 걸러서, 곡을 추가/삭제해도
  코드를 고칠 필요가 없다.
- **다음 곡은 현재 곡을 후보에서 제외하고 뽑는다**(곡이 1개뿐이면 어쩔 수 없이 같은 곡).
  인게임 곡은 `loop=false`라 곡이 끝나면 `Update`가 자동으로 다음 곡을 시작한다.
- 씬 전환은 0.5초 **페이드 아웃 → 곡 교체 → 페이드 인**. 정비/상점은 `Time.timeScale=0`이라
  페이드는 전부 `Time.unscaledDeltaTime`을 쓴다(오디오 자체는 timeScale 영향을 받지 않는다).
- 볼륨은 `PlayerPrefs`(`comstock_music_volume`, 기본 0.7)에 저장되고 `MusicManager.Volume`
  프로퍼티 하나로만 바뀐다. 값이 바뀌면 `OnVolumeChanged`로 알려서 여러 화면의 슬라이더가 동기화된다.

**볼륨 설정 UI**(`MusicVolumeSliderUI.cs` 신설)는 "음악 볼륨 + 슬라이더 + %" 한 줄짜리 컨트롤을
코드로 만들어 붙이는 방식이다(씬 편집 없음). **타이틀 화면**(종료 버튼 아래)과 **상점 화면**
(골드와 '다음 웨이브 시작' 사이의 빈 자리) 두 곳에 같은 구현을 붙였다 - 상점은 웨이브마다 반드시
거치는 화면이라 런 도중 볼륨을 조절할 수 있는 유일한 지점이다.

### 검수 결과 (전부 플레이모드 실측)

- **타이틀 진입**: `Title_BGM` 재생, `loop=true`, 씬 전체 AudioSource **1개**, MusicManager **1개**.
- **타이틀 → 인게임**(시작 버튼 실제 클릭): 전환 직후 `Title_BGM volume=0.122`(페이드 아웃 중)
  → 잠시 뒤 `Game_BGM02 loop=false volume=0.7`. **AudioSource는 내내 1개**(겹침 없음).
- **인게임 → 타이틀**(게임오버 '타이틀로' 실제 클릭): `Title_BGM`으로 복귀, AudioSource 1개,
  MusicManager 1개(누적되지 않음). 타이틀↔인게임 2회 왕복까지 확인.
- **현재 곡 제외 랜덤**: 실제 곡 2개로 각각 200회 추첨 → 현재 곡이 한 번도 안 뽑힘.
  가짜 곡 4개를 주입해 400회 추첨 → 현재 곡 0회, 나머지 3곡에 142/131/127회로 고르게 분포.
- **곡 종료 → 자동 전환**: `Game_BGM01` 재생 중 강제 종료 → `Game_BGM02`로 자동 전환 확인.
- **볼륨**: 슬라이더를 0.35로 옮기면 설정·실제 AudioSource 볼륨·PlayerPrefs가 전부 0.35,
  표시 문구 "35%". 0.7로 되돌리면 그대로 복귀.
- **UI**: 타이틀 / AI 코어 3택 / 상점 / 상세 팝업 / 로봇 정비 / 게임오버 요약 6화면을 **FHD
  (1920x1080) RenderTexture로 캡처**해 눈으로 확인했다. 정비 화면은 격자가 열릴 때의 캔버스 크기로
  계산되므로 **캔버스를 FHD로 만든 뒤 화면을 여는 순서**로 캡처해야 실제 크기가 나온다
  (그냥 캡처하면 Game View 716x403 기준으로 계산된 칸이 그대로 남아 실제보다 작게 보인다) -
  FHD 기준 슬롯 칸 319.4x100.4로 정상. 인벤토리 선택(파랑)·슬롯 강조(노랑)도 새 두 겹 구조에서
  그대로 동작함을 확인.
- `assets-refresh` 재컴파일 콘솔 Error 0건.

**남은 아트**: `Exp_icon` / `Dash_skill` / `Green_bar*` / `Heart_panel` / `Star_panel` /
`Black_ui04~09` / `Purple_ui01·03` / `Panel00·03` / `Blue_barpanel01` / `30_panel_bar_purple_border`는
아직 쓰이지 않았다. 특히 `Dash_skill`은 구르기 쿨다운 아이콘(방사형 게이지)에 쓸 자리가 있으나
HUD에 새 UI를 추가하는 작업이라 이번 범위에서 제외했다.

### (이어서) 남은 UI 아트 마저 적용 + 구르기 쿨다운 아이콘 + 타이틀 무음 버그 수정

앞 항목에서 "아직 안 쓴 아트"로 남겨뒀던 것들을 사용자 요청으로 마저 적용했다.

**적용 내역**
- **`Dash_skill` → 구르기 쿨다운 아이콘 신설**. `PlayerRobotController.DashCooldownRatio`
  (0=사용 가능, 1=방금 사용) 프로퍼티를 새로 노출하고, 체력 바 아래에 `DashSkill` 그룹
  (플레이트 `Black_ui03` + 아이콘 `Dash_skill` + 덮개)을 씬에 만들었다. `GameHUD`가 매 프레임
  덮개의 `fillAmount`를 갱신한다(`Image.type=Filled`, 세로, 위에서부터). **덮개는 처음에 같은
  `Dash_skill` 스프라이트를 어둡게 깔았는데, 그러면 달리는 사람 모양만 어두워져 쿨다운인지
  구분이 안 됐다** → 타일 전체를 덮는 `Black_ui03`(플레이트와 같은 모서리)로 교체했다.
  `GameFlowManager.combatHudObjects`에도 추가해 정비/상점에서 다른 HUD와 같이 사라진다.
- **`Green_bar00` → 체력 바 색 전환**. 체력이 40%를 넘으면 초록, 이하면 빨강 막대로 바뀐다
  (`GameHUD.UpdateHpBarArt`). 채움 이미지는 `hpSlider.fillRect`에서 찾으므로 인스펙터 연결이 필요 없다.
- **`Heart_panel` / `Star_panel` → 아이콘 뒤 실루엣**(외곽선처럼 보이게 1.18배로 뒤에 깔았다).
- **`Exp_icon` → AI 코어 레벨업 화면 제목 왼쪽 아이콘**.
- **`Purple_ui01` → 정비 화면 섹션 제목 배너**("인벤토리" / "장착 파츠").
- **`Blue_barpanel01` → 경험치 바 배경**(체력 바의 `Blue_barpanel00`과 다른 아트라 한눈에 구분).
- **`30_panel_bar_purple_border` → 남은 시간 표시 배경**.
- **`Panel00` → 상점 품목 카드의 등급/카테고리 줄 배경**, **`Black_ui04` → 품목 아이콘 뒤 판**,
  **`Panel03` → 게임오버 요약 헤더 배경**.
- 나머지(같은 아트의 크기 변형: `Red_bar01~04`, `Blue_bar01~04`, `Green_bar01~03`,
  `Black_ui05~09`, `Purple_button02/03`, `Purple_ui00/03`, `Panel02/04` 등)는 이미 같은 계열을
  쓰고 있어 굳이 다 쓰지 않았다.

**9-슬라이스 두 번 조정 (이번 작업에서 제일 손이 많이 간 부분)**

붙이는 자리가 원본보다 훨씬 납작하면 테두리가 높이를 다 먹어 가운데가 사라진다. 두 군데서 발생:
- **HUD 막대**: 체력 바가 18px인데 배경 아트 테두리가 60px이라 막대가 얇은 선처럼 뭉개졌다
  → 막대 계열 `spriteBorder`를 낮추고(barpanel 60→30, Red 24→16, Green/Blue 40→24),
  **`Image.pixelsPerUnitMultiplier`(2~2.5)로 테두리가 그려지는 크기를 한 번 더 줄이고**,
  체력 바를 276x36으로, 경험치 바를 조금 두껍게(앵커 0.912~0.947) 키웠다.
- **정비 섹션 배너**: 449x165 배너(상하 테두리 25)를 60px 높이에 붙여 눌렸다
  → `pixelsPerUnitMultiplier = 2.2`로 해결(에셋 경계값은 그대로 두고 이 자리에서만 축소).

**발견해서 고친 버그: 타이틀 화면이 무음이었다**

콘솔에 `There are no audio listeners in the scene` 오류가 반복되는 것을 보고 조사한 결과,
**`Title.unity`의 Main Camera에 `AudioListener`가 아예 없었다**(`Ground01`에는 있다).
즉 앞 항목에서 만든 타이틀 BGM은 **재생은 되지만 소리가 나지 않는 상태**였다. 씬의 카메라에
`AudioListener`를 추가해 고쳤고, 앞으로 새 씬을 추가할 때 같은 실수로 음악이 조용히 사라지지
않도록 `MusicManager.EnsureSingleAudioListener()` 안전망도 넣었다(리스너가 0개면 자기 자신에
붙이고, 씬에 있으면 붙였던 것을 즉시 떼어내 "2개" 경고도 안 나게 한다).

**검증**(플레이모드 실측)
- 체력 바: 100/100 → `Green_bar00`, 30/100 → `Red_bar00`, 다시 회복 → `Green_bar00`.
- 구르기: 발동 전 ratio=0 / fillAmount=0 / 덮개 꺼짐 → 발동 직후 ratio=1 / fillAmount=1 / 켜짐.
  덮개를 50%로 고정해 캡처, 타일 위쪽 절반만 어두워지는 것을 눈으로 확인.
- 실제 웨이브 종료 흐름(`HandleWaveEnded`)을 태워 **DashSkill과 TopHud가 함께 숨는 것** 확인
  (timeScale=0, 정비중=True).
- AudioListener: 수정 후 `Title`·`Ground01` 양쪽 모두 정확히 1개(안전망이 중복 추가하지 않음).
- 음악: 타이틀 `Title_BGM`(반복) → 인게임 `Game_BGM01` 정상 전환, AudioSource 내내 1개.
- FHD 캡처로 타이틀/HUD/AI 코어/상점+상세 팝업/로봇 정비 화면 재확인. `assets-refresh` Error 0건.

## 2026-08-13 밸런스·시스템 조정 12건 (레벨업 등급화 / 난이도 곡선 재설계 / 근접 판정 교체)

사용자 요청 9건 + 진행 중 추가 요청 3건을 한 번에 반영했다. 작업 중 **원인이 다른 곳에 있던
버그 3건**(난이도 곡선 이중 누적, 투사체 정리 누락, 방어력 하한 0)을 발견해 함께 고쳤다.

### 1) AI 코어 업그레이드 선택지에 등급 도입 + 수치 너프

`AiCoreUpgradePool`에 등급 설정(`GradeSetting`: 배율/가중치/최소 웨이브)을 신설하고,
`Option.amount`의 의미를 **일반 등급 기준값**으로 바꿨다. 등급 배율은 **1 / 5⁄3 / 7⁄3 / 10⁄3 / 5**다 -
기준값 b에 대해 항상 `b / 1.67b / 2.33b / 3.33b / 5b`가 되므로, 기준값을 0.3의 배수로 잡으면
사용자가 제시한 예시(A* 알고리즘 이동속도 0.3 / 0.5 / 0.7 / 1.0 / 1.5)가 그대로 나온다.

`AiCoreManager.DrawChoices()`는 이제 `Option`이 아니라 **`UpgradeChoice`(종류 + 등급 + 실제 증가량)**
를 돌려주고, 등급은 카드마다 따로 굴린다(`RollGrade(RunState.WaveNumber)` - 가중치·최소 웨이브는
ShopCatalog와 같은 값이라 초반에는 상위 등급이 안 나온다). 카드 문구도 에셋의 고정 문자열을 쓰지
않고 **매번 생성**한다(`AiCoreUpgradePool.BuildEffectLine`) - 등급마다 수치가 달라지므로 고정 문구를
두면 화면에 적힌 값과 실제 적용값이 반드시 어긋난다. 그래서 에셋의 `description` 8개는 전부 비웠다.

수치는 사용자 재지정("일반 등급 기준으로 기존의 1/3~1/4 수준으로 너프")에 맞춰 낮췄고,
**방어력만 더 크게(1/6.7) 깎았다** - "방어력 증가가 너무 급격해 난이도가 쉬워진다"는 지적 때문이다
(실제로 플레이어 피해 계산이 `max(0, 적공격력 - 방어력)`이라 방어력이 적 공격력을 넘으면 완전 무적이
됐다. 아래 11) 항목에서 하한도 함께 고쳤다).

| 업그레이드 | 스탯 | 일반 | 희귀 | 서사 | 유일 | 전설 | (기존값) |
|---|---|---|---|---|---|---|---|
| 사격술 최적화 | 공격력 | +0.6 | +1 | +1.4 | +2 | +3 | 2.5 |
| 타겟 추적 기술 | 치명타 확률 | +1.2% | +2% | +2.8% | +4% | +6% | 5% |
| A* 알고리즘 | 이동속도 | +0.3 | +0.5 | +0.7 | +1 | +1.5 | 1.2 |
| 냉각 시스템 강화 | 체력 | +3 | +5 | +7 | +10 | +15 | 10 |
| 장갑판 보강 | 방어력 | +0.3 | +0.5 | +0.7 | +1 | +1.5 | 2 |
| 균형 조정 알고리즘 | 회피율 | +0.9% | +1.5% | +2.1% | +3% | +4.5% | 3% |
| 행운의 회로 | 행운 | +0.3 | +0.5 | +0.7 | +1 | +1.5 | 1 |
| 치명타 증폭기 | 치명타 피해 | +3% | +5% | +7% | +10% | +15% | 10% |

**정수 스탯을 등급마다 반올림하지 않는 이유**: 기준값이 1보다 작은 방어력(0.3)을 등급마다
반올림하면 일반 0 / 희귀 1 / 서사 1처럼 등급 구분이 사라진다. AI 코어 보너스는
`RunState.CoreStatBonuses`에 **소수 그대로 누적**되고 `RobotStats`가 그 합계를 한 번만
반올림하므로, 여러 번 뽑으면 소수 증가분도 정확히 누적된다(0.3을 두 번 = 0.6 → 반영 +1).

등급 추첨 분포 실측(각 1000회): 웨이브 1은 일반 640 / 희귀 360, 웨이브 7 이후 일반 501 /
희귀 266 / 서사 128 / 유일 73 / 전설 32으로 안정된다.

### 2) 레벨업을 훨씬 자주 하도록 경험치 공식 교체 (33% 감소 → 계수 1/12)

처음 요청은 "필요 경험치 33% 감소"였으나, 이어서 "레벨업을 훨씬 더 자주 하되 회당 수치는
1/3~1/4로 너프"로 의도가 확정됐다. 필요 경험치가 레벨에 비례해 늘어나는 구조(누적 ≈ 계수 × 레벨²)라
**33% 감소로는 레벨업 횟수가 1.2배밖에 안 늘어난다.** 그래서 공식 자체를 바꿨다:

- 필요 경험치 `50 + 20 × 레벨` → **`4 + 2 × 레벨`** (계수 1/12) → 같은 경험치로 **약 3.5배 레벨업**
- 최대 레벨 `20` → **`70`** (레벨업 3.5배에 맞춰 상한도 3.5배. 안 올리면 7웨이브쯤 상한에 닿아
  이후 성장이 멈춘다). 누적 필요 경험치는 레벨 70 기준 5110으로 예전 레벨 20 기준 4800과 비슷해서
  **총 성장량은 예전과 거의 같고 성장 단위만 잘게 쪼개진 것**이 된다.

이 값들은 `AiCoreManager` 코드 기본값과 `Ground01.unity` 씬 직렬화 값 **양쪽에 넣었다**(씬 우선).

### 3) 좀비 투사체 속도 25% 감소

`SpitterUnit.projectileSpeed` 6.75 → **5.0625**. 이 컴포넌트는 스폰 시 `AddComponent`로 붙어
씬/프리팹 직렬화가 없으므로 코드 기본값만 바꾸면 된다.

### 4) 강한 좀비 체력 대폭 하향 (+ 방어력도 함께) / 웨이브당 체력·공격력 5% 증가

"3라운드 이상 진행이 불가능"의 원인을 조사한 결과 **체력보다 방어력이 더 큰 문제**였다.
피해 계산이 `max(1, 데미지 - 방어력)`이라, 차저/리더의 방어력 10은 발당 데미지가 낮은 연사 무기를
사실상 무력화했다(기관단총 발당 10 → 1, 중기관총 발당 9 → 1 = DPS 60~90이 6~10으로 폭락).
체력만 내려도 진행 불가가 풀리지 않으므로 방어력도 같이 낮췄다(`GameDataAsset.asset` 직접 수정).

| 몬스터 | 체력 | 방어력 |
|---|---|---|
| 좀비(200001) | 70 (유지) | 1 (유지) |
| 차저(200002) | **650 → 180** | **10 → 3** |
| 스피터(200003) | 75 (유지) | 0 (유지) |
| 스프린터(200004) | **120 → 100** | **5 → 2** |
| 디스럭터(200005) | **150 → 110** | **5 → 2** |
| 리더(200006) | **600 → 170** | **10 → 3** |
| 보스 | 8000 (유지) | 5 (유지) — 사용자 지정으로 제외 |

웨이브 상승분은 `EnemySpawner`에 `statIncreasePerWave`(0.05)를 신설해 스폰 시점에
`데이터값 × 1.05^(웨이브-1)`로 적용한다(처음엔 체력만이었고, 이어진 요청으로 **공격력도** 포함).
`MonsterData`가 struct라 사본을 수정해도 `GameDataManager`의 원본은 오염되지 않는다(class였다면
웨이브마다 값이 영구 누적된다). 보스는 `WaveManager`가 스탯을 직접 만들어 스폰하므로 자동 제외된다.
실측: 웨이브 2 좀비 = 체력 74 / 공격력 16, 웨이브 20 배율 2.527.

### 5) 초반 5웨이브 완화 + 스폰량 점증 — 그리고 난이도 곡선 이중 누적 버그 수정

**함께 발견한 버그**: `EnemySpawner.BaseSpawnInterval`/`BaseMaxAliveEnemies`가 조정 대상 필드를
그대로 돌려주는 프로퍼티였고, `WaveManager`가 매 웨이브 "기준값 × 웨이브 배율"을 계산해 **같은
필드에 덮어쓰기** 때문에 다음 웨이브의 "기준값"이 이미 조정된 값이 됐다. 그래서 난이도가 이중으로
누적되어 실제 최대 동시 생존 수가 웨이브 10에 의도한 54마리가 아니라 **198마리**, 웨이브 20에는
**778마리**였다(간격도 0.96^(웨이브·(웨이브-1)/2)로 급락). 초반 3~4웨이브부터 화면이 적으로
메워져 진행이 막힌 또 하나의 원인이다. → `Awake`에서 원본 값을 한 번만 찍어두고 그것을 기준으로 쓴다.

그 위에 초반 완충 구간을 새로 넣었다(`WaveManager`).
- `easyWaveCount = 5` / `firstWaveIntervalMultiplier = 1.6`: 웨이브 1의 스폰 간격을 1.6배로 늘려
  시작하고 5웨이브에 걸쳐 기준값(×1.0)으로 선형 복귀. 6웨이브부터 기존 감소율(0.96^n) 적용.
- `easyWaveMaxAliveScale = 0.5`: 완충 구간에서는 최대 생존 수 증가폭을 절반만 적용.
- `wavesPerSpawnBatchIncrease = 5`: **한 번에 스폰하는 마리 수**를 5웨이브마다 +1(6·11·16웨이브).
  완충 구간과 같은 값이라 1~5웨이브에는 배치 증가 계단이 끼지 않는다(4로 두면 5웨이브에서
  초당 스폰이 1.74 → 3.0으로 튀었다). `EnemySpawner.ConfigureDifficulty`에 배치 인자를 추가했다.

실측 커브(플레이모드에서 실제 계산 함수를 호출해 확인):

| 웨이브 | 1 | 2 | 3 | 4 | 5 | 6 | 10 | 15 | 20 |
|---|---|---|---|---|---|---|---|---|---|
| 스폰 간격(초) | 1.6 | 1.45 | 1.3 | 1.15 | 1.0 | 0.96 | 0.815 | 0.665 | 0.542 |
| 배치(마리) | 2 | 2 | 2 | 2 | 2 | 3 | 3 | 4 | 5 |
| 최대 동시 생존 | 18 | 20 | 22 | 24 | 26 | 30 | 46 | 66 | 86 |
| 초당 스폰 | 1.25 | 1.38 | 1.54 | 1.74 | 2.0 | 3.13 | 3.68 | 6.02 | 9.22 |

상위 몬스터 해금도 뒤로 밀어 **1~5웨이브는 기본 좀비만** 나온다(`Ground01.unity` 씬 값):
스피터 3→**6**, 차저 4→**7**, 스프린터 5→**8**, 디스럭터 8→**10**, 리더 10→**12**.

### 6) 웨이브 골드 획득량 평균 25% 증가

**골드량이 아니라 드랍 확률에 배율을 곱했다.** 골드량은 `MaxHp / 60`의 정수라 1골드짜리 몬스터에
1.25를 곱해도 반올림에서 그대로 1이 되어 아무 변화가 없다(좀비가 대부분인 초반이 특히 그렇다).
확률에 곱하면 몬스터 체력과 무관하게 기대값이 정확히 25% 늘어난다:
`GoldDropChance = 1/3 × 1.25 = 0.4167`(실측 확인).

여기에 골드량 계산을 **내림 → 반올림**으로 바꿨다. 위 4)의 체력 하향으로 절삭 손실률이 커져
(체력 110 → 1.83이 1로 깎여 -45%) 골드 수입이 조용히 깎이는 부작용이 있었기 때문이다.
결과: 스프린터 1→2, 디스럭터 1→2, 리더 2→3 골드(좀비·차저는 변화 없음).

### 7) 이전 웨이브 투사체가 다음 웨이브에 남는 버그 수정

`GameFlowManager`가 정리하던 것은 **`Projectile`(플레이어 탄) 하나뿐**이었고,
`EnemyProjectile`(스피터 탄)과 `BeamProjectile`(플라즈마캐논 빔)은 그대로 남았다. 정비 화면은
`timeScale=0`이라 그 자리에 얼어 있다가 다음 웨이브가 시작되는 순간 그대로 날아와 플레이어를 때렸다.
→ `ClearFieldProjectiles()`로 **세 종류를 전부** 정리하고, 호출 시점도 자석 연출 뒤가 아니라
**웨이브 종료 즉시**(`HandleWaveEnded` 맨 앞)로 옮겼다(연출 0.35초 동안 맞는 것도 막는다).
자석 연출 뒤에 한 번 더 훑는다. 실측: 투사체 4개(적 3 + 빔 1) 생성 → 웨이브 종료 → 로그
"투사체 4개 정리" → 다음 프레임에 세 종류 모두 0개.

### 8) 고등급 아이템 가격을 등급당 25% 증가로 변경

가격 배율을 `1 / 1.8 / 3 / 5 / 8`(전설이 일반의 8배)에서 **1.25^등급 = 1 / 1.25 / 1.5625 /
1.953 / 2.441**(전설 2.44배)로 바꿨다. 무기는 `WeaponTableGenerator.PriceMultipliers`,
디스크는 `DiscTableGenerator`(개별 정의에 흩어져 있던 `price` 필드를 제거하고 등급별 표
`GradePrices = {20, 25, 31, 39, 49}`로 통합), 표시상의 원본인 `ShopCatalog.gradeSettings.priceMultiplier`도
같은 값으로 맞췄다. 메뉴 재생성 후 실측: 중기관총 45/56/70/88/110, 기관단총 30/38/47/59/73,
생존단검 20/25/31/39/49, 디스크 일반 20 / 희귀 25.

### 9) 근접무기 타격 판정을 실제 찌르기 동작과 일치시킴

사용자 리포트: "근접무기는 코앞에서 휘두르는데 맵 전체의 좀비가 때려진다. 찌르듯이 한번 앞으로
갔다 돌아오며 타격을 주어야 한다."

**원인**: 판정이 `MeleeSwing.Execute(총구, 방향, weapon_range, …)`였고 근접 3종의 `weapon_range`가
**4.95~5.85유닛**이었다. 카메라 세로 가시 반경이 5.4라 사실상 화면 전체가 타격 범위였다.
반면 찌르기 연출은 0.5~0.65유닛만 움직이는 순수 시각 효과였다(둘이 완전히 분리되어 있었다).
이 프로젝트에서 반복된 **"시각 크기와 판정 크기가 서로 다른 상수를 쓰는" 버그의 네 번째 사례**다
(앞선 셋: `EnemyProjectile` 콜라이더, PC 콜라이더, 폭발 반경).

**수정**: 찌르기 동작 자체가 판정이 되도록 바꿨다.
- 발사 시점에 즉발 판정하지 않고 `StartMeleeThrustVisual`이 판정 파라미터(데미지/방어무시/넉백)를
  저장하고, `UpdateMeleeThrustVisual`이 **칼이 나가는 프레임마다**(t < 0.5, 즉 찔러 나가는 구간)
  판정한다 → 최대로 뻗은 지점만이 아니라 **찌르는 경로 전체**가 타격 범위가 된다.
- 판정 범위는 **무기 스프라이트의 월드 bounds**(`hand_sprite_renderer.bounds`)에서 읽는다.
  스케일·회전·좌우 반전·소켓 위치가 전부 자동 반영되므로 "보이는 것 = 맞는 것"이 구조적으로 보장된다.
- 같은 스윙에서 한 적이 여러 프레임에 걸쳐 중복 피격되지 않도록 `WeaponRuntimeState`에
  스윙 단위 `HashSet<EnemyUnit>`을 두고 `MeleeSwing.Execute`에 넘긴다(외부 집합을 받는 오버로드 신설).

**높이 보정이 핵심이었다.** 실측 결과 무기 소켓은 플레이어 발밑 기준 y +0.53~0.93(어깨 높이)인데
적 히트박스는 자기 원점 기준 y −0.62~−0.02(다리~아랫배)에 있다. 이 게임의 y축은 화면상 높이와
진행 방향을 겸하므로, 칼 그림의 월드 위치를 그대로 판정하면 **수직으로 0.5유닛 이상 떠서 아무도
맞지 않는다**(실측으로 확인). 그래서 수평 위치는 칼을 그대로 따르고 **판정 높이만 소켓 높이만큼
내려** 발밑 기준으로 맞췄다(보정 후 판정 중심 y = −0.03, 좀비 콜라이더 중심 −0.22와 맞물린다).
좀비의 근접 공격 판정(원점 간 거리)이나 자동 조준(적의 원점을 겨냥)과 같은 기준이다.

무기 데이터도 실측값으로 다시 잡았다(`WeaponTableGenerator` → 메뉴 재생성). 근접의 `range`/`detect`는
이제 판정 크기가 아니라 **"이 거리에 적이 들어오면 찌르기를 시작한다"는 감지 거리**이고,
`atsize`는 근접에서 **찌르는 거리**로 재활용되는 값이라 함께 늘려 동작이 실제로 앞으로 쭉 나가게 했다.

| 무기 | 사거리·감지 | 찌르는 거리(atsize) |
|---|---|---|
| 생존단검 | 4.95 → **1.4** | 0.5 → **0.9** |
| 전술 마체테 | 5.85 → **1.6** | 0.65 → **1.05** |
| 전기톱검 | 5.4 → **1.5** | 0.55 → **0.95** |

**실측 검증**(정지 좀비를 거리별로 배치):
- 생존단검(감지 1.4): 거리 0.9·1.3 좀비 처치 / **1.8·2.5·4.0 무피해**.
- 전술 마체테(감지 1.6): 1.4·1.6·−1.6(반대 방향)·1.9 처치 / **2.4 무피해**
  (1.9까지 맞는 것은 칼이 실제로 x≈1.65까지 뻗고 좀비 콜라이더 반폭이 0.5이기 때문 - 화면과 일치).
- 정지 상태(찌르지 않을 때) 판정 최대 도달 0.94~0.96유닛.

### 10) 웨이브가 지날수록 공격력도 5% 증가

위 4)의 배율을 체력과 공격력에 함께 적용했다(필드명도 `hpIncreasePerWave` → `statIncreasePerWave`).
실측: 웨이브 2 좀비 공격력 15 → 16.

### 11) 방어력이 적 공격력보다 높아도 반드시 1의 피해를 받도록 변경

`PlayerRobotController.TakeDamage`의 하한이 `Mathf.Max(0, …)`이라 방어력만 올려두면 특정 적에게
**완전 무적**이 됐다(좀비 공격력 15 vs 방어력 15 → 0 피해). 방어력 업그레이드 몇 번으로 난이도가
통째로 무너지던 원인이다. → `Mathf.Max(1, …)`. 적 쪽 피해 계산(`EnemyUnit.TakeDamage`)은 원래부터
최소 1이었으므로 이제 양쪽 규칙이 같다. 실측: 방어력 105 vs 공격력 5 → 체력 100 → 99(정확히 1).

### 검증 요약

- `assets-refresh` 재컴파일 **Error 0건**(경고는 기존 `ModdingPanelUI.enableWordWrapping` 1건뿐).
- 무기 테이블 검증(메뉴 `Comstock/무기 테이블 검증`) **8개 검사 전부 통과**, 무기 65행 / 무기메타 65행 /
  상점 65행, 디스크 21종 재생성.
- 플레이모드 실측: 근접 판정 거리(위 9), 웨이브 2 체력·공격력 배율, 골드 확률 0.4167,
  방어력 하한 1, AI 코어 카드 문구("희귀 장갑판 보강 / 방어력 +0.5" 등 실제 UI 텍스트 확인),
  투사체 3종 정리, 웨이브별 스폰 커브 20웨이브 전량.
- 수정 파일: 스크립트 10개(`AiCoreUpgradePool`, `AiCoreManager`, `GameFlowManager`, `EnemySpawner`,
  `WaveManager`, `EnemyUnit`, `SpitterUnit`, `MeleeSwing`, `PlayerShootManager`, `PlayerRobotController`),
  에디터 2개(`WeaponTableGenerator`, `DiscTableGenerator`), 데이터 3개(`GameDataAsset`,
  `AiCoreUpgradePool`, `ShopCatalog`), 씬 1개(`Ground01.unity`).

### 남은 판단 사항 (사용자 확인 필요)

- **좀비 공격 사거리 1.6유닛**(몸이 닿는 거리 0.586의 2.7배)은 여전히 미해결이다. 이 값 때문에
  근접무기 감지 거리(1.4~1.6)가 좀비 공격 사거리와 거의 같아, 근접은 "맞으면서 때리는" 무기가 된다.
  1.6 → 0.8로 낮추면 근접무기가 먼저 때릴 수 있고 찌르는 거리도 더 짧게(=그림에 더 충실하게) 줄일 수 있다.
- 최대 레벨 70은 "메모리(머리 파츠)가 정하는 값"의 임시값을 3.5배로 맞춘 것이다. 파츠 데이터로
  옮길 때 이 배수를 함께 반영해야 한다.
- 웨이브 20 기준 최대 동시 생존 86마리 / 초당 9.2마리는 이중 누적 버그를 고친 뒤의 값이라
  체감 난이도가 예전과 크게 다르다. 실플레이로 재조정이 필요할 수 있다.

---

## 2026-08-14

### 몬스터 공격력 3건 조정 `[완료]`

사용자 지시로 `GameDataAsset.asset`의 `monsters` 배열을 직접 수정(스크립트 변경 없음, MCP
`assets-modify` pathPatches로 개별 필드만 갱신).

- 스피터(200003) `monster_atk` 10 → **18**
- 차저(200002) `monster_atk` 85 → **35**(2026-08-13에 15→85로 올랐던 값을 대폭 하향)
- 디스럭터(200005) `monster_atk` 100 → **75**

### 근접무기 사거리 10% 상향 `[완료]`

사용자 요청 "근접 무기 사거리 10%씩 늘려줘". 근접 3종의 감지거리(range/detect)와 실제 타격
거리(atsize, 찌르는 거리)를 **둘 다** 1.1배 했다(plan 단계에서 "감지거리만 늘리면 실제로
맞는 범위는 그대로라 스윙만 헛스윙하는 거리가 늘어난다"는 점을 짚어 사용자에게 확인받음 -
둘 다 늘리는 쪽으로 확정).

| 무기 | range/detect | atsize(찌르는 거리) |
|---|---|---|
| 생존단검 | 1.4 → **1.54** | 0.9 → **0.99** |
| 전술 마체테 | 1.6 → **1.76** | 1.05 → **1.155** |
| 전기톱검 | 1.5 → **1.65** | 0.95 → **1.045** |

`Editor/WeaponTableGenerator.cs`의 `Defs` 소스를 고치고(생성된 에셋을 직접 patch하면 다음
재생성 때 값이 되돌아가므로 반드시 소스를 고쳐야 함) 메뉴 `Comstock/무기 테이블 65행 재생성` 실행,
`Comstock/무기 테이블 검증`으로 **8개 검사 전부 통과** 확인.

### AI 코어 신규 업그레이드 "사거리 증폭" `[완료]`

사용자 요청: AI 코어 레벨업 선택지에 무기 사거리/감지거리를 등급별 0.5~2.5%씩 올리는
업그레이드 신설. 확인 결과 "사거리와 감지거리가 항상 함께" 오르는 **단일 옵션**으로 확정
(사용자: "무기 사거리 감지거리 같이 오르는걸로"), 등급 배율은 기존 공용 공식(1/1.67/2.33/
3.33/5배)을 그대로 써서 정확한 0.5/1/1.5/2/2.5%가 아니라 **0.5/0.83/1.17/1.67/2.5%**가
나오는 것을 사용자가 확인하고 받아들임.

- `StatType.cs`에 `WeaponRangeBonus` 열거값 추가 - `GoldGain`과 같은 성격(로봇 스탯이 아니라
  별도 시스템이 `RunState.CoreStatBonuses`에서 직접 읽는 % 보너스)이라 `RobotStats.ApplyBonus`
  스위치에는 케이스를 추가하지 않았다(매칭 case가 없으면 안전하게 무시됨, `GoldGain`과 동일).
- `AiCoreUpgradePool.asset`에 새 Option 추가(`displayName`="사거리 증폭", `statType`=
  `WeaponRangeBonus`, `amount`=0.5) - MCP `assets-modify`로 `options` 리스트 전체를
  덮어써서 9번째 항목으로 추가(리스트 인덱스 확장은 `pathPatches`가 지원하지 않아 전체 배열을
  다시 써야 했음).
- `AiCoreUpgradePool.BuildEffectLine()`에 `WeaponRangeBonus` case 추가(% 표시).
- `DiscData.cs`의 `StatTypeNames.ToKorean()`에 "무기 사거리" 표시명 추가.
- `PlayerShootManager.cs`의 `GetTravelRange()`/`GetDetectRange()` 양쪽에
  `GetWeaponRangeBonusMultiplier()`(= `1 + RunState.CoreStatBonuses[WeaponRangeBonus]/100`)를
  곱해 사거리·감지거리가 항상 함께 오르게 했다. 상점 상세 팝업(`GetEffectiveTravelRange`/
  `GetEffectiveDetectRange`)은 이 두 함수를 그대로 호출하므로 별도 수정 없이 자동 반영됨.

**검증**(script-execute로 정적 계산): `BuildEffectLine` 결과 Normal=+0.5% / Rare=+0.83% /
Epic=+1.17% / Unique=+1.67% / Legendary=+2.5% (사용자가 확정한 공식과 정확히 일치).
`CoreStatBonuses[WeaponRangeBonus]=2.5` 설정 시 배율 1.025 확인.

### 피격 피드백 강화(카메라 흔들림 + 넉백 + 화면 비네트 + SFX 인프라) `[완료]`

사용자 피드백: "지금 캐릭터가 피격당했는지 알 방법이 많지가 않다." Explore 에이전트 2개로
조사한 결과 기존 피격 피드백은 `HitFlash`(0.25초 흰색 점멸)와 HP바 색 변화 두 가지뿐이고
카메라 흔들림·SFX·넉백·데미지 숫자·컨트롤러 진동은 전무했다. Plan 에이전트로 설계를 검증받고
plan mode로 사용자 확인(SFX는 임시 비프음 우선 적용, 넉백 포함)을 거쳐 구현했다.

**아키텍처**: `HitFlash`/`DiscEffectRuntime`과 동일하게 `PlayerHitFeedback.cs`(신규)를
`PlayerRobotController.Awake()`에서 자동 부착. 단, **넉백 필드/소비는 `PlayerRobotController`
안에 그대로 뒀다** - `FixedUpdate()`가 매 프레임 `rb.linearVelocity`를 통째로 덮어쓰므로 넉백은
그 직후에 같은 클래스 안에서 더해야 하며, 별도 컴포넌트의 `FixedUpdate`로 분리하면 Unity가
두 `MonoBehaviour`의 실행 순서를 보장하지 않아 특정 프레임에 조용히 씹힐 수 있다.

- `PlayerRobotController.TakeDamage(int, Vector3? attackerPosition = null)`로 시그니처 확장,
  6개 호출부(`EnemyUnit`/`ChargerUnit`/`DisruptorUnit`/`SprinterUnit`/`EnemyProjectile`/
  `BossUnit`) 전부 공격자 위치를 넘기도록 갱신.
- **넉백**: `knockbackVelocity`를 공격자 반대 방향으로 덮어쓰고(누적 안 함 - `EnemyUnit.
  ApplyKnockback`과 동일 정책) `FixedUpdate`에서 매 프레임 감쇠(`knockbackDecay`=30/초)시키며
  이동 브랜치에 더한다. 구르기/마지막 발악 무적 구간은 `TakeDamage` 최상단의 기존 가드가
  그대로 막아줘 별도 처리가 필요 없다.
- **카메라 흔들림**: `CameraFollow.Shake(duration, magnitude)` 신설. `LateUpdate()`에서
  기존 1차 맵 클램프 결과에 흔들림 오프셋을 더한 뒤 `MapBounds.ClampCameraCenter`로 한 번 더
  클램프해 맵 경계 밖 빈 공간이 비치지 않게 했다.
- **화면 비네트**: `PlayerHitFeedback`이 `FindFirstObjectByType<Canvas>()`로 찾은 캔버스
  밑에 코드로 풀스크린 `Image`를 생성(앵커 Stretch만 사용, `EquipmentDetailPopup`과 동일
  관례). 피격 시 알파 0.35로 즉시 올리고 0.25초 페이드. `GameFlowManager.IsIntermission`을
  직접 체크해 정비/상점 전환 중엔 강제로 숨긴다.
- **SFX**: `SFXManager.cs`(신규) - `MusicManager`와 동일한 부트스트랩 패턴(싱글톤 +
  `RuntimeInitializeOnLoadMethod` + `DontDestroyOnLoad`). `Resources/SFX` 폴더를 스캔해 실제
  파일을 우선 로드하고, 아직 파일이 없는 `"Player_Hit"` 키만 절차적 사인파 비프음
  (`AudioClip.Create`, 0.08초 880Hz)으로 채운다 - 나중에 같은 이름의 실제 오디오 파일만
  추가하면 코드 수정 없이 그 파일이 자동으로 우선 쓰인다.

**검증**(플레이모드, `script-execute`로 리플렉션 실측): 플레이어 기준 +x 방향에 가상의
공격자를 두고 `TakeDamage(20, attackerPos)` 호출 결과 - `knockback=(-3.50, 0.00, 0.00)`
(정확히 반대 방향·설정한 세기), `camShake(timeLeft=0.15, mag=0.15)`(트리거 직후 값과 일치),
`vignetteAlpha=0.35`(설정한 최대 알파), `sfxIsPlaying=True`(효과음 재생 확인, `PlayOneShot`은
`AudioSource.clip`을 갱신하지 않으므로 clip 필드가 null인 것은 정상). `HP 30→15`(공격력 20 -
방어력만큼 차감, 정상 계산). `PlayerRobotController`/`PlayerHitFeedback`/`HitFlash` 컴포넌트가
전부 자동 부착돼 있음도 확인.

**테스트 과정에서 발견한 주의사항**(버그 아님, 기록해 둠): Unity Editor가 이미 플레이모드로
실행 중인 상태에서 스크립트를 수정해 재컴파일(도메인 리로드)이 일어나면
`RuntimeInitializeOnLoadMethod(BeforeSceneLoad)`가 재실행되지 않아 `MusicManager`/
`SFXManager` 둘 다 `Instance`가 null이 된다(`BeforeSceneLoad`는 "최초 씬 로드 전" 한 번만
발동하는 지점이라 도중 도메인 리로드로는 재발동하지 않음). 플레이모드를 완전히 종료했다가
새로 시작하면 정상 동작한다 - 실제 사용자 플레이(에디터에서 Play 버튼을 누르거나 빌드 실행)
에서는 항상 깨끗한 상태로 시작하므로 이 문제가 발생하지 않는다.

**검증 요약**: 스크립트 재컴파일 Error 0건(`assets-refresh` + 콘솔 확인), 무기 테이블 검증
8개 전부 통과, AI 코어 옵션 등급별 문구 실측 일치, 피격 피드백 4종(넉백/흔들림/비네트/SFX)
플레이모드 실측 확인. 수정 파일: 스크립트 12개(`PlayerRobotController`, `CameraFollow`,
`StatType`, `AiCoreUpgradePool`, `DiscData`, `PlayerShootManager`, `EnemyUnit`, `ChargerUnit`,
`DisruptorUnit`, `SprinterUnit`, `EnemyProjectile`, `BossUnit`), 신규 스크립트 2개
(`PlayerHitFeedback`, `SFXManager`), 에디터 1개(`WeaponTableGenerator`), 데이터 2개
(`GameDataAsset`, `AiCoreUpgradePool`).

### 데미지 숫자 팝업 / 컨트롤러 진동 `[보류]`

피격 피드백 계획 과정에서 파생된 아이디어. 데미지 숫자 팝업은 "얼마나 맞았는지"를 더하는
것이라 이번 "피격을 알아차리기 어렵다" 문제와 성격이 달라 이번 범위에서 제외했다. 컨트롤러
진동은 프로젝트에 게임패드 입력 자체가 없어(`Keyboard.current` 전용) 범위 밖. 둘 다 필요시
후속 작업으로 남긴다.

## 2026-08-18 인게임 상단 HUD 재배치 · 병합

### 배경 `[완료]`

사용자가 인게임 HUD 구성 기획서를 첨부하며 지적: "쓸데없이 2개로 나뉘어 있는 UI들이 존재한다.
체력이 게이지와 숫자로 나뉘어 있고, 레벨/경험치도 마찬가지다. 골드·부품상자는 이미 그림
아이콘이 있으므로 항목명을 글자로 또 쓸 필요가 없고, 따라서 좌우로 UI가 길 필요도 없다."

실제 조사 결과 그대로였다. `Canvas/HP`(하트 아이콘 + 숫자 패널)와 `Canvas/Hp_Slider`(게이지)가
**서로 다른 루트 오브젝트**로 위아래에 따로 놓여 있었고, `TopHud/ExpLevelText`와
`TopHud/ExpSlider`도 마찬가지였다. 상단이 3열 2행 구조로 화면 폭의 0.295~0.865(=57%)를
차지하고 있었다.

### 확정한 배치 (plan mode에서 사용자 확인)

| 구역 | 내용 |
|---|---|
| 좌상단 세로 스택 | ① 체력 바(하트 아이콘 + `100 / 100`) ② 경험치 바(별 아이콘 + 레벨 숫자 + `18 / 30`) ③ 골드 아이콘 + `1257` ④ 부품상자 아이콘 + `07 / 20` |
| 상단 중앙 | `WAVE [1]` / `00:19` |
| 우하단 | 구르기(DASH) 쿨다운 아이콘 |

- 바 안 라벨은 **아이콘만** 쓴다("체력"·"레벨" 글자 없음 - 사용자가 기획서의 글자 라벨 대신
  기존 하트/별 아이콘 유지를 택했다. 아이콘이 있으면 글자는 불필요하다는 본인 지적과 일관).
- 웨이브는 **현재 웨이브만** 표시(`WAVE [5]`). 기획서의 `/20`(최종 웨이브)은 사용자 지시로 뺐다.
- DASH는 기존 `Dash_skill` 아이콘 + 위→아래 채움 덮개를 그대로 쓰고 **위치만** 우하단으로 옮겼다
  (기획서의 가로 바로 바꾸면 기존 아이콘 아트를 버리게 된다).

### 씬 변경 — `Ground01.unity`

**`Canvas/HP`를 좌상단 스택의 컨테이너로 승격**시켰다(앵커 (0,0)~(1,1) 전체화면). 이 오브젝트에
**`GameHUD` 컴포넌트가 붙어 있어서** 비활성화하거나 지우면 HUD 전체가 죽는다 - 그래서 새 컨테이너를
만들지 않고 이걸 재활용한 것이 요점이다.

```
Canvas/HP  (GameHUD 부착, 전체화면 컨테이너)
 ├ HpGroup_BG [비활성]        ← 더 이상 필요 없는 숫자 패널 배경
 ├ Hp_ui [비활성]             ← 이전부터 비활성이던 "HP : " 잔재
 ├ Hp_Slider                  ← Canvas 루트에서 이동
 │   ├ Background / Fill Area/Fill
 │   ├ HeartIcon_Shadow, HeartIcon   ← HP에서 이동(바 안 왼쪽)
 │   └ Hp_value                      ← HP에서 이동(바 안 가운데)
 ├ ExpSlider                  ← TopHud에서 이동
 │   ├ Fill Area/Fill
 │   ├ Icon_Shadow, Icon             ← ExpLevelText에서 이동(Star_panel/Star_icon)
 │   ├ ExpLevelText                  ← 레벨 숫자만
 │   └ ExpValueText [신규]           ← ExpLevelText를 Instantiate해서 만듦(폰트/머티리얼 보존)
 ├ GoldRow [신규] ─ Icon(Gold_icon00) + GoldText
 └ PartBoxRow [신규] ─ Icon[신규](Resources/ItemBox) + PartBoxText
Canvas/TopHud   ← 웨이브/시간만 남고 상단 중앙으로. 나머지 _BG 3장은 비활성
Canvas/DashSkill ← 앵커만 우하단으로(픽셀 오프셋 (-104,-108) → 0으로 정리)
```

**오브젝트는 하나도 지우지 않았다** - 안 쓰는 배경 패널 4장(`HpGroup_BG`/`GoldText_BG`/
`PartBoxText_BG`/`ExpLevelText_BG`)은 전부 `SetActive(false)`로만 껐다(이 프로젝트의 기존 관례).

**해상도 안전성**: 캔버스가 `ConstantPixelSize`라 절대 픽셀 배치는 FHD 밖에서 깨진다.
좌상단 스택·웨이브·DASH를 전부 **정규화 앵커 + offsetMin/Max = 0**으로 통일했다
(기존 `HP`/`Hp_Slider`/`DashSkill`은 앵커 + 픽셀 오프셋을 섞어 써서 원래 위험한 상태였다).

주요 앵커(전부 `sizeDelta`·`anchoredPosition` = 0):

| 오브젝트 | anchorMin | anchorMax |
|---|---|---|
| `Hp_Slider` | 0.015, 0.930 | 0.215, 0.986 |
| `ExpSlider` | 0.015, 0.860 | 0.215, 0.916 |
| `GoldRow` | 0.015, 0.805 | 0.145, 0.850 |
| `PartBoxRow` | 0.015, 0.752 | 0.145, 0.797 |
| `WaveText_BG` / `WaveText` | 0.420, 0.945 / 0.425, 0.949 | 0.580, 0.995 / 0.575, 0.991 |
| `WaveTimeText_BG` / `WaveTimeText` | 0.445, 0.893 / 0.450, 0.897 | 0.555, 0.940 / 0.550, 0.936 |
| `DashSkill` | 0.935, 0.055 | 0.985, 0.144 |

바 안 아이콘은 `0.020~0.115` x `0.12~0.88` + `preserveAspect`(고정 46px을 쓰면 해상도가 바뀔 때
바 높이만 변하고 아이콘은 그대로라 어긋난다).

**`GameFlowManager.combatHudObjects`를 `HP` / `TopHud` / `PartBoxIndicators` / `DashSkill` 4개로
정리**했다(`Hp_Slider`는 이제 `HP` 자식이라 자동으로 따라 숨는다). 이 배열에 빠지면 정비/상점
화면에서 HUD가 비쳐 보인다.

작업 중 발견한 별건: **`Canvas/GameOver`가 씬에 켜진 채 저장돼 있었다**(런타임에는 `GameHUD.Awake`가
꺼서 문제없지만 에디터 씬 뷰에서 게임오버 UI가 겹쳐 보였다). 확인해 보니 이번 세션 시점에는 이미
꺼져 있어 추가 조치는 하지 않았다.

### 스크립트 변경 — `GameHUD.cs`

- `expValueText` 필드 신설(경험치 바 가운데 `18 / 30`). `expLevelText`는 레벨 숫자만 담당하게 됐다.
  TMP는 **같은 줄 안에서 왼쪽/가운데 정렬을 섞을 수 없어서**(`<align>` 태그는 줄 단위) 텍스트를
  둘로 나눈 것이다.
- 포맷 문자열 교체:

| 대상 | 이전 | 이후 |
|---|---|---|
| `waveText` | `웨이브 5` | `WAVE [5]` |
| `waveTimeText` | `0:28` | `00:28` (분도 2자리 고정 - 폭이 흔들리지 않게) |
| `goldText` | `골드 56` | `56` |
| `partBoxText` | `부품 상자 3/20` | `03 / 20` |
| `expLevelText` | `레벨 5  16/30` | `5` |
| `expValueText`(신규) | — | `16 / 30`, 최대 레벨이면 `MAX` |

`hpValueText`(`100 / 100`), `"보스 처치"`, `UpdateHpBarArt`(초록↔빨강 교체), `UpdateDashCooldown`은
손대지 않았다.

**건드리지 않은 것**: `ShopPanelUI.RefreshHeader()`가 `웨이브 N`/`골드 N`을 자체적으로 만들지만,
상점은 아이콘이 없는 별도 전체화면 UI라 라벨이 필요해서 그대로 뒀다. `ModdingPanelUI`
(`WAVE 07 / 20`), `GameOverSummaryUI`도 동일한 이유로 유지.

### 검증 (전부 플레이모드 실측)

1. **FHD(1920x1080) 캡처** - 좌상단 4줄 스택 / 상단 중앙 `WAVE [1]`·`00:59` / 우하단 DASH가
   겹침 없이 정상 표시.
2. **해상도 안전성** - 1280x720, 2560x1440에서 각각 캡처. 세 해상도 모두 상대 배치가 동일하게
   유지됨(정규화 앵커 배치가 실제로 동작함을 확인).
3. **값 갱신** - `RunState`를 런타임에 바꿔 확인: 골드 `1257`, 부품상자 `07 / 20`,
   경험치 레벨칸 `13`·값칸 `18 / 30`, 웨이브 `WAVE [1]`·시간 `00:19` 전부 정상.
4. **체력 바 색** - 체력을 20/100(20%)으로 내리자 `Fill` 스프라이트가 `Red_bar00`으로 교체됨
   (`hpDangerRatio`=0.4 기준 정상 동작), 숫자도 `20 / 100`.
5. **구르기 쿨다운** - `dashCooldownLeft`를 0.9/1.2초로 설정하자 덮개가
   `type=Filled fillAmount=0.75 enabled=True`로 정확히 반영.
6. **정비/상점 전환** - `GameFlowManager.SetCombatHudVisible(false)`를 실제 배열로 호출해
   `HP`/`TopHud`/`PartBoxIndicators`/`DashSkill` 4개가 전부 `activeSelf=False`가 되는 것 확인.
7. 스크립트 재컴파일 에러 0건.

### 다듬은 점

첫 캡처에서 골드(`56`)와 부품상자(`03 / 20`)의 글자 크기가 서로 달라 보였다. 두 텍스트 모두
오토사이즈인데 `03 / 20`이 더 길어 칸에 맞추느라 혼자 줄어든 것이 원인이었다. 줄 폭을
`0.115` → `0.145`로 넓혀 둘 다 최대 크기(30)로 그려지게 했다(아이콘도 함께 커져 가독성이 좋아졌다).

**수정 파일**: `Assets/Scripts/GameHUD.cs`, `Assets/Scenes/Ground01.unity`.

### AI 코어 업그레이드 화면에 "골드 리롤" / "레벨업 취소" 버튼 추가 `[완료]`

사용자 요청: "AI 코어 업그레이드에 골드 리롤, 레벨업 취소 후 경험치 50% 반환 버튼 두 개 넣어줘."

plan 단계에서 확정한 사항:
- **리롤 비용**: 상점 새로고침과 같은 누적 공식(기본 10골드 + 회당 +5).
- **누적치 초기화**: **카드 화면마다**. 레벨업이 연달아 밀려도 매번 10골드부터 시작한다.
- **"레벨업 취소"의 의미**: **레벨까지 되돌린다.** 업그레이드를 받지 않고 레벨을 1 내리는 대신
  그 레벨업에 쓴 경험치의 50%를 돌려받는다(레벨만 유지하고 환급하는 안은 이 게임에서 레벨 자체가
  수치 효과가 없어 순수 페널티가 되므로 사용자가 탈락시킴).

#### 구현

**`RunState.cs`** — `CoreRerollCount` 신설(`ShopRefreshCount`와 같은 성격). `Reset()`에도 등록.

**`AiCoreManager.cs`** — 인스펙터 필드 `rerollBaseCost`(10) / `rerollCostIncrement`(5) /
`cancelExpRefundRatio`(0.5) 추가하고 아래 API 신설.

| API | 내용 |
|---|---|
| `CurrentRerollCost` | `base + increment * CoreRerollCount` (상점 `GetRefreshCost`와 동일 공식) |
| `ResetRerollCount()` | 카드 화면을 열 때 누적치를 0으로 |
| `TryReroll()` | 골드가 되면 차감 + 카운트 증가 + `NotifyChanged()`, 아니면 false (`ShopManager.TryRefresh` 복제) |
| `CancelRefundExp()` | 환급량. 지금 레벨 L에 올라오며 쓴 경험치는 **레벨 L-1 기준 필요량**이므로 `(expBase + expPerLevel*(L-1)) * 0.5`를 반올림 |
| `CanCancelLevelUp` | 대기 선택이 있고 레벨 > 1 |
| `CancelLevelUp()` | 레벨 -1, 대기 -1, 경험치 환급 |

**함정 하나를 실제로 밟아서 막았다**: 환급 경험치를 그냥 더하면 `HandleRunStateChanged`의
`while` 루프가 **곧바로 다시 레벨업시켜 같은 카드 화면이 또 뜬다**. 환급량이 정확히 필요량의
절반이라, 레벨업 직후 잔여 경험치가 절반을 넘고 있었으면 **항상** 재레벨업한다(대략 50% 확률).
그래서 환급 후 경험치를 **다음 레벨 필요량 - 1**로 클램프한다. 검증에서 실제로 이 경로를 탔다
(잔여 10 + 환급 8 = 18 → 필요량 16 → **15로 제한**).

**`AiCoreExtraButtonsUI.cs`(신규)** — 버튼 2개 + 안내 문구를 **코드로 생성**한다
(`MusicVolumeSliderUI.Attach()` / `EquipmentDetailPopup` 관례). AI 코어 화면은 전용 UI 스크립트
없이 `GameFlowManager`가 직접 그리고 있어서, 버튼을 씬에 깔면 직렬화 필드가 또 늘어난다 —
**씬 수정 0**으로 끝내려고 코드 생성을 택했다. 아트는 상점 버튼과 같은 `UI/Purple_button00`
(Sliced, 못 찾으면 단색 폴백). 배치는 앵커 비율 `y 0.160~0.240`으로, 카드(중앙 기준 높이 320px)
아래라서 어떤 해상도에서도 겹치지 않는다.

**`GameFlowManager.cs`** —
- `ShowAiCoreUpgradeStep()`에서 **카드를 그리는 부분만 `DrawAndRenderAiCoreChoices()`로 분리**했다.
  리롤은 패널을 닫았다 여는 게 아니라 카드만 다시 그려야 하는데, `ShowAiCoreUpgradeStep()`은
  맨 앞에서 `CloseAllIntermissionPanels()`로 패널을 꺼버리기 때문이다.
- 카드 버튼은 매번 `RemoveAllListeners()`를 돌리므로 **새 버튼은 그 배열에 섞지 않고** 생성 시
  한 번만 리스너를 단다(`ShopPanelUI.Awake` 관례).
- `HandleAiCoreRerollClicked()` / `HandleAiCoreCancelClicked()` 신설. 취소는 대기 건수를 하나
  줄이므로 이후 흐름이 카드를 고른 경우와 완전히 동일하다(`ShowNextIntermissionStep()`).

**`EnsureAiCoreExtraButtons()`(지연 생성)** — 처음에는 `Awake()`에서만 붙였는데 검증 중
**버튼이 아예 안 생기는** 현상이 있었다. 원인은 에디터에서 스크립트 재컴파일로 도메인 리로드가
일어나면 **직렬화되지 않는 private 필드가 null로 돌아가고 `Awake`는 다시 실행되지 않기** 때문이다
(2026-08-14 `MusicManager`/`SFXManager` 항목과 같은 계열의 에디터 전용 함정). 카드 화면을 열 때도
없으면 다시 붙이도록 `EnsureAiCoreExtraButtons()`를 넣어 해결했다(기존 자식이 있으면 재사용).

#### 검증 (플레이모드 실측)

- **리롤 비용/차감**: 10 → 15 → 20 → 25로 증가하고 골드가 506 → 496 → 481 → 461로 정확히
  차감됨. 3회 모두 카드 3장이 실제로 새로 뽑힘.
- **골드 부족**: 골드 5로 만들고 리롤 시 **차감 없음** + `골드가 부족합니다 (리롤 25골드)` 안내 표시.
- **취소**: 레벨 7 → 6, 대기 6 → 5, 경험치 10 → **15**(클램프 동작), 되돌린 레벨의 필요량 16보다
  작아 **즉시 재레벨업이 발생하지 않음**을 확인. 취소 직후 다음 카드가 이어서 뜨고 리롤 비용은
  10골드로 초기화, 안내 문구도 비워짐.
- **반복 취소**: 레벨 6/대기 5에서 다섯 번 연속 취소 → 레벨 1/대기 0, `CanCancelLevelUp`이 false로
  바뀌어 버튼이 비활성화됨. 대기가 0이 되자 AI 코어 패널이 닫히고 정비 화면으로 정상 진행.
- **FHD 캡처**로 두 버튼이 카드 아래 중앙에 겹침 없이 표시되는 것 확인.
- 씬은 **전혀 수정하지 않았다**(`Ground01.unity` dirty=False, 에디트 모드 패널 자식에 런타임
  생성 버튼이 남지 않음).

**수정 파일**: `RunState.cs`, `AiCoreManager.cs`, `GameFlowManager.cs` / **신규**: `AiCoreExtraButtonsUI.cs`.

### AI 코어 업그레이드 화면에 골드 보유량 표시 추가 `[완료]`

사용자 지적: 정비 중이라 `GameHUD`가 꺼져 있어(`GameFlowManager.SetCombatHudVisible(false)`)
리롤 비용을 낼 수 있는지 판단할 골드 보유량이 화면 어디에도 안 보였다.

`AiCoreExtraButtonsUI`에 골드 아이콘(`UI/Gold_icon00`) + 숫자를 우상단(앵커 y 0.895~0.955,
카드 배너보다 위라 겹치지 않음)에 추가했다. 배경은 HUD 골드 표시와 같은 `UI/Panel01`.
`Refresh()`에 `gold` 인자를 추가해 카드를 새로 그릴 때(`DrawAndRenderAiCoreChoices`)마다
갱신되므로, 리롤로 골드가 줄어들면 즉시 반영된다.

**검증**(플레이모드): 골드 1234 표시 확인(아이콘·배경 스프라이트 정상 로드), 리롤 실행 후
1234 → **1229**로 즉시 갱신(실제 `RunState.Gold`와 일치). 씬은 이번에도 수정하지 않았다
(버튼과 마찬가지로 코드 생성).

**수정 파일**: `AiCoreExtraButtonsUI.cs`, `GameFlowManager.cs`(`Refresh()` 호출에 `gold` 인자 추가).

### AI 코어 업그레이드 화면 "레벨업 취소" 버튼 완전 삭제 `[완료]`

사용자 요청: "경험치 반환 제거". 경험치 반환 없이는 취소 기능 자체가 의미 없다고 판단해
**버튼을 통째로 삭제**하기로 확정(질문으로 확인).

- `AiCoreManager.cs`: `CanCancelLevelUp`/`CancelRefundExp()`/`CancelLevelUp()`/
  `cancelExpRefundRatio` 필드를 전부 제거. 골드 리롤 관련 코드는 그대로 유지.
- `GameFlowManager.cs`: `HandleAiCoreCancelClicked()` 삭제, `Attach()`/`Refresh()` 호출에서
  취소 관련 인자 제거.
- `AiCoreExtraButtonsUI.cs`: `cancel_button`/`cancel_label` 필드와 `CancelLevelUpButton` 생성
  코드 삭제. 버튼이 리롤 하나만 남아 **중앙(x 0.410~0.590)으로 재배치**했다.

**검증**(플레이모드): `AiCoreExtraButtons` 자식이 `GoldDisplay_BG`/`GoldIcon`/`GoldText`/
`RerollButton`/`Message` 5개만 남고 `CancelLevelUpButton`은 없음을 확인. 리롤은 이전과 동일하게
정상 동작(골드 500 → 490). FHD 캡처로 리롤 버튼이 카드 아래 중앙에 단독 배치된 것 확인.
씬은 이번에도 수정하지 않았다.

**수정 파일**: `AiCoreManager.cs`, `GameFlowManager.cs`, `AiCoreExtraButtonsUI.cs`.

### 좀비 드랍 아이템(경험치·골드·부품상자) 크기 확대 `[완료]`

사용자 지적: "경험치, 상자 등 좀비 드랍아이템 크기가 너무 작으니 키워줘."

원인은 `RewardPickupManager.cs`의 단일 상수 `VisualWorldSize`(2026-08-10 "인게임 모든 이미지
1/2" 지시로 0.7 → 0.35까지 줄어든 값) — 골드·경험치·부품상자 세 종류 전부 이 상수 하나로
화면에 찍히는 최종 크기(월드 유닛 기준 최대 변 길이)가 정해진다. **0.35 → 0.6**으로 올렸다
(선 길이 기준 약 1.7배, 면적 기준 약 2.9배).

**검증**(플레이모드): 좀비를 강제로 지우고 세 종류를 한 번에 스폰해 `SpriteRenderer.bounds`를
직접 측정 — 골드/경험치/부품상자 **셋 다 정확히 0.6×0.6 유닛**으로 렌더링됨을 확인(코드가
스프라이트 원본 크기와 무관하게 항상 `VisualWorldSize`로 정규화하므로 셋의 절대 크기가
동일하다). FHD 캡처로 눈으로도 확인 - 골드/상자는 뚜렷하게 커졌고, 경험치(가늘고 뾰족한
별 모양 `Energy.png`)는 같은 바운딩 박스 안에서도 실제로 채우는 면적이 적어 상대적으로
작아 보이지만 이는 스프라이트 형태에서 오는 정상적인 차이이지 크기 설정 문제가 아니다.

**수정 파일**: `RewardPickupManager.cs` (상수 하나만 변경, 스크립트 로직 변화 없음). 씬 수정 없음.

### 체력바 모서리 모양 통일 + HUD 자원 아이콘 위치 조정 `[완료]`

사용자 지적 2건: (1) "체력바 배경은 6각형인데 체력바 게이지는 막상 사각형" (2) "레벨, 체력과 같은
각 자원 아이콘들이 너무 왼쪽에 치우쳐져 있어서 UI와 겹쳐서 조잡해 보이게 침범".

**원인 조사**: `Resources/UI` 아트를 하나씩 열어 비교한 결과, 체력바는 배경(`Blue_barpanel00`,
양끝이 둥근 캡슐형)과 채움(`Red_bar00`/`Green_bar00`, 모서리가 45도로 잘린 각진 육각형)이
서로 다른 모서리 스타일을 쓰고 있었다. 반면 경험치 바는 배경(`Blue_barpanel01`)과 채움
(`Blue_bar00`) 둘 다 둥근 캡슐형이라 원래 문제가 없었다 - 그래서 체력바만 고쳤다.
`Red/Green_bar00/01/03`은 전부 각진 스타일뿐이고 둥근 버전이 없어서, 채움을 바꾸는 대신
**배경을 채움과 같은 각진 스타일로 교체**하는 쪽을 택했다. UI 폴더를 뒤져 각진 모서리+
중립색(보라 등 다른 색 아님) 후보를 찾았고, **`UI/Panel00`**이 `Red/Green_bar00`과 같은
대각선 컷 각도의 검정/회색 테두리라 가장 잘 맞았다.

**아이콘 침범 문제**: 체력바/경험치바 안의 하트·별 아이콘이 배경의 둥근(또는 각진) 캡 곡선
영역 안쪽(x 0.02~0.115, 캡 반경과 거의 겹치는 위치)에 앵커돼 있어 아이콘이 모서리 장식과
겹쳐 보였다. 아이콘을 0.095~0.190로 밀어 곡선/모서리 밖으로 빼고, 숫자 텍스트 시작 위치도
그만큼(0.13→0.205, 레벨 0.12→0.205, 경험치 값 0.245→0.335) 같이 밀었다.

**수정 내역**:
- `Canvas/HP/Hp_Slider/Background` 스프라이트 `UI/Blue_barpanel00` → **`UI/Panel00`**(Sliced 유지)
- `HeartIcon(_Shadow)`/`ExpSlider/Icon(_Shadow)` 앵커 `0.02~0.115` → **`0.095~0.190`**
- `Hp_value`/`ExpLevelText`/`ExpValueText` 시작 위치를 아이콘 이동만큼 뒤로
- 좌상단 스택 4행(`Hp_Slider`/`ExpSlider`/`GoldRow`/`PartBoxRow`) 전체를 화면 왼쪽 끝에서
  x 0.015 → **0.028**로 살짝 더 띄움(폭은 그대로 유지, 평행이동만)

**검증**(플레이모드 FHD 캡처 + 확대 크롭): 체력바 배경·채움 모서리가 같은 각진 스타일로
통일된 것을 확대 이미지로 직접 확인, 하트/별 아이콘이 모서리 장식과 겹치지 않고 여백을
확보한 것도 확인. 경험치 바는 변경 없이 기존 둥근 스타일 그대로 일관성 유지.

**수정 파일**: `Assets/Scenes/Ground01.unity`만 수정(스크립트 변경 없음).

### 로봇 무기 소켓 기본 최대치 2 -> 4 확장 `[완료]`

사용자 지시: "기본 로봇 무기 최대 4개로 수정. 모든 로봇의 무기 기본 최대치 4개(더 적을 수도
있지만 특별한 경우)".

`ActiveSocketCount = Min(씬에 리깅된 소켓 수, 머리 파츠의 weaponSocketCount)` 구조는
2026-08-12 "무기 소켓 개별화" 때 이미 N개를 지원하도록 만들어져 있었고, 프로젝트 안내 문서에도
"씬에 3번째 이상 소켓의 Transform을 배치하고 weaponSocketCount를 올리면 코드 변경 없이 자동
활성화된다"고 되어 있었다 — 이번 작업은 그 계획을 실제로 실행한 것이다.

**데이터**: `PartsCatalog.asset`의 `headModdingInfos` 2건(로봇ID 100001 "컴스톡 MK-1",
100002 "미니 컴스톡") 전부 `weaponSocketCount` 2 → **4**. 코드 쪽 폴백 기본값도 두 곳
(`PartsCatalog.GetHeadModdingInfo`의 미등록 로봇 폴백, `ModdingManager.GetHeadInfo`의
카탈로그 없음 폴백) 전부 2 → 4로 맞췄다 — 이후 새 로봇을 추가했을 때 데이터를 깜빡해도
기본이 4가 되도록.

**씬 리깅**: `Player` 밑에 기존 `RigingPoint`(어깨 높이, x=±2.75, y=3.60) 2개를 복제해
**허리 높이(y=1.5, 같은 x=±2.75)에 소켓3/4를 신설**했다(이름: `RigingPoint_Socket3/4`,
자식 `Wp_img_Socket3/4`/`HandWeapon_Socket3/4`). 원본 소켓의 스케일(1.8)·자식 오프셋을 그대로
물려받아 위아래로 두 쌍의 무기가 자연스럽게 겹치지 않고 늘어선 모습이 된다.

**`ShootManager`(PlayerShootManager) weapon_slots**: 배열을 2 → 4로 늘리고, 슬롯2는 슬롯0
(어깨 오른쪽)의 회전 보정각·반전 각도 범위·회전속도 등 튜닝값을 그대로 복사해 허리 오른쪽
소켓에 연결, 슬롯3은 슬롯1(어깨 왼쪽) 값을 복사해 허리 왼쪽 소켓에 연결했다 — 좌우 관계가
같으므로 높이만 다를 뿐 각도 보정은 동일하게 적용된다. 기본 장착 무기는 대칭으로 소켓0/2 =
기관단총, 소켓1/3 = 레이저피스톨.

**상점 소켓 선택 팝업 확장(놓칠 뻔한 하드코딩)**: `ShopPanelUI`의 `socketButtons`/
`socketButtonTexts`가 `new Button[2]`로 **고정 크기 배열**이었다 - `ModdingManager.
ActiveSocketCount`가 4를 돌려줘도 이 배열이 2칸뿐이면 무기 구매 시 소켓 3/4를 고를 방법이
없었다. 씬의 `Canvas/ShopPanel/SocketPicker`를 세로로 확장(화면 높이 비율 0.26→0.44)하고
기존 버튼 1/2를 복제해 2x2 그리드로 재배치했다(버튼 3/4 생성 시 복제된 onClick 리스너는
제거 - `ShopPanelUI.Awake()`가 인덱스에 맞춰 다시 정확히 건다). 배열 자체도 4로 늘려
씬의 새 버튼 2개를 연결했다.

**검증**(플레이모드 실측):
- `RiggedSocketCount=4`, `ActiveSocketCount=4` 확인
- 소켓0~3 전부 `TryGetSocketInfo` 성공 (기관단총/레이저피스톨/기관단총/레이저피스톨)
- FHD 캡처로 무기 4정이 어깨 2 + 허리 2로 겹치지 않고 전부 보이며 각자 독립적으로 자동 조준하는
  것 확인
- 상점 소켓 선택 팝업 - 4버튼 전부 `active=True`, 골드 부족 시 4개 다 정확히 비활성화 + 안내
  문구 표시, FHD 캡처로 2x2 배치 확인
- 로봇 정비 화면 - "무기 소켓 1~4" 4줄이 기존 3열 그리드 레이아웃에 자동으로 편입되어 잘림/겹침
  없이 표시됨(코드 수정 없이 기존 동적 그리드가 그대로 흡수)
- 무기 4정 무게 총합이 지탱력(24)을 넘어 "무기 무게 8/24"→(정비 화면 기준)이지만 실제 게임 중
  치장 상태에서는 "무게 지탱 29/24"로 초과 - 설계상 이동속도 패널티로만 반영되고 장착 자체는
  막히지 않으므로 정상 동작(밸런스는 추후 조정 필요할 수 있음, 별도 논의 사항)

**수정 파일**: `PartsCatalog.cs`, `ModdingManager.cs`, `ShopPanelUI.cs`(배열 크기 2→4),
`Assets/Data/PartsCatalog.asset`(로봇 2건 weaponSocketCount), `Assets/Scenes/Ground01.unity`
(소켓 2개 신설 + weapon_slots 확장 + SocketPicker UI 확장).

**참고(추후 밸런스 논의 필요)**: 무기 4개를 전부 장착하면 무게가 지탱력을 넘어 이동속도가
깎인다. 사용자가 "특별한 경우 더 적을 수도 있다"고 언급했으므로, 만약 특정 로봇을 3개 이하로
낮추고 싶으면 `PartsCatalog.asset`의 해당 `headModdingInfos` 항목만 수정하면 된다(코드 변경 불필요).

## 2026-08-18 `UI 기획서.pdf` 반영 업데이트 계획 `[계획]`

사용자가 `UI 기획서.pdf`(4장: 로봇 정비 UI / 상점 화면 UI / 일시정지·설정 메뉴 / 환경설정)를
첨부하며 업데이트 계획 수립을 요청했다. 기획서 4장을 현재 구현과 전수 대조한 결과와, 사용자가
확정한 결정 사항, 그리고 착수 순서를 아래에 남긴다. **아직 코드/씬 수정은 하지 않았다.**

### 대조 결과 (요약)

| 기획서 | 현재 상태 |
|---|---|
| p.1 로봇 정비 UI | 기능 대부분 있음, 레이아웃·표현이 다름 |
| p.2 상점 화면 UI | 기능 대부분 있음, 표현이 크게 다름(정보가 텍스트 한 덩어리에 뭉쳐 있음) |
| p.3 일시정지 / 설정 메뉴 | **전무** — ESC 처리도, 일시정지 기능 자체도 없음 |
| p.4 환경설정 | **거의 전무** — 배경음 슬라이더만 존재 |

중요한 확인: **기획서 스탯 패널의 9종(체력·공격력·방어력·치명타확률·치명타데미지·이동속도·
회피율·행운·질량)은 이미 전부 `AggregatedRobotStats`에 구현돼 있다**(`RobotStats.cs:4-15`).
표시 형식만 손보면 되고 스탯 신설은 필요 없다. 단 **`행운`(`Luck`)은 값만 존재하고 소비처가
없는 껍데기 스탯**이다(UI 표시와 AI 코어 카드로 수치만 오르내린다).

### 사용자 확정 결정 사항 (2026-08-18)

1. **무기 소켓 최대 개수 = 6** — "게임 최대는 6개 맞음. 기본 로봇만 4개가 최대고 다른 로봇은
   6개인 로봇도 있음". 즉 **시스템 상한은 6**이고 로봇별 `weaponSocketCount`가 4 또는 6이다.
   `ActiveSocketCount = Min(씬 리깅 수, 로봇 weaponSocketCount)` 구조이므로 **씬에 소켓 6개를
   리깅해두면 기본 로봇은 Min(6,4)=4로 그대로 유지**되고, 6소켓 로봇을 추가할 때 데이터만
   바꾸면 된다. UI 쪽(정비 슬롯 카드, 상점 `SocketPicker`)은 6칸까지 견디게 해야 한다.
2. **슬롯 구성을 기획서대로 — `메모리` 추가 / `발` 삭제**.
3. **랭킹 / 도감은 버튼만 배치하고 비활성**("준비 중" 안내). 화면 기획서를 받은 뒤 착수.
4. **착수 순서: Phase A → D → E → B → C** (F는 보류).

### Phase A — 헤더·문구 통일 (저위험, 씬 편집 위주)

정비 화면은 `제목 / WAVE 07 / 20 / 골드`가 이미 기획서와 같은데 **상점 화면만 어긋나 있다.**

- A-1. 상점 헤더에 제목 "상점" 신설 + `웨이브 7`(`ShopPanelUI.cs:321`) → `WAVE 07 / 20`.
  정비 화면의 `WaveManager.FinalWaveNumber` 사용 코드(`ModdingPanelUI.cs:271-274`)를 이식한다.
- A-2. 골드 표기에 코인 아이콘 — 상점엔 이미 `GoldText/Icon` 자식이 있고, 정비는 숫자만
  출력한다(`ModdingPanelUI.cs:277`).
- A-3. 버튼 문구 교정 — 상점 진행 버튼 `다음 웨이브 시작` → **`정비 종료`**(씬 고정 문자열,
  `Ground01.unity:1336`), 새로고침 `새로고침 (10골드)` → **`상점 초기화 - 10`** + 코인 아이콘
  (`ShopPanelUI.cs:324-327`).
- A-4. 스탯 패널 9줄에 스탯 아이콘 부착(정비·상점 공통). **아이콘 에셋 9종이 없으면 이 항목만
  보류**한다.

### Phase D — 일시정지 메뉴 (신규)

- D-1. `PauseMenuUI` 신설. 프로젝트 관례대로 **씬 편집 없이 코드 조립 + `Attach()`**
  (`MusicVolumeSliderUI`, `EquipmentDetailPopup`과 같은 방식).
- D-2. ESC 입력은 **신규 Input System**으로 — 이 프로젝트는 `activeInputHandler: 2`이므로
  `Keyboard.current.escapeKey.wasPressedThisFrame`을 써야 한다(`Input.GetKeyDown`은 동작 안 함).
- D-3. **함정 주의**: 일시정지 해제 시 `Time.timeScale = 1f`를 무조건 넣으면 정비/상점 화면
  (`timeScale=0`)에서 열었다 닫을 때 그 화면의 정지가 풀린다. `GameFlowManager.IsIntermission`
  (`GameFlowManager.cs:65`)을 보고 복원값을 결정한다. `timeScale` 조작 지점은 현재 4곳
  (`GameFlowManager.cs:74/158/450`, `GameOverSummaryUI.cs:43`).
- D-4. 5버튼 배치 — 돌아가기 / 랭킹(비활성) / 도감(비활성) / 설정 / 나가기. 반투명 오버레이 배경.
- D-5. `MusicManager`/`SFXManager` 모두 `ignoreListenerPause = true`이므로 `AudioListener.pause`
  방식을 쓰려면 그 설정을 재검토해야 한다.

### Phase E — 환경설정 (신규)

- E-1. `SettingsPanelUI` 신설. 배경음은 기존 `MusicVolumeSliderUI`(PlayerPrefs
  `comstock_music_volume`, `MusicManager.cs:36`)를 이 화면으로 이전. 타이틀·상점에 붙어 있는
  기존 슬라이더 2곳(`TitleSceneManager.cs:48`, `ShopPanelUI.cs:124`)과의 관계를 정리한다.
- E-2. **효과음 전역 볼륨 신설** — `SFXManager`는 존재하지만 전역 볼륨 개념도 PlayerPrefs 키도
  없다. 새 키(`comstock_sfx_volume`)를 추가한다. **단 `Assets/Resources/SFX` 폴더가 없어 실제
  효과음은 코드로 생성한 880Hz 피격 비프음 1개뿐**이므로(호출부도 `PlayerHitFeedback.cs:48`
  단 1곳), 슬라이더를 넣어도 체감할 소리가 거의 없다 → **효과음 에셋 확보가 선행 조건**이다.
- E-3. 음량에 따른 스피커 아이콘 3단 변화(음소거/소/대).
- E-4. **화면 모드 + 화면비율** — `Screen.SetResolution`/`FullScreenMode` 사용처가 프로젝트에
  0건이라 완전 신규다. 전체화면 / 경계없는 창모드 / 창모드 + 비율 16:10·16:9·4:3·1:1.
  비율 버튼은 기획서대로 **전체화면 선택 시에만 활성**. 캔버스가 `ConstantPixelSize`이므로
  해상도가 바뀌면 HUD 배치를 실측 재검증해야 한다(`PartBoxIndicatorUI`는 이미 해상도 변화를
  감지해 재계산한다 — `PartBoxIndicatorUI.cs:141`).
- E-5. 피드백 웹사이트 버튼(`Application.OpenURL`) — **URL을 사용자에게 받아야 한다**.
- E-6. 저장 후 닫기. 현재 프로젝트의 PlayerPrefs 사용은 음악 볼륨 2줄이 전부이므로 설정 저장
  경로를 새로 만드는 셈이다.

### Phase B — 정비 화면 레이아웃 재구성

- B-1. 슬롯 영역을 기획서 구조로 재배치 — 현재는 3열 균등 그리드 12칸(`slotColumns: 3`)이지만,
  기획서는 **머리 + 무기 소켓(점선 테두리 그룹) / 파츠 2행 / 디스크 슬롯**의 의미 단위 배치다.
- B-2. **슬롯 구성 변경(결정 2 반영)** — `메모리` 추가 / `발` 삭제.
  **주의: `PartSlot`은 직렬화 int라 순서 변경·중간 삭제가 금지**되어 있다(`PartSlot.cs:12-22`).
  따라서 `Foot = 5`는 enum 값을 남겨두고 `DisplayOrder`(`PartSlot.cs:34-43`)에서만 빼고 데이터를
  정리하며, `Memory`는 **값 8로 append**한다. 발이 갖고 있던 이동속도 기여는 다리(`Leg`)로
  통합하는 방향을 검토한다.
- B-3. **메모리 = AI 코어 최대 레벨 결정 파츠** — `AiCoreManager.coreMaxLevelPlaceholder = 70`
  (`AiCoreManager.cs:25`)은 주석에 이미 "메모리(머리 파츠)가 정할 값의 임시값"이라고 적혀 있다.
  메모리 파츠 데이터로 대체하고 기본 파츠가 **50**을 주게 하면 기획서의 `MAX:50` 표기와도
  일치한다(현재 70 → 50 하향은 밸런스 영향이 있으니 착수 시 확인).
- B-4. 인벤토리 칸을 **텍스트(등급/이름/슬롯명) → 파츠 아이콘 + 등급 테두리**로 전환
  (`ModdingPanelUI.cs:302`). 파츠 아이콘 에셋이 없으면 확보가 선행 조건.
- B-5. 스탯 패널에 `레벨: N (MAX:M)` 신설 — 정비 화면엔 현재 레벨 표시가 아예 없다.
- B-6. **캐릭터 미리보기 신설** — `ProceduralCharacterRig`를 `RenderTexture`로 렌더해 패널에
  표시한다. 2026-08-12에 UI 레이어만 컬링한 카메라 캡처를 이미 써봤으므로 그 방식을 재활용.
- B-7. 무기 소켓 카드가 **6칸까지** 레이아웃에서 잘리지 않는지 확인(결정 1 반영).

### Phase C — 상점 카드·보유 목록 재구성

- C-1. 카드 내부를 **아이콘 / 이름 / 종류 / 능력치 / 가격** 개별 필드로 분리. 현재는
  `bodyText` 하나에 `이름\n능력치\n가격` 3줄이 뭉쳐 있다(`ShopPanelUI.cs:359-364`).
- C-2. 가격을 카드 하단 박스로 분리 + 구매 시 **빨간 대각선 "구매 완료" 스탬프 + 가격 자리에
  "구매함"**. 현재는 초록 `구매 완료` 한 줄 + 버튼 비활성이다.
- C-3. 잠금 버튼에 **자물쇠 아이콘** + 잠긴 카드 시각 강조(기능 자체는 이미 있다 —
  `ShopManager.ToggleLock`, 웨이브를 넘어서도 잠금이 유지된다).
- C-4. 보유 디스크/무기를 **여러 줄 텍스트 → 그리드 슬롯**으로 전환하고 `무기 (2/6)` 개수 표기
  추가. 현재는 TMP `<link>` 태그 클릭 방식이므로 기존 `EquipmentDetailPopup` 연결을 슬롯 클릭
  으로 옮겨야 한다(`ShopPanelUI.cs:407-433`, `439`, `569`).
- C-5. `SocketPicker`를 4칸(2x2) → **6칸**으로 확장(결정 1 반영). 2026-08-18에 이 배열이
  `new Button[2]` 하드코딩이었던 것을 4로 늘렸으므로 같은 자리를 다시 손대게 된다.

### Phase F — 랭킹 / 도감 (보류)

구현 흔적이 0건이고 화면 기획이 없다. 결정 3에 따라 **일시정지 메뉴에 버튼만 배치하고
비활성**으로 둔다. 랭킹은 서버/저장 설계가 필요하고, 도감은 파츠·무기·디스크·몬스터 데이터가
이미 로컬에 있어 로컬만으로도 구현 가능하다.

### 착수 전 확보가 필요한 외부 리소스

1. **스탯 아이콘 9종**(체력/공격력/방어력/치명타확률/치명타데미지/이동속도/회피율/행운/질량)
   — Phase A-4, B-5 선행 조건. 기획서 이미지에는 있으나 프로젝트 `Assets/Resources/UI`에
   해당 아이콘이 있는지 미확인.
2. **파츠 아이콘** — Phase B-4 선행 조건. 현재 인벤토리·슬롯은 전부 텍스트다.
3. **효과음 에셋** — Phase E-2 선행 조건. `Assets/Resources/SFX` 폴더 자체가 없다.
4. **피드백 웹사이트 URL** — Phase E-5.
5. **자물쇠 / 구매 완료 스탬프 아트** — Phase C-2, C-3.

### Phase A - 헤더·문구 통일 `[완료]`

위 계획의 Phase A를 실행했다. 기획서 p.1(로봇 정비)과 p.2(상점)의 헤더가 서로 같은 형식
(`제목 / WAVE 07 / 20 / 코인 아이콘 + 숫자`)인데 **상점만 어긋나 있던 것**을 맞추고, 버튼
문구를 기획서 표기로 교정했다.

**코드 수정**
- `ShopPanelUI.RefreshHeader()` — `웨이브 {N}` → **`WAVE {N:00} / {FinalWave}`**.
  총 웨이브는 정비 화면과 같은 방식(`WaveManager.FindFirstObjectByType` → `FinalWaveNumber`,
  0이면 총합 생략)으로 구한다. 골드는 `골드 {N}` → **`{N}`**(옆에 코인 아이콘 오브젝트가
  따로 있으므로 숫자만). 새로고침 라벨은 `새로고침 ({N}골드)` → **`상점 초기화 - {N}`**.
- `ShopPanelUI` 골드 부족 안내 문구도 `(새로고침 N골드)` → `(상점 초기화 N골드)`로 통일.
- `ShopPanelUI` / `GameFlowManager`의 클래스 주석에 남아 있던 옛 버튼 이름("다음 웨이브 시작",
  "새로고침")을 새 문구로 갱신(문구를 바꾸면 주석이 바로 낡는 자리다).

**씬 수정** (`Assets/Scenes/Ground01.unity`)
- `ShopPanel`에 **`TitleText_BG` + `TitleText`("상점") 신설** — 기존 `WaveText_BG`/`WaveText`를
  복제해 만들어 폰트·9-슬라이스 스프라이트(`Panel02`) 설정을 그대로 물려받게 했다.
- 헤더 3요소 재배치: 제목 `0.016~0.118` / 웨이브 `0.124~0.318` / 골드 `0.324~0.476`.
  3요소가 한 줄에 들어가도록 글자 크기를 **56 → 44**로 낮췄다(오른쪽의 음악 볼륨 슬라이더
  `0.53~0.70`과 `NextWaveButton` `0.72~0.975`는 건드리지 않았다).
- `NextWaveButton/Label` **"다음 웨이브 시작" → "정비 종료"**.
- `RefreshButton/Label` **"새로고침 (10골드)" → "상점 초기화 - 10"**, 라벨 폭을 `0~0.87`로
  좁히고 오른쪽에 **코인 아이콘(`Icon`, `Gold_icon00`) 신설**.
- **로봇 정비 `TopBar/GoldText`에 코인 아이콘 신설** — 상점의 아이콘 오브젝트를 복제해 붙였다.
- 코인 아이콘 3개 전부 `preserveAspect = true`(늘어나 찌그러지지 않게).

**중간에 발견해 고친 것 (아이콘과 숫자가 멀리 떨어지던 문제)**: 1차 적용 후 캡처해 보니
코인 아이콘은 패널 왼쪽 끝, 숫자는 오른쪽 끝에 붙어 **가운데가 크게 비었다**. 원인은 숫자가
**오른쪽 정렬**이라 패널이 넓을수록 간격이 그대로 벌어지는 구조였던 것(상점은 원래부터 이랬고,
정비는 이번에 아이콘을 넣으면서 같은 문제가 생겼다). 패널을 좁히고 **왼쪽 정렬 + 아이콘 폭만큼
`TMP_Text.margin` 왼쪽 여백**을 주는 방식으로 바꿔 둘을 붙였다 — 골드 자릿수가 늘어나도
아이콘 바로 오른쪽에서 자라므로 겹치지 않는다.
- 상점 골드 `0.33~0.47`, `MidlineLeft`, `margin.x = 56`, 아이콘 `0.02~0.19`
- 정비 골드 `0.84~0.99`, `MidlineLeft`, `margin.x = 58`, 아이콘 `0.02~0.19`

**검증**(플레이모드 실측, FHD 1920x1080 캡처):
- 상점 헤더 런타임 문자열 직접 조회 — `TitleText="상점"`, `WaveText="WAVE 07 / 20"`,
  `GoldText="1000000"`, `NextWaveButton/Label="정비 종료"`,
  `RefreshButton/Label="상점 초기화 - 10"`, 코인 아이콘 2개 모두 존재 확인
- 정비 헤더도 같은 방식으로 확인, 코인 아이콘 존재 확인
- 보정 전/후 캡처를 비교해 아이콘과 숫자가 붙은 것을 눈으로 확인
- 컴파일 오류 0건

**보류한 항목**: A-4(스탯 아이콘 9종 부착). `Assets/Resources/UI`에 아이콘이 `Heart_icon`,
`Gold_icon00/01`, `Star_icon`, `Exp_icon`, `Dash_skill`뿐이고 **공격력·방어력·치명타 확률·
치명타 데미지·이동속도·회피율·행운·질량 아이콘이 없다.** 에셋을 받으면 정비·상점 스탯 패널에
한 번에 붙인다.

**수정 파일**: `ShopPanelUI.cs`, `GameFlowManager.cs`(주석만), `Assets/Scenes/Ground01.unity`.

### 기본 장착 무기를 1정으로 축소 `[완료]`

사용자 지시: "기본으로 장착하는 무기는 한개로".

2026-08-18 "무기 소켓 2 -> 4 확장" 때 소켓 4개를 전부 무기로 채워뒀는데(소켓0/2=기관단총,
소켓1/3=레이저피스톨), 그 결과 시작하자마자 무게가 지탱력을 넘어(29 / 24) 이동속도 패널티를
안고 시작하는 상태였다. 기본 장착을 **소켓 0의 기관단총 1정**으로 줄이고 나머지 3소켓은 비웠다.

**빈 소켓을 제대로 지원하도록 먼저 고친 것**: 발사 로직은 이미 안전했다
(`UpdateSlot`이 `weapon_data_by_slot.TryGetValue` 실패 시 그냥 return). 하지만 두 가지가 걸렸다.
1. **빈 소켓의 손 무기 이미지가 안 꺼진다** — `RefreshAllWeaponVisuals`가 `i < SocketCount`면
   무조건 `hand_sprite_renderer.enabled = true`로 켜고, `RefreshWeaponVisual`은 무기 데이터를
   못 찾으면 **기존 스프라이트를 유지한 채 return**하는 구조였다(데이터 오류 시 정상 이미지를
   지우지 않으려는 기존 의도). 그래서 씬에 남아 있는 기본 무기 이미지가 그대로 손에 보인다.
2. **경고 로그 도배** — `RefreshAllWeaponData`가 `weapon_id` 조회 실패마다
   `무기ID 0(소켓 i)의 데이터를 찾을 수 없습니다` 경고를 찍는다(빈 소켓은 정상 상태인데도).

**수정**:
- `RefreshAllWeaponData` — `weapon_id <= 0`이면 **경고 없이 continue**(빈 소켓 = 정상).
- `RefreshWeaponVisual` — 무기 데이터를 못 찾았을 때 **`weapon_id <= 0`이면 손 이미지를 끄고
  `expected_weapon_sprite_by_slot`에서도 제거**한다(안 지우면 `LateUpdate`의 스프라이트 복구
  로직이 매 프레임 다시 그린다). `weapon_id`가 있는데 데이터만 없는 경우는 데이터 오류이므로
  기존대로 이미지를 유지한다.
- `RefreshWeaponVisual` — 데이터를 정상적으로 찾았으면 **손 이미지를 켠다**. 이게 없으면
  빈 소켓에 상점에서 무기를 사서 꽂아도(`EquipWeapon` -> `RefreshWeaponVisual`) 렌더러가 꺼진
  채라 무기가 안 보인다.
- `RefreshAllWeaponVisuals` — 무조건 켜던 줄을 없애고 켜고 끄는 판단을 `RefreshWeaponVisual`에
  일임했다(`i >= SocketCount`로 아예 로봇이 못 쓰는 소켓만 여기서 끈다).

**데이터**: `Ground01.unity`의 `ShootManager.weapon_slots` — 소켓0 `300901`(기관단총) 유지,
소켓1 `300701` -> **0**, 소켓2 `300901` -> **0**, 소켓3 `300701` -> **0**.

**검증**(플레이모드 실측):
- `TryGetSocketInfo` — 소켓0 = `일반 기관단총`, 소켓1~3 = `(비어 있음)`
- 손 렌더러 실측 — `RigingPoint/RightWp_img enabled=True sprite=RightSMG`, 나머지 3개
  (`LeftWp_img`, `Wp_img_Socket3`, `Wp_img_Socket4`) 전부 `enabled=False` → **화면에 무기 1정만**
- 게임오버 요약 화면에서 `무게 23.5 / 24`(이전 29/24 초과) → **초과 해소, 이동속도 2.75 -> 3**으로
  회복된 것을 요약 화면 수치로 확인
- `무기ID 0...` 경고 로그 0건

**수정 파일**: `PlayerShootManager.cs`, `Assets/Scenes/Ground01.unity`.

### 정비 화면 UI 개편 - 아이콘 칸 / 등급색 / 옆 설명 칸 `[완료]`

사용자 요청 4건 중 정비 화면 3건(+파츠 아이콘 처리)을 반영했다.
1. "로봇 정비 화면 UI를 글씨가 삐져나가는 경우가 너무 많음"
2. "인벤토리, 상점에서 내가 가진 아이템들은 아이콘으로만 표시 (뒤 사각형도 없어야 함)"
3. "일반등급이 아니면 사각형 슬롯이 해당 등급의 색깔로 칠해져야 함 (UI 스타일은 유지)"
4. "인벤토리에서 아이템 누르면 팝업이 아닌 옆쪽 빈 공간에 설명 표시, 교체할 대상 아이템도 함께"

**선행 조사 - 파츠 아이콘이 아예 없었다**: `Assets/Resources/Parts`가 있어서 파츠 아이콘인 줄
알았으나 열어 보니 **절차적 리그용 몸통·다리 스프라이트 4장**(`Body`/`Foot`/`LegLower`/
`LegUpper`)이었다. `PartData`에는 아이콘 필드조차 없다. 디스크는 아이콘이 있고(`Discs/디스크01~21`)
무기는 손에 드는 이미지를 재사용할 수 있지만, **정비 인벤토리에 담기는 로봇 파츠는 아이콘이 전무**했다.
사용자 확정: **"파츠는 임시 아이콘으로 대체, 나중에 올릴거임"**.

**`PartIconLibrary` 신설** (`Assets/Scripts/PartIconLibrary.cs`) — 나중에 진짜 아이콘을 넣을 때
**코드를 고치지 않아도 되도록** 설계했다.
- `Resources.Load<Sprite>("PartIcons/" + slot)`을 먼저 찾는다 → `Assets/Resources/PartIcons/`에
  슬롯 이름 그대로 PNG를 넣기만 하면 자동으로 교체된다
  (`ArmWeaponSocket`/`ArmArmor`/`MagneticCore`/`Leg`/`LegArmor`/`Foot`/`Helmet`/`DiscSlot`).
- 파일이 없으면 **코드로 그린 임시 아이콘**(64x64, 흰색 단색 실루엣)을 만들어 캐시한다.
  슬롯마다 구분되는 단순 도형이다(헬멧=돔+챙, 무기 소켓=총열+손잡이, 팔 장갑=방패,
  자기장 코어=두꺼운 고리, 다리=세로 막대 2개, 다리 장갑=가로 띠 3개, 발=L자, 디스크 슬롯=얇은 고리).
  흰색이라 칸에 칠하는 등급색 위에서 잘 보인다.

**`ItemGradeExtensions`에 색 헬퍼 추가** — `ToColor()`, `IsPlainGrade()`,
`ToCellColor(plainColor)`. `ToCellColor`는 **일반 등급이면 기본 칸 색을 그대로** 돌려주고,
그 외 등급은 등급색을 **0.45배로 눌러서** 돌려준다(원색 그대로면 흰 아이콘이 묻힌다).

**`ModdingPanelUI` 개편**
- `CreateCellShell()`을 뽑아 `CreateCell`(텍스트 칸)과 신규 `CreateIconCell`(아이콘 칸)이
  "테두리 아트(흰색 고정) + 안쪽 상태색" 2겹 구조를 공유하게 했다.
- **인벤토리**: 텍스트 3줄(등급/이름/슬롯) → **아이콘 1개만**. 아이콘 뒤에 별도 사각형을 깔지
  않고 칸 자체가 배경이다(요청대로). 칸 색 = 등급색.
- **장착 슬롯**: 이름+파츠명+설명 3줄 → **슬롯 이름(위) + 아이콘**. 칸 색 = 장착된 파츠의 등급색.
  긴 설명이 사라지면서 글씨 삐져나감의 주원인이 없어졌다. 머리 칸은 파츠가 아니라 로봇 자체라
  슬롯 아이콘이 없어 리그용 `Parts/Body`를 쓴다.
- **글씨 넘침**: `ApplyCellTextSizing()`으로 일원화하고 **`overflowMode = Ellipsis`**를 넣었다.
  기존에는 자동 크기 조절만 켜져 있어서 **최소 크기(6)에서도 안 들어가는 글은 그대로 칸 밖으로
  흘러나왔다**. 덤으로 obsolete 경고가 나던 `enableWordWrapping`을 `textWrappingMode`로 교체했다.
- **설명 칸 신설**(`EnsureDetailPanel`/`RefreshDetail`) — 씬을 건드리지 않고 코드로 만든다.
  위쪽에 **선택한 파츠**(아이콘+등급+이름+슬롯+설명), 아래쪽에 **교체 대상**(그 슬롯에 지금
  장착된 파츠)을 보여준다. 무기 소켓 파츠는 교체 대상이 하나로 정해지지 않으므로 **소켓 1~N을
  전부 나열**한다. 도메인 리로드로 참조가 날아가는 함정(2026-08-18 `AiCoreExtraButtonsUI`에서
  겪음)을 피하려고 `Refresh()`마다 없으면 다시 만든다.

**씬 수정**: 설명 칸이 들어갈 자리가 없어서(인벤토리 0~0.42 / 파츠 칸 0.44~ 로 빈틈이 없었다)
**`InventoryPanel`을 0~0.42 → 0~0.24로 좁혔다.** 아이콘 전용 칸은 텍스트 칸보다 훨씬 작아도
되므로 4열을 그대로 유지할 수 있다. 비워진 **0.26~0.42가 설명 칸 자리**이고, 파츠 칸(0.44~)은
침범하지 않는다.

**중간에 고친 것 2건** (1차 캡처 후 발견):
- **설명 칸 글씨가 너무 컸다** — 칸 글씨와 같은 상한(42)을 쓰니 폭이 좁은 설명 칸에서 몇 글자만에
  4줄이 됐다. `ApplyCellTextSizing`에 상한 인자를 추가해 제목 28 / 본문 22로 낮췄다.
- **선택 강조가 희귀 등급과 헷갈렸다** — 선택 색(파랑 계열)을 등급색과 섞으니 희귀(파랑)·
  서사(보라) 칸과 구분이 안 됐다. 선택 칸을 **슬롯 강조와 같은 노란색으로 덮도록** 바꿨다.
  어떤 등급색과도 겹치지 않고, "노란 파츠 → 노란 슬롯"으로 짝이 눈에 바로 이어진다.

**검증**(플레이모드 FHD 캡처 2회):
- 인벤토리 9개 파츠가 전부 아이콘만으로 표시되고 등급색(회색/파랑/보라)이 칸에 칠해짐
- 헬멧 파츠 선택 → 인벤토리 칸과 슬롯의 헬멧 칸이 **같은 노란색**으로 짝이 보임
- 설명 칸에 `일반 강화 헬멧 / 헬멧 / 방어력 +4 / 무게 1.5`, 교체 대상에
  `일반 표준 헬멧 / 헬멧 / 무게 1.5`가 정상 표시
- 무기 소켓 파츠 선택 시 교체 대상이 `소켓 1~4` 목록으로 표시
- 어느 칸에서도 글씨가 칸 밖으로 나가지 않음
- 컴파일 오류 0건(중간에 `PartData`가 struct라 `&&` 단락 시 `out` 변수가 미할당으로 잡히는
  CS0170이 3곳 났고, `default`로 먼저 초기화해 해결)

**수정 파일**: `PartIconLibrary.cs`(신규), `ItemGrade.cs`, `ModdingPanelUI.cs`,
`Assets/Scenes/Ground01.unity`(InventoryPanel 폭).

**남은 것**: 상점의 "내가 가진 아이템(장착 무기/디스크)"도 아이콘 그리드로 바꾸는 작업은
사용자가 "정비 화면 3건 먼저"를 택해 아직 안 했다. ESC 일시정지 + 옵션 메뉴 + 환경설정도 대기 중.

### 무기 1정 기준 초반 난이도 밸런싱 `[완료]`

사용자 지적: "무기 1개 기준 초반 좀비 처치가 너무 어려움 밸런싱 진행". 바로 앞 작업(기본 장착
무기 4정 -> 1정)의 직접적인 결과다.

**측정한 원인**(플레이모드 실측):
- 기본 무기 기관단총 = `atk 10 x 투사체 3발 x 초당 2회`. 로봇 공격력 3이 투사체 수로 나뉘어
  더해지므로 투사체당 11뎀, 좀비 방어력 1을 빼면 10뎀 -> **실효 60뎀/초**
- 예전 4정(기관단총 2 + 레이저피스톨 2)은 합계 약 **248뎀/초**
- 좀비 체력 70 기준 처치 시간이 **0.28초 -> 1.17초로 4.2배** 늘었다
- 더 심각한 것은 **방어력**이었다. 투사체당 데미지가 11밖에 안 되는데 **리더는 방어력 10**이라
  `max(1, 11-10)` = **투사체당 1뎀**으로 무력화 -> 실효 6뎀/초, 처치에 **100초**가 걸렸다.
  (2026-08-13에도 같은 계열의 문제를 겪었다 - "방어력 10이 연사 무기의 DPS를 무력화")

**조정**(`Assets/Data/GameDataAsset.asset`) — 기본 무기 1정 실효 60뎀/초로 좀비를 **약 0.67초**에
잡는 것을 기준으로 체력을 0.571배(70 -> 40) 재앵커하고, 나머지 몬스터도 같은 비율로 내렸다.
방어력은 별도로 더 내렸다(연사 무기 하나만 든 상태에서 방어력이 DPS를 통째로 깎지 않도록).

| 몬스터 | 체력 | 방어력 | 1정 기준 처치 시간 |
|---|---|---|---|
| 좀비 | 70 → **40** | 1 → 1 | 1.17초 → **0.67초** |
| 스피터 | 75 → **43** | 0 → 0 | **0.65초** |
| 스프린터 | 100 → **57** | 2 → **1** | **0.95초** |
| 디스럭터 | 110 → **63** | 2 → **1** | **1.05초** |
| 차저 | 180 → **103** | 3 → **2** | **1.91초** |
| 리더 | 600 → **343** | 10 → **4** | 100초 → **8.17초** |

**보상 상수 동반 조정**(`EnemyUnit.cs`) — 경험치·골드가 `MaxHp / 상수`라서 체력만 내리면 보상도
같이 줄어든다(코드 주석에 "체력을 다시 조정하면 이 상수도 반드시 같은 배율로 맞출 것"이라고
경고돼 있다). 체력이 1.75배 줄었으므로 **`ExpPerMaxHp` 30 -> 17, `GoldPerMaxHp` 60 -> 34**로
같은 배율만큼 내려 보상량을 유지했다(좀비 기준 경험치 2·골드 1로 이전과 동일).

**검증**(플레이모드 실측): 무기·로봇 공격력·몬스터 방어력을 실제 런타임 값으로 읽어 위 표의
처치 시간을 계산해 확인했다. 웨이브 배율 1.0(웨이브 1) 기준이다.

**손대지 않은 것과 그 이유**: `EnemySpawner.statIncreasePerWave`(웨이브당 5% 상승)는 그대로
뒀다. 이 값을 올리면 후반 난이도를 되돌릴 수 있지만 **체력뿐 아니라 공격력에도 같이 곱해져**
후반 적 데미지가 함께 폭증한다(웨이브 20에서 좀비 공격력 38 -> 65). 사용자가 요청한 것은 초반
완화이므로 건드리지 않았다. **후반이 쉬워졌다고 느껴지면** 이 값을 올리는 대신 몬스터별
`monster_hp`를 웨이브 구간별로 다루는 쪽을 검토할 것.

**수정 파일**: `Assets/Data/GameDataAsset.asset`(몬스터 6종 체력·방어력), `EnemyUnit.cs`(보상 상수).

### 상점 보유·장착 목록 아이콘화 + 칸 생김새 공용화 `[완료]`

사용자 요청: "상점에서도 보유, 장착 파츠와 아이템은 인벤토리와 같이 전부 아이콘으로 표시".

**`ItemCellUI` 신설** (`Assets/Scripts/ItemCellUI.cs`) — 직전 작업에서 `ModdingPanelUI` 안에
만들어 둔 칸 생성 코드를 정비 화면과 상점 화면이 **같은 규칙**으로 쓰도록 뽑아냈다.
`ApplyTextSizing` / `CreateShell` / `CreateIconCell` / `EnsureGrid` / `ClearChildren` 5개가
들어 있고, "테두리 아트(흰색 고정) + 안쪽 상태색" 2겹 구조와 격자 되먹임 방지 같은
그동안 쌓인 함정 대응이 전부 여기 한곳에 모였다. `ModdingPanelUI`의 같은 메서드들은
얇은 위임으로 바꾸고 쓰이지 않던 `spawnedCells` 필드는 삭제했다.

**상점 3개 목록을 격자로 교체** (`ShopPanelUI.RefreshEquipment`) — 기존에는 세 곳 모두
TMP 링크(`<link=...>`)가 박힌 여러 줄 텍스트였다. 기존 TMP 오브젝트는 **제목 줄로만** 남기고
그 아래를 코드로 만든 격자가 차지한다(씬 수정 없음, `EnsureGridContainer`가 제목 rect까지
같이 잡아준다).

| 목록 | 구성 | 클릭 |
|---|---|---|
| 장착 파츠 | 머리 + 무기 소켓 파츠 N + 나머지 슬롯 7 = 12칸(3열) | `head` / `ws:i` / `p:슬롯` |
| 장착 무기 | 소켓 수만큼(3열). 무기 이미지(`weapon_rgwpimg`)를 아이콘으로 재사용 | `w:i` |
| 디스크 | 디스크 슬롯 수만큼(4열). 낀 것만 아이콘 채움 | `d:디스크id` |

칸 색은 정비 화면과 같은 규칙(`ItemGradeExtensions.ToCellColor`)으로 **일반 등급이 아니면
등급색**이다. 클릭은 기존 `ShowDetail(linkId)` 디스패처를 그대로 호출하므로 **상세 팝업
(`EquipmentDetailPopup`)은 손대지 않고 재사용**했다 — 정비 화면과 달리 상점에는 설명을 붙일
빈 공간이 없어 팝업이 그대로 맞다.

텍스트로만 있던 정보 중 아이콘으로 표현할 수 없는 것(AI 코어 레벨, 무게 지탱)은 **제목 줄로
옮겼다**: `[장착 파츠] AI 코어 Lv 1 · 무게 23.5/24 (클릭 = 상세)`. 무게 초과 시 빨간색으로
표시하는 것도 유지했다. 동작하지 않는 "필살기 슬롯: 1칸 (데모 범위 밖)" 줄은 격자에서 뺐다.

**중간에 고친 것**: 무기를 안 낀 소켓 칸이 캡션만 있고 완전히 비어 보여서, **무기 소켓 아이콘을
흐리게 깔아** "여기에 무기를 낄 수 있다"는 것이 보이게 했다(정비 화면 빈 슬롯과 같은 처리).

**컴파일 중 걸린 것**: `DiscData`도 `PartData`처럼 **struct**라 `DiscData disc = null;`과
`found = null;`이 CS0037로 막혔다 → `default`로 교체(직전 작업의 CS0170과 같은 계열).

**검증**(플레이모드 FHD 캡처): 장착 파츠 12칸·장착 무기 4칸(1개 장착 + 3개 흐린 빈 칸)·
디스크 6칸(3개 장착, 등급색 초록/보라/주황)이 모두 아이콘으로 표시되고, 제목 줄에
`AI 코어 Lv 1 · 무게 23.5/24`가 나오는 것을 확인했다. 캡처 중 플레이어가 죽어 게임오버 패널이
덮이는 일이 있어, 검증 스크립트에서 적을 정리하고 체력을 채운 뒤 캡처하도록 했다.

**수정 파일**: `ItemCellUI.cs`(신규), `ShopPanelUI.cs`, `ModdingPanelUI.cs`(공용화 위임).
씬 수정 없음.

### Phase D(일시정지 메뉴) + Phase E(환경설정) `[완료]`

`UI 기획서.pdf` 반영 계획의 남은 두 Phase. 사용자가 이전에 "ESC → 기획서대로 옵션 메뉴"로
확정한 순서(ESC → 옵션 5버튼 → "설정" → 환경설정)를 그대로 구현했다.

**신규 파일 4개**
- `PauseMenuUI.cs` — ESC 일시정지 메뉴. 반투명 배경 + 옵션 패널(돌아가기/랭킹(준비 중)/
  도감(준비 중)/설정/나가기). 씬에 배치하지 않고 `GameFlowManager.Awake()`가 Canvas 밑에
  코드로 붙인다(`EnsureAttached` - 도메인 리로드로 참조가 날아가도 씬에 남은 오브젝트를
  재사용, `AiCoreExtraButtonsUI`와 같은 대응).
- `SettingsPanelUI.cs` — 환경설정 모달(기획서 p.4). 배경음/효과음 슬라이더, 화면 모드
  3택(전체화면/경계없는 창모드/창모드), 화면 비율 4택(16:10/16:9/4:3/1:1 - **전체화면
  선택 시에만 활성**), 피드백 버튼(비활성), 저장 후 닫기.
- `VolumeSliderUI.cs` — 라벨+슬라이더+퍼센트 범용 컨트롤. 기존 `MusicVolumeSliderUI`는
  배경음 전용으로 이미 두 화면(타이틀·상점)에서 검증된 코드라 손대지 않고, 효과음까지
  같은 슬라이더를 써야 하는 환경설정 화면을 위해 getter/setter 델리게이트를 받는 범용판을
  새로 만들었다.
- (기존 `ItemCellUI.cs`를 여기서도 재사용 - 라디오 버튼·저장 버튼의 칸 뼈대와 텍스트 크기
  조절에 그대로 썼다.)

**ESC 동작과 상태 관리** (`GameFlowManager.cs`)
- `public static bool IsPaused` 신설. **`IsIntermission`과 별개 플래그**로 뒀다 - 정비/상점
  (그 자체로 전체 화면 UI)과 일시정지(어느 화면 위에도 반투명하게 덮이는 오버레이)는 여는
  이유도 부작용도 다르다. `PlayerRobotController`/`PlayerShootManager`/`DiscEffectRuntime`의
  기존 "정비 중이면 입력 무시" 가드 3곳에 `|| GameFlowManager.IsPaused`만 추가했다.
- **timeScale은 열기 직전 값을 저장했다가 닫을 때 그대로 복원**한다(`GameFlowManager.
  IsIntermission`을 보고 0/1 중 하나로 판단하는 대신). 실제 값을 기억하는 쪽이 더 일반적이고
  안전하다 - 지금은 일시정지가 Combat 상태에서만 열리므로 항상 1을 저장/복원하지만, 나중에
  timeScale을 1이 아닌 다른 값(슬로우 모션 등)으로 쓰게 되어도 그대로 맞는다.
- ESC는 `PauseMenuUI.Update()`가 매 프레임 `Keyboard.current.escapeKey.wasPressedThisFrame`으로
  확인한다(신규 Input System - `PlayerRobotController`의 WASD/Space와 같은 API). 설정 화면이
  열려 있으면 ESC가 그것부터 닫고(옵션 패널로 한 단계만 복귀), 게임오버·승리 화면이나
  정비/상점 중에는 열리지 않는다(`GameFlowManager.IsIntermission`으로 판별 - `CurrentState`는
  인스턴스 프로�터라 정적 컨텍스트에서 못 쓰지만 이 플래그가 1:1로 같은 뜻이라 대신 썼다).
- "나가기"는 게임오버 요약 화면의 "타이틀로"와 같은 처리(timeScale=1 복원 후 Title 씬 로드).

**효과음 전역 볼륨** (`SFXManager.cs`) — `MusicManager.Volume`과 같은 방식으로
`public static float Volume`(PlayerPrefs `comstock_sfx_volume`) 신설, `Play()`가 이 값을
곱해서 재생한다. 실제 효과음 에셋은 아직 없지만(`Assets/Resources/SFX` 폴더 없음) **플레이어
피격 시 나는 절차 생성 비프음 1개가 있어 슬라이더 자체는 지금도 청각적으로 테스트 가능**하다
(당초 "효과음 에셋이 없어 보류"로 판단했던 것을 재검토해 뒤집었다 - 볼륨 조절 자체는
에셋 없이도 완성 가능한 기능이다).

**중간에 발견해 고친 버그 2건** (플레이모드 실측으로 발견)
1. **화면 모드·화면비율 버튼 7개가 전부 화면에 안 보였다** — `CreateRadioButton`이
   `ItemCellUI.CreateShell`을 호출한 뒤 **RectTransform의 anchorMin/anchorMax를 실제로
   설정하는 코드를 빠뜨렸다**. `ItemCellUI.CreateShell`은 GridLayoutGroup이 위치를 정해주는
   것을 전제로 앵커를 건드리지 않는데(정비/상점 화면은 전부 격자 안에서 쓴다), 여기는 격자가
   아니라 좌표를 직접 지정해야 하는 자리였다. 그 결과 버튼 7개가 전부 앵커 기본값
   `(0.5,0.5)-(0.5,0.5)`(크기 0인 한 점)에 뭉쳐 있었다. 만든 직후 `xMin/yMin/xMax/yMax`를
   RectTransform에 적용하도록 고쳤다.
2. **화면비율 아래로 패널의 1/3 넘게 빈 공간이 남고 "저장 후 닫기" 버튼이 패널 밖(y<0)으로
   튀어나가 있었다** — 세로 배치를 손 계산으로 띄엄띄엄 배치하면서 화면비율(0.335~0.43)과
   피드백(0.06~0.14) 사이 0.335의 공백을 남겼다. 패널 안쪽(0~1 로컬)을 빈틈없이 채우도록
   전체 세로 배치를 다시 계산했다.

**검증**(플레이모드 실측, 리플렉션으로 private 메서드 직접 호출 + FHD 캡처):
- `OpenPause()` 후: `IsOpen=True IsPaused=True timeScale=0`
- `OpenSettings()` 후: `settings.IsOpen=True`
- 볼륨 변경: `MusicManager.Volume 0.40→0.35`, `SFXManager.Volume 0.70→0.55` 실제 반영 확인
- `SelectScreenMode(2)`(창모드) → 화면비율 버튼 `interactable=False`,
  `SelectScreenMode(0)`(전체화면) → `interactable=True` 확인
- `ClosePause()` 후: `IsOpen=False IsPaused=False timeScale=1 settings.IsOpen=False`(설정이
  열려 있어도 함께 닫힘) 확인
- FHD 캡처로 일시정지 패널(HUD가 반투명 배경 뒤로 비쳐 보임), 환경설정 패널(창모드 기본
  선택 + 화면비율 비활성, 전체화면 선택 시 화면비율 4개 전부 활성 + 4:3 선택 표시) 확인
- 컴파일 오류 0건(1차 시도에서 `GameFlowManager.CurrentState`가 인스턴스 프로퍼티라 정적
  컨텍스트에서 CS0120 - `IsIntermission`으로 대체해 해결)

**참고(Editor 한계, 코드 주석에도 남김)**: `Screen.SetResolution`/`Screen.fullScreenMode`는
빌드된 실행 파일에서만 실제로 창 크기·모드를 바꾼다. Unity 에디터의 Game 뷰는 에디터 자체가
관리하므로 플레이모드에서 호출해도 Game 뷰 크기가 바뀌지 않는 것이 정상이다(에디터의 잘 알려진
한계) - 이 기능은 실제 빌드에서 확인해야 한다.

**보류**: 피드백 웹사이트 버튼은 열람할 URL을 받지 못해 비활성으로 남겼다. URL이 정해지면
`SettingsPanelUI`의 `FeedbackButton` 생성부 onClick에 `Application.OpenURL(그 주소)`만 넣으면
된다(코드 자리는 이미 준비돼 있음). 랭킹/도감은 사용자 확정대로 버튼만 두고 비활성이다.

**수정 파일**: `PauseMenuUI.cs`(신규), `SettingsPanelUI.cs`(신규), `VolumeSliderUI.cs`(신규),
`GameFlowManager.cs`, `SFXManager.cs`, `PlayerRobotController.cs`, `PlayerShootManager.cs`,
`DiscEffectRuntime.cs`. 씬 수정 없음(전부 코드 생성 UI).

이로써 `UI 기획서.pdf` 반영 Phase A/D/E가 모두 끝났다. 남은 것은 Phase B(정비 화면 레이아웃
재구성 - 슬롯 구성을 메모리/발로 교체, 캐릭터 미리보기)와 Phase C(상점 카드 필드 분리,
소켓 6칸)이며, 계획은 앞선 "2026-08-18 UI 기획서.pdf 반영 업데이트 계획" 항목에 있다.

### Phase B(정비 화면 레이아웃 재구성) `[완료, B-6 캐릭터 미리보기만 제외]`

사용자 확정(2026-08-18): AI 코어 최대 레벨 기본값 **70 → 50**(기획서 MAX:50과 일치)으로 낮춤,
**캐릭터 미리보기(B-6)는 이번 범위에서 제외**하고 나머지만 진행.

**B-2/B-3. 슬롯 구성 변경 - 메모리 추가 / 발 삭제, 메모리가 AI 코어 최대 레벨을 결정**
- `PartSlot` — `Memory`를 새 값(8)으로 append(enum은 int 직렬화라 중간 삽입 금지, 기존 관례
  그대로 맨 끝에 추가). `Foot`은 값 자체는 남기되(직렬화 호환) 주석으로 폐기 명시.
- `PartSlotExtensions.DisplayOrder` — `Foot` 제거, `Memory`를 맨 앞에 추가:
  `[Memory, Helmet, ArmArmor, Leg, MagneticCore, LegArmor, DiscSlot]`(7개, 3열 그리드에 순서대로
  채우면 메모리/헬멧/팔장갑 → 다리/코어/다리장갑 → 디스크 슬롯(홀로 3행)이 자동으로 나온다).
- `PartData`에 `coreMaxLevelBonus`(int) 신설, `BuildDescription()`에 Memory 전용 분기
  (`"AI 코어 최대 레벨 {N}"`) 추가.
- `ModdingManager.CoreMaxLevel` 신설 — 장착된 Memory 파츠의 `coreMaxLevelBonus`를 읽고,
  없으면 기본값 50으로 폴백. `ModdingManager.EnsureDefaultPartsEquipped()`의 기본 장착 루프에서
  **Foot을 명시적으로 건너뛰도록** 추가(안 하면 매 런마다 "PartsCatalog에 발 슬롯의 기본
  파츠가 없습니다" 경고가 찍힌다 - Foot 파츠 데이터를 전부 지웠기 때문).
- `AiCoreManager.MaxLevel`이 이제 `ModdingManager.Instance.CoreMaxLevel`을 읽는다(예전에는
  인스펙터 고정값 `coreMaxLevelPlaceholder = 70`이었다). 그 필드는 `coreMaxLevelFallback = 50`
  으로 이름과 용도를 바꿔 ModdingManager를 못 찾는 극단적인 경우의 안전망으로만 남겼다.
- **`PartsCatalog.asset` 데이터 마이그레이션**(에디터 스크립트로 일괄 처리):
  - 기본 발(500006, 표준 발) **완전 삭제**.
  - 발의 이동속도 계열 10건(경량 부츠 5등급 · 제트 부스터 5등급, id 510130~510144)은
    **삭제하지 않고 슬롯만 `Foot → Leg`로 이전**했다("발이 갖고 있던 이동속도 기여를 다리로
    통합" - 계획 문서 그대로). partId·이름·수치는 그대로라 다리가 이제 회피(유압/안정화 다리)
    또는 이동속도(경량 부츠/제트 부스터) 중 하나를 고르는 4계열 슬롯이 됐다.
  - 메모리 파츠 11건 신설 — 기본(500009, 표준 메모리 카드, 일반, **50**, 시작 장착) +
    고속 메모리 카드(510190~194, 55/65/75/90/105) + 대용량 메모리 카드(510200~204,
    60/70/85/100/120). 무게는 0(디스크 슬롯과 동일 취급 - 머리 쪽 구성요소라 무게에 안 잡힌다).
    전부 "밸런스 미확정 임시값"(다른 파츠 계열과 같은 문서 원칙).

**B-1/B-7. 슬롯 레이아웃 재배치 - 머리+무기소켓 그룹 분리**
- `ModdingPanelUI`에 `headSocketRow`(RectTransform, 선택적) 신설. 씬의 `SlotPanel` 안에
  **`HeadSocketRow`를 새 자식으로 만들고 기존 `SlotContent`를 아래로 좁혀**(0.02~0.9 →
  0.02~0.66) 위쪽(0.70~0.895)에 자리를 만들었다. 필드가 비어 있으면(호환용) 예전처럼
  `slotContent` 맨 앞에 머리+소켓을 섞어 그리므로 하위 호환이 깨지지 않는다.
  머리 + 무기 소켓 N개가 이제 **한 줄**로 그려지고(열 수 = 1+소켓수, 소켓 수가 바뀌면
  `Update()`가 매 프레임 다시 맞춘다), 은은한 톤의 배경 패널로 그룹임을 표시했다(이 프로젝트에
  점선 테두리 스프라이트가 없어 기획서의 점선 테두리를 배경색 구분으로 단순화했다).
  나머지 `PartSlotExtensions.DisplayOrder` 7개는 그대로 `slotContent`의 3열 그리드에 그려져
  자동으로 "파츠 2행 + 디스크 슬롯 1행"이 된다 - 별도 컨테이너 없이 그리드의 자연스러운
  줄바꿈만으로 기획서 배치가 나온 것이라 구조를 더 쪼갤 필요가 없었다.
- **B-7(소켓 6칸까지 확인)**: 열 수가 `1+WeaponSocketCount`로 완전히 동적이고
  `ItemCellUI.EnsureGrid`가 열 수에 관계없이 컨테이너 폭을 나눠 칸 크기를 계산하는 공용
  로직이라(이미 2·4소켓에서 검증됨) 6소켓(로봇 데이터가 `weaponSocketCount=6`을 주는 순간)도
  같은 코드 경로로 잘리지 않고 그려진다 - 단, 지금은 씬에 물리 소켓(RigingPoint)이 4개까지만
  리깅돼 있어 실제 6소켓 로봇으로 시각 확인은 못 했다(6소켓 로봇 자체가 아직 카탈로그에 없음 -
  Phase C 영역).

**B-5. 레벨 표시 신설** — `ModdingPanelUI.RefreshStats()` 맨 윗줄에
`레벨: {RunState.CoreLevel} (MAX:{aiCoreManager.MaxLevel})` 추가. 정비 화면에 레벨 표시가
아예 없던 것을 기획서 표기(예시: "레벨: 26 (MAX:50)")와 맞췄다.

**PartIconLibrary** — Memory 전용 임시 아이콘(칩 몸통 + 사방 핀 8개) 추가. 처음엔 슬롯 이름을
못 찾아 기본 사각형(fallback)으로 나온 것을 발견해 바로 고쳤다.

**검증**(플레이모드 실측, FHD 캡처 2장 + 리플렉션 없이 직접 API 호출):
- 슬롯 레이아웃: 상단 한 줄에 머리+무기소켓 1~4(은은한 그룹 배경), 그 아래 3열 그리드에
  메모리/헬멧/팔장갑, 다리/자기장코어/다리장갑, 디스크 슬롯(홀로) - 기획서 구조와 일치
- `발` 슬롯이 화면에서 완전히 사라짐, `메모리` 슬롯이 새로 나타남(칩 아이콘)
- 능력치 패널 첫 줄에 `레벨: 1 (MAX:50)` 표시
- 인벤토리에 전설 등급 대용량 메모리 카드(510204)를 넣고 `TrySwapInventoryWithSlot`으로 실제
  교체 → 슬롯 칸이 전설 등급색(빨강)으로 칠해지고, **`AiCoreManager.MaxLevel`이 50 → 120으로
  실시간 반영**됨을 확인(`ModdingManager.CoreMaxLevel`과 값 일치)
- `PartsCatalog` 데이터 무결성: `Foot` 파츠 개수 0, `Memory` 11개, `Leg` 21개(기존 11 +
  이전된 10)로 정확히 확인
- 컴파일 오류 0건

**수정 파일**: `PartSlot.cs`, `PartData.cs`, `ModdingManager.cs`, `AiCoreManager.cs`,
`ModdingPanelUI.cs`, `PartIconLibrary.cs`, `Assets/Data/PartsCatalog.asset`,
`Assets/Scenes/Ground01.unity`(HeadSocketRow 신설 + SlotContent 크기 조정 + 필드 와이어링).

**제외한 것**: B-6(캐릭터 미리보기, `ProceduralCharacterRig`를 `RenderTexture`로 렌더) - 사용자
확정에 따라 이번 범위에서 뺐다. 다음 착수 후보는 Phase C(상점 카드 필드 분리, 소켓 6칸)다.

### Phase C(상점 카드·보유 목록 재구성) `[완료]`

`UI 기획서.pdf` 반영의 마지막 단계. **씬 수정은 한 줄도 없다** - 카드 안에 필요한 요소를
런타임에 코드로 덧붙이는 방식(Phase D/E의 일시정지·환경설정 화면과 같은 관례)으로 전부 처리했다.

**C-1. 카드 내부 필드 분리**
- 기존에는 `bodyText` 하나에 `이름\n능력치\n가격` 세 줄이 뭉쳐 있었다. 이제 카드 안(로컬 0~1)
  세로 배치가 **0.72~0.98 아이콘+종류(씬에 이미 있던 것) / 0.55~0.70 이름 / 0.26~0.54 능력치 /
  0.05~0.24 가격 박스**로 나뉜다.
- 씬의 `BodyText`는 지우지 않고 **"능력치" 자리로 옮겨서** 재사용한다(이 파일의
  `EnsureGridContainer`가 보유 목록 제목 텍스트를 같은 방식으로 옮기고 있어 새로운 수법은 아니다).
  `NameText`/`PriceBox`는 새로 만든다.
- **이름은 등급색으로** 칠한다(`ItemGrade.ToColorHex()`). 카드에서 가장 먼저 눈에 들어와야 하는
  정보라 헤더의 "전설 · 무기"와 색을 맞췄다.

**C-2. 가격 박스 분리 + 구매 완료 스탬프**
- 가격은 카드 하단의 독립 박스가 됐다(`UI/Black_ui04` + `UI/Gold_icon00` 코인 + 숫자).
  아이콘 칸(`IconImage_BG`)과 같은 아트를 써서 카드 안에서 톤이 어긋나지 않는다.
- **구매 시**: 카드 전체를 어둡게 덮고(alpha 0.55) **14도 기울어진 빨간 테두리 + "구매 완료"**
  스탬프를 올리며, **가격 자리는 "구매함"(회색)** 이 되고 코인 아이콘은 숨는다.
  기존에는 본문 마지막 줄에 초록 글씨 한 줄이 전부였다.
- 스탬프는 카드의 **마지막 자식**으로 만들어야 다른 요소 위에 그려진다(UI는 형제 순서가 곧
  그리기 순서다).

**C-3. 잠금 자물쇠 아이콘 + 잠긴 카드 강조**
- 잠금 버튼에 자물쇠 아이콘을 붙이고(글자 자리를 0.30~1.0으로 밀었다), **잠기면 자물쇠가
  노란색**(`#F2BF26`)이 된다.
- 잠긴 카드는 **노란 테두리**가 카드 전체를 감싼다. 노란색은 정비 화면의 슬롯 강조와 같은 색이다
  (파랑 계열은 희귀 등급과 헷갈린다 - 2026-08-18 정비 화면에서 이미 겪은 문제).

**C-4. 보유 목록 개수 표기** — `[장착 무기] 1/4` 형식 추가. 디스크 줄의 `N/M` 표기와 같은 형식이고
분모가 실제 사용 가능한 소켓 수라 6소켓 로봇이면 저절로 `N/6`이 된다.
(그리드 전환 자체는 2026-08-18 "상점 보유·장착 목록 아이콘화"에서 이미 끝나 있었다.)

**C-5. 소켓 선택 창 6칸 확장**
- 씬에 고정 좌표로 깔린 버튼 4개를 쓰고 남는 것만 숨기던 방식을 버리고, **소켓 수에 맞춰 만들고
  배치**한다(`EnsureSocketButtons` / `LayoutSocketButtons` 신설). 모자라면 1번 버튼을 복제하고,
  자리는 창 안쪽 띠(y 0.24~0.84)를 1열 또는 2열 격자로 나눠 계산한다.
- **복제본은 원본의 `onClick` 리스너까지 복사해 온다**(= 5번 버튼을 눌렀는데 1번 소켓에 장착되는
  버그가 된다). 그래서 창을 열 때마다 `RemoveAllListeners()` 후 자기 인덱스로 다시 단다.
  이 때문에 `Awake()`에 있던 소켓 버튼 리스너 등록 루프는 삭제했다.
- 6칸이 되면 칸이 작아지는데 버튼 글은 3줄("소켓 N / 현재 무기 / 경고")이라
  `ItemCellUI.ApplyTextSizing`을 걸어 자동 축소되게 했다.
- `ShopPanelUI.MaxWeaponSockets = 6` 상수 신설(2026-08-18 사용자 확정 "게임 최대는 6개").

**중간에 발견해 고친 버그 1건 (실측 캡처로 발견)**
- **소켓 선택 창이 보유 목록 격자에 가려졌다.** `PartsGrid`/`WeaponsGrid`/`DiscsGrid`와 음악 볼륨
  슬라이더는 전부 런타임에 만들어져 패널의 **마지막 자식**으로 붙는데, 씬에 있는 `SocketPicker`는
  그보다 앞 형제라 UI 그리기 순서상 뒤로 밀린다. 2026-08-18 아이콘화 작업 때 격자가 생기면서
  들어온 문제이고, 캡처를 눈으로 보기 전에는 드러나지 않았다(숫자 검증만으로는 못 잡는 종류다).
  `OpenSocketPicker`에서 열기 직전 `SetAsLastSibling()`을 부르는 것으로 해결.

**신규 파일 `UiIconLibrary.cs`** — 아트가 없는 UI 장식(자물쇠, 사각 테두리)을 코드로 그린다.
`PartIconLibrary`와 같은 방식이라 **`Assets/Resources/UI/Lock_icon.png` / `Stamp_frame.png`를 넣으면
코드 수정 없이 자동 교체**된다. 테두리는 9-slice 경계(8px)를 넣어 늘려도 선 두께가 유지된다.
도형을 **흰색 단색 실루엣**으로 그리는 것이 핵심 - 프로젝트의 기존 테두리 아트(`UI/Black_ui03`)는
거의 검정이라 색을 곱하면 죽어버려 등급색·강조색 용도로 못 쓴다(ItemCellUI 주석의 2026-08-13 함정).

**검증**(플레이모드 실측, FHD 1920x1080 캡처 4장)
- 캔버스를 **FHD로 만든 뒤 상점을 여는 순서**로 캡처했다(ConstantPixelSize라 격자·칸 크기가 여는
  시점의 캔버스 크기로 계산된다 - 2026-08-13에 문서화한 주의사항).
- 카드 4장에서 아이콘 / `희귀 · 디스크` / 이름(등급색) / 능력치 / 가격 박스가 각각 제자리에
  그려지는 것을 확인. 무기 카드와 디스크 카드 양쪽 모두 확인했다.
- 잠근 카드: 노란 테두리 + 노란 자물쇠 + "잠금 해제" 표시.
- 구매한 카드: 어두워짐 + 빨간 대각선 "구매 완료" 스탬프 + 가격 자리 "구매함"(코인 숨김).
- `[장착 무기] 1/4` 표기 확인.
- 소켓 선택 창: **4소켓(실제 경로) 2x2**, **6소켓(강제) 2x3** 모두 겹침 없이 그려짐을 앵커 값
  비교로 수치 검증(겹침 0건) + 캡처로 확인. z-order 수정 후 `SocketPicker` 형제 순번 30/31(맨 뒤).
- **기능 검증**: 소켓 배열이 6으로 늘어난 상태에서 `OpenSocketPicker` → **3번 버튼을 실제로
  클릭** → 소켓 3에 대물저격총이 장착되고(1·2·4는 그대로), 구매 처리·골드 차감(9999→9944)·
  창 닫힘까지 확인. 복제 버튼의 리스너가 엉뚱한 소켓으로 가지 않는다는 것을 실제로 증명한 셈이다.
- 컴파일 오류 0건(기존 `EquipmentDetailPopup.cs`의 `enableWordWrapping` 사용 중단 경고 1건은
  이번 작업과 무관한 기존 항목). 씬 수정 0건 - `Ground01.unity`는 `IsDirty=false` 그대로다.

**수정 파일**: `ShopPanelUI.cs`, `UiIconLibrary.cs`(신규). 씬·데이터 에셋 수정 없음.

**남은 것**: 보류 중인 **B-6(캐릭터 미리보기)** 와 **Phase F(랭킹·도감, 화면 기획서 대기)**.
자물쇠·스탬프 아트를 실제로 받으면 위 경로에 PNG만 넣으면 되고, 스탯 아이콘 9종(Phase A-4)도
여전히 미확보라 능력치 패널은 글자만 나온다.

### 좀비 6종 전용 아트 적용 (`좀비temp` 폴더) `[완료]` (2026-08-19)

사용자가 프로젝트 루트 `좀비temp/`에 좀비·스피터·스프린터·디스럽터·차저·리더 6장(각
918~1086px 정사각, 2026-08-18 제공)을 넣어두고 적용을 요청.

**적용 전 상태 확인**: 6종 몬스터 프리팹(`Assets/Prefebs/Zombie·Spitter·Sprinter·
Disruptor·Charger·Leader.prefab`)이 전용 아트 없이 `Enemy_zombie_S/M/L/XL.png` 4장을
`SpriteRenderer.m_Color` 색조만 다르게 입혀 임시로 구분하고 있었다(좀비/스피터/스프린터
= S 250px, 디스럽터 = M 350px, 차저/리더 = L 450px, 색조로만 종 구분). `MonsterSizeSpec.
SpriteResourceName` 필드가 있지만 실제로는 어디서도 참조되지 않는 죽은 코드였다 - 스프라이트
배정은 전부 프리팹에 직접 박혀 있다.

**작업 내용**:
1. `좀비temp`의 6장을 각 몬스터가 원래 쓰던 규격 픽셀 크기(위 S/M/L 값)로 물리적으로
   축소(고품질 Bicubic)해 `Assets/Resources/Zombie·Spitter·Sprinter·Disruptor·Charger·
   Leader.png`로 저장했다 - 이 프로젝트의 기존 규칙("원화를 규격 픽셀 크기로 축소 저장,
   PPU=100, 스케일 그대로 두면 확대·축소 없이 원본 비율 유지")을 그대로 따른 것이라 콜라이더·
   월드 크기·밸런스는 전혀 건드리지 않는다.
2. 새 GUID로 `.meta`를 `Enemy_zombie_S.png.meta`와 동일한 임포트 설정(spriteMode Single,
   PPU 100, pivot 중앙, textureType Sprite)으로 작성해 함께 배치한 뒤 `assets-refresh`로
   임포트, `assets-find`로 6장 전부 정상 등록을 확인했다.
3. 6개 프리팹을 `assets-prefab-open`으로 열어 `SpriteRenderer.sprite`를 새 스프라이트로
   교체하고, 실제 아트라 더는 필요 없는 색조(`m_Color`)를 전부 흰색(1,1,1,1)으로 되돌린 뒤
   저장·닫았다. 보스는 이번 아트 세트에 없어 기존 `Enemy_zombie.png`(빨간 색조) 그대로 둔다.

**검증**: 6개 프리팹을 `Ground01` 씬에 임시로 나란히 배치하고 `screenshot-isolated`로 각각
캡처해 새 아트가 정확히 적용된 것을 눈으로 확인(좀비/스피터/스프린터/디스럽터/차저/리더 6장
전부 서로 다른 고유 외형 - 스피터는 독액을 뱉는 큰 머리, 스프린터는 네발 자세, 디스럽터는
갈라진 배, 차저는 육중한 몸집, 리더는 왕관+빛나는 눈). 확인 후 임시 오브젝트는 전부 삭제했고
씬 파일은 저장하지 않았다(`Ground01.unity`는 디스크상 수정 없음, 프리팹 6개만 수정됨).

**수정 파일**: `Assets/Prefebs/Zombie·Spitter·Sprinter·Disruptor·Charger·Leader.prefab`.
**신규 파일**: `Assets/Resources/Zombie·Spitter·Sprinter·Disruptor·Charger·Leader.png`(+`.meta`).
씬 수정 없음.

## 2026-08-19 머리(로봇) 12종 전면 구현 — `머리 기획서 Ver04` 반영 `[작업 중]`

사용자가 `20260818_머리기획서_Ver04_백승오.pdf`(8쪽, 머리 12종)와 `final_250/` 폴더(신규 머리
아트 9종 + 네온아이 눈 색상 8프레임)를 제공. **머리는 게임 시작 시 1회 고르고 상점에 등장하지
않는 = 플레이어 캐릭터의 정체성**이라는 전제로 구현한다.

### 착수 전 조사에서 확인한 사실 (설계의 근거)

- **"머리"는 곧 `ProceduralCharacterRig`의 몸통 스프라이트다.** `Assets/Resources/Parts/Body.png`가
  **정확히 250x250**이고 그게 컴스톡 MK-01의 얼굴 달린 원통이다. `final_250/`의 9장도 전부
  250x250 — 폴더 이름이 곧 "250 규격 최종본"이라는 뜻이었다. 즉 머리 적용 = 몸통 스프라이트 교체.
  리그에 별도 head 오브젝트·렌더러는 아예 없다(`ProceduralCharacterRig.LoadPartSprites`).
- **로봇 데이터는 두 에셋에 쪼개져 있다** — 스탯은 `GameDataAsset.asset`의 `robots:`(`RobotData`),
  소켓/디스크/적재량은 `PartsCatalog.asset`의 `headModdingInfos:`(`HeadModdingInfo`). 같은
  `robot_id`로 join된다. 둘째를 빼먹으면 조용히 4/6/20으로 폴백한다.
- **로봇에 스프라이트 필드가 아예 없다.** 정비/상점 화면의 머리 칸은 둘 다
  `Resources.Load<Sprite>("Parts/Body")`를 하드코딩해 쓰고 있었다(`ModdingPanelUI.cs:389`,
  `ShopPanelUI.cs:844`). `RobotSelectManager.panelImage`도 선언만 있고 채우는 코드가 없다.
- **기본 장착 무기라는 데이터 경로가 없다.** 시작 무기는 씬에 박혀 있다
  (`Ground01.unity` `PlayerShootManager.weapon_slots[0].weapon_id: 300901` = 기관단총 일반).
- **`WeaponType` enum이 이미 기획서의 카테고리와 1:1로 존재한다** — `RapidFire`(연사) /
  `Shotgun`(산탄) / `Precision`(정밀) / `Explosive`(폭발) / `Energy`(에너지) / `Melee`(근접).
  `PartsCatalog.weaponMeta`에 무기 65행 전부 이미 채워져 있는데 **런타임에서 아무도 읽지 않는
  죽은 데이터**였다(상점 분류 표시 1곳 제외). 머리 효과의 조건 축으로 그대로 쓸 수 있다 —
  데이터 마이그레이션이 필요 없다.
- **`행운`(Luck) 스탯도 같은 상태** — 집계·표시만 되고 게임플레이에서 아무도 읽지 않는다.
  해피 픽셀 효과가 이 스탯의 첫 소비자가 된다.
- **`공격속도`는 로봇 스탯이 아니다.** 무기별 `weapon_atsp`(0.25~10/초)뿐이고, 배율을 곱하는
  자리가 `PlayerShootManager.CurrentAttackSpeedMultiplier()`로 이미 관례화돼 있다.
- **씬에 물리 소켓이 4개뿐**이다(`weapon_slots` 4칸). 기획서의 네온아이·픽시는 6소켓이라
  `ActiveSocketCount = Min(리깅 4, 머리 6)`에 걸려 4로 잘린다 → **소켓 5/6을 씬에 신설해야 한다.**
- **`로켓 엔진` 다리 파츠가 프로젝트에 없다.** 다리 파츠는 표준/유압/안정화/경량 부츠/제트 부스터
  5종이고 전부 고정 보너스라 "패시브 최대 추가 이동 속도" 램프업 개념 자체가 없다.

### 사용자 확정 사항 (착수 전 질의)

1. **소다캔 효과는 보류** — 머리·스탯·소켓은 다 넣되 효과는 미구현으로 두고 다리 기획서를 기다린다.
2. **네온아이/핫팟의 공격속도 수치는 배율로 환산**하고 플레이 후 조정한다(문서 수치를 그대로
   `weapon_atsp`에 더할 수 없어서다 — 아래 "수치 환산" 참고).
3. **가드맨의 "2번 발사"는 연속 2회 발사**(짧은 간격을 두고 8발씩 두 번)로 구현한다.
4. **머리 선택 화면은 `Title.unity` 안의 패널**로 만든다(새 씬·빌드 세팅 변경 없음).

### 머리 12종 데이터 (기획서 p.5~8 전사)

| id | 이름 | 체력 | 질량 | 소켓 | 기본 무기 | 효과 |
|---|---|---|---|---|---|---|
| 100001 | 컴스톡 MK-01 | 100 | 10 | 2 | 기관단총 300901 | [연사] 공격속도 +15% |
| 100003 | 가드맨 | 80 | 7 | 2 | 전투산탄총 300201 | [산탄] 피해량 +15%, 연속 2회 발사 |
| 100004 | 메테우스 | 120 | 15 | 2 | 로켓런처 300501 | [폭발] 폭발 범위 +20%, 공격속도 +10% |
| 100005 | 버서커 | 120 | 20 | 2 | 로켓런처 300501 | 체력 50% 이하 시 공격력 x1.5, 공격속도 x1.8 |
| 100006 | 해피 픽셀 | 100 | 10 | 4 | 레이저피스톨 300701 | 행운 10당 이동속도 +1·공격속도 x1.2 (최대 3중첩) |
| 100007 | 네온아이 | 50 | 10 | 6 | 기관단총 300901 | 공격력/공격속도 +7.5, 무기 1정당 -1.25 / 체력 +10·방어력 +2 |
| 100008 | 핫팟 | 100 | 20 | 4 | 플라즈마캐논 300401 | 디스크 수 x0.2 만큼 공격력·공격속도·이동속도·방어력 |
| 100009 | 픽시 | 100 | 10 | 6 | 전술 마체테 301201 | 근접 6개면 사거리 x2 / 원거리를 끼면 그 무기 사거리가 근접급으로 |
| 100010 | 팬봇 | 100 | 10 | 4 | 소드오프샷건 300601 | 기본 다리만 장착 가능, 구르기 쿨다운 0, 구르기 무적 제거 |
| 100011 | 소다캔 | 100 | 20 | 4 | 로켓런처 300501 | (보류) 로켓 엔진 장착 + 최대 이동속도 유지 시 공격력 x2 |
| 100012 | 프라이빗 컴스톡 | 140 | 10 | 1 | 지정사수소총 300801 | [정밀] 공격력 x2, 공격속도 x0.5, 관통 +2 |
| 100013 | 미니 픽시 | 100 | 10 | 2 | 기관단총 300901 | 경험치 +50%, 골드 -50% |

기존 `100002 미니 컴스톡`(체력 10000짜리 디버그 로봇)은 **지우지 않고 그대로 두되 선택 목록에서
제외**한다(`selectableInHeadSelect: 0`).

### 수치 환산 근거 (문서 수치를 그대로 못 쓰는 두 곳)

- **네온아이** "공격력/공격속도 7.5 상승, 장착 무기 개수당 1.25 하락": 공격력은 고정값 그대로
  `+(7.5 - 1.25n)`. 공격속도는 로봇 스탯이 없으니 `x(1 + (7.5 - 1.25n)/10)` — 무기 1정이면
  **+62.5%**, 6정이면 **+0%**. 무기당 체력 +10·방어력 +2가 같이 붙어 "화력 vs 생존"이 정확히
  맞물린다(무기 1정: 체력 60·방어 +2 / 6정: 체력 110·방어 +12). `/10`을 쓴 이유는 다른 머리들의
  15~80% 범위와 체감을 맞추기 위함이다(퍼센트로 읽으면 +7.5%라 효과가 거의 안 느껴진다).
- **핫팟** "소지한 디스크 수 = 공격력/공격 속도/이동 속도/방어력 * 0.2": `보너스 = 디스크수 x 0.2`를
  공격력·방어력·이동속도에 **더하고**, 공격속도는 `x(1 + 보너스)`. 디스크 6개면 각 +1.2, 공격속도 x2.2.

### 작업 단계

- **1단계 아트**: `final_250/` 9종 + 네온아이 8프레임을 `Assets/Resources/Heads/`로 임포트
  (`Parts/Body.png`와 동일 설정: spriteMode Single, PPU 100, pivot 중앙). 컴스톡 MK-01은
  기존 `Parts/Body.png` 복제. **가드맨·메테우스는 아트가 없어** 기획서 PDF 5쪽에서 아이콘을
  추출(카드 배경 flood-fill 제거 후 250x250 리샘플)해 **임시 아트**로 쓴다.
- **2단계 데이터**: `HeadModdingInfo`에 `robotName`/`spriteName`/`defaultWeaponId`/`effect`/
  `selectableInHeadSelect` 필드 신설 → `PartsCatalog.asset`에 12종 등록,
  `GameDataAsset.asset`의 `robots:`에 100003~100013 추가.
- **3단계 효과 런타임**: `HeadEffect` enum + `HeadEffects` 정적 클래스 신설. 훅 지점은
  `PlayerShootManager.ComputeDamage`(피해량) / 발사 쿨다운 계산부(공격속도) /
  `FireProjectiles`의 관통·폭발 반경·탄 수 / `GetTravelRange`·`GetDetectRange`(사거리) /
  `RobotStats.Compute`(체력·공격력·방어력·이동속도) / `PlayerRobotController`(버서커 체력 조건,
  팬봇 구르기) / `RewardPickup`(미니픽시 경험치·골드).
- **4단계 비주얼**: `ProceduralCharacterRig`가 선택된 머리의 스프라이트를 몸통에 적용.
  네온아이는 8프레임을 느리게 순환(빨강→주황→노랑→초록→하늘→파랑→보라→분홍)시켜
  "눈 색이 천천히 바뀌는" 컨셉을 살린다.
- **5단계 선택 화면**: `Title.unity`에 머리 선택 패널(코드 생성). 12종 격자 + 선택한 머리의
  능력치·효과 설명 + 출발 버튼.
- **6단계 씬**: `Ground01.unity` Player에 무기 소켓 5/6 신설 + `weapon_slots` 6칸 확장.
  시작 무기를 씬 고정값에서 **머리의 `defaultWeaponId`** 로 전환.
- **7단계 검증**: 플레이모드 실측.

### 구현 결과 `[완료]`

**신규 파일 5개**
- `HeadEffect.cs` — 머리 효과 12종 enum + 한글 이름 + **설명 문구 생성기**. 문구를 데이터에
  고정 문자열로 두지 않고 `HeadEffects`의 상수를 읽어 만든다 — AI 코어 3택 카드에서 고정 문구가
  수치와 어긋났던 함정(2026-08-13)을 되풀이하지 않으려는 것. 밸런스를 바꾸면 화면 설명이 따라온다.
- `HeadEffects.cs` — 12종의 실제 계산 전부. **훅 지점이 6개 파일에 흩어져 있어서**(피해량·공격속도
  =PlayerShootManager, 스탯=RobotStats, 구르기=PlayerRobotController, 파츠 제한=ModdingManager,
  보상=RewardPickup) 각 호출부가 `switch`를 따로 갖게 두면 머리를 추가할 때마다 6곳을 고쳐야 한다.
  그래서 호출부는 "배율 하나만 물어보는" 형태로만 두고 분기는 여기서만 한다.
  씬을 뒤지지 않도록 `Bind`/`RegisterPlayer`/`RegisterShootManager`로 참조를 받아두고,
  아무것도 바인딩되지 않은 상태(타이틀 화면·리그 데모 씬)에서도 전부 "효과 없음" 기본값을 돌려준다.
- `HeadSpriteLibrary.cs` — `Resources/Heads/` 로더 + 네온아이 프레임 순환. `Time.timeScale=0`인
  정비/상점 화면에서도 색이 멈추지 않도록 **unscaled 시간**을 받는다.
- `HeadSelectPanelUI.cs` — 타이틀 씬 위에 뜨는 선택 화면(코드 생성). 머리 개수가 데이터에서 오므로
  씬에 칸을 미리 깔 수 없다(상점 소켓 창이 버튼 4개 고정이라 6소켓에서 막혔던 것과 같은 함정).
- `DashGaugeUI.cs` — 캐릭터 발밑을 따라다니는 DASH 게이지(아래 "추가 요청" 참고).

**수정 파일 11개**: `PartsCatalog.cs`(HeadModdingInfo에 spriteName/defaultWeaponIds/effect/
selectableInHeadSelect/spriteFrameCount/spriteFrameSeconds 6필드 추가 + `GetSelectableHeads()`),
`PlayerShootManager.cs`, `RobotStats.cs`, `PlayerRobotController.cs`, `ModdingManager.cs`,
`RewardPickup.cs`, `ProceduralCharacterRig.cs`, `ModdingPanelUI.cs`, `ShopPanelUI.cs`,
`TitleSceneManager.cs`, `GameHUD.cs`.
**수정 에셋**: `GameDataAsset.asset`(robots 2 → 13행), `PartsCatalog.asset`(headModdingInfos 13개),
`Ground01.unity`(무기 소켓 5/6 신설 + weapon_slots 6칸), `Title.unity`(DataManager 신설 +
partsCatalog 연결). **신규 아트 19장** `Assets/Resources/Heads/`.

**구현 시 걸린 함정**
- **무기 소켓 5/6이 없으면 6소켓 머리가 조용히 4소켓이 된다.** `ActiveSocketCount =
  Min(씬 리깅 수, 머리 값)`이라 데이터만 6으로 올려도 화면엔 4개만 나온다. 씬에 소켓을 신설하고
  `weapon_slots`를 6칸으로 늘려야 실제로 6이 된다(어깨 y3.60 / 허리 y1.50에 이어 **x±4.2, y2.55**).
- **관통 +2를 무제한 관통(-1) 무기에 더하면 관통이 줄어든다**(-1+2=1). `pierce >= 0`일 때만 더한다.
- **`ProceduralCharacterRig.RestorePartSprites()`가 매 프레임 몸통을 강제 복구**하므로, 네온아이
  색 순환은 이 함수 안에서 프레임을 갱신해야 한다(밖에서 바꾸면 즉시 되돌려진다).
- **`EquipWeapon(i, 0)`으로는 소켓을 비울 수 없다**(무기ID 0의 데이터가 없어 false 반환 후 기존
  무기 유지). 소켓 비우기는 `weapon_slots[i].weapon_id = 0` 직접 대입 경로(ApplyHeadDefaultWeapons)뿐이다.
- **머리 선택 화면에 암막만 깔면 부족하다.** 볼륨 슬라이더는 런타임에 캔버스의 마지막 자식으로
  붙어 형제 순서상 선택 화면보다 뒤에 그려지고, 종료 버튼도 암막 위로 비쳤다. z-order를 다투는
  대신 타이틀 UI 5개를 아예 끈다(`SetTitleUiVisible`).

**검증**
- **효과 계산 40개 항목 전수 자동검증(edit 모드, pass=40 fail=0)** — 컴스톡/가드맨/메테우스/
  프라이빗 컴스톡의 카테고리 조건부 배율, 네온아이의 무기 0정↔6정 양 끝값(공속 x1.75↔x1.0,
  체력 50↔110, 방어 5↔17), 핫팟 디스크 0↔6(공속 x1.0↔x2.2, 이동속도 3↔4.2), 해피 픽셀 행운
  0/25/90 중첩(이동속도 3/5/6, 상한 3중첩 확인), 팬봇 파츠 제한(기본 다리 허용·강화 다리 거부),
  미니 픽시 보상 배율, 카테고리 불일치 시 무효 처리까지 포함.
- **플레이모드 실측** — 타이틀에서 "게임 시작" → 선택 화면 12칸(이름·아이콘 전부 일치, 네온아이
  아이콘이 `NeonEye_5`/`NeonEye_7`로 바뀌는 것으로 색 순환 동작 확인) → 머리 선택 시 강조 1칸만
  이동 → "출발" → `PlayerSession.SelectedRobotId` 반영 → Ground01.
  프라이빗 컴스톡: 체력 140 / 활성 소켓 **1** / s0=지정사수소총 / **s1~s5 스프라이트 전부 꺼짐** /
  몸통=`PrivateComstock`. 네온아이: 체력 60·공격력 9·방어력 7(무기 1정 기준 계산과 일치) /
  활성 소켓 6 / 몸통=`NeonEye_7`. 팬봇: s0=소드오프샷건 / 몸통=`FanBot` / 쿨다운배율 0·무적 false.
  버서커: 만피 1.0/1.0 → 체력 40%에서 **1.5/1.8**. 픽시: 근접 6정에서 사거리 x2(1.76→3.52),
  원거리를 섞으면 배율 1로 떨어지고 그 원거리 무기 사거리가 **1.76(근접급)** 으로 잘림.
- 컴파일 오류 0건(기존 `EquipmentDetailPopup.cs` 경고 1건은 이번 작업과 무관).
- **검증 방법 주의**: 플레이모드는 MCP 호출 사이에도 실시간으로 계속 돌아간다. 방치하면 조작 없는
  플레이어가 웨이브 1에 죽어 `IsGameOver=true`가 되고, 그 상태로 읽으면 체력 0·게이지 비활성 같은
  값이 나와 버그로 오해하게 된다(이번에 3번 겪었다). `Time.timeScale=0`도 씬 로드 시 게임 코드가
  1로 되돌린다. **읽을 값은 한 호출 안에서 다 읽는 것이 안전하다.**

**남은 것**: 소다캔 효과(다리 기획서 대기), 가드맨·메테우스 전용 아트(현재 기획서 PDF에서 추출한
임시 아트 - `Assets/Resources/Heads/Guardman.png`·`Meteus.png`에 250x250 PNG를 덮어쓰면 코드
수정 없이 교체된다).

### 추가 요청 2건 `[완료]`

**1. DASH UI를 우하단 버튼형 → 캐릭터 밑 게이지 바로 교체** (사용자 첨부 이미지 기준)
- 신규 `DashGaugeUI` — 바탕(빈 게이지) + 초록 채움(`UI/Green_bar00`) + 흰색 굵은 `DASH` 글자.
  **게이지가 가득 차면 구를 수 있다**(`1 - DashCooldownRatio`). 차오르는 중에는 글자 알파를 0.55로
  낮춰 "아직 못 쓴다"를 알린다. 팬봇은 쿨다운이 0이라 항상 가득이다.
- **월드 오브젝트가 아니라 캔버스 UI**로 만들었다 — 글자가 또렷해야 하고 막대가 9-슬라이스라
  적 체력바처럼 SpriteRenderer로 하면 3D 텍스트를 따로 얹어야 한다. 대신 매 프레임 캐릭터
  월드좌표를 화면좌표로 변환해 따라가고, **크기도 카메라에서 역산**(`Screen.height /
  (2×orthographicSize)`)하므로 해상도가 바뀌어도 캐릭터 대비 비율이 유지된다(실측 155x30px @FHD).
- 9-슬라이스 함정 재발: 막대가 원본 아트보다 훨씬 납작해 테두리가 가운데를 다 먹으므로
  `pixelsPerUnitMultiplier = 3`으로 테두리 크기를 줄였다(2026-08-13 체력 바와 같은 처리).
- 구버전 우하단 아이콘(`Canvas/DashSkill`)은 **삭제하지 않고 런타임에 비활성**한다
  (`GameHUD.EnsureDashGauge`). 되돌리려면 그 오브젝트를 켜고 게이지 생성만 막으면 된다.
- 런타임 생성 오브젝트라 `GameFlowManager.combatHudObjects`(씬 오브젝트 배열)에 등록할 수 없어
  정비/상점/게임오버 중 숨김은 `GameFlowManager.IsIntermission`을 스스로 확인한다
  (`PlayerHitFeedback`과 같은 관례). 도메인 리로드로 참조가 날아가도 매 프레임 다시 붙인다.

**2. 머리 선택 화면 안내 문구에서 "(상점에 등장하지 않음)" 삭제** — 사용자 요청.
문구는 이제 `머리는 게임 시작 시 한 번만 고르며 런 중에는 바꿀 수 없습니다`다.

부수적으로 로봇 100001의 이름을 기획서 표기에 맞춰 `컴스톡 MK-1` → **`컴스톡 MK-01`** 로 고쳤다.

### 드랍 아이템(경험치·골드·부품상자) 전용 아이콘 적용 완료 (2026-08-19)

사용자가 프로젝트 루트 `드롭아이템/` 폴더에 제공한 신규 아트 3장(`드롭아이템_경험치.png`
`드롭아이템_골드.png` `드롭아이템_부품상자.png`)을 적용했다. `RewardPickupManager`가
`Resources.Load<Sprite>`로 아이콘을 찾는 구조라(2026-08-10 이전부터 `Gold`/`Energy`/`ItemBox`
리소스명 고정) 코드 수정 없이 이미지만 교체하면 되는 부분과, 이름을 바꿔야 하는 부분이 갈렸다.

- **골드**(`Assets/Resources/Gold.png`)와 **부품 상자**(`Assets/Resources/ItemBox.png`)는 기존
  파일을 새 이미지로 그대로 덮어썼다(같은 파일명 → 같은 GUID·기존 `.meta` 재사용, 참조 갱신 불필요).
  `ItemBox.png`는 `PartBoxIndicatorUI`의 화면 밖 화살표 아이콘과도 공유되므로 그쪽도 함께 갱신된다.
- **경험치**는 기존에 전용 아이콘이 없어 **`Energy` 리소스를 임시로 재사용**하고 있었는데
  (`RewardPickupManager.cs` 주석에 "나중에 교체 권장"이라고 남아 있었음), `Energy.png`는
  플라즈마 캐논/레이저 피스톨 등 **에너지 빔 무기의 투사체 스프라이트로도 쓰이고 있어**
  (`WeaponTableGenerator.cs`의 `tanhwan="Energy"`, `PlayerShootManager.beam_sprite_name`) 그대로
  덮어쓰면 빔 무기 시각이 깨진다. 그래서 신규 `Assets/Resources/Exp.png`를 새로 만들고(`.meta`는
  `Gold.png.meta`를 본떠 새 GUID로 생성), `RewardPickupManager.ExpResourceName`을
  `"Energy"` → `"Exp"`로 바꿨다.
- 세 리소스 모두 `Resources.Load<Sprite>` 성공을 확인했고(텍스처 108x108/108x108/180x180),
  `RewardPickupManager.CreateVisual()`이 스프라이트 bounds 기준으로 최종 크기를
  `VisualWorldSize`(0.6유닛)에 맞춰 정규화하므로 원본 픽셀 크기가 달라도 인게임 표시 크기는
  기존과 동일하게 유지된다.
- **검증**: `assets-refresh` 후 콘솔 에러 0건. `script-execute`로 `RewardPickupManager.SpawnReward()`
  를 세 종류 다 호출해 실제로 씬에 생성하고 `screenshot-isolated`로 캡처해 골드 코인·초록 경험치
  보석·부품 상자 궤짝이 각각 새 아트로 렌더링되는 것을 육안 확인했다(검증용으로 만든 오브젝트는
  즉시 정리, `Ground01`/`Title` 씬은 변경 없이 원상태로 복귀 - `IsDirty: false` 확인).

### 일반 좀비 이동 애니메이션 회귀 수정 (2026-08-19)

사용자 리포트: "일반 좀비 이동모션이 없어졌음". 원인은 이 세션 이전에 이미 (커밋되지 않은 채로)
적용돼 있던 **몬스터별 전용 아트 교체**였다 — `Charger`/`Disruptor`/`Leader`/`Spitter`/`Sprinter`/
`Zombie` 6종 프리팹이 전부 기존에 공유하던 placeholder 스프라이트(색만 다르게 물들인 좀비
스프라이트 재활용, 예: 디스럭터=빨강 틴트, 리더=보라 틴트)를 버리고 각자 전용 이미지
(`Assets/Resources/{Charger,Disruptor,Leader,Spitter,Sprinter,Zombie}.png`)로 교체되고 몸 색도
전부 흰색(틴트 없음)으로 초기화돼 있었다.

`EnemyUnit.cs`의 `CanPlayZombieAttackFrames()`(걷기·공격 프레임 재생을 기본 좀비에만 한정하는
안전장치)가 `original_body_sprite.name == "Enemy_zombie_S"`를 조건으로 걸고 있었는데, 새 전용
아트의 스프라이트 이름이 `"Zombie"`로 바뀌면서 이 조건이 항상 거짓이 되어 **걷기(`walk_left_f0~7`)
와 공격 프레임이 통째로 재생을 멈췄다.** `BasicZombieSpriteName` 상수를 `"Zombie"`로 갱신해
해결([EnemyUnit.cs:20](Assets/Scripts/EnemyUnit.cs:20)). 플레이모드 실측으로 좀비가 이동 중
`walk_left_f7` 등 프레임을 순환 재생하는 것을 확인했다.

### 적 피격 시 "빨간색" 리포트 조사 — 원인 미확정, 사용자 확인 대기

사용자 리포트: "적 피격할때 하얀색으로 잠깐 변하는 것만 있어야 하는데 빨간색으로 변하는 이상한
게 들어가있음". 플레이모드 실측(`Time.timeScale=0`으로 `HitFlash` 플래시를 진행 중 상태로 고정한
뒤 캡처)으로 다음을 확인했다:

- **몸체 자체는 어떤 경우에도 항상 흰색으로만 물든다** — `HitFlash`/`SpriteFlash` 셰이더·
  `SpriteFlashBase.mat` 전부 `_FlashColor=(1,1,1,1)`이고, 위 프리팹 아트 교체로 6종 전부 기본
  틴트도 흰색이라 붉은 색이 나올 경로가 없다(플래시 중 캡처 = 완전한 흰 실루엣, 플래시 종료
  후 캡처 = 좀비 본연의 다색 아트, 둘 다 빨간 기미 없음 - 스크린샷으로 확인).
- 유일하게 빨간색인 요소는 **머리 위 체력바**(2026-08-09 추가 - 체력 100% 미만이면 나타나고
  빨강↔초록으로 변함)였으나, **사용자가 이것이 아니라고 확인**함.
- `EnemyUnit`의 공격 예비 동작(텔레그래프) 색은 `AttackTelegraphColor = (1, 0.55, 0.15)`(주황)로
  코드상 빨강은 아니지만 육안으로는 붉은 계열로 보일 여지가 있다 - 아직 실측 확인은 못함
  (플레이모드가 무인 상태로 흘러가며 플레이어가 먼저 죽어 공격 중인 적을 못 잡음). 보스
  AoE 예고 원(`BossUnit.telegraphColor = (1, 0.15, 0.15, 0.35)`, 반투명 빨강)도 후보이나 20웨이브
  전용이라 가능성 낮음.
- **다음 세션에서 확인할 것**: 정확히 어떤 몬스터·어떤 타이밍(피격 순간/직후/공격 시)에
  빨간색이 보이는지, 화면 전체인지 몬스터 한정인지 스크린샷과 함께 사용자에게 재확인 필요.

### 가드맨 "연속 2회 발사" 시 두 번째 탄이 엉뚱한 방향으로 나가는 버그 수정 (2026-08-19)

사용자 리포트: "샷건 2연속 발사할때 같은 방향으로 두번 발사하여야 하는데, 한번 발사하고 방향을
180도 회전하고 두번째 발사하는 경우가 있음".

원인은 오늘(2026-08-19) 먼저 구현된 **가드맨 머리 전용 효과 "연속 2회 발사"**
([HeadEffects.cs:299](Assets/Scripts/HeadEffects.cs:299) `ExtraBursts()` - 산탄총(`WeaponType.Shotgun`)
에만 적용, 1·2회차 사이 간격 0.12초)의 구현 버그였다.
[PlayerShootManager.cs:934](Assets/Scripts/PlayerShootManager.cs:934) `FireExtraBursts()`가
"타겟이 그 사이 죽었으면 조준 방향만 마지막으로 알던 쪽으로 유지한 채 쏜다"는 주석과 달리,
**`last_direction`의 초기값을 실제 1회차 발사 방향이 아니라 고정값 `Vector3.right`(0도, 오른쪽)로
잡고 있었다.** 2회차 시점(`WaitForSeconds(0.12초)` 이후)에 `target.IsDead`이면 방향 갱신
블록(`if (target != null && !target.IsDead) { ... last_direction = ... }`)을 건너뛰므로
`last_direction`이 초기값 그대로 남는다 - **1회차가 적을 즉사시키는 경우가 흔한 산탄총 특성상**,
1회차 발사가 0도(오른쪽)가 아닌 다른 각도였다면 2회차는 그 각도와 무관하게 항상 오른쪽으로
튀어, "쏘고 나서 엉뚱한 각도로 도는" 것처럼 보였다(사용자가 본 "180도 회전"은 1회차 방향이 마침
왼쪽(180도)이었던 경우).

`FireExtraBursts()`에 `initial_direction` 매개변수를 추가하고, 호출부인
[TryFireSlot()](Assets/Scripts/PlayerShootManager.cs:723)이 이미 계산해 둔 1회차 `aim_direction`을
그대로 넘겨 `last_direction`의 초기값으로 쓰도록 수정했다. 이제 2회차 시점에 타겟이 죽어 있어도
1회차와 정확히 같은 방향으로 발사된다.

**검증**: 컴파일 에러 0건 확인(`assets-refresh` 후 콘솔 클린). 코드 경로 추적으로 원인·수정
모두 확정적으로 검증했다(초기값이 `Vector3.right`였다가 실제 1회차 방향으로 바뀌는 것이
버그 재현 조건과 정확히 일치) - 가드맨 머리 + 산탄총 장착 + 1회차 즉사라는 특정 조합을 갖춰야
재현되는 조건이라 플레이모드 실측은 다음 세션 과제로 남긴다.

### 구르기(DASH) 게이지 "완충 시 숨김" 적용 + 웨이브 1 이후 게이지가 사라지는 버그 수정 (2026-08-19)

사용자 요청 2건: (1) 구르기 게이지가 완충되면 사라지고 쿨타임(충전 중)일 때만 보이게 할 것.
(2) 1웨이브 이후로 게이지 바가 아예 없어지는 버그 수정.

**두 요청의 원인이 같은 코드 한 곳으로 귀결됐다.** 기존 `DashGaugeUI.LateUpdate()`는 숨겨야 할 때
`root.gameObject.SetActive(false)` — **즉, 이 스크립트 자신이 붙어 있는 바로 그 오브젝트를
직접 꺼버렸다.** Unity는 비활성 GameObject에 대해 `LateUpdate()`를 아예 호출하지 않으므로,
한 번 숨은 뒤에는 **자기 자신을 다시 켤 기회 자체가 영원히 사라진다.** 이전 구현은 "정비/상점
화면 진입 시에만" 숨기는 용도로 이 패턴을 썼는데, 마침 그때는 마지막에 `GameFlowManager.
HandleNextWaveRequested()`가 `combatHudObjects`(HP 등)는 되살리지만 `DashGauge`(런타임 생성
오브젝트라 그 배열에 등록되지 않음)는 아무도 되살려주지 않았다 - 그래서 **1웨이브 종료(정비
화면 진입) 시 숨은 뒤 다음 웨이브가 시작돼도 계속 사라진 채로 남아 있었다.**

**플레이모드 실측으로 재현부터 확인했다** — `script-execute`로 `GameFlowManager.
HandleWaveEnded(1)`(웨이브 종료 → 정비 진입)과 `HandleNextWaveRequested()`(정비 종료 → 전투
재개)를 리플렉션으로 직접 호출해 웨이브 전환을 그대로 재현했고, `Resources.FindObjectsOfTypeAll
<DashGaugeUI>()`(비활성 오브젝트도 찾는다 - `GameObject.Find`는 비활성 오브젝트를 못 찾아
"파괴됐다"는 오진단을 할 뻔했다)로 오브젝트 자체는 살아있는데 `activeSelf=False`인 채로
전투 중에도 계속 숨어 있는 것을 확인해 원인을 확정했다.

**수정**: `DashGaugeUI`에 배경/채움/글자를 묶는 자식 `visual`(신규)을 만들고, 숨길 때
`root`가 아니라 **`visual`만** `SetActive(false)`한다. `root`(= 이 스크립트가 붙은 오브젝트)는
항상 켜 둔 채라 `LateUpdate()`가 매 프레임 계속 돌고, 그래서 조건이 바뀌면 스스로 다시
보여줄 수 있다. 같은 김에 (1)도 처리했다 - 가시성 조건에 `ready < 0.999f`(완충되지 않음)를
추가해 **정비/상점/게임오버 중이 아니고 + 쿨타임이 남아있을 때만** 보이게 했다(완충되면 숨고,
구르기를 쓴 순간부터 다시 나타난다). "다 찼을 때만 글자를 또렷하게" 로직은 이제 보이는 동안은
항상 충전 중이라 의미가 없어져 제거했다.

**검증**(플레이모드 실측, 리플렉션으로 `dashCooldownLeft` 필드를 직접 조작해 재현):
쿨다운 50%(`ready=0.5`) 상태에서 `visual.activeSelf=True`(보임) 확인 → `HandleWaveEnded(1)`로
정비 진입 → `visual`은 숨었지만 `root.activeSelf=True`(자기 자신은 살아있음) 확인 →
`HandleNextWaveRequested()`로 웨이브 2 재개 → 다시 쿨다운 50%로 설정 → **`visual.activeSelf=True`
로 정상 재표시 확인**(수정 전에는 이 마지막 단계에서 계속 `False`로 남아 있었을 것). 완충
상태(`ready=0`)에서는 전투 중에도 `visual.activeSelf=False`로 숨는 것도 함께 확인. 검증에
사용한 시간 조작(`Time.timeScale=0` 고정, 리플렉션으로 쿨다운 값 강제 설정)은 테스트 전용이며
씬에는 변경을 남기지 않았다(`Ground01`/`Title` 씬 `IsDirty: false` 확인 후 원래 열려 있던
`Title.unity`로 복귀).

### 부머(디스럭터) 밸런스 조정 + 프라이빗 컴스톡 정밀 효과 조정·버그 수정 (2026-08-19)

**1. 부머(=디스럭터, 자폭 좀비) 피해량 -50% / 폭발 범위 -25%** — 사용자가 "부머"라고 불러
처음엔 대상이 불명확했으나(코드·기획 문서 어디에도 이 이름이 없음) 확인 결과 자폭 좀비
디스럭터를 가리키는 것이었다. `GameDataAsset.asset`의 몬스터ID 200005 `monster_atk`를
75 → **38**(75×0.5=37.5 반올림)로, [DisruptorUnit.cs](Assets/Scripts/DisruptorUnit.cs)의
`explosionRadius`를 3.0 → **2.25**(×0.75)로 낮췄다. 둘 다 임시값이었던 것을 조정한 것.

**2. 프라이빗 컴스톡(머리 100012) [정밀] 효과 수치 조정** —
[HeadEffects.cs](Assets/Scripts/HeadEffects.cs)의 `PrivateComstockAttackSpeed` 0.5 → **0.75**
(발사 간격이 2배가 아니라 1.33배로 늘어남), `PrivateComstockPierce` +2 → **+1**로 완화. 수치가
반영된 설명 문구([HeadEffect.cs](Assets/Scripts/HeadEffect.cs)의 `GetDescription()`)는 상수를
그대로 읽어 만들어지므로 자동으로 갱신된다.

**3. 버그 수정 — "정밀화기 장착 시 공격력 x2"가 로봇 전체 공격력까지 함께 두 배로 늘리던 문제.**
사용자 지적: "정밀화기 장착시 공격력 2배도 정밀화기의 공격력만 2배가 되어야 함".
[PlayerShootManager.ComputeDamage()](Assets/Scripts/PlayerShootManager.cs:1093)는
`damage = weapon.weapon_atk + robot_atk / 투사체수`를 구한 뒤 **맨 마지막에 그 합계 전체**에
`HeadEffects.DamageMultiplier(weapon)`(프라이빗 컴스톡이면 x2)를 곱하고 있었다. `robot_atk`는
"슬롯 수만큼 균등 분배되는 로봇 전체 공격력"(모든 무기 슬롯에 공평하게 나눠주려는 설계 의도,
2026-08-07 확정)이라, 여기까지 정밀 배율이 덮쳐 정밀 무기 하나만 껴도 다른 슬롯 무기의
실질 화력까지 덤으로 오르는 부작용이 있었다.

`HeadEffects.cs`에 무기 자체 위력에만 곱하는 신규 `WeaponAttackMultiplier(weapon)`를
추가하고(현재는 프라이빗 컴스톡의 x2가 유일하게 여기 속한다), 기존 `DamageMultiplier`에서는
프라이빗 컴스톡 케이스를 제거해 1을 돌려주게 했다(가드맨 산탄·버서커는 원래 의도대로 전체
데미지에 계속 적용됨 - 둘 다 서로 배타적이라 중복 적용되지 않는다). `ComputeDamage()`는 이제
`weapon.weapon_atk * WeaponAttackMultiplier(weapon)`을 먼저 구한 뒤 `robot_atk` 분배분을
더한다.

**검증**(플레이모드 실측): 리플렉션으로 `HeadEffects`의 캐시 필드(`cached_effect`/`resolved`/
`cached_robot_id`)를 직접 `PrivateComstock`으로 강제하고(`PlayerSession.SelectedRobotId`를
정상 경로로 설정했을 때는 플레이 진입 시 도메인 리로드로 초기화돼 버렸다 - 이 우회가 필요했다),
대물저격총 일반(무기ID 300301, `weapon_atk=120`, 정밀 타입)으로 `ComputeDamage()`를 리플렉션
직접 호출했다. 결과 243 = **120×2 + 3(robot_atk)** — 수정 전 공식이었다면
(120+3)×2 = 246이 나왔어야 한다. `WeaponAttackMultiplier=2`/`DamageMultiplier=1`도 함께 확인해
서로 배타적으로 정확히 분리된 것을 검증했다. 컴파일 에러 0건, 씬 변경 없음(원래 열려 있던
`Title.unity`로 복귀).

### ESC 일시정지 버그 수정 + 우상단 설정 아이콘 + 근접무기 대기 자세 + 머리 정렬 (2026-08-19)

사용자 리포트 3건. **ESC 버그의 원인이 오늘 `DashGaugeUI`에서 고친 것과 완전히 같은 유형**이었다.

#### 1. ESC 일시정지가 아예 동작하지 않던 버그 `[확정 원인]`

리포트: "인게임에서 esc누르면 설정창 나오고 일시정지 되어야 하는데 지금 적용이 안됨".

[PauseMenuUI.cs](Assets/Scripts/PauseMenuUI.cs) `Build()`의 마지막 줄이
`root.gameObject.SetActive(false)`였는데, 이 `root`는 **`PauseMenuUI` 컴포넌트 자신이 붙어 있는
바로 그 GameObject**다(`EnsureAttached`에서 `new GameObject("PauseMenu")` → `AddComponent
<PauseMenuUI>()` → `Build(go.transform)`). Unity는 비활성 GameObject의 `Update()`를 호출하지
않으므로 **ESC 폴링(`Keyboard.current.escapeKey.wasPressedThisFrame`)이 단 한 번도 실행되지
않았다.** 메뉴는 구조적으로 절대 열릴 수 없었다.

작업.md에 "테스트 완료"로 기록돼 있던 이유는, 당시 `OpenPause()`/`ClosePause()`를 리플렉션으로
직접 호출해 상태만 검증하고 **실제 ESC 키 경로는 한 번도 밟지 않았기** 때문으로 보인다.
(`SettingsPanelUI`도 자기 자신을 끄지만 `Update()`가 없고 명시적 호출로만 동작해 문제없다.)

**수정**: `DashGaugeUI`와 같은 해법 - 딤 배경·옵션 패널을 자식 `Content`로 묶고, 숨길 때
루트가 아니라 `Content`만 끈다. 루트는 항상 활성이라 `Update()`가 계속 돌아 스스로 열 수 있다.
`settingsPanel`은 그리기 순서를 유지하려고 지금처럼 `root` 직속으로 뒀다.
ESC 폴링과 새 설정 아이콘이 **게이팅 조건을 공유**하도록 `public bool TryOpenPause()`를 추출했다.

#### 2. 우상단 설정 아이콘 신설 (사용자 선택: 코드로 톱니바퀴)

신규 [SettingsIconUI.cs](Assets/Scripts/SettingsIconUI.cs) — 씬 수정 0건, `EnsureAttached`로
Canvas 아래에 런타임 생성(`PauseMenuUI`·`AiCoreExtraButtonsUI`와 같은 관례).
`GameFlowManager.EnsurePauseMenu()`에서 일시정지 메뉴 **뒤에** 붙인다(형제 순서 = 그리기 순서).
정규화 앵커(0.930~0.985 / 0.895~0.975) + offset 0으로만 배치해 해상도에 안 흔들린다.
클릭하면 `PauseMenuUI.TryOpenPause()`를 호출한다.

**이번 버그의 교훈을 그대로 반영**해 자기 자신을 끄지 않고 자식 `Content`만 토글한다.
정비·상점·게임오버·승리 중이거나 **일시정지 메뉴가 열려 있으면 숨긴다**(메뉴보다 뒤 형제라
딤 배경 위에 떠 버리기 때문).

아이콘은 `UiIconLibrary.Settings()` 신설 — `Lock()`/`Frame()`과 같이
`Resources.Load<Sprite>("UI/Settings_icon")`을 먼저 찾고 없으면 코드로 그린다(원판 + 중앙 구멍 +
톱니 8개, 채워진 원용 `Disc()` 헬퍼 추가). **`Assets/Resources/UI/Settings_icon.png`를 넣으면
코드 수정 없이 자동 교체된다.**

#### 3. 근접무기 대기 자세 좌/우 미러링 `[확정 원인]`

리포트: "근접무기 왼팔에 달려있는건 공격 대기 시간동안 칼끝이 왼쪽을 향하고 오른팔에
달려있는것도 마찬가지로 오른쪽을 향하게".

근접무기 조준식이 `atan2(dir) + weapon_imgangle`이라 `imgangle`은 "피벗 회전 ↔ 실제 칼끝 방향"
변환 상수다. 대기 자세도 같은 값을 더하므로 **상쇄되어 화면상 칼끝 각도 = 기준값 그 자체**가
된다. 그런데 기준값이 총기용 `rest_rotation_degrees`(좌 8.112 / 우 **-3.233**)여서 **둘 다 0도
(오른쪽) 근처** — 왼팔 칼도 오른쪽을 향했다. 이 값들은 좌우가 따로 그려진 미러링 총 이미지
기준으로 튜닝된 것이라 좌우 이미지가 동일한 근접무기 3종과 공유하면 안 되는 값이다.

**수정**: `MeleeRestFacingDegrees(slot_index)` 신설 — 왼쪽 소켓은 `180 - tilt`, 오른쪽은 `tilt`.
사용자 확정에 따라 **위로 15도**(`melee_rest_tilt_degrees = 15`, 인스펙터 조절 가능)이며
좌우 정확한 미러다(15도를 세로축 대칭 → 180-15 = 165도). **총기 경로는 한 줄도 안 건드렸다.**

좌/우 판정은 `rig_point.localPosition.x` 부호이며 `Awake`의 `CacheSocketSides()`에서 **한 번만
굳힌다** — `StartMeleeThrustVisual`이 찌르기 중 이 Transform을 실제로 밀어내고 `Player.localScale`
이 0.219라 월드 1유닛 ≈ 로컬 4.5유닛이어서, 라이브 좌표에 의존하면 부호가 뒤집힐 수 있다.
⚠ **`use_left_hand_image`는 물리적 좌우와 반대로 박혀 있어(왼쪽 소켓이 0) 방향 판정에 쓰면 안 된다.**
캐릭터 좌우 반전은 `flipX`만 바꾸고 Transform은 안 건드리므로 소켓의 좌/우 정체성은 항상 고정이다.

#### 4. 가드맨·메테우스 머리가 다리와 떨어져 보이던 문제 + 12종 전수 검증

리포트: "가드맨 대가리가 다리하고 너무 멀리 떨어짐. 다른 머리도 검증".

머리 12종은 전부 250x250 / `spritePivot (0.5,0.5)`인데, 리그가 이 피벗을 원점으로 몸통을 그리므로
**이미지 안에서 내용물이 위로 치우친 머리는 다리보다 높이 떠 보인다.** PNG 알파 bbox를 전수
실측하니 내용물 세로 중심이 나머지 10종은 121~126px(이미지 중심 125 ±4)인데
**가드맨·메테우스만 95px(30px 위)** 였다. 작업.md에 이 둘은 "기획서 PDF에서 추출한 임시 아트"로
기록돼 있고 파일 타임스탬프도 둘만 달라(14:22 vs 09:49) 최근 교체된 것과 일치한다.
→ **나머지 10종은 정상이라 손대지 않았다.**

`Guardman.png.meta`·`Meteus.png.meta`의 `alignment: 0 → 9`(Custom), `spritePivot.y: 0.5 → 0.62`
(내용물 중심이 아래에서 155px → 155/250). `alignment`를 Custom으로 바꿔야 Unity가 `spritePivot`을
실제로 반영한다. **부작용 없음**: 피벗을 원점으로 쓰는 건 `SpriteRenderer`(리그 몸통)뿐이고,
정비·상점·게임오버의 머리 아이콘은 `Image`(Simple)라 RectTransform 기준으로 그려진다.

#### 검증 (전부 플레이모드 실측)

- **ESC**: `PauseMenu` 루트가 `activeSelf=True`(수정 전엔 False - 이게 버그의 본질)이고
  `enabled=True`임을 확인. **`InputSystem.QueueStateEvent`로 실제 ESC 키를 주입**해
  `IsOpen=True / timeScale=0 / IsPaused=True` → 다시 ESC로 `IsOpen=False / IsPaused=False`까지
  왕복 확인. timeScale 라운드트립도 실전 조건(1→0→1)으로 별도 확인.
  ⚠ 처음에 `InputSystem.Update()`를 수동 호출했더니 프레임 타이밍이 어긋나 키가 먹지 않았다 -
  **큐에만 넣고 정규 플레이어 루프가 소비하게 해야 한다**(ProcessEventsInDynamicUpdate).
- **일시정지 화면**: `Content` 재구성 후에도 딤 배경·옵션 5버튼이 기존과 동일하게 렌더링됨을
  FHD 캡처로 확인. 이때 설정 아이콘이 **의도대로 숨는** 것도 함께 확인.
- **설정 아이콘**: 우상단에 톱니바퀴가 또렷하게 렌더링됨을 FHD 캡처로 확인(HUD의 보라 톤과 일치).
  메뉴를 닫으면 다시 나타나는 것(`content.activeSelf=True`)도 확인.
- **근접무기**: 생존단검(301101, `imgangle=-140`)을 좌·우 소켓에 끼우고 적을 전부 없앤 뒤 실측 —
  좌 소켓 `pivotZ=25.0 → 화면 칼끝 165.0도`, 우 소켓 `pivotZ=235.0 → 15.0도`로 **기대값과 정확히
  일치**. 캡처로도 왼쪽 칼은 왼쪽 위, 오른쪽 칼은 오른쪽 위를 향하는 것을 육안 확인.
  ⚠ 첫 시도는 웨이브가 계속 스폰돼 플레이어가 죽어(`IsGameOver`) 각도가 대기값에 도달하지
  못했다 - `WaveManager`와 `EnemySpawner`를 **둘 다** 끄고 다시 측정했다.
- **머리 12종**: 프레임에 의존하지 않는 수치로 검증하려고 몸통 스프라이트 아래끝의 월드 y를
  12종 전부 측정했다. 10종은 전부 동일(`pivot 0.500 / bottomY 1.3284`)이고 가드맨·메테우스만
  `0.620 / 1.1784`(0.15유닛 아래). 알파 bbox까지 반영해 계산하면 **다리와의 실제 간격 차이가
  0.13 → 0.02유닛으로 줄어** 나머지 10종과 사실상 동일해졌다. 가드맨 캡처로 육안 확인.
- 콘솔 에러 0건, `Ground01`/`Title` 씬 `IsDirty: false`(플레이모드에서만 조작해 씬 오염 없음),
  원래 열려 있던 `Title.unity`로 복귀.

**교훈(재발 방지)**: **`MonoBehaviour`가 자기 자신이 붙은 GameObject를 `SetActive(false)`하면
`Update()`/`LateUpdate()`가 영영 호출되지 않아 스스로 다시 켤 수 없다.** 오늘 하루에
`DashGaugeUI`(구르기 게이지)와 `PauseMenuUI`(ESC 메뉴) **두 곳**에서 같은 원인으로 터졌다.
런타임 생성 UI를 숨길 때는 반드시 **자식 컨테이너**를 토글할 것. 신규 `SettingsIconUI`는 이
규칙을 처음부터 적용했다.

### 근접무기 칼끝 방향 버그 + 이끼 낀 디스크 버그 수정 + 디스크 개명 + 머리 아트 2종 교체 (2026-08-19)

사용자 리포트 5건: (1) 근접무기가 대기 중일 때 칼끝이 팔(소켓) 방향과 반대로 가 있다 - 왼쪽을
보든 오른쪽을 보든 오른쪽 소켓의 칼끝은 오른쪽을 향해야 한다. (2) 게임 한 판이 끝나고 타이틀로
돌아갈 때 데이터가 남아 골드를 얻어도 0에 고정되는 버그. (3) "나무 뭐시기 디스크"를 "숲의 소리
디스크"로 개명. (4) 이끼 낀 디스크(5초마다 HP 2 회복)가 적용되지 않는 버그. (5) 새머리 폴더에
올려둔 이미지로 일부 머리 이미지 교체. 진행 중 사용자가 "나머지 디스크들도 작동되는지 검증 후
의도대로 작동 안하는 건 수정"을 추가 요청해 21종 전부를 감사했다.

**1. 근접무기 대기 자세 칼끝 방향 버그.**
[PlayerShootManager.cs:684](Assets/Scripts/PlayerShootManager.cs:684) `UpdateSlot()`의
"타겟 없음"(대기) 분기가 `slot.rest_rotation_degrees`만으로 회전각을 정하고 있었다. 반면 타겟을
조준하는 분기([PlayerShootManager.cs:706](Assets/Scripts/PlayerShootManager.cs:706))는 근접무기에
한해 `weapon_imgangle`(원본 그림이 그려진 방향과 조준각의 차이를 메우는 보정각 - 생존단검/전술
마체테/전기톱검 전부 원본 그림의 칼끝이 우측이 아니라 좌상단 ~140~145도를 향하고 있어서 그
반대값을 더해야 한다, 2026-08-12 커밋 사유와 동일)를 더하고 있었다. 대기 분기만 이 보정을
빠뜨려서, 총기라면 문제없을 `rest_rotation_degrees`(총기 전용 대기각, 소켓0=8.112도/
소켓1=-3.233도 - 둘 다 절대각으로 "오른쪽 언저리"를 가리키도록 튜닝된 값)를 근접무기 그림에
그대로 적용하면 칼끝이 그림 원본 기울기(140도, 좌상단)만큼 어긋나 보였다 - "칼끝이 반대로
가있다"는 리포트와 정확히 일치한다.

수정: 대기 분기도 조준 분기와 같은 공식(`rest_rotation_degrees + (근접이면 weapon_imgangle,
아니면 0)`)을 쓰도록 통일했다.

```csharp
// 근접무기는 조준 중과 마찬가지로 weapon_imgangle을 더해야 한다 - 안 더하면 대기
// 자세가 총기 전용 기준(rest_rotation_degrees, 그림이 오른쪽을 향한다고 가정)으로만
// 돌아가서, 칼끝이 실제로 그려진 방향(~140도, 좌상단)이 그대로 드러나 반대로 향해
// 보인다(2026-08-19 "칼끝이 팔 방향과 반대로 가있다" 리포트로 발견).
float rest_target_angle = slot.rest_rotation_degrees + (is_melee ? weapon.weapon_imgangle : 0f);
float rest_angle = RotatePivotTowards(slot, weapon, pivot, rest_target_angle, slot_index);
```

**검증**(플레이모드 실측): 생존단검(무기ID 301101)을 소켓0(왼쪽, x=-2.75)과 소켓1(오른쪽,
x=+2.75)에 각각 장착하고 `screenshot-isolated`로 캡처. 수정 후 소켓1(오른쪽) 칼끝이 화면
오른쪽을, 소켓0(왼쪽) 칼끝이 화면 왼쪽을 향해 몸에서 바깥쪽으로 뻗는 것을 확인했다(수치로도
검증: 소켓1 회전각 0도 + imgangle(-140) = -140 → 그림 원본 기울기 140도를 더하면 최종 시각
방향 0도 = 오른쪽, 의도한 값과 일치).

**2. 이끼 낀 디스크(주기 회복) 미발동 버그.**
[DiscEffectRuntime.cs:170](Assets/Scripts/DiscEffectRuntime.cs:170) `UpdatePeriodicAndOscillating()`의
기존 코드:

```csharp
if (!periodic_next_time.TryGetValue(discId, out float next)) next = Time.time + disc.interval;
if (Time.time >= next) { ... periodic_next_time[discId] = Time.time + disc.interval; }
```

"다음 발동 시각"을 딕셔너리에서 못 찾으면 지역 변수 `next`에만 `Time.time + interval`을
계산해두고, **저장은 조건이 참일 때만** 했다. 그런데 조건 자체가 "지금 계산한 미래 시각을
지금 시각과 비교"하는 것이라 항상 거짓이고, 다음 프레임에도 딕셔너리에 키가 여전히 없으므로
**매 프레임 다시 "지금+5초"로 재계산**되는 무한 루프였다 - 자기 자신보다 5초 뒤인 값과는
영원히 비교에서 이길 수 없다. 결과적으로 이 조건은 단 한 번도 참이 될 수 없어 회복이 전혀
발동하지 않았다. `OscillatingAtkDef`(공명의 소리)는 우연히 초기값을 `Time.time` 그 자체로
잡아서(`next = Time.time`) 첫 프레임에 `Time.time >= next`가 항상 참이 되어 저장 분기를 타므로
같은 버그가 없었다 - 두 코드가 거의 같은 모양인데 초기값 하나 차이로 한쪽만 깨진 사례.

수정: 최초 계산 시점에 바로 딕셔너리에 저장한다.

```csharp
if (!periodic_next_time.TryGetValue(discId, out float next))
{
    next = Time.time + disc.interval;
    periodic_next_time[discId] = next;
}
if (Time.time >= next) { ... }
```

**나머지 20종 디스크 감사**(사용자 추가 요청) - `DiscEffectRuntime`/`ShopManager.EquipDisc`/
`PlayerShootManager`/`PlayerRobotController`/`EnemyUnit` 전체를 훑어 각 `DiscEffectType`의
소비처가 실제로 존재하고 계산이 설명과 일치하는지 확인했다. `OnKillChainLightning`/
`OnKillTempMoveAtkSpeed`/`OnKillTempDefDodge`/`OnKillHeal`/`OnKillStackStat`/
`OnAttackChanceBonusDamage`(`PlayerShootManager.ComputeDamage`가 호출)/`PassiveAuraSlow`
(`EnemyUnit.SetAuraSlowMultiplier`로 소비)/`CritChancePerDisc`/`LastStand`
(`PlayerRobotController.TakeDamage`가 호출)/`MoveSpeedWhenNotAttacking`/`OscillatingAtkDef`/
`WaveShieldMaxHp` 전부 소비처가 존재하고 로직도 설명과 일치함을 확인했다.
`SkillCooldownReduction`(은하수)은 필살기 시스템 자체가 데모 범위 밖이라 소비처가 없는 것이
**의도된 상태**(버그 아님, 헤더 주석에 이미 명시돼 있음)다. **버그는 PeriodicHeal 하나였다.**

**검증**(플레이모드 실측): 필드의 모든 `EnemyUnit`/`EnemyProjectile`을 파괴하고 스포너·웨이브
매니저를 비활성화한 뒤, 플레이어 체력을 50으로 낮추고 `RunState.EquippedDiscIds`에 이끼 낀
디스크(400009)를 직접 추가했다. 리플렉션으로 `DiscEffectRuntime`의 비공개 필드
`periodic_next_time`을 관찰한 결과, 최초 프레임에 `[400009]=213.0164`가 저장되고 이후
`Time.time`이 그 값을 넘기자 `[400009]=255.4426`(213.0164 + 5×N)으로 갱신된 것을 확인했다 -
수정 전 코드였다면 이 딕셔너리에 값이 영구히 채워지지 않았을 것이다. 실측 중 배경 전투로
플레이어가 반복적으로 사망해(테스트 씬이라 완전한 무피해 격리는 못 함) 순수 HP 증가량까지
깨끗하게 캡처하지는 못했지만, 타이머 자체가 정상적으로 재무장되는 것으로 로직 수정을 확정했다.

**3. 디스크 개명.** "나무 뭐시기 디스크"(discId 400008, 일반 등급, 이동속도 -5%/체력 +30) →
**"숲의 소리 디스크"**로 이름만 변경. [DiscTableGenerator.cs:111](Assets/Scripts/Editor/DiscTableGenerator.cs:111)
의 `Defs` 배열 항목과 아이콘 매칭 근거 주석을 고친 뒤 메뉴 `Comstock/디스크 테이블 21종
재생성`을 실행해 `ShopCatalog.asset`에 반영했다(효과·수치·아이콘은 그대로, 이름만 바뀜).
**검증**: 재생성 로그로 21종(등급별 4/5/6/4/2) 정상 재생성 확인, `ShopCatalog.asset`에서
discId 400008의 `discName`이 "숲의 소리 디스크"로 바뀐 것을 리플렉션으로 직접 조회해 확인.

**4. 머리 아트 2종 교체.** 2026-08-19 앞선 항목("머리 12종 전면 구현")에서 가드맨·메테우스는
전용 아트가 없어 기획서 PDF에서 추출한 임시 아트를 쓰고 있었다(`Assets/Resources/Heads/
Guardman.png`/`Meteus.png`). 사용자가 프로젝트 루트 `새머리/` 폴더에 정식 아트 2장(`가드맨.png`/
`메테우스.png`)을 올려둬 그대로 덮어썼다 - 둘 다 기존과 같은 250x250이라 리그 스케일/콜라이더/
무기 소켓을 건드릴 필요가 없었고, 파일명이 같아 GUID·머티리얼 참조도 그대로 유지된다.
**검증**: `Resources.Load<Sprite>("Heads/Guardman")`/`"Heads/Meteus"`가 각각 250x250으로 정상
로드되는 것을 확인. 프로젝트 안내.md의 "미구현" 목록에서 해당 항목을 제거했다.

**5. 골드 0 고정 버그 - 재현 실패.** [프로젝트 안내.md](프로젝트%20안내.md)의 "조사했으나 재현
실패" 항목 참고. `RunState.Reset()`(골드 포함 전체 초기화)은 `PlayerRobotController.Awake()`
에서만 호출되므로, 플레이모드에서 실제 프로덕션 코드 경로(골드 획득 → `GameOverManager.
TriggerGameOver()` → `SceneManager.LoadScene("Title")` → `SceneManager.LoadScene("Ground01")`
재시작 → 골드 재획득)를 **3회 반복**해 재현을 시도했으나 매번 골드가 정확히 0으로 리셋된 뒤
정상적으로(직접 호출·실제 트리거 접촉 둘 다) 누적됐다. 과거 "static 이벤트를 가진 매니저의
Reset()이 이벤트를 비워 구독이 끊기는" 3회 반복 패턴(프로젝트 안내.md "주의사항" 참고)이
원인이었을 가능성이 높다고 보는데, `RunState.OnChanged`/`GameOverManager.OnGameOver`/
`GameWinManager.OnGameWon`의 `Reset()`은 현재 전부 이벤트를 비우지 않도록 이미 고쳐져 있어
재현되지 않는 것으로 보인다. **사용자에게 재확인 요청**: 계속 재현되면 어느 화면(HUD/상점/정비)
에서 0으로 보이는지, 게임오버였는지 20웨이브 승리였는지, 타이틀↔인게임을 몇 번 오간 뒤인지
알려주면 그 조건을 그대로 재현해본다.

**커밋 상태**: 미커밋. (1)(3)(4)는 스크립트 2개 + 에셋 1개(`ShopCatalog.asset`) + 이미지 2장
수정, (2)는 스크립트 1개 수정.

### 골드 반올림 버그 원인 규명·수정 + 밸런스 3건 (2026-08-19)

사용자 요청 4건: (1) 골드가 오르지 않거나 일부 증가량이 반영되지 않는 버그가 자료형/내림·올림
처리 문제인지 **검증**할 것. (2) 라운드당 좀비 체력·공격력 증가량을 기존의 50%로 낮추고,
차저·부머 등의 스폰량을 줄이는 대신 일반 좀비 스폰량을 줄어든 비율의 2배 정도로 늘릴 것.
(3) 정비 화면에서 이미 장착된 파츠의 설명을 클릭해도 볼 수 없는 문제. (4) 총기 사거리를
전체적으로 15% 늘리고 AI 코어 사거리 업그레이드 증가량도 조금 더 늘릴 것.

**1. 골드 버그 - 정수 반올림이 원인임을 확정했다(이전 세션에서 재현 실패했던 그 버그).**

이전 세션에서 "타이틀 복귀 후 골드 0 고정"을 씬 재시작 경로로 3회 재현했으나 실패했었다.
이번에 계산식 자체를 실측한 결과 **재시작과 무관한 순수 산술 버그**였다.
[RewardPickup.cs](Assets/Scripts/RewardPickup.cs)의 골드 수령 코드:

```csharp
RunState.Gold += Mathf.RoundToInt(Amount * (1f + gold_gain_percent / 100f) * HeadEffects.GoldGainMultiplier);
```

`Amount`는 `Mathf.Max(1, RoundToInt(MaxHp / 34f))`라 **대부분의 몬스터가 골드 1~3**을 떨군다
(실측: 좀비 1 / 스피터 1 / 스프린터 2 / 디스럭터 2 / 차저 3 / 리더 10). 이 작은 값에 배율을
곱한 뒤 곧바로 정수로 반올림하니 배율이 통째로 사라졌다. `Mathf.RoundToInt`는 **은행가 반올림
(round-half-to-even)** 이라 0.5가 1이 아니라 **0**으로 내려가는 것이 결정타였다(실측 확인:
0.5→0 / 1.5→2 / 2.5→2 / 3.5→4).

- **미니 픽시 머리(골드 -50%)**: 골드 1짜리 픽업 → `RoundToInt(0.5)` = **0골드**.
  1~5웨이브는 기본 좀비(골드 1)만 나오므로 **골드가 문자 그대로 전혀 오르지 않는다** →
  사용자가 리포트한 "골드를 획득해도 0으로 고정" 증상과 정확히 일치.
- **금화의 잔향 디스크(+10%)**: 골드 1/2/3 → 1.1/2.2/3.3이 전부 원래 값으로 반올림되어
  **증가분이 완전히 소멸**(리더의 골드 10에서만 +1) → "일부 증가량이 반영 안 됨" 증상.

경험치 경로는 `Mathf.Max(1, Mathf.RoundToInt(Amount * ExpGainMultiplier))`로 **가드가 이미
있었는데 골드에만 없던 비대칭**이 직접 원인이다. 이 프로젝트는 같은 함정을 이미 한 번 겪어
2026-08-13에 웨이브 골드 +25%를 "금액이 아니라 **드랍 확률**에 곱하는" 방식으로 우회한 적이
있는데(정수 반올림 때문에 금액에 곱하면 변화가 없다고 그때 주석까지 남겼다), 디스크·머리
배율은 여전히 금액에 곱하고 있어 같은 함정이 남아 있었다.

**수정**: `Max(1,...)` 가드로 막으면 0은 피하지만 +10%는 여전히 소멸하고 -50%는 오히려
과지급된다. 그래서 **소수점 나머지를 다음 획득으로 이월하는 누적기**를 신설했다
([RunState.AddGoldWithFraction](Assets/Scripts/RunState.cs)) - `Gold`는 정수로 두고
`goldFraction`(float)에 나머지를 쌓아 정수부만 떼어 준다(반올림이 아니라 **내림**이어야
이월이 정확하다). 기대값이 정확히 보존된다. `RunState.Reset()`에서 `goldFraction`도 함께
비워 이전 판의 나머지가 다음 판으로 새지 않게 했다.

**검증**(플레이모드 실측, 누적기 단위 + 실제 `RewardPickup.CollectImmediately()` 경로 양쪽):

| 조건 | 수정 전 | 수정 후 | 기대 |
|---|---|---|---|
| 미니픽시 -50%, 골드1 픽업 x10 | 0 | **5** | 5 |
| 잔향 +10%, 골드1 픽업 x10 | 10 | **11** | 11 |
| 잔향 +10%, 골드3 픽업 x7 | 21 | **23** | 23 |
| 배율 없음, 골드1 픽업 x10 | 10 | **10** | 10 (회귀 없음) |
| Reset 직후 0.5 한 번 | - | **0** | 0 (나머지 미이월) |

실제 경로 검증은 `PlayerSession.SelectedRobotId`를 100013(미니 픽시)으로 바꾸고
`HeadEffects.resolved`를 false로 만들어 캐시를 다시 풀게 하는 방식으로 했다 - 캐시 필드만
강제하면 `Current` 프로퍼티가 `SelectedRobotId`와 대조해 즉시 되돌려버려서 배율이 1로
남는다(첫 시도에서 실제로 이 함정을 밟아 잘못된 통과를 볼 뻔했다).

**2. 웨이브 난이도 곡선 완화 + 스폰 구성 재조정.**
`EnemySpawner.statIncreasePerWave` 0.05 → **0.025**(기존의 50%). 웨이브 20 기준 배율이
x2.53 → **x1.60**으로 낮아진다.

스폰 구성은 **가중 추첨을 신설**했다. 예전에는 후보를 리스트에 나열하고 `Random.Range`로
**균등** 추첨해서, 상위 몬스터가 전부 해금되는 웨이브 12 이후 일반 좀비가 **1/6(16.7%)** 까지
밀려나 화면이 차저·부머로 가득 찼다. 이제 후보마다 비중을 함께 담아 뽑는다
(`spawn_weight_buffer` 신설 + `PickWeightedMonsterId()`). 기본 좀비 비중 **2**,
상위 몬스터 비중 **0.5**(= 기존 1에서 절반으로 감소, 그 절반의 2배를 좀비에 부여).

**검증**(웨이브 12, 6만 회 추첨 실측): 일반 좀비 **16.7% → 44.5%**, 상위 5종 각각
16.7% → 11.0~11.2%. 리더는 팔로워로 일반 좀비 3마리를 함께 스폰하므로 실제 좀비 비율은
이보다 더 높다. 모든 비중이 0이면 균등 추첨으로 폴백하는 안전망도 넣었다.

**3. 정비 화면 - 장착된 파츠 설명 조회.**
[ModdingPanelUI.cs](Assets/Scripts/ModdingPanelUI.cs)의 `HandleSlotClicked` /
`HandleWeaponSocketCellClicked`가 **오직 "교체"만** 담당하고 있었다 - 인벤토리에서 뭔가를
선택하지 않은 상태로 슬롯을 누르면 "먼저 인벤토리에서 장착할 파츠를 선택하세요" 안내만
내보내고 `return`해서, **이미 끼워둔 파츠의 능력치를 확인할 방법이 아예 없었다.**
(상점 화면에는 `EquipmentDetailPopup`이 있지만 정비 화면에는 그 경로가 없다.)

**수정**: "장착 파츠 조회 모드"를 신설했다(`inspectingEquipped` / `inspectedSlot` /
`inspectedWeaponSocket`). 인벤토리 선택이 **없을 때만** 슬롯 클릭이 조회로 동작하고, 선택이
있으면 기존 교체 흐름이 그대로 우선한다(교체를 방해하지 않는다). 같은 슬롯을 다시 누르면
조회가 풀리는 토글 규칙은 인벤토리 칸과 동일하게 맞췄다. 설명은 기존 설명 칸을 재사용하며
(`RefreshEquippedInspectionDetail()`), 위쪽에 `[장착 중] 슬롯명`을 노란색으로 붙이고
아래쪽 "교체 대상" 칸은 비교 대상이 없으므로 접는다. 인벤토리 선택·교체 완료·화면 열기/닫기
시점에는 `ClearEquippedInspection()`으로 조회 상태를 해제한다.

**검증**(플레이모드 실측): 부품 상자 8개를 넣고 정비 화면을 열어 자동 개봉 → 인벤토리[0]
"경량 부츠"를 다리 슬롯에 실제로 장착 → **인벤토리 선택이 없는 상태(index=-1)에서 다리 슬롯
클릭** → 설명 칸에 `[장착 중] 다리 / 일반 경량 부츠 / 다리 / 이동속도 +0.6 · 무게 1.2`와
아이콘이 정상 표시됨을 확인. 무기 소켓 경로도 소켓1 클릭 시 `[장착 중] 무기 소켓 1 /
일반 표준 소켓 / 사거리 x1 · 감지 x1 · 회전 x1`로 정상 동작. 같은 슬롯 재클릭 시 조회 해제,
인벤토리 선택 시 `inspectingEquipped=False`로 교체 흐름이 유지되는 것(회귀 없음)까지 확인했다.

**4. 총기 사거리 15% 상향 + AI 코어 사거리 증폭 상향.**
[WeaponTableGenerator.cs](Assets/Scripts/Editor/WeaponTableGenerator.cs)의 `Defs`에서
**원거리 10종(kind 1~10)의 `range`/`detect`에 x1.15**를 적용하고 메뉴
`Comstock/무기 테이블 65행 재생성` 실행. **근접 3종(kind 11~13)은 제외** - 이 값들은 화면이
아니라 "적 몸통에 닿는 거리"에 종속되고 타격 판정은 칼 스프라이트 bounds가 정하므로 늘려도
의미가 없다(기존 환산 규칙과 동일한 사유).

**중요 - 감지거리 상한에 잘리는 것을 함께 막았다**: `PlayerShootManager.max_detect_range`가
5.3인데 상향 후 대물저격총의 감지거리가 **5.98**이 되어 상한에 걸렸다. 그대로 뒀으면 상향분이
**조용히 잘려나갈 뻔했다**(이 프로젝트가 반복해서 겪은 "silent clipping" 계열). 상한도 같은
비율로 5.3 → **6.1**로 올렸다. 가로 가시 반경이 9.6이라 대부분의 교전은 여전히 화면 안이다.

AI 코어 업그레이드의 `WeaponRangeBonus` 증가량은 **0.5 → 1.5**(`AiCoreUpgradePool.asset`).
등급 배율(1/1.67/2.33/3.33/5)을 곱하면 일반 +1.5% ~ 전설 +7.5%가 된다(예전 +0.5% ~ +2.5%).
다른 스탯 옵션(공격력 0.6 = 기본 공격력 3의 20%)에 비해 사거리만 유독 체감이 없던 것을
맞춘 것이다.

**검증**: 무기 테이블 재생성 65행 + `Comstock/무기 테이블 검증` **8개 검사 전부 통과**.
플레이모드에서 `GetTravelRange`/`GetDetectRange`를 리플렉션 직접 호출해 일반 등급 13종의
실제 적용값을 확인 - 중기관총 사거리 5.23·감지 4.49, 대물저격총 11.21·5.98, 지정사수소총
7.48·5.23 등 원거리 10종이 정확히 15% 오르고 **상한 6.1에 잘리는 무기가 하나도 없음**을 확인,
근접 3종(1.54/1.76/1.65)은 변동 없음을 확인했다.

**씬 직렬화 값 동기화**: `statIncreasePerWave`(0.05→0.025)와 `max_detect_range`(5.3→6.1)는
**씬(`Ground01.unity`)에도 값이 박혀 있어 코드 기본값만 고치면 무시된다** - 두 값 모두
`SerializedObject`로 씬에 직접 반영하고 씬을 저장했다(이 프로젝트의 기존 관례).
신규 필드 2개(`basicMonsterSpawnWeight`=2 / `advancedMonsterSpawnWeight`=0.5)는 씬에 없던
필드라 코드 기본값이 그대로 적용됨을 확인했다.

**커밋 상태**: 미커밋. 스크립트 5개(`RunState`/`RewardPickup`/`EnemySpawner`/`ModdingPanelUI`/
`PlayerShootManager`) + 에디터 1개(`WeaponTableGenerator`) + 에셋 3개(`GameDataAsset`·
`PartsCatalog`·`ShopCatalog`는 무기 테이블 재생성으로, `AiCoreUpgradePool`) + 씬 1개(`Ground01`).

### 동시 생존 상한에 막혀도 3초에 1마리는 계속 스폰 (2026-08-19)

사용자 요청: "좀비 예상 스폰량이 모두 스폰되더라도 3초에 1마리씩은 계속 생성되게".

**문제**: `EnemySpawner.SpawnLoop()`의 배치 루프가
`if (maxAliveEnemies > 0 && alive_enemies.Count >= maxAliveEnemies) break;`로 상한에 걸리면
**스폰이 완전히 멈춘다.** 현재 상한이 18이라 필드가 포화되면 플레이어가 적을 줄이기 전까지
새 적이 하나도 나오지 않아, 전투 압박이 뚝 끊기는 구간이 생겼다.

**수정**: `trickleSpawnInterval`(기본 3초) 신설 + **별도 코루틴** `TrickleSpawnLoop()` 추가.
상한에 도달한 동안에도 이 간격마다 1마리를 계속 내보낸다.

- **별도 코루틴으로 뺀 이유**: `SpawnLoop` 안에 넣으면 주기가 `spawnInterval`에 끌려간다 -
  이 값은 `WaveManager.ConfigureDifficulty()`가 웨이브마다 바꾸고(초반 완충 구간에서는 1.6배)
  씬 값이 1초라, 같은 루프에 두면 "3초에 1마리"가 웨이브마다 흔들린다.
- **가중 추첨을 쓰지 않고 `spawnMonsterIds`(기본 좀비)에서만 고른다**: 이 경로는 상한을
  무시하는 유일한 정규 경로라(리더의 팔로워 스폰과 같은 예외), 상위 몬스터가 뽑히면 차저·부머가
  상한 위로 쌓인다. 특히 **리더가 뽑히면 팔로워 3마리까지 함께 상한을 우회해 한 번에 4마리**가
  추가된다. 사용자 요청도 "좀비 1마리씩"이라 기본 좀비로 한정했다.
- 게임오버/승리, 정비·상점 중(`IsSpawningEnabled=false`)에는 기존 루프와 동일하게 멈춘다.
- 상한을 무시하지만 웨이브 길이가 유한해 무한정 쌓이지 않는다(60초 웨이브 기준 최대 20마리).

**검증**(플레이모드 실측): 플레이어를 무적으로 만들고(`lastStandInvulnUntil`을 리플렉션으로
+99999초 - 기존 "마지막 발악" 무적 구간 로직을 그대로 활용), `PlayerShootManager`를 꺼 적이
죽지 않게 하고, `WaveManager`를 꺼 웨이브가 끝나지 않게 한 뒤, `maxAliveEnemies`를 3으로 낮춰
**정규 스폰이 완전히 막힌 포화 상태**를 만들었다.

| 시각 t | 생존 수 | 경과 | 증가 | 기대(경과/3) |
|---|---|---|---|---|
| 103.67 | 9 | - | - | - |
| 118.56 | 14 | 14.89초 | +5 | 4.96 |
| 129.64 | 18 | 11.08초 | +4 | 3.69 |
| **누계** | **9 → 18** | **25.97초** | **+9** | **8.66** |

**정확히 3초당 1마리**로 일치했고, 상한 3을 훨씬 넘겨 18까지 계속 늘어나 상한을 실제로
우회하는 것을 확인했다. 스폰된 18마리가 **전부 순수 `EnemyUnit`(일반 좀비)** 이라
차저·부머·리더가 상한 위로 새지 않는 것도 함께 확인했다. 정비 진입을 재현하면
(`SetSpawningEnabled(false)`) 트리클이 멈추고, 이때 `Time.timeScale=0`이라 `WaitForSeconds`
자체가 진행되지 않아 **이중으로 안전**하다.

**측정 중 겪은 함정**: 처음에는 플레이어 `CurrentHp`만 크게 설정했는데, `RunState.OnChanged`로
스탯이 재계산될 때 `CurrentHp = Mathf.Min(MaxHp, ...)`로 다시 깎여 플레이어가 죽었고
(`GameOver=True`) 두 스폰 루프가 전부 `continue`로 빠져 "트리클이 동작하지 않는 것처럼" 보였다.
무적 처리를 `lastStandInvulnUntil`로 바꾼 뒤에야 깨끗한 측정이 됐다.

**커밋 상태**: 미커밋. 스크립트 1개(`EnemySpawner`)만 수정했고 씬 변경은 없다
(`trickleSpawnInterval`은 신규 필드라 씬에 직렬화된 값이 없어 코드 기본값 3초가 그대로 적용된다 -
플레이모드에서 런타임 값이 3인 것을 확인했다).

## 2026-08-19 밸런스 4건 + 점수·엔드리스·악세사리·해금 구현 계획 `[계획]`

사용자가 기획서 4종(`20260818_해금기획서_Ver01_김재원.pdf`(26쪽), `20260818_악세서리기획서_Ver03_김재원.pdf`(9쪽),
`20260819_Endless모드기획서_Ver01_오승명.pdf`(9쪽), `20260814_디스크기획서_Ver01_김재원.pdf`(15쪽))과
프로젝트 루트 `악세사리/` 폴더(신규 아트 6장, 전부 128x128 RGBA)를 제공하고 7개 항목을 요청했다.

**기획서 텍스트 추출 방법**: 네 PDF 모두 폰트 인코딩이 깨져 `pdftotext`/PyMuPDF의 `get_text()`가
한글을 대부분 유니코드 쓰레기로 뱉는다(직접 입력한 텍스트 박스 일부만 정상 추출됨).
**PyMuPDF로 페이지를 PNG(dpi=110)로 렌더해 이미지로 읽는 것이 유일하게 확실한 경로다.**
다음에 이 팀 기획서를 읽을 때도 같은 방법을 쓸 것.

### 착수 전 조사에서 확인한 사실

- **점수(Score) 시스템이 프로젝트에 아예 없다.** `Assets/Scripts` 전체에 `Score`/`점수` 식별자가 0건이다.
  그런데 악세사리 기획서는 "별도의 효과를 부여하지 않고 구매량에 따라 점수 보너스 지급 / 엔드리스 모드
  클리어 시 랭킹이 매겨지는 점수에 가산"이고, 엔드리스 기획서도 "20웨이브 클리어 → 결과 화면"을
  전제한다. 즉 **점수 시스템이 악세사리·엔드리스의 선행 조건**이다.
- **의존 순서가 강제된다**: 악세사리는 "엔드리스 모드 진입 이후에만 상점 등장"이고, 해금 기획서의
  악세사리 해금 조건이 **전부 "엔드리스 모드에서 OO"** 이다. 따라서 만들 수 있는 순서는
  `밸런스 수정 → 점수 → 엔드리스 → 악세사리 → 해금(도감)` 뿐이다. 악세사리·해금을 먼저 "적용"할 수 없다.
- **아웃게임 영속 저장 경로가 없다.** `PlayerPrefs`는 음악/효과음 볼륨과 화면 모드에만 쓰인다
  (`MusicManager`/`SFXManager`/`SettingsPanelUI`). 해금 진행도는 새 저장 계층이 필요하다.
- **승리 화면은 텍스트 오브젝트 하나뿐이다.** `GameHUD.victoryObject`를 `SetActive(true)` 하는 게 전부이고
  ([GameHUD.cs:255](Assets/Scripts/GameHUD.cs:255)), 게임오버에만 `GameOverSummaryUI`(도달 웨이브·골드·
  파츠·무기·디스크·최종 스탯 + "타이틀로" 버튼)가 붙어 있다. 승리 쪽 정산 화면은 새로 만들어야 한다.
- **엔드리스 무한 루프의 걸림돌은 `WaveManager.finalWaveNumber` 하드 분기다.** `BeginWave()`가
  `wave == finalWaveNumber`일 때만 보스를 스폰하고, `WaveTimer()`가 같은 조건에서 `EndWave()`(정비/상점)
  대신 `WinGame()`으로 빠진다. 엔드리스에서는 이 두 분기를 "사이클 기준"으로 바꿔야 한다.
- **적 스탯 상승 곡선은 이미 있다**: `EnemySpawner.CurrentStatMultiplier = 1.025^(wave-1)`. 엔드리스
  기획서의 "웨이브가 오를수록 적 스탯 상승[체력·공격력·방어력]"은 이 값이 방어력에도 적용되는지만
  확인하면 된다.
- **몬스터 해금 웨이브가 1~19 사이클과 잘 맞는다**: 스피터 3 / 차저 4 / 스프린터 5 / 디스럭터 8 / 리더 10
  (`EnemySpawner.MonsterUnlockEntry`). 엔드리스 기획서의 "웨이브 21 = 웨이브 1과 동일 구성"은
  `SetCurrentWave()`에 넘기는 값을 사이클 내 웨이브 번호로 바꾸면 그대로 성립한다.
- **머리 12종·디스크 21종이 이미 데이터로 존재하고 이름이 해금 기획서와 1:1 대응한다.** 해금은 새
  데이터를 만드는 게 아니라 기존 목록에 "해금 여부 + 조건"을 얹는 작업이다.
- **맵은 도시 폐허 1종뿐이다.** 해금 기획서의 설원 지대·용암 지대 배경 아트가 프로젝트에 없다.

### 사용자 확정 사항 (착수 전 질의)

1. **진행 순서**: 밸런스 항목을 먼저 끝내고 검증한 뒤, 점수 → 엔드리스 → 악세사리 → 해금을 순차 구현한다.
2. **경험치 커브**: 구간 배율 **x2 / x4 / x8**(완만) 안을 채택. 1~14레벨은 현행 유지.
3. **맵 해금은 제외**. 아트가 없어서다. 도감 데이터 구조만 확장 가능하게 열어두고 아트가 오면 추가한다.
4. **디스크 기획서는 참고용이며 적용 대상이 아니다.** 21종을 신 기획서 수치로 갈아끼우지 않는다.
   요청의 진의는 "**영구 누적형 디스크의 누적 수치가 제대로 적용되지 않는 문제를 고쳐라**"였다.

### 검증 결과 (요청 4·5·6번) — 수정 전 실측

#### 1. 고등급 확률의 웨이브 스케일링 — 무기·AI코어·부품상자는 정상, **디스크만 버그**

`ShopCatalog.RollGrade()` / `AiCoreUpgradePool.RollGrade()` / `PartsCatalog.RollBoxGrade()` 세 곳은 전부
**가중치 고정(50/26/14/7/3) + `minWave` 게이팅**이다. 에셋 직렬화 값도 코드 기본값과 같다
(`AiCoreUpgradePool.asset`의 레어 minWave만 1 → 2로 다름). 웨이브가 올라도 가중치는 변하지 않으므로
"웨이브마다 확률이 증가"하지는 않는다 — 등장 가능 등급만 1/2/3/5/7웨이브에 하나씩 열린다.

**문제는 디스크다.** [ShopManager.CreateDiscOffer()](Assets/Scripts/ShopManager.cs:343)가 `RollGrade()`를
**아예 호출하지 않고**, `minWave`를 만족하는 디스크 전체에서 **균등 추첨**한다. `ShopCatalog.asset`의
21종 등급 분포가 일반 4 / 레어 5 / 에픽 6 / 유니크 4 / 전설 2라서 실효 확률이 이렇게 된다
(에셋에서 직접 집계):

| 웨이브 | 일반 | 레어 | 에픽 | 유니크 | 전설 | 에픽 이상 |
|---|---|---|---|---|---|---|
| 1~2 | 44.4% | 55.6% | - | - | - | 0% |
| 3~4 | 26.7% | 33.3% | 40.0% | - | - | **40.0%** |
| 5~6 | 21.1% | 26.3% | 31.6% | 21.1% | - | **52.7%** |
| 7+ | 19.0% | 23.8% | 28.6% | 19.0% | 9.5% | **57.1%** |
| **의도값** | 50% | 26% | 14% | 7% | 3% | **24%** |

전설 디스크가 의도 3% → 실제 **9.5%**(3.2배), 유니크가 7% → **19%**(2.7배)다. 게다가 등급이 하나씩
열릴 때마다 균등 분모가 커져 **고등급 비중이 웨이브 3·5·7에서 계단식으로 크게 뛴다** — 사용자가
"웨이브가 오를수록 고등급이 잘 나오고 증가량이 너무 높다"고 느낀 정체가 이것이다.

#### 2. 경험치 요구량이 너무 낮다 — 확인

[AiCoreManager.RequiredExpForNextLevel()](Assets/Scripts/AiCoreManager.cs:63) `= 4 + 2 * 레벨`
(2026-08-13에 `50+20*N`에서 계수 1/12로 낮춘 값). 누적은 `(L-1)(L+4)`이므로
**15레벨 266 / 30레벨 986 / 45레벨 2156 / 50레벨(최대) 2646**이다.
좀비(체력 40) 처치 경험치는 `Max(1, 40/17)` = **2**(정수 나눗셈)에 드랍 확률 0.5 → **처치당 평균 1**.
웨이브당 수십~100마리이므로 20웨이브 안에 최대 레벨에 여유롭게 도달한다.

#### 3. 영구 누적형 디스크 — **초기화 버그는 없다. 상한이 너무 낮은 것이 진짜 원인이다** `[실측 확정]`

`Ground01`을 **일시정지 상태로 플레이모드 진입**시킨 뒤(작업.md에 기록된 디버깅 기법), 암석(400007
방어력) / 바람 소리(400012 이동속도) / 금속음(400015 최대체력) 3종을 장착하고
`DiscEffectRuntime.HandleEnemyKilled`를 리플렉션으로 반복 호출해 처치를 재현했다. 이어서
`GameFlowManager.HandleWaveEnded(1)` → `HandleNextWaveRequested()`로 웨이브 전환까지 그대로 통과시켰다.
`EnemyUnit.OnKilledByPlayer`의 구독자 수는 1개(정상 연결)였다.

| 시점 | 암석 진행도 | 바람 진행도 | 금속 진행도 | 실제 방어력 | 실제 이동속도 | 실제 최대체력 |
|---|---|---|---|---|---|---|
| 장착 직후 | 0.000 | 0.0000 | 0.00 | 5 | 3.000 | 100 |
| 처치 60회 후 | 5.000 | 0.1250 | 10.00 | 10 | 3.125 | 110 |
| 웨이브 종료(정비 진입) 직후 | 5.000 | 0.1250 | 10.00 | 10 | 3.125 | 110 |
| 다음 웨이브 재개 직후 | 5.000 | 0.1250 | 10.00 | 10 | 3.125 | 110 |
| 추가 처치 20회 후 | 5.000 | 0.1250 | 10.00 | 10 | 3.125 | 110 |

**누적값은 웨이브 전환을 넘어 정확히 유지된다.** 코드에서도 `RunState.DiscStackProgress`를 비우는 곳은
씬 진입 시 `RunState.Reset()`(`PlayerRobotController.Awake`) 하나뿐이다. 에디트 모드에서 `ApplyKillStack`의
산술만 100회 재현한 별도 검증에서도 50회에 cap 5에 도달해 이후 변화가 없는 것을 확인했다.

**진짜 결함은 네 가지다.**

| 디스크 | 처치당 증가 | 현재 상한 | 상한 도달 | 최종 효과 |
|---|---|---|---|---|
| 교향곡: 암석 | 방어력 0.1 | 5 | **50마리** | 방어력 5 → 10 |
| 교향곡: 화염 | 공격력 0.1 | 5 | **50마리** | 공격력 3 → 8 |
| 바람 소리 | 이동속도 0.0025 | 0.125 | **50마리** | 이동속도 3.0 → 3.125 (+4%) |
| 금속음 | 최대체력 1 | 10 | **10마리** | 체력 100 → 110 |

- **(A) 상한이 웨이브 1의 첫 1분 안에 다 찬다.** 네 종 모두 10~50마리에 상한에 닿고 **남은 19웨이브
  동안 단 한 번도 더 오르지 않는다.** 플레이어 관점에서는 "누적이라고 적혀 있는데 값이 안 늘어난다
  = 적용이 안 되거나 초기화됐다"와 구분이 불가능하다.
- **(B) 방어력·공격력은 int라 초반 증가분이 화면에 아예 안 보인다.** `RobotStats.ApplyBonus`가
  스탯별 총합을 `Mathf.RoundToInt`하므로 누적 0.1~0.5(1~5마리)는 전부 0으로 깎인다
  (0.5는 은행가 반올림으로도 0). **처음 5마리는 아무 일도 일어나지 않는다.**
- **(C) 설명 문구가 처치당 값만 보여준다.** `effectDescription`은 "적을 처치하면 방어력이 0.1
  증가합니다 (최대 5)"라는 고정 문자열이라, 현재 누적치가 어디까지 찼는지 알 수 없다.
- **(D) `cap`이 장 수를 곱하지 않는다.** `RunState.DiscStackProgress` 주석은 "같은 종류 디스크를 두 장
  장착해도 각자 독립적으로 cap까지 쌓여야 한다"고 적혀 있는데, `ApplyKillStack`은 공유 총합을
  `Mathf.Min(disc.cap, ...)`로 막는다. 2장 사면 **도달 속도만 2배가 되고 최종치는 1장과 같다.**

#### 4. 근접무기 대기 자세 좌/우 방향 — 2026-08-19 수정이 여전히 듣지 않는다는 재리포트

사용자 재리포트: "절대적인 기준으로 왼쪽에 장착된 칼은 공격 후 칼끝이 화면 왼쪽을 향하고 오른쪽에
있는 칼은 오른쪽을 향해야 하는데 아직도 적용이 안 됨".

같은 날 앞선 수정에서 `MeleeRestFacingDegrees(slot_index)`를 신설해 왼쪽 소켓 `180 - tilt`(165도) /
오른쪽 소켓 `tilt`(15도)를 넣었는데도 재현된다. 코드를 다시 훑어 나온 후보 원인 3개:

- **(a) 좌/우 판정 기준이 틀렸을 가능성** — `CacheSocketSides()`가
  [PlayerShootManager.cs:265](Assets/Scripts/PlayerShootManager.cs:265)에서 `rig_point.localPosition.x < 0f`로
  판정한다. 이 값이 **화면 절대 좌우와 일치하는지 실측하지 않았다.** 소켓 Transform의 부모 체인에
  음수 스케일이 끼거나(리그가 좌우 반전을 `LegsGroup.localScale.x` 부호로 처리한다) 캐릭터가
  왼쪽을 볼 때 소켓이 함께 반전되면, 로컬 x 부호와 화면 좌우가 어긋난다.
- **(b) 대기 자세가 "타겟이 없을 때"만 적용된다** — `UpdateSlot()`은 `target == null`인 경로에서만
  대기 각도를 쓴다. 사용자가 말한 "**공격 후**"는 적이 아직 감지거리 안에 남아 있는 시점일 가능성이
  높고, 그때는 대기 자세가 아니라 **그 적을 조준한 방향**이 나온다. 즉 수정은 들어갔지만 사용자가
  보는 타이밍에는 애초에 적용되지 않는 코드 경로일 수 있다.
- **(c) 찌르기 연출 복귀 직후의 각도** — `StartMeleeThrustVisual`이 소켓 Transform을 실제로 밀어내고,
  복귀할 때까지 `melee_thrust_active`로 조준을 조기 반환한다. 복귀 프레임에 회전이 어떤 값으로
  남는지 확인이 필요하다.

**(b)가 사실이면 "대기 자세" 수정만으로는 리포트를 절대 해결할 수 없다** — 이 경우 요구사항을
"타겟이 없을 때"가 아니라 "**찌르기 동작이 끝난 뒤 다음 조준까지**"로 재해석해 적용 지점을 옮겨야 한다.
따라서 수정 전에 **플레이모드 실측으로 (a)(b)(c)를 먼저 확정**한다(아래 A-4).

### 작업 단계

#### Phase A — 밸런스·버그 4건 (선행)

- **A-1. 디스크 상점 등급 추첨을 무기와 동일한 규칙으로 교체.** `ShopManager.CreateDiscOffer()`가
  `catalog.RollGrade(wave)`로 등급을 먼저 뽑고 **그 등급의 디스크 중에서** 추첨하도록 바꾼다. 해당
  등급에 디스크가 없으면 `CreateWeaponOffer()`/`TryRollLootPart()`와 같이 **하위 등급으로 내려가며**
  찾는다(상위로 올라가면 `minWave` 게이팅이 다시 깨진다 - `PartsCatalog`에서 실제로 겪은 함정).
  기대 결과: 일반 50 / 레어 26 / 에픽 14 / 유니크 7 / 전설 3%.
- **A-2. 경험치 커브에 구간 배율 도입.** `AiCoreManager`에 `[SerializeField]` 구간표를 신설하고
  `필요경험치 = (expBase + expPerLevel * L) * 구간배율(L)`로 바꾼다. 배율은 **1~14 x1 / 15~29 x2 /
  30~44 x4 / 45+ x8**. 누적 예상치:

  | 도달 레벨 | 현재 | 변경 후 | 배수 |
  |---|---|---|---|
  | 15 | 266 | 266 | x1.0 |
  | 30 | 986 | 1,706 | x1.7 |
  | 45 | 2,156 | 6,386 | x3.0 |
  | 50 | 2,646 | 10,306 | x3.9 |

  주의: **`expBase`/`expPerLevel`은 씬(`Ground01.unity`)에 직렬화돼 있을 수 있으므로 코드 기본값만
  고치면 무시된다** - 이 프로젝트가 반복해서 겪은 함정이라 씬 값을 실측 확인하고 필요하면
  `SerializedObject`로 함께 반영한다. 신규 구간표 필드는 씬에 없으므로 코드 기본값이 그대로 적용된다.
- **A-3. 영구 누적형 디스크 4종 수정.** (디스크 기획서 수치를 적용하는 것이 아니라, 현재 설계를
  유지한 채 위 (A)~(D)를 고친다.)
  - **(A) 상한 상향 + 처치당 증가량 재조정 — 기준: "약 200마리 처치에 상한 도달".** 20웨이브 런의
    중반(웨이브 8~12)에 상한에 닿게 만드는 값이다.

    | 디스크 | 처치당 (현재→변경) | 상한 (현재→변경) | 상한에서의 효과 |
    |---|---|---|---|
    | 교향곡: 암석 | 0.1 → **0.05** | 5 → **10** | 방어력 5 → 15 (x3) |
    | 교향곡: 화염 | 0.1 → **0.05** | 5 → **10** | 공격력 3 → 13 |
    | 바람 소리 | 0.0025 → **0.003** | 0.125 → **0.6** | 이동속도 3.0 → 3.6 (+20%) |
    | 금속음 | 1 → **0.2** | 10 → **40** | 최대체력 100 → 140 (+40%) |

    바람 소리의 +20%는 디스크 기획서의 "누적 최대 20%"와 일치시킨 값이다(수치를 적용한 것이 아니라
    상한의 근거로만 참고). 전부 `DiscTableGenerator.Defs`의 임시값이므로 플레이 후 재조정한다.
  - **(B) int 반올림으로 증가분이 묻히는 문제**: 처치당 값이 0.05로 더 작아지므로 방어력·공격력은
    20마리마다 +1씩 눈에 보이게 오른다. `RobotStats.ApplyBonus`의 반올림 구조 자체는 건드리지 않는다
    (AI 코어 보너스도 같은 경로로 소수 누적 후 한 번만 반올림하는 것이 의도된 설계다).
  - **(C) 설명 문구를 실시간 진행도로 교체**: `DiscData.BuildDescription()`이 `OnKillStackStat`일 때
    `RunState.DiscStackProgress`를 읽어 `"적을 처치하면 방어력이 0.05 증가합니다 (현재 +3.20 / 최대 10)"`
    형태로 만들게 한다. **고정 문구를 데이터에 두면 실제 값과 어긋나는** 함정을 이미 AI 코어 3택
    카드(2026-08-13)와 머리 효과 설명(2026-08-19)에서 겪었으므로 같은 패턴을 따른다.
    상점 카드·장비 상세 팝업(`EquipmentDetailPopup`) 양쪽에 반영된다.
  - **(D) `cap`에 장 수 반영**: `ApplyKillStack`의 `Mathf.Min(disc.cap, ...)`을 `disc.cap * copies`로
    바꿔 주석에 적힌 의도(장마다 독립적으로 cap까지)와 실제 동작을 일치시킨다.
- **A-4. 근접무기 좌/우 칼끝 방향 재수정.** 먼저 **플레이모드 실측으로 원인을 확정**한다:
  (1) 6개 소켓의 `rig_point.localPosition.x` / `position.x`(월드) / 부모 체인의 `lossyScale.x` 부호를
  모두 읽어 **로컬 x 부호가 화면 절대 좌우와 일치하는지** 검증한다.
  (2) 근접무기 3종의 `weapon_imgangle`과 실제 렌더된 칼끝 각도를 대조해 `pivot 회전 - imgangle =
  화면 각도` 관계를 실측으로 확인한다.
  (3) 적을 살려둔 상태와 전멸 상태 양쪽에서 칼 각도를 캡처해 **사용자가 보는 "공격 후" 시점이
  `target == null` 경로인지 조준 경로인지** 확정한다.
  확정 결과에 따라 (a)면 좌/우 판정 기준을 월드 기준으로 교체, (b)면 대기 자세 적용 조건을
  "찌르기 종료 후 다음 발사까지"로 확장, (c)면 복귀 프레임의 회전 리셋을 추가한다.
  총기 경로는 이번에도 건드리지 않는다.

#### Phase B — 점수(Score) 시스템 (신규)

악세사리·엔드리스의 선행 조건. `RunState`와 같은 static 클래스 패턴으로 신규 `RunScore`를 만든다.

- **공식(임시값)**: `도달 웨이브 x 1,000 + 누적 처치 수 x 10 + 최종 AI 코어 레벨 x 200 + 남은 골드 x 1
  + 악세사리 점수 합계`. 20웨이브 클리어 기준 약 45,000점이 되도록 잡은 값이다
  (웨이브 20,000 + 처치 약 15,000 + 레벨 9,000 + 골드).
- 누적 처치 수는 지금 어디에도 집계되지 않으므로 `RunScore`가 `EnemyUnit.OnKilledByPlayer`를 구독해
  직접 센다(해금 조건 "적 N마리 처치"도 이 카운터를 공유한다).
- 인게임 HUD 상단에 현재 점수를 표시한다(`GameHUD`).

#### Phase C — 엔드리스 모드

- **정산 팝업 신설**: 20웨이브 보스 처치 → `GameWinManager.OnGameWon` → **점수 정산 팝업**
  (점수 내역 항목별 + 총점) → `계속 진행` / `타이틀로` 2택. `GameOverSummaryUI`의 조회 패턴을 재사용한다.
- **`계속 진행` 시 엔드리스 진입**: `RunState`에 `IsEndless` 플래그를 신설하고,
  `WaveManager`의 `wave == finalWaveNumber` 하드 분기 2곳을 **사이클 기준**으로 바꾼다.
  - 웨이브 반복: 21 = 1, 39 = 19 구성. `EnemySpawner.SetCurrentWave()`에 넘기는 값을
    `((wave - 1) % 19) + 1`(사이클 내 번호)로 바꿔 좀비 조합·등장 순서를 그대로 반복시킨다.
  - 적 스탯 상승은 **누적 웨이브 번호**를 그대로 쓴다(`1.025^(wave-1)`) - 사이클마다 강해지는 것이
    엔드리스의 난이도 곡선이다. 방어력에도 배율이 적용되는지 확인해서 빠져 있으면 추가한다.
  - 보스는 사이클 마지막(20, 39, 58 ...)에 스폰하되, 처치해도 `WinGame()`으로 빠지지 않고
    `EndWave()`(정비/상점)로 이어지게 한다.
- **종료 조건**: 플레이어 사망 / 일시정지 메뉴의 `나가기`. 둘 다 점수 정산 화면으로 간다.
- 상점·정비 HUD의 `WAVE NN / 20` 표기를 엔드리스에서는 `WAVE 무한`으로 바꾼다(악세사리 기획서 목업 기준).

#### Phase D — 악세사리 6종

`악세사리/` 폴더의 6장을 `Assets/Resources/Accessories/`로 임포트한다(전부 128x128 RGBA,
`Parts/Body.png`와 같은 설정: spriteMode Single, PPU 100, pivot 중앙).

| 이름 | 기획서 점수 | 기획서 금액 | **적용 금액** | 부착 위치 | 파일 |
|---|---|---|---|---|---|
| 8비트 선글라스 | 200 | 100 | **50** | 얼굴 | `8Bitsunglass-transparent.png` |
| 왕관 | 500 | 200 | **100** | 머리 위 (스택) | `Crown-transparent.png` |
| 합격 목걸이 | 1000 | 300 | **150** | 목 | `Passneck-transparent.png` |
| 유니콘 뿔 | 400 | 200 | **100** | 머리 위 (스택) | `Unicon-transparent.png` |
| 조이스틱 | 200 | 100 | **50** | 머리 위 (스택) | `Joystick-transparent.png` |
| 의문의 검은 고양이 귀 | 444 | 150 | **75** | 머리 위 (스택) | `Kkami-transparent.png` |

- **금액은 기획서의 절반**으로 낮췄다(사용자 지정 "너무 비싸면 안 된다"). 기획서 100~300골드는
  무기 한 정 값(45~134 x 등급배율)과 맞먹어 엔드리스 상점에서 살 이유가 사라진다. 점수/골드 효율은
  4~6.7점/골드 범위로 목걸이가 가장 좋게 유지했다(고급 아이템 컨셉).
- **점수는 기획서 값 그대로.** 20웨이브 클리어 총점 약 45,000점 대비 하나가 0.4~2.2%이므로,
  여러 개를 겹쳐 사는 것이 유의미하면서 점수를 지배하지는 않는다.
- **엔드리스 모드에서만, 상점 4칸 각각 5% 확률로 등장**(사용자 지정). 무기/디스크 추첨보다 먼저
  굴려서 당첨된 칸만 악세사리로 대체한다.
- **중복 구매 가능하고 머리 위로 하나씩 쌓인다**(사용자 지정: 뿔 위에 왕관 위에 뿔...). 구매 순서를
  `RunState`에 리스트로 보관하고, `ProceduralCharacterRig`의 몸통(=머리) 위쪽 기준점에서 위로
  차례차례 쌓아 올린다. 얼굴(선글라스)·목(목걸이)은 스택이 아니라 고정 위치에 겹쳐 그린다.
  주의: `ProceduralCharacterRig.RestorePartSprites()`가 매 프레임 몸통 스프라이트를 강제 복구하므로
  (2026-08-19 네온아이 색 순환에서 겪은 함정) 악세사리는 **별도 자식 오브젝트**로 붙인다.
- 효과는 없다(기획서: "별도의 효과를 부여하지 않고 구매량에 따라 점수 보너스 지급").

#### Phase E — 해금(도감)

- **범위**: 머리 12종 + 디스크 21종 + 악세사리 6종. **맵은 제외**(사용자 확정, 아트 부재).
- **저장**: `PlayerPrefs` 기반 신규 `UnlockState`(볼륨 저장과 같은 패턴). 해금 진행도(처치 수, 보스 처치
  횟수, 도달 스탯, 누적 레벨, 누적 디스크 구매 수 등)와 해금 여부를 함께 보관한다.
- **초기 해금**: 머리는 컴스톡만, 디스크는 기획서에서 `초기 해금 여부: O`인 것(번개 / 아침의 새 소리 /
  숲의 소리 / 화염 / 이끼 낀 / 네잎클로버 / 금화의 잔향)만, 악세사리는 전부 잠김.
- **해금 조건**(해금 기획서 p.10~13, 15~21, 23~24 전사):
  - 머리: 가드맨=방어력 10 달성 / 메테우스=공격력 20 달성 / 버서커=체력 50 이하일 때 적 50마리 처치 /
    해피 픽셀=행운 20 달성 / 네온아이=보스 1회 처치 / 핫팟=디스크 누적 구매 50회 /
    픽시=근접 무기로 적 300마리 처치 / 팬봇=스킬 100번 사용 / 소다캔=모든 종류의 다리 파츠를 착용하고
    각각 웨이브 1회 이상 클리어 / 프라이빗 컴스톡=정밀화기로 적 200마리 처치 / 미니 픽시=누적 레벨 150 달성
  - 디스크: 광분=적 10마리 처치 / 물 흐르는 소리=HP 20 이하 달성 / 777 슬롯머신=웨이브 7 클리어 /
    바람 소리=웨이브에서 데미지를 입지 않고 클리어 / 금속음=체력 200 달성 후 웨이브 클리어 /
    염동력=디스크 4개 이상 착용 / 생존 장치=체력 30 이하로 웨이브 클리어 /
    에너지 베리어=웨이브 종료 회복 외의 방법으로 체력 회복 / 위장=적에게 피격 30회 /
    은하수=기본 다리 외의 다리를 착용 이후 스킬 사용 / 변압기=차저 10마리 처치 /
    파도=스프린터 10마리 처치 / 결정의 마찰음=적 처치 500회 / 암석=엔드리스 모드 진입
  - 악세사리(전부 엔드리스 모드): 선글라스=엔드리스 상점에서 아이템 1회 구매 / 왕관=엔드리스에서 적
    300마리 처치 / 목걸이=엔드리스에서 전설 등급 장비 구매 / 유니콘 뿔=엔드리스에서 각자 다른 6종류의
    무기 착용 / 조이스틱=엔드리스 진입 이후 코나미 커맨드 입력(상상하하좌우좌우 스페이스바 스페이스바) /
    고양이 귀=엔드리스에서 스프린터 200마리 처치
  - 주의: **"스킬 사용"이 조건인 것이 2건(팬봇, 은하수)인데 필살기는 데모 범위 밖이다.** 구르기(Space)를
    "스킬"로 볼지 확인이 필요하다 - 구현 시점에 질의한다.
- **도감 UI**: `Title.unity` 안의 패널로 코드 생성한다(`HeadSelectPanelUI`와 같은 관례 - 항목 수가
  데이터에서 오므로 씬에 칸을 미리 깔 수 없다). 해금 기획서 목업대로 카테고리별 격자 + 진행도 바
  (`3 / 12`) + 선택 항목의 해금 조건·진행 카운터를 보여준다.
- **머리 선택 화면 연동**: 잠긴 머리는 잠금 아이콘으로 표시하고 선택할 수 없게 한다
  (`PartsCatalog.GetSelectableHeads()`가 이미 `selectableInHeadSelect`로 거르고 있으므로 여기에
  해금 여부를 한 겹 더 얹는다).

### 미확정 / 구현 시 질의할 것

- 팬봇·은하수의 "스킬 사용" 조건을 구르기(Space)로 대체할지.
- Phase A-3의 상한·처치당 값(위 표)은 임시값이며 플레이 후 재조정 대상이다.
- 점수 공식 계수(웨이브 1,000 / 처치 10 / 레벨 200 / 골드 1)도 임시값이다.

## 2026-08-19 Phase A - 밸런스·버그 5건 (디스크 등급 추첨 / 경험치 커브 / 누적 디스크 / 근접 방향 / 행운)

바로 위 계획의 **Phase A**를 실행했다. 계획 수립 세션이 한도로 끊겨 계획서가 `작업.md`에
기록되지 못한 채 남아 있던 것을 세션 익스포트에서 복구해 먼저 붙이고 이어서 착수했다.
작업 중 사용자가 **A-5(행운 보정 하향)** 를 추가 요청했다.

### A-1. 상점 디스크 등급 추첨을 무기와 같은 규칙으로 교체

[ShopManager.CreateDiscOffer()](Assets/Scripts/ShopManager.cs)가 `RollGrade()`를 **호출하지 않고**
등장 가능한 디스크 전체에서 균등 추첨하고 있었다. 등급별 보유 수가 제각각(일반 4 / 레어 5 /
에픽 6 / 유니크 4 / 전설 2)이라 **등급 확률이 그대로 데이터 개수 비율**이 됐고, 등급이 하나씩
열리는 웨이브 3·5·7마다 고등급 비중이 계단식으로 뛰었다.

이제 **등급을 먼저 뽑고 그 등급의 디스크 중에서** 고른다. 해당 등급이 비면 `CreateWeaponOffer`·
`PartsCatalog.TryRollLootPart`와 같이 **아래로만** 내려가며 찾는다(위로 올라가면 `minWave`
게이팅이 다시 깨진다). `DiscData`가 **struct**라 "못 찾음"을 null로 표현할 수 없어 `found`
플래그를 따로 쓴다(처음에 `DiscData disc = null`로 썼다가 컴파일 에러로 발견).

**검증**(플레이모드, 웨이브 7, 20,000회 추첨):

| 등급 | 수정 전(실측) | 수정 후 | 의도 |
|---|---|---|---|
| 일반 | 19.0% | **50.0%** | 50% |
| 레어 | 23.8% | **26.2%** | 26% |
| 에픽 | 28.6% | **13.9%** | 14% |
| 유니크 | 19.0% | **6.8%** | 7% |
| 전설 | 9.5% | **3.1%** | 3% |

### A-2. 경험치 커브에 레벨 구간 배율 도입

`필요경험치 = 4 + 2 x 레벨`(2026-08-13에 계수를 1/12로 낮춘 값)이라 45레벨 누적이 2,156밖에
되지 않아 20웨이브 안에 최대 레벨에 여유롭게 도달했다. [AiCoreManager](Assets/Scripts/AiCoreManager.cs)에
구간 배율(`expTierStartLevels` / `expTierMultipliers`, 기본 15·30·45레벨에서 **x2 / x4 / x8**)을
신설하고 `RequiredExpForNextLevel()`에 곱한다. 1~14레벨은 종전과 동일하다(사용자 확정: 완만 안).

**검증**(플레이모드, 레벨 1~49 실제 함수 호출 누적): 15레벨 **266** / 30레벨 **1,706** /
45레벨 **6,386** / 50레벨 **10,306** - 계획값과 정확히 일치(종전 266 / 986 / 2,156 / 2,646).

씬(`Ground01.unity`)에 직렬화된 `expBase: 4` / `expPerLevel: 2`는 코드 기본값과 같아 손대지
않았고, 신규 구간 배열은 씬에 없는 필드라 코드 기본값이 그대로 적용된다(실측 확인).

### A-3. 영구 누적형 디스크 4종 - 상한·표기·장 수 반영

이전 세션 실측에서 **초기화 버그는 없고**(웨이브를 넘겨도 누적치가 그대로 유지됨) **상한이
웨이브 1 안에 다 차버리는 것**이 사용자가 본 "효과가 적용되지 않는다"의 정체임을 확정했다.

- **(A) 상한·처치당 재조정 — 기준 "약 200마리 처치에 상한 도달"**
  ([DiscTableGenerator.cs](Assets/Scripts/Editor/DiscTableGenerator.cs)의 `Defs` 수정 후 메뉴
  `Comstock/디스크 테이블 21종 재생성` 실행):

  | 디스크 | 처치당 | 상한 | 상한에서의 효과 |
  |---|---|---|---|
  | 교향곡: 암석 | 0.1 → **0.05** | 5 → **10** | 방어력 5 → 15 |
  | 교향곡: 화염 | 0.1 → **0.05** | 5 → **10** | 공격력 3 → 13 |
  | 바람 소리 | 0.0025 → **0.003** | 0.125 → **0.6** | 이동속도 3.0 → 3.6 (+20%) |
  | 금속음 | 1 → **0.2** | 10 → **40** | 최대체력 100 → 140 |

  종전에는 10~50마리에 상한에 닿아 남은 19웨이브 동안 한 번도 오르지 않았다.
- **(B) int 반올림 문제**: 처치당 값이 0.05로 작아져 방어력·공격력이 20마리마다 +1씩 눈에 보이게
  오른다. `RobotStats.ApplyBonus`의 반올림 구조 자체는 건드리지 않았다(AI 코어 보너스도 같은
  경로로 소수 누적 후 한 번만 반올림하는 것이 의도된 설계다).
- **(C) 설명 문구를 실시간 진행도로 교체**: [DiscData.BuildDescription()](Assets/Scripts/DiscData.cs)이
  `OnKillStackStat`일 때만 문구를 **매번 생성**한다 -
  `적을 처치할 때마다 방어력 +0.05 (현재 +3.2 / 최대 +10)`. 고정 문구는 처치당 값만 보여줘서
  상한 도달 후에는 "효과 없음"과 구분이 불가능했다(AI 코어 3택 카드·머리 효과 설명에서 이미
  겪은 함정과 같은 계열). 상점 카드와 장비 상세 팝업 양쪽에 자동 반영된다.
- **(D) 상한에 장 수 반영**: [DiscEffectRuntime.ApplyKillStack()](Assets/Scripts/DiscEffectRuntime.cs)이
  공유 총합을 `disc.cap` 하나로 막고 있어서 2장을 사면 **도달 속도만 2배가 되고 최종치는 1장과
  같았다**(`RunState.DiscStackProgress` 주석에 적힌 의도와 어긋남). `disc.cap * copies`로 고쳤다.

**검증**(플레이모드 리플렉션 실측): 암석 `amountA=0.05 cap=10`, 400회 처치 시 1장 누적 **10** /
2장 누적 **20**, 설명 문구가 미장착 `최대 +10` → 2장 장착 `최대 +20`으로 따라감.

### A-4. 근접무기 좌/우 칼끝 방향 - 원인 확정 후 재설계

같은 날 두 번 고쳤는데도 재리포트가 온 건이다. **플레이모드 실측으로 원인을 확정했다.**

- 가설 (a) *좌/우 판정이 틀렸다* → **아니었다.** 소켓 로컬 x(-2.75/+2.75)와 월드 x(-0.602/+0.602)의
  부호가 일치하고 부모 스케일도 양수(0.394)라 `CacheSocketSides()`의 판정은 정확했다.
- 가설 (b) *사용자가 보는 시점이 대기 자세 경로가 아니다* → **이것이 원인이었다.**
  적이 없을 때의 칼끝 각도는 **왼쪽 소켓 165도 / 오른쪽 소켓 15도로 이미 정확**했다(실측).
  그러나 감지거리 안에 적이 있으면 `UpdateSlot`이 총기와 같은 조준 경로를 타서 좌/우와 무관하게
  **적을 향해 회전**해 있었다. 사용자가 말한 "공격 후"가 바로 이 구간이다.

**사용자 확정**: "찌르기 후 즉시 좌/우 복귀". 이에 맞춰 [PlayerShootManager](Assets/Scripts/PlayerShootManager.cs)를
바꿨다 - 근접무기는 **조준 회전을 아예 하지 않는다.** 매 프레임 소켓의 좌/우 대기 각도를 그대로
박고(회전 속도를 태우지 않는 즉시 반영), 발사 각도 허용치 게이트도 타지 않는다.
적을 향하는 것은 **찌르는 순간뿐**이며 `StartMeleeThrustVisual()`이 진행 방향으로 각도를 잡는다
(찌르는 동안에는 `UpdateSlot`이 조기 반환하므로 유지되고, 끝나면 다음 프레임에 즉시 복귀).
근접무기가 빠져나가면서 조준 경로에 남아 있던 `is_melee` 분기 2개는 총기 전용으로 정리했다.

**검증**(플레이모드, 좀비 1마리를 체력 999999로 고정 배치):

| 상태 | 왼쪽 소켓 칼끝 | 오른쪽 소켓 칼끝 | 소켓→적 방향 |
|---|---|---|---|
| 적 없음 | 165.0 | 15.0 | - |
| 적이 감지거리 안 · 찌르지 않는 동안 | **165.0** | **15.0** | 55.9 / 124.1 |
| 찌르는 중 | 55 | 116 | 55.9 / 124.1 |

찌르지 않는 동안 좌/우 대기 각도를 유지하고, 찌를 때만 각 소켓에서 본 적 방향으로 뻗는 것을
확인했다. 대기 상태를 확정적으로 만들려고 무기 공격속도를 0.01로 낮춰 찌르기가 겹치지 않게 했다.

### A-5. 행운(Luck)의 고등급 확률 보정 (사용자 추가 요청)

사용자 요청은 "행운에 의한 고등급 확률 보정을 많이 낮춰야 한다"였는데, **전수 조사 결과 행운은
등급 확률에 전혀 관여하지 않고 있었다** - `ShopCatalog.RollGrade` / `PartsCatalog.RollBoxGrade` /
`AiCoreUpgradePool.RollGrade` 세 곳 모두 고정 가중치 + `minWave`만 쓰고, 행운을 실제로 소비하는
곳은 해피 픽셀 머리 효과 하나뿐이었다. 사용자에게 이 사실을 보고하고 **"아주 약한 보정을 새로
추가"** 로 확정받았다.

신규 [LuckBonus](Assets/Scripts/LuckBonus.cs) - **행운 10당 등급 한 단계마다 가중치 +1%**
(일반 무보정, 전설은 4단계라 +4%), 행운 기여분은 **50에서 상한**. 상점과 부품 상자 두 곳에만
적용했다(AI 코어 업그레이드는 주운 아이템이 아니라 레벨업 보상이라 제외). 플레이어 참조는
`HeadEffects.RegisterPlayer`와 같은 자리에서 등록/초기화한다(매 추첨마다 `FindObjectOfType`을
돌리지 않으려는 기존 관례).

**검증**(플레이모드, 웨이브 7, 행운값별 40,000회 추첨):

| 행운 | 일반 | 레어 | 에픽 | 유니크 | 전설 |
|---|---|---|---|---|---|
| 0 | 50.04% | 26.10% | 13.83% | 7.02% | 3.02% |
| 20 | 48.87% | 26.33% | 14.32% | 7.28% | 3.20% |
| 50 | 47.78% | 26.49% | 14.74% | 7.52% | 3.47% |
| 200 | 47.79% | 25.92% | 14.95% | 7.75% | **3.59%** (상한 작동 - 50과 사실상 동일) |

### 커밋 상태

미커밋. 스크립트 6개(`ShopManager` / `AiCoreManager` / `DiscData` / `DiscEffectRuntime` /
`PlayerShootManager` / `PlayerRobotController`) + 신규 1개(`LuckBonus`) + 등급 추첨 2개
(`ShopCatalog` / `PartsCatalog`) + 에디터 1개(`DiscTableGenerator`) + 에셋 1개
(`ShopCatalog.asset`은 디스크 테이블 재생성으로). **씬 변경은 없다.**

### 다음 단계

계획서의 **Phase B(점수 시스템) → C(엔드리스) → D(악세사리) → E(해금·도감)** 순서로 이어간다.

## 2026-08-19 Phase B(점수 시스템) + Phase C(엔드리스 모드) 완료

앞선 계획(`2026-08-19 밸런스 4건 + 점수·엔드리스·악세사리·해금 구현 계획` 항목)의 Phase B·C를
구현했다. 사용자 추가 지시: "랭킹은 나중에 파이어베이스 연결할 거니까 그거 고려해서, 연결하는
것만 제외하고 만들어놔" - 랭킹 제출/조회 호출부와 로컬 저장 구현을 전부 완성하되 실제 Firebase
네트워크 연동만 비워뒀다.

### Phase B - 점수 시스템

신규 [RunScore.cs](Assets/Scripts/RunScore.cs) (RunState와 동일한 static 패턴).
**공식**: `도달 웨이브 x1,000 + 누적 처치 수 x10 + AI 코어 레벨 x200 + 보유 골드 x1 + 악세사리 점수`.
누적 처치 수는 프로젝트에 처음 생기는 카운터라 `EnemyUnit.OnKilledByPlayer`를 직접 구독해서 센다
(`RunScore.EnsureKillTrackingSubscribed()` - static 이벤트라 씬 재시작 시 중복 구독을 막으려고
먼저 `-=` 해제 후 `+=`하는 idempotent 패턴을 썼다. `PlayerRobotController.Awake()`에서
`HeadEffects.RegisterPlayer`/`LuckBonus.RegisterPlayer`와 같은 자리에 호출).
`RunState.Reset()`과 별개로 `RunScore.Reset()`도 같은 자리에서 호출한다.

인게임 HUD에 실시간 점수를 보여주는 신규 [ScoreHudUI.cs](Assets/Scripts/ScoreHudUI.cs) -
씬 수정 없이 코드로 생성해 붙인다(`SettingsIconUI`와 같은 관례, 설정 아이콘 바로 왼쪽).

**검증**(플레이모드 실측): `OnKilledByPlayer` 구독자 2개(DiscEffectRuntime + RunScore) 확인,
처치 1회 재현 시 킬카운트 0→1 정확히 증가. 웨이브20/레벨12/골드500/악세사리300 조건에서
`wave=20000 kill=10 core=2400 gold=500 accessory=300 total=23210` - 공식과 정확히 일치.

### Phase C - 엔드리스 모드

**설계 요점**: 원래 계획은 "WinGame()/EndWave() 두 경로를 분기"였는데, 구현하면서 더 단순한
구조로 정리했다 - **보스를 처음(웨이브 20) 클리어했을 때만 `WinGame()`(=`GameWinManager.TriggerWin()`,
기존의 "게임 전체 정지" 가드를 그대로 재사용해 팝업이 뜨는 동안 플레이어·적·스포너를 얼린다)로
빠지고, 그 외의 모든 보스 클리어(이미 엔드리스 중일 때의 재등장 보스 포함)는 일반 웨이브처럼
`EndWave()`로 흘려보낸다.** `GameFlowManager`가 `GameWinManager.OnGameWon`을 직접 구독해서
점수 정산 팝업(`ScoreSummaryPopup`)을 띄우는 구조라, 기존의 "정비 화면 진입" 파이프라인
(`HandleWaveEnded`)과는 완전히 별도 경로다.

1. **`RunState.IsEndless`** 신설 - "계속 진행"을 선택하면 true. `Reset()`에도 추가.
2. **`WaveManager.IsBossWave(wave)`** 신설 - 첫 보스는 `finalWaveNumber`(20)에, 그 뒤로는
   `finalWaveNumber - 1`(19)웨이브마다 반복(20→39→58→77...). 기존의 `wave == finalWaveNumber`
   하드코딩 2곳(보스 스폰 조건 / 보스 대기 조건)을 이 함수로 교체했다.
3. **보스 스탯 배율**: 첫 보스(웨이브 20)는 **기존 밸런스 그대로**(배율 x1 - 이미 "웨이브 20
   도달 플레이어" 기준으로 실측해 둔 값이라 임의로 배율을 곱하면 검증 안 된 난이도가 된다).
   **재등장 보스만** 일반 몬스터와 같은 `EnemySpawner.CurrentStatMultiplier`를 곱한다 - 안
   그러면 플레이어는 계속 강해지는데 보스만 제자리라 엔드리스가 무의미해진다.
4. **`ScoreSummaryPopup.cs`** 신규(씬 수정 0, `PauseMenuUI`와 같은 코드 생성 관례) - 점수
   내역(웨이브/처치/레벨/골드/악세사리 각 줄 + 총점)을 보여주고 **계속 진행 / 타이틀로** 2택.
5. **`GameFlowManager.HandleGameWon()`**이 `GameWinManager.OnGameWon`을 구독해 팝업을 띄운다
   (`Time.timeScale=0`, 전투 HUD 숨김). **계속 진행**(`HandleEndlessContinueChosen`):
   `RunState.IsEndless=true` → `GameWinManager.Reset()`(플레이어·적·스포너 Update 가드를
   다시 풀어준다) → 정규 웨이브 종료와 완전히 같은 절차(`EnterPostWaveIntermission` - 체력 회복
   + 필드 정리 + AI코어/정비/상점 순서)를 밟아 웨이브 21로 자연스럽게 이어간다
   (`HandleWaveEnded`에 있던 로직을 이 메서드로 추출해 두 곳이 공유하게 했다 - 동작 변경 없음).
   **타이틀로**(`HandleEndlessDeclineChosen`): `RunScore.SubmitToLeaderboard()` → 타이틀 씬 이동.
6. **동시 생존 상한 안전장치**: `WaveManager.ComputeMaxAlive()`에 150마리 상한을 신설했다 -
   엔드리스는 웨이브 번호가 무한정 커질 수 있어 이 값도 무한정 커지는데, 막지 않으면 아주 늦은
   웨이브에서 "버벅여서 죽는" 상태가 될 수 있다(기획 의도인 "난이도 상승"과는 다른 종류의 실패).
7. **정비/상점 헤더**: `ShopPanelUI`/`ModdingPanelUI`의 `WAVE NN / 20` 표기가 엔드리스 중에는
   `WAVE NN / 무한`으로 바뀐다.
8. **사망도 종료 조건**: `GameOverSummaryUI`가 화면이 뜨는 시점에 점수 요약 한 줄
   (`총점 N`)을 기존 헤더에 붙이고 `RunScore.SubmitToLeaderboard()`를 자동 호출한다(선택할 게
   없으므로 확인 절차 없이 바로 제출 - 엔드리스에 진입하지 않은 일반 사망도 점수 자체는 유효해
   함께 제출한다).
9. **일시정지 "나가기"**: `RunState.IsEndless`일 때만 `RunScore.SubmitToLeaderboard()`를 호출한
   뒤 기존과 동일하게 타이틀로 나간다. **원 계획은 "나가기도 정산 화면으로 간다"였는데, 별도
   확인 팝업 없이 조용히 제출 후 나가는 것으로 단순화했다** - 이미 "나가기"를 눌러 의사를 밝힌
   행동이라 한 번 더 확인시키는 것이 과하다고 판단했다(점수는 그대로 랭킹에 남으므로 원 의도인
   "기록 보존"은 달성된다).

### 랭킹 - Firebase 연동 전 스캐폴딩

신규 [LeaderboardService.cs](Assets/Scripts/LeaderboardService.cs) - `ILeaderboardService`
인터페이스(`SubmitScore`/`FetchTopScores`, 둘 다 콜백 방식이라 Firebase의 비동기 응답과
호출부 코드가 그대로 호환된다) + 기본 구현 `LocalLeaderboardService`(PlayerPrefs에 JSON으로
상위 50개 저장, `JsonUtility`가 최상위 배열을 못 다뤄 `Wrapper` 클래스로 감쌌다).
`LeaderboardService.Current`가 진입점이고, **나중에 Firebase를 붙일 때는 이 한 줄만 바꾸면
된다**(호출부는 인터페이스만 알고 있어 수정 불필요). 플레이어 이름 입력 UI가 없어(범위 밖)
선택한 로봇(머리) 이름을 대신 쓴다.

### 검증 (플레이모드 실측, 리플렉션 + 실제 버튼 클릭 양쪽)

- `IsBossWave`: 1/5/19→false, **20/39/58/77→true**, 21/38/40/57→false - 계획한 사이클과 정확히 일치.
- 로컬 랭킹 왕복: 점수 2건 제출 → 내림차순 정렬된 목록 조회 정상(99999가 12345보다 위).
- **전체 흐름을 실제 버튼 클릭으로 재현**: 웨이브 20에서 `GameWinManager.TriggerWin()` →
  팝업 표시(`popup visible=True`, `IsGameWon=True`, `timeScale=0`, 헤더 "웨이브 20 클리어!") →
  **"계속 진행" 버튼을 실제로 클릭** → `IsEndless=True`, `IsGameWon=False`, 팝업 숨김,
  `CurrentState=Intermission` → 상점이 정상적으로 열리고 헤더가 **"WAVE 20 / 무한"** 표시 →
  **실제 "정비 종료" 버튼 클릭** → 웨이브 21 시작 로그 확인.
- **보스 스탯 배율**: 씬 값 기준 `bossMaxHp=8000`(코드 기본값 12000이 아니라 씬 직렬화 값이
  적용됨을 재확인). 웨이브 20(첫 클리어) 스폰 시 **HP 8000 그대로**(배율 x1) / 웨이브 39
  (재등장) 스폰 시 **HP 20445**(= 8000 x 2.556, `CurrentStatMultiplier`와 정확히 일치).
- 테스트 중 로컬 랭킹에 남긴 더미 데이터는 검증 후 `PlayerPrefs.DeleteKey`로 정리했다.

### 커밋 상태

미커밋. 신규 스크립트 5개(`RunScore`/`LeaderboardService`/`ScoreSummaryPopup`/`ScoreHudUI`) +
기존 스크립트 7개 수정(`RunState`/`PlayerRobotController`/`WaveManager`/`GameFlowManager`/
`GameHUD`/`GameOverSummaryUI`/`PauseMenuUI`/`ShopPanelUI`/`ModdingPanelUI`). **씬 변경 없음.**

### 다음 단계

Phase D(악세사리 6종) → Phase E(해금·도감)로 이어간다.

## 2026-08-19 Phase D(악세사리 6종) 완료

직전 세션이 사용량 한도로 끊긴 지점을 세션 익스포트에서 복구해 이어받았다. 데이터·상점·구매·
점수 연결(`AccessoryData`/`AccessoryCatalog`/`ShopManager`/`ShopPanelUI`/`RunState.AccessoryPurchaseOrder`)
까지는 이미 커밋되어 있었고(a.260819.05~06), **남아 있던 "캐릭터에 실제로 그리는 부분"** 을
이번에 만들었다. 씬 변경은 없다.

### 신규 [AccessoryVisual.cs](Assets/Scripts/AccessoryVisual.cs)

`PlayerRobotController.Awake()`에서 `HitFlash`/`DiscEffectRuntime`과 같은 관례로 자동 부착한다.
매 프레임(LateUpdate) `RunState.AccessoryPurchaseOrder`를 그대로 그린다.

- **붙이는 자리**: `ProceduralCharacterRig.BodyVisual`(몸통=머리 스프라이트)의 **자식**.
  리그가 매 프레임 `RestorePartSprites()`로 몸통 렌더러를 강제 복구하므로 몸통 자체를 건드리면
  다음 프레임에 지워진다(2026-08-19 네온아이 색 순환에서 겪은 함정). 리그가 `BodyVisual`/
  `BodyRenderer` 프로퍼티를 노출하는 것은 직전 세션에서 이 목적으로 미리 열어둔 것이다.
- **크기·위치 기준은 "실제로 픽셀이 그려진 영역"**: 원본 PNG의 여백이 제각각이라(머리 12종은
  250x250 안에서 좌우 6~68px, 악세사리 6종은 128x128 안에서 10~40px가 비어 있다) 스프라이트
  사각형을 그대로 쓰면 왕관이 머리 위에 붕 뜨거나 파고든다. **스프라이트의 타이트 메시 정점
  (`Sprite.vertices`) 범위**를 쓰면 텍스처를 Read/Write 가능으로 바꾸지 않고도 알파 여백을
  뺄 수 있다(임포터 기본값이 Tight 메시라 6장·19장 모두 그대로 쓸 수 있었다). 계산 결과는
  `Sprite`별로 static 캐시에 담는다.
- **부착 위치 3종**(`AccessoryAttachPoint`):
  - `Stack`(왕관·유니콘 뿔·조이스틱·고양이 귀) — 머리 꼭대기에서 **구매 순서대로 위로** 쌓인다.
    머리 높이의 6%만큼 눌러 앉히고(`StackSeatDepth`), 서로 15%씩 겹친다(`StackOverlap`).
    처음 만든 0% 겹침 버전은 그림끼리 한 점에서만 닿아 "탑처럼 떠 있는" 모양이 됐다(캡처로 확인).
  - `Face`(8비트 선글라스) — 머리 위쪽 끝에서 머리 높이의 46% 내려온 곳(`FaceHeightRatio`).
  - `Neck`(합격 목걸이) — 머리 아래쪽 끝에서 16% 올라간 곳.
  얼굴·목은 스택이 아니라 고정 위치라 여러 개 사면 같은 자리에 겹쳐 그려진다(점수는 매번 더해진다).
- **가로 크기**는 카탈로그의 신규 필드 `AccessoryData.visualWidthRatio`(머리 폭 대비 배수)로 정한다 —
  선글라스 0.72 / 왕관 0.55 / 목걸이 0.45 / 유니콘 뿔 0.30 / 조이스틱 0.20 / 고양이 귀 0.80.
- **좌우 반전**: 몸통은 `flipX`(픽셀만 뒤집고 자식 Transform은 그대로)이므로, 악세사리도 같은
  `flipX`를 켜고 **가로 좌표의 부호를 직접 뒤집는다**. 그리기 순서는 `몸통 sortingOrder + 1 + 인덱스`.
- **리그 재생성 가드**: `ProceduralCharacterRig.Build()`가 이전 RigRoot를 `DestroyImmediate`하므로
  우리가 붙인 자식도 함께 사라진다. 구매 개수만 비교하면 이 경우를 놓쳐 악세사리가 조용히
  사라지므로, 자식이 파괴됐거나 부모가 바뀌었으면 다시 만든다(`SpawnedObjectsLost`).

### 머리 12종 실측 후 예외 1건

머리 12종 전부를 바꿔가며 캡처해 보니 **11종은 기본 비율(얼굴 46%)로 눈 위치에 정확히 맞았고
팬봇만 어긋났다** — 머리 위로 솟은 프로펠러까지 스프라이트 높이에 포함되는 바람에 선글라스가
이마 위로 올라갔다. `FanBot.png`의 눈 중심을 픽셀로 실측(알파 14~233px 중 눈 중심 155px =
위에서 64%)해 `faceHeightRatioOverrides`에 팬봇만 **0.64**로 넣었다. "위로 긴 장식이 달린 머리"만
여기에 적으면 되고, 네온아이처럼 프레임이 여러 장인 머리는 `_0` 앞부분으로 비교한다.

### 검증 (전부 플레이모드 실측)

**1) 등장 확률** — `ShopManager.CreateRandomOffer()`를 리플렉션으로 20,000회씩 호출:

| 모드 | 악세사리 칸 | 결과 |
|---|---|---|
| 일반(웨이브 21, `IsEndless=false`) | **0 / 20,000** | 엔드리스가 아니면 절대 등장하지 않음(기획 확정) |
| 엔드리스 | **994 / 20,000 = 4.97%** | 지정값 5% |

6종 분포도 균등했다(왕관 162 / 뿔 161 / 조이스틱 175 / 선글라스 149 / 목걸이 175 / 고양이 귀 172).

**2) 구매 — 실제 상점 카드 버튼 클릭**(0번 칸을 왕관으로 고정한 뒤 `cardButton.onClick.Invoke()`):

- 카드 표시: 아이콘 `Crown-transparent`, 헤더 `서사 · 악세사리`, 본문 `점수 +500 (효과 없음) / 보유 0개`
- 1회 클릭: 골드 **1000 → 900**, 악세사리 점수 **0 → 500**, 구매 목록 1개, 안내 문구
  "왕관 구매 완료 (점수 +500)"
- **같은 칸 재클릭**: 골드 **900 → 800**, 점수 **500 → 1000**, 목록 2개, 본문이 `보유 2개`로 갱신 —
  중복 구매가 막히지 않는다(기획 확정).
- 점수 내역 조회에서 `악세사리=1000`이 총점에 그대로 반영됨.

**3) 화면** — 캡처로 확인:

- 왕관 + 선글라스 + 목걸이를 동시에 착용한 상태에서 각각 머리 위·눈·목에 정확히 붙는다.
- **왕관 2개 중복 구매 시 왕관 위에 왕관**이 쌓인다(사용자 지정 "뿔 위에 왕관 위에 뿔").
- 머리 12종 전부 교체하며 캡처 — 매번 리그가 다시 만들어지는데도 악세사리가 살아남는다
  (위 재생성 가드가 실제로 동작함을 함께 검증).

### 알려진 한계

- `HitFlash`는 **첫 피격 때 한 번만** 렌더러를 수집하므로, 그 이후에 산 악세사리는 피격 시
  흰색 번쩍임에 참여하지 않는다(악세사리만 원래 색으로 남는다). 상점에서만 구매가 일어나
  전투 중 목록이 바뀌지 않으므로 실사용에서는 대부분 드러나지 않는다.
- 스택을 많이 쌓으면 화면 위쪽으로 계속 솟는다(의도된 동작. 상한을 두지 않았다).

### 곁다리 정리

`RunScore.killTrackingSubscribed`가 값만 넣고 아무 데서도 읽지 않아 컴파일 경고(CS0414)를
내고 있었다. 구독 중복은 `-=` 후 `+=`로 이미 막고 있어 필드 자체가 불필요하므로 삭제했다.

### 커밋 상태

미커밋. 신규 1개(`AccessoryVisual`) + 수정 3개(`AccessoryData` 가로 크기 필드 /
`PlayerRobotController` 자동 부착 / `RunScore` 미사용 필드 제거). **씬 변경 없음.**

### 다음 단계

Phase E(해금·도감)로 이어간다. 착수 전에 확인이 필요한 것: 해금 조건 중 "스킬 사용"이 걸린
2건(팬봇 머리 / 은하수 디스크)에서 **필살기가 데모 범위 밖이라 구르기(Space)를 스킬로 볼지**.

## 2026-08-19 Phase E(해금·도감) 완료

`20260818_해금기획서_Ver01_김재원.pdf` 반영. 직전 세션이 사용량 한도로 끊긴 지점을 세션
익스포트에서 복구해 이어받았다 — **신규 5개 파일과 훅 12개 파일까지는 직전 세션에서 이미
작성돼 있었고**(미커밋), 이번 세션에서 **잠긴 항목의 상점 차단 · 도감 레이아웃 3건 · 저장
시점 2건**을 마무리하고 **전 항목을 플레이모드로 실측 검증**했다.

착수 전 질의한 "스킬"의 정의는 사용자 확정으로 **스페이스바로 발동하는 다리 파츠의 기술
(현재는 구르기)** 이다(필살기는 데모 범위 밖). 맵 해금은 아트가 없어 범위에서 제외(사용자 확정).

### 구조 — 왜 4개로 쪼갰나

해금 조건 39개(머리 12 / 디스크 21 / 악세사리 6)는 **조건표 · 저장 · 게임 이벤트 수집 · 화면**이
서로 다른 이유로 바뀐다. 한 파일에 두면 밸런스 조정(조건 수치)과 훅 추가가 같은 파일을 건드리게
되므로 넷으로 나눴다.

| 파일 | 역할 |
|---|---|
| [UnlockData.cs](Assets/Scripts/UnlockData.cs) | 조건표 39줄(`UnlockCatalog`) + 진행도 키(`UnlockProgressKey`) |
| [UnlockState.cs](Assets/Scripts/UnlockState.cs) | PlayerPrefs 저장·판정(해금 여부 + 진행도) |
| [UnlockTracker.cs](Assets/Scripts/UnlockTracker.cs) | 게임에서 일어난 일 → 진행도로 번역하는 단일 지점 |
| [CollectionPanelUI.cs](Assets/Scripts/CollectionPanelUI.cs) | 도감 화면(타이틀에서 열린다) |

`UnlockTracker`를 둔 이유는 `HeadEffects`와 같다 — 훅 지점이 10개 파일에 흩어져 있어 호출부마다
`UnlockState.AddProgress("문자열")`을 직접 쓰면 키가 프로젝트 전체로 퍼진다. 호출부는 "무슨 일이
일어났는지"만 알리고 그것이 어떤 진행도인지는 `UnlockTracker`만 안다.

### 진행도는 3종류다

조건 문구를 그대로 옮기면 세 가지 셈법이 필요하다(하나로 뭉치면 오답이 난다).

- **누적**(`AddProgress`) — "적 500마리 처치", "스킬 100번 사용", "피격 30회"
- **최고 기록**(`ReportMax`) — "방어력 10 달성". 스탯은 정비 중에 오르고 내리므로 도달한 최댓값만
  남겨야 한다(실측: 방어력 10을 찍은 뒤 3으로 내려도 기록은 10 유지).
- **서로 다른 것의 개수**(`AddDistinct`) — "모든 종류의 다리 파츠를 착용하고 각각 웨이브 클리어",
  "서로 다른 6종류의 무기 착용". 값이 아니라 ID 집합을 저장하고 그 크기를 진행도로 쓴다.
  다리는 등급별로 ID 끝자리만 다르므로(유압 다리 510090~510094) **10으로 나눠 종류로 묶는다.**

"한 번이라도 있었나"(HP 20 이하 달성, 엔드리스 진입, 코나미 커맨드)는 목표치 1짜리로 다룬다.

### 저장 — 진행도와 해금 여부를 둘 다 남긴다

`PlayerPrefs` 키 하나(`unlock_state_v1`)에 JSON으로 담는다(볼륨·로컬 랭킹과 같은 패턴).

- **진행도만 저장하면** 나중에 밸런스 조정으로 목표치를 올렸을 때 이미 해금한 항목이 도로 잠긴다.
- **해금 여부만 저장하면** 도감의 `3 / 12`, `0 / 500` 같은 진행 표시를 만들 수 없다.

처치 1마리마다 직렬화하지 않고 더티 표시만 해 두었다가 **해금이 발생한 순간 · 웨이브 종료 ·
게임 오버 · 게임 클리어 · 앱 종료**에 실제로 쓴다. 이번 세션에 발견해 채운 것이 뒤의 두 개다 —
[GameOverManager](Assets/Scripts/GameOverManager.cs)/[GameWinManager](Assets/Scripts/GameWinManager.cs)에
`UnlockState.Flush()`가 없어서 **웨이브 도중에 죽으면 그 웨이브의 진행도가 사라졌다**.
보스 웨이브는 `WaveManager.EndWave`를 타지 않으므로(제한시간으로 끝나지 않는다) 승리 쪽도 필요하다.

### 훅 12개 파일

| 조건 | 훅 지점 |
|---|---|
| 처치(전체/근접/정밀/차저/스프린터/보스/저체력) | `EnemyUnit.OnKilledByPlayer` 구독 |
| 피격 30회 / HP 20 이하 / 무피격 클리어 | `PlayerRobotController.TakeDamage` |
| 웨이브 종료 회복 **외**의 회복 | `PlayerRobotController.Heal(amount, fromWaveEnd)` |
| 스킬 100회 / 기본 외 다리 + 스킬 | `PlayerRobotController.TryStartDash` |
| 방어력·공격력·행운 달성 | `PlayerRobotController.ApplyAggregatedStats` |
| 누적 레벨 150 | `AiCoreManager`의 레벨업 루프 |
| 디스크 누적 구매 / 디스크 4개 착용 / 엔드리스 구매·전설 구매·무기 종류 | `ShopManager`의 구매 3종 |
| 웨이브 7·무피격·체력 조건 클리어 / 다리 종류 | `WaveManager.BeginWave`·`EndWave` |
| 엔드리스 진입 | `GameFlowManager.HandleEndlessContinueChosen` |

**"무슨 무기로 죽였나"를 알기 위해 피해 경로 전체에 무기 ID를 실었다** — 픽시(근접 300마리)와
프라이빗 컴스톡(정밀 200마리) 조건이 이걸 요구한다. `EnemyUnit.TakeDamage(amount,
def_ignore_percent, source_weapon_id = 0)`로 세 번째 인수를 늘리고(기본값 0이라 기존 호출부는
무수정) `Projectile`·`BeamProjectile`·`MeleeSwing`·`PlayerShootManager`가 자기 무기 ID를 넘기며,
`EnemyUnit.LastDamageWeaponId`에 마지막 타격 무기만 남는다. 처치 시점에 `PartsCatalog`의 무기
메타에서 분류(연사/산탄/정밀/폭발/에너지/근접)를 조회한다 — 디스크의 연쇄 번개처럼 무기가 아닌
피해는 0이 넘어와 어느 무기 카운터도 오르지 않는다. `DisruptorUnit`은 `TakeDamage`를 오버라이드
하므로 시그니처를 함께 맞췄다.

**웨이브 종료 진행도는 체력 회복 전에 기록해야 한다.** 조건 2건이 "체력 30 이하로 클리어" /
"최대 체력 200 이상으로 클리어"인데, `GameFlowManager`가 웨이브를 넘길 때 체력을 전부
회복시키므로(2026-08-12 확정 사항) 회복 후에 재면 저체력 조건이 영원히 성립하지 않는다.
`WaveManager.EndWave()` 안, `OnWaveEnded` 이벤트를 던지기 **전**이 유일하게 맞는 자리다.

### 잠긴 항목은 상점에 뜨지 않는다 (이번 세션)

직전 세션까지는 해금이 **도감 표시와 머리 선택에만** 걸려 있어서, 잠긴 디스크·악세사리가
상점에 그대로 나왔다(= 해금이 실제 게임에 아무 영향이 없었다). [ShopManager](Assets/Scripts/ShopManager.cs)에
두 군데를 넣었다.

- `GetAvailableDiscs()`에서 `UnlockState.IsUnlocked(disc.discId)`가 아닌 디스크를 제외한다.
  초기 해금 7종의 등급 분포가 **일반 3 / 에픽 3 / 유니크 1**(레어·전설 0)이라 레어·전설을 뽑으면
  후보가 비는데, `CreateDiscOffer()`가 **등급을 아래로만** 내려가며 찾으므로 빈 칸이 되지 않는다
  (레어→일반, 전설→유니크). 위로 올라가면 `minWave` 게이팅이 깨지므로 아래로만 내려가는 규칙은
  Phase A에서 정한 그대로 유지했다.
- 악세사리는 `AccessoryCatalog.TryGetRandomUnlocked()`(신규)로 해금된 것만 뽑고, **하나도 해금되지
  않았으면 그 칸은 무기/디스크가 된다**. 5% 확률을 다시 굴리지 않고 "당첨됐지만 후보가 없어
  넘어가는" 방식이라 해금이 늘어날수록 5%에 수렴한다. 이제 쓰이지 않는 `GetRandom()`은 삭제했다.
- **교착은 없다** — 6종 전부 엔드리스 조건이지만 8비트 선글라스가 "엔드리스 상점에서 아이템 1회
  구매"라, 엔드리스에 들어가 아무 물건이나 하나 사면 첫 칸이 열린다.

### 도감 화면

`Title.unity` 위에 코드로 만드는 패널이다(`HeadSelectPanelUI`와 같은 관례 — 항목 수가 데이터에서
오므로 씬에 칸을 미리 깔 수 없다). 타이틀 우상단 `도감` 버튼도 런타임 생성이라 **씬 수정은
`TitleSceneManager.shopCatalog` 참조 1줄뿐**이다(디스크 이름·아이콘 조회용).

- 카테고리 탭 3개에 `머리 1 / 12` 식 진행 표시, 5열 격자, 오른쪽에 선택 항목의 아이콘·이름·해금
  조건·진행도 막대.
- **잠긴 항목은 실루엣 + 자물쇠 + `???`** 로 보여주되 **클릭은 된다**(해금 조건을 읽을 수 있어야
  목표가 된다). 이름·아이콘은 도감이 따로 들고 있지 않고 각 카탈로그(로봇/디스크/악세사리)에서
  그때그때 조회한다.
- 이름 대응: 해금 기획서와 디스크 기획서(Ver01)의 표기가 3건 달라 효과를 대조해 맞췄다 —
  **아침의 새 소리 = 포근한 치유**(처치 시 회복) / **생존 장치 = 마지막 발악**(사망 직전 1회 생존) /
  **변압기 = 공명의 소리**(공격력·방어력이 번갈아 바뀜). 나머지 18종은 이름이 그대로 일치하며,
  21종 전부 `ShopCatalog.asset`의 `discId`와 1:1로 확인했다.

이번 세션에 실측으로 잡은 레이아웃 문제 3건:

1. **제목이 탭 줄과 겹쳐 있었다** — 제목 y 0.905~0.975, 탭 0.862~0.932로 범위가 실제로 교차했다.
   제목을 0.915~0.985, 탭을 0.828~0.898로 올리고 격자·상세 패널의 위쪽 끝을 함께 맞췄다
   (검증: 제목 448~524px, 탭 354~430px으로 18px 간격).
2. **악세사리 탭만 칸이 세로로 늘어졌다** — 6종이라 2행뿐인데 남은 높이를 2행이 다 나눠 가졌다.
   칸 높이 계산에 **최소 3행**을 가정해 머리 탭과 같은 크기가 되게 했다(205x228, 가로/세로 0.90).
   행이 3보다 많은 디스크 탭은 종전대로 꽉 채운다.
3. **탭을 바꾼 프레임에 이전 탭 칸이 남아 있었다** — `Destroy()`는 프레임 끝에 처리되므로
   격자 자식 수가 21이 아니라 **33**(12+21)이었다. 부모에서 먼저 떼어낸 뒤(`SetParent(null)`)
   `Destroy`하도록 고쳤다(검증: 12 / 21 / 6 / 12).

### 머리 선택 화면 연동

[HeadSelectPanelUI](Assets/Scripts/HeadSelectPanelUI.cs)는 잠긴 머리를 **실루엣 + 자물쇠 + `???`**
로 그리고 `button.interactable = false`로 고를 수 없게 한다. 기본 선택도 "첫 머리"에서 **"해금된
첫 머리"** 로 바꿨다(그러지 않으면 잠긴 머리가 선택된 채로 출발할 수 있다).

### 코나미 커맨드

조이스틱 악세사리 조건용 [KonamiCodeListener](Assets/Scripts/KonamiCodeListener.cs)를 신설했다
(`PlayerRobotController.Awake`에서 자동 부착 — 씬 수정 없음). 위 위 아래 아래 좌 우 좌 우 스페이스
스페이스이며, 3초 이상 끊기면 처음부터 다시 받는다. 엔드리스에서만 인정하지만(`UnlockTracker`가
확인) **입력 자체는 항상 받아둔다** — 매 입력마다 모드를 따지면 "엔드리스 진입 직전에 절반쯤
입력해 둔" 애매한 상태가 생긴다.

### 검증 (전부 플레이모드/에디터 실측)

**1) 조건표·저장 계층** (39개 전수)

| 항목 | 결과 |
|---|---|
| 초기 해금 | **8 / 39** — 머리 1(컴스톡) / 디스크 7 / 악세사리 0 (기획서 `초기 해금: O`와 일치) |
| 누적 진행도 | 적 9마리 → 광분 잠김, **10마리 → 해금** / 500마리 → 결정의 마찰음 해금 |
| 최고 기록 | 방어력 9 → 가드맨 잠김, **10 → 해금**, 이후 3을 보고해도 기록 10 유지 |
| distinct | 무기 종류 0·0·1 보고 → 진행도 **2** |
| 저장 왕복 | JSON 141바이트, 카운터·집합·해금 목록 전부 복원 |

**2) 상점 차단** (`ShopManager.CreateRandomOffer()` 리플렉션, 웨이브 20에서 20,000회씩)

| 조건 | 결과 |
|---|---|
| 일반 모드 | 디스크 7,983칸 전부 **초기 해금 7종만**, 잠긴 디스크 **0회** |
| 적 500마리 해금 후 | 광분 + 결정의 마찰음이 **679회** 등장(해금이 실제로 목록을 늘린다) |
| 엔드리스 + 악세사리 전부 잠김 | 악세사리 **0회**(무기/디스크로 대체됨) |
| 선글라스만 해금 | 악세사리 **998회 = 4.99%**, 그중 선글라스 아닌 것 **0회** |

**3) 훅** (실제 게임 오브젝트로)

| 훅 | 결과 |
|---|---|
| 처치 + 무기 분류 | 좀비 3마리를 근접(301101)/정밀(300301)/연사(300101)로 처치 → 전체 3 / **근접 1 / 정밀 1** |
| 피격 | `TakeDamage(1)` 1회 → 0 → **1** |
| 회복 | 일반 회복 → 진행 1(에너지 베리어 해금) / **웨이브 종료 회복은 무시** |
| 스킬 | 구르기 1회 → 1. 기본 다리에서는 은하수 조건 0, **다리 510090으로 바꿔 구르니 즉시 해금** |
| 스탯 | 스탯 재계산 → 최고 방어력 5 / 공격력 3 / 행운 0 (실제 값과 일치) |
| 레벨업 | 경험치 10,000 주입 → 누적 레벨업 **46**(코어 레벨 49) |
| 웨이브 클리어 | 웨이브 7 클리어 → **777 해금**, 무피격 → **바람 소리 해금**, HP 20으로 클리어 → **마지막 발악 해금**(회복 후 100/100이 아니라 회복 전 20을 봤다는 뜻) |
| 웨이브 클리어(음성 대조) | 최대 체력 100이라 금속음(200 이상) **미해금** |
| 다리 종류 | 기본 다리로 1회 + 510090으로 1회 클리어 → 진행도 **2**(집합 `50000,51009`) |
| 게임 오버 저장 | 적 5마리(해금 없음) 누적 시 저장값 비어 있음 → 게임 오버 후 `kills:5` 기록됨 |

**4) 화면** (게임 뷰 1333x985 캡처 + 캔버스 픽셀 좌표 실측)

- 도감 머리 탭: 12칸(컴스톡만 정상, 11칸 실루엣+자물쇠+`???`), 상세 `기본 제공`
- 디스크 탭: 21칸(7칸 정상 아이콘·이름 / 14칸 잠김), 잠긴 칸 클릭 → `적 500마리 처치 · 0 / 500`
- 악세사리 탭: 6칸 전부 잠김, 조이스틱 클릭 → `엔드리스 진입 이후 위위아래아래좌우좌우 + 스페이스 2번 입력 · 0 / 1`
- 머리 선택 화면: 11칸 자물쇠 + `클릭가능=False`, 컴스톡만 자동 선택(노란 테두리)

검증 과정에서 만든 진행도는 전부 지웠다(`PlayerPrefs` 키 삭제 확인) — 사용자는 초기 상태에서
시작한다. 검증 전 백업값 길이가 0이었으므로 **원래 저장돼 있던 사용자 진행도는 없었다.**

### 알려진 한계 / 남은 것

- **해금 알림 UI가 없다.** `UnlockState.OnUnlocked` 이벤트와 콘솔 로그(`해금: 가드맨 (방어력 10
  달성)`)까지만 있고 화면 연출은 없다(기획서에도 없다). 붙일 자리는 이벤트 하나만 구독하면 된다.
- **일시정지 메뉴의 `랭킹·도감(준비 중)` 버튼은 그대로 비활성**이다. 도감 자체는 타이틀에서만
  열린다 — `CollectionPanelUI.Attach()`가 캔버스와 카탈로그 2개만 받으므로 인게임에서도 열 수
  있지만, 같은 버튼에 묶인 랭킹(Firebase 미연결)이 아직 없어 함께 열지 않았다. **사용자 판단 대기.**
- **소다캔(100011)** 은 "모든 종류의 다리 파츠"를 5종으로 잡았다(현재 데이터의 다리 종류 수).
  다리 기획서가 오면 종류 수와 함께 재확인해야 한다.
- 팬봇(스킬 100회)·미니 픽시(누적 레벨 150)·픽시(근접 300)처럼 **목표치가 큰 조건은 실제 플레이
  분량 기준으로 재조정 대상**이다(기획서 값 그대로 옮겨둔 상태).

### 커밋 상태

미커밋. 신규 5개(`UnlockData`/`UnlockState`/`UnlockTracker`/`KonamiCodeListener`/`CollectionPanelUI`)
+ 수정 14개 + `Title.unity` 1줄(`shopCatalog` 참조).

### 다음 단계

기획서 4종 반영 계획(2026-08-19)의 **Phase A~E가 모두 끝났다.** 남은 선택지는 (1) 위 "알려진
한계"의 해금 알림·인게임 도감, (2) `UI 기획서.pdf`의 보류 항목 **B-6(캐릭터 미리보기)** 와
**Phase F(랭킹·도감 화면)**, (3) 랭킹 Firebase 연결(사용자 지시로 인터페이스만 만들어 둔 상태),
(4) 목표치·밸런스 실플레이 재조정이다.

## 2026-08-20 Firebase Realtime Database 랭킹 연동 + 랭킹 화면 신설

`LeaderboardService.cs`(2026-08-19 Phase C)가 `ILeaderboardService` 인터페이스로 미리 막아둔
자리에 실제 Firebase 연동을 채워 넣고, 그동안 없었던 **랭킹을 "보는" 화면**을 새로 만들었다.

### 1) Firebase 연동 — REST 방식 (SDK 미사용)

사용자가 Firebase 콘솔에서 프로젝트(`comstock-d3868`)와 Realtime Database(싱가포르 리전)를
직접 만들었다. 공식 Firebase Unity SDK(EDM4U·Gradle 설정)를 쓰지 않고 `UnityWebRequest`로
REST 엔드포인트를 직접 호출하는 방식을 골랐다 — 설치가 가볍고 PC/Android/iOS 어디서든
동일 코드로 동작한다(이 프로젝트는 아직 타깃 플랫폼이 확정되지 않았다).

- `Packages/manifest.json`에 공식 `com.unity.nuget.newtonsoft-json`(3.2.1) 패키지 추가 —
  Firebase 응답이 "푸시ID를 키로 하는 객체"라 `JsonUtility`로는 못 읽는다(동적 키). 매니페스트
  편집만으로는 Unity가 바로 안 물어와서(파일 감시 타이밍) `UnityEditor.PackageManager.
  Client.Resolve()`를 직접 호출해 강제 리졸브했다.
- 신규 `FirebaseLeaderboardService.cs` — `ILeaderboardService` 구현체. `SubmitScore`는
  `/leaderboard.json`에 POST(자동 키 생성), `FetchTopScores`는
  `?orderBy="Score"&limitToLast=N` 조회 후(Realtime DB는 오름차순만 주므로) 코드에서
  내림차순으로 뒤집는다.
- `LeaderboardService.Current`의 기본값을 `LocalLeaderboardService`에서
  `FirebaseLeaderboardService`로 교체(예고했던 "한 줄만 바꾸면 된다"가 실제로 한 줄이었다).
  문제가 생기면 이 한 줄을 되돌리면 로컬 저장으로 즉시 폴백된다.

**겪은 함정 — `.indexOn` 누락.** `orderBy` 쿼리는 Realtime Database 규칙에 `.indexOn`이
없으면 **400 Bad Request**를 던진다. `FirebaseLeaderboardService.FetchTopScores`는 실패 시
빈 리스트로 `onComplete`를 부르므로(경고 로그만 남기고 조용히), 화면에는 "기록 없음"과
똑같이 보여서 처음엔 정상 작동으로 착각했다 — 실제로는 매번 400을 먹고 있었다. 스크린샷으로
사용자의 Rules 탭 내용을 직접 확인해서야 `.indexOn": "Score"` 한 줄이 빠진 걸 발견했다(사용자가
첫 버전 규칙만 게시하고 인덱스 추가분은 반영을 못 했었다). **교훈: 실패를 빈 상태와 구분 없이
삼키는 폴백은 디버깅을 어렵게 한다 - 이번엔 그대로 두되(사용자에게 노출할 에러 UI는 범위
밖), 비슷한 문제가 또 나오면 로그를 먼저 확인할 것.**

검증은 실제 라이브 데이터베이스에 대고 `script-execute`로 제출→조회→삭제 왕복을 두 차례
했다(첫 검증 때 넣은 테스트 데이터는 확인 즉시 삭제, 두 번째 정렬 검증용 더미 2건도 확인 후
삭제 - 남겨진 실사용자 데이터는 없다).

### 2) 랭킹 화면 신설 — `RankingPanelUI.cs`

지금까지는 점수 **제출**만 있고 **조회 화면**이 없었다(일시정지 메뉴의 랭킹 버튼은 "준비 중"
비활성 상태였다). `CollectionPanelUI`와 같은 관례로(씬에 배치하지 않고 코드로 만들고, 닫으면
파괴) 새로 만들었다: 상위 20위를 `1위 · 이름 · 점수` 형식의 단일 텍스트 블록으로 보여주고,
불러오는 중 상태와 빈 상태("아직 등록된 기록이 없습니다")를 처리한다.

연 곳 3군데(사용자 확정 - "만들고, 둘 다, 타이틀 우하단 버튼으로도"):

- **일시정지 메뉴**(`PauseMenuUI.cs`) — 비활성이던 "랭킹 (준비 중)" 버튼을 활성화. ESC로
  랭킹 화면부터 닫히게 하고(설정 화면과 같은 우선순위), 일시정지를 닫을 때도 함께 닫는다.
- **게임오버 요약 화면**(`GameOverSummaryUI.cs`) — 신규 `rankingButton` 필드 추가. 이 화면은
  다른 UI와 달리 **씬에 배치된** 컴포넌트라(기존 관례), 버튼 오브젝트 자체는 씬을 열어
  `TitleButton`을 복제(`gameobject-duplicate`)해 위치만 옮기고 라벨을 바꿨다(Ground01.unity
  `Canvas/GameOver/RankingButton`) — 코드는 여기서도 `AddListener`만 하고 UnityEvent
  퍼시스턴트 리스너는 쓰지 않는다(기존 `titleButton`과 동일 패턴).
- **타이틀 화면**(`TitleSceneManager.cs`) — 우하단에 신규 "랭킹" 버튼(도감 버튼과 같은 관례로
  코드 생성).

### 3) 타이틀 화면 하단 볼륨 슬라이더 → "설정" 버튼 교체 (사용자 요청)

타이틀 하단의 `MusicVolumeSliderUI`(음악 볼륨 슬라이더)를 없애고, 일시정지 메뉴와 똑같은
`SettingsPanelUI`(배경음/효과음 슬라이더 + 화면 조정을 한 화면에서 다룸)를 여는 "설정" 버튼으로
바꿨다 - 같은 자리(하단 중앙)를 그대로 쓴다. 볼륨 조절 자체가 없어진 게 아니라 설정 화면
안으로 옮겨간 것이다. `TitleSceneManager`의 `volumeSliderParent` 직렬화 필드는 씬에 이미
연결돼 있을 수 있어 이름은 그대로 두고 역할 설명(툴팁)만 갱신했다.

### 검증

Unity 플레이모드에서 `script-execute`로 각 버튼의 `Button.onClick.Invoke()`를 직접 호출해
화면이 실제로 뜨는지, 랭킹 화면이 실제 Firebase 데이터를 정렬해서 보여주는지 확인했다(더미
점수 500/900 제출 → 랭킹 화면 재오픈 → "1위 · 900 / 2위 · 500" 순서로 정확히 표시). 확인 후
더미 데이터는 삭제했다.

### 커밋 상태

미커밋. 신규 2개(`FirebaseLeaderboardService`/`RankingPanelUI`) + 수정
5개(`LeaderboardService`/`PauseMenuUI`/`GameOverSummaryUI`/`TitleSceneManager`/
`Packages/manifest.json`,`packages-lock.json`) + `Ground01.unity`(RankingButton 오브젝트
추가 + `GameOverSummaryUI.rankingButton` 참조 연결).

### 다음 단계

**Firebase 연동 + 랭킹 화면까지 끝났다.** 남은 선택지는 이전 항목과 동일 — 해금 알림 UI,
인게임 도감 진입점, `UI 기획서.pdf` 보류 항목(B-6/Phase F 나머지), 목표치·밸런스 실플레이
재조정.

## 2026-08-20 랭킹 맵별 분리 (사용자: "앞으로 맵 여러개 추가할거임")

바로 위에서 만든 랭킹이 맵 구분 없이 전체 통합이었는데, 사용자가 "맵마다 랭킹 다르게 해야됨" +
"앞으로 맵 여러개 추가할거임"이라고 지시해서 **모든 랭킹 데이터를 맵별로 분리**했다.

- `ILeaderboardService.SubmitScore`/`FetchTopScores`에 `mapId` 매개변수 추가(둘 다 첫
  인자). `LocalLeaderboardService`는 PlayerPrefs 키 뒤에 `.{mapId}`를 붙여 분리,
  `FirebaseLeaderboardService`는 저장 경로를 `leaderboard/{pushId}` →
  `leaderboard/{mapId}/{pushId}`로 한 단계 더 감쌌다.
- **mapId의 정체 = 씬 이름.** 별도의 "맵 카탈로그"나 맵 선택 상태를 새로 만들지 않았다 -
  `RunScore.SubmitToLeaderboard()`(런 중·게임오버)는 `SceneManager.GetActiveScene().name`을
  그대로 쓰고, 랭킹을 여는 세 곳도 각자 문맥에 맞는 씬 이름을 넘긴다: 일시정지 메뉴·게임오버
  요약 화면은 활성 씬(플레이 중인 맵), **타이틀 화면은 `TitleSceneManager.nextSceneName`**
  (다음에 플레이할 맵 - 지금은 "게임 시작" 버튼과 같은 값)을 쓴다. 맵이 늘어나 새 씬이
  생기면 그 씬 이름이 자동으로 새 mapId가 되므로 **이 파일들을 다시 고칠 필요가 없다** -
  나중에 맵 선택 화면이 생기면 그 화면이 `nextSceneName`을 갱신해주기만 하면 된다.
- `RankingPanelUI`도 `mapId`를 받아 그 맵의 랭킹만 조회하고, 제목에 `"랭킹 - {mapId}"`로
  어느 맵인지 표시한다(맵이 여러 개가 되면 헷갈리지 않게).
- **Firebase 규칙도 구조가 달라져 다시 갱신했다** - 데이터 경로가 한 단계 깊어졌으므로
  `.indexOn`/`.validate`도 `"leaderboard": { "$mapId": { ... "$entryId": {...} } }`처럼
  한 단계 안쪽(`$mapId` 위스카드)으로 옮겨야 했다. 옮기기 전에 실측으로 **401 Unauthorized**를
  먼저 확인했다 - 기존 규칙의 `$entryId`가 이제 mapId 위치를 가리키게 돼서 `.validate`가
  "PlayerName/Score/DateIso 자식이 있는가"를 mapId 노드(실제로는 여러 점수 항목을 담은
  컨테이너)에 검사해 항상 실패했었다.
- 검증: 가상 맵 2개("Ground01"/"TestMap2")에 각각 점수를 넣고 서로의 목록에 안 섞이는지
  확인(각 1건씩만 조회됨). 확인 후 두 맵 노드를 통째로 삭제해 정리했다.

### 커밋 상태

미커밋(이전 항목의 미커밋 변경사항에 이어서). 수정 5개 추가
(`LeaderboardService`/`FirebaseLeaderboardService`/`RunScore`/`RankingPanelUI`/
`PauseMenuUI`/`GameOverSummaryUI`/`TitleSceneManager` 중 mapId 관련 부분).

### 다음 단계

맵별 랭킹 분리까지 끝났다. 실제로 맵이 2개 이상이 되면 타이틀 화면에 **맵 선택 UI**(지금은
`nextSceneName` 하나로 고정)가 필요해진다 - 지금은 맵이 1개뿐이라 미리 만들지 않았고, 맵이
실제로 추가될 때 함께 설계하는 게 맞다.

## 2026-08-20 기본점수 버그 수정 + 디스럭터 자폭 지연 + 랭킹 닉네임 입력 + 타이틀/빅토리 화면 수정

사용자 피드백 5건을 한 세션에서 처리했다.

### 1) 기본점수 1200점 버그 수정

`RunScore.ComputeBreakdown()`이 `WaveNumber`(웨이브 1 시작 시점에 1)와 `CoreLevel`(기본값 1)을
"진행도"가 아니라 "현재 값" 그대로 곱해서, **런을 시작하자마자 1x1000 + 1x200 = 1200점**이
조건 없이 붙어 있었다(사용자 리포트: "기본점수 1200점 주는거 고치셈 기본으로 0점 시작해야됨").
`RunScore.cs`에서 둘 다 1을 뺀 값(= 실제로 "클리어한" 웨이브 수 / "레벨업한" 횟수)을 곱하도록
고쳤다. `ScoreSummaryPopup.cs`의 내역 표시 문구도 같은 식으로 맞췄다(안 맞추면 화면에 보이는
곱셈식과 실제 점수가 어긋난다). 실측: 웨이브1/코어레벨1(시작 직후) → 총점 **0**, 웨이브3/코어레벨4
(2웨이브 클리어 + 3회 레벨업) → 정확히 **2600점**(2000+600) 확인.

### 2) 디스럭터(자폭 좀비) - 즉시 폭발 대신 깜빡이다 2초 뒤 폭발

사용자 요청: "디스럭터 폭발하기 직전에 깜빡거리면서 2초뒤에 터지게 만들어주셈. (이때는 무적
상태로, 디스럭터가 죽진 않음)". `DisruptorUnit.cs`를 고쳐서, 자폭 계기(큰 피해 / 사거리 안
도달)가 오면 즉시 터지는 대신 `fuseStarted` 플래그를 세우고 2초(`fuseSeconds`) 코루틴을
돌린다 - 몸 색을 원래색↔경고색(빨강)으로 0.15초 간격(`flickerInterval`)으로 반복 교대하다가
끝에 실제로 터진다(`Explode()`, 예전 `Detonate()`와 같은 내용). **심지가 붙은 동안은
`TakeDamage`가 아무 것도 안 하고 바로 돌아가 완전히 무적**이다(사용자 지시 그대로) - 같은
계기가 또 와도(공격 쿨다운이 돌아 재시도 등) `fuseStarted` 가드가 막아 코루틴이 중복 실행되지
않는다. 실측(EnemySpawner.SpawnMonster(200005, ...)로 실제 스폰): 큰 피해 → `IsDead=False`
(안 죽음) → 그 상태에서 999999 피해를 더 줘도 여전히 `IsDead=False`(무적 확인) → 이후
오브젝트가 실제로 파괴됨(폭발 확인).

### 3) 랭킹 등록 시 닉네임 입력 (신규 `NicknameInputPopup.cs`)

사용자 요청: "게임 끝나서 랭킹 등록할때 닉네임 넣는 기능도 만드셈". 예전에는 로봇(머리) 이름을
자동으로 썼는데(`RunScore.SubmitToLeaderboard()`에 인자가 없었다), 이제 제출 직전에 닉네임
입력 팝업을 띄운다. 입력창은 **로봇 이름(또는 PlayerPrefs에 저장된 마지막 닉네임)으로 미리
채워져 있어 확인만 눌러도 예전과 동일하게 동작**한다 - 매판 타이핑을 강제하지 않는다.
`RunScore.SubmitToLeaderboard()`가 `string playerName` 인자를 받도록 바뀌었고
(`ResolveDefaultPlayerName()`이 기본값 계산을 담당), 제출이 일어나는 3곳(게임오버 요약 화면 /
일시정지 메뉴 "나가기" / 정산 팝업 "타이틀로") 모두 `NicknameInputPopup.Attach(...)`로
감쌌다. 확인한 닉네임은 다음 판을 위해 PlayerPrefs(`comstock_nickname`)에 저장한다.
실측: 기본값 프리필 확인 → 앞뒤 공백 있는 문자열 입력 후 확인 → 콜백에 트림된 문자열 전달 +
PlayerPrefs 저장까지 확인.

### 4) 타이틀 이미지 교체 (사용자가 새 로고 아트 제공)

사용자가 `타이틀이미지.png`(2115x643, 가로로 넓은 로고 형태)를 새UI 폴더에 넣어주고 적용을
요청했다. **처음에 전체화면 배경(`Assets/Resources/UI/titleimage.png`)에 그대로 덮어씌웠다가
사용자에게 크게 지적받았다** - "타이틀 이미지는 제목 이미지로 넣어야지 전체화면으로 늘려서
넣으면 어떡하냐". 배경 이미지는 git으로 원상복구하고, 새 이미지는 `Assets/Resources/
UI/title_logo.png`로 따로 넣어(텍스처 임포트 설정을 `titleimage.png`와 같은 Sprite로 맞춤)
기존 텍스트 제목(`TitleText`/`TitleText_BG`) 자리에 **비율을 유지하는 로고 이미지**로
교체했다(신규 `TitleLogo` 오브젝트, `Image.preserveAspect=true`). 텍스트 오브젝트는 지우지
않고 비활성화만 했다. **캔버스가 ConstantPixelSize라 고정 픽셀 크기를 쓰면 해상도마다 깨진다는
프로젝트 관례를 처음에 어겼다가**(sizeDelta를 픽셀 값으로 넣음) 스스로 발견해 앵커 비율
기반(`anchorMin`/`anchorMax` 사각형 + offset 0)으로 다시 고쳤다.

### 5) 빅토리 화면 - 화면을 가리는 암막 제거

사용자 요청: "빅토리 뜨면서 화면 가리는거 없애". 20웨이브를 처음 클리어하면 `GameHUD`의
단순 "Victory" 문구와 `GameFlowManager.HandleGameWon()`이 띄우는 `ScoreSummaryPopup`(점수
내역 + 계속/타이틀로 선택)이 동시에 뜨는데, 이 팝업에 **화면 전체를 75% 검게 덮는
DimBackground**가 있어서 승리 직후 필드(쓰러진 보스·HUD·Victory 문구)가 통째로 가려졌다.
`ScoreSummaryPopup.cs`에서 이 DimBackground 생성 코드를 지웠다 - 점수 내역 패널과 버튼만
그 위에 뜨고 게임 화면은 그대로 보인다.

### 커밋 상태

미커밋. 신규 3개(`NicknameInputPopup`/`title_logo.png`+`.meta`) + 수정 6개
(`RunScore`/`ScoreSummaryPopup`/`DisruptorUnit`/`GameOverSummaryUI`/`PauseMenuUI`/
`GameFlowManager`) + `Title.unity`(TitleLogo 오브젝트 추가, TitleText/TitleText_BG
비활성화) + `Assets/Resources/UI/titleimage.png`는 원상복구(변경 없음).

## 2026-08-20 파츠 25종 전면 교체 + 스탯 소수화 + 골드 아이콘 통일 + 누적형 디스크 표시

사용자가 파츠 명세 2종(`20260820_컴스톡장갑명세_Ver01_김지수.md`,
`컴스톡_소켓_메모리_자기장코어_디스크슬롯_명세.md`)과 아이콘 아트 25장(`새파츠들/`, 전부 320x320
투명 배경)을 제공하며 "기존에 임시로 들어가있던 파츠들 대신 새로운 파츠 기획하고 이미지 만들어왔으니
적용해"라고 요청했고, 작업 중 UI 결함 3건을 추가로 지적했다.

### 계획 단계에서 사용자가 확정한 것

- **헬멧은 "머리부 장갑"의 새 명칭**이다 → 기존 `PartSlot.Helmet`을 그대로 쓴다.
- 소켓 체계는 **명세로 전면 교체**(기존 사거리/감지/회전 배율은 폐기).
- 스탯은 **데미지 파이프라인까지 전부 소수화**("전부 소수화 해라 안짤리게").
- 골드 아이콘은 **UI를 인게임 금화(`Resources/Gold.png`)로 통일**.
- 각 슬롯의 **"표준 OO" 기본 장착 파츠는 유지**(신규 25종은 부품 상자 드랍 전용).

명세 문서 제목에 "자기장 코어"가 있으나 본문에 해당 섹션과 이미지가 없어 **자기장 코어 / 팔 장갑 /
다리 슬롯은 기존 임시 파츠를 그대로 두었다**(사용자에게 보고함).

### 1) 파츠 25종 x 5등급 = 125행 (신규 `PartsTableGenerator`)

`Assets/Scripts/Editor/PartsTableGenerator.cs`를 신설했다 — 무기(`WeaponTableGenerator`, 65행)·
디스크(`DiscTableGenerator`, 21종)와 같은 패턴이며, 파츠에는 그동안 생성기가 없어 103개가 전부
인스펙터 수기 입력이었다. 메뉴는 `Comstock/파츠 테이블 25종 재생성` / `Comstock/파츠 테이블 검증`.

- 헬멧 6종(연극 마스크 / 행운의 양동이 / 방탄모 / 탈 / 철인 헬멧 / 빵봉투)
- 다리장갑 4종(양철·가시·경사·반응형)
- 무기 소켓 7종(연사/산탄/정밀/폭발/에너지/근접/범용)
- 메모리 3종(메모리 칩 / 뉴럴 캐시 / 아카식 레지스터)
- 디스크 슬롯 5종(기본 슬롯 / 확장 프레임 / 코어 연결망 / 허브 접속기 / 교향곡 모음집)

생성기는 **그 5개 슬롯의 루트 파츠만** 갈아끼운다(기본 파츠와 다른 슬롯은 건드리지 않는다).
결과: **파츠 103개 → 173개**(루트 55개 제거, 125개 추가). 검증 결과 ID 중복 0건, 아이콘 누락 0건,
등급별 후보 수는 일반 41(기본 파츠 8개 포함) / 나머지 등급 33개씩으로 균등하다.

**등급별 수치는 배율 계산 없이 명세의 5개 값을 그대로 넣었다**(예: 행운 +2/2.5/3/4/5). 값이 하나만
적힌 항목은 등급 무관 고정 수치다. 무게는 명세에 없어 슬롯별 기존 임시값을 승계했다
(헬멧 1.5 / 다리장갑 2 / 소켓 2 / 메모리 0 / 디스크슬롯 0.8). ID는 슬롯별로
`530000/531000/532000/533000/534000 + 종류*10 + 등급`.

**함정 하나**: 기존 "표준 메모리 카드"는 `coreMaxLevelBonus = 50`(예전엔 최대 레벨을 **대체**하는
절대값)을 들고 있었는데, 명세가 가산형(`+15/25/35/43/50`)이라 `CoreMaxLevel`을 "머리 기본 50 + 파츠"로
바꾸는 순간 **기본 상태에서 최대 레벨이 100이 된다.** 생성기가 표준 메모리의 값을 0으로 보정한다.

### 2) `PartData` 확장 + 신규 `PartEffects` 레이어

명세 파츠는 대부분 효과가 2개 이상인데 기존 `PartData`는 `bonusStat`/`bonusAmount` **한 쌍**뿐이었다.
`DiscData`(statA~C + effectType + 파라미터)와 같은 방식으로 확장했다.

- 가산 2쌍: `bonusStat`/`bonusAmount` + `bonusStat2`/`bonusAmount2`
- 특수효과: 신규 `PartEffect` enum + `effectStat` + `effectAmount`
  (`DefFromLuckPercent` / `DefFromAtkPercent` / `DefPercentBonus` / `DefWhenLowHp` /
  `MeleeReflectPercent` / `CoreStartLevel` / `PerDiscStat` / `PerSymphonyDiscAtk`)
- 소켓 전용 6개 필드(공격속도% / 공격력 절대값·% / 치명타% / 스플래시% / 방어력무시%p)로
  기존 `socketRange/Detect/RotationSpeedMultiplier` 3개를 대체
- `iconName`(파츠별 아이콘) 신설
- `BuildDescription()`을 전면 재작성 — **문구를 데이터에서 매번 생성**한다(고정 문구는 반드시
  어긋난다는 것을 AI 코어 카드·머리 효과에서 이미 겪었다). 상점의 파츠 상세도 항목을 하나하나
  나열하던 것을 이 함수 하나로 바꿨다.

파생·조건부·％ 계산은 신규 `Assets/Scripts/PartEffects.cs`가 담당한다(머리의 `HeadEffects`와 같은
역할·구조). **`RobotStats.Compute`에서 `HeadEffects.ApplyStatBonuses` 다음에 호출해야 한다** —
연극 마스크는 최종 행운을, 철인 헬멧은 최종 공격력을 읽어야 하고, 방탄모의 방어력 ％증가는
가산·파생이 모두 끝난 값에 곱해져야 한다. 적용 순서는
`가산 → HeadEffects → 파생(행운%·공격력% → 방어력) → 방어력 %증가 → 무게 패널티 → 클램프`.

효과별 실제 구현 위치:

| 효과 | 구현 위치 |
|---|---|
| 행운/회피/치명타/체력/공격력/방어력 가산 | 기존 `RunState.PartStatBonuses` + `RobotStats.ApplyBonus` |
| 파생 방어력(연극 마스크·철인 헬멧), 방어력 ％증가(방탄모) | `PartEffects.ApplyStatBonuses` |
| 체력 50% 이하 방어력(탈) | `PlayerRobotController.TakeDamage`에서 `PartEffects.GetLowHpDefBonus` |
| 근접 반사(가시 플레이트) | `EnemyUnit.MeleeAttackPlayer` 신설 — 기본 근접·차저 돌진·스프린터 대시 3경로 |
| 경험치·골드 획득량(메모리) | `StatType.ExpGain` 신설 + `RewardPickup`이 `PartEffects.GainMultiplier` 곱 |
| AI 코어 최대 레벨(메모리) | `ModdingManager.CoreMaxLevel` — **대체 → 가산**으로 변경 |
| AI 코어 시작 레벨(뉴럴 캐시) | `AiCoreManager.ApplyPartStartLevelBonus` + `RunState.CoreStartLevelGranted` |
| 디스크 1개당 / 교향곡 디스크 1개당 | `ModdingManager.RecomputePartStatBonuses` (+ `ShopManager`가 디스크 장착 후 재계산) |

**AI 코어 시작 레벨은 "런 시작"에 줄 수가 없다** — 파츠는 런 중에 부품 상자로 얻는다. 장착한 순간
아직 못 받은 만큼만 레벨을 채워주고 지급량을 `RunState.CoreStartLevelGranted`에 기록해 중복 지급을
막았다(등급을 내려도 레벨을 빼앗지는 않는다 - 이미 쓴 업그레이드를 되돌릴 방법이 없다).

**근접 반사는 접촉 근접 3경로에만 적용된다** — 스피터의 투사체(`EnemyProjectile`), 디스럭터 자폭,
보스 광역은 `MeleeAttackPlayer`를 거치지 않아 자동으로 제외된다(명세 "근접 공격 반사"를 글자 그대로 따랐다).

### 3) 무기 소켓 - 제한축과 등급 효과를 통째로 교체

- 제한축: **무기 타입(`WeaponClass` 경/중/근접) → 무기 카테고리(`WeaponType` 연사/산탄/정밀/폭발/
  에너지/근접, 범용은 제한 없음)**. 마침 `WeaponType`이 명세의 6종과 정확히 같아 새 enum이 필요 없었다.
- 등급 효과: 사거리·감지거리·회전속도 배율 → **공격속도% / 공격력(절대값·%) / 치명타% /
  스플래시% / 방어력무시%p**. `ModdingManager.SocketModifiers`를 통째로 갈아끼우고
  `GetWeaponSocketModifiers(socketIndex, weaponId)`로 시그니처를 바꿨다(카테고리 판정을 위해 무기가 필요).
- 소비 지점은 `PlayerShootManager`에서 **`HeadEffects`가 이미 같은 값을 곱하는 자리 옆**이다 —
  공격속도(쿨다운), `ComputeDamage`(공격력·치명타), 스플래시 반경, 방어력 무시(투사체·빔·근접 3경로).
- **카테고리가 안 맞으면 보정이 0**이고 무게에 x2가 붙는다(장착 자체는 막지 않는 기존 정책 유지).
  소켓의 공격력 절대값은 로봇 공격력과 같은 이유로 **투사체 수로 나눠서** 더한다(산탄총만 이득을
  몰아가지 않게).
- 폐기된 배율에 딸려 있던 코드도 함께 정리했다(`GetTravelRange`/`GetDetectRange`/`RotatePivotTowards`,
  상점의 소켓 상세 3줄).

### 4) 스탯을 데미지 파이프라인까지 전부 소수화

**원인**: `RobotStats.ApplyBonus`가 `Mathf.RoundToInt(amount)`로 **보너스를 개별 반올림**했다.
`공격력 +0.3`을 여러 개 모아도 절대 소수가 되지 않고 전부 0으로 사라진다 — 명세에 소수 수치가
대량으로 들어오는 상황에서는 치명적이다.

`int → float`으로 바꾼 것: `AggregatedRobotStats.MaxHp/Atk/Def`,
`PlayerRobotController.MaxHp/CurrentHp/Atk/Def`(+`TakeDamage`/`Heal`/`SetShieldMaxHp`/실드),
`EnemyUnit.MaxHp/CurrentHp/Atk/Def`(+`TakeDamage`, `DisruptorUnit` override),
`PlayerShootManager.ComputeDamage`와 발사 경로 4종, `Projectile.Spec.Damage`,
`BeamProjectile`, `MeleeSwing`, `EnemyProjectile`, `PlayerHitFeedback.OnHit`,
`UnlockTracker.ReportStats/ReportPlayerDamaged`(해금 조건은 정수라 안에서 내림).
`HeadEffects.ApplyStatBonuses`의 반올림 3곳과 `DiscEffectRuntime`의 회복·연쇄번개·실드 반올림도 제거했다.

부수적으로 **적의 웨이브 스탯 배율도 정확해졌다** — 예전에는 `EnemySpawner`가 `MonsterData`의 int
필드를 반올림해 곱했는데(2.5%/웨이브가 반올림에 먹히는 구간이 있었다), 이제
`EnemyUnit.ApplyWaveStatMultiplier(float)`가 Init 직후 float 스탯에 곱한다. `MonsterData`(데이터 스키마)는
그대로 int다.

표시는 `0.##`으로 통일했다(정수일 때는 소수점이 붙지 않는다) — 정비 화면 능력치, 인게임 HUD 체력,
게임오버 요약, 상점 현재 능력치. 정수로 남긴 것은 골드·경험치 수령량, 웨이브/레벨/슬롯 개수다.

### 5) 파츠 아이콘을 슬롯 공용 → 파츠별로

`PartIconLibrary`는 슬롯 단위(`PartIcons/{PartSlot}`)로만 아이콘을 찾았고 그 폴더가 아예 없어서
**코드로 그린 흰색 실루엣**이 표시되고 있었다(같은 슬롯이면 종류가 달라도 그림이 같았다).
`Get(PartData)` 오버로드를 추가해 **`iconName` → 슬롯 공용 → 실루엣** 순으로 폴백하게 하고,
정비/상점의 호출부(칸·설명 패널·상세 팝업)를 파츠 단위로 바꿨다.

아이콘 25장은 `Assets/Resources/PartIcons/`에 **영문 파일명**으로 복사했다
(`Helmet_TheaterMask`, `LegArmor_SpikePlate`, `Socket_RapidFire`, `Memory_NeuralCache`,
`DiscSlot_SymphonyCollection` 등). **Unity가 자동 생성한 `.meta`는 `textureType: 0`(Default)이라
`Resources.Load<Sprite>`가 실패한다** — 기존 UI 아이콘(`UI/Gold_icon00.png.meta`)을 템플릿으로 삼아
GUID만 각 파일의 것으로 유지하고 임포트 설정을 통째로 덮어써서 Sprite(2D and UI)로 맞췄다.

### 6) UI 골드 아이콘을 인게임 금화로 통일 (사용자 지적)

필드 드랍 픽업은 `Assets/Resources/Gold.png`(구릿빛 기계 금화, 2026-08-19 사용자 제공 아트)를,
UI는 전부 `Assets/Resources/UI/Gold_icon00.png`(노란 동전+별)를 쓰고 있었다 — 2026-08-13 UI 아트팩을
들여올 때 UI 쪽만 갈아끼우고 필드 픽업을 그대로 둔 결과다. **UI 쪽을 `Gold.png`로 맞췄다**:
코드 로드 2곳(`ShopPanelUI` 상품 칸 가격 코인, `AiCoreExtraButtonsUI` AI 코어 화면)과
`Ground01.unity`의 씬 직렬화 4곳(HUD 골드 / 정비 상단바 / 상점 헤더 / 상점 초기화 버튼).
**이미지 파일을 덮어쓰는 방식은 쓰지 않았다** — 같은 날 타이틀 배경을 덮어써서 지적받은 방식이다.
안 쓰게 된 `UI/Gold_icon00`·`Gold_icon01`은 프로젝트 관례대로 삭제하지 않고 남겼다.

### 7) 누적형 디스크의 초록 글씨를 "현재 누적 총량"으로 (사용자 지적)

사용자 리포트: "적 처치하면 공격력 늘려주는 디스크 밑에 초록글씨로 공격력 +0.05 ... 이 부분에는
총 공격력 증가 지금 몇됐는지 공격력 +4.5 이런식으로 나와야 할 것 같은데 저 숫자는 왜 있는지 잘모르겠음"
(대상: 교향곡: 화염 디스크).

정체는 `ShopPanelUI.AppendDiscStat`이 `disc.amountA`(= **처치 1마리당 증가량** 0.05)를 그대로 찍은
것이었다. `OnKillStackStat` 디스크일 때는 `RunState.DiscStackProgress`(이미 존재하는 누적치)를 읽어
**`공격력 +4.5 (처치당 +0.05 · 최대 +10)`** 형태로 바꿨다. 다른 효과 종류의 디스크는 그대로다.

### 검증 (전부 실측)

컴파일 오류 0건(소수화 과정에서 4건 발생 → 전부 수정). 플레이모드에서 `RobotStats.Compute()`를 직접
호출해 파츠별 결과를 명세와 대조했다.

- 헬멧: 연극 마스크 일반 `행운 2 / 방어력 5+2x0.15=5.3`, 전설 `행운 5 / 5.75` · 행운의 양동이 일반
  `방어력 8 / 회피 8`, 전설 `회피 12` · 방탄모 일반 `(5+4)x1.10=9.9`, 전설 `x1.20=10.8` ·
  탈 `체력 110`, 조건부 방어력은 체력 100%에서 +0 / 40%에서 +5 · 철인 헬멧 일반 `공격력 6 / 방어력 5.3`,
  전설 `공격력 12 / 5.6` · 빵봉투 `치명타 20 / 방어력 5-10 → 0(하한)`
- 다리장갑: 양철 3~5 · 경사 `회피 5+4=9` · 반응형 전설 `회피 9.5` · 가시 플레이트 반사 일반 10% / 전설 20%
- **근접 반사 실동작**: 가시 플레이트 전설(20%) 장착 후 좀비 근접 1회 →
  플레이어 `100 → 90.375`(피해 9.625 = 좀비 공격력 15 − 방어력 5.375, **소수 그대로**),
  좀비 `40 → 38`(반사 = 15x20% = 3 − 좀비 방어력 1). 표준 다리장갑 대조군은 반사 0
- 소켓 7종 전설: 연사·산탄 `공속+17% 공격력+1.5`, 정밀 `치명타+16% 공격력+1.5`,
  폭발 `치명타+16% 스플래시+22%`, 에너지 `방무+16%p 공격력+1.5`, 근접 `공격력+20% 공속+17%`,
  범용은 6개 카테고리 전부에 `공속+12% 치명타+20%`. **카테고리 불일치**(근접 소켓 + 연사 무기)는
  보정 0 + 무게 2.5 → 5
- 실제 발사 데미지: 기관단총(atk 10, 3발, 로봇 공격력 3) → 표준 소켓 `11`,
  연사 소켓 전설 `11.5`(= 11 + 1.5/3발), 치명타 시 `17.25`
- 메모리: 메모리 칩 최대레벨 65/100 · 뉴럴 캐시 시작레벨 16/36 + 경험치 x1.1/x1.3 ·
  아카식 최대레벨 65/85 + 경험치·골드 x1.05/x1.25 · 표준 메모리는 `MAX:50`(가산 전환 후에도 정상)
- 디스크 슬롯(디스크 4장 중 교향곡 2장 장착 상태): 기본 슬롯 `4~8칸 방어력 7.5` ·
  확장 프레임 `7~12칸 체력 120/152` · 코어 연결망 `5~9칸 공격력 11/31` ·
  허브 접속기 `5~9칸 방어력 17/45` · 교향곡 모음집 `6~11칸 공격력 3.2`(교향곡 2장 x 0.1)
- 정비 화면 능력치 텍스트 실측: `방어력 7.78`(실제 7.775) `회피율 7.4` `행운 2.5` — **소수점 표시 확인**
- 파츠 아이콘: 25종 전부 `Resources.Load<Sprite>` 성공, 정비 화면 인벤토리 12칸에 각각 다른 아이콘
  (`Socket_Melee`, `DiscSlot_HubAdapter`, `Memory_NeuralCache`, `Helmet_TheaterMask`, …) 부착 확인
- 골드 아이콘: HUD / 정비 상단바 / 상점 헤더 / 상점 초기화 버튼 4곳 모두 `Gold` 스프라이트
- 누적형 디스크: 누적 4.5 상태에서 `공격력 +4.5 (처치당 +0.05 · 최대 +10)` 출력 확인

**주의(검증 환경)**: Unity 에디터 창이 비활성이면 플레이모드에서도 **프레임이 진행되지 않는다**
(`Time.frameCount`가 고정). `Application.runInBackground`와 `EditorApplication.QueuePlayerLoopUpdate()`
둘 다 효과가 없었다. 그래서 `ScreenCapture.CaptureScreenshot`으로 화면을 찍는 검증은 하지 못했고,
대신 **TMP 텍스트와 `Image.sprite` 값을 직접 읽어** 검증했다(숫자 검증에는 오히려 정확하다).
스크린샷이 필요하면 에디터 창을 활성 상태로 두고 다시 실행해야 한다.

### 커밋 상태

미커밋(이전 미커밋 변경사항에 이어서).
**신규**: `PartEffect.cs`, `PartEffects.cs`, `Editor/PartsTableGenerator.cs`,
`Assets/Resources/PartIcons/*.png` 25장(+`.meta`).
**수정**: `PartData.cs`, `PartIconLibrary.cs`, `ModdingManager.cs`, `RobotStats.cs`, `StatType.cs`,
`DiscData.cs`, `PlayerRobotController.cs`, `PlayerShootManager.cs`, `EnemyUnit.cs`, `EnemySpawner.cs`,
`ChargerUnit.cs`, `SprinterUnit.cs`, `DisruptorUnit.cs`, `Projectile.cs`, `BeamProjectile.cs`,
`MeleeSwing.cs`, `EnemyProjectile.cs`, `PlayerHitFeedback.cs`, `HeadEffects.cs`, `DiscEffectRuntime.cs`,
`RewardPickup.cs`, `RunState.cs`, `AiCoreManager.cs`, `ShopManager.cs`, `ShopPanelUI.cs`,
`ModdingPanelUI.cs`, `GameHUD.cs`, `GameOverSummaryUI.cs`, `UnlockTracker.cs`,
`AiCoreExtraButtonsUI.cs`, `Assets/Data/PartsCatalog.asset`, `Assets/Scenes/Ground01.unity`.

### 남은 것 / 사용자 판단 대기

- **자기장 코어 파츠**: 명세 문서 제목에는 있으나 본문에 항목이 없어 기존 임시 파츠를 그대로 두었다.
  명세가 오면 `PartsTableGenerator.ManagedSlots`에 `MagneticCore`를 추가하고 `Defs`에 넣으면 된다.
- **팔 장갑 / 다리 슬롯**도 아직 임시 파츠다(명세 없음).
- 화면 캡처 검증(정비/상점 화면 눈으로 확인)은 에디터 창 활성 상태에서 다시 해야 한다.

## 2026-08-20 (이어서) 한글 폰트 아틀라스 글리프 누락 수정 + 점수/버튼 겹침 재검증

에디터 창을 다시 활성화한 뒤 FHD 캡처로 이전 작업을 재검증하다가 발견한 실제 버그 1건을 고치고,
같은 캡처에서 의심됐던 문제 1건은 재현 방법의 오류였음을 확인했다.

### 1) 한글 글리프 일부가 렌더되지 않는 버그 (실제 버그, 수정함)

캡처에서 "상점"이 "점"으로, "(클릭 = 상세)"가 "(클릭 = 세)"로, "상점 초기화"가 "점 초기화"로,
"부품 상자 12개를 열어..."가 "자 12개를 열어..."로 보였다. 글자가 있어야 할 자리는 정확히
비어 있고(폭은 잡힘) 그림만 없는 형태라 `TMP_Text.text` 값 자체는 정상이었다 - 코드로
`HasCharacter()`를 찍어봐도 True가 나와서 숫자 검증만으로는 잡히지 않고 캡처를 봐야 드러난다.

**원인**: `NotoSansKR-Regular SDF`가 Dynamic 폰트인데, 그동안 플레이 중 여러 세션을 거치며 만난
글자를 아틀라스에 그때그때 구워 넣다가 **1024x1024 한 장을 넘겨 2번째 텍스처로 넘어간 상태로
에셋이 저장돼 있었다.** 글리프 테이블에는 각 글자의 위치 기록이 남아 있지만, 그 기록이 가리키는
2번째 텍스처 쪽 픽셀이 비어 있어(또는 다른 시점에 롤백되어) 해당 글자만 그림 없이 렌더링됐다.

**수정**: 신규 `Assets/Scripts/Editor/FontAtlasMaintenance.cs` -
메뉴 `Comstock/한글 폰트 아틀라스 초기화 (2048)` / `Comstock/한글 폰트 아틀라스 상태 확인`.
(1) 아틀라스 크기를 1024 → **2048**로 올려 글리프가 한 장에 다 들어가게 하고(2번째 텍스처 자체가
안 생기게), (2) `ClearFontAssetData(true)`로 동적으로 쌓인 글리프/아틀라스를 비운다(Dynamic
폰트라 다음 렌더에서 필요한 글자를 다시 굽기 때문에 손실이 없다), (3) `Clear Dynamic Data on Build`를
켜서 빌드에 어긋난 아틀라스가 실려 나가지 않게 한다. `NotoSansKR-Bold SDF`(사용은 안 하지만 같은
구조라 함께)도 같은 처리를 한다.

**검증**: 실행 전 `NotoSansKR-Regular SDF: 1024x1024, 텍스처 2장, 글리프 171개 ⚠`,
실행 후 `2048x2048, 텍스처 1장, 글리프 0개`(비운 뒤 상태). 플레이모드에서 상점/정비 화면을 다시
캡처해 "상점" / "(클릭 = 상세)" / "상점 초기화 - 10" / "부품 상자 12개를 열어..." 전부 정상
표시됨을 확인했다. **글자가 다시 안 보이면 이 메뉴를 재실행하면 된다**(재발 시 대응 수단을 코드로 남겼다).

### 2) 상점 화면 "정비 종료" 버튼과 점수가 겹치는 것처럼 보였던 문제 (재현 안 됨 - 실제 버그 아님)

이전 검증에서 `ShopPanelUI.Open()`을 **`GameFlowManager`를 거치지 않고 직접 호출**해서
캡처했는데, 이때 `GameFlowManager.IsIntermission`이 여전히 `False`인 상태였다. `ScoreHudUI`는
`!IsIntermission && !GameOver && !GameWon && !Paused`일 때만 보이도록 이미 설계돼 있어서
(`ScoreHudUI.cs` `LateUpdate`), 이 상태에서는 상점 화면 위에 점수 HUD가 함께 그려져 "정비 종료
8,884"처럼 겹쳐 보인 것이다(폰트 버그로 "종료"의 "종" 글자도 비어 있어 더 헷갈리게 보였다).

**실제 진입 경로로 재현 시도**: `GameFlowManager.HandleWaveEnded()` → `EnterPostWaveIntermission()`
(`IsIntermission=true`) → 정비 화면 → `HandleModdingProceedRequested()` → `ShowShop()`까지
전부 실제 핸들러를 리플렉션으로 호출해 재현했다. **이 경로에서는 `IsIntermission`이 처음부터
끝까지 `true`로 유지**되고, 상점 화면 캡처에도 점수 HUD가 보이지 않아 겹침이 없었다.
`grep`으로 확인한 결과 `shopPanel.Open()`을 호출하는 곳은 `GameFlowManager` 한 곳뿐이라
실제 플레이에서는 이 경로만 탄다. **결론: 실제 게임에서는 재현되지 않는, 이전 검증 스크립트가
정식 상태 전환을 건너뛴 데서 나온 허상이었다** - 코드 수정 없음.

### 커밋 상태

미커밋. 신규 `Assets/Scripts/Editor/FontAtlasMaintenance.cs`, 수정
`Assets/Fonts/NotoSansKR/NotoSansKR-Regular SDF.asset` / `NotoSansKR-Bold SDF.asset`
(아틀라스 크기·동적 데이터만 바뀜, 다른 설정 변경 없음).

## 2026-08-20 (이어서) 부품 상자 드랍 크기 2배 + 맵 밖으로 나가는 버그 수정

사용자 요청 2건: "인게임 드롭되는 부품 상자 크기 가로세로 2배로 늘려줘",
"가끔 상자가 맵 밖으로 나가는데, 상자가 맵 밖으로 안나가게 만들어".

### 1) 부품 상자만 2배 확대 (골드·경험치는 그대로)

`RewardPickupManager.VisualWorldSize`(0.6)는 골드·경험치·부품상자 **세 종류가 공유**하는 상수라
그대로 올리면 셋 다 커진다. 부품 상자만 키우기 위해 `PartBoxVisualWorldSize = VisualWorldSize * 2f`를
추가하고, `GetVisualWorldSize(type)`가 타입별로 갈라 돌려주도록 `CreateVisual()`을 고쳤다.

실측: `RewardPickupManager.SpawnReward`로 세 종류를 직접 스폰해 `SpriteRenderer.bounds` 확인 -
부품 상자 `1.2 x 1.2`(기존 0.6의 2배), 골드·경험치는 그대로 `0.6 x 0.6`.

### 2) 보상 픽업이 맵 밖으로 나가는 버그

`SpawnReward()`가 겹쳐 보이지 않도록 위치에 반경 0.6유닛의 무작위 산개(scatter)를 더하는데,
몬스터가 **맵 경계 근처**에서 죽으면 이 산개가 맵 밖으로 밀어낼 수 있었다(부품 상자뿐 아니라
골드·경험치도 같은 경로라 이론상 셋 다 영향받는다 - 부품 상자가 화면 밖 화살표 UI로 눈에 띄어
가장 먼저 리포트된 것으로 보인다).

플레이어·카메라를 맵 안에 가두는 데 이미 쓰이던 `MapBounds.ClampPosition(position, margin)`
(`Assets/Scripts/MapBounds.cs`)을 그대로 재사용해 `SpawnReward()`의 산개 적용 직후 위치를 맵 안으로
접었다. `margin`은 아이콘 절반 크기(`GetVisualWorldSize(type) * 0.5f`)로 줘서 아이콘이 화면
가장자리에서 반쯖 삐져나오지 않게 했다. `MapBounds`가 맵을 못 찾으면(다른 씬 등) 기존과 동일하게
제한 없이 통과한다.

**검증**: 맵 경계(`extents=(19.16, 10.76)`) 바로 안쪽(0.05유닛)에서 8회 연속 스폰 →
**맵 밖으로 나간 개수 0개**(반경 0.6 산개를 감안하면 원래 코드로는 상당수가 밖으로 나갔을 위치).

### 커밋 상태

미커밋. 수정 `Assets/Scripts/RewardPickupManager.cs`(타입별 크기 분기 + `MapBounds.ClampPosition` 적용).

## 2026-08-20 (이어서) 데미지 숫자 팝업(치명타 아이콘 포함) + VICTORY 화면 완전 제거

사용자 요청 2건: "적이나 아군 피격시에 데미지 몇 터지는지 잠깐 보이게 만들어줘 + 치명타는 조금
다르게 치명타 아이콘도 표시", "게임 클리어하면 victory 글씨 가운데에 뜨면서 아무것도 안눌러지는데
걍 글씨하고 이 시스템 자체를 없애고 클리어 명세만 나오게 만들어"(+ "무한모드 들어가면 이거때문에
영원히 해당부분 클릭이 안되고 없어지지도 않음").

### 1) 데미지 숫자 팝업 (신규 `DamageNumberUI.cs`)

맞은 자리 위로 숫자가 잠깐(0.7초) 떠올랐다 사라진다 - `DashGaugeUI`/`PlayerHitFeedback`과 같은
방식으로 씬에 배치하지 않고 코드로 만들며, 캔버스가 ConstantPixelSize라 매 프레임
`cam.WorldToScreenPoint`로 화면 좌표를 구해 위치를 맞춘다(해상도가 바뀌어도 정확).
겹쳐 보이지 않도록 약간의 무작위 산개(RewardPickupManager와 같은 방식)를 더했고, 표시 자릿수는
`0.##`(스탯 소수화와 같은 관례 - 반올림하면 작은 데미지가 사라진다).

- **플레이어가 적에게 입힌 피해**: 흰색(일반) / **치명타는 주황색 + 더 큰 글자 + 옆에 별 모양
  아이콘**. 아이콘은 전용 아트가 없어 `PartIconLibrary.BuildPlaceholder`와 같은 방식으로
  극좌표 공식(r(θ) = R x (0.30+0.70x|cos2θ|^0.6))으로 4방향 스파클을 코드에서 그려 캐시한다.
- **적이 플레이어에게 입힌 피해**: 빨간색(치명타 개념 없음).
- 치명타 여부를 실제 피해가 적용되는 지점(`EnemyUnit.TakeDamage`)까지 흘려보내야 해서, 무기
  발사 파이프라인 전체에 `isCrit` 한 값을 새로 꿰었다 - `PlayerShootManager.ComputeDamage`가
  `out bool isCrit`를 추가로 돌려주고, `Projectile.Spec.IsCrit` / `BeamProjectile`(발사 시점에
  1번만 굴린 값을 모든 틱이 공유) / `MeleeSwing.Execute` / `WeaponRuntimeState.melee_is_crit`를
  거쳐 최종적으로 `EnemyUnit.TakeDamage(..., bool isCrit)`에 도달한다. `DisruptorUnit`의
  override 시그니처도 함께 맞췄다. 실제 피해 적용 지점(`EnemyUnit.TakeDamage`/
  `PlayerRobotController.TakeDamage`)에서만 팝업을 띄우므로 회피·구르기 무적처럼 피해가 실제로
  없었던 경우는 뜨지 않는다.
- 정지된 화면에서 캐릭터 스탯 텍스트가 잘 보이도록 `DamageNumberUI.ResetCache()`를
  `PlayerRobotController.Awake()`의 다른 static 캐시 초기화와 함께 등록했다.

**검증**: 플레이모드에서 좀비에게 `12.34`(방어력 1 반영 후 `11.34`, 흰색) / `55.7`(치명타,
방어력 반영 후 `54.7`, 주황+아이콘) 피해를 직접 주고, 플레이어에게 `8.2` 피해(방어력 반영 후
`3.2`, 빨강)를 준 뒤 캡처 - 세 색상·아이콘 전부 화면에 정확히 표시됨을 확인했다.

### 2) VICTORY 화면 완전 제거 (실제 버그 수정)

**원인**: 씬의 `Victory` TextMeshProUGUI 오브젝트가 화면 중앙(694x454px)에 `raycastTarget: 1`로
떠 있었다. `GameHUD.HandleGameWon()`이 `GameWinManager.OnGameWon` 발생 시 이 오브젝트를
`SetActive(true)`로 켜는데, **`GameWinManager.TriggerWin()`은 런 전체에서 단 한 번만 호출되고
그 뒤로 다시 끌 계기가 코드에 없었다** - 첫 클리어 후 무한모드로 넘어가도 이 텍스트가 화면
중앙에 계속 남아, `ScoreSummaryPopup`("계속 진행"/"타이틀로")과 그 뒤에 이어지는 웨이브들의
UI 클릭을 그 자리에서 영구히 가로챘다(사용자 리포트: "무한모드 들어가면 이거때문에 영원히
해당부분 클릭이 안되고 없어지지도 않음").

**수정**: `GameHUD.cs`에서 `victoryObject` 필드·활성화 코드·`HandleGameWon` 메서드·
`GameWinManager.OnGameWon` 구독을 **전부 제거**했다(사용자 지시: "이 시스템 자체를 없애고").
씬의 `Victory` 오브젝트는 지우지 않고(프로젝트 관례) `raycastTarget`만 0으로 꺼서 혹시 다른
경로로 다시 켜지더라도 클릭을 막지 못하게 방어선을 하나 더 뒀다. 승리 시에는 이제
`GameFlowManager.HandleGameWon()`이 띄우는 `ScoreSummaryPopup`(클리어 명세)만 뜬다.

**검증**: `GameWinManager.TriggerWin()`을 직접 호출해 재현 - Victory 오브젝트는 계속
`active=False`로 유지되고, `ScoreSummaryPopup`이 정상적으로 뜨며 화면 캡처에서 "계속 진행"/
"타이틀로" 버튼이 어떤 텍스트에도 가려지지 않고 그대로 보였다.

### 커밋 상태

미커밋. 신규 `Assets/Scripts/DamageNumberUI.cs`. 수정
`Assets/Scripts/EnemyUnit.cs`, `DisruptorUnit.cs`, `Projectile.cs`, `MeleeSwing.cs`,
`BeamProjectile.cs`, `PlayerShootManager.cs`, `PlayerRobotController.cs`, `GameHUD.cs`,
`Assets/Scenes/Ground01.unity`(Victory 텍스트 raycastTarget 끔).

## 2026-08-20 (이어서) 데미지 숫자 팝업 - 픽셀 단위 오프셋 + 좌우 낙하

사용자 요청 2건: "숫자는 좌표 1~10픽셀씩 랜덤으로 어긋나게 만들어",
"숫자는 올라가면서 사라지는게 아닌 왼쪽이나 오른쪽으로 떨어지면서 사라지도록 만들어".

`DamageNumberUI.cs`(직전 항목에서 신설) 애니메이션을 교체했다.

- **오프셋**: 기존에는 월드 유닛(±0.18) 기준으로 겹침 방지 산개를 넣었는데, 이제는 **화면
  픽셀 좌표에 직접** 1~10px 크기의 랜덤 오프셋을 더한다(방향도 무작위) - `cam.
  WorldToScreenPoint`로 계산한 화면 위치에 이 오프셋을 얹는 식이라 카메라 줌·해상도와
  무관하게 항상 1~10px만큼만 어긋난다.
- **낙하 애니메이션**: 기존의 위로 떠오르는 `RiseSpeed`(등속)를 없애고, 스폰 시점에
  **왼쪽/오른쪽 중 한 방향을 무작위로 고정**해 그쪽으로 등속 이동하면서, 동시에 아래로
  **중력 가속**(`FallGravity`, 시간이 지날수록 더 빠르게 떨어진다)으로 떨어지도록 바꿨다.
  좌우로 흔들리지 않고 한쪽으로만 흐르며, 페이드 아웃 타이밍(수명의 45% 지점부터)은
  그대로 유지했다.

**검증**: 필드를 정지(적 스포너·플레이어 사격 정지 + `Time.timeScale=0`)해 두고 일반/치명타/
피격 팝업 3개를 스폰한 뒤, 리플렉션으로 "0.4초가 지난 상태"를 직접 계산해 대입하고
(`world_pos.x += side x SideSpeed x t`, `world_pos.y -= 0.5 x Gravity x t^2`) 캡처했다 -
세 팝업이 각자 정해진 방향으로 좌/우 이동 + 하강한 값이 코드의 적분식과 정확히 일치했고,
캡처에서도 각각 다른 방향으로 흩어져 떨어지는 모습을 확인했다.

### 커밋 상태

미커밋(직전 `DamageNumberUI.cs` 신설에 이어지는 수정).

## 2026-08-20 (이어서) 타이틀 설정 버튼을 씬 오브젝트로 전환

사용자 리포트: "타이틀에 설정 버튼 너무 좌우로 김. 다른 버튼과 맞춰야함. 그리고 하이라키에
있어야함. 플레이에서만 나타남".

**원인**: `TitleSceneManager.AttachSettingsButton()`이 설정 버튼을 정규화 앵커 범위
(0.36~0.64, 0.05~0.10 - 화면의 28%x5%)로 코드에서 매 실행마다 새로 만들었다. 반면
게임시작/종료 버튼은 씬에 **고정 픽셀 크기**(320x90, 320x70)로 미리 배치돼 있다 - 서로
다른 방식이라 크기가 안 맞았고(가로로 훨씬 넓고 낮음), 코드 생성 오브젝트라 플레이 모드에
진입해야만 하이라키에 나타났다.

**수정**: 씬의 `QuitButton`을 복제해 `SettingsButton`으로 만들고(같은 크기 320x70,
같은 앵커 방식), 종료 버튼 바로 아래(y=0.04)에 배치, 라벨을 "설정"으로 바꿨다 - 이제
게임시작/종료/설정 세 버튼이 정확히 같은 크기로 나란히 정렬되고 에디트 모드에서도 항상
하이라키에 보인다. `TitleSceneManager.cs`는 코드로 버튼을 만드는 대신 `[SerializeField]
private Button settingsButton`으로 씬의 버튼을 받아 `SettingsPanelUI`만 연결한다
(`AttachSettingsButton()`이 오브젝트 생성 코드 없이 리스너 연결만 하도록 축소됨).

**검증**: 플레이모드 캡처로 세 버튼이 같은 크기·간격으로 정렬됨을 확인, 설정 버튼 클릭 시
`SettingsPanelUI`(환경 설정 패널)가 정상적으로 열리는 것도 확인. 에디트 모드에서
`Canvas/SettingsButton`이 하이라키에 항상 존재함을 확인.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/TitleSceneManager.cs`, `Assets/Scenes/Title.unity`
(SettingsButton 신규 배치, QuitButton 복제 기반).

## 2026-08-20 (이어서) 도감에 무기 13종 추가

사용자 요청: "도감에 무기도 추가해주세요" -> "무기는 해금 조건 없으니 도감에 명세만
추가하면 됨"(확인 질문에 대한 답).

기존 도감(UnlockCategory: Head/Disc/Accessory)에는 무기가 없었다 - 2026-08-19
해금 기획서 반영 범위(머리 12/디스크 21/악세사리 6)에 원래 없던 항목이었을 뿐, 명시적으로
제외된 적은 없었다(맵만 "아트 없어서 제외"라고 명시돼 있었다).

### 구현

- `UnlockCategory`에 `Weapon` 추가.
- `UnlockCatalog.All`에 무기 13종을 추가 - **전부 counterKey를 비워 초기 해금**으로 등록했다
  (`UnlockEntry.UnlockedFromStart`가 이미 "조건 없이 항상 해금"을 지원하는 기존 메커니즘이라
  별도 코드 없이 그대로 재사용됨). itemId는 각 종류의 일반 등급 행 ID
  (`300000 + 종류(1~13)x100 + 1` - `WeaponTableGenerator.MakeWeaponId(kind, 0)`과 동일 체계,
  다만 UnlockData.cs는 런타임 코드라 에디터 전용 함수를 호출할 수 없어 값을 직접 적었다).
- `CollectionPanelUI`: 탭 배열에 Weapon 추가(4번째 탭, 폭 계산은 기존 코드가 이미
  일반화돼 있어 자동 대응). ResolveName/ResolveIcon에 분기 추가 -
  이름은 GameDataManager.Instance.Weapons, 아이콘은 weapon_rgwpimg(손에 드는 이미지)를
  그대로 재사용한다(ShopPanelUI.ResolveWeaponIcon과 같은 관례 - 전용 아이콘 아트 불필요).
- **무기 탭의 상세 패널은 "해금 조건" 대신 실제 무기 스탯을 보여준다** - 사용자가 원한
  "명세"가 조건 문구가 아니라 스탯이라고 판단해, conditionTitle을 "무기 정보"로 바꾸고
  분류(무기 타입· 투사체 타입)/공격력/공격속도/사거리/감지거리/발사 방식과, 있을 때만
  스플래시 반경·방어력 무시·동시 발사 수를 보여주는 BuildWeaponSpecText()를 신설했다.
  값은 **데이터 원본값**이다 - 도감은 특정 로봇/소켓을 전제하지 않아 상점 상세 팝업처럼
  "실제 적용값"을 계산할 수 없다.

### 검증

플레이모드에서 도감을 열어 무기 탭(13/13, 전부 해금 표시)을 확인하고, 중기관총(연사화기 x
중무장, 공격력 9 x 공격속도 10/초 x 사거리 5.23 x 감지거리 4.49 x 투사체)과 로켓런처(폭발화기,
공격력 75 x 사거리 5.23 x 감지거리 3.59 x 스플래시 반경 2.4) 두 종의 상세 패널이 데이터
(Assets/Data/GameDataAsset.asset)와 정확히 일치함을 확인했다. 13개 무기 아이콘 전부
정상 로드됨을 캡처로 확인.

### 커밋 상태

미커밋. 수정 Assets/Scripts/UnlockData.cs, Assets/Scripts/CollectionPanelUI.cs.

## 2026-08-20 (이어서) 몬스터 5종(스피터/스프린터/디스럭터/리더/보스) 이동 애니메이션 적용 `[사후 기록]`

> 이 항목은 커밋(`e794268 a.260820.02`) 당시 작업.md에 기록되지 않고 넘어갔던 것을
> 2026-08-21 뒤늦게 커밋 diff를 근거로 재구성해 적었다. 실시간 검증 기록(캡처 등)이 남아
> 있지 않아 "검증" 항목은 코드 근거만 있고 실제 플레이 확인은 별도로 필요하다.

사용자가 스피터·스프린터·디스럭터·리더·보스("좀비 군집체")의 이동 모션 8프레임씩과
보스·디스럭터·리더·스피터·스프린터의 신규 정지 스프라이트를 제공했다(보스몬스터기획서 Ver01
포함). 기존에는 기본 좀비(200001)만 전용 8프레임 걷기 애니메이션이 있었고(2026-08-12),
나머지는 전부 정지 스프라이트 고정이었다.

### 구현

- 신규 `Assets/Scripts/MonsterAnimationLibrary.cs` — 그동안 `EnemyUnit` 안에 기본 좀비
  전용으로 하드코딩돼 있던 `ZombieMoveFrames` 캐시 로직을 **몬스터ID → 프레임 세트(`Clip`)**
  조회 테이블로 일반화했다. 폴더명 단위로 스프라이트를 캐시하고(`GetByMonsterId`/
  `GetByFolder`), 세트마다 "멈췄을 때 보여줄 프레임"(`StillFrameIndex`, -1이면 프리팹의
  원래 정지 스프라이트 유지)과 "기준 이동속도에서의 FPS", "정지 중에도 계속 재생할지"
  (`PlayWhileIdle`)를 개별 지정한다.
  - 등록: 좀비(`ZombieMove`, StillFrameIndex=-1 유지) / 스피터(`SpitterMove`, fps 6) /
    스프린터(`SprinterMove`, fps 14 - "네 발로 기는 질주 사이클"이라 높게 잡음) /
    디스럭터(`DisruptorMove`, fps 6) / 리더(`LeaderMove`, fps 7).
  - **차저(200002)는 사용자가 애니메이션을 제공하지 않아 의도적으로 목록에서 제외**했다 -
    프리팹의 정지 스프라이트(`Charger.png`)를 그대로 쓴다.
- `EnemyUnit.cs`: `UpdateWalkAnimation()`/`CanPlayMoveFrames()`가 `MoveClip`(가상 프로퍼티
  `ResolveMoveClip()`으로 조회)을 쓰도록 교체. 프레임 세트는 `Init()`으로 MonsterId가
  들어온 뒤에야 정해지므로 Awake가 아니라 첫 사용 시점에 지연 조회한다(`move_clip_resolved`).
  `ResetStaticCaches()`에 `MonsterAnimationLibrary.ResetCache()`를 추가해 씬 재로드로
  Resources가 언로드된 뒤에도 스테일 캐시를 안 쓰게 했다.
- `BossUnit.cs`: 보스는 `WaveManager`가 데이터테이블 밖에서 `monster_id = -1`로 만들어
  넘기므로 몬스터ID 조회가 불가능해 `ResolveMoveClip()`을 override해 폴더명
  (`MonsterAnimationLibrary.BossFolder` = `"BossMove"`)을 직접 지정했다. 보스 세트는
  보행이 아니라 **제자리 꿈틀거림**이라 `playWhileIdle: true`로 둬서 정지 시에도 계속
  재생되게 했다(멈추면 죽은 것처럼 보이는 문제 방지), `idleMotionFps`(기본 5)로 속도
  조절 가능.
- 프리팹 5종 갱신(`Assets/Prefebs/Boss.prefab`, `Disruptor.prefab`, `Leader.prefab`,
  `Spitter.prefab`, `Sprinter.prefab`): 정지 스프라이트를 신규 아트로 교체.
  **보스는 이번에 처음으로 전용 아트가 생겼다** - 그 전까지 좀비 스프라이트를 확대(스케일
  0.2012805) + 붉게 물들인(`m_Color` r1/g0.35/b0.35) 임시 비주얼이었는데, 전용 "좀비
  군집체" 스프라이트로 교체하며 색조도 원래 색(흰색)으로 되돌리고 스케일을 0.5로,
  콜라이더도 (8, 6.6, center y=-1.7) → (6, 3, center y=-1.83)으로 실측 재조정했다.
- `WaveManager.cs`: 보스 프리팹 필드 툴팁을 "전용 아트 없음" → "2026-08-20부터 전용 아트
  '좀비 군집체' 사용"으로 갱신(코드 동작 변경 없음).

### 검증

커밋 당시 별도 기록 없음(위 안내 참고). 다음 세션에서 플레이모드 캡처로 5종 각각의
걷기/제자리 모션이 규격(픽셀 크기)을 유지한 채 재생되는지, 차저는 여전히 정지 스프라이트인지
확인이 필요하다.

### 커밋 상태

`e794268 a.260820.02`로 이미 커밋됨. 신규 `Assets/Scripts/MonsterAnimationLibrary.cs`,
수정 `Assets/Scripts/EnemyUnit.cs`, `BossUnit.cs`, `WaveManager.cs`, 프리팹 5종
(`Boss`/`Disruptor`/`Leader`/`Spitter`/`Sprinter`), 신규 리소스 `Assets/Resources/
BossMove`, `SpitterMove`, `SprinterMove`, `DisruptorMove`, `LeaderMove`(각 8프레임).

## 2026-08-21 웨이브 종료 시 데미지 숫자/보스 텔레그래프가 화면에 계속 남는 버그 수정

사용자 리포트: "웨이브 종료 후 정비 화면 넘어갈때 데미지 숫자나 이펙트 같은거 안없어지고
화면에 계속 남아있기도 한데 이거 다 없애줘야해".

코드를 조사해 서로 다른 원인 2건을 찾았다(둘 다 실제 재현 가능한 버그).

### 1) DamageNumberUI - 정지 화면에서 얼어붙는 버그

`DamageNumberUI.Update()`가 `Time.deltaTime`으로 `elapsed`를 늘려 `Lifetime`(0.7초)에
도달하면 스스로 `Destroy()`하는 구조인데, 웨이브가 끝나 `GameFlowManager`가
`Time.timeScale = 0`으로 만드는 순간 `Time.deltaTime`도 0이 되어 `elapsed`가 더 이상 늘지
않는다 - 그 시점에 떠 있던 팝업은 Lifetime에 영영 못 미쳐 정비/상점 화면 위에 그 프레임
그대로 얼어붙은 채 계속 남는다(다음 웨이브가 시작해 timeScale이 1로 돌아와야 뒤늦게
애니메이션이 이어지다 사라진다 - 그동안 정비 화면 내내 보인다).

수정: `PlayerHitFeedback.Update()`가 이미 쓰고 있던 관례(런타임 생성 오브젝트는 스스로
`GameFlowManager.IsIntermission`을 체크해 정리한다)를 그대로 적용했다.
`DamageNumberUI.Update()` 맨 앞에서 `IsIntermission`이면 곧바로 `Destroy(gameObject)`.

### 2) HitFlash - 같은 이유로 하얗게 물든 채 얼어붙는 버그

`HitFlash.Update()`도 `Time.deltaTime`으로 `flash_time_left`를 줄이는 구조라 같은 문제를
겪는다. 적은 대부분 웨이브 종료와 함께 파괴되지만 플레이어는 계속 남아있어, 정비 진입
직전에 피격했다면 로봇이 하얗게 물든 채로 정비 화면 배경에 얼어붙을 수 있었다. 같은
방식으로 `IsIntermission`이면 즉시 `flashing = false; SetFlashAmount(0f);` 처리하도록 수정.

### 3) BossUnit의 광역 공격 경고 원(텔레그래프) - 진짜 화면 누수

`BossUnit.CreateTelegraphVisual()`이 만드는 BossAoeTelegraph 오브젝트는 보스의 자식이
아닌 독립 GameObject이고(월드 좌표를 직접 잡으려고), `PerformAoeAttack()` 코루틴이
`WaitForSeconds(aoeTelegraphDuration)` 뒤에 `Destroy(telegraph)`로 스스로 지우는 구조다.
그런데 모든 웨이브 종료(`WaveManager.EndWave()`/`WinGame()`)가 남은 적을
`EnemySpawner.DespawnAllAliveEnemies()`로 `Die()`를 거치지 않고 그냥 `Destroy()`해버린다
- 보스가 텔레그래프를 띄운 채로 이 경로를 타면 코루틴이 중간에 끊기고, 이미 만들어진
원은 임자를 잃은 채 영원히 화면에 남는다(정비/상점 화면 뒤에서 계속 렌더링됨). 매 웨이브
반복 가능한 재현성 있는 누수다.

수정: `EnemyUnit.OnDestroy()`를 `protected virtual`로 열고, `BossUnit`이
`active_telegraph` 필드로 현재 떠 있는 텔레그래프를 추적하다가 `OnDestroy()`에서 override해
남아있으면 확실히 `Destroy()`한다(정상 종료 경로와 별개로 안전망을 하나 더 둔 것 - 정상
경로는 그대로 자기 스스로 지운다).

### 검증

Play 모드에서 리플렉션으로 직접 재현했다. 보스 텔레그래프: `BossUnit.PerformAoeAttack()`을
강제 호출해 텔레그래프 오브젝트 생성을 확인한 직후(WaitForSeconds가 단 한 프레임도
지나기 전) `Destroy(boss.gameObject)`로 보스를 파괴 - 수정 전이라면 영구 누수될 상황을
만들고, 이후 씬에서 `GameObject.Find("BossAoeTelegraph")`로 조회해 누수 없이 정상
정리됨을 확인했다. DamageNumberUI/HitFlash는 PlayerHitFeedback과 동일한 검증된 패턴을
그대로 재사용한 것이라 별도 실측 대신 코드 리뷰로 갈음했다.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/DamageNumberUI.cs`, `HitFlash.cs`, `EnemyUnit.cs`, `BossUnit.cs`.

## 2026-08-21 행운(Luck) 등급 확률 보정 1/10로 완화

사용자 리포트: "행운 조금만 올려도 고등급 무기가 미친듯이 많이 나오는데 왜이럼 지금의
1/10으로 효과 체감되어야 할거같은데".

`LuckBonus.cs`(2026-08-19 신설, 당시에도 "체감이 거의 없는 수준"을 목표로 잡았었다)의
가중치 배율 상수 `PerTenLuckPerGrade`를 0.01(행운 10당 +1%)에서 0.001(행운 10당 +0.1%)로
10배 낮췄다. 상한(`LuckCap` = 50)은 그대로 유지 - 행운을 상한까지 채워도 전설 등급
가중치는 이제 3에서 3.06(+2%)에 그친다(기존에는 3에서 3.6, +20%).

이 상수는 `ShopCatalog.RollGrade`(상점 무기/디스크 등급)와 `PartsCatalog.RollBoxGrade`
(부품 상자 등급) 둘 다에서 공유하므로 두 경로 모두 함께 완화됐다.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/LuckBonus.cs`.

## 2026-08-21 리더 좀비 개체수 제한 + 무리 동반 기믹 삭제 + 회복 지원기 대체 + 체력 상향

사용자 지시 2건(연속 요청):
1. "지금 리더 너무 많이 나오니까 개체수 조절해. 20웨이브 이전에는 1 웨이브에 3마리 이상
   나오는 일은 없어야 해. 대신 다른 좀비들을 더 많이 스폰하는걸로 난이도 조절해. 그리고
   이에 맞춰서 리더 좀비 체력 늘려줘."
2. "리더 지금은 다른 좀비 무리 이끄는 기믹으로 되어있는데, 그 기믹 자체를 제거하고, 다른
   기믹으로 대체할거야. 앞으로 좀비는 다른 좀비가 따라다니지 않음. 대신 리더 좀비는 3초마다
   주변 좀비 최대 3마리를 20%씩 회복하게 만들어."

### 1) 웨이브당 리더 등장 상한 (20웨이브 미만 최대 3마리)

`EnemySpawner`에 `leaderCapWaveThreshold`(기본 20) / `leaderMaxPerWave`(기본 3) 신설,
웨이브별 등장 수는 `leaders_spawned_this_wave` 카운터로 추적하고 `SetCurrentWave()`(매
웨이브 시작 시 `WaveManager.BeginWave()`가 호출)에서 0으로 리셋한다 - 웨이브 단위 상한이
정확히 웨이브 경계에서 초기화된다.

`SpawnLoop()`에서 가중 추첨(`PickWeightedMonsterId()`) 결과가 리더인데 상한에 걸렸으면,
그 스폰 슬롯을 비우지 않고 리더를 뺀 후보로 즉시 재추첨한다
(`PickWeightedMonsterId(excludeLeader: true)` 신설 오버로드). 이렇게 하면 리더가 빠진 자리를
자동으로 다른 좀비(기본 좀비/차저/스피터 등)가 채우게 되어, 별도의 "다른 좀비 스폰량 증가"
로직 없이도 사용자가 요청한 "리더 대신 다른 좀비를 더 많이 스폰"이 자연스럽게 달성된다
(전체 스폰 물량 자체는 그대로 유지 - 난이도가 갑자기 물러지거나 세지지 않는다). 20웨이브
이상(보스 웨이브 포함 엔드리스)은 상한 없이 기존 가중 추첨을 그대로 따른다.

몬스터ID가 리더인지 판별은 하드코딩(200006) 대신 기존 `MonsterComponentTypes`(몬스터ID→
행동 컴포넌트) 표를 재사용하는 `IsLeaderMonsterId()`로 뽑아냈다 - 리더 판별 로직이 이제
한 곳에만 존재한다.

### 2) 무리(팔로워) 동반 스폰 기믹 완전 삭제

`EnemySpawner.SpawnLeaderPack()`(리더 스폰 시 팔로워 좀비 3마리를 반경 안에 흩어 함께
스폰하던 메서드)과 관련 필드(`leaderPackMonsterId`/`leaderPackSize`/`leaderPackSpawnRadius`)를
전부 제거했다. `LeaderUnit.SetPack()`/`pack` 목록도 삭제 - 이제 어떤 몬스터도 다른
몬스터를 대동하고 스폰되지 않는다(사용자 지정: "앞으로 좀비는 다른 좀비가 따라다니지 않음").
`TrickleSpawnLoop()`의 관련 주석(리더 팔로워가 상한을 우회해서...)도 함께 정리했다
(그 코루틴 자체는 원래도 리더/팔로워를 뽑지 않았으므로 동작 변화 없음).

### 3) 신규 기믹 - 주변 좀비 광역 회복(3초마다 최대 3마리, 20%)

`LeaderUnit.cs`를 재작성했다. `Update()`에서 3초(`healInterval`)마다
`HealNearbyZombies()`를 호출 - 반경(`healRadius`, 기본 6유닛) 안에서 자신을 뺀 살아있는
좀비 중 체력이 덜 찬 유닛만 후보로 모아 가장 가까운 순으로 최대 3마리(`healTargetCount`)를
골라 최대체력의 20%(`healPercent`)씩 회복시킨다. 대상 판정에 좀비 종류를 구분하지 않는다
(디스럭터·차저 등도 포함 - "주변 좀비"를 이 프로젝트의 관례대로 "주변 적 전체"로 해석했다).

체력 회복 API가 없어 `EnemyUnit.Heal(float amount)`를 새로 추가했다(`MaxHp`를 넘지 않게
`Mathf.Min` 클램프, 체력바 갱신까지 처리, 죽은 유닛에는 적용 안 함).

### 4) 리더 체력 상향 (343에서 700으로, 약 2배)

등장 빈도가 크게 줄고 무리 호위도 없어지는 만큼, 마주쳤을 때의 위협감을 유지하려고
`GameDataAsset.asset`의 리더(200006) monster_hp를 343에서 700으로 올렸다(정확한 배율
지정은 없었어 보스 체력 12000 대비 6%대로 여유 있게 잡은 값 - 필요하면 인스펙터에서
더 조정 가능). 공격력/방어력/이동속도 등 다른 스탯은 그대로 유지.

### 검증

Play 모드에서 확인: 스크립트 전체 컴파일 에러 없음(콘솔 클린). 리더 개체수 상한/회복
로직 자체는 스탯 밸런스 특성상 짧은 세션 안에서 확률적으로 재현하기 어려워 코드 리뷰로
갈음했다 - 다음 세션에서 실제 20웨이브 미만 플레이로 리더가 웨이브당 3마리를 넘지
않는지, 회복 효과가 의도대로 20%/3초로 작동하는지 실측 검증이 필요하다.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/EnemySpawner.cs`, `LeaderUnit.cs`, `EnemyUnit.cs`,
`Assets/Data/GameDataAsset.asset`(리더 monster_hp).

## 2026-08-21 웨이브 전환/보스 등장 알림 배너 신설 (UI 기획서 반영)

사용자 요청: "웨이브 진입할때 잠시 게임 일시정지 하면서 UI나왔다가 사라지게 하고싶음.
기획서 참고해서 적용해" - 기획서 시안 이미지(웨이브 전환/보스 등장 알림, 동작 조건:
웨이브 시작 시 자동으로 출력 / 별도 입력 없이 잠시 후 삭제)를 그대로 반영했다.

### 구현

신규 `Assets/Scripts/WaveTransitionBannerUI.cs` - Canvas 밑에 코드로 만들어 붙이는
순수 표시 전용 컴포넌트(PauseMenuUI/ScoreSummaryPopup과 같은 EnsureAttached 관례).
일반 웨이브는 어두운 카드에 "WAVE 08" 텍스트만, 보스 웨이브는 배경을 어두운 빨강으로
바꾸고 안쪽에 빨간 리본과 "BOSS INCOMING" 경고 문구를 추가로 띄운다(시안 이미지의 두
배너 구성을 그대로 반영).

타이밍(정지·재개)은 전부 `GameFlowManager`가 맡는다 - 이 프로젝트의 Time.timeScale
전환은 원래 GameFlowManager 한 곳에서만 일어나는 원칙(EnterPostWaveIntermission/
HandleGameWon/HandleNextWaveRequested와 동일)을 그대로 따랐다. `WaveManager.OnWaveStarted`
(기존에 이미 있던 이벤트, 여태 구독하는 곳이 없었다)를 구독해 `HandleWaveStarted()` →
`PlayWaveTransitionBannerRoutine()` 코루틴이 Time.timeScale = 0 → 배너 표시 →
WaitForSecondsRealtime(1.2초, timeScale이 0이라 실시간 대기 필수) → 배너 숨김 →
Time.timeScale = 1 순으로 진행한다.

구독 위치를 Start()가 아니라 Awake()로 잡았다 - 웨이브 1은 WaveManager.Start()가 곧바로
시작시키는데, GameFlowManager와 WaveManager의 Start() 실행 순서는 Unity가 보장하지
않는다(스크립트 실행 순서 지정 없음). Start()에서 구독하면 순서에 따라 웨이브 1의 배너를
놓칠 수 있어, 모든 스크립트의 Awake()가 Start()보다 항상 먼저 실행된다는 보장을 이용해
Awake()에서 구독하도록 했다.

보스 웨이브 판정은 `WaveManager.IsBossWave(int)`를 private에서 public으로 열어 그대로
재사용했다(엔드리스 모드에서 20, 39, 58...로 반복되는 주기 판정까지 중복 없이 맞는다).

### 검증

Play 모드에서 웨이브 1 시작 로그 직후 Time.timeScale이 다시 1로 정상 복귀했고,
WaveTransitionBannerUI 오브젝트가 생성되어 있으면서 activeSelf=False(표시 후 정상적으로
숨겨짐)임을 확인했다. 실제 화면 캡처로 문구·색상까지 보는 시각 검증은 다음 세션 과제로 남긴다.

### 커밋 상태

미커밋. 신규 `Assets/Scripts/WaveTransitionBannerUI.cs`. 수정
`Assets/Scripts/GameFlowManager.cs`, `WaveManager.cs`(IsBossWave public화).

## 2026-08-21 디스럭터 자폭 폭발 애니메이션 적용

사용자 제공: 프로젝트 루트 좀비 이펙트 폴더의 boomer_boom_ani00~07.png(8프레임, 736x544).
사용자 요청: "디스럽터 폭발 이미지 넣었으니까 기존 폭발 이펙트에서 이거로 대체해. 다른
폭발형 무기 등은 제외."

조사 결과 `DisruptorUnit.Explode()`는 그동안 폭발 시각 효과가 전혀 없었다(범위 판정과
Die()만 수행). 반면 로켓런처 등 스플래시 무기는 `Projectile.cs`가 별도의 원형 스프라이트
연출을 이미 갖고 있다 - 사용자가 "다른 폭발형 무기 등은 제외"라고 명시한 대상이 바로 이
경로이므로, 이번 작업은 Projectile.cs를 전혀 건드리지 않고 디스럭터 전용 경로만 새로
만들었다.

### 구현

- 프레임 8장을 `Assets/Resources/DisruptorExplosion/disruptor_explosion_0~7.png`로
  임포트(스프라이트 Single, PPU 100, 기존 DisruptorMove 임포트 설정과 동일 규격).
- 신규 `Assets/Scripts/DisruptorExplosionEffect.cs` - 8프레임을 16fps(0.5초)로 한 번만
  재생하고 스스로 파괴되는 원샷 이펙트. 크기는 하드코딩하지 않고
  `Projectile.ComputeBlastVisualScale()`과 같은 방식(스프라이트의 실제 bounds.extents를
  읽어 폭발 반경과 일치시키는 스케일 역산)으로 계산해, 프레임 크기가 바뀌어도 자동으로
  맞는다. 위 항목(잔여 이펙트 정리)과 같은 이유로 Update()에서
  GameFlowManager.IsIntermission을 체크해 정비 화면으로 넘어가면 즉시 정리한다(같은
  버그를 새 코드에 재도입하지 않기 위해 처음부터 반영).
- `DisruptorUnit.Explode()`가 데미지 판정 직전에 `DisruptorExplosionEffect.Play()`를
  호출하도록 연결(정렬순서는 디스럭터 본인 스프라이트보다 10 위).

### 검증

Play 모드에서 `DisruptorUnit.Explode()`를 리플렉션으로 직접 호출 - 이펙트 오브젝트가
disruptor_explosion_0 스프라이트로 생성되고 explosionRadius(2.25) 기준 스케일이 정확히
0.61(= 2.25 / 스프라이트 반경 3.68)로 계산됨을 확인했다. 이후 약 46초 뒤(8프레임 재생에
필요한 0.5초를 충분히 넘긴 시점) 재조회해 오브젝트가 정상적으로 사라졌음을 확인.

### 커밋 상태

미커밋. 신규 `Assets/Scripts/DisruptorExplosionEffect.cs`, 신규 리소스
`Assets/Resources/DisruptorExplosion`(8프레임). 수정 `Assets/Scripts/DisruptorUnit.cs`.

## 2026-08-21 인벤토리/슬롯 칸 강조색이 글씨를 덮어버리던 문제 수정

사용자가 스크린샷 2장을 제시하며 지적: "상점 인벤이나 정비화면 인벤, 장착중인 아이템이나
UI 버튼이 있는데도 그거 무시하고 위에 사각형 도형을 덮어버리고 그 위에 이미지랑 글씨를
드로우하고있는데 이거 잘못된거임. 그리고 다른 UI들도 이런 것들 있으면 수정해." - 처음엔
정확한 위치를 특정 못 해 재질문했고, 사용자가 정비 화면 "디스크 슬롯" 강조 스크린샷으로
콕 짚어줬다: **선택된 슬롯이 통짜 노란색 사각형으로 칠해지고 그 위에 흰 글씨("디스크
슬롯")와 아이콘이 그대로 그려져 있어 가독성이 나쁘고 "카드 위에 색종이를 덮어씌운" 것처럼
보이는 문제**였다.

### 원인

상점·정비 화면의 모든 칸(인벤토리·장착 슬롯·상점 품목)이 공유하는 `ItemCellUI.CreateShell()`
구조가 "테두리 아트(흰색 고정) + **안쪽 상태색을 등급/강조색으로 불투명하게 전체 채움**"
두 겹이었다. 등급색이 파랑/보라처럼 어두운 톤일 때는 흰 글씨가 그럭저럭 읽혔지만,
**강조색(노란색, `slotHighlightColor`)처럼 밝고 채도 높은 색이 오면 흰 글씨·아이콘과
정면으로 부딪혀 가독성이 나빠지고, 사용자 입장에서는 "원래 있던 칸 디자인을 무시하고
그 위에 색깔 사각형을 새로 덮어 그린 것"처럼 보였다.

### 수정

`ItemCellUI.CreateShell()`을 세 겹 구조로 바꿨다.
1. **테두리 아트(`UI/Black_ui03`, 흰색 고정)** - 기존 그대로. 이 스프라이트는 거의 검정이라
   색을 곱하면 등급색이 죽어버리는 함정이 이미 2026-08-13에 발견돼 있었다(주석 참고) -
   그래서 여기엔 등급색을 넣지 않는다.
2. **신규 "AccentRing" 레이어** - 등급/강조색은 이제 여기에만 입힌다. `UiIconLibrary.Frame()`
   (자물쇠/구매완료 스탬프에 이미 쓰이던, 색을 곱해도 죽지 않는 흰색 실루엣 전용 링 스프라이트)을
   재사용해 칸 테두리를 색 있는 링으로 감싼다.
3. **안쪽 상태색(State)** - 이제 항상 어두운 고정색(`CellBaseColor`)에 등급/강조색을 **25%만
   섞은** 옅은 색조로 유지한다. 그 위에 그려지는 캡션/아이콘은 항상 어두운 배경 위에서
   읽히므로, 등급/강조색이 무엇이든 대비가 깨지지 않는다.

결과적으로 "이 칸은 무슨 등급/강조 상태다"라는 신호는 테두리 링 색으로 한눈에 파악되고,
안쪽 글씨·아이콘은 항상 또렷하게 읽힌다. 이 컴포넌트는 상점(`ShopPanelUI`)과 정비
(`ModdingPanelUI`)의 인벤토리 칸·장착 슬롯 칸·품목 카드가 전부 공유하므로, 한 곳만 고쳐
사용자가 말한 "다른 UI들도 이런 것들 있으면 수정"까지 함께 해결됐다.

### 검증

Play 모드에서 리플렉션으로 재현: 인벤토리에 디스크 슬롯 파츠(id 500008)를 하나 넣고
정비 화면을 강제로 열어 그 파츠를 선택 - "디스크 슬롯" 장착 칸이 **노란 테두리 링 + 어두운
안쪽 배경**으로 강조되고, "디스크 슬롯" 캡션과 원형 아이콘이 선명하게 읽히는 것을 캔버스를
`ScreenSpaceCamera`로 임시 전환해 RenderTexture로 캡처한 스크린샷으로 확인했다(사용자가
지적한 스크린샷과 정확히 같은 화면 - "WAVE 00/20"·인벤토리 1/20·디스크 슬롯 선택 상태).
왼쪽 인벤토리의 선택된 칸도 같은 방식으로 정상 렌더링됨을 함께 확인.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/ItemCellUI.cs`.

## 2026-08-21 (이어서) 인벤토리/슬롯 칸 - 안쪽 사각형 레이어를 완전히 제거 (직전 수정이 불충분했음)

직전 수정(안쪽 상태색을 등급색 25%만 섞은 옅은 톤으로 낮춘 것)은 문제를 절반만 풀었다.
사용자가 다시 스크린샷 2장(테두리 아트 리소스 원본 1장 + 인게임 "머리" 칸 1장)으로
정확히 짚어줬다: "내가 올린 파일 보면 저기 1번 파일 UI 위에다가 바로 머리 글씨 쓰고 머리
이미지 넣어야 하는데, 지금 UI는 1번 UI 리소스 위에 회색 사각형을 덮어버리잖아 그러면
안된다는 얘기다." - 즉 색이 옅어졌어도 **안쪽에 별도로 채색한 사각형 레이어가 여전히
존재한다는 것 자체**가 문제였다. 테두리 아트(`UI/Black_ui03`, 스크린샷 1번 파일)는 그
자체로 이미 완성된 배경이라, 그 위에 캡션·아이콘을 곧바로 그려야지 중간에 아무 사각형도
끼우면 안 된다는 지적이다.

### 수정

`ItemCellUI.CreateShell()`에서 안쪽 "State" 사각형(색 불문하고 존재 자체가 문제였다)을
**완전히 삭제**했다. 이제 구조는 두 겹뿐이다.
1. 테두리 아트(`Black_ui03`, 흰색 고정) - 이게 칸의 배경 전부다.
2. 등급/강조색 전용 "AccentRing"(`UiIconLibrary.Frame()` 재사용, 테두리 자리에만 겹쳐
   그려지는 속이 빈 링이라 안쪽 면을 전혀 덮지 않는다).

캡션·아이콘은 이제 프레임(`frame`) 바로 위에 그려진다. 클릭 처리도 `Button.targetGraphic`을
그동안 쓰던 별도 State 이미지 대신 `frame` 자체로 바꿨다(어차피 클릭은 원래도 프레임이
받고 있었다 - 시각적 피드백 대상만 일치시킴). `CreateShell()`/`CreateIconCell()`이
돌려주던 참조도 `frame`으로 바뀌었다(호출부는 그대로 `Image` 타입을 받으므로 무영향).

### 검증

Play 모드에서 정비 화면을 다시 열어 캡처 - "머리" 칸이 이제 테두리 카드 위에 "머리"
글씨와 로봇 아이콘만 바로 그려지고 **안쪽에 어떤 사각형도 보이지 않음**을 확인했다.
"디스크 슬롯" 강조도 같은 카드 위에 노란 테두리 링만 추가되고 안쪽은 그대로임을 확인 -
사용자가 제시한 두 스크린샷과 정확히 같은 모양이 됐다.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/ItemCellUI.cs`.

## 2026-08-21 (이어서) 정비 화면 "인벤토리"/"장착 파츠" 배너 - 글씨가 뾰족한 테두리에 닿던 문제 수정

사용자 요청: "UI 버튼에서 최소한 버튼 테두리(두꺼운 부분)을 벗어나지 않게 글씨와 이미지
위치와 크기 조절해줘" - 이전 두 건(ItemCellUI)을 고친 뒤 "다른 UI도 검수해달라"는 요청에
따라 상점·정비 화면을 캡처해 확인하던 중 발견했다.

### 원인

정비 화면의 "인벤토리 0/20"·"장착 파츠" 제목 배너는 양 끝이 뾰족하게 잘린 육각형(쉐브론)
모양 테두리 아트 위에 얹힌 씬 배치 텍스트인데, 텍스트 RectTransform의 왼쪽 anchor가
배경 아트의 anchor와 거의 같았다(`InventoryTitleText` anchorMin.x=0.03 vs 배경
`InventoryTitleText_BG`의 0.025, `SlotTitleText`는 0.02) - 텍스트 상자가 배경의
뾰족한 왼쪽 꼭짓점까지 거의 그대로 뻗어 있어 첫 글자("인", "장")가 대각선으로 잘린
테두리 선에 바짝 붙거나 겹쳐 보였다.

### 수정

두 텍스트의 `anchorMin.x`를 왼쪽으로 늘려 뾰족한 꼭짓점을 확실히 지나친 지점부터
시작하도록 옮겼다(`InventoryTitleText` 0.03 → **0.14**, `SlotTitleText` 0.02 → **0.14**).
Unity MCP의 `object-modify`로 씬의 RectTransform을 직접 수정하고 `scene-save`로
저장했다(코드가 아니라 씬에 미리 배치된 텍스트라 스크립트 수정이 아니라 씬 편집이 맞는
수정 지점이었다). 오른쪽(anchorMax)은 겹침 증거가 없어 손대지 않았다.

같은 화면의 "로봇 정비" 제목은 겉보기엔 비슷해 보이지만 실제로는 **텍스트를 감싸는
테두리가 아니라 텍스트 아래에 깔리는 장식용 리본**이라 이 문제에 해당하지 않았고,
"파츠 설명" 제목은 뾰족한 모양이 아닌 일반 둥근 사각형이라 원래도 여유 있었다 - 둘 다
건드리지 않았다. 상점 화면의 모든 헤더(상점/WAVE/골드/정비 종료)도 뾰족한 모양이 아닌
일반 사각형이라 이 패턴이 없었다.

### 검증

Play 모드에서 정비 화면을 열어 수정 전/후를 각각 캡처 - 수정 전에는 "인"/"장" 글자가
꼭짓점 대각선에 바짝 붙어 있었고, 수정 후에는 꼭짓점과 글자 사이에 확실한 여백이
생겨 어떤 글자도 테두리의 두꺼운 부분에 닿지 않음을 확인했다.

### 커밋 상태

미커밋. 수정 `Assets/Scenes/Ground01.unity`(InventoryTitleText/SlotTitleText의
anchorMin.x만 변경 - 씬 저장 시 이번 세션의 다른 스크립트 변경사항으로 갱신된
직렬화 필드 몇 개도 함께 반영됨: EnemySpawner의 리더 무리 필드 제거,
GameHUD의 victoryObject 필드 제거, GameFlowManager의 waveTransitionBannerDuration 추가 -
전부 이미 스크립트에서 삭제/추가된 필드라 씬이 그 상태를 그대로 따라간 것뿐, 별도
의도적 변경 아님).

## 2026-08-21 (이어서) UI 텍스트/테두리 겹침 전수조사

사용자 요청: "파츠설명도 지금 두꺼운 테두리에 글씨가 겹치잖아 그러면 안된다는 얘기임.
모든 UI와 글씨들 전부 전수조사해서 수정해" - 직전 수정에서 "파츠 설명"은 괜찮다고
잘못 판단했던 것을 사용자가 재지적했다.

### 1) "파츠 설명" 재수정 (실제로 테두리에 닿고 있었음)

고배율로 다시 확인해보니 "설"/"명"의 위로 뻗는 자모가 패널(`Black_ui03`) 위쪽 테두리에
닿아 있었다. 원인은 `ModdingPanelUI.EnsureDetailPanel()`의 `CreateDetailLabel(... "파츠
설명", 0.06f, 0.945f, 0.94f, 0.995f, ..., 28f)` - 텍스트 칸의 위쪽 경계(yMax=0.995)가
패널 맨 위(1.0)에서 0.5%밖에 안 떨어져 있어 28pt 글자가 여유 없이 꽉 찼다.

**수정**: yMax를 0.995 → **0.98**로 낮춰 위쪽 여백을 만들고, 최대 글자 크기도 28 → **20**으로
낮춰 좁아진 칸 안에서도 확실히 여유가 남게 했다. yMin(0.945)은 그대로 둬 바로 아래
`SelectedIcon`(~0.935까지)과는 여전히 안 겹친다.

### 2) 전체 UI 전수조사

`CreateText`/`MakeText`/`CreateDetailLabel`/`CreateIconCell`류 헬퍼 호출을 코드 전체에서
찾아 anchor 값이 0에 가깝거나(<0.04) 1에 가까운(>0.96) 것 - 즉 테두리에 닿을 위험이 있는
지점을 전부 추렸고, 그 중 실제로 테두리 아트가 있는 컨테이너 위에 놓인 것들을 Play
모드에서 직접 캡처해 확인했다.

**정상 확인됨** (수정 안 함):
- 일시정지 메뉴 "옵션" 제목, 버튼 5종("돌아가기"/"랭킹"/"도감 (준비 중)"/"설정"/"나가기")
- 환경 설정 패널 "환경 설정" 제목, 화면 조정 버튼 8종(전체화면/경계없는 창모드/창모드/
  16:10/16:9/4:3/1:1), "피드백"/"저장 후 닫기" 버튼
- 랭킹 패널 "랭킹 - {맵}" 제목, "닫기" 버튼(리본형 - 텍스트를 감싸는 테두리가 아니라
  아래 깔리는 장식이라 애초에 해당 없음)
- AI 코어 업그레이드 "AI 코어 업그레이드 선택" 제목(리본형), 업그레이드 카드 2장,
  "리롤 (10골드)" 버튼
- 신규 웨이브 전환 배너("WAVE 01") - 이번 세션에 새로 만든 것이라 함께 검증
- 도감 화면 카테고리 탭 4종("머리 3/12" 등, 리본형), 칸 이름 라벨("컴스톡 MK-01" 등)
- 상점 화면 전체(장착 파츠/무기/디스크 칸, 품목 카드 4장, "잠금"·"상점 초기화" 버튼,
  "정비 종료" 버튼) - 지난 항목에서 이미 확인, 재검토 결과도 동일

**수정함**:
- "파츠 설명"(위 1번)
- "인벤토리"/"장착 파츠" 배너(직전 항목에서 이미 수정)

**시간 관계상 스크린샷으로 직접 확인하지 못한 것**: 머리 선택 화면의 칸 이름 라벨
(`HeadSelectPanelUI`) - 다만 도감 화면의 칸 이름 라벨과 **같은 헬퍼 함수·거의 동일한
anchor 값·같은 테두리 아트**를 쓰고 있어(코드 비교로 확인) 도감에서 문제없었던 것과
같은 결론일 가능성이 높다. 다음 세션에서 실제로 확인 필요.

### 참고: 캡처 방법의 한계

Play 모드 캡처 도중 Canvas를 일시적으로 `ScreenSpaceCamera`로 바꾸는 우회법(작업.md
"주의사항" 참고)을 쓰면, 실제 게임(Canvas가 `ScreenSpaceOverlay`라 UI가 항상 3D/2D
장면 위에 그려짐)과 달리 **월드 스페이스 오브젝트(플레이어 로봇 등)가 UI 패널 위로
잘못 겹쳐 보이는 경우가 있었다**(예: 일시정지 메뉴의 "랭킹" 버튼 위로 플레이어가 겹쳐
보임). 이는 캡처 방법 자체의 한계이지 실제 게임에서 재현되는 버그가 아니므로, 이번
조사에서는 텍스트 대 테두리(UI 대 UI) 겹침만 실제 버그로 판단하고 월드 오브젝트가
끼어든 경우는 전부 무시했다.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/ModdingPanelUI.cs`("파츠 설명" 앵커/글자 크기).

## 2026-08-21 (이어서) UI 텍스트/테두리 겹침 전수조사 - 마무리(머리 선택 화면 확인)

사용자 요청: "진행해 남은것들도 전수조사 하도록" - 직전 항목에서 "시간 관계상 확인 못 함"으로
남겨뒀던 항목들을 마저 확인했다.

### 확인한 항목 (전부 정상 - 수정 없음)

- **타이틀 머리 선택 화면**(`HeadSelectPanelUI`) - 칸 이름 라벨("컴스톡 MK-01" 등, 노란
  선택 테두리와 충분한 여백), "출발"/"뒤로" 리본 버튼(대각선 꼭짓점과 여백 충분),
  오른쪽 상세 패널의 제목·특성·스탯 텍스트 전부 확인. 예상대로 도감 화면과 같은 헬퍼·같은
  anchor 값이라 문제없었다.
- **상점 화면 음악 볼륨 슬라이더**("음악 볼륨" / "35%") - 라벨·수치 텍스트 모두 테두리와
  여백 충분.

이로써 코드 전체에서 anchor가 0.04 미만이거나 0.96 초과인(테두리에 닿을 위험이 있는)
지점을 전부 추려 실제 테두리 아트 위에 있는 것들을 Play 모드 캡처로 검증하는 작업을
마쳤다. 이번 세션에서 발견해 수정한 것은 "인벤토리"/"장착 파츠" 배너와 "파츠 설명" 총
3건이며, 그 외에는 전부 정상이었다.

### 커밋 상태

미커밋(코드/씬 변경 없음 - 확인만 진행). 검증에 쓴 스크린샷은
`C:\Users\user\AppData\Local\Temp\claude\...\scratchpad`가 아니라 프로젝트의 `Temp/`
폴더에 저장했다(Unity가 자동으로 비우는 캐시 폴더라 커밋 대상 아님, 별도 정리 불필요).

## 2026-08-21 (새 세션) 좀비군집체 그로기(기절) 상태 신설 + 총기 이펙트 4종 적용 + 드롭아이템 신규 아이콘 적용

사용자가 프로젝트 루트에 리소스 3세트를 추가로 올리고 "적용해줘"라고 요청했다: "좀비 애니메이션"
(이미 이전 세션에서 5종 적용 완료 상태였고, 이번엔 그 밑 `좀비군집체/그로기` 하위 폴더만 신규),
"좀비 이펙트"(디스럭터 폭발은 이미 적용 완료, 이번 요청 시점엔 추가 파일 없음), "총기 이펙트"
(총구 화염 + 투사체 4종, 전부 신규). 작업 중 "드롭아이템 신규" 폴더도 추가로 요청받았다.

### 1) 좀비군집체 그로기(기절) 상태 - 보스 전용 신규 기믹

사용자 제공: `그로기/frame_05~16.png`(12프레임, 640x640) + `그로기/기절 머리 위 이펙트/
groggy_stars_rotation_01~06.png`(6프레임, 512x512). 기존 게임에 "그로기/기절" 개념 자체가
없었고 트리거 조건도 지정되지 않아, **"큰 기술(광역 공격)을 쓴 직후 잠깐 무방비해지는 회복
딜레이"** 라는 통상적인 보스 패턴으로 직접 설계했다(사용자 확인 필요 - 밸런스 미확정 임시값).

- `Assets/Resources/BossGroggy`(PPU 80 - 640px가 보스의 실제 규격인 8유닛이 되도록 역산,
  `BossMove`의 512px/PPU64와 같은 최종 크기), `Assets/Resources/BossGroggyStars`(PPU 100)로
  임포트.
- `BossUnit.PerformAoeAttack()`의 데미지 판정 직후(맞았든 안 맞았든) `PlayGroggySequence()`를
  호출한다. 이 코루틴은 `groggyDuration`(3초) 동안 `BossGroggy` 12프레임을 직접 재생하고
  `GroggyStarsEffect`(신규, DisruptorExplosionEffect와 같은 원샷 이펙트 관례)로 머리 위에
  별 이펙트를 띄운다.
- 이동/공격을 잠그는 별도 플래그를 추가하는 대신 **기존 `EnemyUnit.IsAttacking`을 재사용**한다 -
  이 플래그가 이미 이동 정지(`ComputeSeekDirection`)·공격 시작 차단(`TryAttack`)·걷기 애니메이션
  간섭 방지(`CanPlayMoveFrames`)를 전부 커버해서, 그로기 코루틴이 몸 스프라이트를 직접 그려도
  기존 로직과 충돌하지 않는다.
- 그로기 상태에서는 피해를 `groggyDamageMultiplier`(1.5배, 임시값) 배 더 받는다
  (`BossUnit.TakeDamage` override) - 플레이어에게 주는 반격 기회.
- 검증: 플레이모드에서 보스를 리플렉션으로 스폰하고 `PlayGroggySequence()`를 직접 호출 -
  호출 직후 `is_groggy=True, IsAttacking=True, sprite=frame_05`(그로기 첫 프레임)를 확인했고,
  이후 재조회 시 `is_groggy=False, sprite=boss_idle_0`(정상 BossMove 프레임)로 스스로
  복귀함을 확인했다. 데미지 배율은 테스트 보스의 초기화 누락(HP 미설정)으로 재현하지
  못해 코드 리뷰로 갈음 - 다음 세션에서 실제 웨이브 20 플레이로 재검증 필요.

### 2) 총구 화염 이펙트

사용자 제공: `총구 화염 이펙트/frame_01~03.png`(3프레임, 200x200) + README("총을 발사할 때
총구 끝에서 불을 뿜는 이펙트"). 신규 `MuzzleFlashEffect`(DisruptorExplosionEffect와 같은
원샷 이펙트 관례, `Assets/Resources/MuzzleFlash`)를 만들고 `PlayerShootManager.TryFireSlot()`
에서 근접무기(`MeleeSwing`)를 제외한 모든 발사(`Projectile`/`Beam`) 시점에 호출한다.
크기는 판정에 쓰이는 값이 없어 `muzzle_flash_target_width`(1.2유닛, 임시값)로 스프라이트
실측 크기를 역산해 맞춘다. 검증: 플레이모드에서 자동발사 중인 `MuzzleFlashEffect` 인스턴스가
`frame_01`을 재생 중임을 확인.

### 3) 투사체 이펙트 4종 - 무기별 전용 탄환 시각 적용

사용자 제공: `투사체/기본탄환투사체.png`(60x60), `동그란탄환 투사체.png`(60x60),
`레이저피스톨_탄환이펙트_00~01.png`(256x64, 2프레임), `플라즈마캐논_탄환이펙트_00~01.png`
(768x64, 2프레임). 기존에는 원거리 10종 무기가 전부 `Bullets`/`Energy` 프리팹 단 2종의
스프라이트(전용 아트 없이 자리만 차지하던 상태)를 공유하고 있었다.

- **기본탄환** → `Bullets.prefab`(중기관총/대물저격총/로켓런처/지정사수소총/기관단총/
  유탄발사기 6종이 공유)의 스프라이트를 교체. 콜라이더(`SphereCollider.radius`)도
  3.2 → **0.3**으로 재계산 - 이 프로젝트에 세 번이나 있었던 "시각 크기와 판정 크기가 다른
  상수를 쓰는" 버그 계열을 재발시키지 않으려고, 새 스프라이트의 실측 `bounds.extents`(PPU 100
  기준 0.3)를 그대로 콜라이더에 넣었다(스크립트로 하드코딩 계산하지 않고 Unity에서 실제
  임포트된 스프라이트를 읽어 확인).
- **동그란탄환** → 산탄 계열(전투산탄총/소드오프샷건)만 구분되는 새 `Pellet.prefab` 신설,
  `weapon_tanhwan`을 "Bullets"에서 **"Pellet"** 으로 변경(`WeaponTableGenerator.Defs`
  수정 후 메뉴 재생성). 콜라이더 반지름도 실측값 0.3.
- **레이저피스톨** → 기존 `Energy.prefab`(레이저피스톨 전용 - 플라즈마캐논은 빔이라 이
  프리팹을 애초에 쓰지 않는다, `tanhwan="Energy"`가 사실상 죽은 데이터였음을 확인)의
  스프라이트를 교체하고, 신규 `ProjectileSpriteAnimator`(프리팹에 붙는 범용 프레임 애니메이터,
  Resources 폴더명을 인스펙터에서 지정) 컴포넌트를 추가해 2프레임을 순환 재생한다.
  콜라이더는 가로로 긴 모양(실측 1.28 x 0.32)이라 **짧은 축(0.32)** 을 썼다(EnemyUnit의
  몸 반지름 계산과 같은 원칙 - 긴 축을 쓰면 그림보다 판정이 튀어나간다).
- **플라즈마캐논**(빔) → 빔은 프리팹이 아니라 `BeamProjectile`이 코드로 시각을 만든다.
  기존에는 정적 스프라이트 1장(`beam_sprite_name="Energy"`, `Resources.Load` 단건 조회)만
  지원했는데, `Resources.LoadAll`로 폴더 전체를 불러와 순환 재생하도록
  `BeamProjectile.Fire()`/`PlayerShootManager.FireBeam()`을 함께 수정했다(`beam_sprite_name`
  값도 "Energy" → **"PlasmaCannonBeam"**). 빔은 이미 `BuildVisual()`이 스프라이트 원본
  종횡비와 무관하게 길이/반폭에 맞춰 X/Y를 독립적으로 늘리므로 콜라이더 이슈 자체가 없다.
- 검증: 플레이모드에서 기관단총 자동발사 중 `Bullets(Clone)` 3개가 전부 `BasicBullet`
  스프라이트로 나가는 것을 확인. `BeamProjectile.Fire()`를 직접 호출해 `PlasmaCannonBeam`
  2프레임 로드 및 빔 생성 성공을 확인(게임오버로 인한 자동 정리까지 정상 동작 확인).
  각 프리팹의 스프라이트/콜라이더 반지름/애니메이터 부착 여부는 에디터 스크립트로 재조회해
  전부 의도한 값과 일치함을 확인.

### 4) 드롭아이템(경험치/골드/부품상자) 신규 아이콘 - 강제 재조정 로직 제거

사용자 제공: `드롭아이템 신규/경험치_108x108.png`, `금화_108x108.png`, `상자_180x180.png`.
사용자 지시: "크기는 내가 맞춰서 올려놨으니까 인게임에서 원본과 차이나게 크기 변경하지 말고
그대로 적용되게 해줘".

기존 `RewardPickupManager`는 **모든 드랍 아이콘을 고정 월드 크기(0.6, 부품상자만 2배인
1.2)로 강제 재조정**하고 있었다(스프라이트 원본 크기와 무관하게 `localScale`을 역산).
이 상태로 새 아이콘만 덮어썼다면 상자가 사용자가 의도한 1.667배(180/108)가 아니라 코드가
강제하는 정확히 2배로 나왔을 것이다 - 그래서 이번에는 **강제 재조정 로직 자체를 제거**하고
`localScale = 1`(임포트 PPU 100을 그대로 따름)로 바꿔, 세 아이콘의 상대 크기가 픽셀 비율
그대로 나오게 했다. 맵 경계 클램프에 쓰던 고정 크기 상수도 아이콘 실측 크기
(`GetVisualHalfSize`, `sprite.bounds.extents`)로 대체했다. 기존 `Exp.png`/`Gold.png`/
`ItemBox.png`를 그대로 덮어썼다(파일명이 같아 참조 유지, 2026-08-19 골드/부품상자 아이콘
교체와 같은 방식). 검증: 에디터에서 세 스프라이트의 `bounds.extents`가 0.54/0.54/0.90
(정확히 1.667배 비율)로 임포트됐음을 확인.

### 신규/수정 파일

신규: `Assets/Scripts/GroggyStarsEffect.cs`, `MuzzleFlashEffect.cs`,
`ProjectileSpriteAnimator.cs`, `Assets/Prefebs/Pellet.prefab`.
수정: `BossUnit.cs`(그로기 상태), `EnemyUnit.cs`(ResetStaticCaches에 신규 이펙트 캐시 등록),
`PlayerShootManager.cs`(총구 화염 훅, 빔 프레임 배열화), `BeamProjectile.cs`(다중 프레임 지원),
`RewardPickupManager.cs`(강제 크기 재조정 제거), `Assets/Scripts/Editor/WeaponTableGenerator.cs`
(산탄 2종 tanhwan → Pellet), `Assets/Prefebs/Bullets.prefab`·`Energy.prefab`(스프라이트/콜라이더/
애니메이터). 리소스 신규: `BossGroggy`, `BossGroggyStars`, `MuzzleFlash`, `LaserPistolBullet`,
`PlasmaCannonBeam`, `BasicBullet.png`, `Pellet.png`. 리소스 교체: `Exp.png`, `Gold.png`,
`ItemBox.png`. 씬 수정: `Ground01.unity`의 `PlayerShootManager`(`projectile_prefabs`에 Pellet
항목 추가, `beam_sprite_name` 값 변경) - 저장 완료.

### 커밋 상태

미커밋. 사용자가 커밋을 지시하지 않아 커밋하지 않은 상태다.

## 2026-08-21 (이어서) 드롭아이템 크기 - "너무 커짐" 재지적으로 배율 앵커링 추가

사용자 리포트: "드롭아이템 왤케 커짐?" - 위 4)번에서 "스케일 1(임포트 PPU 그대로)"로
바꾼 결과, 108px 아이콘이 그대로 1.08유닛이 되어 기존에 익숙했던 크기(0.6)보다 훨씬 커진
것이었다. 사용자가 원한 "그대로 적용"은 **세 아이콘 사이의 상대 비율**(1.667배)을 그대로
두라는 뜻이었지, 절대 크기까지 임포트 픽셀 그대로 키우라는 뜻은 아니었던 것으로 재해석했다.

`RewardPickupManager`에 `GetIconScaleMultiplier()`를 추가했다 - 기준 아이콘(Gold)이
예전과 같은 0.6유닛으로 보이는 배율을 한 번 구해서(실측 1.08유닛 기준 0.5556배), **세
타입 전부에 같은 배율**을 곱한다(타입마다 다시 계산하지 않는다 - 그러면 상대 비율이 도로
깨진다). 결과: 골드/경험치 0.6x0.6(기존과 동일), 부품상자 1.0x1.0(1.667배 비율 유지,
기존 강제값이던 1.2보다는 작음 - 사용자가 정한 비율이 2배가 아니라 1.667배이기 때문에
정상적인 차이). 에디터에서 `RewardPickupManager.SpawnReward()`를 직접 호출해 세 결과물의
`localScale`/`SpriteRenderer.bounds.size`가 정확히 0.6/0.6/1.0임을 확인했다.

### 커밋 상태

미커밋. 수정 `Assets/Scripts/RewardPickupManager.cs`.

## 2026-08-22 다리 4종 마무리 - 캐터필러/로켓 "머리가 공중부양" 의 진짜 원인은 그리는 순서였다

다른 PC(`C:\Project\ComstockPJ`)에서 진행하던 세션의 내보내기 파일을 이어받아 **중단된 지점부터
재개**했다. 그 세션은 `20260818_다리기획서_Ver02_백승오.pdf`를 반영해 다리 4종(기본/거미/캐터필러/
로켓)을 만들던 중이었고, 마지막 사용자 요청 2건을 코드로 반영한 직후(에셋 리프레시 단계에서)
중단됐다 - **즉 코드는 들어갔지만 눈으로 확인한 적이 한 번도 없는 상태**였다.

> 참고: 다리 4종 구현 자체는 그 세션에서 이뤄져 커밋 `a.260821.02`에 이미 포함돼 있는데,
> 그 세션이 다른 PC에서 돌아 **작업.md에는 기록이 남지 않았다**. 이번 항목이 이 기능에 대한
> 첫 기록이라 아래에 현재 구조를 함께 정리해 둔다.

### 사용자 요청

1. **"거미다리 밑에 몸통은 왜 없앴니 그것도 포함시켜야지"**
2. **"캐터필러와 로켓은 몸통에서 머리가 공중부양하고 있다"** (레퍼런스 이미지 첨부)
3. (1차 수정 후 재지적) **"캐터필러는 여전히 중심에서 어긋나있고, 로켓은 머리가 여전히 로켓과
   맞닿아 있는게 아닌 둥둥 떠다녀. 둘다 윗면의 동그라미에 머리의 아랫부분 동그라미가 겹쳐지는
   자연스러운 모습이어야 함"**

### 삽질 1 - 머리를 내리는 방향으로 접근했다가 재지적당함

처음엔 "머리 밑단이 다리 유닛 윗면에 얼마나 걸치는가"를 겹침 거리로 재고 `treadBodyYOffset`을
0 → -0.16으로 내려 로켓과 같은 겹침(0.132유닛)을 만들었다. 수치상으로는 맞췄지만 **사용자가
보기엔 여전히 떠 있었다** - 겹침 거리를 늘려도 정작 다리 몸통이 머리 **뒤**에 그려지고 있어서
머리 밑단 외곽선이 다리 위에 그대로 얹혀 보였기 때문이다.

### 삽질 2 - 원본 캔버스 정합(registration)도 정답이 아니었다

원본 아트를 조사하니 다리 3종이 전부 **머리(`Parts/Body.png`, 250x250)와 같은 250px 캔버스**에
그려져 있었다(로켓 251x251, 캐터필러 250x250). 같은 자리에 겹치면 작가가 의도한 조립이 나올
것이라 보고, 2족 보행용으로 `bodyHipAnchor`만큼 위로 밀려 있던 머리를 그만큼 되돌리는
`CanvasAlignY()`를 넣었다(머리가 월드 0.36유닛 내려감).

결과는 **더 나빠졌다.** 캐터필러는 하우징이 머리와 색·폭이 같은 탓에 머리 뒤로 완전히 숨어
"트랙 두 짝 위에 머리만 얹힌" 모습이 됐고, 무기 소켓은 리그 밖(`Player` 직속)이라 따라 내려오지
않아 총이 머리 위로 떠버렸다. **이 접근은 통째로 되돌렸다.**

### 진짜 원인 - sortingOrder(그리는 순서)

오프라인에서 PNG를 직접 합성해 후보를 비교하고 나서야 원인이 잡혔다. 사용자가 준 레퍼런스
그림은 **다리 몸통이 머리보다 앞**에 있어 머리 밑단이 그 뒤로 가려진 그림이다. 그런데 코드는
반대로 두고 있었다:

- 캐터필러 `Back`(= 왼쪽 트랙 **+ 머리가 꽂히는 하얀 하우징**)이 `bodySortingOrder - 1`
- 로켓 `Base`(= 파란 호버 허브)가 `bodySortingOrder - 1`

즉 **머리가 다리 몸통을 덮고 있었다.** 그래서 (a) 머리 밑단 외곽선이 다리 위에 얹혀 보이고
("둥둥 떠다닌다"), (b) 캐터필러는 하우징이 안 보이니 좌우 비대칭인 트랙 두 짝만 남아
**"중심에서 어긋나 보였다"**. 두 리포트가 같은 원인이었다.

**Y 오프셋은 애초에 건드릴 필요가 없었다** - `treadBodyYOffset`/`rocketBodyYOffset`을 둘 다
**0으로 되돌리고** 순서만 바꿨다.

### 수정 - 다리 유닛을 머리 앞으로, 단 무기보다는 뒤로

무기 스프라이트(`RigingPoint/*_img`)가 정렬순서 **14 고정**이라 다리 몸통을 머리(13) 위로
올리면 무기와 **동률**이 되어 겹칠 때 앞뒤가 불확정해진다(특히 허리 높이인 소켓 3/4는 다리
유닛과 실제로 겹친다). 그래서 다리를 올리는 대신 **머리를 두 칸 내렸다**
(`ApplyBodySortingForLegMode()` 신설 - 대체 다리 모드에서만 `bodySortingOrder - 2`, 2족으로
돌아가면 원복).

| | 화염 | 머리 | 다리 몸통 | 앞 트랙/노즐 | 무기 |
|---|---|---|---|---|---|
| 캐터필러 | - | 11 | 12 | 13 | 14 |
| 로켓 | 10 | 11 | 12 | 13 | 14 |

플레이모드 실측으로 4종 전부 확인했다 - 캐터필러/로켓은 위 표대로 나오고, **거미(13/12/10/9)와
기본 2족(13/9/8/7/4/3/2)은 종전 그대로**이며 대체 다리를 거쳤다가 기본 다리로 돌아와도 머리가
13으로 정확히 복구된다.

### 거미 몸통은 이미 정상이었다

`BuildSpiderVisual()`이 `Resources/Parts/Spider/body`(호버 패드 하우징)를 머리와 다리 사이에
끼우는 코드가 직전 세션에서 이미 들어가 있었고 정상 동작했다(머리 밑단이 몸통 윗면 안으로
0.264유닛 파묻힌다). 거미 몸통은 원본 캔버스 규격이 827x509로 달라 위의 정합 논의와도 무관하다.

### 함정 5개(다음에 또 밟기 쉬움)

1. **"legVisualMode가 안 바뀐다"의 정체는 플레이어 사망이다.** 다리 교체는
   `RunState 변경 → PlayerRobotController.ApplyAggregatedStats() → RefreshLegSkillInfo() →
   rig.SetLegVisual()` 경로인데, 죽으면 컨트롤러가 `enabled=false`가 되어(의도된 동작) 사슬이
   끊긴다. **장착 데이터는 바뀌는데 그림만 안 바뀌는** 증상이라 버그로 오인하기 딱 좋다.
   해결책은 `EditorApplication.isPaused=true`를 먼저 켜고 진입해 **1프레임에서 얼린 뒤** 적을
   지우고 스포너를 끄고 재개하는 것이다(직전 세션도 같은 곳에서 헤맸다).
2. **`isPlaying=false`와 `=true`를 한 호출 안에서 연달아 쓰면 재시작되지 않는다.** Unity가
   플레이모드 전환을 프레임 끝으로 미뤄 둘이 상쇄된다(frameCount가 66,000대 그대로였다).
   **종료와 진입을 별도 호출로 나눠야 한다.**
3. **코드 기본값을 고쳐도 이미 로드된 씬 컴포넌트에는 안 먹는다.** 씬 YAML에 그 필드가 없어도,
   컴파일 후 도메인 리로드 때 Unity가 인메모리 씬 오브젝트를 직렬화/복원하면서 **그 시점 값**을
   되살린다. 기존 관례대로 **코드 기본값과 씬 직렬화 값 양쪽에** 넣어야 한다.
4. **겹침 판정에 `Renderer.bounds`를 쓰면 안 된다** - 투명 여백이 포함돼 캐터필러가 0.887유닛
   겹치는 것처럼 나온다(실제 그림 기준 0.052). `sprite.vertices`(타이트 메시)를 렌더러
   `localToWorldMatrix`로 월드 변환해서 재야 한다(2026-08-19 악세사리 작업과 같은 기법).
5. **`ExitPlay` 직후에 `assets-refresh`를 곧바로 붙여 호출하면 MCP 플러그인이 죽는다.**
   플레이모드 종료 시 `Startup.OnApplicationQuitting → TryDisconnectAndCleanup`이 도는데 그와
   리프레시가 겹치면서 Unity 메인 스레드가 멈췄다(에디터 로그가 13분간 멈추고 CPU 0%,
   `ping`조차 60초 타임아웃, 에디터 재시작으로만 복구). **두 호출 사이에 상태 확인을 한 번
   끼워 넣어야 한다.**

### 검증 방법(재현용)

`Comstock` → `디버그 부품상자`(`DebugPartBox`)를 쓰거나, 리플렉션으로 `ModdingManager.EquipPart`를
직접 불러 다리를 갈아끼우고 `Camera.main`을 `RenderTexture`로 렌더해 PNG로 저장했다. 적이
계속 리스폰해 캡처를 방해하고 플레이어를 죽이므로 **캡처 스크립트 안에서 적 제거와 렌더를 같은
호출에 넣어** 사이에 프레임이 끼지 않게 했고, 체력을 크게 잡아 두었다.

### 현재 다리 4종 구조 요약(첫 기록)

- `LegVisualMode`(신규 파일): `Biped` / `Tread` / `Rocket` / `Spider`. `PartData.legVisualType`이
  들고 있고 `ProceduralCharacterRig.SetLegVisual()`이 받는다.
- `Biped` 외 3종은 `Update()`에서 **보행 위상(phase) 계산 자체를 건너뛰고** `UpdateAltVisual()`로
  빠진다. `Tread`/`Rocket`은 프레임 재생만(`FrameCycler`), `Spider`는 4다리 2관절 IK +
  대각선 교대 스텝이다. 대체 다리 모드는 `bodyPivot.localPosition`을 매 프레임 상수로 고정하고
  bob/breath/lean/sway를 전부 끄므로 **이동이 머리 높이에 영향을 줄 수 없다**.
- 파츠 4종: `500004 기본 다리` / `500010 거미 다리` / `500011 캐터필러` / `500012 로켓 추진기`.
- 다리마다 액티브 스킬이 다르다(`LegSkillType`): 기본=구르기(`Roll`), 거미=폴짝(`Hop`),
  캐터필러=순간 부스트(`Boost`), 로켓=없음(`None`, 패시브 램프업만).
- 리소스: `Assets/Resources/Parts/Spider`(upper_leg/lower_leg/body 등), `Parts/Tread`(앞뒤 6프레임씩),
  `Parts/Rocket`(본체/노즐/화염 6프레임씩).
- **디버그 도구**: 하이라키 우클릭 → `Comstock` → `디버그 부품상자`(`DebugPartBox`). 슬롯을 골라
  놓으면 플레이 중 F2/F3로 같은 슬롯 파츠를 즉시 순환 장착한다.

### 신규/수정 파일

수정: `Assets/Scripts/ProceduralCharacterRig.cs`(대체 다리 sortingOrder 재배치 +
`ApplyBodySortingForLegMode()` 신설, 오프셋 툴팁 정리),
`Assets/Scenes/Ground01.unity`(그동안 씬에 없던 다리 관련 직렬화 필드 26줄이 저장되며 추가됨 -
값은 전부 코드 기본값과 동일).

씬 저장 시 TMP 자동 크기 캐시(`m_fontSize` 6곳)와 폰트 아틀라스
(`NotoSansKR-Regular SDF.asset`, 2,779줄)가 플레이모드 실행 때문에 같이 더러워졌는데,
**설정이 아니라 런타임 캐시**라 전부 되돌렸다(`m_fontSizeBase`는 원래 그대로다).

### 커밋 상태

미커밋. 사용자가 커밋을 지시하지 않아 커밋하지 않은 상태다.

## 2026-08-23 (이어서) 캐터필러/로켓 최종 해법 - "이미지 상으로 붙어야 해": 원본 캔버스 정합 + 순서 + 소켓 동반 하강

직전 항목의 순서(sortingOrder) 수정 후에도 사용자가 재지적했다: "아직도 둥둥 떠다님",
"프레임 문제가 아니라 **원끼리 겹쳐야 한다**. 애초에 붙어있질 않아. **좌표상으로 대충 붙이는게
아니라 이미지 상으로 붙어야 해**." 이 말이 정답이었다 - 순서 수정만으로는 겹침이 0.05유닛
(원본 10px 상당)뿐이라 타원 두 개가 가장자리만 스치는 상태였다.

### 최종 진단 - 필요한 것은 세 가지였고, 앞선 두 시도는 각각 하나씩만 갖고 있었다

작가가 준 캐터필러/로켓 아트는 **머리(Parts/Body.png)와 같은 250px 캔버스에 "조립된 상태로"**
그려져 있다(픽셀 실측: 머리 44~199, 캐터필러 하우징 120~222, 로켓 허브 119~209 - 같은 자리에
포개면 머리 밑단이 다리 안으로 약 80px 파묻힌다). 즉 완성형이 이미 아트에 들어 있고, 게임은
그 두 장을 **같은 캔버스 위치에, 다리 몸통이 앞으로 오는 순서로** 그리기만 하면 된다.

1. **캔버스 정합(위치)**: 머리 스프라이트는 2족 보행용으로 "고관절 앵커(bodyHipAnchor)가
   bodyPivot에 오도록" 캔버스 중심보다 (0.5-0.21)x2.5 = 0.725 리그유닛 위로 밀려 있다
   (ApplyBodyFacing). 다리 프레임은 캔버스 중심이 그대로 standHipY에 놓이므로, 캐터필러/로켓
   에서는 그 밀린 양을 되돌려야 두 캔버스가 포개진다. → `AltCanvasAlignY()` (월드로 -0.3625)
2. **그리는 순서**: 다리 몸통(하우징/허브)이 머리보다 앞이어야 머리 밑단이 꽂혀 보인다.
   → 직전 항목의 [머리 11 < 몸통 12 < 앞트랙·노즐 13 < 무기 14] 유지
3. **무기 소켓 동반 하강**: 머리가 내려간 만큼 귀 옆의 무기도 내려와야 한다. 소켓(RigingPoint)
   은 리그 밖 Player 직속이라 리그가 못 옮긴다. → `ProceduralCharacterRig.HeadCanvasWorldOffsetY`
   (월드 단위) 신설, `PlayerShootManager.ApplyHeadCanvasOffsetIfChanged()`가 읽어서 적용

2026-08-22의 1차 시도(-0.16 오프셋)는 셋 다 없었고(순서도 위치도 어중간), 2차 시도
(CanvasAlignY)는 1번만, 3차 시도(순서 뒤집기)는 2번만 갖고 있어서 전부 "여전히 떠 보이는"
결과였다. 셋을 합치니 원본 합성(PIL로 PNG를 같은 캔버스에 포갠 검증 이미지)과 게임 화면이
일치한다.

### 소켓 동반 하강 구현 세부

- PSM의 기존 구르기 인프라를 재사용한다: `roll_home_local_position`(씬 원위치 캐시)을 기준으로
  `home + (0, 월드오프셋/부모lossyScale.y, 0)`을 놓는다.
- **매 프레임 덮어쓰지 않는다** - 근접무기 찌르기 연출이 pivot 위치를 직접 움직이므로 매 프레임
  강제하면 연출이 무력화된다. 오프셋 "값이 바뀐 프레임"에만 적용한다(다리 교체는 정비 화면
  = 게임 정지 중에만 일어나 연출과 겹칠 일이 없다). 구르기 복귀(RestoreRollHomePositions)
  직후에는 applied 캐시를 0으로 리셋해 다음 프레임에 재적용시킨다.
- 프레임 팔로우(직전 커밋 전 임시로 넣었던 AltLegFollowY - 다리 애니메이션의 상하 움직임을
  머리가 따라가는 보정)는 사용자가 "프레임 문제가 아니라"고 확정해 **넣지 않고 제거했다**.
  정합으로 80px이 파묻히면 다리 애니메이션의 8~10px 상하 움직임은 겹침 안에서 흡수된다.

### 검증(플레이모드 실측)

- 캐터필러: 머리 밑단 6.127 / 하우징 윗면 6.542 → **겹침 0.415유닛(원본 83px)**. 로켓: 6.127 /
  6.550 → 0.423(85px). 둘 다 원본 캔버스 정합값과 일치.
- 소켓: 메인 localY 3.600 → 1.945 (= 월드 -0.3625 / Player lossy 0.2191 - **소켓 하강량을 검산할
  때 Player lossyScale을 반드시 실측할 것**. 문서상 0.43812로 알려져 있지만 현재 씬 실측은
  0.2191이다).
- **전 조합 왕복**: 캐터필러→기본→로켓→거미→기본 순서로 갈아끼우며 오프셋(-0.3625/0/-0.3625/0/0),
  소켓(1.945/3.600/...), 머리 정렬(11/13/...)이 전부 정확히 적용·원복됨을 확인.
- **이동/반전**: 오른쪽·왼쪽 활공 중에도 정합 유지, 트랙 프레임 정상 재생, 좌향 반전 시에도
  하우징이 머리 앞.
- 거미/기본 2족은 종전과 동일(무변화)을 실측 확인.

### 이번에 추가로 밟은 함정 2개

6. **WaveManager/EnemySpawner를 컴포넌트 disable해도 이미 돌던 스폰 코루틴은 멈추지 않는다.**
   그래서 "적을 다 지웠는데 계속 다시 나타나 테스트 플레이어를 죽이는" 일이 반복됐다
   (죽으면 함정 1번 - 다리 교체 사슬 끊김 - 으로 이어져 증상이 꼬인다). 오브젝트째
   `SetActive(false)`로 꺼야 코루틴까지 죽는다.
7. **WaveManager를 오브젝트째 끄면 Time.timeScale이 0에 머문다**(첫 웨이브 시작 루틴이
   timeScale을 1로 만드는 흐름이 안 돌아서). 캡처 세션에서 "속도는 3인데 위치·페이즈가 안
   변하는" 증상의 정체. `Time.timeScale = 1` 직접 세팅으로 해결.

### 신규/수정 파일

수정: `Assets/Scripts/ProceduralCharacterRig.cs`(`AltCanvasAlignY()` +
`HeadCanvasWorldOffsetY` 신설, alt 모드 bodyPivot에 정합 반영, 임시 프레임 팔로우 제거),
`Assets/Scripts/PlayerShootManager.cs`(`player_rig` 캐시 + `ApplyHeadCanvasOffsetIfChanged()` -
소켓 동반 하강). 씬 변경 없음(2026-08-22의 직렬화 필드 26줄 추가는 그대로 유효).

### 커밋 상태

미커밋. 사용자가 커밋을 지시하지 않아 커밋하지 않은 상태다.

## 2026-08-23 (이어서 2) 시트 정렬 - "위쪽면 동그라미에 합쳐야지" + 머리를 다리 앞 레이어로

직전 항목(완전 캔버스 정합)에 사용자가 레퍼런스 이미지를 첨부하며 재지적했다:
"**아래쪽 동그라미가 아니라 위쪽면 동그라미에 합쳐야지**", "**머리가 왜 다리 레이어보다
뒤로가냐**". 완전 정합(80px 파묻힘)은 머리 밑단이 다리 유닛의 아래쪽 타원 근처까지 내려간
과한 깊이였고, 하우징을 머리 앞에 그린 것도 레퍼런스와 반대였다.

### 최종 구성(사용자 레퍼런스 확정)

- **머리가 다리 유닛보다 앞 레이어다.** 머리 밑단 타원이 개구부 위에 그대로 보이고, 개구부의
  뒷림이 머리 양옆으로 살짝 드러난다. [화염 10 < 하우징/허브 11 < 앞트랙/노즐 12 < 머리 13 <
  무기 14] - 직전의 "머리를 두 칸 내리는" `ApplyBodySortingForLegMode()`는 삭제했다(머리는
  항상 13이므로 무기 14와의 동률 문제도 애초에 없다).
- **깊이는 "머리 밑단 곡선 = 다리 윗면 개구부 타원의 앞림"이 되는 시트 정렬.** 원본 250px
  캔버스 픽셀 실측: 머리 밑단 곡선 중심 y=195, 캐터필러 하우징 개구부 앞림 y=150(뒷림
  120~124, 윗면 125~147, 앞림 148~152), 로켓 허브 앞림 y=162.5(0번 프레임). 따라서 완전
  정합에서 캐터필러 **+45px**, 로켓 **+32.5px** 올린다(`TreadHeadSeatRaise` 0.45 /
  `RocketHeadSeatRaise` 0.325, 리그 로컬 상수 - 씬 직렬화 함정을 피하려고 SerializeField가
  아니라 const로 넣었다. 수동 미세조정은 기존 treadBodyYOffset/rocketBodyYOffset이 담당).
- 로켓 허브는 프레임마다 최대 10px 오르내리는 호버링 애니메이션이지만 개구부 타원 높이(37px)
  안에서 흡수되므로 0번 프레임 기준으로 고정.
- 무기 소켓 동반 하강은 `HeadCanvasWorldOffsetY`가 `AltCanvasAlignY()`를 그대로 읽으므로
  자동으로 새 깊이를 따라간다(캐터필러 월드 -0.1375, 로켓 -0.200 - 모드별로 다른 값이 되는
  것도 자동 처리).

### 검증(플레이모드 실측)

- 픽셀 검산: 머리 밑단 곡선(월드 6.372)과 하우징 앞림(6.392)이 4px 이내로 포개짐.
- 왕복: 기본→캐터필러→로켓→기본에서 오프셋(0/-0.1375/-0.2/0), 소켓 localY
  (3.600/2.972/2.687/3.600), 머리 order(항상 13)가 정확히 적용·원복.
- 좌향 반전 + 이동 중에도 시트 유지·트랙 프레임 정상.
- 캡처를 사용자 레퍼런스와 비교 - 같은 구성(머리 앞, 밑단이 개구부에 안착, 뒷림이 양옆에
  보임) 확인.

### 신규/수정 파일

수정: `Assets/Scripts/ProceduralCharacterRig.cs`(시트 정렬 상수 2개 + AltCanvasAlignY에 반영,
렌더 순서를 "다리 전부 < 머리"로 교체, ApplyBodySortingForLegMode 삭제).
`PlayerShootManager.cs`는 무변경(오프셋 값을 읽기만 하므로 자동 추종). 씬 변경 없음.

### 커밋 상태

미커밋. 사용자가 커밋을 지시하지 않아 커밋하지 않은 상태다.

## 2026-08-23 (이어서 3) 좌우 어긋남 - 머리와 다리의 반전 부호가 정반대였다 + x 시트 정렬

사용자 재지적: "좀 어긋나있어 아직도. 레퍼런스 보고 좌우 어긋난것도 보정해봐",
"몸통과 다리의 몸통 보조부분이 일자로 딱 맞아야해".

### 원인 1 - 반전(facing) 부호 관례가 머리와 다리에서 정반대

원본 캔버스의 가로 실측(행 스캔)으로는 머리 몸통 벽 중심 129.5 / 캐터필러 컵 중심 131로
거의 일자다 - 즉 어긋남은 아트가 아니라 코드에서 났다. 머리 스프라이트는
`facingSign > 0`일 때 뒤집히는데(`ApplyBodyFacing`의 `flipX = facingSign > 0`), 대체 다리
루트들은 `localScale.x = facingSign`을 그대로 써서 **머리가 뒤집히는 프레임에 다리는 안
뒤집혔다**(그 반대도 마찬가지). 캔버스에 조립된 좌우 관계가 방향 전환 즉시 깨져 컵/허브가
머리 옆으로 미끄러졌다. 추가로 **캐터필러/로켓 루트는 생성 시점에 반전을 아예 적용하지
않아**(거미만 있었음) 첫 방향 전환 전까지 항상 원본 방향이었다.

`ApplyAltVisualFacing()` 신설 - `mirror = facingSign > 0 ? -1 : 1`(머리 flipX와 같은 조건)로
캐터필러/로켓/거미 루트를 머리와 **함께** 뒤집는다. 생성 직후(`EnsureAltVisualBuilt`)와
방향 전환 시 양쪽에서 호출.

### 원인 2 - 로켓 허브가 캔버스에서 머리 몸통과 4.5px 어긋나게 그려져 있음

컵/허브 중심을 머리 몸통 중심(129.5)에 맞추는 x 시트 오프셋을 신설했다:
`TreadSeatXOffset = -0.015`(컵 131 → 1.5px 왼쪽), `RocketSeatXOffset = +0.045`(허브 125 →
4.5px 오른쪽). 반전은 rigRoot 원점 기준이라 `localPosition`은 스스로 안 뒤집히므로
**오프셋에 mirror 부호를 곱해서** 넣는다.

### 검증(플레이모드 실측)

캐터필러/로켓 각각 왼쪽 보기(원본 방향)·오른쪽 보기(반전) 4장 캡처 - 양방향 모두 컵/허브가
머리 정중앙 아래에 일자로 붙고, 반전 시 트랙 비대칭(뾰족한 쪽)이 캔버스 조립째 자연스럽게
미러링된다. 기본/거미 복귀 시 오프셋 0·소켓 3.600·머리 order 13 원복 확인.

### 함정 8 (테스트 인프라)

**활공 테스트(컨트롤러 disable + Rigidbody 속도 주입)를 한 뒤 컨트롤러를 다시 켜지 않으면
이후의 다리 장착이 화면에 반영되지 않는다**(RefreshLegSkillInfo 사슬이 끊긴 채라 함정 1과
같은 증상). 이번에 로켓 캡처 2장이 사실은 캐터필러 화면이었던 사고의 원인. 장착 전에
반드시 `controller.enabled = true`로 되돌릴 것.

### 신규/수정 파일

수정: `Assets/Scripts/ProceduralCharacterRig.cs`(`ApplyAltVisualFacing()` 신설, 반전 부호
동기화, x 시트 오프셋 상수 2개, BuildSpiderVisual 인라인 반전 제거). 씬 변경 없음.

### 커밋 상태

미커밋. 사용자가 커밋을 지시하지 않아 커밋하지 않은 상태다.

## 2026-08-23 (이어서 4) 사용자가 추가한 이미지 다수 적용 + 보스 적용 계획서 작성

사용자가 프로젝트 루트 한글 폴더 여러 곳에 이미지를 대량으로 추가했다("이미지들 여러 폴더에
다수 추가했으니 적용해줘"). 보스(좀비 군집체)는 기획서 2종(`20260818_보스몬스터기획서_Ver01.pdf`,
`20260821_보스연출가이드라인_Ver01_오승명.pdf`)까지 함께 전달했는데, 사용자가 도중에
"보스는 적용 계획서만 만들고, 나머지 이미지들 먼저 적용해"로 범위를 축소해 **보스 행동 자체는
구현하지 않고 계획서(HTML 아티팩트)만 작성**했다. 그 외 이미지는 전부 이번 세션에 적용했다.

### 적용한 것 (코드 반영 완료)

- **차저(200002) 전용 애니메이션 3종 신설** - 그 전까지 차저는 유일하게 정지 스프라이트
  고정이었다(사용자 애니메이션 미제공으로 일부러 비워뒀던 자리). `MonsterAnimationLibrary`에
  200002 → `ChargerMove`(걷기 16프레임) 항목을 추가했고, `ChargerUnit.ResolveMoveClip()`을
  오버라이드해 `is_charging`인 동안만 `ChargerCharge`(돌진 16프레임) 폴더로 바꿔치기한다 -
  `EnemyUnit.UpdateWalkAnimation()`은 `IsAttacking`이 꺼진 뒤에만 이 값을 읽으므로(예비 동작
  중엔 안 읽음), 예비 동작이 끝나고 실제 돌진이 시작된 뒤에만 자동으로 전환된다(별도 코루틴
  불필요). 예비 동작(`ChargerChargePrep` 16프레임)은 반대로 `IsAttacking`이 켜져 있는 구간이라
  이 파이프라인을 못 타서, `TryAttack()`을 오버라이드해 예비 동작과 병렬로 도는 별도 코루틴
  (`PlayChargePrepFrames`)으로 직접 재생한다 - 베이스는 이 구간에서 몸 색만 주황으로 물들이므로
  두 연출이 함께 나온다. `EnemyUnit.body_sprite_renderer`가 private라 접근이 안 돼
  `protected SpriteRenderer BodySpriteRenderer` 읽기 전용 접근자를 새로 열었다.
- **차저 돌진 경고 이펙트 신설** (`ChargeWarningEffect`, `Resources/ChargeWarning`, 좀비
  이펙트/몬스터 돌진 4프레임) - 예비 동작 시작 시점에 방향 표시로 한 번 재생한다. 보스 기획서의
  "돌진 박치기" 패턴이 "기존 차저 이펙트 재사용"이라고 명시해서, 차저 쪽에 먼저 만들어두면
  나중에 보스가 그대로 재사용할 수 있다.
- **스피터 투사체 전용 아트 적용** (`EnemyProjectile`, `Resources/SpitterProjectile`) - 그
  전까지는 코드로 생성한 붉은 원형 스프라이트였다. 콜라이더 반지름은 여전히 실제 스프라이트
  bounds에서 역산한다(2026-08-12에 이 클래스에서 판정-시각 불일치 버그가 실제로 있었던 자리라
  하드코딩하지 않음). `transform.localScale = visualSize`가 나중에 자동으로 반지름에도 곱해지므로
  콜라이더 반지름 자체는 스케일 1 기준값만 넣는다(처음에 여기서 이중 곱셈 버그를 만들었다가
  바로 잡았다).
- **폭발 이펙트 전용 애니메이션 적용** (`ExplosionEffect`, `Resources/Explosion`, 10프레임) -
  `Projectile.Explode()`가 예전엔 날아온 투사체 자신의 스프라이트를 폭발 반경만큼 확대해서
  보여주던 임시 처리(`ComputeBlastVisualScale()`)를 완전히 대체했다. 이제 투사체 자신은
  숨기고 전용 폭발 이펙트를 재생한다. 로켓런처/유탄발사기 등 `weapon_splash > 0`인 모든 무기에
  자동 적용된다. 디스럭터 자폭 전용의 `DisruptorExplosionEffect`(2026-08-21)와는 별개 클래스로
  유지했다(사용자가 그때 "다른 폭발형 무기 등은 제외"라고 지정한 경계를 그대로 지킴).
- **좀비 피격 이펙트 신설** (`ZombieHitEffect`, `Resources/ZombieHitEffect`, 8프레임) -
  `EnemyUnit.TakeDamage()`가 맞을 때마다 몸통 중앙에 재생한다. 기존 `HitFlash`(몸 전체가
  하얗게 물드는 셰이더 연출)와는 독립적으로 동시에 나온다. 크기는 본인 스프라이트 폭에
  비례시켜 좀비/차저/보스 등 규격이 달라도 자동으로 맞는다.
- **로봇 구르기 땅먼지 이펙트 신설** (`RollDustEffect`, `Resources/RollDust`, 3프레임) -
  `PlayerRobotController.TryStartDash()`가 구르기/폴짝 뛰기를 시작하는 순간 재생한다. 굴러가는
  반대 방향으로 먼지가 튀는 것처럼 `-dashDirection`으로 회전시켰다.
- **AI 코어 레벨업 이펙트 신설** (`LevelUpEffect`, `Resources/LevelUpEffect`, 24프레임) -
  `AiCoreManager.HandleRunStateChanged()`의 레벨업 while 루프가 끝난 뒤, 실제로 레벨이 올랐을
  때만(한 프레임에 여러 레벨이 올라도 한 번만) 플레이어 위치에 재생한다. 이 매니저는 플레이어가
  아닌 별도 GameObject에 있어 태그(`"Player"`)로 찾는다.
- **정밀화기(대물저격총/지정사수소총) 전용 탄환 적용** - 그 전까지는 나머지 4종과 같은
  "기본탄환"(`Bullets`)을 공용으로 썼다. `Assets/Prefebs/Bullets.prefab`을 복제해
  `PrecisionBullet.prefab`을 만들고 스프라이트를 교체했다(`Resources/PrecisionBullet.png`,
  80x80 - 기존 60x60 기준 콜라이더 반지름 0.3도 0.4로 같이 조정, "보이는 크기 = 맞는 크기"
  원칙 유지). `WeaponTableGenerator.cs`의 kind=3/8 `tanhwan`을 `"PrecisionBullet"`으로 바꾸고
  메뉴(`Comstock/무기 테이블 65행 재생성`)로 테이블을 재생성했다. 이 이름은
  `Assets/Resources/`도 `Assets/Resources/Prefebs/`도 아닌 `Assets/Prefebs/`에 있어
  `PlayerShootManager.ResolveProjectilePrefab()`의 Resources 폴백으로는 못 찾는다 - 인스펙터의
  `projectile_prefabs` 목록(Bullets/Pellet/Energy가 있는 곳)에 새 항목을 리플렉션으로 직접
  추가했다(이 필드를 MCP로 직접 읽으려 하면 `SpriteRenderer`가 아니라 `Rigidbody.
  attachedRigidbody` 계열 순환 참조로 항상 500 에러가 나서, `script-execute`로 필드를 직접
  reflection 조작했다 - Unity-MCP 자체의 한계로 보인다).
- **보스 그로기 프레임 800x800 재적용** - 사용자가 기존 그로기 프레임(`그로기/frame_05~16`)의
  캔버스를 640px → **800px**로 바꿔 다시 전달했다("기존 이미지들도 규격 변경해서 넣어놨어").
  `Assets/Resources/BossGroggy/`에 덮어쓰기만 하면 픽셀 크기가 달라 보스가 갑자기 커진다 -
  이 폴더는 원래 640px 원본을 PPU=80으로 임포트해 "8유닛" 크기를 맞춰 놨었다(`BossMove`는
  512px@PPU=64로 같은 8유닛). 800÷8=100이므로 **PPU를 80 → 100으로 같이 바꿔서** 픽셀이
  커진 만큼 보이는 크기는 그대로 유지되게 했다(둘 다 "800px 규격 = PPU 100 = 8유닛"이라는
  프로젝트의 초대형 규격 원칙과 맞아떨어진다).

### 적용하지 않은 것 (확인·후속 필요)

- **`총기 이펙트/레이저/` 폴더** - 안의 파일들이 이미 쓰이고 있는
  `총기 이펙트/투사체/` 안의 동명 파일과 **바이트 단위로 완전히 동일**함을 해시로 확인했다.
  중복 전달로 보고 별도 조치 없음.
- **`좀비 이펙트/boomer_boom_ani*.png`**(8프레임) - 어느 몬스터/이펙트에 대응하는지 기획서·
  코드 어디에도 근거가 없다. 디스럭터 자폭엔 이미 전용 `DisruptorExplosion`(8프레임)이 있어
  중복/이전 버전으로 추정되나 확정할 수 없어 그대로 두었다.
- **`좀비 이펙트/보스등장알림.png`**(BOSS INCOMING 배너) - 보스 소환 연출에 종속된 에셋이라
  보스 작업과 함께 미룸.
- **보스(좀비 군집체) 신규 애니메이션 7세트**(Panting/roar/대시/보스 돌진/보스 사망/보스 사망
  폭발 이펙트/보스 소환 이펙트, 전부 800px 통일) - 사용자 지시로 이번엔 **계획서만 작성**하고
  코드 미반영. 아래 계획서 참고.

### 검증

플레이모드 실측: 웨이브 1 진행 중 스크립트로 차저(200002)를 플레이어 옆에 직접 스폰해 예비
동작(주황 틴트+준비 프레임) → 돌진(경고 이펙트+돌진 프레임) → 접촉(근접 반사 판정) →
플레이어 처치까지 **전체 시퀀스가 콘솔 에러 0건으로 완주**하는 것을 확인했다(가장 코드
변경이 큰 부분이라 실제 전투 경로로 검증). `ExplosionEffect`/`RollDustEffect`/`LevelUpEffect`도
직접 호출해 예외 없이 재생/자동 소멸(정비 중이면 즉시 파괴하는 기존 관례 그대로)을 확인했다.
`Resources.LoadAll<Sprite>("ChargerMove")`로 16프레임이 정상 로드되는 것도 별도 확인.
새로 복사한 PNG 99장은 기본 임포트 시 `Texture2D`로만 잡혀(`Sprite`가 아님) 전부 무음 실패할
뻔한 것을 스크립트로 일괄 재설정(`textureType=Sprite`, `spriteImportMode=Single`,
`spritePixelsPerUnit=100`)해 잡았다 - **Resources 폴더에 새 PNG를 추가할 때마다 반드시 이
임포트 설정을 확인할 것**(프로젝트에 이걸 자동으로 해주는 AssetPostprocessor가 없다).

### 보스 적용 계획서

`좀비 군집체` 보스 몬스터 기획서/가이드라인을 현재 `BossUnit.cs`/`WaveManager.cs` 구조에
반영하기 위한 항목별 계획(스탯 비교, 페이즈 시스템, 공격 패턴 3종, 그로기 재설계, 사망/소환
연출, FSM, 리소스 매핑, 미해결 사항, 구현 순서)을 HTML 아티팩트로 작성해 전달했다. 현재
코드는 "쿨다운마다 원형 광역 공격 1종 → 자동 그로기(시간 기반)"인 반면 기획서는 "페이즈
2단계 + 패턴 3종(돌진 박치기/잔해 낙하/좀비 소환) + 피해 누적형 그로기 게이지"라 사실상
다른 보스다. 코드 조사로 확인한 핵심 불일치: 체력 1800(기획) vs 12000(현재 인스펙터값,
단일 패턴 기준 튜닝), 그로기 지속 5초·피해+200%(기획) vs 3초·+50%(현재), 잔해 낙하용
리소스(18+16프레임, 256px)는 이미 2026-08-21 무렵 전달돼 있었는데 코드에서 한 번도 참조된
적이 없었음. Panting(6프레임)·대시(14프레임)는 기획서 어느 패턴에도 명시적으로 매핑되지
않아 미해결 사항으로 남김.

### 신규/수정 파일

신규: `Assets/Scripts/ChargeWarningEffect.cs`, `ExplosionEffect.cs`, `ZombieHitEffect.cs`,
`RollDustEffect.cs`, `LevelUpEffect.cs`, `Assets/Prefebs/PrecisionBullet.prefab`.
수정: `MonsterAnimationLibrary.cs`(차저 항목 추가), `ChargerUnit.cs`(애니메이션 3종 재생
로직), `EnemyUnit.cs`(`BodySpriteRenderer` 접근자, `ResetStaticCaches` 목록, 피격 이펙트 호출),
`Projectile.cs`(`Explode()`를 `ExplosionEffect`로 교체, `ComputeBlastVisualScale()` 삭제),
`EnemyProjectile.cs`(전용 스프라이트로 교체), `PlayerRobotController.cs`(구르기 먼지 호출),
`AiCoreManager.cs`(레벨업 이펙트 호출), `Editor/WeaponTableGenerator.cs`(정밀화기 tanhwan
변경). 씬: `Ground01.unity`(`PlayerShootManager.projectile_prefabs`에 PrecisionBullet 등록).
리소스: `Assets/Resources/{ChargerMove,ChargerCharge,ChargerChargePrep,ChargeWarning,
Explosion,ZombieHitEffect,RollDust,LevelUpEffect,SpitterProjectile.png,PrecisionBullet.png,
BossGroggy(덮어씀)}`.

### 커밋 상태

미커밋. 사용자가 커밋을 지시하지 않아 커밋하지 않은 상태다.

## 2026-08-23 (이어서 5) 보스 "좀비 군집체" 전면 재구현 — 기획서 2종 반영

직전 항목에서 계획서만 작성하고 미뤄뒀던 보스 구현을 실제로 진행했다. 사용자 지시:
"지금 신규 리소스들 적용했고, 보스도 적용하면 거의 끝난것같다. **보스 대시는 돌진 경고 바닥에
표시되는거로 바꿨고**, 그에 맞춰서 적용해주면 돼. **Panting은 나도 뭔지 잘 모르겠음 일단 놔둬.**"

`좀비 애니메이션/좀비군집체/`를 다시 확인해보니 계획서 작성 시점에 있던 **`대시`(14프레임)
폴더가 `돌진 경고`(14프레임, 256px)로 교체**돼 있었다 — 계획서에서 "돌진 박치기와 관계 불명"으로
남겨뒀던 미해결 항목이 사용자 쪽에서 해소된 것이다. 실제 그림도 세로로 긴 화살표 레인(위쪽이
뾰족하고 아래쪽에 `!` 표시)이라 바닥 텔레그래프가 맞다.

### 리소스 임포트 (신규 8폴더 + 배너 1장)

| 원본 | 프레임 | Resources 폴더 | 캔버스 |
| --- | --- | --- | --- |
| `좀비군집체/roar` | 36 | `BossRoar` | 800px |
| `좀비군집체/보스 소환 이펙트` | 99 | `BossSummon` | 800px |
| `좀비군집체/보스 돌진` | 120 | `BossCharge` | 800px |
| `좀비군집체/보스 사망` | 16 | `BossDeath` | 800px |
| `좀비군집체/보스 사망 폭발 이펙트` | 60 | `BossDeathExplosion` | 800px |
| `좀비군집체/돌진 경고` | 14 | `BossChargeWarning` | 256px |
| `좀비군집체/잔해 낙하` | 18 | `BossDebrisFall` | 256px |
| `좀비군집체/잔해 낙하 주의` | 16 | `BossDebrisWarning` | 256px |
| `좀비 이펙트/보스등장알림.png` | 1 | `BossIncomingBanner.png` | 1024x288 |

전부 PPU 100으로 임포트했고, 800px 세트는 자동으로 **8유닛**이 되어 기존
`BossMove`(512px@PPU64)·`BossGroggy`(800px@PPU100)와 월드 크기가 정확히 일치한다.

**중요 - 좌우 반전**: 신규 몸통 세트 3종(`BossCharge`/`BossDeath`/`BossRoar`)은 원본이
**오른쪽**을 보고 있었다. 이 프로젝트는 `EnemyUnit.LateUpdate`가 `flipX = 이동속도.x > 0`으로
뒤집으므로 **아트는 왼쪽을 봐야 한다**(`BossMove`도 2026-08-20 임포트 때 반전한 것). 그래서
PNG 픽셀 자체를 좌우 반전해서 넣었다. 이 과정에서 **기존 `BossGroggy`(2026-08-21 적용분)가
오른쪽을 보고 있어 그로기에 들어가는 순간 보스가 갑자기 반대편을 보던 불일치**를 발견해
함께 반전했다(사용자 리포트는 없었지만, 이번에 몸통 세트가 4종으로 늘어나 방치하면 패턴마다
방향이 튀는 상태가 된다).

바닥 이펙트 2종(`BossChargeWarning`/`BossDebrisFall`)은 **피벗을 아래-중앙(0.5, 0)으로 재설정**
했다. 두 그림 모두 "배치 좌표에서 위로 자라는" 구도(레인은 보스 발밑 → 돌진 끝점, 잔해는
착탄 지점 → 위로 쌓인 더미)라, 피벗만 옮기면 배치 좌표가 곧 시작점/착탄점이 되어 코드가 단순해진다.

### 신규 스크립트

- **`BossFrameEffect.cs`** — 보스 전용 프레임 이펙트 5종(소환진 99 / 사망 폭발 60 / 잔해 낙하 18 /
  잔해 경고 16 / 돌진 경고 14)의 공용 재생기. 이 프로젝트의 관례는 "이펙트 하나 = 클래스 하나"
  (`MuzzleFlashEffect`/`ChargeWarningEffect`/`ExplosionEffect` 등)였지만, 보스 하나에만 성격이
  똑같은 이펙트가 5종 붙어서 같은 코드를 다섯 번 복사하는 대신 **폴더명 + 크기 + 지속시간만
  받는 재생기 하나**로 묶었다. 기존 이펙트 클래스들은 각자 고유 인자(판정 반경 역산, 부모 추적)가
  있어 그대로 뒀다. 정비 화면 자멸·정적 캐시·`ResetCache()`는 기존 관례 그대로 따른다.
  가로/세로를 따로 줄 수 있어(돌진 레인은 폭과 길이가 다르다) 비균등 스케일도 지원한다.

### `BossUnit.cs` 전면 재작성

기존 구조(광역 공격 1종 + 시간 기반 자동 그로기)를 **행동 하나 = 코루틴 하나**(`BossAction`
enum + `action_routine` 한 자리)로 갈아엎었다. 진행 중인 행동이 항상 한 자리에만 있으므로
그로기/페이즈 전환/사망이 `CancelAction()` 한 번으로 무엇이든 확실히 끊을 수 있다 —
예전 구조에는 캔슬 대상 패턴이 하나뿐이라 캔슬 개념 자체가 없었다.

**① 돌진 박치기** (기획서: 사거리 350 · 쿨타임 10.5초, 페이즈 1·2 공통)
예비 동작(돌진 프레임 001~033) 동안 **바닥에 경고 레인**을 깔고, 그 방향으로 직선 돌진
(프레임 034~070 순환) → 접촉 시 근접 피해(`MeleeAttackPlayer` - 가시 플레이트 반사 대상) →
기립(071~120). 방향은 예비 동작 시작 시 한 번만 정해 고정하고, 대신 "시작 시점 플레이어 거리 +
오버슈트 2유닛"을 상한으로 둔다(차저/스프린터에서 2026-08-12에 확정한 것과 같은 규칙).
경고 레인은 스프라이트 +y가 진행 방향이 되도록 `atan2 - 90도` 회전시키고, 세로 스케일에
돌진 거리를 그대로 넣어 **레인 길이 = 실제 돌진 거리**가 되게 했다.

**② 잔해 낙하** (사거리 500 이내 · 쿨타임 7초 · 7곳)
플레이어 발밑 1곳 + 주변 6곳에 경고 서클을 깔고, 예고가 끝나는 순간 잔해가 떨어져 반경 안의
플레이어에게 고정 피해를 준다. 낙하 이펙트 18프레임 중 **착탄 프레임(5/18)이 판정 순간과
겹치도록** 낙하를 판정보다 그만큼 먼저 띄운다. 보스 전용 시전 모션 아트가 없어 다른 몬스터의
예비 동작과 같은 관례대로 몸 색을 주황으로 물들여 대신한다.

**③ 좀비 소환** (페이즈 2 전용 · 쿨타임 15초 · 8마리)
포효 모션(36프레임) 후 보스 주위 원형으로 8마리를 소환한다. **`EnemySpawner.SpawnMonsterTracked()`
를 새로 만들어 쓴다** — 기존 `SpawnMonster()`로 만들면 스포너 생존 목록에 등록되지 않아
웨이브 종료 시 `DespawnAllAliveEnemies()`가 못 지우고 정비 화면 뒤·다음 웨이브까지 남는다.
리더(200006)는 웨이브당 등장 수 제한 기믹이 따로 있어 소환 후보에서 뺐다.

**그로기 재설계** — 트리거를 시간 기반에서 **피해 누적 게이지**로 교체했다. 기획서는 "게이지
100"이라고만 적혀 있고 피해 → 게이지 환산이 없어, `groggyGaugeDamageRatio`(기본 0.12 =
최대 체력의 12%를 넣을 때마다 만충 → 한 판에 약 8번)로 두고 인스펙터에서 조정하게 했다.
게이지는 **그로기 배율이 곱해지기 전 원본 피해**로 쌓는다(증폭된 피해로 쌓으면 "그로기라서
더 아프고 → 그래서 그로기가 더 빨리 온다"는 순환이 생긴다). 만충 시 진행 중이던 패턴을 즉시
캔슬한다. 지속 3초 → **5초**, 피해 배율 x1.5 → **x3**(기획서 지정값).

**페이즈 전환** — 체력 50%에 최초로 닿는 순간 1회. 판정은 `TakeDamage` 안에서 체력 갱신 직후에
한다(별도 폴링보다 데미지 프레임에서 확정적으로 잡힌다). 진행 패턴 캔슬 → **완전 무적 +
행동정지 5초** → 무적 해제 + 페이즈 2. 전용 전환 아트가 없어 등장 연출과 같은 소환진 이펙트를
몸 위에 겹쳐 "재구성"으로 읽히게 하고 앞부분에 포효를 얹었다. 페이즈 2("폭주")는 기획서에
수치 명세가 없어 **이동속도 x1.25 / 패턴 쿨타임 x0.8**을 임시값으로 넣었다.
`EnemyUnit.MoveSpeed`의 세터를 `private set` → **`protected set`**으로 열었다(이 용도 전용).

**사망 연출** — `Die()`를 오버라이드해 사망 모션(16프레임) → 폭발 이펙트(60프레임) → 그제서야
`base.Die()`(보상 지급 + 파괴) → **`OnDefeated` 발행** 순서로 바꿨다. 예전에는 `OnDefeated`가
즉시 발행돼서, 연출을 넣으면 사망 애니메이션이 재생되는 동안 이미 승리 화면이 뜬다.
연출 중에는 자동 조준 대상(`EnemyUnit.Alive`)에서 빼고 `TakeDamage`도 막아 "죽은 몸을 계속
때리는" 상태를 없앴다.

**등장(소환) 연출** — `WaveManager.SpawnBoss()`가 `Init()` 직후 `boss.PlaySpawnIntro()`를 부른다.
소환진 99프레임이 도는 동안 보스는 완전 무적 + 본체 스프라이트 숨김이고, **한가운데서 크게
번쩍하는 프레임(49/99)에 본체가 드러나** 포효까지 마쳐야 전투가 시작된다.
무적 구간에는 `Alive`에서도 빠져 플레이어가 안 맞는 표적에 총알을 쏟지 않는다.

**BOSS INCOMING 배너** — `WaveTransitionBannerUI`가 보스 웨이브에서 텍스트 리본 대신
사용자 제공 아트(`BossIncomingBanner.png`)를 비율 그대로 띄운다(문구가 그림에 이미 있어
카드 배경·리본·텍스트를 전부 끈다). 에셋이 없으면 예전 텍스트 배너로 폴백한다.

### 함정 2개

- **`CancelAction()`은 `action_routine` 하나만 멈춰선 부족하다.** 베이스의 근접 공격 모션
  (`EnemyUnit.PerformAttackMotion`)이 돌던 중에 그로기가 들어오면 그 코루틴이 살아남아, 나중에
  `RestoreAttackVisual()`로 `IsAttacking`을 꺼버리고 스프라이트를 되돌려 그로기 연출과 싸운다.
  `StopAllCoroutines()`로 이 오브젝트의 코루틴을 전부 정리한다(돌진 프레임 루프도 별도 코루틴이라
  같이 정리돼야 한다).
- **바닥 이펙트는 `sortingOrder 0` + `z = -1.5`로 깐다.** 배경(`map`)도 `sortingOrder 0`이라
  같은 순서에서는 카메라와의 거리로 순서가 갈리는데, 배경이 `z = -1`이라 `z = 0`에 두면 배경이
  덮어버린다. 정렬 순서가 거리보다 우선하므로 `0` + 더 가까운 z면 **배경 위 · 모든 캐릭터
  (1 이상) 아래**가 확정된다.

### 검증 (플레이모드 실측)

`Ground01`에서 `WaveManager.SpawnBoss()`를 리플렉션으로 직접 호출해 **실제 보스 웨이브 경로**를
그대로 태우고, `Time.timeScale`을 0.03~0.25로 낮춰(MCP 왕복 지연보다 연출이 느리게) 단계마다
게임뷰를 캡처했다.

- **등장 연출**: 본체 렌더러 `enabled=False`, `Alive` 미포함, 소환진 이펙트 1개 재생 확인
- **돌진**: 예비 동작에서 몸 프레임 `frame_023`(윈드업 구간) + 바닥 레인이 보스 → 플레이어
  방향으로 정확히 회전·연장된 것을 캡처. 돌진 중 `is_dashing=True` / `frame_067`(돌진 구간),
  접촉 시 플레이어 체력 20 감소(보스 공격력 25 − 방어력 5) 확인
- **잔해 낙하**: 경고 서클 7개가 플레이어 주변 바닥에 깔리고(캐릭터 아래·배경 위) 착탄 시
  플레이어 체력 15 감소(고정 20 − 방어력 5) 확인. 잔해 더미가 서클 위쪽에만 얹혀 보여
  `debrisFallDropOffset`(-0.6유닛)을 추가해 서클 안에 앉게 보정
- **그로기**: 플레이어 자동사격만으로 게이지가 0 → 0.97까지 차오르고 만충 시 자동 진입.
  그로기 중 피해가 정확히 **x3**(1040 입력 → 3115 표시) 적용되는 것을 데미지 팝업으로 확인
- **페이즈 전환**: 체력 45%로 떨어뜨리자 `phase 1 → 2`, `action=PhaseChange`, `invul=True`,
  `Alive` 미포함으로 전환. **무적 검증 - 전환 중 1000 피해를 넣어도 체력 3605 그대로**
- **페이즈 2**: `MoveSpeed 1.3 → 1.625`(x1.25 정확히 일치), 소환 8마리(좀비3/차저2/스프린터2/
  스피터1 - 종류 랜덤), **스포너 `alive_enemies`에 8마리 전부 등록**된 것 확인
- **사망**: 킬 직후 `is_dying=True` / `IsDead=False` / `WaveManager.boss_defeated_this_wave=False`
  (= 승리 판정이 아직 안 났다) → 사망 모션 → 폭발 60프레임 → 오브젝트 파괴 후에야
  `boss_defeated_this_wave=True`로 바뀌는 **순서**를 확인
- **BOSS INCOMING 배너**: 아트가 비율 그대로 화면 중앙에 뜨는 것 확인
- 컴파일 경고 0건(보스 관련), 전 과정 보스 코드 예외 0건

### 신규/수정 파일

신규: `Assets/Scripts/BossFrameEffect.cs`.
수정: `BossUnit.cs`(전면 재작성), `EnemyUnit.cs`(`MoveSpeed` 세터 protected + 정적 캐시 목록에
`BossFrameEffect`/`BossUnit` 추가), `EnemySpawner.cs`(`SpawnMonsterTracked()` 신설),
`WaveManager.cs`(`PlaySpawnIntro()` 호출), `WaveTransitionBannerUI.cs`(보스 배너 아트),
`Editor/WeaponTableGenerator.cs`(폭발 이펙트 관련 낡은 주석 정정).
리소스: `Assets/Resources/{BossRoar, BossSummon, BossCharge, BossDeath, BossDeathExplosion,
BossChargeWarning, BossDebrisFall, BossDebrisWarning, BossIncomingBanner.png}` 신규,
`BossGroggy` 좌우 반전 재적용.

### 남은 것

- **스탯 재조정** — 기획서 수치(체력 1800 / 공격력 75 / 방어력 14)는 그대로 대입하지 않았다.
  현재 씬 값은 체력 8000 / 공격력 25 / 방어력 5로, 단일 패턴 시절에 맞춰 실측 튜닝된 값이다.
  패턴이 3종으로 늘고 그로기가 피해 누적형이 된 지금은 플레이모드 실측으로 다시 잡아야 한다.
- **`Panting`(6프레임)** — 사용자 지시로 미적용.
- **그로기 게이지 UI** — 기획서에 명세가 없어 만들지 않았다(연출은 별 이펙트 + 그로기 프레임으로
  읽힌다).

### 산출 문서 (2026-08-23)

- **좀비 군집체 적용 계획서 (구현 결과로 갱신)** — https://claude.ai/code/artifact/2a97ee2b-995a-440f-b67b-6c28517ed124
  기존 계획서를 "설계 전용 · 코드 미반영"에서 **구현 완료 상태**로 전면 갱신했다. 항목별
  계획 → 실제 구현 차이, 구현 중 정한 값(그로기 게이지 환산 비율 등), 함정 2건, 플레이모드
  검증 결과 표를 추가했다. 계획서의 미해결 6건 중 4건 해소(대시 → 돌진 경고 확정 / 사거리
  ÷100 환산 확정 / FSM 표기 오류 판단 유지 / 리소스 표 확정), 남은 것은 Panting 용도와
  페이즈 2 폭주 수치.
- **미적용·임시 리소스 명세** — https://claude.ai/code/artifact/e2113274-6bb7-4023-a434-693bc79ec06f
  사용자 요청("아직 적용되지 않은 리소스나 임시로 적용된 리소스들 뭔지 명세 목록 뽑아서 html
  파일 보내놔")으로 작성. 파일명 문자열 + `.meta` GUID를 스크립트·씬·프리팹·데이터 에셋 전체에서
  역추적해 판정했다. 주요 확정 사항:
  - **`좀비 이펙트/boomer_boom_ani00~07.png`의 정체가 해시 대조로 확정됐다** — 이미 적용된
    `Resources/DisruptorExplosion`과 **8프레임 전부 바이트 동일**한 중복 전달본이다(지난 세션에
    "어느 몬스터인지 근거가 없다"고 남겨둔 항목).
  - 몬스터 정지 스프라이트 4장(`Spitter`/`Sprinter`/`Disruptor`/`Leader`.png)은 2026-08-20
    이동 애니메이션 도입 때 프리팹이 각 세트의 0번 프레임을 쓰도록 바뀌면서 **고아가 됐다**.
  - `map.png`는 이미지 자체가 미사용이다(씬 오브젝트 이름 `map`은 `MapBounds`가 쓰지만, 실제
    타일 네 장은 `ground_ruined_city_v2_tile.png`).
  - `Resources/UI` 51장 중 26장 미참조 — 대부분 색·번호 변형 세트에서 하나만 고른 결과라
    누락이 아니지만, **`Exp_icon`/`Gold_icon00`/`Gold_icon01` 3장만은 변형이 아니라
    "전용 아이콘인데 안 쓰이는" 상태**라 확인이 필요하다.
  - **임시 대체 9건** — 효과음 전량(`Resources/SFX` 폴더 자체가 없어 절차 생성 비프음),
    파츠 아이콘 15종(156행 중 31행이 코드 생성 도형 - 다리 4종 포함), 몬스터 4종 공격 모션,
    보스 잔해 낙하 시전 모션, 보스 페이즈 전환 이펙트(소환진 재활용), 적 체력바, 부품 상자
    화살표, UI 아이콘 3종(자물쇠/도장/설정), 치명타 아이콘.
