# 2026-08-20 작업 정리

이 문서는 오늘 세션에서 진행한 모든 변경사항을 한 번에 훑어볼 수 있도록 정리한 것이다.
각 항목의 자세한 배경·시행착오는 [작업.md](작업.md)의 해당 날짜 항목을 참고.

---

## 1. Firebase Realtime Database 랭킹 연동

- SDK(EDM4U/Gradle) 없이 `UnityWebRequest` 기반 REST 방식으로 구현 — PC/Android/iOS 어디서든
  동일 코드로 동작.
- 신규: [FirebaseLeaderboardService.cs](Assets/Scripts/FirebaseLeaderboardService.cs)
- [LeaderboardService.cs](Assets/Scripts/LeaderboardService.cs) — 기본 구현체를 로컬 저장에서
  Firebase로 교체(문제 생기면 한 줄만 되돌리면 로컬로 폴백).
- Firebase 콘솔에서 프로젝트(`comstock-d3868`) + Realtime Database(싱가포르 리전) 생성.
  `Packages/manifest.json`에 `com.unity.nuget.newtonsoft-json` 패키지 추가(동적 키 JSON 파싱용).
- 실제 라이브 DB에 제출→조회→삭제 왕복 검증 완료.

## 2. 랭킹 조회 화면 신설 (그동안 제출만 있고 "보는" 화면이 없었음)

- 신규: [RankingPanelUI.cs](Assets/Scripts/RankingPanelUI.cs) — 상위 20위를 보여주는 팝업.
- 여는 곳 3군데:
  - 일시정지 메뉴 "랭킹" 버튼 활성화 — [PauseMenuUI.cs](Assets/Scripts/PauseMenuUI.cs)
  - 게임오버 요약 화면에 신규 버튼 추가 — [GameOverSummaryUI.cs](Assets/Scripts/GameOverSummaryUI.cs)
    (씬: `Ground01.unity`에 `RankingButton` 오브젝트 추가)
  - 타이틀 화면 우하단 신규 "랭킹" 버튼 — [TitleSceneManager.cs](Assets/Scripts/TitleSceneManager.cs)
- 타이틀 화면 하단의 음악 볼륨 슬라이더 → **"설정" 버튼**으로 교체(기존 `SettingsPanelUI` 재사용,
  볼륨 조절은 그 안으로 이동).

## 3. 랭킹 맵별 분리 ("앞으로 맵 여러개 추가할거임")

- `ILeaderboardService.SubmitScore`/`FetchTopScores`에 `mapId` 매개변수 추가.
- **mapId = 씬 이름을 그대로 사용** — 별도 맵 카탈로그 없이, 새 맵 씬을 추가해도 랭킹 코드는
  수정할 필요 없음.
  - 일시정지/게임오버: 활성 씬(플레이 중인 맵)
  - 타이틀: `TitleSceneManager.nextSceneName`(다음에 플레이할 맵)
- Firebase 저장 경로: `leaderboard/{pushId}` → `leaderboard/{mapId}/{pushId}`로 변경.
- Firebase 규칙도 `$mapId` 와일드카드 구조로 재구성(맵이 늘어나도 규칙 재게시 불필요).
- 랭킹 화면 제목에 `랭킹 - Ground01`처럼 맵 이름 표시.
- 검증: 가상 맵 2개에 점수를 넣고 서로 안 섞이는 것 확인.

> 남은 것: 맵이 실제로 2개 이상이 되면 타이틀에 **맵 선택 UI**가 필요함(지금은 맵이 1개뿐이라
> 미리 만들지 않음).

## 4. 기본점수 1200점 버그 수정

- **문제**: `RunScore.ComputeBreakdown()`이 `WaveNumber`(웨이브 1 시작 시 1)와 `CoreLevel`
  (기본값 1)을 "진행도"가 아니라 "현재 값" 그대로 곱해서, 런을 시작하자마자
  1×1000 + 1×200 = **1200점**이 조건 없이 붙어 있었음.
- **수정**: [RunScore.cs](Assets/Scripts/RunScore.cs) — 둘 다 1을 뺀 값(실제로 "클리어한" 웨이브 수 /
  "레벨업한" 횟수)을 곱하도록 변경. [ScoreSummaryPopup.cs](Assets/Scripts/ScoreSummaryPopup.cs)의
  내역 표시 문구도 같은 식으로 맞춤.
- **검증**: 웨이브1/코어레벨1(시작 직후) → 총점 **0**, 웨이브3/코어레벨4(2웨이브 클리어+3회
  레벨업) → 정확히 **2600점**(2000+600).

## 5. 디스럭터(자폭 좀비) - 즉시 폭발 대신 2초 지연

- **요청**: "폭발하기 직전에 깜빡거리면서 2초뒤에 터지게, 이때는 무적 상태로 죽지 않게".
- [DisruptorUnit.cs](Assets/Scripts/DisruptorUnit.cs) 재작성 — 자폭 계기(큰 피해 / 사거리 안
  도달)가 오면 즉시 터지지 않고 2초 코루틴을 돈다: 몸 색이 원래색↔빨강으로 0.15초 간격으로
  깜빡이다가 끝에 실제로 폭발. **심지가 붙은 동안은 `TakeDamage`가 아무 것도 안 해서 완전
  무적**이며, 중복 트리거는 `fuseStarted` 가드로 막는다.
- **검증**: 큰 피해 후 안 죽음 → 그 상태에서 999999 피해를 더 줘도 여전히 안 죽음(무적 확인) →
  이후 실제로 폭발(오브젝트 파괴) 확인.

## 6. 랭킹 등록 시 닉네임 입력

- **요청**: "게임 끝나서 랭킹 등록할때 닉네임 넣는 기능도 만드셈".
- 신규: [NicknameInputPopup.cs](Assets/Scripts/NicknameInputPopup.cs) — 입력창은 **로봇 이름
  (또는 마지막으로 쓴 닉네임)으로 미리 채워져 있어 확인만 눌러도 예전과 동일하게 동작**한다.
  확정한 닉네임은 PlayerPrefs(`comstock_nickname`)에 저장해 다음 판에도 이어서 씀.
- `RunScore.SubmitToLeaderboard()`가 `playerName` 인자를 받도록 변경, 제출이 일어나는 3곳
  (게임오버 요약 화면 / 일시정지 메뉴 "나가기" / 정산 팝업 "타이틀로") 모두 닉네임 팝업을 거치게 함.
- **검증**: 기본값 프리필 확인 → 공백 포함 문자열 입력 후 확인 → 트림된 문자열로 제출 +
  PlayerPrefs 저장 확인.

## 7. 타이틀 로고 이미지 교체

- 사용자가 제공한 `타이틀이미지.png`(2115×643, 가로로 넓은 로고 형태)를 적용.
- **시행착오**: 처음에 전체화면 배경(`titleimage.png`)에 그대로 덮어씌워 사용자에게 지적받음
  ("타이틀 이미지는 제목 이미지로 넣어야지 전체화면으로 늘려서 넣으면 어떡하냐") → 배경 이미지는
  git으로 원상복구.
- 새 이미지는 `Assets/Resources/UI/title_logo.png`로 별도 추가(Sprite 임포트 설정 적용),
  기존 텍스트 제목(`TitleText`/`TitleText_BG`) 자리에 **비율을 유지하는 로고 이미지**로 교체
  (신규 `TitleLogo` 오브젝트, 앵커 비율 기반 배치 — 고정 픽셀 크기를 쓰면 해상도마다 깨지므로
  회피). 텍스트 오브젝트는 지우지 않고 비활성화만 함.

## 8. 빅토리 화면 - 화면을 가리는 암막 제거

- **요청**: "빅토리 뜨면서 화면 가리는거 없애".
- 20웨이브를 처음 클리어하면 뜨는 `ScoreSummaryPopup`(점수 내역 + 계속/타이틀로 선택)에
  화면 전체를 75% 검게 덮는 `DimBackground`가 있어서 승리 직후 필드(쓰러진 보스·HUD·"Victory"
  문구)가 통째로 가려졌음. [ScoreSummaryPopup.cs](Assets/Scripts/ScoreSummaryPopup.cs)에서
  이 암막 생성 코드를 제거 — 점수 내역 패널과 버튼만 그 위에 뜨고 게임 화면은 그대로 보임.

---

## 9. 파츠 25종 전면 교체 (헬멧 6 / 다리장갑 4 / 무기소켓 7 / 메모리 3 / 디스크슬롯 5)

- 사용자 제공 명세 2종 + 아이콘 아트 25장(`새파츠들/`)을 반영. **헬멧 = 머리부 장갑의 새 명칭**.
- 신규 [PartsTableGenerator.cs](Assets/Scripts/Editor/PartsTableGenerator.cs) —
  메뉴 `Comstock/파츠 테이블 25종 재생성` / `Comstock/파츠 테이블 검증`.
  무기·디스크 테이블과 같은 방식이며, **명세 5개 슬롯은 앞으로 이 생성기로만 고친다**.
- `PartsCatalog.asset` 파츠 **103개 → 173개**(임시 루트 파츠 55개 제거, 25종 x 5등급 125개 추가).
  기본 장착 "표준 OO" 파츠와 명세 없는 슬롯(팔 장갑 / 자기장 코어 / 다리)은 그대로 유지.
- 등급 수치는 배율 없이 **명세의 5개 값을 그대로** 입력(예: 행운 +2/2.5/3/4/5).
- 신규 [PartEffect.cs](Assets/Scripts/PartEffect.cs) / [PartEffects.cs](Assets/Scripts/PartEffects.cs) —
  파생 방어력(연극 마스크·철인 헬멧) / 방어력 ％증가(방탄모) / 조건부 방어력(탈) /
  근접 반사(가시 플레이트) / 코어 시작 레벨(뉴럴 캐시) / 디스크 비례(확장 프레임 등)를 구현.
- **무기 소켓 전면 교체**: 제한축이 무기 타입(경/중/근접) → **무기 카테고리**(연사/산탄/정밀/폭발/
  에너지/근접 + 범용)로, 등급 효과가 사거리·감지·회전 배율 → **공격속도·공격력·치명타·스플래시·
  방어력무시**로 바뀌었다. 카테고리 불일치면 보정 0 + 무게 x2(장착은 계속 허용).
- **파츠 아이콘이 파츠별로** 바뀌었다(`PartData.iconName` → `Assets/Resources/PartIcons/`).
  25장을 영문명으로 복사하고 임포트 설정을 Sprite(2D and UI)로 맞췄다.

## 10. 스탯 표시 소수점 (사용자 요청: "소수점 아래 숫자 있으면 그것도 표시하게")

- **원인**: 표시 서식만의 문제가 아니라 `RobotStats.ApplyBonus`가 `Mathf.RoundToInt`로 **보너스를
  개별 반올림**해서 `공격력 +0.3` 같은 소수가 계산 단계에서 통째로 사라지고 있었다.
- 사용자 지시("전부 소수화 해라 안짤리게")에 따라 **플레이어·적의 체력/공격력/방어력과 투사체·빔·
  근접 데미지를 전 구간 float로** 바꿨다. 표시는 `0.##`(정수일 때는 소수점 없음).
- 실측: 정비 화면에 `방어력 7.78` `회피율 7.4` `행운 2.5`, 적 피해 `9.625`가 그대로 표시·적용된다.

## 11. UI 골드 아이콘을 인게임 금화로 통일 (사용자 지적)

- 필드 드랍은 `Resources/Gold.png`(기계 금화), UI는 `Resources/UI/Gold_icon00.png`(별 코인)를 쓰고 있었다.
- UI 쪽 6곳(코드 2 + `Ground01.unity` 씬 직렬화 4)을 `Gold`로 교체. **이미지 파일은 덮어쓰지 않았다.**
- 안 쓰게 된 `UI/Gold_icon00`·`Gold_icon01`은 관례대로 남겨 뒀다.

## 12. 누적형 디스크의 초록 글씨를 "현재 누적 총량"으로 (사용자 지적)

- 교향곡: 화염 디스크 아래 초록 `공격력 +0.05`는 **처치 1마리당 증가량**을 그대로 찍은 것이었다.
- [ShopPanelUI.cs](Assets/Scripts/ShopPanelUI.cs)의 `AppendDiscStat`이
  `RunState.DiscStackProgress`(누적치)를 읽어 **`공격력 +4.5 (처치당 +0.05 · 최대 +10)`** 로 출력한다.

---

## 13. 한글 폰트 아틀라스 글리프 누락 수정 (에디터 재활성화 후 재검증 중 발견)

- FHD 캡처로 이전 작업을 재검증하다가 "상점"→"점", "(클릭 = 상세)"→"(클릭 = 세)" 처럼
  특정 글자가 자리는 잡히고 그림만 안 나오는 현상을 발견.
- 원인: `NotoSansKR-Regular SDF`(Dynamic 폰트)의 아틀라스가 1024x1024 한 장을 넘겨 2번째
  텍스처로 넘어간 상태로 저장돼 있어 일부 글리프가 텍스처 없이 기록만 남아 있었다.
- 신규 [FontAtlasMaintenance.cs](Assets/Scripts/Editor/FontAtlasMaintenance.cs) -
  메뉴 `Comstock/한글 폰트 아틀라스 초기화 (2048)`. 아틀라스 2048로 확장 + 동적 데이터 초기화.
- 상점/정비 화면 재캡처로 정상 표시 확인. 상점의 "정비 종료" 버튼과 점수가 겹쳐 보이던 것은
  실제 게임 흐름(`GameFlowManager`)으로 재현되지 않았다 - 검증 스크립트가 상태 전환을 건너뛴
  허상이었다(코드 수정 없음).

---

## 변경 파일 요약

**신규**
- `Assets/Scripts/FirebaseLeaderboardService.cs`
- `Assets/Scripts/RankingPanelUI.cs`
- `Assets/Scripts/NicknameInputPopup.cs`
- `Assets/Resources/UI/title_logo.png` (+ `.meta`)

**수정**
- `Assets/Scripts/LeaderboardService.cs`
- `Assets/Scripts/RunScore.cs`
- `Assets/Scripts/ScoreSummaryPopup.cs`
- `Assets/Scripts/DisruptorUnit.cs`
- `Assets/Scripts/PauseMenuUI.cs`
- `Assets/Scripts/GameOverSummaryUI.cs`
- `Assets/Scripts/GameFlowManager.cs`
- `Assets/Scripts/TitleSceneManager.cs`
- `Packages/manifest.json` / `Packages/packages-lock.json`
- `Assets/Scenes/Ground01.unity` (RankingButton 오브젝트 추가 + 참조 연결)
- `Assets/Scenes/Title.unity` (TitleLogo 오브젝트 추가, TitleText/TitleText_BG 비활성화)

**2026-08-20 오후 추가분 (파츠 25종 / 스탯 소수화 / 골드 아이콘 / 디스크 표시)**

신규
- `Assets/Scripts/PartEffect.cs`, `Assets/Scripts/PartEffects.cs`
- `Assets/Scripts/Editor/PartsTableGenerator.cs`
- `Assets/Resources/PartIcons/` 아이콘 25장 (+`.meta`)

수정
- 파츠/스탯: `PartData.cs`, `PartIconLibrary.cs`, `ModdingManager.cs`, `RobotStats.cs`, `StatType.cs`,
  `DiscData.cs`, `RunState.cs`, `AiCoreManager.cs`, `ShopManager.cs`, `HeadEffects.cs`,
  `DiscEffectRuntime.cs`, `RewardPickup.cs`, `UnlockTracker.cs`
- 전투 소수화: `PlayerRobotController.cs`, `PlayerShootManager.cs`, `EnemyUnit.cs`, `EnemySpawner.cs`,
  `ChargerUnit.cs`, `SprinterUnit.cs`, `DisruptorUnit.cs`, `Projectile.cs`, `BeamProjectile.cs`,
  `MeleeSwing.cs`, `EnemyProjectile.cs`, `PlayerHitFeedback.cs`
- UI: `ModdingPanelUI.cs`, `ShopPanelUI.cs`, `GameHUD.cs`, `GameOverSummaryUI.cs`, `AiCoreExtraButtonsUI.cs`
- 데이터/씬: `Assets/Data/PartsCatalog.asset`, `Assets/Scenes/Ground01.unity`(골드 아이콘 4곳)

**커밋 상태**: 전부 미커밋.
